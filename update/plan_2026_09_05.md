# Codex Go update alignment plan - 2026-09-05 (sync30: 271-commit upstream range)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust previous HEAD (sync29 last applied): `28327355b8` (#41630, test-only)
- Rust new HEAD (sync30): `574a36ff99` (#42883, "Add client-side exec-server RPC attempt metrics")
- `git pull origin main` advanced `28327355b8..574a36ff99` (271 commits, 1588 files, +100290/-16836).
- Go baseline commit: `337e305` (sync29 landed, `docs: rewrite mantest.md in Chinese`).
- Certified baseline (`parity.json` / `candidateRustTo`): unchanged at `970b7f2ff4` (#40031, sync26).

## L0 static re-freeze - DONE (this pass)

The upstream range adds new crates, schema surfaces, feature keys, config
keys, model-metadata fields, TUI snapshots, and sandbox tests. The L0 static
layer was re-pinned against `574a36ff99` and `go test ./parity -count=1` is
green:

- `features/features.go`: added 6 new Rust feature keys with matching stage/defaults:
  `unified_exec_tty` (Stable, on, #42718), `worktrees` (Experimental, #42196),
  `windows_sandbox_service` (UnderDevelopment, #42353),
  `mcp_oauth_refresh_coordination` (UnderDevelopment, #42413),
  `guardian_thread_context` (UnderDevelopment, #42529),
  `context_management` (UnderDevelopment, #42385).
- `config/config.go`: recognized `allow_symlinked_codex_home` (#42716) and
  `thread_unload_delay_secs` (#42320) with accessors
  `AllowSymlinkedCodexHome()` and `ThreadUnloadDelaySecs()` (default 60s).
- `model/catalog.go`: added `ModelInfo.Guardian` (`guardian`, #42744) plus
  `GuardianModelPolicy` / `GuardianReviewMode` types.
- `appserver/schema/precomputed/*.zst`: re-vendored byte-identical to Rust
  (stable 143884 -> 148121, experimental 148284 -> 151703).
- `parity/rust_snapshot_test.go`: workspace members 138 -> 146 (8 new crates),
  critical-file SHA256 re-pinned for 6 changed files.
- `parity/rust_inventory_test.go`: top-level directory inventory 107 -> 114
  (7 new crates).
- `parity/rust_fixture_manifest_test.go`: fixture-root counts re-pinned
  (cli/tests 21->23, exec/tests 20->21, exec/tests/suite 17->18,
  app-server/tests/suite/v2 119->127, core/tests/suite 177->185,
  chatwidget/tests 39->59, chatwidget/tests/snapshots 14->27, tools/src 31->36).
- `parity/rust_tui_snapshot_manifest_test.go`: TUI snapshot total 722 -> 832;
  per-dir counts refreshed and 3 new snapshot dirs registered
  (`bottom_pane/chat_composer`, `bottom_pane/tests`, `custom_terminal/tests`).
- `parity/rust_surface_drift_test.go`: ConfigToml top-level field count 99 -> 101.
- `parity/rust_enum_surface_test.go`: ModelInfo wire field count 42 -> 43.
- `parity/rust_unified_exec_sandbox_manifest_test.go`: added 4 new sandbox
  test names across `unified_exec.rs`, `windows_sandbox.rs`, and
  `unified_exec_tests.rs`.
- `parity/config_schema_surface_test.go`: config.schema.json top-level property
  count 96 -> 98, Go recognized count 102 -> 104.

Verification: `go build ./...` clean; `go test ./parity ./features ./config ./model -count=1` green.

## sync30 runtime roadmap (work queue)

Each batch is `pending` until its runtime behavior and verifier land. Entries
are ordered by Go-side value/feasibility. Upstream commits that are Rust-internal,
or tied to subsystems the Go port does not implement (voice-host, Windows
sandbox service, native MXC adapter, WebRTC, Bazel, Python packaging), are
`rust-coupled` and inherit via the Rust components rather than ported.

### batch-schema-v2-sync30
- `728cb12fe5` #42458 Expose thread originators through the app-server API
  - **status: done (originator surface + list filter)** - `Thread.Originator`
    (`*string`) added to the shared thread object and populated from
    `session.Metadata.Originator` in `BuildThread`; `ThreadListParams.Originators`
    added, and the local app-server rejects a nonempty list with
    `originator filtering is not supported by the local app-server`.
- `2b554fd3f9` #42386 Expose loaded thread environments in app-server responses
  - **status: done (surface + persisted selection)** - `ThreadEnvironment` type
    and nullable `Thread.Environments` added; `BuildThread` maps the persisted
    `runtime_environments` selection (id/cwd/runtimeWorkspaceRoots) into the
    field, emitting `null` when unloaded/absent.
- `986ff1cc7c` #42151 Expose model settings in app-server thread metadata
  - **status: done (surface + persisted latest + start precedence)** -
    `Thread.Model` (`*string`) and `Thread.ReasoningEffort` (`*ReasoningEffort`)
    added and populated from `session.Metadata.Model` and the persisted
    `model_reasoning_effort` config override; thread start applies the live
    configured model/reasoning effort onto the returned thread.
- `bfa9646787` #41949 Add plugin reconciliation app-server API
  - **status: done (protocol surface)** - `plugin/reconcile` method constant,
    client-request entry, and `PluginReconcileParams`/`PluginReconcileResponse`
    signatures added; app-server v2 suite manifest re-pinned with the 6 new
    Rust modules (`config_requirements_application`, `config_requirements_browser_use`,
    `luna_reserve`, `plugin_reconcile`, `thread_environments`, `turn_cost_otel`).
- `3921a30d6b` #42854 Persist Daybreak preferences in thread metadata
- `3b2d9a69e6` #42863 Preserve precedence across feature requirement aliases
  - **status: done** - `feature_requirements`/`featureRequirements` are
    normalized onto `features` per cloud requirements layer before merging,
    so mixed aliases share one merge path and retain layer precedence.
- `e017e93ace` #41980 Preserve raw response usage metadata
  - **status: done** - `ResponseUsageMetadata.Metadata` carries the raw
    `response.usage` object; SSE `response.completed` now captures non-null
    `usage` into `usage_metadata.metadata`, and `rawResponse/completed` emits it.
- `577a4fcd06` #42358 Extend rate limit reads with usage capabilities
  - **status: done (protocol surface)** - `account/rateLimits/read` TypeScript
    params corrected to `GetAccountRateLimitsParams` (stable JSON keeps the
    nullable `anyOf` reference), matching Rust fixtures.

### batch-config-requirements-sync30
- `b27a6321fa` #42417 Expose managed application network requirements
- `0f64d70808` #42823 Expose managed WebMCP policy through the app server
- `387bc6ba59` #42821 Report managed filesystem policy in `codex doctor`
- `0d502a4230` #42328 Support durable reasoning configuration updates
- `9d57be71ba` #42202 Separate TUI preferences from server configuration
- `e1d0ef995f` #42377 Make app-server realtime sessions always available
  - **status: done** - `realtime_conversation` is a removed compatibility
    setting; `realtimeStartOptions` no longer gates `thread/realtime/start` on
    the per-thread feature.

### batch-guardian-sync30
- `4e48cd02da` #42744 Honor model-provided Guardian review policies
  - **status: done (model layer)** - `ModelInfo.Guardian` / `GuardianModelPolicy` /
    `GuardianReviewMode` / `GuardianScope` added; `ComputerUseReviewRequired`
    consults the model-owned `guardian.computer_use` policy over the legacy
    `node_repl_auto_review_required` bit, and appserver's
    `nodeReplAutoReviewRequiredForModel` uses it.

### batch-config-tools-sync30
- `a9519cbcd` #41744 Make the update_plan tool opt-in
  - **status: done** - `Config.UpdatePlanEnabled()` now defaults to `false`
    (absent/tools-absent/update_plan-absent/`enabled`-missing all resolve false),
    so `update_plan` is registered only when `tools.update_plan.enabled` is
    explicitly true, mirroring Rust `resolve_update_plan_enabled`.
- `5e26f7621c` #42320 Make the app-server thread unload delay configurable
  - **status: done (config)** - `Config.ThreadUnloadDelaySecs()` accessor (default
    60s) added; app-server delayed-unload wiring remains a follow-up.
- `d13aeb77ea` #42716 Allow trusted symlinks beneath CODEX_HOME on macOS
  - **status: done (config)** - `Config.AllowSymlinkedCodexHome()` accessor added.
- `38ba8cdceb` #42422 Honor model requirements in Guardian computer-use scoring
- `a0dcfe2ada` #42256 Skip Guardian scoring in User approval mode
- `e576993911` #42147 Skip Guardian reviews in Full Access
- `0ae02915bd` #42807 Add request-scoped Guardian approval decisions
- `4636819a35` #42852 Harden Guardian reviews after context compaction
- Guardian history retention/compaction cluster (#41846-#42290, #42579, #42758,
  #42762, #42770, #42844, #42832, #42838, #42844, #42852)

### batch-mcp-oauth-sync30
- `88912c04cd` #42413 Enable coordinated MCP OAuth refresh
- `bd89ce67cc` #42128 Prepare MCP connections for coordinated OAuth refresh
- `0650d6d1ca` #42552 Preserve MCP authentication challenges on tool calls
- `8f31b64c7f` #42598 Report MCP tool discovery errors in server status

### batch-exec-sync30
- `3c837e568c` #42718 Gate unified exec TTY support behind a feature flag
  - **status: done** - `ShellExecutorOptions.AllowTTY` (nil defaults true) plus
    `ShellExecutor.allowTTY`; the resumable exec_command schema omits `tty` and
    an explicit `tty: true` call returns `TTY execution is disabled by config;
    omit `tty` or set it to false.` when `unified_exec_tty` is disabled. Wired
    from `features.Enabled(cfg.FeatureSettings(), "unified_exec_tty")`.
- `fe140d4c8e` #42391 Authorize `apply_patch` in the executor path context
- `1f4c47343a` #42113 Apply remote platform semantics to exec safety checks
- `eb078b4f44` #42314 Preserve target-native cwd in permission approval requests
  - **status: N/A (already satisfied)** - Go's `PermissionsRequestApprovalParams.CWD`
    is a plain `string`, so a remote environment's target-native working
    directory is already preserved without a host-native `PathUri` conversion.
- `7a7c188682` #42577 Preserve target-native paths in command approvals
- `b3f5e45cc1` #42781 Add direct SigV4 transport to exec-server
- `3b2d9a69e6` (see above) and `574a36ff99` #42883 client-side exec-server RPC attempt metrics

### batch-tui-sync30
- `a1294e57f1` #42749 Improve automatic thread naming in the TUI
- `9c4253ffc1` #42847 Preserve Markdown formatting when copying TUI responses
- `d6350e24be` #42354 Add free-form asynchronous user messages
- `0588fc941c` #42380 Require confirmation for safety-buffered retries
- `a526f54b00` #42319 Show live context compaction status in the TUI
- `9c7edd4bc3` #41941 Add Vim undo; `b192442fc7` #42140 redo; `5a0419edb5` #42194 replace mode
- `7a8092a447` #42847 (see above); `f46671b14a` #42650 assistant file citations as local links

### batch-app-server-daemon-sync30 (Windows lifecycle; rust-coupled on Unix host)
- `b7f710273e` #42405 Support the app-server daemon on Windows
- `715294448f` #42381 Support managed app-server lifecycle on Windows
- `91608236ea` #42392 Support managed daemon updates on Windows
- `10aca93f18` #42364 Support graceful daemon shutdown on Windows

### batch-not-applicable / rust-coupled (documented)
- voice-host / realtime-webrtc / audio: #42404, #42431, #42209, #42208, #42204,
  #41890, #41884, #41934, #42332, #42676, #42631 (Go has no voice subsystem).
- windows-sandbox-service / mxc-sandbox / native MXC: #42330-#42353, #42841,
  #42596, #42835, #42833, #42801 (native Windows service adapter not in Go).
- Bazel/schema-bundle/Python tooling: #42174, #42288, #42109, #41915, #42161.

## Remaining / not done this pass
- Full runtime ports for the batches above (status `pending`).
- L3 SDK differential (`sdktests`) remains gated on explicit model-cost authorization.
- L4 certification matrix (multi-platform build) not re-audited here.

## Upstream commit inventory (271 commits, oldest -> newest)

See `git -C D:\qax\reagent\dev\git\codex log --oneline 28327355b8..574a36ff99`.
