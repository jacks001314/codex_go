# codex_go 对齐 Rust Codex `bbbf396839` 执行与认证计划

> 日期：2026-08-01  
> 上一正式审计起点：`25eecb071e`  
> Rust 目标：`bbbf396839b3bab872291354878980ba82ad4aee`  
> Go 工作树起点：`70d8f0bfbd2938132c48c347643c1e3de6761c7e`  
> 目标：外部协议、持久化、运行时、安全边界和平台行为零例外对齐

## 当前结论

- `25eecb071e..bbbf396839` 的 39 个 Rust 提交已全部进入机器可校验台账，最后 16 个 intake 均已实现或按 Rust-only 发布变更完成处置。
- Go 功能域当前为 `complete=22 / partial=4 / missing=0`；8 个公共契约域全部完成。
- 最终 Windows Go 候选已通过 `go test ./...`、全仓 race、`go vet ./...`、exec-server Rust 源码协议校验、最终二进制 raw app-server 审计和两轮 42/42 clean SDK 差分矩阵。
- 目前不能声明 100% 认证完成。剩余项不是未实现功能，而是三类门禁证据：第三轮 clean SDK 矩阵、Rust Windows cancellation 上游缺陷、五个非本机平台的原生 CI 运行。
- `certificationReady` 必须保持 `false`，直到下面“退出条件”全部满足。

## 冻结身份

| 组件 | 路径/版本 | SHA-256 |
|---|---|---|
| Rust CLI | `../git/codex/codex-rs/target/debug/codex.exe`, `codex-cli 0.0.0` | `3c5ba9285bdc4b1a1ffab196e0af157b95437c33ac0a06cb3646ffb56c3ab503` |
| Rust app-server | `../git/codex/codex-rs/target/debug/codex-app-server.exe` | `4e063a43221ef324222f3ff4ed0fed9b42fed7b3f550ca92c5086479d8a26e2c` |
| Go CLI | `sdktests/artifacts/binaries/2026-08-01/codex-go.exe`, `codex-cli 0.0.0-dev+70d8f0bfbd29` | `0ec56eed907aff3ff6d31e05466ab6e6e09b634aab19c1aa2117a89914d6abe6` |
| Go code-mode host | `sdktests/artifacts/binaries/2026-08-01/codex-code-mode-host.exe` | `3da7ec14375785389384bcf890fa2b82d7002c00599cc28eb6da9bc78b2acd35` |
| TypeScript SDK dist | `../git/codex/sdk/typescript/dist/index.js` | `0970cdd99525e6675804b81a3dc7197a682cfeda140a929839bdbfd3b6f02434` |

## 分阶段状态

| 阶段 | 范围 | 状态 | 主要证据 |
|---|---|---|---|
| P0 | 基线、提交账本、契约清单 | 完成实现，认证控制仍 partial | `parity/baseline.json`, `commits.json`, `contracts/manifest.json` |
| P1 | 配置、认证、MCP、skills、connectors | 完成 | focused tests + full/race |
| P2 | SQLite state、迁移、WAL、线程历史 | 完成实现，跨平台原生证据 partial | Windows/amd64 SQLite 3.51.3 原生 gate 通过 |
| P3 | rollout、history、fork、projection、搜索 | 完成 | Go tests + raw paginated fork parity |
| P4 | goals、spawn graph、logs、feedback、外部会话同步 | 完成 | state/appserver/race |
| P5 | memories 独立数据库与两阶段流水线 | 完成 | memories/state/appserver/race |
| P6 | realtime V1/V2/V3、transport、handoff | 完成 | realtime/appserver/full/race |
| P7 | TUI、SDK、Windows cancellation | SDK 主矩阵功能通过；cancellation partial | 两轮 42/42 clean；Rust cancellation 缺陷 |
| P8 | 六平台发布与连续认证 | partial | Windows 原生 + 六目标交叉编译；远端原生 CI 被 billing 阻塞 |

## 最后 16 个 Rust intake 处置

| Rust commit | 主题 | 处置结果 |
|---|---|---|
| `bbbf396839` | external imported session sync | 完成：严格前缀、冷会话、唯一 ledger、条件 checkpoint、六场景 |
| `c4f2746c43` | MCP calls bound to thread | 完成：thread-bound coordinator 与生命周期测试 |
| `35eab50501` | sessions direct to tasks | 完成：外部 session/task 路由与同步语义 |
| `2c005abb07` | collaboration mode catalog | 完成：catalog、world state 与 collaboration 消息 |
| `7b38c48da9` | shell sandbox permissions/justification | 完成：权限、justification、local environment 和 fail-closed 校验 |
| `3d1d26915a` | stop legacy Linux archives | Rust-only 发布处置完成；新平台矩阵已按当前目标定义 |
| `5548c95d66` | skills in MCP server | 完成：MCP skill surface 与测试 |
| `448118f544` | connector detection in external sessions | 完成：external connector detection |
| `ef293f7ac9` | shell command local environment | 完成：shell restriction 与环境映射 |
| `66d63afd18` | host skills discovery order | 完成：host/MCP/plugin precedence |
| `aea26afaee` | skills in debug prompt output | 完成：debug prompt skill 输出 |
| `164b3bfeab` | MCP OAuth isolation by environment | 完成：环境隔离与回归测试 |
| `5e8b22488f` | host skill catalog world state | 完成：catalog world state |
| `bf4d3f51ea` | hosted MCP credentials local only | 完成：本地限定、fail-closed |
| `f0c30e528a` | prompt hashes from rollouts | 完成：feedback/rollout prompt hash |
| `53d06e24ea` | bundled code mode host | 完成：同目录 host 解析、构建产物和测试 |

## 本次发现并关闭的协议缺口

Rust 当前 exec-server 不再使用旧 `capabilities/discover` 路径清单，而是：

```text
capabilityRoots/discoverV1
```

Go 已改为 V1 完整文件 bundle，并覆盖：

- 每次最多 128 roots，客户端自动分批并稳定聚合；
- `.codex-plugin`、`.claude-plugin`、`.cursor-plugin` 及 root manifest 优先级；
- `.mcp.json`、apps、完整 UTF-8 文件内容、skill `SKILL.md`/`agents/openai.yaml`；
- 最近祖先 namespace 与嵌套 namespace manifest；
- scan depth、目录/条目、单文件/单 root 字节上限；
- 相对路径与 root 逃逸防护、per-root warning/error、Rust 排序；
- Rust `protocol.rs` 全部 method 常量与 Go exact set 的源码级 verifier。

验证：`go test ./execserver -count=1` 与 `go test ./parity ./features -count=1` 通过。

## 持久化对齐结果

- Go 与 Rust 一样以 SQLite 作为线程、rollout projection、goals、spawn edges、logs、guardian、thread history、memories 等持久化事实源。
- state runtime 负责数据库打开、迁移、恢复、checkpoint 与关闭顺序；memories 使用独立 `memories_1.sqlite`。
- Windows/amd64 原生 gate 报告嵌入 SQLite `3.51.3`，满足 Rust 下限；生命周期、迁移和已提交 WAL 的进程骤停恢复测试通过。
- `CGO_ENABLED=0 go build ./...` 已通过 linux/windows/darwin 的 amd64/arm64 六个目标；这是编译覆盖，不替代对应平台原生 SQLite 运行。

## Go 与协议验证

```text
go test ./... -count=1
PASS (121.5s)

go test -race ./... -count=1
PASS (272.2s)

go vet ./...
PASS

npm --prefix sdktests run test:unit
39 pass, 1 Windows signal-semantics skip, 0 fail

CODEX_EXPECT_GOOS=windows CODEX_EXPECT_GOARCH=amd64 go test ./state \
  -run 'Test(BundledSQLiteMeetsRustMinimum|NativeSQLiteArtifactPlatform|SQLiteRecoversCommittedWALAfterAbruptExit|StateRuntimeOwnsRustDatabaseLifecycle)$' -v -count=1
PASS; SQLite 3.51.3
```

## 最终二进制差分证据

同一 Rust/Go/SDK 哈希、默认 `gpt-5.6-sol` + `xhigh` 下，两轮完整 clean matrix：

| Suite | 结果 | 耗时 | Infra retry |
|---|---|---:|---:|
| `sdktests/artifacts/suites/2026-08-01T024647417Z-parity` | 42/42 strict parity | 2842.5s | 0 |
| `sdktests/artifacts/suites/2026-08-01T033419130Z-parity` | 42/42 strict parity | 2902.4s | 0 |

最终二进制 raw app-server 审计：

- `sdktests/artifacts/2026-08-01T065723289Z-raw_paginated_fork`: pass/parity。
- `sdktests/artifacts/2026-08-01T065723280Z-raw_config_requirements`: pass/parity。

第三轮 clean matrix 尚未形成。Rust 侧随后持续收到 provider `server_overloaded`：`Selected model is at capacity`；Go 同场景完成。单场景顺序反转与 `gpt-5.6-terra/luna` 探针仍只在 Rust 侧触发容量错误。`gpt-5.4/5.5` 有容量，但更低模型在严格工具提示中产生随机 commentary、漏工具或多工具，属于行为 mismatch，不能拿来替代冻结默认模型的 clean 认证。最新默认模型尝试 `sdktests/artifacts/suites/2026-08-01T071032345Z-parity` 在前五项最终通过后，对 `resume-apply-patch-parse-recovery` 连续四次得到 Rust infra failure，按 `--fail-fast-infra` 保持 interrupted 并停止。

为避免基础设施抖动隐藏证据或浪费整轮，SDK harness 已增加：

- `--infra-retries N`：只重试 `infra_failure`，默认 0；
- 每个 attempt 的工件、分类和时间写入 suite summary；
- 15/30/60 秒指数退避；
- `--fail-fast-infra`：重试耗尽即停止，行为 mismatch 永不重试。

## 明确阻塞项

1. **第三轮 clean SDK 矩阵**：当前 Rust/provider 的 5.6 模型路由持续容量不足。已有两轮 clean 42/42，要求仍是三轮，不能降标。
2. **Windows interrupted-command**：Rust `bbbf396839` 会遗留 PowerShell descendant，并使 resume 失败；Go 会安全杀进程树并继续。该项等待 Rust 上游修复，禁止让 Go 回退到不安全行为。
3. **五个平台原生门禁**：Windows/amd64 已通过；linux amd64/arm64、windows/arm64、darwin amd64/arm64 仍需原生 runner。GitHub Actions 现有运行未启动，API annotation 为账号 billing 锁定。

## 100% 认证退出条件

- [x] 39 个 Rust 提交全部有处置与测试证据。
- [x] 公共协议契约 `missing=0`，exec-server exact method verifier 通过。
- [x] 全量 Go、race、vet、SDK unit、raw fork、raw requirements 通过。
- [x] Windows/amd64 原生 SQLite 3.51.3、WAL 和 lifecycle 通过。
- [ ] 同一冻结身份连续 3 次 42/42 clean SDK 差分矩阵。
- [ ] Rust Windows interrupted-command 上游行为可完成并与 Go 收敛。
- [ ] 六个平台原生 SQLite/runtime/release gates 全绿。
- [ ] `parity/domains.json` 从 `partial=4` 收敛到 `partial=0`。
- [ ] `parity/baseline.json.certificationReady` 改为 `true`。

## 后续执行顺序

1. Rust 5.6 provider 容量恢复后，用冻结默认配置从零跑第三轮；若出现任何行为 mismatch，修复后重新从 1/3 计数。
2. 解除 GitHub billing 锁并提交/触发 `sqlite-platform-gates.yml`，收集六个原生 runner 工件。
3. Rust 上游修复 Windows process-tree cancellation 后，重建同 commit/修复 commit Rust binary，重跑 opt-in cancellation 和三轮矩阵。
4. 仅在四个 partial 域全部关闭后设置 `certificationReady=true`，生成最终 release manifest。
