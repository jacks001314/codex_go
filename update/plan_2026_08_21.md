# Codex Go update alignment plan - 2026-08-21 (sync22 #39731-#39825 + follow-up #39827-#39837)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` frozen target: `9bf673718a` (sync21) -> `bd19459358` (41 commits, #39731-#39837)
- Rust frozen SHA: `bd19459358f534ed1cae464ec13d56600aeb45f2`
- Go baseline: `a56410c` (`main`, sync22 complete committed)
- Go baseline state: `go build ./...` clean before the cycle.

## Drift report

New upstream commits since frozen `9bf673718a` (38 commits, #39731-#39825).
Full per-commit ledger lands in `parity/commits.json`.

### Implemented this cycle

1. `bf2aee99c5` #39731 Avoid rollout reads for configured TUI sessions
   - N/A (equivalent): Go TUI session state comes from authoritative lifecycle
     responses; there is no rollout-read-based session inference for
     `ThreadStarted`, so nothing to skip.
2. `8c828b18d6` #39736 Remove private executor directory creation
   - N/A (equivalent): Go execserver has no `private` fs/createDirectory flag or
     owner-private directory creation path.
3. `c3db180493` #39744 Skip postprocessing for short composer input
   - N/A (equivalent): Go's grapheme-safe composer wrapper is a single pass with
     no separate word-boundary postprocessing pass to skip.
4. `2e1f18e0db` #39746 Refresh resumed thread capability roots from executors
   - Implemented: `combineSelectedCapabilityRoots` refreshes matching thread-owned
     roots from live attachment roots (same root id + environment id) while
     preserving persisted roots when the executor reports none; used by
     `EnvironmentManager.InspectSelectedCapabilityRoots` and
     `inspectSelectedCapabilityRootsForThread` (appserver/environment.go,
     runtime_router.go). Tests added.
5. `a26d50852a` #39749 Require filesystem backends to implement directory walks
   - N/A (equivalent): Go file-system backends already implement directory walks
     natively (`fs/readDirectory`, local/remote filesystem); no shared
     file-system trait boundary to enforce.
6. `85a1b0e33d` #39752 Expose uncompiled permission profile selection
   - Implemented: `ResolvePermissionProfileSelection` returns the effective
     profile id + merged configured/managed catalog without compiling executor
     paths; managed defaults override configured, allowlists enforced, and
     undefined allowlisted profiles rejected (config/permissions.go,
     requirements_file.go, api.go).
7. `ce950dcf26` #39755 Add managed developer instructions to requirements
   - Implemented: `additional_developer_instructions` parsed from
     requirements.toml, exposed as `additionalDeveloperInstructions` on
     configRequirements/read, and contributed as a separate developer message
     with the managed markers each turn (10,000-token bound).
8. `5bcd7b0fbc` #39770 Refresh bundled model definitions
   - Equivalent (automatic): Go reads `models-manager/models.json` from the Rust
     checkout; the refreshed definitions apply without a vendored copy.
9. `8a40095ea3`/`bce5f2fcfc` #39757/#39772 Standardize shell execution on unified exec
   - N/A (architecture): Go already routes all shell execution through its own
     unified exec implementation; no legacy shell runtime variants to retire.
10. `097825f75a` #39761 Add app-server MCP event streaming
    - Implemented: experimental `mcpServer/event/stream/start|stop` requests and
      `mcpServer/event/stream/notification` forwarding; connection-scoped
      subscriptions (64/connection), unique ids, startup activation wait (90s),
      3-attempt reconnect with 1s/2s/4s backoff, auth-change/thread-unsubscribe/
      connection-close termination; continuous SSE streaming support in the MCP
      HTTP client (mcp/events.go, mcp/http_client.go, appserver/mcp_event_stream.go).
      Tests added.
11. `39073ca3a7` #39765 Include suggestion IDs in plugin install metadata
    - Implemented: plugin install elicitation meta now carries `suggestion_id`
      (same value as the elicitation request id); connectors omit it
      (tool/plugin_install.go). Tests added.
12. `5cada24434` #39776 Verify Codex app signatures before launch or install
    - N/A (platform): macOS-only signature verification; Go doctor desktop
      checks report N/A on non-macOS hosts (Windows development host).
13. `5663754f62` #39777 Retry transient registry failures during initial exec connection
    - Implemented: initial Noise rendezvous connect retries transient registry
      failures (connect/timeout, interrupted bodies, 5xx/408/429/409
      environment_offline) with 6s per-request timeout, 14s overall deadline,
      4 retries, exponential jittered backoff; malformed complete JSON is
      terminal; one-time 401 bundle refresh keeps a fresh retry window
      (execserver/remote_harness.go, remote.go). Tests added.
14. `854cbb2fd4` #39779 Make tool-result telemetry limits configurable
    - N/A (equivalent): Go has no OTLP tool-result telemetry pipeline or
      configurable tool-result telemetry limits.
15. `90c67e6f33` #39784 Classify rollout migration failures
    - Implemented: rollout migration outcomes carry `failure_reason`
      (rollout_read_failed vs invalid_session_metadata + the full Rust enum).
16. `0cc80b8db5` #39785 Support turn cost telemetry for custom model providers
    - N/A (equivalent): Go has no OTLP turn-cost worker or backend turn-usage
      client (documented api-key-turn-cost-telemetry architecture difference).
17. `9e680a52e7` #39786 Support host-accepted exec-server WebSockets
    - Implemented: `ConnectAcceptedClient` initializes over a host-accepted
      WebSocket (initial resume rejected); `ReplaceAcceptedConnection` retires
      the transport and resumes the session with serialized handoff.
18. `763787d061` #39782 Support standalone named function call outputs
    - Implemented: `function_call_output` may omit call_id and carry optional
      name/namespace; parsing/history normalization preserve standalone named
      outputs while retaining paired orphan removal.
19. `d9fd91edab` #39795 Add hostname to the configurable TUI status line
    - Implemented: `hostname` status-line item (normalized OS hostname without
      DNS), setup preview, accent mapping, and rendering
      (tui/chatwidget/status_controls.go, tui/bottom_pane/*). Tests added.
20. `bfb8986f7f` #39794 CI tooling - N/A.
21. `010738a25c` #39792 Reject settings updates for parent-owned subagents
    - Implemented: `ensureDirectInputAllowed` now guards turn steer, thread
      settings update, and queue add/update/start in addition to turn start,
      mirroring Rust (appserver/runtime_router.go). Tests added.
22. `aead844f64` #39791 Handle standalone tool outputs as external context
    - Implemented: standalone function_call_output items mark the thread memory
      mode polluted when memories.disable_on_external_context is enabled, and
      guardian transcripts render them as namespaced tool-result entries.
23. `43526b8561` #39790 Test dedup - N/A.
24. `cc801d7048` #39797 Enrich thread archive analytics with thread context
    - N/A (equivalent): Go has no OTLP analytics reducer pipeline for archive
      events; thread archive RPC is implemented without analytics enrichment.
25. `a3bce23f3b` #39798 Update rmcp to 3.1.3
    - Implemented: server/discover JSON-RPC errors fall back to legacy
      initialization while preserving diagnostics; OAuth issuer-check failures
      keep pending authorization state.
26. `53cec04646` #39802 Optimize case-insensitive thread history matching
    - N/A (equivalent): performance-only optimization; Go history matching
      already returns correct original byte ranges.
27. `f3cd299428` #39804 Use multi-agent V1 for Amazon Bedrock models
    - Implemented: Bedrock catalog models advertise `multi_agent_version: v1`
      (model/catalog.go). Tests added.
28. `2790c13899` #39807 Finalize reserved PDF uploads with creation context
    - Implemented: `pdf_c2pa_reservation` flag on file creation echoes the
      original creation payload as `pdf_c2pa_create_request` on finalization;
      legacy servers keep an empty payload (mcp/openai_file.go). Tests added.
29. `3cde5d4ccd` #39809 Preserve WINDIR in core Windows shell environments
    - Implemented: `WINDIR` added to the Windows core env allowlist in both
      execserver and execpolicy (case-insensitive retention). Tests added.
30. `21facf2273` #39811 Restrict macOS preference reads - N/A (seatbelt platform).
31. `969efa5470` #39812 Avoid materializing writable-root carveouts
    - N/A (equivalent): performance-only presence check; Go semantics unchanged.
32. `67b2c8c6fb` #39813 Defer legacy filesystem policy projection
    - N/A (equivalent): Go session settings updates never compute the Rust
      legacy filesystem projection.
33. `54201093d4` #39822 Guardian classifier instructions - N/A (guardian-v2 crate).
34. `f20b63e85c` #39825 Use Responses compaction for Amazon Bedrock
    - Implemented: remote compaction is V2-only in Go (no dedicated compact
      endpoint); Bedrock providers now advertise remote compaction support
      (model/provider_info.go). Tests added.

## Verification

```powershell
go build ./...
go test ./appserver ./mcp ./model ./tool ./execserver ./execpolicy ./tui/chatwidget ./tui/bottom_pane ./parity
go test ./... -count=1
```

Cycle result (2026-08-21):

- `go build ./...` clean.
- All 38 commits implemented or documented N/A-equivalent with evidence
  (`parity/commits.json`: 38/38 complete).
- Full tree `go test ./... -count=1`: 0 FAIL at the new frozen target.
- Parity snapshots re-frozen at `bd19459358`; app-server schema/precomputed
  exports re-vendored; ledger/baseline/domains/contracts/README/parity.json
  updated.

## Follow-up batch (same day, #39827-#39837)

New upstream commits after `f20b63e85c` (3 commits) aligned and committed:

1. `daa48072f4` #39827 Add history and notes tools for token-budget sessions
   - Implemented: new `historynotes` package mirrors `ext/history-notes` - a
     backend calling `alpha/history/v2/*` and `alpha/notes/v2/*` with Codex
     backend auth and context injection, plus nine namespaced tools
     (`history.list_windows/list_items/read_item/search_contents`,
     `notes.list_files_by_prefix/read_file/search_contents/append_to_file/
     write_file`) with Rust schemas and limits; wired into the turn tool router
     when `features.token_budget.use_history_notes_extension` is enabled with
     an OpenAI provider + Codex backend auth. Tests added.
2. `d8ec270183` #39830 Rename the history notes extension config option
   - Implemented: Go `TokenBudgetConfig.UseHistoryNotesExtension` parsed from
     `features.token_budget.use_history_notes_extension`; the gate is excluded
     from explicit-setting detection. Tests added.
3. `bd19459358` #39837 Ignore project instructions for untrusted projects
   - Implemented: project-scoped AGENTS.md discovery is skipped for untrusted
     projects (user instructions preserved); the instruction cache key includes
     the trust level. Tests added.

Parity: ledger re-targeted `f20b63e85c..bd19459358` (3/3 complete), fixture
roots 113->114, critical hashes refreshed, workspace members + Go top-level
inventory updated, app-server v2 suite manifest gains `history_notes_extension`,
baseline/domains/contracts/README/parity.json re-pointed.
