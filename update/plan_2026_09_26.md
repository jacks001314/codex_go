# Codex Go update alignment plan - 2026-09-26 (a0b85c7a66)

Continues `update/plan_2026_09_25.md` (sync138-sync214).

## Baseline

- `git fetch origin main` + `git pull origin main` in
  `D:\qax\reagent\dev\git\codex` report **no new upstream commits**: the Rust
  checkout, `origin/main` and `git ls-remote origin main` all agree on
  `a0b85c7a664505a95ad0cfbaa2291ee34aa5133d` (#48158, the Guardian
  retained-context spacing/empty-assistant fix).
- Go repo was at `159bb7c1` (`sync214`, #48158's rendering half) before this
  pass, so the daily pull exposes no unlanded upstream commit.

Per the workflow's step 6 (re-check the recorded unlanded items), this pass
works the most recently recorded remaining lane instead of stopping: the
`history` crate's retained-context model. sync214 landed the *rendering* half of
#48158 (`state/guardian_root_messages.go`); the data model the renderer consumes
did not exist in Go.

## Why the model lives in its own package

Go's `history` package is the counterpart of Rust's **`message-history`** crate
(the append-only global `history.jsonl`: `history.go` / `message_history.go`
mirror `message-history/src/lib.rs`). Rust's **`codex-history`** crate
(`history/src/lib.rs` and its `retained_context` / `retained_source` /
`retained_assistant_messages` / `sender_user_messages` / `heartbeat` /
`reconciled_retained_context` modules) had no Go counterpart at all, so the port
lands as `codex_go/retainedctx` and the Go top-level directory inventory is
re-pinned (`parity/rust_inventory_test.go`).

Go also has no typed `CodexHarnessMetadata` (persisted items carry
`Metadata map[string]any`), so the package takes the typed view it needs
(`retainedctx.HarnessMetadata`: `user_input_order`, `inherited_user_message`,
`sender_user_messages`). Wiring the rollout/compacted-item wire to the typed
metadata is recorded below as the follow-up.

## sync215: the retained-context model (`codex-history`)

`retainedctx` ports the bounded, host-owned evidence families that live outside
the model's compaction contract, with Rust's admission rules intact:

- **`heartbeat.go`** - `HEARTBEAT_CONTENT_KIND`, `UserInputOrigin`
  (`FromTurnTrigger`, `FromMessage`, `IsUser`), and `Heartbeat`
  (`ParseHeartbeat`, `HeartbeatFromMessage`). `ParseHeartbeat` reproduces Rust's
  byte-budget guard (`len + lines*6 + 64 > 3600` with `str::lines()` counting),
  the single-trailing-newline strip, the exact envelope prefix/suffix, and the
  automation-id/timestamp byte-class limits. The Go zero value is normalized
  onto `User` so an unset origin never round-trips into a different stored value.
- **`retained_source.go`** - `ResponseItemID` (`NewResponseItemID("retained")`,
  UUIDv7), `RetainedSourceRole`, `RetainedSourceID`, `RetainedSource`, plus
  `RestoreSourceRevision` and `Source` (delivery proof requires both a message id
  and a recorded revision).
- **`retained_context.go`** - `VerifiedQuestionAnswer`, `VerifiedAnswer`,
  `RetainedUserMessage` (skips the ordinary-user origin on the wire, defaults it
  on decode), `RetainedInputSource` (`Local(order)` / `Inherited`),
  `RetainedContextOrder` (inherited prefixes order before local acceptance
  order), `RetainedContextEntry` / `OrderedEntry`, `RetainedContextEvent`
  (internally tagged `verified_answer`), and `RetainedContext` itself with
  Rust's serde layout: `verified_answers` + `incomplete`,
  `user_messages` + `user_messages_incomplete` (legacy default **true**),
  `assistant_messages` + `assistant_messages_incomplete`, `next_order`, and the
  empty-skipped `sender_deliveries`. Storage limits are `MAX_FAMILY_RECORDS = 8`,
  `MAX_RECORD_BYTES = 16_384`, `MAX_FAMILY_BYTES = 65_536`; `boundFamily` sorts
  by acceptance order (not persistence order) and evicts the oldest, marking the
  family incomplete; `RetainedUserMessage.bound` clears text/incompleteness and
  truncates ids at a char boundary; `RetainedContextEvent.Bound` clears an
  oversized answer's questions and truncates its ids.
- **`retained_messages.go`** - `RecordUserMessage` (heartbeat coalescing that
  compares only the latest version of the same automation, id-based replacement,
  inherited prefix order via `next_inherited_order`, revision minting,
  `verified_answers_incomplete |= inherited`), `RecoverUserMessageExcerpts`,
  `Record`, `Restore` (deep copy, re-bound every family, repair pre-adoption
  checkpoints, re-derive `next_order` from surviving item metadata), and
  `Rollback` (boundary by acceptance order or first removed message id; the
  legacy fallback by turn id, with inherited/local scoping).
- **`retained_assistant_messages.go`** - `RecordAssistantMessage`, including
  #48158's rules: an unsequenced `Local(None)` source is left to legacy
  transcript selection; a **complete, empty** assistant message is neither
  recorded nor a dedup hit (an id collision removes the stale record instead);
  an incomplete message keeps its omission notice; `HasOmittedAssistantMessages`
  reports storage loss or an undeliverable record.
- **`reconciled_retained_context.go`** - `LiveUserMessage`,
  `NewReconciledRetainedContext` (source-identity matching before ordering,
  conflicting/absent orders mark the result incomplete, latest ordered user turn
  tracking), `OrderedEntries` (with heartbeat-version coalescing after ordering,
  scoped per inherited/local prefix), and `UnmatchedUserMessages`.

Tests mirror Rust's suites: `retained_context_tests.rs` (order through recovery,
checkpoint and rollback; family storage limits without changing snapshots;
recovered excerpts obeying record/family limits; legacy checkpoints marking user
messages incomplete; surviving local sources advancing the counter; delayed
acceptance order across checkpoint replay plus `RestoreSourceRevision`; adopted
instructions preserving local order and rollback scope), the assistant rules
from #48158, `reconciled_retained_context_tests.rs` (source identity/acceptance
order/checkpoint gaps; missing and conflicting orders; heartbeat versions across
retention, restore and reconciliation), and `heartbeat_tests.rs` (exact
instruction body with and without a trailing newline, plus the rejection
matrix), with an added origin-classification and sender-budget matrix.

### Verification (fifty-fifth pass)

- `go test ./retainedctx/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `a0b85c7a66` (only the Go top-level
  directory inventory changed: `+retainedctx`).
- `go test ./... -count=1` reported only the documented load-sensitive
  `appserver.TestSkillInstructionsSelectAppsTheyMentionLikeRust`, which passes
  in isolation (`-run ... -count=1`) and as a full-package run
  (`go test ./appserver/ -count=1`, `ok ... 140.6s`).

## Remaining for the retained-context program (recorded)

- The rest of the `codex-history` crate domain that this slice does not yet
  model: `RetainedContext`'s typed home in the rollout wire
  (`ResponseItemEnvelope` + the typed `CodexHarnessMetadata` with
  `guardian_sources`, `guardian_source_order_guidance`, `retained_source`,
  `history_truncation_token_limit`, `delivered_assistant_message`,
  `harness_authored_configuration`, `compaction_model_hash`, `compaction_output`,
  `mcp_attribution`), `GuardianHistoryCheckpoint`, `CompactedItem`,
  `RolloutItem`/`RolloutLine`, and `CompactionCheckpoint` /
  `CompactionResumeMetadata`. Go persists these as untyped maps today, so the
  retained model has no consumer yet.
- `guardian-context`'s `composition.rs` / `retained_instructions.rs` sections
  (the retained-instruction fragment spacing, source-order guidance detection and
  empty-section dedup introduced alongside #48158). `state/guardian*.go` carries
  only the root-message renderer (sync214) and the bounded root authorization
  list.

The next pass continues with the typed harness-metadata/rollout wire that gives
`retainedctx` its consumer, then the guardian-context composition sections.

---

## Follow-up pass (same day, nineteenth pull): `44a9bfa145` (two commits)

`git fetch` advanced `a0b85c7a66..44a9bfa1456e51269fa824fa2f93b435e3e800ed`:

| Commit | Subject | Lane |
|---|---|---|
| `f6a4bd81e3` | Generate unique exec-server process IDs for every request (#48168) | unified-exec (core) |
| `44a9bfa145` | Preserve usage limit windows in turn and compaction analytics (#48174) | analytics + codex-api |

`go test ./parity/ -count=1` is green at the new pin: both commits modify
existing files (test suites and sources), so no fixture root, inventory,
critical-file hash, snapshot total or precomputed export changes.

### sync216: unique exec-server process IDs for every request (#48168)

Rust previously suffixed a fresh UUID onto the exec-server process id only when
the request carried sandbox or shell-snapshot settings; a thread sharing an
executor, or a sandbox retry, could reuse a public handle while the executor
still retained its previous process. The suffix is now unconditional.

Go's unified-exec remote start built `remoteID` as the bare stringified public
process id, i.e. exactly the pre-#48168 "no suffix" branch, so it had the same
collision window.

- **`tool/unified_exec.go`**: `remoteID` is now
  `"<public id>-<uuid v4>"`. The public id keeps addressing the session
  (`unifiedExecProcess.id`, the tool's process list, writes and signals), while
  the suffixed value is what the executor is addressed with: process-event
  subscription, network-policy controller registration, `ExecParams.ProcessID`
  and every later read/write/signal/terminate against that process. The trace
  event still reports the public id separately from the executor key.
- **`tool/unified_exec_spans_test.go`**: the process-start event's executor key
  is now asserted to be the public id plus a parseable UUID suffix, mirroring
  Rust's request-parameter test (`assert_regex_match` on the suffixed format and
  `assert_ne!` across repeated calls).

### sync217: usage-limit windows in turn and compaction analytics (#48174)

Rust preserves an optional `limit_window_minutes` from usage-limit error
responses on `UsageLimitReachedError`, accepting unsigned integers that fit in
`u16` and treating missing/null/malformed/out-of-range values as unknown, and
reports it as `usage_limit_window_minutes` in turn and compaction analytics
(`null` for every other error kind).

Go had no home for the value at all: its HTTP 429 with `error.type ==
"usage_limit_reached"` fell through to a plain `ResponsesAPIError`, so no
usage-limit classification (and therefore no window) reached the app-server.
This pass lands the classification together with the window.

- **`codexapi/error.go`**: `APIError` (and `APIErrorDetails`) gained
  `UsageLimitWindowMinutes *uint16`. `Details()` and `NewAPIErrorWithDetails`
  carry it on both the literal and the details-backed construction paths.
- **`model/responses_agent.go`**: `responsesAgentAPIErrorBody` gained the
  untyped `limit_window_minutes` member; `responsesHTTPError` classifies an HTTP
  429 whose `error.type` is `usage_limit_reached` as `ErrorRateLimit` (Rust's
  `CodexErr::UsageLimitReached`, checked before the quota codes as in
  `api_bridge.rs`) and attaches the window through
  `usageLimitWindowMinutesFromValue` (unsigned integers that fit in `u16` only).
  The wrapped-WebSocket `error` event path gained `websocketUsageLimitError`,
  which requires a numeric status and a `usage_limit_reached` error type before
  producing the same classified error with its window, mirroring Rust's
  `parse_wrapped_websocket_error_event` / `map_wrapped_websocket_error_event`
  followed by `map_api_error`.
- **`appserver/turn_runtime.go`**: `turnAnalyticsErrorFields` gained
  `UsageLimitWindowMinutes`, populated only for `codexapi.ErrorRateLimit`, and
  the value is threaded through `turnCompletionAnalyticsContext` into
  `emitCodexTurnAnalyticsEvent`. A status-only 429 (`ErrorAPI` /
  `ErrorRetryable`) keeps reporting `null`, as Rust does for other error kinds.
- **`appserver/compaction_analytics.go`**: the compaction event carries the same
  field from its error classification, so local and remote compaction failures
  report the window.
- **`telemetry/turn_event.go` / `telemetry/compaction_event.go`**: the turn and
  compaction event params gained `usage_limit_window_minutes *uint16` (always
  serialized, `null` when unknown) and the constructors copy it from the input.

Tests: the HTTP window matrix (five-hour 300, weekly 10080, missing, null,
malformed, out-of-range 70000), the unchanged quota classification without a
window, the wrapped-WebSocket mapping with and without a status, the
app-server analytics field matrix (usage limit with and without a window, and
another error kind that must report null), the turn and compaction event JSON
nullability, and the exec-server process-id suffix and uniqueness.

Recorded as remaining from #48174's lane: Rust's `UsageErrorBody` also models
`plan_type` and `resets_at`, and its 429 branch classifies `usage_not_included`
as `CodexErr::UsageNotIncluded`; Go's HTTP path still does not model those, so
the window is the only usage-limit detail Go currently preserves on the HTTP
error. Rust's `analytics` crate also folds the value through
`TurnCodexError`/reducer aggregation, which has no Go counterpart (Go's
telemetry events are the wire shape only).

### Verification (fifty-sixth pass)

- `go test ./codexapi/ ./telemetry/ ./retainedctx/ -count=1` is green.
- `go test ./model/ -run 'UsageLimit|ResponsesHTTPError|WebsocketUsageLimit'
  -count=1` is green.
- `go test ./appserver/ -run 'TurnAnalytics|Compaction|UsageLimit' -count=1`
  and `go test ./tool/ -run 'UnifiedExec' -count=1` are green.
- `go test ./parity/ -count=1` is green at `44a9bfa145`.
- `go test ./... -count=1` recorded below.

## Remaining after this pass

The recorded lanes from sync215 are unchanged (typed harness metadata / rollout
wire for `retainedctx`, the `guardian-context` composition and
retained-instruction sections), plus the #48174 items named above.

---

## Follow-up pass (same day, twentieth pull): `1d804e91b7` (one commit)

`git fetch` advanced `44a9bfa145..1d804e91b75454927fb4f51615b067ba6158c37c`:

| Commit | Subject | Lane |
|---|---|---|
| `1d804e91b7` | Protect `.aws` directories under sandbox writable roots (#48176) | sandbox (security) |

`go test ./parity/ -count=1` is green at the new pin: the commit modifies
existing sources and snapshots, so no fixture root, inventory, critical-file
hash, snapshot total or precomputed export changes.

### sync218: `.aws` joins the protected workspace metadata paths (#48176)

AWS profiles can select credential helpers that the application executes, so
granting write access to a containing directory must keep `.aws` protected.
Rust adds `.aws` to `PROTECTED_METADATA_PATH_NAMES`, to the default read-only
project-root subpaths of `FileSystemSandboxPolicy::restricted`, and to
`default_read_only_subpaths_for_writable_root` (where, like `.agents`, it is
materialized only when the top-level directory exists), preserving explicit user
rules that override the default protection.

Go carries the same metadata list in several places; all now include `.aws`
(Go rebrands the codex home directory to `.gcode`, which is unchanged):

- **`sandbox/policy.go`**: `WritableRoot.ProtectedMetadataNames` gains `.aws`
  (the name-based protection `IsPathWritable` and the seatbelt profile consume)
  and `protectedSubpaths` gains `<root>/.aws`.
- **`sandbox/run.go`**: `rustPermissionProfileWireFromPermissionProfile` adds the
  `.aws` read-only `project_roots` special for a workspace-write policy,
  mirroring `FileSystemSandboxPolicy::restricted`.
- **`config/permissions.go`**: the built-in `:workspace` profile builder gains
  the `.aws` read-only `project_roots` entry next to `.git`/`.agents`/`.gcode`.
- **`sandbox/linuxsandbox/linux.go`**: both the legacy workspace-write entry
  list and `protectedReadOnlySubpaths` (materialized only for existing
  directories) include `.aws`.
- **`sandbox/windowssandbox/resolved_permissions.go`**: the Windows deny
  subpaths gain `.agents` and `.aws`; the deny computation keeps only subpaths
  that exist, so the existing "only existing `.git`" behavior is unchanged.
- **`sandbox/windowssandbox/elevated/ipc_framed.go`**: the elevated
  workspace-write profile's protected project-root subpaths include `.aws`.

Tests: the workspace writable-root matrix (`.aws` in
`ProtectedMetadataNames` and `ReadOnlySubpaths`, and `.aws/credentials` staying
read-only), the built-in `:workspace` resolution asserting the `.aws` read-only
project-root entry, the Windows deny set for `.git`/`.agents`/`.aws`, and a
Linux-only case asserting an existing top-level `.aws` becomes a protected
read-only subpath.

Recorded as remaining for this lane: Rust's
`append_default_read_only_project_root_subpath_if_no_explicit_rule` applies the
default metadata carveouts to *custom* `[permissions]` profiles and skips them
when the profile already has an explicit rule for that subpath. Go's
custom-profile path (`compileConfigPermissionProfile`) applies no defaults at
all, so a custom profile that only writes `:workspace_roots` leaves the
metadata names writable - stricter in Rust than in Go. That divergence predates
#48176 and needs the "no explicit rule" predicate modelled before it can be
closed. Go also has no `bwrap` builder, so Rust's Linux synthetic mounts for
`.aws` have no Go counterpart (the Landlock/deny-policy path above is the Go
equivalent).

### Verification (fifty-seventh pass)

- `go test ./sandbox/... ./config/ -count=1` is green.
- `GOOS=linux CGO_ENABLED=0 go build ./sandbox/...` type-checks the Linux-only
  sandbox additions (the host has no Linux cgo toolchain for `go vet`).
- `go test ./parity/ -count=1` is green at `1d804e91b7`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `1d804e91b7`), so this
pass works the next recorded lane from sync215: the `guardian-context`
retained-instruction and verified-answer sections, which give `retainedctx` its
first consumers.

Rust's `guardian-context` composes the review prompt from independently scoped
section contributors. Go's `state.BuildPromptWithOptions` renders the same
prompt as one string, and previously modelled only the root conversation,
transcript, node-repl evidence and planned action; the retained snapshot had no
renderer at all.

### sync219: the retained-instruction, verified-answer and sender sections

- **`state/guardian_retained_context.go`** ports
  `guardian-context/src/retained_instructions.rs` and
  `guardian-context/src/verified_answers.rs`:
  - `HasLegacyRetainedOrder` (a repeated `RetainedContextOrder` means the
    checkpoint cannot order its records) and `RetainedSourceOrderLabels`, which
    render `Local(order)` labels, `inherited <order>` prefixes, or the legacy
    positional enumeration.
  - `RetainedAssistantMessage`, which selects a *whole* complete assistant
    message only when its rendered framing fits
    `ApproxBytesForTokens(900)` with the 32-byte order label reserved.
  - `RenderRetainedInstructions`, which emits one `RetainedInstructionFragment`
    per whole instruction in acceptance order, skips complete+empty assistant
    messages (#48158), keeps assistant context as optional (untrusted
    commentary) evidence, omits any record that cannot fit its budget instead of
    truncating it, and prepends the incomplete-assistant-context then the
    unavailable-instructions host notices.
  - `RetainedUserInstructionsSectionItems`, which returns the delivered user
    content: the modern `START` banner (with the inherited-prefix sentence) or
    the legacy `LEGACY_START` banner, the fragments, and the `END` footer, each
    with Rust's extra newline so blank lines separate the banner, fragments and
    footer (#48158).
  - `RenderVerifiedAnswer` / `RenderVerifiedAnswers`, which keep both sides of
    every question/answer pair within their own 900-token budget, mark the
    result incomplete when a record does not fit, and prepend the incomplete
    verified-answers host notice.
  - `SenderUserMessagesSectionItems`, the `SenderUserMessagesSection`
    counterpart: the delivery-bound host-rendered sender text, or nothing.
- **`state/guardian.go`**: `BuildPromptOptions` gained
  `RetainedContext *retainedctx.RetainedContext`, and the prompt now renders the
  sender evidence and then the retained-instruction section between the root
  conversation and the transcript, matching Rust's registry order. A nil
  context renders no section, so existing callers are unchanged.

Tests mirror Rust's `retained_instructions_tests.rs` cases that do not need the
ComposedContext/budget machinery: the order/whole-record matrix (an existing
900-token answer still fits, an oversized instruction is omitted with the notice
and never partially rendered), the role-labelling and assistant-context rules
(forged `user:` text inside an assistant message stays labelled `assistant:`, a
complete empty message renders nothing, an incomplete or oversized one reports
the omission notice first while the user instruction still renders), the legacy
positional labels, the section banners/blank-line spacing, the
verified-answer rendering and completion flag, the assistant-message selection
matrix, the sender section, and the prompt-level section order.

Recorded as remaining for this program: `composition.rs`'s
`ComposedContext`/`SectionDelivery` machinery (`retain_new_instructions`,
`deduplicate_transcript_instructions`, `retained_instructions()`,
`enforce_budget`, `into_annotated_messages`) and Rust's `SectionRegistry` /
`SectionContributor` abstraction; Go renders the sections directly rather than
through a registry. The remaining section contributors (`previous_reviews`,
`trusted_tool`, `trusted_skills`, `trusted_user_answers`, `permissions`,
`transcript`, `images`, `node_repl`, `action`) are still unmodelled or rendered
inline. Rust's shared `truncate_text` (`guardian-context/src/truncation.rs`) is
already implemented in Go as `context.guardianTruncateText`, so it needs no port.

### Verification (fifty-eighth pass)

- `go test ./state/ -count=1` is green.
- `go test ./state/ ./appserver/ ./app/ ./parity/ -count=1` is green.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-first pull): `782826663d` (two commits)

`git fetch` advanced `1d804e91b7..782826663df3e898d0c594a13f6f75cc2a498644`:

| Commit | Subject | Lane |
|---|---|---|
| `8f0b05910e` | Fix zsh alias quoting in sourced shell snapshots (#48187) | shell snapshots |
| `782826663d` | Bound agent message board SSE frames before parsing (#48190) | agent message board client |

`go test ./parity/ -count=1` is green at the new pin: both commits modify
existing sources and test suites, so no fixture root, inventory, critical-file
hash, snapshot total or precomputed export changes.

### sync220: zsh alias quoting in sourced snapshots (#48187)

A sourced snapshot's group is parsed before its restored options take effect, so
aliases serialized with `RC_QUOTES` could be misread by the replay shell. Rust
now serializes them with `NO_RC_QUOTES` in a subshell (no optional module), and
keeps the plain alias dump for the evaluating form.

- **`shell/snapshot_scripts.go`** is regenerated byte-for-byte from the Rust
  source with the existing extractor
  (`python scripts/extract_snapshot_scripts.py`): the zsh capture script's alias
  dump is now the `SNAPSHOT_ZSH_ALIASES` placeholder.
- **`shell/snapshot_capture.go`**: `snapshotCaptureScript` substitutes
  `\alias -L` for the evaluating form and `(\setopt NO_RC_QUOTES; \alias -L)`
  for the sourced form, next to the existing option-group substitutions.
- **`shell/snapshot_capture_test.go`**: the marker list covers the new
  placeholder, and the zsh case asserts the sourced form normalizes quoting
  while the evaluating form leaves shell options alone (and that no other shell
  sets zsh options).

### sync221: board SSE frames are bounded before parsing (#48190)

Checking the notification size after SSE parsing left oversized fields and
unterminated frames free to accumulate in the parser, and malformed UTF-8 could
be retained indefinitely. Rust now enforces its bound on the incoming frame
bytes before parsing, resets the count at blank lines (supporting LF, CR and
CRLF across chunks), validates UTF-8 incrementally, and stops reading the source
after an error.

- **`agentboard/remote.go`**: `remoteBoardFrameScanner` reproduces that state
  machine - per-frame byte counting with the blank-line reset and the deferred
  CR handling, plus incremental UTF-8 validation that only carries an incomplete
  code point (using `utf8.Valid`/`utf8.FullRune` for Rust's
  `error_len().is_some()`). `readRemoteBoardEvent` feeds every wire byte through
  it before interpreting the line and returns
  `board SSE frame exceeds the service limit` (Rust's message) or the
  malformed-encoding failure; the caller closes the stream, so reading stops
  after an error. `Next` drops the now-redundant post-parse length check, since
  the frame bound is strictly stronger.

Tests mirror Rust's `remote_board.rs` additions: a long heartbeat sequence using
all three line endings still delivers the notification (the bound is per frame,
not per connection), and an oversized readiness field, an unterminated trailing
field and an oversized multiline data frame are each refused with the
service-limit message before parsing; malformed UTF-8 in the readiness frame and
in a later frame is rejected incrementally.

### Verification (fifty-ninth pass)

- `go test ./shell/ ./agentboard/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `782826663d`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `782826663d`), so this
pass works the next recorded lane from sync219: `guardian-context`'s permission
evidence, which the sync review had no way to include.

Rust's `PermissionContextSection` is `SectionScope::Shared`, so the synchronous
reviewer receives the reviewed environment's denied read paths and globs and is
told not to approve an escalation whose purpose is to read them. Go built its
review prompt without any permission evidence at all.

### sync222: parent-turn permission evidence for the review prompt

- **`sandbox/unreadable.go`** ports
  `FileSystemSandboxPolicy::get_unreadable_roots_with_cwd` and
  `get_unreadable_globs_with_cwd` over Go's resolved profile wire:
  `UnreadableRootsWithCWD` lists the deny entries that are still unreadable
  (skipping the filesystem root, skipping entries a readable rule wins at, and
  deduplicating), and `UnreadableGlobsWithCWD` resolves the deny glob patterns
  against the cwd and returns them sorted and deduplicated. An unrestricted or
  disabled profile reports nothing, exactly as Rust's non-restricted kinds do.
- **`state/guardian_permission_context.go`** ports the section: the
  `PermissionContext` struct, Rust's body text (paths before globs, the
  environment-scoped scope sentence, the "no explicit denied-read
  paths/globs" sentence for an environment-only context), the marked items
  (`\n>>> PARENT TURN PERMISSION CONTEXT START\n` … `>>> PARENT TURN PERMISSION
  CONTEXT END\n`), the synchronous contribution, and the asynchronous rule that
  evidence over one thousand estimated tokens is an error so the async scorer
  falls back to synchronous review instead of dropping restrictions.
- **`state/guardian.go`**: `BuildPromptOptions` gained `PermissionContext`, and
  the prompt renders the section after the node-repl evidence and before the
  planned action, matching Rust's registry order; the marker's own leading
  newline is normalized away because the block separator already supplies that
  paragraph break.
- **`appserver/agent_runtime.go`**: `guardianTurnPermissionProfileForTurn`
  resolves the reviewed turn's own profile and cwd, and
  `guardianReviewPermissionProfileForTurn` (the read-only profile for the
  reviewer's session) now builds on it, so the two are no longer conflated.
- **`appserver/guardian_reviewer.go`**: the reviewer takes a
  `turnPermissionProfile` callback, derives the denied paths and globs, and
  passes the permission context into every prompt build, so a turn with denied
  read rules now reviews with that evidence.

Tests: the body matrix (no evidence, environment-only, paths before globs), the
section items (nothing for an empty context, marked section otherwise), the
asynchronous limit boundary (exactly 3,000 bytes delivered, one byte more
rejected), the sandbox enumerators (deny path and glob, relative glob resolution
against the cwd, duplicate removal, filesystem-root exclusion, and no
restrictions for read-only/full-access/nil profiles), the prompt-level section
order (after node-repl evidence, before the action), and the reviewer wiring
(the denied path and the restriction guidance reach the sampled prompt, and a
profile without deny entries adds no section).

Recorded as remaining for this lane: Rust's resolver also carries the reviewed
environment's *selection id* (so the body names the environment) and uses the
selected executor environment's own `permission_profile_with_workspace_roots`
when the turn runs there; Go currently reports the parent turn's profile with no
environment id, and does not consult an executor environment's own profile. Rust
additionally derives the evidence from executor path contexts
(`get_unreadable_roots_with_context` for remote environments); Go's enumerators
resolve against the host cwd only.

### Verification (sixtieth pass)

- `go test ./state/ ./sandbox/... ./appserver/ ./tool/ ./exec/ ./config/ ./mcp/
  -count=1` is green.
- `go test ./parity/ -count=1` is green at `782826663d`.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-second pull): `dd9a7101d9` (four commits)

`git fetch` advanced `782826663d..dd9a7101d9ddbaab68030e04d288d71bead68552`:

| Commit | Subject | Lane |
|---|---|---|
| `888948e522` | Optimize `blake3` in Bazel fastbuilds (#48197) | build only |
| `8f5387d104` | Honor execution environment proxy requirements (#48198) | network proxy |
| `7596ab19a6` | Keep archived threads with empty previews visible in listings (#48199) | state / rollout listing |
| `dd9a7101d9` | Extract Responses header conversion into a shared module (#48200) | codex-api refactor |

`go test ./parity/ -count=1` is green at the new pin: none of the four commits
touches a pinned artifact (no schema, precomputed export, inventory or snapshot
changes).

### Triage: `#48197` is build-only (N/A in Go)

The commit adds a Bazel fastbuild option for the `blake3` crate (`MODULE.bazel`
plus a comment). Go has no Bazel graph, so there is no Go surface to change; no
stub is added.

### sync223: the owner proxy requirement (#48198's data model)

An execution environment's enabled proxy configuration was reduced to traffic
restrictions, leaving restricted commands offline without a controller proxy or
an approved network grant. Rust carries `NetworkProxyConfig.enabled` into
`EnvironmentNetworkPolicy.requires_proxy` (serialized as `requiresProxy`, omitted
when false) and lets the orchestrator treat it as a managed-network activation
trigger alongside an enabled controller proxy.

- **`network/network.go`**: `EnvironmentNetworkPolicy` gained `RequiresProxy`
  with Rust's wire name and omission rule, and
  `EnvironmentNetworkPolicyFromConfig` now captures it from `Config.Enabled`, so
  the owner's enabled proxy configuration survives into the portable policy
  instead of being dropped with the other controller-owned settings. The doc
  comments record the new "proxy activation plus traffic restrictions" meaning.

Tests: the capture matrix (an enabled owner proxy produces a proxy requirement,
a disabled one does not, and the omitted controller allow-all flag still
resolves to false) and the serialized shape (`requiresProxy` emitted when true,
absent when false).

Recorded as remaining for this lane, with evidence: the *activation consumer*.
Rust's rule lives in `core/src/tools/orchestrator.rs`'s `managed_network_active`,
which reads the resolved environment config's `network_policy`. Go has neither
an `EnvironmentConfig` network-policy field
(`appserver/environment_config.go` carries `AllowLoginShell`,
`ShellEnvironmentPolicy`, `ExecPolicy`, `McpPolicy` and the permission profile
only) nor a production caller of `network.NewSpecForEnvironment` (it is
exercised by tests only), so no host currently constructs an
`EnvironmentNetworkPolicy`. That host path is the already-recorded "app/exec-level
policy owner that composes the CLI host's requirements and binds them to the
remaining transports" lane; once it lands, the activation rule can consume
`RequiresProxy` exactly as Rust does.

### Triage: `#48199` needs the frozen migration baseline and a listing filter Go does not have

The commit has three parts, none of which maps onto a Go edit:

1. **SQLite preview filter**: `push_thread_filters_with_preview` no longer adds
   `threads.preview <> ''` when `archived_only`. Go's thread listing does not
   have that filter at all: `session.matchesListOptions` selects by archive
   collection, section, provider, cwd, source, search and relation only, so an
   archived thread with an empty preview is already listed. Go's `state` layer
   exposes row queries (`state/thread_query.go`) without Rust's
   `ThreadFilterOptions`/`push_list_threads_query` predicate builder.
2. **Archive sort indexes** (`0058_threads_archive_sort_indexes.sql`): Go's
   migration inventory is pinned byte-for-byte to the certified baseline
   (`TestStateMigrationsMatchRustTarget`, `candidateRustTo`), which ends at
   `0050_threads_section_empty_preview_indexes.sql`; the same constraint already
   recorded `0052_projects_recency.sql` as a deferred pure-performance index.
   Rust's 0051-0057 migrations are likewise absent from Go, with their behavior
   implemented outside the state DB where Go needs it (for example
   `thread_attachments` lives in `session.Metadata.Extra`). Adding 0058 would
   break the pinned inventory and the file set it verifies against.
3. **Rollout-file listing**: `rollout/src/list.rs` now passes
   `PreviewFilter::IncludeEmpty` for the flat-layout traversals while the
   hierarchical created-at/updated-at traversals still require a preview. Go's
   `rollout.BuildThreadItem` has no preview or session-meta requirement at all
   (it accepts any loadable rollout with lines), so Go's rollout listing already
   includes empty-preview threads - but it also includes them where Rust still
   requires a preview.

The Go-visible remainder of this commit is therefore the *narrowing* half: Go's
listing never requires a discoverable preview, and Rust's active listing still
does. Pinning that difference needs Rust's `PreviewFilter` semantics (which
orderings require a preview and which include empty) plus a listing predicate
seam in `session.ListRecords`; it is recorded here rather than approximated.

### Triage: `#48200` is a Rust-internal refactor

The commit moves `json_headers_to_http_headers` from
`codex-api/src/endpoint/responses_websocket.rs` into a new crate-wide
`responses_headers` module and updates the WebSocket endpoint's imports; the
conversion behavior (string/number/bool values accepted, invalid names and
unsupported values ignored) is unchanged. Go has no crate boundary to align and
no shared-module extraction to perform.

Recorded as remaining from the same area (already noted under sync217): Go's
wrapped-WebSocket error path folds the transport directly into the classified
error instead of building Rust's `ApiError::Transport(Http { .. })`, so it never
converts the event's JSON `headers` map into HTTP headers at all. Go has no
`jsonHeadersToHTTPHeaders` equivalent; that belongs with the wrapped-transport
mapping when it is ported.

### Verification (sixty-first pass)

- `go test ./network/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `dd9a7101d9`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dd9a7101d9`), so this
pass closes the Go-visible half of `#48199` rather than leaving it recorded as
needed.

Rust's listing rules are: the SQLite query requires a non-empty `preview` for the
plain active collection and exempts the archived collection, relation listings
(`include_empty_preview = relation_filter.is_some()`) and specific-section
listings (`matches!(section, Some(Some(_)))`); the flat rollout traversal
includes empty previews while the nested one still requires them.

### sync224: the state-backed listing preview rule (#48199's part 1)

- **`appserver/router.go`**: `stateThreadListRequiresPreview` mirrors
  `push_thread_filters_with_preview`, and `listRecordsFromState` drops a state
  row with an empty (or absent) preview when it applies. Because Go's
  state-backed path is the counterpart of Rust's SQLite listing, this is the
  same rule in the same place; the state-less path (store records plus the
  rollout scan) keeps Rust's flat-traversal behavior of including empty
  previews.

Tests: the exemption matrix over a state-backed router - the plain active
listing and an *unsectioned* listing hide the two empty-preview threads, a
specific-section listing and a relation listing keep the empty-preview thread,
and archiving one keeps it in the archived collection while the active
collection stays filtered. A second test ports Rust's
`thread_list_keeps_archived_threads_without_previews` /
`list_archived_threads_without_db_keeps_threads_without_previews` end to end:
two archived rollouts with empty previews are listed one page at a time
(`limit = 1`, cursor pagination) reporting an empty preview, for both
`useStateDbOnly` values, and the active collection stays empty.

Recorded as remaining for this lane: Rust's rollout-file *nested* traversal
still requires a preview (`PreviewFilter::RequirePreview`), so a nested rollout
with session metadata but no preview is hidden from Rust's file-based listing
while Go's state-less scan shows it. Go's recorder writes the nested layout, and
Go's existing `TestRouterThreadListUseStateDBOnlySkipsRolloutScan` pins the
current behavior (an active rollout with no user message is listed), so adopting
the nested rule is a deliberate listing narrowing that needs the layout
distinction modelled (`ThreadListLayout::NestedByDate` vs `Flat`) rather than a
one-line filter. The archive sort indexes of Rust's `0058` migration remain
pinned out by the certified migration baseline (the same constraint that already
records `0052_projects_recency.sql`), and are a pure-performance addition.

### Verification (sixty-second pass)

- `go test ./appserver/ -count=1` is green (141.0s).
- `go test ./parity/ -count=1` is green at `dd9a7101d9`.
- `go test ./... -count=1`: two whole-suite runs each hit one of the documented
  load-sensitive flakes in a different package
  (`tool.TestUnifiedExecRecoversDisconnectedExecServerSessionLikeRust`, then
  `appserver.TestOtelProviderReloadsAfterAccountChange`, both documented in
  `plan_2026_09_25.md`); each package passes as a full-package run and the test
  passes in isolation with `-count=3` (`ok ... 0.047s` / `ok ... 0.280s`).

---

## Follow-up pass (same day, twenty-third pull): `f5ffa46959` (three commits)

`git fetch` advanced `dd9a7101d9..f5ffa4695995bb6664fe8754ecb5ed2973f2801e`:

| Commit | Subject | Lane |
|---|---|---|
| `f5ffa46959` | Preserve queued output for observers during code-mode termination (#48207) | code-mode (core) |
| `4193f1acfb` | Add a keep-and-next action to the warnings viewer (#48206) | TUI |
| `7703f8b354` | Dismiss viewed TUI warnings when closing the viewer (#48205) | TUI |

### sync225: the static layer is re-pinned to `f5ffa46959`

`go test ./parity/` failed on the new checkout in one place: the TUI snapshot
total was 1327 rather than 1326, because #48206 added
`tui/src/app/snapshots/codex_tui__app__warnings_tests__warnings_partial_dismissal.snap`.
The manifest now records total 1327, `tui/src/app/snapshots` at 74 files, and the
new snapshot as a required entry (the two #48205/#48206 snapshots that changed in
place - `warnings_page` and `warnings_narrow` - need no count change).

### sync226: code-mode termination preserves produced output (#48207)

Rust's cell actor recorded the active yield signal separately from the observer,
so termination could disable yielding while keeping the observer attached and
ignore the queued `Started`/`Pending`/`YieldRequested` events, letting output the
script had already produced reach the observer in `CellEvent::Terminated`.

Go's runtime has no observer/event model: each cell's script runs one
`Engine.Execute` call whose result (and therefore its output) is published once.
The equivalent bug was that a termination racing that publish dropped the
produced output:

- **`codemode/session_runtime.go`**: each running cell now has an `engineState`
  (its engine, a `wake` channel and the published outcome). The script's
  goroutine publishes the outcome, and the terminating caller and the completion
  goroutine each consume it at most once (`takeEngineOutcome`), so exactly one
  path delivers it. `Terminate` captures the cell's wakeup channel first, and
  when the cell was still running it mirrors Rust's `begin_termination`:
  interrupt the engine, wait (bounded by `terminationDrainGrace`) for the
  publish and for the finishing path's append, then drain anything left. The
  completion path keeps a terminated cell's status and attaches the produced
  output to it (`AppendTerminatedOutput`) instead of failing the append and
  losing the output.
- **`codemode/codemode.go`**: `CellStore.AppendTerminatedOutput` adds output the
  runtime had already produced to a terminated cell without changing its status.

Tests: a termination regression mirroring Rust's (an immediate yield timeout, a
canceled yield signal, and output the script had already produced) asserting
that both the termination caller and a later observer receive the queued output
in the terminated event, exactly once, repeated 50 times so both select outcomes
(the script's completion racing the termination) are exercised. The existing
preempt/yield suites and the tool and turn suites that drive code mode stay
green.

### Verification (sixty-third pass)

- `go test ./codemode/ -count=5` is green.
- `go test ./codemode/... ./tool/ ./turn/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `f5ffa46959`.

### Triage: `#48205`/`#48206` need a warnings viewer Go does not have

Both commits change the TUI warnings viewer, and that component is absent from
Go entirely:

- Rust's `bottom_pane/warnings_view.rs` (`WarningsView` with a frozen
  `Vec<WarningEntry>`, per-entry visited cells, paging and an intentional
  `close()` that sends the visited entries), `app_event.rs`'s
  `AppEvent::OpenWarnings` / `CopyWarning` / the new
  `UpdateWarnings { transcript, dismissed }`, and
  `chatwidget.warning_display_state` (`dismissed`, `synced_cells`, `transcript`)
  have no Go counterpart: `git grep` finds no `WarningsView`, no
  `open_warnings`/`CopyWarning`/`UpdateWarnings` surface, and no `WarningEntry`.
- Go's `tui/chatwidget/warnings.go` carries only `WarningDisplayState` with
  `fallbackModelMetadataSlugs` and `startupConfigWarnings`;
  `tui/history_cell/startup_warnings.go` renders the startup warnings *cell*
  but nothing opens a viewer over the diagnostics.

So #48205 (dismiss viewed warnings on intentional close; the cancel key no longer
closes the viewer) and #48206 (a keep-and-next action that advances to the next
warning without closing) require building the viewer first: the frozen entry
list with its pagination, the visited tracking driven by the render path, the
keymap action and its footer hint, the app-level `UpdateWarnings` event with the
transcript-identity guard and sync invalidation, and the snapshot surfaces the
parity manifest pins (`warnings_page`, `warnings_narrow`, and the new
`warnings_partial_dismissal`). That is the recorded TUI lane; the L0 manifest is
already re-pinned for its snapshots so the static layer stays honest while the
component is unported.

---

## Follow-up pass (same day, twenty-fourth pull): `58670eeac4` (two commits)

`git fetch` advanced `f5ffa46959..58670eeac4b0bdb9fcb86929d8631c14aee0d9f6`:

| Commit | Subject | Lane |
|---|---|---|
| `d5355e95ef` | Keep Codex visible during external editor handoff (#48211) | TUI |
| `58670eeac4` | Isolate executable fixture copies in CLI tests on Linux (#48213) | CLI tests (Rust infra) |

### sync227: the static layer is re-pinned to `58670eeac4`

`go test ./parity/` failed on the new checkout in two places:

- **Fixture root**: `cli/tests` grew to 39 files for the new
  `cli/tests/support/executable.rs` (#48213), recorded in
  `rust_fixture_manifest_test.go`.
- **TUI snapshot manifest**: the total is 1328 and
  `tui/tests/suite/snapshots` grew to 5, with
  `all__suite__focus_palette__external_editor__external_editor_visible_handoff.snap`
  recorded as a required entry (#48211).

### Triage: `#48213` is Rust test infrastructure (N/A in Go)

The commit adds `cli/tests/support/executable.rs`, which copies an executable
fixture through a separate process on Linux so a sibling test's inherited
writable descriptor cannot cause `ETXTBSY`, and routes the CLI suites through
it. Go's CLI tests have no such helper and no `ETXTBSY` handling (`git grep` for
`ETXTBSY`/`copyExecutable` finds nothing), so there is no Go surface to change;
no stub is added.

### Triage: `#48211` rides Bubble Tea's terminal handoff in Go

Rust threads a `TerminalHandoff::{Restore, KeepScreen}` through its own
`Tui::with_restored`: the alternate screen's input modes are released without
leaving the screen (`AlternateScreen::release_input` with a new
`keyboard_active` flag), the last frame is repainted so a separate-window editor
leaves Codex visible, and the screen is re-entered before the editor returns.

Go's TUI does not own that handoff: it has no alternate-screen enter/leave
machinery, no keyboard-mode stack and no `restore_keep_raw` equivalent
(`git grep` for those surfaces finds only job-control and resize-reflow
helpers). The external editor runs through `bubbletea.Exec`
(`tui/tea/external_editor.go`'s `defaultExternalEditor`), so Bubble Tea releases
and restores the terminal around the editor and repaints on return; the
`KeepScreen` repaint has no Go code to modify. The observed end state - Codex's
screen is restored after the editor exits - is therefore provided by the
framework rather than by Go, and the Rust snapshot that pins it is recorded in
the manifest.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass closes the remaining Go-visible half of `#48200` together with the
safety-buffering wire it feeds.

Tracing the Rust oracle showed that Go's safety buffering was wrong in three
ways: it read the wrong wire key, it let the payload control a flag Rust marks
`#[serde(skip)]`, and it never read the two response headers that supply the
fallback model. It also only looked for safety buffering on `response.metadata`
events, while Rust checks every event.

### sync228: the safety-buffering wire, visibility and header treatment

- **`model/responses_headers.go`** ports `codex-api/src/responses_headers.rs`
  (#48200): `jsonHeadersToHTTPHeaders` converts a Responses event's JSON
  `headers` object with RFC 7230 token names and Rust's
  `HeaderValue::from_str` value rules (string/number/bool values only,
  unsupported or invalid names and values dropped).
- **`model/safety_buffering.go`** ports `codex-api/src/safety_buffering.rs`: the
  `x-codex-safety-buffering-enabled` and
  `x-codex-safety-buffering-faster-model` headers and
  `safetyBufferingTreatmentFromHeaders` / `...FromJSONHeaders`, which report
  whether a treatment exists and what model it carries.
- **`model/responses_stream.go`**:
  - the payload's faster model now comes from Rust's wire name `retry_model`
    first (the server's actual key, previously ignored), with Go's
    `faster_model`/`fasterModel` aliases kept;
  - `use_cases` and `reasons` are required arrays of strings, mirroring Rust's
    `SafetyBuffering` struct, so a malformed payload produces no event and an
    empty pair still does;
  - `show_buffering_ui` is no longer read from the payload (Rust marks it
    `#[serde(skip)]`) and is always set, so a delivered payload always asks the
    UI to show;
  - when the payload omits `retry_model`, the header treatment's model is used
    (`retry_model: null` suppresses that fallback, as in the Rust matrix);
  - the treatment is threaded from the HTTP response headers for a streamed
    response (`parseResponsesStreamWithTreatment`) and refreshed from each
    WebSocket event's JSON `headers` before the event is applied;
  - safety buffering is now detected on *every* event (Rust's
    `safety_buffering_for_event`), not only on `response.metadata`, so the
    top-level `safety_buffering` field on an ordinary event reaches the
    app-server and TUI.

Tests: the header treatment matrix (neither/enabled-only/model-only/both/blank
model), the JSON-header conversion matrix (invalid names, object/array/null
values, control characters and padded values dropped; the converted headers then
feed the treatment lookup), Rust's retry-model matrix (omitted falls back to the
header, `retry_model` wins, explicit null suppresses the fallback, a non-string
value is rejected), the required-array rules, the forced visibility flag for both
payload forms, and an end-to-end WebSocket test mirroring Rust's
`websocket_safety_buffering_event_controls_visibility_when_header_disables_it`
(the event header supplies `gpt-fast-header` and the payload cannot disable the
UI).

### Verification (sixty-fourth pass)

- `go test ./model/ -count=1` is green.
- `go test ./appserver/ ./app/ ./tui/... ./exec/ ./turn/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `58670eeac4`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass closes the rest of the Go-visible `#48174`/api_bridge usage-limit contract
that sync217 left recorded.

### sync229: the usage-limit error's evidence and the analytics HTTP-status rule

- **The 429's rate-limit headers reach the client.** Rust's `map_api_error`
  parses the response's rate-limit headers into
  `UsageLimitReachedError.rate_limits`, and the turn loop then refreshes the
  session snapshot from them (`core/src/session/turn.rs`'s
  `sess.update_rate_limits`). Go returned the 429 before ever emitting
  header-derived events, so a usage-limit failure left the client's rate-limit
  display stale. `emitUsageLimitErrorHeaderEvents` now emits the same
  header-derived rate-limit events on the error path - for a usage-limit error
  only, as Rust attaches them only there - for both the streamed and the
  non-streamed request paths (the stream path now builds its handler before the
  status check).
- **The HTTP `usage_not_included` classification.** Rust's 429 branch classifies
  `error.type == "usage_not_included"` as `CodexErr::UsageNotIncluded`, the same
  kind Go's streamed `response.failed` path already reported for that code;
  `responsesHTTPError` now does too, with no HTTP status on the detail (Rust's
  `http_status_code_value` reports none for it).
- **The analytics HTTP status now follows Rust's rule.** Rust's
  `CodexErrorDetails::http_status_code_value` only reports a status for
  `RetryLimit`, `UnexpectedStatus`, `ConnectionFailed`, `ResponseStreamFailed`
  and `FlexUnavailable` (429); every other detail - usage limits, quota,
  usage-not-included, context window, policy refusals - reports `null`.
  `turnAnalyticsErrorFieldsFromAPIError` previously copied the error's HTTP
  status for every kind, so Go's turn and compaction analytics reported 400/429
  where Rust reports `null`. `analyticsErrorHTTPStatus` now applies the same
  rule, and the three test expectations that pinned the old behaviour
  (`ErrorInvalidRequest`, `ErrorMisalignmentPolicyViolation`, `ErrorBioPolicy`
  all carrying 400) were updated to the Rust-faithful `null`, with the reason
  recorded at each site.

Tests: the runner-level usage-limit case (a 429 `usage_limit_reached` with
primary-window headers emits one rate-limit event carrying the window while the
error keeps its classification; an unrelated 429 emits none), the
`usage_not_included` classification matrix, and the existing analytics matrices
under the corrected status rule.

### Verification (sixty-fifth pass)

- `go test ./model/ -count=1` is green.
- `go test ./appserver/ ./app/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `58670eeac4`.

### sync230: the wrapped-WebSocket usage limit refreshes the snapshot too

Rust's wrapped-WebSocket error mapping builds `ApiError::Transport(Http { status,
headers, body })`, so `map_api_error` reads the event's `x-codex-*` JSON headers
and a WebSocket-delivered usage limit refreshes the session's rate-limit snapshot
exactly like the HTTP path. Go classified the event but dropped its headers.

- **`model/responses_agent.go`**: `websocketEventHTTPHeaders` converts the event's
  JSON `headers` through the shared `jsonHeadersToHTTPHeaders`, and the
  usage-limit branch emits the header-derived rate-limit events before returning
  the classified error.

Test: a WebSocket `error` event with `status: 429`,
`error.type: usage_limit_reached` and primary-window headers returns the
usage-limit error with its window and emits exactly one rate-limit event carrying
the window's percent, duration and reset time.

### Verification (sixty-sixth pass)

- `go test ./model/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass re-verified two recorded items against the current source and landed the
WebSocket half of the turn-state contract.

### sync231: the WebSocket turn state

Rust's ModelClientSession keeps one turn state per turn: `connect_websocket`
records the handshake response's `x-codex-turn-state`, `ResponseEvent::turn_state`
records the one a `response.metadata` event carries in its JSON headers (the
`OnceLock` keeps the first value), and every later request's client metadata
sends it (`client.rs`'s `turn_state.get()`).

- **`model/responses_agent.go`**: after a successful WebSocket dial the runner
  remembers the turn state from the handshake response headers, and for a
  `response.metadata` event it remembers the state from that event's JSON
  `headers` (through the shared `jsonHeadersToHttpHeaders`). Go previously
  learned the state only from HTTP response headers, so a WebSocket-delivered
  state was dropped and the next dial in the turn carried nothing.

Tests: the handshake case (the first dial fails before any event, so the retry
dial must carry the handshake's state, and a later metadata event cannot replace
it - Rust's first-wins `OnceLock`), and the metadata case (a handshake without
the header followed by a `response.metadata` carrying it, so the next dial in the
turn sends the event's state).

### Correction: the custom-profile metadata carveouts are not a divergence

sync218 recorded that Rust applies `append_default_read_only_project_root_subpath_if_no_explicit_rule`
to custom `[permissions]` profiles while Go does not. Re-checking the current
source shows that record was wrong:

- `FileSystemSandboxPolicy::restricted(entries)` is a plain constructor
  (`protocol/src/permissions.rs`), and the four appends live only in
  `FileSystemSandboxPolicy::workspace_write(...)`, which is used by
  `BUILT_IN_WORKSPACE_PROFILE` (`core/src/config/permissions.rs`) and the legacy
  `SandboxPolicy::WorkspaceWrite` conversion -
  the two sites Go already mirrors (`builtinPermissionProfileBuilder(":workspace")`
  and `rustPermissionProfileWireFromPermissionProfile`).
- A custom profile compiles through
  `compile_resolved_permission_profile`, which starts from
  `restricted(Vec::new())` and appends only the profile's own entries, and
  `with_materialized_project_roots_for_path_uris` materializes existing entries
  only - so Rust's custom profiles carry no metadata carveouts either, exactly
  like Go's.

One unpinned nuance stays recorded: a custom profile with `extends = ":workspace"`
inherits the parent's *special* entries in Go (the builder clone), while Rust
projects the builtin into TOML and drops entries with `skips_missing_path()`
(which the metadata carveouts use). No Rust test pins that case, and Go's
behaviour is the stricter one, so it is left as is rather than weakened.

### Triage: `#48073`'s credential-broker snapshot grant has no consumer yet

Rust's unified-exec launch composes `internal_permissions` from the metrics
sidecar plus `snapshot_read_permissions(snapshot_path, profile, sandbox_cwd)`,
consulted only while `CREDENTIAL_BROKER_ACTIVE_ENV_KEY == "1"`: the replayed
snapshot gets a read-only grant unless the launch's profile can already read it
with the sandbox cwd. Go has the sidecar half (`tool/shell_executor.go`'s
`internalPermissions`) but not the snapshot half, because Go does not replay a
snapshot for brokered launches at all: `MaybeWrapShellLCWithSnapshot` returns the
command unchanged when the broker is active, with a comment recording that Rust
additionally wraps brokered captures and replays protected environment files
around them.

Landing the grant alone would widen the sandbox's readable set for a snapshot the
launch never reads, so the whole brokered-snapshot program stays recorded
together: the creation-side broker handling (`core/src/shell_snapshot.rs`'s
env-key insert/remove and the `active == "1"` branch), the wrap's brokered rules
(`maybe_wrap_shell_lc_with_snapshot`'s `-c` acceptance, session-shell reuse and
`-lfc`/`-fc` flags), and then the grant with its merge into the launch's internal
permissions.

### Verification (sixty-seventh pass)

- `go test ./model/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass starts the recorded `codex-history` program: the retained-context data path
that gives sync215's model, sync219's renderer and sync222's prompt option a
production route.

Rust's retained evidence is host-owned: the session records it when the host
accepts an input, each compaction persists the bounded snapshot in its
`CompactedItem`, and both the transport and the Guardian sections read it back
from the checkpoint plus later evidence. Go had none of that - no
`retained_context` on the compacted record, no session-owned snapshot, and no
reviewer consumer - so the retained user-instruction section could never render.

### sync232: the retained-context checkpoint, store and review wiring

- **`rollout`**: `CompactedEvent` gained `retained_context` (Rust
  `CompactedItemWire::retained_context`),
  `Recorder.AppendCompactedWithContext` writes a checkpoint with the snapshot
  (with `AppendCompacted` kept as the no-snapshot form), and
  `CompactedRetainedContext(lines)` reads the newest checkpoint's snapshot - the
  evidence a resumed thread restores before later evidence is replayed.
- **`appserver/retained_context.go`**: a per-thread store,
  `RuntimeRouter.retainedContextForThread`, seeds the thread's context from its
  newest checkpoint and then records every accepted user message the thread's
  history still holds, in order. Re-recording is idempotent (Rust's id-based
  dedup), so the store can be refreshed on every resolution, a restarted process
  rebuilds the same evidence from the durable checkpoint, and a compaction can
  never drop it from the reviewer's view. Resolutions return a copy so a
  concurrent review renders while later resolutions keep recording; the thread's
  in-memory evidence is dropped with its runtime
  (`forgetThreadRetainedContext`), matching Rust's `Arc` drop.
- **`appserver/turn_runtime.go`**: `appendRuntimeCompacted` persists the store's
  snapshot with every checkpoint, so the evidence survives the history rewrite
  the same compaction performs.
- **`appserver/guardian_reviewer.go` / `agent_runtime.go`**: the model reviewer
  gained a `retainedContext` callback and passes the resolved snapshot into every
  prompt build (`state.BuildPromptWithOptions.RetainedContext`), so the review
  now renders the retained user-instruction section between the sender evidence
  and the transcript, as Rust's registry order requires.

Tests: the rollout round trip (a checkpoint with a snapshot reads back, and one
without carries none), the store (accepted user messages become ordered evidence,
assistant text never does, repeated resolution dedupes by message id, an unknown
thread has none), compaction survival (the history is replaced, the checkpoint
carries the snapshot, and a fresh router rebuilds the original instruction), and
the reviewer prompt (the marked section with the order label and role prefix, and
no section when a thread has no evidence).

Remaining for this program (recorded): the other producers (retained assistant
messages, verified answers recorded when `request_user_input` is accepted, sender
deliveries) and the sparse `RetainedContextEvent` rollout item Rust writes
between checkpoints; the source identities (`RetainedSource`, the
`retained_source`/`guardian_sources`/`guardian_source_order_guidance` harness
metadata) that carry delivery proof and drive redelivery/deduplication; the
`inherited_user_message` provenance for adopted parent instructions, which is why
the reviewer still resolves retained evidence only for non-subagent threads (a
worker keeps the root-conversation section rather than mislabeling inherited
instructions as its own); and the `CodexHarnessMetadata` type itself, since Go
persists harness metadata as an untyped map.

### Verification (sixty-eighth pass)

- `go test ./appserver/ ./rollout/ ./session/ ./compact/ ./exec/ ./app/ ./parity/ -count=1` is green.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-fifth pull): `63eb71c9da` (one commit)

`git fetch` advanced `58670eeac4..63eb71c9da95a6e4deaf34ee9340c88a7cac9a65`:

| Commit | Subject | Lane |
|---|---|---|
| `63eb71c9da` | Preserve late result metadata for truncated code-mode calls (#48222) | executed-tool-calls (core) |

`go test ./parity/ -count=1` is green at the new pin: the commit edits existing
sources and test suites, so no fixture root, inventory, critical-file hash,
snapshot total or precomputed export changes.

This lane was the oldest recorded Go-visible gap in the repo. Go's recorder
(`turn/executed_tool_calls.go`, last synced at `sync135`/#46712) kept attached
calls only in the *pending* inventory and dropped them on commit, so it modelled
neither Rust's `RetainedToolCalls` binding nor the origin/runtime cell split it
depends on. #48222 is the third Rust pass over that model (#47714/#47758/#47957
preceded it), so this pass lands the binding itself together with #48222's rules
instead of re-deriving them.

### sync233: the retained output binding and late truncated result metadata

- **Retained bindings (`retainedToolCallBinding`).** `AttachPendingToPrompt`
  now names the output it attaches to (`executedToolCallGroupAttachment`), and
  `CommitAttachment` moves that inventory into `retained[outputCallID]` instead
  of dropping it. Every later request re-emits the binding
  (`attachRetainedBinding`), so an output that was attached while its cell was
  still running, or before its result arrived, keeps its inventory instead of
  losing it on the next sampling request. Bindings carry Rust's two identities
  (`originCallID`, the exec/wait call ID written as the item's metadata
  `cell_id`; `runtimeCellID`, the Code Mode handle used for input matching) and
  the `callIndexByID` / `truncatedCallIndexByID` / `lateTruncatedIndices`
  indices Rust uses to address one call.
- **Late result backfill.** `RecordToolResultMetadata` first checks the pending
  inventory, then the retained bindings: a full-argument call found by
  `callIndexByID` is replaced in place, and a truncated call found by
  `truncatedCallIndexByID` is updated only when exactly one binding claims the
  ID, recording the index in `lateTruncatedIndices`. A contradictory second
  claim leaves the result unattributed (Rust
`record_tool_result_metadata`'s `candidates.next().is_some()` guard).
- **Ambiguity invalidation.** `invalidateUntrustedTruncatedBindings` runs before
  any retained calls are cloned into a request: a duplicated output, or an input
  that no longer identifies the same execution, marks the whole runtime cell's
  attribution ambiguous, clears its late-backfilled evidence
  (`clearLateTruncatedMetadata`) and stops admitting more. The same clearing
  happens on origin/cell invalidation, on a repeated nested invocation ID
  (`seenNestedIDs`), and when a new nested call arrives after the dispatch gate
  closed.
- **Cell lifecycle.** `StartCell` (the exec path) always observes the runtime
  handle, so a reused runtime ID releases the previous execution's pending
  calls, output mappings and late bindings (`dropReusedCell`); `RegisterCell`
  (the wait path) registers into an already started cell. `FinishCell` marks the
  dispatch gate closed and keeps a cell whose truncated calls are still eligible
  for backfill. A repeated nested invocation ID now replaces its pending attempt
  instead of consuming the pending budget twice, and truncation/overflow now
  records Rust's `CellCompletion::Incomplete` (a distinct `incomplete` flag)
  rather than revoking the cell's identity, so a truncated call's binding stays
  valid while the inventory is reported incomplete.
- **Metadata cell id corrected.** The item's metadata `cell_id` now names the
  originating exec/wait call, not the runtime handle, matching Rust
  (`seen_ids.rs`'s "Metadata cell_id identifies the originating exec call, not a
  runtime handle", `compact_tests.rs`'s `start_cell(&cell, "exec")` →
  `set_tool_call_cell_id("exec")`, and
  `code_mode_terminated_empty_cell_has_complete_tool_inventory`, where the
  `call-2` wait output reports `cell_id == "call-1"`). Go previously wrote the
  runtime handle; the recorder, dispatcher and capacity tests were updated with
  the Rust citations.

Tests mirror Rust's `request_metadata_tests.rs` additions: late metadata across
retries and compaction for two truncated calls in one cell; the eight-case
matrix over `result_before_wait` / `call_before_wait` / `close_before_wait` with
a following second wait; the ambiguity matrix (duplicate output, wrong input,
wrong sibling input, repeated ID, reused cell, dispatch after close); and the
reused-runtime-cell / repeated-pending-ID capacity behaviour.

Recorded as remaining for this lane: Rust's `retry_cache`
(`ExecutedToolCallCache`) that lets a failed sampling attempt replay the same
snapshot without re-draining the cell (`attach_to_prompt`'s `retry_cache`
parameter), the metadata-budget re-derivation in
`bound_executed_tool_calls_for_prompt_prioritizing_recent`
(Go bounds in `model.BoundExecutedToolCallsForPrompt` instead), and Rust's
`UsageErrorBody`-style request-level shedding of oversized late metadata before
it reaches the wire. Rust captures raw result metadata and bounds it at request
time; Go bounds it at capture (`model.NewToolResultMetadata`), so an oversized
late snapshot becomes the omission marker earlier than Rust's request budget
would shed it.

### Verification (sixty-ninth pass)

- `go test ./turn/ -count=1` is green, including the new #48222 regression suites.
- `go test ./model/ ./tool/ ./exec/ ./codemode/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `63eb71c9da`.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-sixth pull): `5f3180c793` (one commit)

`git fetch` advanced `63eb71c9da..5f3180c79374bd9440db733e23ead8bc9f62d64b`:

| Commit | Subject | Lane |
|---|---|---|
| `5f3180c793` | Preserve model and access program pairs during compaction (#48224) | turn-level access programs (core) |

### sync234: the static layer is re-pinned to `5f3180c793`

`go test ./parity/` failed on the new checkout in two places:

- **Fixture root**: `core/tests/suite` grew to 259 files for the new
  `core/tests/suite/compact_program_tests.rs` (#48224), recorded in
  `rust_fixture_manifest_test.go` (with the file added to the root's required
  list).
- **Vendored precomputed export**: `app-server-exports-stable.json.zst` changed
  (155387 → 155425 bytes) and was re-vendored from the checkout. Decompressing
  the old and new exports shows exactly one difference, in the **internal**
  schema's `RolloutLine.json`: the previous-turn settings definition now carries
  `cyber_access_program` next to `model` and `comp_hash`. The stable
  `json_schema` and `typescript` sets are byte-identical, so `turn/start` is
  unchanged. `appserver/schema_test.go`'s artifact pin moved to
  `8691e8ec516b44dcd86e495290a88e10c4447321e25baab4001304410aa05bc4`.

### Triage: `#48224` refines a lane Go has not ported

The commit has three functional halves, all downstream of **a turn carrying a
cyber access program**, which Go does not model:

- `session/turn.rs` / `session/rollout_reconstruction.rs` persist and restore
  `PreviousTurnSettings.cyber_access_program` (the new field in
  `history/src/compaction_resume_metadata.rs`).
- `maybe_run_previous_model_inline_compact` restores the previous turn's program
  after `with_model`, so compaction uses that turn's model/program pair.
- `compact.rs` passes the selected program into the local compaction prompt
  (`Prompt.cyber_access_program`).

Go's evidence of absence, checked against the current source:

- `appserver/protocol.go`'s v2 `TurnStartParams` has no `cyberAccessProgram`
  member (its `DaybreakEnabled` comment records the distinction explicitly:
  "It does not select a turn's cyber access program"), so no turn ever selects
  one.
- `runtimeTurnContext` / `rollout.TurnContextRecord` / `TurnContextSettings`
  carry only `model` and `comp_hash`; there is no program to persist or restore.
- `runPreviousModelInlineCompact` rebuilds the previous model's turn context
  without a program, and the local compaction prompt has no
  `cyber_access_program` field: `git grep` finds the access-program surface only
  in the catalog metadata (`model/catalog.go`, `model/api.go`) and the v2 suite
  manifest (`cyber_access_program`, owner "safety, appserver"), never in request
  building.
- The Responses request body has no `access_programs` field and there is no
  `cyber_access_program::for_auth` (ChatGPT-auth-gated) equivalent, so Rust's
  rejected model/program pair cannot be constructed by Go today.

`appserver/appserver_v2_suite_manifest_test.go` already records the
`cyber_access_program` module as a tracked but unported surface. The whole lane
stays recorded together - the v2 `turn/start` selection, the ChatGPT-auth gate
and request-body `access_programs`, the turn context field, the rollout
persistence this pass re-pinned, and the compaction program - because landing
only the persistence half would write a field nothing selects or reads. The
static layer is re-pinned now so the vendored export and fixture manifest stay
honest while the component is unported (the same treatment as the warnings
viewer in #48205/#48206).

### Verification (seventieth pass)

- `go test ./parity/ -count=1` is green at `5f3180c793`.
- `go test ./appserver/ -run 'Precomputed|Schema|Export' -count=1` is green.
- `go test ./... -count=1` recorded below.

### Full-suite confirmation (this pass)

- `go test ./... -count=1` is green across all 99 packages, zero failures, at
  `5f3180c793` and with `590dd1f0` pushed to `origin/main`.

## Unlanded work after this pass

The recorded lanes still open, in the order the workflow's step 6 would take
them:

1. The turn-level cyber access program lane (`#48224`'s subject, plus the v2
   `turn/start` selection, the ChatGPT-auth gate, the request-body
   `access_programs`, and the turn-context/rollout field).
2. The `codex-history` retained-context producers (assistant messages, verified
   answers, sender deliveries, source identities, typed `CodexHarnessMetadata`)
   left open by `sync232`/`sync215`.
3. The TUI warnings viewer (`#48205`/`#48206`) and the remaining TUI lanes.
4. The `#48174` usage-limit `plan_type`/`resets_at` fields.
5. The brokered-snapshot program recorded under `#48073`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `5f3180c793`), so this
pass closes recorded lane 1: the turn-level cyber access program. Go had the
catalog half of `#44893` (the `availableAccessPrograms` metadata and the
app-server summary) but nothing selected a program for a turn, so every
downstream field - including `#48224`'s model/program pair preservation - had no
Go surface.

### sync235: the turn-level cyber access program (#44893 and #48224)

- **The request body (`model`).** `model/access_programs.go` ports Rust
  `core::cyber_access_program::for_auth` and `codex_api::AccessPrograms`: the
  body field is `access_programs: {"cyber": "<core name>"}`, it is sent only for
  an authenticated human ChatGPT account, and an absent program preserves the
  backend's automatic behavior. The gate is Rust's
  `CodexAuth::is_chatgpt_auth` (`AuthMode::has_chatgpt_account`: `chatgpt`,
  `chatgptAuthTokens`, `personalAccessToken`), which is *not* the pre-existing
  `model.authHasChatGPTAccount` predicate (that one also accepts Agent Identity,
  i.e. `uses_codex_backend`), so the new gate is its own function. Known
  programs are the three catalog values; an unknown selection is dropped, since
  Rust's v2 enum is validated while deserializing. `responsesAgentRequest`
  gained `access_programs`, both runners (WebSocket and HTTP/SSE) populate it
  from `AgentRequest.CyberAccessProgram`, and both WebSocket payload builders
  emit it, mirroring Rust's `ResponsesApiRequest` /
  `ResponseCreateWsRequest` pair.
- **The turn selection (`turn`).** `TurnStartParams` gained the app-server wire
  selection `cyberAccessProgram` (camelCase: `standard`, `daybreakBlue`,
  `daybreakRed`) with `CoreValue()` performing Rust's explicit
  `impl From<…>` mapping onto the core snake_case program, plus an internal
  `CoreCyberAccessProgram` (json "-") for internally started turns. The turn
  loop's `AgentLoopRequest` and Rust's `Prompt` counterpart
  (`model.AgentRequest`) carry the core value.
- **The provider gate (`appserver`).** `appCyberAccessProgramForTurn` mirrors
  Rust's turn-context construction: the selection is recorded only when the
  turn's provider is the OpenAI provider. The regular turn's sampling request
  carries it; a review turn and the guardian reviewer's request do not, matching
  Rust (`session/review.rs` sets `cyber_access_program: None` for the review
  turn context and `guardian/review_session.rs` builds the reviewer from
  `new_default_turn`).
- **Persistence and restore.** `rollout.TurnContextRecord` gained
  `cyber_access_program` (snake_case, omitted when unset) and
  `TurnContextSettings` now returns it, so `runtimePreviousTurnSettings` reports
  the recorded model/hash/program triple. `recordRuntimeTurnContext` writes the
  resolved program with the model, which is what makes `#48224`'s
  resume/fork/rollback/checkpoint replay keep the pair.
- **Compaction.** `runtimeCompactRequest` gained an explicit program: an attempt
  that fixes it keeps it verbatim (including an explicitly absent one), while a
  nil value resolves the thread's latest turn context. The previous-model
  pre-turn compaction passes the previous turn's recorded program
  (`previousModelCompaction.PreviousProgram`) and the fallback retry passes the
  current turn's, mirroring `maybe_run_previous_model_inline_compact`'s
  `previous_model_turn_context.cyber_access_program = previous_turn_settings.…`;
  the post-turn, pre-turn and mid-turn auto compactions pass the current turn's,
  and `agentCompactRunner` puts the resolved program on the compaction
  `AgentRequest` (Rust's `Prompt.cyber_access_program` in `compact.rs`).
- **Subagents.** A spawned or continued child turn inherits the initiating
  turn's program (`runtimeAgentController.cyberAccessProgram` feeding the spawn,
  send-input and message param builders), gated again by the child's own
  provider, mirroring the `cyber_access_program` field Rust passes through
  `SpawnAgentOptions` / `SendInputOptions`.

Tests: the auth-gate matrix (chatgpt / chatgptAuthTokens / personalAccessToken
versus api-key / agent-identity / bedrock / no auth, unknown and absent
programs) plus an end-to-end HTTP request-body check that only a ChatGPT account
gets `access_programs`; the wire/core mapping matrix and the params JSON round
trip; the provider gate matrix including the inherited core value; the
compaction pair (fixed, explicitly absent, and recorded-turn resolution) and the
`agentCompactRunner` request; the spawned-child inheritance; and the rollout
round trip for a present and an absent program.

Recorded as remaining for this lane: Rust's daemon interrupted-turn snapshot
(`app-server-transport/src/daemon_recovery.rs`'s `InterruptedTurn.
cyber_access_program`, restored by `daemon_continuation`/`daemon_snapshot`). Go
has no `RecoverySnapshot`/`codex-interrupted-v1` mechanism at all (`git grep`
finds neither), so there is nothing to persist into yet; the whole daemon
recovery snapshot stays recorded with its lane. Go's `exec` CLI and TUI keep
sending no program, matching Rust (`exec/src/lib.rs` and
`tui/src/app_server_session.rs` both pass `None`).

### Verification (seventy-first pass)

- `go test ./appserver/ ./turn/ ./model/ ./rollout/ ./exec/ ./compact/ -count=1` is green.
- `go test ./appserver/ -count=1` is green in isolation (139.9s).
- `go test ./... -count=1` is green (99 packages, zero failures). One earlier
  99-package run reported the load-sensitive
  `appserver.TestOtelProviderReloadsAfterAccountChange`, which is green in
  isolation, in the full appserver package run, and in the later full-suite runs
  (it waits on a localhost OTLP collector while every package builds and runs in
  parallel).

---

## Follow-up pass (same day, twenty-seventh pull): `4b9e0cc77f` (two commits)

`git fetch` advanced `5f3180c793..4b9e0cc77f264399385903b2a169b4bfd8f3d262`:

| Commit | Subject | Lane |
|---|---|---|
| `10fd75bba7` | Extract Responses failure parsing into a dedicated module (#48229) | codex-api SSE (core) |
| `4b9e0cc77f` | Suppress console windows for local Windows MCP servers (#48238) | MCP launcher (Windows) |

`go test ./parity/ -count=1` is green at the new pin: the added files live under
`codex-api/src/sse`, which no tracked fixture root, inventory, critical-file
hash or snapshot total covers, so the static layer needs no re-pin.

### sync236: the failed-response decode contract (#48229)

`#48229` is a Rust-internal move (`sse/responses.rs` -> `sse/responses_error.rs`)
that preserves the mappings, fallback messages and - the part with a Go
counterpart - the **malformed-payload behavior**, now pinned by
`malformed_failed_responses_use_stream_error`. Rust decodes the `response.error`
object into a struct whose every field carries a declared type (`code`,
`message`, `plan_type`, `resets_at` as `Option<String>`/`Option<i64>`,
`misalignment` as a free `Value`, `type` as `Option<String>`), so a wrong-typed
value *anywhere* - including in a field the classification never reads - makes
the decode fail and the event degrade to `ApiError::Stream("response.failed
event received")`. The Flex check (`Value::get("code") == "flex_unavailable"`)
runs before that decode, so a Flex failure with an otherwise malformed payload
still terminates.

Go's classifier decoded into `responsesAgentAPIErrorBody`, whose `Code` is
untyped (`any`) and whose field set lacks `plan_type`/`resets_at`, and it also
accepted a *top-level* `error` object that Rust never reads for a
`response.failed` event. So Go classified two shapes Rust degrades:
`{"response":{"error":{"code":42}}}` and
`{"response":{"error":{"code":"server_is_overloaded","plan_type":42}}}`.

- **`model/responses_stream.go`**: `responseFailedError` now reads only
  `response.error`, decodes it through the new strict
  `responsesFailedErrorBody` (Rust's field set and types), returns the stream
  sentinel when the decode fails, and keeps the Flex shape ahead of the decode
  (`flexUnavailableErrorBody`). The code mapping is unchanged except that the
  Rust fallback messages are now applied on this path too: a missing or blank
  `cyber_policy` message becomes `CyberPolicyFallbackMessage` (Rust's
  `cyber_policy_message`; Go only had it on the HTTP path) and a missing
  `invalid_prompt` message becomes "Invalid request." (a present blank message is
  kept, as Rust's `unwrap_or_else` does). `parseMisalignmentDetails` now takes
  the raw JSON so both callers share it. The unclassified arm still reports
  `Retryable` with no retry delay, matching Rust's `_` arm.

Tests mirror Rust's new matrix (missing event payload, `null`/array/empty
`response`, `null`/non-object `error`, non-string `code`, malformed `plan_type`,
malformed `resets_at`, non-string `message`/`type`), the Flex-first ordering, and
the two fallback messages; the existing code, retry-delay, quota and
misalignment suites stay green unchanged.

### sync237: console suppression for local Windows MCP servers (#48238)

Rust's local stdio MCP launcher now sets `CREATE_NO_WINDOW` on the shared command
wrapper for Windows - so both a direct program and a batch shim launched through
`cmd.exe` run without a console window - and, because the suspended-spawn helper
*replaces* creation flags, re-applies `CREATE_SUSPENDED | CREATE_NO_WINDOW` for
the Job Object path. Go set no flags at all: `mcp/stdio_command_windows.go`'s
constructor only filled `CmdLine` for the `cmd.exe` shim (the direct path set
nothing), and `startMCPStdioProcess` set only `CREATE_SUSPENDED`, so a local MCP
server flashed a console window over the terminal.

- **`mcp/stdio_command_windows.go`**: a shared
  `suppressMCPStdioConsoleWindow` sets `CREATE_NO_WINDOW` on both branches,
  mirroring the code-mode host's existing `suppressCodeModeHostConsoleWindow`
  (#48138) and Rust's `command.creation_flags(CREATE_NO_WINDOW)`.
- **`mcp/stdio_process_windows.go`**: `suspendMCPStdioForJobAssignment` sets
  `CREATE_SUSPENDED | CREATE_NO_WINDOW`, mirroring Rust's replacement call after
  `prepare_suspended_spawn`. The uncontained fallback keeps the constructor's
  flag, as Rust's `build_command()` rebuild does.

Tests: the flag matrix for a direct program and a `cmd.exe` shim, the
suspension-plus-suppression pair, and an end-to-end check mirroring Rust's
Windows regression test - the launched helper (direct and through a `.cmd` shim)
reports through `MCP_TEST_CONSOLE_STATE_FILE` that `GetConsoleWindow()` is null,
i.e. the child really has no console, not merely the right flags.

### Verification (seventy-second pass)

- `go test ./model/ -count=1` is green.
- `go test ./mcp/ ./appserver/ ./turn/ ./rollout/ ./exec/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `4b9e0cc77f`.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `4b9e0cc77f`), so this
pass closes the remainder of the `#48174` lane that `sync217`/`sync229` recorded:
Rust's `UsageErrorBody` also models `plan_type` and `resets_at`, and the
user-facing message for a usage limit is not the response body's message at all.

### sync238: the usage-limit evidence and the recovery copy (#48174)

Rust's `CodexErr::UsageLimitReached(UsageLimitReachedError)` displays the error
through a dedicated `Display` (`#[error("{0}")]`), and `turn.rs` reports
`CodexErr::to_string()` as the turn's `ErrorEvent.message`. So the client never
sees the 429 body's message: it sees copy rebuilt from
`plan_type`/`resets_at`/`promo_message`/`rate_limit_reached_type` and the active
rate-limit snapshot's `limit_name`. Go kept the body message and only the window.

- **Evidence (`codexapi`).** `APIError`/`APIErrorDetails` gained the remaining
  `UsageLimitReachedError` fields - `UsageLimitPlanType`, `UsageLimitResetsAt`,
  `UsageLimitLimitName`, `UsageLimitPromoMessage`,
  `UsageLimitRateLimitReachedType` - alongside the existing
  `UsageLimitWindowMinutes`, and `NewAPIErrorWithDetails`/`Details()` copy them.
- **Parsing (`model/usage_limit.go`).** `usageLimitErrorBody` mirrors Rust's
  `UsageErrorBody` with its declared field types (`code`/`type`/`plan_type` as
  strings, `resets_at` as an integer, `limit_window_minutes` free), so a
  malformed value makes the decode fail exactly where Rust's
  `serde_json::from_str::<UsageErrorResponse>` does and the 429 falls through to
  the generic retry classification. The headers supply the promo message
  (`x-codex-promo-message`), the reached type
  (`x-codex-rate-limit-reached-type`, validated against Rust's `FromStr`), and
  the active limit's name (`x-codex-active-limit`, defaulting to `codex`, read
  from `x-<limit>-limit-name`), mirroring `parse_rate_limit_for_limit`.
- **Copy.** `usageLimitReachedMessage` ports `UsageLimitReachedError::Display`
  branch for branch: the named-limit copy (excluding `codex`/`gpt-reserve`
  case-insensitively), the four workspace copies, the promo copy, the
  Plus/admin/Free-or-Go/Pro/Enterprise plan copies and the generic copy, with
  `retry_suffix`/`retry_suffix_after_or`. The retry timestamp ports
  `format_retry_timestamp` (`3:05 PM` for the same local day, otherwise
  `Oct 5th, 2026 9:07 AM` with Rust's `day_suffix`); `now_for_retry` is mirrored
  as an injectable package function because the branch depends on the local date.
- **Display.** `(*APIError).Error()` for `ErrorRateLimit` is now the copy alone:
  Rust's usage-limit display carries no prefix, and the app-server reports
  `err.Error()` as the turn error message (`TurnError{Message: err.Error()}`).
  Both the HTTP path (`responsesHTTPError`) and the wrapped-WebSocket path
  (`websocketUsageLimitError`, which now derives the evidence from the event's
  `error` object and its JSON headers, as Rust's `map_api_error` does with the
  whole event text and converted headers) share the same evidence assembly.

Tests: the copy matrix (named limit with and without a reset, `codex` and
`GPT-Reserve` falling through, the four workspace copies, the promo copy, every
plan group, `rate_limit_reached` falling through, and the unknown/absent plan
arms), the other-day timestamp format with the ordinal-suffix matrix, the HTTP
evidence round trip (plan type, reset instant, window, limit name, promo and
reached type, plus the no-prefix display and the empty-evidence case), the
malformed-body fall-through matrix, and the WebSocket evidence round trip.

Recorded as remaining for this lane: Rust parses the 429 body **once** for the
`usage_limit_reached`, `usage_not_included` and quota branches, so a body it
cannot decode skips all three; Go's strict decode currently gates only the
usage-limit branch (the sibling branches keep the lenient
`responsesAgentErrorBody`). Rust's `get_error_message_ui` also truncates every
UI error message to `ERROR_MESSAGE_UI_MAX_BYTES` (2 KiB) and gives several other
kinds their own display strings (for example `ServerOverloaded` renders
"Selected model is at capacity. Please try a different model."), which Go's
`APIError.Error()` does not match yet; the image-generation extension's use of
`usage_limit.resets_at` and the TUI usage notice belong to the TUI lane.

### Verification (seventy-third pass)

- `go test ./model/ ./codexapi/ -count=1` is green.
- `go test ./appserver/ -count=1` is green in isolation (139.5s), and
  `TestRuntimeAgentControllerSpawnUsesCapturedStepSettingsLikeRust` is green
  5/5 in isolation.
- `go test ./... -count=1` is green (99 packages, zero failures) on the
  re-run; one earlier 99-package run reported that same spawn test, which is
  load-sensitive (it reads the child turn's captured step settings while every
  package builds and runs in parallel) and green in isolation and in the full
  package run.

---

## Follow-up pass (same day, twenty-eighth pull): `dfdb40cd0b` (one commit)

`git fetch` advanced `4b9e0cc77f..dfdb40cd0b72dfba3293db5c7c441232e8ef1a60`:

| Commit | Subject | Lane |
|---|---|---|
| `dfdb40cd0b` | Prevent Windows daemon launches from retaining launcher stdio (#48272) | app-server daemon (Windows) |

`go test ./parity/ -count=1` is green at the new pin: the commit edits existing
sources and existing test suites, so no fixture root, inventory, critical-file
hash, snapshot total or precomputed export changes.

### sync239: the detached daemon does not retain the launcher's stdio (#48272)

Rust's `PidBackend::start` now spawns managed Windows processes through
`spawn_without_inheriting_stdio`, which clears `HANDLE_FLAG_INHERIT` on the
launcher's own stdin/stdout/stderr before `CreateProcess` (tolerating a null,
already-closed or `ERROR_INVALID_HANDLE` handle). Without it a detached daemon
keeps the launcher's output pipes open, so a caller that captured them waits for
EOF forever after the launcher exits.

Go reaches the same end state through `os/exec` rather than by touching the
launcher's handles: `syscall.StartProcess` always passes an explicit
`PROC_THREAD_ATTRIBUTE_HANDLE_LIST` containing exactly the child's configured
stdio (plus `SysProcAttr.AdditionalInheritedHandles`), so unrelated inheritable
handles are never propagated. The spawn in
`appserverdaemon/pid_process_windows.go` is now the named entry point
`startDetachedCommand`, which documents that guarantee and is what the Windows
regression test drives.

`appserverdaemon/daemon_stdio_windows_test.go` ports Rust's
`captured_stdio_closes_while_child_is_alive`: a launcher role marks its own
stdout/stderr handles inheritable (as Rust's test does), starts a detached child
through `startDetachedCommand` with the managed launch's stdio configuration
(NUL stdin/stdout, the configured stderr log), records the child's pid, prints
its own output and exits; the test asserts the captured launcher output reaches
EOF while the child is still alive, keeps writing to its configured log, and is
then killed. The test was checked for sensitivity: pointing the child's
stdout/stderr at the launcher's handles makes it fail with
"the launcher's captured stdio stayed open", which is the Rust defect.

Triage: the commit's other half raises timeouts in Rust's
`core/tests/suite/mcp_optional_startup_grace.rs` so slow runners finish the turn
before the pending server's startup timeout. That is Rust test infrastructure
with no production surface; Go's own optional-startup-grace tests (`mcp/api_test.go`
with `MCP_OPTIONAL_STARTUP_BARRIER`) are green and carry their own timings, so
nothing is ported.

### Verification (seventy-fourth pass)

- `go test ./appserverdaemon/ -count=1` is green, including the new Windows
  regression test.
- `go test ./mcp/ ./parity/ -count=1` is green at `dfdb40cd0b`.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dfdb40cd0b`), so this
pass starts closing the retained-context producers `sync232` recorded. Rust's
`Session::record_retained_context` writes a **sparse rollout fact** alongside the
in-memory history (`RolloutItem::RetainedContext`), and the first producer that
feeds it is `request_user_input`'s verified answer.

### sync240: the verified-answer producer and the sparse retained-context line

- **The rollout wire (`rollout`).** `AppendRetainedContext` persists one
  `retained_context` line and `RetainedContextEvents` reads them back, both using
  Rust's `RolloutItemWire { type, payload }` shape (the payload is the
  internally tagged `verified_answer` body Go already modelled in `retainedctx`).
  The same reader/writer split exposed a real writer-side divergence: Go wrote
  `turn_context` under its own `turn_context` key, which Rust's tagged enum
  cannot read, so `AppendTurnContext` now writes Rust's `payload` key (the reader
  already accepted both). Both are covered by a wire-shape round trip.
- **The producer (`tool`, `turn`, `appserver`).** `RequestUserInputHandler`
  captures the host's accepted answers exactly as Rust's handler does: per
  requested question, the non-blank submitted answers joined by `\n`, with the
  question text extended by one `\n{label}: {description}` line per *selected*
  option; unanswered, blank-answer and timed-out responses contribute nothing.
  The capture reaches the appserver through `CoreHandlerOptions`/
  `turn.ToolRegistryOptions` as `VerifiedAnswerRecorder`, which
  `RuntimeRouter` installs per turn only when the `guardian_approval` feature is
  enabled (Rust's `if turn.config.features.enabled(Feature::GuardianApproval)`).
- **The store (`appserver`).** `recordRetainedContextEvent` mirrors
  `Session::record_retained_context`: bound the fact, refresh the thread's
  derived user messages (so the fact keeps the acceptance order Rust captured),
  reserve the next acceptance order, record it, and append the sparse rollout
  line. A resumed thread restores its newest checkpoint and then replays the
  sparse facts, so a verified answer survives a restart without a compaction.
- **The reviewer prompt (`state`).** Rust feeds the review's
  `render_verified_answers(..).fragments` into the composer as
  `trusted_user_answers`, which `TrustedUserAnswersSection` renders. Go had the
  renderer (sync219) and the section builder but never connected them, so
  `BuildPromptWithOptions` now renders the `>>> TRUSTED USER ANSWERS START/END`
  section from the retained snapshot, in Rust's section order (after the retained
  user instructions).

Tests: the rollout wire round trip for a `retained_context` line plus the
`payload`-key turn-context shape; the capture matrix (selected options, blank and
absent answers, the single-answer fallback, a timed-out response) and the
core-handler plumbing; the store (evidence, rejection and the rollout fact, with
the answer ordered after the accepted instructions), the restart replay, the
reviewer's trusted-answers section, and the feature gate.

Recorded as remaining for this program: the other producers (retained assistant
messages and sender deliveries), the source identities `RetainedSource` carries
with the typed `CodexHarnessMetadata` (`retained_source`,
`inherited_user_message`, `sender_user_messages`, `user_input_order`), the
`inherited_user_message` provenance, and the remaining Rust re-derivation
narrowing: Go derives user-message acceptance orders from history position, which
can tie with a replayed fact when a message was accepted after that fact.

### Verification (seventy-fifth pass)

- `go test ./state/ ./tool/ ./rollout/ ./turn/ -count=1` is green.
- `go test ./app/ -count=1` is green in isolation, as is
  `TestAppServerListenOffRemoteControlUsesStateDBWhenAvailable` (3/3).
- `go test ./... -count=1` is green (99 packages, zero failures). Two earlier
  99-package runs flaked in `app` (the state-DB migration subprocess hit its
  deadline under full parallel load); both are green in isolation and in the
  later full run.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dfdb40cd0b`), so this
pass continues the retained-context producers with the assistant arm of Rust's
`ContextManager::record_retained_message`, which is what makes the reviewer's
assistant-context evidence live.

### sync241: the retained assistant-message producer

Rust retains assistant output as **untrusted context**, bounded by
`guardian::GUARDIAN_MAX_ROOT_MESSAGE_TOKENS` (900 tokens), and
`guardian-context`'s `retained_instructions.rs` renders it inside the retained
user-instruction section. The order is the one the session reserved for that
message (`Session::reserve_assistant_message_order`), so the assistant text sits
between the instruction it answers and later user input; an *unsequenced* source
is deliberately left to legacy transcript selection instead of being recorded.

- **`appserver/retained_context.go`**: the derivation now records assistant
  messages (`sessionItemIsAssistantMessage`, the predicate the memory-citation
  scan already uses) as assistant evidence, with the text bounded by
  `utils.TruncateText(.., TokensPolicy(900))` (mirroring
  `guardian_truncate_text`, including its token-estimated bound) and `Complete`
  false when that bound had to shorten the original. The order comes from
  `AssistantMessageOrder` when the message was already recorded and is otherwise
  reserved from the thread's counter, so history order — and therefore Rust's
  acceptance order — is preserved while repeated resolutions stay idempotent. It
  also skips **compaction output** (Rust's `metadata.compaction_output`), which
  the session item carries as its compacted-history kind, so a summary is no
  longer retained as user or assistant evidence.
- **`retainedctx`**: `AssistantMessageOrder` exposes the recorded order of an
  assistant message so a re-derivation reuses it instead of advancing the
  counter.

Tests: the derivation retains the assistant text between the two instructions,
keeps it out of the user family, bounds an oversized message while marking it
incomplete, drops the compaction summary, and keeps the same entries and orders
across a second resolution; the reviewer prompt then renders
`assistant: Understood.` in the retained section together with the
omitted-assistant-context notice and without the truncated text; and
`AssistantMessageOrder` reports only a sequenced record's order.

Recorded as remaining for this program: the `delivered_assistant_message`
producer (the host-captured bounded assistant text bound to a tool call's output,
which Rust records at the call's position), the sender-delivery producer
(`capture_sender_user_messages` + the `sender_user_messages` harness metadata),
the `retained_source`/`inherited_user_message` provenance with the typed
`CodexHarnessMetadata`, and the acceptance-order narrowing described in sync240.

### Verification (seventy-sixth pass)

- `go test ./retainedctx/ ./state/ ./appserver/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dfdb40cd0b`), so this
pass takes the first item of the error-display lane recorded under sync238: the
user-facing copy the app-server reports for each error kind.

### sync242: the error-kind display copy

`turn.rs` reports `CodexErr::to_string()` as the turn's `ErrorEvent.message`, and
`CodexErr` delegates to `CodexErrorDetails`'s `#[error(..)]` strings. Go's
`(*codexapi.APIError).Error()` carried its own prefixes and short labels for most
kinds, so the app-server showed text Rust never produces.

- **`codexapi/error.go`**: the display now mirrors Rust's `CodexErrorDetails`
  strings - `ContextWindowExceeded` ("Codex ran out of room in the model's
  context window. Start a new thread or clear earlier history before
  retrying."), `QuotaExceeded` ("Quota exceeded. Check your plan and billing
  details."), `UsageNotIncluded` ("To use Codex with your ChatGPT plan, upgrade
  to Plus: https://chatgpt.com/explore/plus."), `ServerOverloaded` ("Selected
  model is at capacity. Please try a different model."), and `Stream` ("stream
  disconnected before completion: {0}") - and the message-only kinds
  (`InvalidRequest`, `CyberPolicy`, `BioPolicy`, `MisalignmentPolicyViolation`)
  now display their message without a Go-only prefix. `Retryable` uses the same
  stream copy, because Rust maps `ApiError::Retryable` onto `CodexErr::Stream`.
  `RateLimit` (the usage-limit copy) and `RateLimitExceeded` ("rate limit
  exceeded: {0}") already matched.
- The parallel telemetry labels in `codexapi/response_debug.go`
  (`TelemetryResponseAPIErrorMessage`) are a different Rust contract
  (`response-debug-context`'s short labels) and stay as they are.

Tests: a display matrix pinning Rust's copy per kind, and the streamed
context-window failure test now asserts Rust's message.

Recorded as remaining for this lane: Rust's `UnexpectedStatus`
(`UnexpectedResponseError`'s "unexpected status {status}: {body}" plus its
`url`/`cf-ray`/`request id`/auth-error suffixes, behind `user_message`) and
`ConnectionFailed`/`ResponseStreamFailed` displays, which Go models as its own
`ResponsesAPIError`/transport messages.

### Verification (seventy-seventh pass)

- `go test ./codexapi/ ./model/ -count=1` is green.
- `go test ./appserver/ ./app/ ./tui/... ./exec/ ./session/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dfdb40cd0b`), so this
pass closes the recorded source-identity item: Rust persists the host-observed
source (delivery proof) and the acceptance order **with the recorded item**
(`ContextManager::record_annotated_items`), and replays them so a resumed thread
restores the same identity instead of minting a new revision.

### sync243: the recorded item's retained provenance

- **`retainedctx`**: `HarnessMetadata` gained `retained_source` (Rust's
  `CodexHarnessMetadata::retained_source`) next to the existing
  `user_input_order`/`inherited_user_message`/`sender_user_messages` members, with
  Rust's wire names; `UserMessageOrder` exposes a recorded instruction's order the
  way `AssistantMessageOrder` already did.
- **`appserver/retained_context.go`**: the derivation is now split into the
  record a history item produces (`retainedRecordForSessionItem`: family, bounded
  text, completeness, and the identity/turn the recorded source captured) and the
  recording itself (`recordRetainedSessionItem`), which honours the item's
  persisted `user_input_order`/`inherited_user_message`, restores a recorded
  revision when the replay reproduces the same source and completeness (Rust's
  `replay_annotated_item`), and narrows completeness with
  `retained_source.complete`. Seeding the live context is now one helper
  (`retainedLiveContextLocked`) shared by the resolver, the sparse-event recorder
  and the annotation.
- **`appserver/runtime_router.go`**: `runtimeAppendItems` annotates the items it
  is about to persist (`annotateRetainedHarnessMetadata`), mirroring
  `record_annotated_items`: the retained record is captured, and the source plus
  acceptance order are merged into the item's `harness_metadata` sidecar so they
  round-trip through the rollout and a restarted process restores the identical
  revision and order.

Tests: appending an instruction and its reply writes both sources (role, turn,
complete, revision) and the two orders into the harness metadata; a fresh router
restores the *same* revisions and orders from the rollout; and an item whose
recorded source was captured incomplete stays incomplete with the recorded
revision. `UserMessageOrder`/`AssistantMessageOrder` are covered directly.

Recorded as remaining for this program: the `delivered_assistant_message` and
sender-delivery producers, the `inherited_user_message` *producer* (the evidence
model and the metadata flag exist; nothing marks adopted parent instructions
yet), the content-kind completeness rule for instruction items, and the
`guardian_sources`/`guardian_source_order_guidance` metadata.

### Verification (seventy-eighth pass)

- `go test ./retainedctx/ ./appserver/ ./state/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Follow-up pass (same day, twenty-ninth pull): `25270df261` (one commit)

`git fetch` advanced `dfdb40cd0b..25270df2615eb4da5b9d4a9a392226933fb096c5`:

| Commit | Subject | Lane |
|---|---|---|
| `25270df261` | Keep TUI reconnect attempts running until the shared deadline (#48318) | TUI reconnect |

`go test ./parity/ -count=1` is green at the new pin: the commit edits three
existing files (`tui/src/app/reconnect.rs`, its active-reconnect test and the
reconnect integration test) with no added fixtures or snapshots, so no fixture
root, inventory, critical-file hash or snapshot total changes.

### Triage: `#48318` refines a TUI component Go has not ported

The commit replaces the TUI's fixed five-attempt schedule (`[0, 1, 2, 4, 8]`)
with `[0, 1, 2, 4]` followed by an eight-second delay repeated until the shared
120-second deadline expires, so a longer temporary failure can still recover.
That schedule lives in `tui/src/app/reconnect.rs`'s `reconnect`, which also owns
the connect attempt, the history/inventory hydration, the draft restoration and
the notification routing the commit's integration test exercises.

Go has no TUI reconnect routine at all, checked against the current source:

- `tui/app/app_server_events.go`'s `HandleAppServerDisconnectedDecision` returns
  a `FatalExitMessage`, i.e. Go's TUI surfaces a disconnect and exits; there is no
  retry, no shared deadline and no resume target.
- `git grep` finds no reconnect attempt schedule, no `beginReconnect`/
  `reconnectFailed`, no resume-target retry and no draft restoration on reconnect;
  the only disconnect surface is the app-server event kind and the notice text
  (`event_dispatch.go`'s "Disconnected from the app server").

The whole TUI-reconnect lane therefore stays recorded together - the reconnect
target and deadline, the retry loop with its delay schedule (this commit's
change), the connect/hydrate attempt, the `thread/resume` recovery, the draft and
notification restoration, and the reconnect snapshots - because landing only the
schedule would be dead code. This is the same treatment as the warnings viewer
(#48205/#48206) and the Rust-side TUI handoff (#48211), and matches the
workflow's ordering of TUI alignment last.

### Verification (seventy-ninth pass)

- `go test ./parity/ -count=1` is green at `25270df261`.
- `go test ./codemode/ -count=3` is green, including
  `TestSessionRuntimeTerminationPreservesProducedOutputLikeRust`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `25270df261`), so this
pass takes the next recorded item of the retained-context program: the
instruction item's authorization predicate and completeness proof, which decide
whether the reviewer may treat a retained instruction as authorization.

### sync245: the instruction family's authorization and completeness rules

Rust's `ContextManager::record_retained_message` only retains a user message that
passes `is_user_authorization_message` (its harness content classifications must
not show contextual or media content), bounds the retained text by
`GUARDIAN_MAX_ROOT_MESSAGE_TOKENS`, and marks the record complete only when every
content entry is classified as genuine user text (`user.*`, excluding
`UserGoalUpdate::OMITTED_OBJECTIVE_KIND`). Go recorded every user message, kept
the text unbounded and marked it complete unconditionally, so an unverified or
oversized instruction reached the reviewer as complete authorization evidence.

- **`appserver/retained_context.go`**: `sessionItemResponseItemFieldsFromItem`
  reads the persisted response item's content types and
  `internal_chat_message_metadata_passthrough.content_item_kinds` (the same
  classification the turn runtime writes for user content);
  `sessionItemIsUserAuthorizationMessage` ports Rust's predicate, including its
  conservative treatment of missing, empty or count-mismatched classifications and
  its media/unknown kinds; `retainedInstructionComplete` ports the completeness
  proof (aligned classifications, every kind `user.*` and not the omitted-objective
  placeholder, and every entry textual). The instruction arm now bounds its text
  with the guardian budget and marks a truncated or unverified instruction
  incomplete, while assistant context keeps its own rule.

Tests: the classification matrix (properly classified text complete; legacy
without classifications retained but incomplete; misaligned classifications and
the omitted-objective placeholder incomplete; a contextual fragment not retained
at all; a media-preparation failure still authorization evidence; image content
incomplete), the oversized-instruction excerpt, and an end-to-end case where an
unclassified instruction reaches the reviewer as the unavailable-instructions
notice instead of rendering as authorization.

Recorded as remaining for this program: the `delivered_assistant_message` and
sender-delivery producers, the `inherited_user_message` producer, and the
`guardian_sources`/`guardian_source_order_guidance` metadata.

### Verification (eightieth pass)

- `go test ./appserver/ -count=1` is green (154.5s).
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `25270df261`), so this
pass lands the recorded `inherited_user_message` provenance, which `agent_runtime.go`
recorded in a comment: a delegated subagent's history carries the parent's adopted
instructions, and without the provenance the reviewer could only be given a
thread's own evidence, so a worker kept the root-conversation section instead of
its own.

### sync246: the inherited-instruction provenance

Rust marks every copied conversational message when a MultiAgent V2 spawn forks the
parent's history (`spawn.rs`'s `retain_forked_item`), so persist-and-resume can
never recapture the parent's authorization as the child's own; the fork also drops
the parent's sender evidence and, for positions that belong to the parent's
counter, the parent's acceptance order. Rust then only adopts inherited messages
for a thread that is not a non-root agent
(`ContextManager::retain_inherited_user_messages`), which is why a worker's
retained context holds its own input but not the parent's.

- **`appserver/retained_context.go`**: `markInheritedUserMessages` ports the fork
  provenance (mark each copied user/assistant message, drop
  `sender_user_messages`, and clear `user_input_order` for assistant and tool
  positions); `retainedRecordForSessionItem` gained the adoption gate, skipping an
  inherited item unless the thread is a root or standalone, and the derivation
  reads that from the thread record (`runtimeRecordIsSubagent`, the Go counterpart
  of `Source::is_non_root_agent`).
- **`appserver/agent_controller.go`**: the V2 fork marks the copied items right
  after `ForkRecord` (and after the existing inherited-reminder filtering), so the
  provenance is persisted with the child's history.
- **`appserver/agent_runtime.go`**: `guardianRetainedContextForTurn` no longer
  returns nil for a subagent: with the provenance in place a worker resolves its
  own local instructions, while the parent's adopted ones stay out of its
  evidence, matching Rust's worker behaviour.

Tests: the fork marking (inherited flag, kept user order, cleared assistant order,
dropped sender evidence, untouched non-message items); the adoption matrix (a root
thread adopts the copied prefix with the inherited scope, a worker omits it); and a
worker that holds its own instruction keeps that evidence as local.

Recorded as remaining for this program: the `delivered_assistant_message` and
sender-delivery producers, and the
`guardian_sources`/`guardian_source_order_guidance` message metadata (which needs
Go's prompt composition to carry per-message metadata).

### Verification (eighty-first pass)

- `go test ./appserver/ -count=1` is green (154.0s on the re-run); one earlier
  package run flaked in `TestRuntimeRouterTurnStartElevatedSandboxDoesNotPersistProjectTrustLikeRust`
  at TempDir cleanup ("sessions: The directory is not empty", a background rollout
  writer racing the removal), which is green 3/3 in isolation.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `25270df261`), so this
pass closes another recorded item of the retained-context program: the derived
records now carry Rust's input origin and message phase.

### sync247: the derived records' origin and phase

Rust records each retained message with `origin: UserInputOrigin::from_message(item)`
(only a user message whose harness classifications are exactly
`user.heartbeat` is a scheduled heartbeat) and with the message's `phase`. The
origin drives `RetainedContext::record_user_message`'s heartbeat coalescing: an
unchanged scheduled heartbeat reuses the existing evidence instead of adding a
duplicate, while changed instructions still record a new version. Go's derivation
set neither field, so a scheduled heartbeat accumulated one retained record per
delivery and the persisted wire shape differed from Rust's.

- **`appserver/retained_context.go`**: `sessionItemResponseItemFieldsFromItem` now
  also reads the response item's role and phase, and
  `retainedRecordForSessionItem` sets `Origin` through
  `retainedctx.UserInputOriginFromMessage` for instruction items (falling back to
  the session item's role when the response item is unavailable) and carries the
  phase on every record; `normalizeOrigin` keeps the serialized default identical
  to Rust's `User`.

Tests: a scheduled heartbeat delivered three times (twice unchanged, once with new
instructions) yields exactly two retained records, each with the heartbeat origin
and the message phase, and an ordinary instruction keeps the default origin.

Recorded as remaining for this program: the `delivered_assistant_message` and
sender-delivery producers. The first needs the post-hook delivery text to travel
from the tool dispatch (where Go applies input-rewriting hooks) to the recorded
item, and `turn.ToolResponseItem` has no harness-metadata channel today, so that
plumbing has to land before the retention rule; the
`guardian_sources`/`guardian_source_order_guidance` metadata needs Go's prompt
composition to carry per-message metadata.

### Verification (eighty-second pass)

- `go test ./appserver/ -count=1 -timeout 600s` is green (155.3s).
- `go test ./... -count=1 -timeout 900s` is green (99 packages, zero failures).
- `go test ./... -count=1` is green (99 packages, zero failures). One earlier
  99-package run flaked in `codemode` at that concurrent yield/terminate test
  ("iteration 31: observer response = … want terminated"), which is green 3/3 in
  isolation and in the final full run.

---

## Follow-up pass (same day, thirtieth pull): `e72da2b538` (four commits)

`git fetch` advanced `25270df261..e72da2b53805894878023d01949a25a082e0a5cb`:

| Commit | Subject | Lane |
|---|---|---|
| `c9e2520707` | Preserve tool metadata for OpenAI provider endpoint overrides (#48344) | model client (core) |
| `09a3d2108f` | Display reconnect commands on a separate line (#48350) | CLI + TUI exit summary |
| `ca41ed337f` | Show turn tips while working and after completion in the TUI (#48352) | TUI transcript |
| `e72da2b538` | Stabilize skill catalogs across executor availability changes (#48353) | skills extension (core) |

The parity layer re-pins for the pull: `core/src/client.rs` moves to the
`e72da2b538` hash (the #48344 edit), the golden fixture root gains #48353's
`cloud_skills_across_executor_readiness` snapshot (259 → 260 files), and the TUI
snapshot total gains #48352's turn-tip placement snapshot (1328 → 1329 files,
`tui/src/app` 74 → 75). `go test ./parity/ -count=1` is green at the new pin.

Ordering follows the workflow's step 4 (core first, TUI last): #48344 and #48353
are core and land first, then #48350 and #48352 (TUI).

### sync248: the runtime-only internal-metadata provider grant (#48344)

Rust's destination check used to be the *only* rule for sending internal tool
metadata, so the built-in OpenAI provider stripped raw tool-result metadata (and
MCP attribution) as soon as it used a custom endpoint. #48344 makes the grant
explicit and provider-owned: `ModelProviderInfo::include_internal_metadata` is a
runtime-only opt-in (`#[serde(skip)]`/`#[schemars(skip)]`, default false) that
`create_openai_provider` sets to true, and `ModelProvider::include_internal_metadata`
returns the grant **or** the existing first-party HTTPS destination check. Both
the HTTP and WebSocket Responses request paths read it.

- **`model/provider_info.go`**: `ProviderInfo.IncludeInternalMetadata` is the
  runtime-only grant, tagged `json:"-"` so it is neither loaded from nor written
  to serialized provider configuration and stays false for a provider received
  through remote/configuration; `CreateOpenAIProvider` sets it true (only the
  built-in OpenAI provider, matching Rust); `CreateOSSProviderWithBaseURL` and
  every other built-in keeps it false; `ToAPIProvider` carries it onto the
  resolved `APIProvider`.
- **`model/responses_agent.go`**: `includeInternalMetadata` returns the resolved
  provider's grant or the destination check
  (`toolResultMetadataDestinationAllowed`), and `filterToolResultMetadataForDestination`
  sends metadata unchanged when either holds. Both the WebSocket
  (`responsesWebsocketPayload`) and HTTP (`responsesAgentAPIRequest`) request
  builders already call that filter, so the grant covers both paths.
- **`model/responses_agent.go` (`cloneAPIProvider`)**: the runner clones the
  resolved provider in `NewResponsesAgentRunner`, and the clone constructed a
  fresh `APIProvider` without the new field, which would have dropped the grant
  on the production path and silently re-enabled destination-only filtering for
  an overridden OpenAI endpoint. The clone now carries
  `IncludeInternalMetadata`.

Triage: Rust's `include_internal` also gates the internal `client_metadata`
entries (`CodexResponsesMetadata::client_metadata`), whose only internal entry is
`mcp_attribution`. Go has no MCP-attribution client-metadata field at all
(`grep -i mcp_attribution` across the Go tree is empty), so there is nothing to
gate there; when that lane is ported it must read this same predicate. The commit's
`rmcp_client.rs` change is the Rust regression proving attribution is now sent to
the OpenAI provider — infrastructure for the unported feature, not ported.

Tests (`model/executed_tool_call_metadata_test.go`): the filter matrix (a provider
grant keeps raw metadata at an overridden endpoint; a provider without it strips
at an untrusted endpoint; the destination check keeps it at a first-party
endpoint); the grant pipeline (`CreateOpenAIProvider` carries it,
`ToAPIProvider` preserves it, a configured provider does not gain it, JSON
marshalling never emits `include_internal_metadata`, and decoding a payload that
sets it leaves it false); and the production wiring
(`TestToolResultMetadataGrantSurvivesRunnerConstruction`) builds a runner through
`NewResponsesAgentRunner` from the resolved built-in OpenAI provider at an
overridden endpoint and asserts the clone kept the grant and keeps sending
metadata, while a runner built from an ungranted configured provider at the same
endpoint strips it.

### Verification (eighty-third pass)

- `go test ./model/ -count=1` is green (22.0s).
- `go test ./parity/ -count=1` is green (11.4s) at `e72da2b538`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`), so this
pass works the remaining commits of the thirtieth pull, starting with the
reconnect-command layout.

### sync249: the reconnect command on its own line (#48350)

Rust prints `To reconnect, run:` and then the resume command on its own indented
line in disconnect exit summaries (`tui/src/app/exit_summary.rs`, and the CLI's
`format_exit_messages` test), replacing the single `Reconnect: {command}` line.

- **`tui/app/event_dispatch.go`**: `AppExitInfo.FormatExitMessages` renders the
  disconnect command as two lines (`To reconnect, run:` then `"  " + command`).
  Go's `AppExitInfo` is the exit-summary model the TUI tests exercise; it has no
  separate CLI-side summary, and the TUI snapshot fixtures are pinned through the
  parity manifest (unchanged here - #48350 only edits existing `.snap` files).

Tests: `TestAppExitInfoFormatsDisconnectGuidance` asserts the two-line layout.

### Triage: `#48353` stabilizes the cloud skill catalog Go has not ported

The commit caps cloud skills at three quarters of the metadata budget, persists
that allocation across disconnects/compaction/resume, rebalances only when it
lets omitted filesystem entries fit, and emits a short "Skills update" when an
unchanged selected-environment catalog returns while its full text remains in
history. Every one of those behaviors lives in Rust's skills-extension
world-state machinery, which Go has not ported:

- Go renders skills directly, not through world-state sections. Rust's
  `render_combined_available_skills` allocates a three-way split (executor,
  cloud, host) under `CatalogBudgetAllocation` (`ext/skills/src/world_state_catalogs.rs`),
  while Go's `prompt.RenderCombinedAvailableSkills` (`prompt/skills_render.go`)
  shares one budget between exactly two catalogs (host + executor/selected
  capability).
- Go has no cloud skill catalog. The only cloud-skill surface is
  `config.Config.CloudSkillsEnabled()` (the #47074 flag); Rust's
  `cloud_catalog_contribution` needs `SkillProviders::has_cloud_provider()` and
  `SkillsThreadState::cloud_catalog_snapshot()`, and `git grep -i
  "cloud_provider\|cloud_catalog"` over the Go tree is empty.
- Go has no `ContextContributor`/`WorldStateContributionInput` abstraction
  (`git grep -l "ContributeWorldState\|ContextContributor"` is empty) and no
  per-section persisted snapshot. The commit's allocation persistence, the
  `retain_world_state_after_compaction` hook, and the previous-state diff with
  the legacy and retained-fragment matchers
  (`ext/extension-api/src/contributors/world_state.rs: previous_world_state`,
  `with_legacy_matcher`, `with_retained_fragment_matcher`) all depend on it,
  while Go re-renders and concatenates the catalog bodies into the instructions
  every turn (`appserver/turn_runtime.go`'s `instructionsWithSkillsContextForTurn`).
- With no cloud catalog the allocation math is a no-op in Go (cloud cost 0 leaves
  the full budget to filesystem skills), so landing only the allocator would be
  dead code, and the executor "unchanged catalog returns" update needs the
  world-state fragment matcher Go lacks.

The whole `ext/skills` world-state catalog lane therefore stays recorded
together: the cloud provider and its thread-state catalog snapshot, the
three-bucket section rendering, the previous-state diffing with legacy and
retained-fragment matchers, and this commit's allocation. This is the same
treatment as the TUI reconnect lane (#48318), the warnings viewer and the
Rust-side TUI handoff.

### Triage: `#48352` adds TUI turn tips Go has not ported

The commit adds a working tip (after 30s under the working status), completion
tip cadence (third turn start onward, spaced by three, two per session), and the
whole rendering/selection plumbing. Go has the tooltip *text* only:

- `tui/tooltips.go` ports the startup/announcement catalog (`DefaultTooltips`,
  `PickTooltip`, `ParseAnnouncementTipTOML`), but not the keybinding-substitution
  renderer the tip reuses: Rust's `tooltips::render_tooltip` /
  `tooltip_templates` / `render_tooltip_lines` (the `{key:context.action}`
  substitution) have no Go counterpart.
- No `TurnTips` lifecycle state, `TurnTip` history cell, `TipSurface` or
  `AppEvent::TurnTipReady` exist in Go (`git grep -i "turn_tip\|TipSurface"` is
  empty), and none of the render hooks - `owned_transcript.rs`'s
  `turn_tip`/`render_with_turn_tip_space`, the `bottom_pane`/`chatwidget`
  signature changes, `transcript_view/layout` tipping space or `footer_state` -
  are present.
- Go's transcript view has no tip spacer, no acknowledgment, and `tui/app` has
  no per-turn tip cadence.

The whole TUI turn-tips lane stays recorded with the rest of TUI alignment
(last per the workflow's ordering).

### Verification (eighty-fourth pass)

- `go test ./tui/app/ -run Exit -count=1` is green.
- `go test ./tui/... -count=1` and `go test ./parity/ -count=1` are green at
  `e72da2b538`.

## Remaining after this pass

Unchanged recorded lanes: the retained-context producers
(`delivered_assistant_message`, sender deliveries), the
`guardian_sources`/`guardian_source_order_guidance` metadata, the
`guardian-context` composition sections, the TUI reconnect lane, the TUI
warnings viewer, the Rust-side TUI handoff, and the smaller #48174 / permissions
/ brokered-snapshot items. Added this pass: the `ext/skills` world-state catalog
and cloud-skill lane (#48353) and the TUI turn-tips lane (#48352).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`), so this
pass closes the strict-decode item recorded under sync242.

### sync250: the 429 body is parsed once for all three classifications

Rust's `api_bridge` decodes the 429 body once as `UsageErrorResponse`
(`codex-rs/codex-api/src/api_bridge.rs`); a body it cannot decode skips the
`usage_limit_reached`, `usage_not_included` and quota branches alike and falls
through to the generic retry classification. Go gated only the usage-limit
branch on the strict decode, while the quota and usage-not-included branches
used the lenient `responsesAgentErrorBody`, so a body with (for example) a
non-integer `resets_at` or a non-string `plan_type` still classified as
quota/usage-not-included in Go but not in Rust. The restructure also moves the
lenient `flex_unavailable` check ahead of the strict decode, matching Rust's
`parse_flex_unavailable` ordering.

- **`model/responses_agent.go`**: a single `if statusCode == 429` block now
  checks `code == "flex_unavailable"` on the lenient error object first, then
  decodes once with `decodeUsageLimitErrorBody` and dispatches
  `usage_limit_reached` / `usage_not_included` / quota from that strict body.
  The now-unused `responsesIsQuotaError` helper is removed; the quota code set
  is still the shared `responsesQuotaErrorCodes`.
- **`model/usage_limit.go`**: `usageLimitErrorBodyIsQuotaError` ports Rust's
  quota arm (`error_type == "insufficient_quota"` or one of the quota codes)
  over the strict `usageLimitErrorBody`.

Tests (`model/responses_quota_error_test.go`): the sibling-branch fall-through
matrix (malformed `resets_at`/`plan_type` on usage-not-included and quota bodies
yields the generic `ResponsesAPIError`), a decodable quota body with an extra
declared field still classifying, and `flex_unavailable` winning when the body
also carries `usage_limit_reached`.

Recorded as remaining for this lane: Rust's `get_error_message_ui`
(`ERROR_MESSAGE_UI_MAX_BYTES`, 2 KiB) has no consumer outside its own tests in
the current upstream tree (`git grep` finds only the definition and
`error_tests.rs`), so there is no UI behavior to align yet; it stays unported
until a caller appears. The `UnexpectedStatus`/`ConnectionFailed` display
strings recorded under sync242 remain the lane's other item.

### Verification (eighty-fifth pass)

- `go build ./...` is green.
- `go test ./model/ -count=1` is green (22.2s), `go test ./codexapi/ -count=1`
  is green.
- `go test ./parity/ -count=1` is green at `e72da2b538`.

## Remaining after this pass

Unchanged recorded lanes: the retained-context producers
(`delivered_assistant_message`, sender deliveries), the
`guardian_sources`/`guardian_source_order_guidance` metadata, the
`guardian-context` composition sections, the TUI reconnect lane, the TUI
warnings viewer, the Rust-side TUI handoff, the `ext/skills` world-state catalog
and cloud-skill lane (#48353), the TUI turn-tips lane (#48352), and the smaller
permissions / brokered-snapshot items. For the error lane only the
`UnexpectedStatus`/`ConnectionFailed` displays and the consumer-less
`get_error_message_ui` helper remain.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass re-checked the permission-profile item recorded under sync218 with evidence,
because it claimed a live security divergence.

### sync251: the custom-profile metadata protection is already present

sync218 recorded: "Go's custom-profile path (`compileConfigPermissionProfile`)
applies no defaults at all, so a custom profile that only writes
`:workspace_roots` leaves the metadata names writable - stricter in Rust than in
Go." That runtime claim is **refuted** by the current code:

- Rust's runtime protection is `protected_metadata_names_for_writable_root`
  (`codex-rs/protocol/src/permissions.rs`), which marks a
  `PROTECTED_METADATA_PATH_NAMES` entry protected for a writable root whenever
  the matching policy cannot write `<root>/<name>` - it is name-based and
  independent of the profile's declared entries.
- Go derives the same protection from the resolved policy's writable roots:
  `SandboxPolicy.GetWritableRootsWithCWD` ->
  `sandbox/policy.go`'s `buildWritableRoots`, which sets `ReadOnlySubpaths`
  (`protectedSubpaths`) and `ProtectedMetadataNames`
  (`{".git", ".agents", ".gcode", ".aws"}`) for **every** root, and
  `WritableRoot.IsPathWritable` refuses `PathContainsProtectedMetadataName`.
  The platform enforcers all consume that: `sandbox/seatbelt.go`
  (`GetWritableRootsWithCWD`), `sandbox/linuxsandbox/linux.go`'s
  `protectedReadOnlySubpaths` (iterates `p.writableRoots(cwd)`), and
  `sandbox/windowssandbox/resolved_permissions.go`'s deny subpaths.
- The one real difference is declarative: Rust's `workspace_write` pushes the
  carveouts as `skip_missing_path` entries (and
  `permission_profile_toml_from_file_system_policy` drops skip-missing entries
  when materializing the extensible `:workspace` parent), whereas Go's builtin
  `:workspace` lists them as ordinary read-only `project_roots` subpath entries.
  Runtime enforcement is the same on both sides, so this is a wire-shape nuance,
  not a writable-metadata gap.

`config/permissions_test.go` now pins the behavior:
`TestCustomPermissionProfileProtectsProjectMetadataRootsLikeRust` resolves a
custom profile whose only filesystem rule is `:workspace_roots: {".": "write"}`
and asserts the derived writable roots refuse writes beneath
`.git/.agents/.gcode/.aws`. The recorded sync218 item is closed (no code change
was needed).

### Verification (eighty-sixth pass)

- `go test ./config/ ./sandbox/... -count=1` is green.

### Evidence gathered while probing the remaining lanes

Two recorded items were checked and found non-actionable in the current tree, so
later passes should not treat them as open work:

- Rust's `get_error_message_ui` (`ERROR_MESSAGE_UI_MAX_BYTES`, 2 KiB) has no
  consumer outside its own tests (`git grep get_error_message_ui` matches only
  the definition in `codex-rs/protocol/src/error.rs` and `error_tests.rs`), so
  there is no Go UI path to align yet.
- `codexapi.ErrorTransport` / `codexapi.ErrorStream` are classified by
  `appserver/turn_runtime.go`'s error-field mapping, but the Go tree constructs
  them only in tests (`git grep ErrorTransport` matches the kind definition, the
  mapping and `appserver/runtime_router_test.go`), so the recorded
  `ConnectionFailed`/`ResponseStreamFailed` display item has no production
  producer to align. This is left as an observation rather than a closure: if a
  future pass adds the producer, the display copy becomes actionable.
- The remaining retained-context producers are blocked on unported subsystems:
  the `delivered_assistant_message` producer's trigger is the app's User
  Messaging MCP tool (`mcp__codex_apps__user_messaging__send_message`), which Go
  does not register, and `guardian_sources` needs the guardian prompt composer
  to carry per-message metadata.

## Remaining after this pass

The sync218 permission-profile item is closed. Unchanged recorded lanes: the
retained-context producers (`delivered_assistant_message`, sender deliveries),
the `guardian_sources`/`guardian_source_order_guidance` metadata, the
`guardian-context` composition sections, the TUI reconnect lane, the TUI
warnings viewer, the Rust-side TUI handoff, the `ext/skills` world-state catalog
and cloud-skill lane (#48353), the TUI turn-tips lane (#48352), and the smaller
brokered-snapshot items. For the error lane only the
`UnexpectedStatus`/`ConnectionFailed` displays and the consumer-less
`get_error_message_ui` helper remain.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass works the TUI tooltip module, which the #48352 lane builds on and which had
drifted from upstream.

### sync252: the tooltip catalog and the `{key:context.action}` substitution

Go's `tui/tooltips.go` carried an old snapshot of Rust's
`tui/assets/tooltips.txt` (missing every tip added since: `/keymap`, `/vim`,
`/import`, `/hooks`, `/archive`, `/worktree`, `/recap`, `/voice`, `/goal`,
`/agents`, `/subagents`, `/export`, `/usage`, `/title`, `/theme`, `/plugins`,
the Markdown emphasis, and all eight `{key:...}` templates) and stale promo
copy, and it had no placeholder substitution at all: Rust resolves
`{key:context.action}` against the current keybindings and skips a tip whose
action is unknown or unbound (`render_tooltip` / `resolved_tooltips` /
`tooltip_templates`), while Go would have shown the raw placeholder.

- **`tui/tooltips.txt`**: replaced with Rust's current
  `codex-rs/tui/assets/tooltips.txt` verbatim (46 lines, eight `{key:...}`
  templates).
- **`tui/tooltips.go`**: the promo constants now match Rust's copy
  (`AppTooltip`, new `MacosAppTooltip`/`LinuxAppTooltip`, `OtherTooltip`,
  `FreeGoTooltip` with its en dash and right single quote);
  `tooltipsForOS` chains the platform app tip onto the asset tips the way Rust's
  `TOOLTIPS` does (macOS always, Linux only for a desktop session, Windows
  never); `paidAppTooltip` falls back to `linuxAppTooltip` on Linux;
  `linuxDesktopSession`/`isProbablyWSL` mirror Rust's display/WSL detection;
  `PickTooltip` now draws from `ResolvedTooltips(nil)`, so a `{key:...}`
  template can never be shown as a raw placeholder.
- **`tui/tooltip_render.go`** (new): `TooltipTemplates`, `ResolvedTooltips`,
  `RenderTooltip` and the `keybindingDisplayLabel` renderer, porting Rust's
  `render_tooltip` and `ShortcutHint::display_label` (modifier order
  ctrl/shift/alt, arrows, `pgup`/`pgdn`, `minus` -> `-`, chords joined with a
  space). The primary hint comes from `ResolvedKeymapBindings`; Rust's
  "unbound action drops the whole tip" rule is preserved, so a tip whose action
  Go does not register is skipped rather than rendered with a raw placeholder.

Tests (`tui/tooltips_test.go`): the platform chain (`macOS` pool ends with the
Desktop-app tip, Windows carries no app tip), substitution from the default and
remapped bindings, the skip matrix (unbound custom binding, unknown
context/action, malformed placeholder, nil keymap) and the reasoning tip's
two-binding requirement. `TestResolvedTooltipsRenderRustSnapshotLabels` pins the
resolved default pool against Rust's
`configured_shortcut_tips_render_at_narrow_width` snapshot labels (`tab`,
`ctrl+o`, `ctrl+t`, `ctrl+g`, `ctrl+r`, the two alt chords).

Recorded as remaining for this lane: Rust's `experimental_tooltips`
(feature `experimental_announcement` copy, gated on the realtime feature and
voice support) is not appended to `TooltipTemplates`; `global.find_transcript`
has no Go catalog entry or transcript-search behavior, so Rust's contract
currently skips that one tip; and Go's TUI still never shows a startup/session
tip (`tui/tea/model.go`'s `addStartupSessionHeader` passes an empty tooltip and
the tea model carries no `show_tooltips` flag), which is the wiring the #48352
lane needs.

### Verification (eighty-seventh pass)

- `go test ./tui/... -count=1` is green.
- `go test ./parity/ -count=1` is green at `e72da2b538`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass closes the experimental part of the tooltip templates recorded under
sync252.

### sync253: the experimental feature announcements in the tooltip templates

Rust's `tooltip_templates` returns `ALL_TOOLTIPS` = `TOOLTIPS` (the asset tips
plus the platform app tip) followed by `experimental_tooltips(features,
voice_supported)`: for each feature spec, skip Realtime Conversation unless
voice is supported on the host, then keep the experimental stage's announcement
copy when it is non-empty.

- **`tui/tooltip_render.go`**: `TooltipTemplates` now appends
  `experimentalTooltips()`, which walks `features.Registry`, applies the same
  Realtime-Conversation/voice gate (via `voicehost.IsSupported`), keeps only
  `StageExperimental` specs and returns their non-empty
  `ExperimentalAnnouncement` copy in registry order.

Tests: `TestTooltipTemplatesAppendExperimentalAnnouncementsLikeRust` pins the
catalog prefix and the experimental tail against the registry-derived
expectation, so a stage or copy change in `features` propagates through the
tooltip pool.

Recorded as remaining for this lane: `global.find_transcript` has no Go catalog
entry or transcript-search behavior, so Rust's contract currently skips that one
tip, and Go's TUI still never shows a startup/session tip (`addStartupSessionHeader`
passes an empty tooltip and the tea model carries no `show_tooltips` flag or
`tui.show_tooltips` config surface).

### Verification (eighty-eighth pass)

- `go build ./...` is green.
- `go test ./tui/... -count=1` is green.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass wires the session-header tip and fast-status marker, closing the two
recorded wiring items from sync252/sync253.

### sync254: the session-header tooltip, `tui.show_tooltips` and the fast marker

Rust's `new_session_info` (`history_cell/session.rs`) shows, for a non-first-event
header gated on `local_settings.tui.show_tooltips`, the tip from
`tooltips::get_tooltip(auth_plan, show_fast_status, &keymap)`, and marks the
header with `should_show_fast_status`. Go's `addStartupSessionHeader` passed an
empty tooltip and a hardcoded `false` fast flag, and there was no
`tui.show_tooltips` config surface, so the ported tooltip module had no consumer.

- **`auth/account.go`**: `PlanTypeFromString` exports Rust's
  `PlanType::from_raw_value` classification for the tooltip policy.
- **`tui/tooltips.go`**: `GetTooltipWithKeymap` / `PickTooltipWithKeymap` and an
  internal `selectStartupTooltip` carry the keymap through the promo-then-random
  policy; the existing `GetTooltip` / `SelectStartupTooltip` / `PickTooltip`
  keep their signatures and delegate with a nil keymap.
- **`tui/tea/model.go`** + **`settings_commands.go`**: `Options.ShowTooltips` and
  `SettingsWriteResult.ShowTooltips` (live updates), plus
  `sessionShowFastStatus` (Rust's `ChatWidget::should_show_fast_status`: ChatGPT
  account, fast service tier, model-supported tier) and `startupSessionTooltip`
  (`auth.PlanTypeFromString` of the session plan, the fast flag, the model
  keymap). `addStartupSessionHeader` passes the tip and the fast flag to
  `historycell.NewSessionHeader`/`NewSessionInfo`. A nil `ShowTooltips` keeps the
  header tip-free, so callers must opt in; the real app always supplies the
  resolved value.
- **`app/interactive.go`** + **`remote_tui.go`**: `interactiveShowTooltips`
  resolves `tui.show_tooltips` (default enabled, matching Rust's `Tui::default`)
  and both TUI entry points pass it through `Options`.

Tests: the config resolver default/explicit matrix (`app/tooltips_test.go`), the
header tip appearing only when enabled (with the live settings toggle) and the
fast-status matrix (`tui/tea/session_tooltip_test.go`).

Recorded as remaining for this lane: the first-run help block is still
unreachable in Go (`addStartupSessionHeader` passes `isFirstEvent=false` and
nothing sets `chatwidget.Config.IsFirstRun`, so Rust's `is_first_event =
show_welcome_banner` has no Go source), and `global.find_transcript` has no Go
catalog entry or transcript-search behavior, so Rust's contract drops that one
tip.

### Verification (eighty-ninth pass)

- `go build ./...` is green.
- `go test ./tui/... -count=1` and `go test ./app/ -count=1` are green.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass lands the sender-delivery producer of the retained-context program, which
the plan recorded as outstanding since sync215.

### sync255: the sender-delivery producer

Rust captures a delegation's sender evidence at turn-input admission
(`LocalAgentRuntime::capture_sender_user_messages`): a standalone
`function_call_output` from `codex_app`/`codex_tui` named
`send_message_to_thread` gets its own bounded snapshot, even when the output
carries no usable provenance. The snapshot renders Rust's reviewer-only
`GuardianSenderMessages` fragment, records up to three recent complete local user
messages from the sender thread, and travels with the item's harness metadata so
`record_sender_user_messages` retains it on both the live and replayed history.
Go had the consumer (`state.SenderUserMessagesSectionItems`) and the model
(`retainedctx.SenderUserMessages`, `RecordSenderUserMessages`) but no producer at
all.

- **`state/guardian_sender_messages.go`** (new): the fragment renderer, with
  Rust's type markers (`>>> SENDER USER MESSAGES START/END`), the fixed host
  preamble, the per-message 900-byte budget that turns an unusable message into
  the "unavailable within the evidence budget" notice, and the
  "no sender user messages" notice for a provenance-free delivery. Messages
  render through `RootMessage{Kind: RootMessageUser}` so a multi-line instruction
  keeps one role label per line.
- **`appserver/retained_context.go`**: `captureSenderUserMessages` gates exactly
  like Rust (`function_call_output`, no `call_id`, `send_message_to_thread`, a
  trusted namespace, a non-empty delivery id) and parses the delegation envelope
  with `context.ParseDelegatedPrompt`; `senderContextMessages` takes the last
  three complete local user messages in acceptance order;
  `captureSenderDeliveries` attaches the snapshot to the item's harness metadata;
  `annotateRetainedHarnessMetadata` now also records sender deliveries (Rust's
  `record_annotated_items -> record_sender_user_messages`) with a new
  `retainedSenderCandidates` gate so a delivery-only batch is not skipped; and
  `mergeSessionItemHarnessMetadata` merges fields without dropping the rest of
  the sidecar.
- **`appserver/runtime_router.go`**: `runtimeAppendItems` captures the deliveries
  before the provenance annotation.

Tests: the renderer exact-text matrix (`state/guardian_sender_messages_test.go`;
markers, multi-line labels, unavailable source, no-messages notice, and the
two-notice budget case) and the end-to-end producer
(`appserver/retained_sender_test.go`): four sender instructions yield the last
three (the multi-line one keeping two `user: ` lines) in the recorded snapshot,
a provenance-free `codex_tui` delivery still snapshots with
`Source thread: unavailable`, the receiver's retained context holds the delivery,
and the reviewer section returns exactly the captured text; plus the gate matrix
(paired `call_id`, another tool name, an untrusted namespace).

Recorded as remaining for this program: the snapshot's `receiver_turn_id` comes
from the thread's active turn, because Go captures at append time rather than at
turn-input admission, so a delivery appended with no active turn stores an empty
turn id (Rust always has the turn id); the `delivered_assistant_message` producer
still needs the app's User Messaging tool; and the
`guardian_sources`/`guardian_source_order_guidance` metadata still needs the
guardian prompt composer to carry per-message metadata.

### Verification (ninetieth pass)

- `go build ./...` is green.
- `go test ./state/ ./retainedctx/ ./appserver/ -count=1` is green
  (`appserver` 169.0s).
- `go test ./parity/ -count=1` is green at `e72da2b538`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass closes the guardian permission-evidence item recorded under the sixtieth
pass: the reviewed environment's own profile and its selection id.

### sync256: the reviewed environment's profile and selection id

Rust's `guardian::permissions::for_environment`
(`core/src/guardian/permissions.rs`) resolves the reviewed turn's *environment*
and reports `environment_id` alongside the denied paths/globs, using that
environment's own `permission_profile_with_workspace_roots` and falling back to
the turn's profile. Go modeled both pieces (`state.PermissionContext` already
renders `The active permission profile for environment %q`, and
`tool.UnifiedExecEnvironment` carries the per-environment `PermissionProfile`)
but the resolver never produced them, so every review reported the parent turn's
profile anonymously.

- **`appserver/agent_runtime.go`**:
  `guardianTurnPermissionProfileForTurn` now also returns the environment
  selection id and prefers the environment's own profile; the new
  `guardianTurnEnvironmentForTurn` resolves the turn's primary environment
  (`unifiedExecEnvironmentsForTurn`) to its id, cwd and profile.
- **`appserver/guardian_reviewer.go`**: the `turnPermissionProfile` callback
  carries the environment id, and the reviewer sets
  `state.PermissionContext.EnvironmentID`, so the section body names the
  environment (Rust's `GuardianPermissionContext::environment_id`).

Tests: `appserver/guardian_permission_test.go` (a ready environment's profile
JSON and cwd are resolved with id `ready-env`, the profile is the environment's
own rather than the turn's, and a turn that selects no environment reports no
id), and the reviewer permission test now also asserts the prompt names
`The active permission profile for environment "env-1"`.

Recorded as remaining for this lane: Rust's `for_tool` also resolves a per-tool
`environment_id` (`apply_patch` patches, `write_stdin` terminal sessions,
`request_permissions`, `exec_command`/`view_image` arguments), which Go's
sync-review resolver does not read; and Rust resolves a remote environment's
restrictions with its own sandbox policy context
(`get_unreadable_roots_with_context` / `get_unreadable_globs_with_context`),
while Go's enumerators still resolve against the host cwd.

### Verification (ninety-first pass)

- `go build ./...` is green.
- `go test ./appserver/ -count=1` is green (232.2s).
- `go test ./state/ ./sandbox/... ./parity/ -count=1` is green at
  `e72da2b538`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass completes the per-tool half of the guardian permission-evidence lane.

### sync257: the tool payload's own environment in the guardian action

Rust's `guardian_approval_request_to_json` appends a top-level `environment_id`
for every variant except `McpToolCall` (`GuardianApprovalRequest::
target_environment_id`), and `guardian::permissions::for_tool` resolves the
reviewed environment from the *tool payload* (an apply_patch body's
`environment_id`, an exec_command's resolved environment) before falling back to
the turn's primary environment; an unavailable id makes the resolution error and
the caller (`tools/lifecycle.rs`) drops the permission context entirely. Go's
action JSON rendered `environment_id` only for `write_stdin`, never populated it,
and always resolved the primary environment.

- **`state/guardian_action_prompt.go`**: `exec_command`/`execve` (the default
  branch), `apply_patch`, `request_permissions` and `network_access` now render
  `environment_id`, matching Rust's always-present key.
- **`appserver/runtime_router.go`**: `applyPatchApprovalAction` carries the
  parsed patch's own `EnvironmentID`, and `commandApprovalAction` carries
  `Request.UnifiedExecEnvironmentID`.
- **`appserver/agent_runtime.go`**: `guardianTurnPermissionProfileForTurn` and
  `guardianTurnEnvironmentForTurn` take the tool payload's environment id,
  select that environment among the turn's environments, and return no evidence
  when it is unavailable (Rust's `.ok()`).
- **`appserver/guardian_reviewer.go`**: the `turnPermissionProfile` callback
  receives `action.EnvironmentID`, so the evidence is scoped to the tool's own
  environment.

Tests: the action-JSON matrix now pins `environment_id` for exec_command,
apply_patch, request_permissions and execve (and the network-access prompt test
asserts the extra key), the approval builders carry the tool environment
(`appserver/guardian_permission_test.go`), and the resolver selects a requested
environment / drops an unavailable one.

Recorded as remaining for this lane: `write_stdin` (the process-to-environment
lookup), `request_permissions` and `network_access` still have no Go source for
their environment id, so their action JSON renders an empty value where Rust
names the real environment; and Rust's remote path-context resolution
(`get_unreadable_roots_with_context`) is still unmatched.

### Verification (ninety-second pass)

- `go build ./...` is green.
- `go test ./state/ ./appserver/ ./tool/ ./parity/ -count=1` is green
  (`appserver` 262.3s, `tool` 105.0s, `parity` 53.6s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass closes the `request_permissions` half of the per-tool environment item.

### sync258: `request_permissions`' own environment

Rust's `create_request_permissions_tool` (`tools/handlers/shell_spec.rs`)
declares an `environment_id` property ("Environment id from
<environment_context>. Omit to use the primary environment."), its description
tells the model to target an attached environment with it, and the handler
parses `RequestPermissionsEnvironmentArgs` (snake case with a camelCase alias)
to resolve the tool environment; the approval action then carries that id.
Go's tool declared only `reason`/`permissions`, never parsed an environment id,
and its reviewer callback could not receive one, so a `request_permissions` call
that targeted an environment lost that scope.

- **`tool/request_permissions.go`**: the spec gains the `environment_id`
  property (only `permissions` stays required) and Rust's description copy; the
  arguments parse `environment_id` with the `environmentId` alias; the reviewer
  callback gained an `environmentID` parameter.
- **`appserver/runtime_router.go`**: `requestPermissionsGuardianReviewer` sets
  the action's `EnvironmentID`, so the guardian action JSON and the permission
  evidence scope to the call's environment.

Tests: the reviewed context now asserts the environment id reaches the reviewer
(`tool/request_permissions_test.go`), and a spec test pins the declared property,
its guidance and the required list against Rust's schema.

Recorded as remaining for this lane: `write_stdin` (the unified-exec
process-to-environment lookup) and `network_access` still have no Go source for
their environment id, and Rust's remote path-context resolution
(`get_unreadable_roots_with_context`) is still unmatched.

### Verification (ninety-third pass)

- `go build ./...` is green.
- `go test ./appserver/ ./tool/ ./turn/ ./state/ ./parity/ -count=1` is green
  (`appserver` 234.5s, `tool` 103.9s, `parity` 50.0s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass closes the last action variant that lacked its own environment.

### sync259: the network approval's own environment

Rust's network approval passes the proxy request's `environment_id` into
`ApprovalAction::NetworkAccess` (`tools/network_approval.rs`), so the guardian
action JSON names it and `for_tool` resolves that environment. Go's network
approval service already had the value (`network.ProxyPolicyRequest::
EnvironmentID`) but did not copy it into the action, so the reviewer saw an
anonymous empty scope.

- **`appserver/network_approval.go`**: `requestGuardianApproval` sets
  `action.EnvironmentID` from `request.EnvironmentID`.

Tests: the existing network approval Guardian matrix now also asserts
`action.EnvironmentID == expected.environmentID` for the remote and local
environments. `write_stdin` was already wired (the terminal approval request
carries the process's environment, and `terminalWriteApprovalAction` copies it),
so only the remote path-context resolution remains recorded for this lane.

### Verification (ninety-fourth pass)

- `go build ./...` is green.
- `go test ./appserver/ -run 'NetworkApproval' -count=1` is green.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass takes the recorded request-level metadata-shedding item and lands its
capture/marker half.

### sync260: tool-result metadata is captured whole and shed at request time

Rust's `ToolResultMetadata::new` captures the complete MCP `_meta` snapshot -
"retention and outgoing-request budgets apply later" - and its shedding marker
carries the overage it removed (`omitted_due_to_size_limit (overage_bytes=N)`),
replacing a snapshot only when the marker is actually smaller
(`omit_if_smaller`), while `is_omitted_due_to_size_limit` accepts both the bare
and overage forms. Go omitted at capture, so an oversized late snapshot became
the marker before the request budget ever saw it, and it wrote only the bare
marker.

- **`model/executed_tool_calls.go`**: `NewToolResultMetadata` keeps the raw
  snapshot (Rust's `ToolResultMetadata::new`); new
  `omittedToolResultMetadataForOverage`, `isOmittedDueToSizeLimit` and
  `omitIfSmaller` mirror Rust's marker rules; the prompt bound's metadata step
  now sheds through `omitIfSmaller` (so a tiny snapshot is never replaced by a
  larger marker) and writes the overage form.

Tests: the capture test now asserts the oversized snapshot is retained until the
prompt bound, a new marker matrix pins the overage form, the "only when smaller"
rule, both parse forms and the malformed-overage rejection, and the shedding
test expects Rust's overage marker.

Recorded as remaining for this lane: Rust's shedding is size-ordered with
`prioritize_recent` and keeps a resource-access field when present
(`retain_resource_access` inside `shed_generic_result_metadata` /
`shed_remaining_result_metadata`), and the whole-message scope
(`bound_executed_tool_calls_for_message`) is unported; Go still degrades
wholesale rather than in size order, so a single large snapshot can still
displace unrelated small snapshots earlier than Rust would.

### Verification (ninety-fifth pass)

- `go test ./model/ ./turn/ ./tool/ ./codemode/ ./exec/ -count=1` is green
  (`model` 23.8s, `tool` 104.7s).
- `go test ./appserver/ ./parity/ -count=1` is green (184.9s / 48.8s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass lands the shedding order of the same recorded item.

### sync261: size-ordered metadata shedding with resource-access retention

Rust's `shed_generic_result_metadata` sheds snapshots largest-first - ties by
output order, then the call's own order within the output - so one large result
cannot discard unrelated small results; a snapshot carrying the resource-access
field (`openai/resource_access`) keeps only that field rather than becoming the
omission marker, and everything else is replaced only when the marker is
smaller (`omit_if_smaller`). Go replaced *every* snapshot with a marker as soon
as the prompt budget was exceeded, so a single large result could displace
unrelated small results and resource-access evidence was lost to a marker.

- **`model/executed_tool_calls.go`**: new `shedGenericResultMetadata` ports the
  ordering, the resource-access retention (`retainResourceAccess`) and the
  incremental byte accounting, and the prompt bound now uses it instead of the
  wholesale metadata replacement.

Tests: a new matrix pins largest-first shedding (a plain large snapshot becomes
the overage marker while an unrelated small snapshot survives) and
resource-access retention (the large snapshot keeps only
`openai/resource_access`, the small snapshot is untouched).

Recorded as remaining for this lane: Rust's `prioritize_recent` ordering (the
retained-history entry point reverses the items and breaks size ties by
`usize::MAX - item_index`), the post-marker re-sort in
`shed_remaining_result_metadata`, the whole-message scope
(`bound_executed_tool_calls_for_message` with its `shed_result_sources` /
`truncate_call_arguments_to_fit` order) and `clear_damaged_cell_completeness`.

### Verification (ninety-sixth pass)

- `go test ./model/ ./turn/ ./codemode/ ./exec/ -count=1` is green.
- `go test ./tool/ ./appserver/ ./parity/ -count=1` is green
  (`tool` 104.8s, `appserver` 109.8s, `parity` 23.8s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass corrects the prompt metadata budget, which the plan's audit of the shedding
lane uncovered.

### sync262: the prompt metadata budget is Rust's 2 MiB, not the 32 KiB retention gate

Rust has two distinct limits: the protocol's
`MAX_EXECUTED_TOOL_CALL_METADATA_BYTES = 2 * 1024 * 1024` is the budget for one
serialized prompt (`bound_executed_tool_calls_for_prompt[_with_priority]`), while
the core's `MAX_EXECUTED_TOOL_CALL_FULL_ARGUMENT_BYTES_PER_OUTPUT = 32 * 1024` is
the *retention* gate that decides when the recorder runs its
`prioritize_recent` bound over retained calls. Go used the 32 KiB retention
number as `model.MaxExecutedToolCallMetadataBytes`, so its prompt bound shed
metadata 64x more eagerly than Rust and produced prompt-wide omission counts for
inventories Rust would keep whole.

- **`model/executed_tool_calls.go`**: `MaxExecutedToolCallMetadataBytes` is now
  `2 * 1024 * 1024` (Rust's protocol constant), with the 32 KiB per-output gate
  documented as belonging to the turn recorder, where Go already models it
  (`turn.maxExecutedToolCallFullArgumentBytesPerItem`).

Tests: the idempotency test that relied on the smaller budget now builds an
inventory that genuinely exceeds Rust's 2 MiB limit (1000 calls with 16 KiB
names), so the prompt-wide omission path is still exercised.

Recorded as remaining for this lane: Rust's retention-stage bound over the
recorder's retained calls (`bound_executed_tool_calls_for_prompt_prioritizing_recent`
with the 32 KiB gate and the reversed item order) still has no Go equivalent -
Go enforces the per-output full-argument budget at record time instead.

### Verification (ninety-seventh pass)

- `go test ./model/ -count=1` is green.
- `go test ./turn/ ./codemode/ ./exec/ ./tool/ ./appserver/ ./parity/ -count=1`
  is green (`tool` 102.6s, `appserver` 140.2s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass closes the remaining-metadata and damaged-cell parts of the shedding item.

### sync263: the remaining-metadata re-sort and the damaged-cell completeness rule

Rust's `shed_remaining_result_metadata` re-sorts by the *updated* sizes (largest
first, and on a size tie a plain snapshot before an existing omission marker)
and omits what is still over budget, so an unrelated marker survives while the
largest plain snapshot is shed. Separately,
`bound_executed_tool_calls_for_prompt_with_priority` tracks damaged cells: an
item whose calls or arguments were lost clears its own `tool_calls_complete` and
marks its Code Mode cell, and `clear_damaged_cell_completeness` then discards the
claim on every item of that cell, so a later output cannot present a damaged
cell's surviving calls as complete. Go did neither.

- **`model/executed_tool_calls.go`**: new `shedRemainingResultMetadata` (Rust's
  re-sorted omit pass, run between the generic pass and the clear-all step),
  plus the damaged-cell bookkeeping in `boundExecutedToolCallItems` with the
  `executedToolCallCellID` / `clearExecutedToolCallsComplete` helpers.
- **`model/agent.go`**: `AgentItem.ClearExecutedToolCallsComplete`, mirroring
  Rust's `ResponseItem::clear_tool_calls_complete`.

Tests: a damaged-cell matrix (a cell with a truncated call and an intact sibling
loses both completion claims and never serializes `tool_calls_complete`) and a
direct `shedRemainingResultMetadata` case (the largest snapshot is shed while an
unrelated existing marker is untouched).

Recorded as remaining for this lane: Rust's `prioritize_recent` ordering (the
retention entry point reverses the items and breaks size ties by
`usize::MAX - item_index`), the whole-message scope
(`bound_executed_tool_calls_for_message` with its `shed_result_sources` /
`truncate_call_arguments_to_fit` order), and Rust's exact source-shedding
accounting (it subtracts the removed field framing; Go drops source evidence
wholesale). The whole-message trigger is also unported with concrete evidence:
Rust's `client_tool_metadata.rs::bounded_input` serializes the *whole* outgoing
message and only bounds when it exceeds `MAX_RESPONSE_MESSAGE_BYTES = 15 MiB`,
using `metadata_bytes - overage` as the budget, while `git grep` finds neither
that constant nor a `ForMessage` bound anywhere in the Go tree (`model`'s only
entry point is the fixed 2 MiB `BoundExecutedToolCallsForPrompt`), so a huge
outgoing message keeps metadata Rust would shed.

### Verification (ninety-eighth pass)

- `go test ./model/ ./turn/ ./codemode/ ./exec/ -count=1` is green.
- `go test ./tool/ ./appserver/ ./parity/ -count=1` is green
  (`tool` 104.6s, `appserver` 250.0s, `parity` 54.6s).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `e72da2b538`). This
pass lands the whole-message bound recorded as the lane's next step.

### sync264: the 15 MiB whole-message bound

Rust's `client_tool_metadata::bounded_input` serializes the *whole* outgoing
message and bounds only when it exceeds `MAX_RESPONSE_MESSAGE_BYTES = 15 MiB`,
passing `metadata_bytes - overage` to
`bound_executed_tool_calls_for_message`, whose whole-message scope sheds
optional sources and recorded arguments before resource-access evidence. Go had
no message-level cap at all: only the fixed 2 MiB prompt bound
(`BoundExecutedToolCallsForPrompt`), so a huge outgoing message kept metadata
Rust would shed.

- **`model/executed_tool_calls.go`**: `boundExecutedToolCallItems` is now a
  wrapper over `boundExecutedToolCallItemsWithBudget(items, max, wholeMessage)`;
  `shedGenericResultMetadata` and `shedRemainingResultMetadata` gained the
  whole-message scope (the extra "clear the complete field" pass and the
  plain-before-marker ordering); new `shedResultSources` (budget-aware for the
  whole-message scope, wholesale for the prompt scope),
  `truncateCallArgumentsToFit`, `clearDamagedCellCompleteness`,
  `executedToolCallMetadataBytesOf` and the exported
  `ExecutedToolCallMetadataTotalBytes` / `BoundExecutedToolCallsForMessage`.
- **`model/responses_agent.go`**: `MaxResponseMessageBytes` and
  `boundedInputForResponseMessage` mirror `bounded_input`, wired at all three
  request sites (the WebSocket connect payload, the WebSocket v2 payload and the
  HTTP/SSE body), so only the wire copy changes.

Tests: the whole-message ordering (a snapshot holding only
`openai/resource_access` keeps it while the sources are shed first) and the
message-level trigger (a message under the cap is untouched, an over-cap message
loses its optional snapshot while ordinary content survives, and an over-cap
message without tool metadata is untouched).

Recorded as remaining for this lane: the retention entry point's
`prioritize_recent` ordering (Rust reverses the items and breaks size ties by
`usize::MAX - item_index`) and the recorder's retention-stage bound
(`bound_executed_tool_calls_for_prompt_prioritizing_recent` behind the 32 KiB
gate) still have no Go equivalent - Go enforces the per-output full-argument
budget at record time instead.

### Verification (ninety-ninth pass)

- `go test ./model/ ./turn/ ./tool/ ./codemode/ ./exec/ -count=1` is green
  (`model` 27.5s, `tool` 105.3s).
- `go test ./appserver/ ./parity/ -count=1` is green (`appserver` 82.0s on the
  re-run; one earlier package run flaked in the documented
  `TestRuntimeRouterTurnStartElevatedSandboxDoesNotPersistProjectTrustLikeRust`
  TempDir cleanup race, green 3/3 in isolation).
