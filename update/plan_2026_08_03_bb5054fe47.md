# codex_go 对齐 Rust Codex `bb5054fe47` 更新计划

> 日期：2026-08-03（第二轮）
> 上一对齐基线（Rust）：`5157493c23`（Go 提交 `deec3b9` 已完成对齐）
> Rust 目标：`bb5054fe47`（Capture rollout budget units from response usage, #36641）
> 变更范围：1 个提交，18 个文件，+63 / -3
> 目标：将 Go 版 Codex 同步至上游 `bb5054fe47`，完成响应用量中 rollout budget units 的捕获，并通过测试验证

## 一、上游变更概况（5157493c23..bb5054fe47）

### 1. 核心功能

| 文件（Rust） | 变更 | Go 对应组件 |
|---|---|---|
| `codex-rs/protocol/src/protocol.rs` | `TokenUsage` 新增 `codex_rollout_budget_units: Option<serde_json::Number>`，`#[serde(default, skip_serializing)]`、`#[schemars(skip)]`、`#[ts(skip)]`（仅内部捕获，不进入协议/schema/TS） | `model.AgentUsage` |
| `codex-rs/codex-api/src/sse/responses.rs` | `ResponseCompletedUsage` 解析 `codex_rollout_budget_units` 并写入 `TokenUsage`（provider 上报的共享 rollout 预算消耗单位，可为小数如 2.5） | `model.responsesAgentAPIUsage` + `usageFromResponses` |

### 2. 其余变更（均为 `codex_rollout_budget_units: None` 结构体字面量补齐）

- `codex-rs/core/src/compact_remote_v2.rs`、`core/src/guardian/{metrics,review_session}.rs`、`core/src/session/{mod,tests}.rs`、`core/src/tasks/mod.rs`
- `codex-rs/ext/goal/src/accounting.rs`、`ext/goal/tests/*`（delta 记账不携带 provider 值）
- `codex-rs/memories/write/src/phase1.rs`、`state/src/runtime/threads.rs`、`app-server/src/bespoke_event_handling.rs`、`analytics/src/analytics_client_tests.rs`
- `codex-rs/codex-api/tests/sse_end_to_end.rs`、`app-server/tests/*`（新增 SSE 端到端断言）

## 二、本地 codex_go 当前状态

- 最新对齐提交：`deec3b9`（Align Go Codex with Rust upstream 5157493c23），工作区干净。
- Go 侧尚无 `codex_rollout_budget_units` 相关代码（`rg` 无命中），需新增。
- `model` 包基线测试通过（`go test ./model/...` ok）。

## 三、对齐计划（阶段）

| 阶段 | 范围 | 状态 | 验证 |
|---|---|---|---|
| P0 | 生成本轮计划文件，确认上游范围与基线 | 完成 | `git pull` 最新、commit 范围核对 |
| P1 | `model/responses_agent.go`：`responsesAgentAPIUsage` 新增 `CodexRolloutBudgetUnits json.Number`（`json:"codex_rollout_budget_units"`，对应 `serde_json::Number`，可保存 2.5 这类小数） | 待开始 | model 单测 |
| P2 | `model/agent.go`：`AgentUsage` 新增 `CodexRolloutBudgetUnits json.Number`（`json:"-"`，仅内部捕获，不序列化，对应 Rust `skip_serializing`/`ts(skip)`/`schemars(skip)`） | 待开始 | 编译 + 协议序列化测试 |
| P3 | `usageFromResponses` 拷贝该字段（非流式与 SSE 流式共用此函数，天然双路径覆盖） | 待开始 | model 单测 |
| P4 | 保持协议不泄漏：`addAgentUsage` 聚合、`protocol.Usage` 转换、notifications/telemetry 等不携带该字段（对应 Rust 各处 `None` 语义） | 待开始 | appserver 协议 surface 测试 |
| P5 | 测试覆盖：`responses_agent_test.go` usage fixture 增加 `"codex_rollout_budget_units": 2.5` 并断言；`responses_stream_test.go` 补充流式用例 | 待开始 | `go test ./model/...` |
| P6 | 全量验证：`go build ./...`、`go test ./model/... ./runtimeutil/... ./turn ./appserver`、`go vet` 变更包；如需 SDK 差分则按既有策略执行 | 待开始 | 测试报告 |
| P7 | 提交：更新计划文件 + 代码 + 测试，提交信息 `Align Go Codex with Rust upstream bb5054fe47`；按惯例更新 `parity.json` 基线（若本轮未做完整 parity 矩阵，则仅更新 rustBaseline 并在总结中注明） | 待开始 | git log 确认 |

## 四、风险与注意

1. `json.Number` 仅在字段类型直接声明时由 `json.Unmarshal` 保留原始字面量（整数/小数均不丢精度），与 `serde_json::Number` 语义一致；Go 代码库已有 `json.Number` 使用先例。
2. 该字段为 provider 专属值，不得进入任何序列化协议（protocol/notifications/telemetry/TS schema），与 Rust `skip_serializing` 保持一致。
3. 上游消费点（goal 记账、guardian delta）目前均为 `None` 占位，Go 侧无需实现增量记账，仅保证解析与内部携带。

## 五、后续执行顺序

1. P1→P3 落地字段解析与内部携带。
2. P4 核对所有 `AgentUsage → protocol` 转换点不泄漏。
3. P5 补充单元/流式测试。
4. P6 全量测试与 vet。
5. P7 提交并输出对齐总结。
