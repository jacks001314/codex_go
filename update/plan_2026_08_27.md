# Codex Go update alignment plan - 2026-08-27 (sync28: 33-commit upstream range)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` frozen target (sync27, static freeze): `bde9db1375` (#40906)
- Rust new HEAD (sync28): `307ce6cda9` (#41023)
- Post-freeze upstream HEAD (this pass): `5af6979986` (#41030) - "Update the
  stable exec-server test to Codex 0.150.1". This is a Bazel-only test-version
  bump (MODULE.bazel + codex-rs/exec-server/BUILD.bazel); it touches none of the
  frozen Rust source files, so the L0 static re-freeze at `307ce6cda9` remains
  valid and no Go functional change is required.
- Post-baseline functional commit: `57e2edc6e9` (#41041) - "Encrypt sensitive
  history and notes tool arguments". Landed in `b2d8392` (encrypted-arguments
  header on the sensitive history/notes routes; encrypted schema markers on the
  search query / appended note text / replacement note text fields).
- Post-baseline commits (57e2edc6e9..f1433fc71f): `72c96598c6` (#41046)
  (L0 static re-freeze to `f1433fc71f` landed in the parity TUI snapshot manifest:
  `replayed_delegated_tool_output.snap` added, chatwidget/snapshots 239->240, total 718.)
  preserve tool authority for TUI delegation prompts, and `f1433fc71f` (#41050)
  add developer instructions for persistent mode. Both rely on Rust-specific modules
  (codex_app delegation tools, world_state) that the Go port does not implement;
  tracked as the post-baseline queue. `f1433fc71f` (#41050 persistent-mode developer
  instructions) landed in `3fe8304` / `490f8c1` (context fragment builder +
  appserver wiring for persistent-reasoning turns). `72c96598c6` (#41046 delegation tool
  authority) landed in `5b826a0` (context.ParseDelegatedToolOutput for TUI attribution).
- Post-baseline commit `d5caceccb1` (#41058): Code Mode tool call metadata
  completeness (cell_id + tool_calls_complete) landed in `fe82993` / `514e304`.
- Go baseline commit: `213a97e` (`main`, sync26/sync27 landed)
- Go baseline state: `go build ./...` clean; `go test ./parity -count=1` green at
  `bde9db1375`.

Rust `git pull origin main` advanced `bde9db1375..307ce6cda9` (33 upstream
commits). The range is dominated by Guardian V2 review hardening, MCP
trusted-access / provenance, streaming rate-limit classification, plugin
layering, filesystem policy matching, Vim motions, and gRPC code-mode trace
propagation.

## New upstream commits (bde9db1375..307ce6cda9)

### Guardian V2 / review
- `5b92c2d2f5` #40911 Make Guardian risk score persistence opt-in
- `f74bcd2811` #40964 Build Guardian V2 synchronous review prompts
- `daa3eaf10f` #40967 Allow Guardian scoring for required computer-use models
- `d61ba72f2f` #40982 Give Guardian trusted context for configured MCP tools
- `102ae5e2e6` #40985 Prewarm Guardian WebSockets without blocking thread startup
- `b68acc4d4b` #41006 Trust invoked user skills in Guardian reviews
- `307ce6cda9` #41023 Track Guardian reviewer turn and tool analytics

### MCP trusted access / provenance
- `a98b94625e` #40992 Add trusted access context for MCP metadata
- `21ff2e802c` #40976 Expose MCP provenance to tool lifecycle extensions
- `ae357e7250` #41005 Attach verified access context to eligible plugin MCP calls

### Streaming / errors / turn metadata
- `e0c727de04` #40931 Classify streaming rate-limit errors
- `0d654e6536` #40987 Track window and fork positions in turn metadata
- `b9c4b9a0cf` #40991 Support standalone function outputs in turn routing
- `e56e4922eb` #41002 Support standalone tool outputs in `turn/start`
- `7276d67081` #40952 Expose resumable misalignment details through app server
- `0e9a2bae52` #40942 Enable clock tools for persistent reasoning turns
- `6e008417bb` #40989 Expose permission profile resolution in the core API

### Plugin / config layering
- `6ac012a0d4` #40954 Honor layered configuration when loading plugins
- `0340e12f5d` #40993 Allow bundled browser plugins to run executor cleanup hooks
- `81e180044d` #41020 Scope extension capabilities to invocation lifetimes

### Sandbox / filesystem / platform
- `7f82397363` #40961 Restrict macOS scratch access to process sandboxes
- `2926014075` #41001 Make filesystem policy matching URI-native
- `37a5149822` #40983 Record Windows world-writable scan telemetry
- `a57b398351` #40978 Require approval for input to escalated terminals
- `f3741880f6` #40999 Harden managed proxy listener handoff
- `7625bd5665` #40912 Honor environment-resolved workspace roots

### Vim / TUI / skills / misc
- `d47e5cc0e2` #40958 Add Vim buffer jump motions
- `e77773085c` #40962 Give Responses Lite prefix items stable IDs
- `ac644ed112` #40966 Stop preserving bounds in tool input schemas
- `528fd7ace5` #40994 Enable retained-image budgeting by default
- `7c3747941a` #41011 Reduce skill catalog prompts with path aliases
- `eed1dee69e` #41017 Propagate trace context through gRPC code mode
- `07d260c623` #40979 Update the stable exec-server test to Codex 0.150.0

## L0 static re-freeze - DONE

Refreshed the L0 static layer over the full `bde9db1375..307ce6cda9` range:

- `parity/rust_snapshot_test.go`: re-pinned critical SHA256 hashes for
  `exec/src/lib.rs`, `core/src/client.rs`,
  `app-server/tests/suite/v2/mod.rs`, and `core/tests/suite/mod.rs`.
- `appserver/schema/precomputed/app-server-exports-{stable,experimental}.json.zst`:
  re-vendored byte-identical to the regenerated Rust blobs.
- `parity/rust_fixture_manifest_test.go`: bumped
  `app-server/tests/suite/v2` 118->119, `core/tests/suite` 171->172,
  `tui/src/chatwidget/tests` 38->39, `tui/src/chatwidget/tests/snapshots`
  13->14.
- `parity/rust_tui_snapshot_manifest_test.go`: bumped total 715->717
  (`chatwidget/snapshots` 238->239, `chatwidget/tests/snapshots` 13->14).
- `features/features.go`: registered the new `write_stdin_approval` feature key
  (under development / default off) and promoted `compaction_image_budget` to
  stable / default-on, mirroring `features/src/lib.rs`.
- `codexapi/error.go` + `model/responses_stream.go` + `appserver/turn_runtime.go`:
  added `rateLimitExceeded` CodexErrorInfo classification for the upstream
  `RateLimitExceeded` streaming error (#40931), preserving the parsed retry
  delay.

Verification after re-freeze:
- `go build ./...` -> clean.
- `go test ./parity -count=1` -> pass (all snapshot/manifest/surface gates).
- `go test ./features -count=1` -> pass.

## Runtime batch roadmap (sync28)

Each batch is tracked `status: pending` until its runtime behavior and verifier
land. The Guardian V2 / MCP-trust / policy-matching batches are the highest
value and are driven in the sibling sub-agent passes.

### batch-guardian-v2-sync28
- `5b92c2d2f5` #40911 Make Guardian risk score persistence opt-in
- `f74bcd2811` #40964 Build Guardian V2 synchronous review prompts
- `daa3eaf10f` #40967 Allow Guardian scoring for required computer-use models
- `d61ba72f2f` #40982 Give Guardian trusted context for configured MCP tools
- `102ae5e2e6` #40985 Prewarm Guardian WebSockets without blocking thread startup
- `b68acc4d4b` #41006 Trust invoked user skills in Guardian reviews
- `307ce6cda9` #41023 Track Guardian reviewer turn and tool analytics

### batch-mcp-trust-sync28
- `a98b94625e` #40992 Add trusted access context for MCP metadata
- `21ff2e802c` #40976 Expose MCP provenance to tool lifecycle extensions
- `ae357e7250` #41005 Attach verified access context to eligible plugin MCP calls

### batch-turn-stream-sync28
- `0d654e6536` #40987 Track window and fork positions in turn metadata
- `b9c4b9a0cf` #40991 Support standalone function outputs in turn routing
- `e56e4922eb` #41002 Support standalone tool outputs in `turn/start`
- `7276d67081` #40952 Expose resumable misalignment details through app server
- `0e9a2bae52` #40942 Enable clock tools for persistent reasoning turns
- `6e008417bb` #40989 Expose permission profile resolution in the core API

### batch-plugin-layering-sync28
- `6ac012a0d4` #40954 Honor layered configuration when loading plugins
- `0340e12f5d` #40993 Allow bundled browser plugins to run executor cleanup hooks
- `81e180044d` #41020 Scope extension capabilities to invocation lifetimes

### batch-sandbox-policy-sync28
- `7f82397363` #40961 Restrict macOS scratch access to process sandboxes
- `2926014075` #41001 Make filesystem policy matching URI-native
- `37a5149822` #40983 Record Windows world-writable scan telemetry
- `a57b398351` #40978 Require approval for input to escalated terminals
- `f3741880f6` #40999 Harden managed proxy listener handoff
- `7625bd5665` #40912 Honor environment-resolved workspace roots

### batch-misc-sync28
- `d47e5cc0e2` #40958 Add Vim buffer jump motions
  - LANDED (this pass): `tui/tea/vim_mode.go` adds `gg` / `G` buffer line jumps
    (`jumpToVimBufferLine`) with a pending-g chord state and operator forms
    (`dgg` / `dG` / `ygg` / `yG` / `cG`); `tui/tea/model.go` adds `vimPendingG`;
    `tui/keymap.go` registers `vim_normal.jump_top/jump_bottom` and
    `vim_operator.motion_jump_top/motion_jump_bottom`. Coverage in
    `tui/tea/vim_mode_test.go`.
- `e77773085c` #40962 Give Responses Lite prefix items stable IDs
- `ac644ed112` #40966 Stop preserving bounds in tool input schemas
-  - ALIGNED (already present): Go `jsonschema/jsonSchemaSubsetKeys` drops
    `minimum` / `maximum` / `maxLength`, and the sanitize type-inference no
    longer treats `maxLength` as a string marker - matching the Rust change.
- `528fd7ace5` #40994 Enable retained-image budgeting by default
-  - DONE (sync28 re-freeze): `features/features.go` promoted
    `compaction_image_budget` to stable / default-on.
- `7c3747941a` #41011 Reduce skill catalog prompts with path aliases
- `eed1dee69e` #41017 Propagate trace context through gRPC code mode
- `07d260c623` #40979 Update the stable exec-server test to Codex 0.150.0
-  - No Go impact (Bazel/exec-server test version only). The follow-up #41030 is
    the same category; see the baseline note above.

## Verification (this sync)

- `go build ./...` clean.
- `go test ./parity -count=1` pass (all static-freeze/surface gates at
  `307ce6cda9`).
- `go test ./features -count=1` pass.
- Remaining: dynamic L1-L3 differential (record-replay, SDK) for the runtime
  batches above, driven as each batch lands.

## Status summary

Static layer re-frozen to `307ce6cda9` (#41023) (L0 gate green). The
`rateLimitExceeded` streaming classification (#40931) landed as part of the
re-freeze so the enum surface stays green. The runtime batch roadmap above is
the work queue. Landed so far:

- `c1814c3` L0 static re-freeze to `307ce6cda9` + `rateLimitExceeded` (#40931).
- `0879f1b` Vim buffer-jump motions `gg`/`G` (#40958), with operator forms
  (`dgg`/`dG`/`ygg`/`yG`/`cG`) and the `gg` chord routed through the
  configurable keymap; `go test ./tui/...` green.
- `e24b2c8` Responses Lite stable prefix IDs (#40962) + resumable misalignment
  details through the app server (#40952); `model`, `codexapi`, and `appserver`
  tests green.
- `24185dc` clock tools for persistent reasoning turns (#40942): persistent
  reasoning effort defaults the current-time reminder and `clock.sleep` on
  (explicit config + managed policy win; review turns excluded); `appserver`
  tests green.
- `2b2e21c` reduce skill catalog prompts with path aliases (#41011): evaluate
  aliased catalogs regardless of budget pressure and select them when they
  preserve inclusion/description while reducing prompt size.
- `be5f364` make filesystem policy matching URI-native (#41001): add
  PathURI Overlaps/IsOpaque/LexicalDepth/JoinDescendant and wire the sandbox
  deny-constrains-grant check to fail closed on ambiguous component boundaries.
- `529aa39` standalone function-call-output turn routing (#40991) + `turn/start`
  `toolOutput` (#41002): `TurnStartParams.ToolOutput` validation, inject a
  standalone `function_call_output` model item, and surface a distinct
  `functionCallOutput` thread item.
- `808b7f9` require approval for input to escalated terminals (#40978): the
  unified exec manager tracks whether a process escalated sandboxing and
  invokes an optional per-process writeStdinApproval callback (process, thread,
  turn, chars) before writing non-empty input; a rejected approval surfaces
  `write_stdin rejected: ...`. The callback carries thread/turn so the
  app-server layer can resolve the per-turn `write_stdin_approval` feature flag
  and route a fresh approval. The feature flag is off by default, so runtime
  behavior is unchanged until the app-server installs the callback.
- `077ecb9` wire write_stdin_approval through the app server (#40978): the
  runtime router installs `writeStdinApprovalForTurn` as the unified exec
  manager callback, resolving the per-turn feature flag and requiring a fresh
  session command approval for escalated-terminal input. Tests pin feature-on
  rejection, session-approval allowance, and feature-off no-gate.
- `eed6f9e` carry trace context on code-mode execute requests (#41017): adds
  the `TraceContext` (traceparent/tracestate) wire field to `ExecuteRequest`
  round-tripped through the JSON framing codec. The full span-linking wiring
  (injecting traceparent into session/execution requests and carrying it on
  streamed tool callbacks via OTel span state) is the remaining integration.
- `45da429` expose MCP provenance to tool lifecycle extensions (#40976):
  `PreToolUsePayload` gains an `McpToolContext` with server/tool/connector/
  plugin IDs and an `McpToolSource` classification (connector/config/plugin/
  selected_plugin/other), populated from the MCP executor's known provenance.
- `b2d8392` encrypt sensitive history and notes tool arguments (#41041): the
  history/notes backend sends `x-openai-encrypted-tool-arguments: true` on the
  sensitive routes, and the search-query / appended-note-text / replacement-
  note-text schema fields are marked `encrypted: true`.
- `dc4facc` make Guardian risk score persistence opt-in (#40911):
  `config.Config` gains `GuardianV2PersistScores()` reading
  `[features.guardianv2].persist_scores` (default false), mirroring the Rust
  Guardian V2 debug option so reviewed actions/risk scores only persist when
  explicitly enabled.
- `ea26c55` honor environment-resolved workspace roots (#40912): the resolved
  `EnvironmentConfig` carries its effective `WorkspaceRoots` (from turn
  `RuntimeWorkspaceRoots`, falling back to the environment/primary CWD), so
  permission profiles and filesystem context materialize with the correct
  roots.
- `1ec7c13` add trusted access context for MCP metadata (#40992): the mcp
  package gains the verified-access grant data structures
  (`VerifiedAccessResponse/Program/State/Grant`, `TrustedAccessLevel`,
  `VerifiedAccessSource`) and the `openai/entitlementContext` metadata
  derivation (`cyberTrustedAccessStatus` -> granted/not_granted/unknown).
  The runtime grant fetch (GET verified_access + account identity validation), the
  eligibility-based attachment, and the app-server wiring from the ChatGPT auth snapshot
  are landed (also `#41005`).
- `7751b8a` restrict macOS scratch access to process sandboxes (#40961): the
  sandbox-process Seatbelt policy grants /tmp, /private/tmp, /var/tmp, /private/var/tmp.
- `1736ee3` record Windows world-writable scan telemetry count (#40983): the audit
  result carries the total flagged-directory count.
- `7991860` / `ec6c014` track window and fork positions in turn metadata
  (#40987): `window_number` in turn metadata (codexapi/turn builder + per-thread
  advancing counter), and fork-cutoff ordinal persisted separately from the history
  base (rollout `ForkedFromOrdinal`).
- `e1f6c64` wire trusted access context into the MCP service (#40992/#41005):
  the context is built from the ChatGPT auth snapshot with account identity and
  installed on the runtime MCP service.
- `#40966` (stop preserving tool-schema bounds) confirmed already aligned: the
  Go `jsonSchemaSubsetKeys` never preserved `minimum`/`maximum`/`maxLength`,
  and the string-type inference does not key off `maxLength`.

Remaining sync28 runtime batches (`batch-guardian-v2-sync28`,
`batch-mcp-trust-sync28`, `batch-turn-stream-sync28`, `batch-plugin-layering-sync28`,
`batch-sandbox-policy-sync28`, and the misc items) remain the work queue; each
still needs its own differential verifier (record-replay / SDK) before
`parity.json` certification can advance.

Notes on the remaining items:
- Several upstream commits in the range are Rust-internal refactors with no
  Go-side behavior change (`#40989` permission-profile API visibility,
  `#41020` extension callback lifetimes, `#40979`/`#41030` Bazel version
  bumps). These do not require Go alignment.
- The remaining deep runtime batches (Guardian V2 async-scorer extension,
  Linux managed-proxy listener handoff) are coupled to Rust-specific subsystems
  (ext/guardian-v2, linux-sandbox binary) with no Go-equivalent runtime; the
  Go port inherits them via the Rust components. They each need their own
  record-replay / SDK differential verifier before `parity.json` certification.

Additional pass (this turn):
- Pushed the sync28 static re-freeze (`c1814c3`) to `origin/main`.
- Confirmed upstream `git pull origin main` advanced only `5af6979986` (#41030),
  a Bazel-only test-version bump with no Go functional impact; the L0 static
  freeze at `307ce6cda9` therefore remains valid without a re-freeze.
- Landed the Vim buffer jump motions (#40958) from `batch-misc-sync28`
  (see the batch note above). `go build ./...`, `go test ./parity -count=1`,
  `go test ./features -count=1`, and `go test ./tui/... -count=1` are green.
- The remaining sync28 runtime batches (guardian-v2, mcp-trust, turn-stream,
  plugin-layering, sandbox-policy, and the rest of misc) are still the tracked
  work queue.

Additional pass (this turn): #41070 + #41072 landed
- Upstream `git pull origin main` advanced the frozen target to `b592a0bfed`
  (#41072). The two new functional commits on top of the previously applied
  `4cb8d8679c` (#41062) are:
  - `218a3e50ae` #41070 Clarify when to send asynchronous user messages.
  - `b592a0bfed` #41072 Forward model confirmation policies to actor MCP tools.
- `#41070`: expand the `send_user_message_async` tool description
  (`tool/send_user_message_async.go`) to the upstream wording.
- `#41072`: forward model-owned confirmation-policy Markdown to actor MCP calls.
  - `model/catalog.go`: `ModelMessages` gains `confirmation_policies`
    (`ConfirmationPolicies{BrowserUse,ComputerUse} *string`), parsed in
    `UnmarshalJSON`, and cleared by `setInstructionsTemplate` on a base-
    instructions override (mirrors Rust `with_config_overrides`).
  - `mcp/tool_executor.go`: `ConfirmationPoliciesMetaKey =
    "openai/confirmation_policies"`, `IsNodeReplBackedServer` (node_repl/cua_repl),
    and request-meta injection for eligible actor calls. Nil policies still attach
    an empty object (clearing startup defaults); `SuppressActorConfirmationPolicies`
    omits it for Guardian sessions.
  - `turn/tools.go`: `ToolRegistryOptions` carries `ModelConfirmationPolicies` +
    `SuppressActorConfirmationPolicies`; `registerMCPToolSet` forwards them to the
    executor; `mcpActorConfirmationPolicies` maps the catalog type to the executor.
  - `appserver/runtime_router.go` + `exec/exec.go`: resolve the model's
    confirmation policies from the turn model catalog and suppress for Guardian
    review sessions.
- Verification: `go build ./...` clean; `go test ./model ./mcp ./turn ./tool
  -count=1` green; `go test ./parity -count=1` and `go test ./features -count=1`
  green (L0 static freeze remains valid at `307ce6cda9`; `b592a0bfed` did not
  change the frozen Rust source hashes or fixture-root file counts).
- Known pre-existing (unrelated) appserver test failure:
  `TestRuntimeRouterChatGPTTurnIncludesStandaloneImageGenerationByDefaultLikeRust`
  asserts the older skills-protocol phrasing `"must read its \`SKILL.md\`
  completely"` while the current skill-instruction generator emits `"open and
  read its \`SKILL.md\` completely"`. Confirmed failing on a clean HEAD (unrelated
  to this sync pass).
- The remaining sync28 runtime batches (guardian-v2, mcp-trust, turn-stream,
  plugin-layering, sandbox-policy, and the rest of misc) remain the tracked work
  queue.
