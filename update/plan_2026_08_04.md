# codex_go 对齐 Rust Codex `5af85998c2` 更新计划

> 日期：2026-08-04
> 上一对齐基线（Rust）：`bb5054fe47`（Go 提交 `3636d13` + `066b4c5` 已完成对齐）
> Rust 目标：`5af85998c2`（Keep API request metrics out of Statsig exports, #36840）
> 变更范围：40 个提交，252 个文件，+26,450 / -2,060（codex-rs 232 文件，+9,337 / -2,037）
> 目标：将 Go 版 Codex 同步至上游 `5af85998c2`，优先落地可独立验证的功能对齐项并通过测试

## 一、上游变更概况（bb5054fe47..5af85998c2，按功能域归类）

### 1. Rollout / 预算
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `8b8fa7276f` | 使用 provider 上报的 rollout budget units：存在 `codex_rollout_budget_units` 时按其扣减共享预算，否则回退加权 token 记账；非有限/负数视为 fatal 响应错误 | `runtimeutil.Budget` | 本轮落地 |
| `7431f10d0d` | token budget 上下文用 agent 名代替 thread id（`<context_window>` 元数据） | context/token_budget、rollout context_window | 评估（Go 无 "Thread id:" 片段，需在 remote compaction v2 对齐时确认） |

### 2. MCP / Apps / 插件
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `e4e040881a` | host-owned `codex_apps` 目录上限提至 8,192；其他 MCP 保持 2,048；共享 Apps 工具缓存仅限 host-owned | `mcp/pagination.go`、`mcp/config.go`、`mcp/http_client.go`、`mcp/stdio_client.go` | 本轮落地（含补齐上一轮 2,048 基线） |
| `51c9ed6d4f` | MCP 工具按 surface 暴露控制：`omit_tools_from`（direct/deferred/code_mode），剔除客户端 `_meta` | `mcp/config.go`、`tool`、`config.schema` | 待落地（P2） |
| `bd12b3a9ec` | Agent Plugins `mcp.json` → Codex MCP 配置解析（stdio/streamable-http、PLUGIN_ROOT/PLUGIN_DATA 展开、路径包含、安全端点校验、客户端 header 过滤、UTF-8 header） | `plugin/agent_plugin_mcp_config.go` | **完成**（本轮，已接入 readPluginMCPServerConfigs） |
| `df72fdb415` | `ModelMessages.instructions_template` 统一承载模型指令（base_instructions 覆盖写入 template、保留其余字段；personality 禁用时渲染默认人格） | `model/catalog.go` | **完成**（本轮 WithConfigOverrides） |
| `e4e0c7070e` | 按模型能力门控插件使用说明（`include_plugin_usage_instructions`，默认 false） | `model`、`appserver` | **完成**（本轮） |
| `62839fec5d` | extension registry 直接存入 builder | `plugin`（重构） | N/A（Go 无同构 registry builder） |
| `61dc1d97f6` | MCP 客户端一致性回归门禁（Python/SDK TS 测试基建） | `sdktests`（评估 Go 适配器） | 待评估（P5） |

### 3. 权限 / 会话 / world state
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `1bbfb5cfad` | 命令审批后避免重注入完整权限说明：已批准命令前缀单独跟踪，快照只 diff 新增前缀 | `context`、`session`、`safety` | 待落地（P2） |
| `79479cdf09` | turn skill 状态存入 extension data | `turn`、`skillprovider` | N/A（Go 在 prompt/appserver 按 turn 处理 skill 状态，无 extension-data 等价物） |
| `dae2122214` | turn metadata 状态所有权简化（去 Arc 包裹） | `turn`（重构） | N/A（Go turn metadata 走 JSON client header，非同构结构） |
| `7dd2f689e9` | 简化 contextual user fragment 注册（删冗余 trait 方法） | `context`（重构） | N/A（Go fragment 接口不同构） |

### 4. 工具执行
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `7750465934` | apply_patch 运行统一：直接工具调用与 shell 拦截共用执行路径 | `applypatch`、`tool` | N/A（Go `ApplyPatchExecutor` 已是统一执行路径） |
| `d6407d7359` / `1b594980f3` | MCP resource list/read 处理去重（公共 handler） | `mcp/resource.go` | 待落地（P3） |
| `ca2b47997e` | 注册阶段不构建 code-mode 定义（spec_plan 惰性化） | `codemode`、`tool` | 待落地（P3） |

### 5. code mode / 实时
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `60c722e075` | code mode 双 WebSocket 传输（host 侧 transport/peer 扩展） | `codemode/websocket_transport.go` | 待落地（P2） |
| `8e3b5d3e87` | stalled code-mode host 请求超时 | `codemode/remote_provider.go` | **完成**（本轮：wait/terminate 传输超时 + 连接失效重连） |
| `155f1ca9e5` | code mode 工具分析与模型响应关联（analytics facts/reducer） | `codemode`、`telemetry` | 待落地（P3） |

### 6. exec / TUI / 终端
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `41e2f67e56` | `exec resume --last` 状态库优先；校验索引 rollout 的 session id 与 thread id 一致；全 miss 后回退扫描 | `exec/exec.go` | N/A（Go exec 以 session store 为权威源，无状态库优先路径；记录） |
| `9c8f9ce897` | 本地 session archive 命令优先使用 SQLite 名称 | `tui/session_archive_commands.go` | N/A（Go `listSessionsByArchived` 已 store 优先 + rollout 修复，语义等价） |
| `db1a414569` | Ghostty 不请求 key-release 事件（`xterm-ghostty` 识别 + flags 抑制） | `tui`（terminal/keyboard） | N/A（Go TUI 未构造 push flags，仅解析 probe；记录） |
| `b258c028fe` | 按环境登录 shell 策略：shell 工具仅当所选环境允许时才暴露 `login` 参数，按环境校验命令 | `shell`、`session`、`tool` | N/A（Go 按工具注册单环境 AllowLoginShell 校验；无多环境选择模型） |

### 7. config / 执行 / 沙箱 / git
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `78306a32af` / `224ea64cdc` | config layer 迭代 API 澄清；active user layer 由 layer 栈反向扫描推导（移除缓存索引） | `config/api.go` | N/A（Go ConfigService 直接持有 userConfig/profile，无缓存索引问题） |
| `3149fa4b99` | 超时 Git 命令终止整个进程树（Unix 进程组 / Windows Job Object + CREATE_SUSPENDED） | `exec`（git 元数据命令）、`utils/pty` | 待落地（P3） |
| `82ccbc757a` | Linux managed proxy helper 生命周期加固 | `sandbox`（Linux-only） | N/A（Linux-only，Go sandbox 为 Windows 路径） |
| `136f75e7b7` | network policy 并发测试事件捕获稳定化 | `network`（测试） | N/A（Rust tracing 测试基建，Go 无等价 callsite 缓存） |
| `cc03518c36` | audio preparation 提取为 utils 工具 crate（纯重构） | `utils`（音频） | N/A（Go 无 audio_preparation 模块可迁移） |

### 8. 遥测 / 构建 / 测试（Rust-only 或行为不进入 Go 协议面）
| 提交 | 主题 | Go 对应组件 | 状态 |
|---|---|---|---|
| `5af85998c2` | API request 指标不进 Statsig 导出（runtime-only） | `telemetry` | N/A（Go 无 Statsig/OTLP metrics exporter，HTTP analytics 路径不受影响） |
| `b2dc8b3e4b` / `64bb8094ba` | 审批遥测上下文合并；approval resolver 命名 typo 修复 | `tool`、`telemetry` | N/A（Rust 内部重构，Go 审批流不同构） |
| `db1a414569`（另见上） | Ghostty key-release | — | 见上 |
| `f94b5d899a` / `c82cb044f3` / `8922a784fe` / `c39d3e99d5` / `87e2d41eb3` / `bbcf5e10fb` / `51d4aa946c` / `bbcf5e10fb` | 各项 Rust 集成测试稳定化 / V8 checksum | Rust-only | 不落地，仅记录 |

## 二、本地 codex_go 当前状态

- 最新对齐提交：`066b4c5`（Fix app-server protocol schema/manifest parity and update Rust baseline），工作区干净，本地领先 origin 2 个提交。
- Go 已具备上一轮 `bb5054fe47` 的 `CodexRolloutBudgetUnits` 解析/携带（`model/agent.go`、`model/responses_agent.go`），但 `runtimeutil.Budget.RecordUsage` 尚未按 provider units 扣减，且仅测试引用、未接入 turn 循环（对齐后仅修正记账原语语义，接入工作另列）。
- `mcp/pagination.go` 目录上限仍为 1,024（Rust 标准 2,048 为上一轮 5157493c23 遗留未对齐项），本轮一并补齐。
- Go exec 使用 `session.Store`（JSON）作为 resume 权威源，无状态库优先路径；`41e2f67e56` 判定为架构差异，不落地。
- Go TUI 不构造键盘增强 flags 请求（仅解析 probe 响应），`db1a414569` 判定为 N/A。

## 三、本轮对齐计划（阶段）

| 阶段 | 范围 | 状态 | 验证 |
|---|---|---|---|
| P0 | 生成本轮计划文件，确认上游范围与基线 | 完成 | `git pull` 最新、commit 范围核对 |
| P1 | `runtimeutil.Budget`：`TokenUsage` 增加 `CodexRolloutBudgetUnits json.Number`；`RecordUsage` 改为 `(bool, error)`——存在 units 时校验有限且非负（否则 fatal 错误）并按 units 扣减，缺失时回退加权 token 记账 | 完成 | `go test ./runtimeutil/...` |
| P2 | `mcp` 目录上限：标准 1,024→2,048；`ServerConfig.CatalogItemLimit`（默认 2,048）+ `CodexAppsServerConfig` 设为 8,192；`collectMCPPaginated` 支持按 server 限额，http/stdio client 透传 | 完成 | `go test ./mcp/...` |
| P3 | 本轮续做已落地：code-mode host 请求超时（8e3b5d3e87）、Agent Plugins mcp.json（bd12b3a9ec）、ModelMessages 统一（df72fdb415）、插件使用说明能力门控（e4e0c7070e）；其余大规模项（per-surface exposure、权限重注入、MCP resource 去重、双 WS、Git 进程树等）留待后续 | 完成（本会话） | go test ./codemode ./plugin ./model ./appserver 全绿 |
| P4 | 全量验证：`go build ./...`、`go test ./runtimeutil/... ./mcp/... ./model/... ./turn/... ./exec/... ./appserver/...`、`go vet` 变更包；更新 `parity.json` rustBaseline | 完成（本会话） | 全部 ok |
| P5 | 提交并输出对齐总结 | 完成（本会话） | git log 确认 |

## 四、风险与注意

1. `runtimeutil.Budget` 目前未接入 turn 循环（Go 侧 rollout budget 记账尚未接线）；本轮只对齐原语语义与测试，接入会话预算属后续独立工作。
2. Go `ServerConfig` 无 Rust 的 `McpServerSource` 概念；本轮以“仅 `CodexAppsServerConfig` 构造时置 8,192”近似 host-owned 语义，扩展/插件注册的 `codex_apps` 名保持 2,048。
3. `41e2f67e56`、`db1a414569`、`5af85998c2` 因 Go 架构差异标记 N/A，后续若 Go 引入 exec 状态库路径 / 键盘 flags 构造 / OTLP metrics 再对齐。
4. 大量提交为 Rust 集成测试稳定化或 CI/构建侧变更（Rust-only），不进入 Go 功能对齐清单。

## 五、后续执行顺序

1. P1：rollout budget units 记账原语 + 测试。
2. P2：MCP 目录上限（含 2,048 基线补齐）+ 测试。
3. P3：按功能域逐项落地清单内“待落地”项，每项跑对应 `go test -count=1`。
4. P4：全量构建/测试/vet，更新 `parity.json`。
5. P5：提交并输出对齐总结。
