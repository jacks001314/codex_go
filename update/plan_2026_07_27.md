# Codex Go 对齐计划（2026-07-27）

## 1. 目标与结论

本轮的 P0 架构切片已经从“只有 `ThreadManager`”推进到统一持久化 owner。Go HEAD
`b20094292f` 之上的 worktree 已增加 `session.LiveThread`、init guard 和 appserver 组合 owner，集中管理
record store access、可选 writer lock、rollout recorder 串行化与关闭；active turn、diff、ephemeral
record、订阅、status 和 terminal ledger 仍由现有 `ThreadManager` 统一管理。当前 P0 的 LiveThread、完整
线程生命周期、paginated fork 和 replay 范围已经完成实现并通过 store/router/raw-event/race 门禁；Windows/Linux
的 P1 代码审计、缺口实现和 focused/race 门禁也已完成；P2 skills/plugins/deferred tools 的 12 个提交
经逐项审计均已存在于当前 Go HEAD，并通过 focused/race 门禁；非 macOS 的 P3 也已完成实现、审计和
focused/race/SDK/full 门禁。48 个提交现在均有 `done/audit` 或有证据的 `no-port` 结论。

实施优先级如下：

| 优先级 | 工作域 | 目标 |
| --- | --- | --- |
| P0 | Live thread 状态模型 | 建立单一线程生命周期和资源所有权边界 |
| P0 | Paginated fork | 对齐 Rust 的分页 fork、ephemeral fork、边界和 replay 契约 |
| P1 | Remote code mode | 对齐远程 host、WebSocket、fallback 和 Responses metadata |
| P1 | MCP runtime | 对齐跨线程 refresh、auth 变化、authority 和重连语义 |
| P1 | Network / exec-server | 对齐 approval 取消与并发、network policy request 和 hook 输出 |
| P2 | Skills / plugins | 补齐 executor skill、Agent Plugin 和 publish 协议 |
| P3 | TUI / analytics / Rust-only | 独立吸收用户可见改动，内部重构不机械移植 |

本计划采用渐进迁移，不一次性重写 `appserver.RuntimeRouter`。先引入统一对象和契约测试，再按
handler 逐条迁移；旧字段只能作为短期适配层，迁移完成后删除。

## 2. Rust 参考与当前基线

- Go 仓库 HEAD：`b20094292fdfc14ec55da711a9244d4b01bc82b6`
- Go worktree：执行中为 dirty，包含本计划尚未提交的实现和测试
- 正式 parity baseline：`9d82334302`，`codex-cli 0.145.0`
- 2026-07-24 已处理到的 Rust 参考：`94ebae725e`
- 当前 Rust HEAD：`18f50c9e628af083a52d9240de09fc2db24d79ce`
- 本计划审计范围：`94ebae725e..18f50c9e62`
- 非合并提交数：48
- Rust 仓库更新结果：从 `61a44880a8` fast-forward 到 `18f50c9e62`

`parity.json` 在所有门禁完成前保持正式基线不变。`18f50c9e62` 是本计划的候选对齐参考，不能仅因
代码已合入就更新 parity baseline；必须有 Rust/Go 双端 artifacts 和无 first mismatch 的差分报告。

### 2.1 2026-07-27 执行进度

- Rust candidate：`codex-cli 0.0.0`，SHA-256
  `db25932ce7bdb067a20431cb7653ee0943b46e3a8de248d7dec6c3b6dbf63bde`。
- Go candidate：`codex-cli 0.0.0`，SHA-256
  `04eabaf188ab8c9b888b50a869bf57425afb673d09baee77dae166c6c1ca5eec`。
- SDK：`@openai/codex-sdk 0.0.0-dev`，dist SHA-256
  `0970cdd99525e6675804b81a3dc7197a682cfeda140a929839bdbfd3b6f02434`。
- `streaming-smoke`、`js_exec_single_trace`、`js_exec_two_commands`、`js_exec_rejection_catch`、
  `js_exec_failure_no_retry`、`js_exec_no_empty_dispatch`、`js_exec_resume_no_replay` 和
  `js_exec_parallel_join` 已完成真实双端运行并通过，均为 `firstMismatch: null`；分类为
  `baseline-drift`，因为正式 `parity.json` 尚未推进。
- 最终候选最新通过 artifacts：`2026-07-27T061105856Z-streaming-smoke`（`go-rust` 顺序）、
  `2026-07-27T043324333Z-js_exec_single_trace`、
  `2026-07-27T043358771Z-js_exec_resume_no_replay`、
  `2026-07-27T061209180Z-raw_paginated_fork`；此前通过矩阵还包括
  `2026-07-27T035042737Z-streaming-smoke`、
  `2026-07-27T031530469Z-js_exec_two_commands`、
  `2026-07-27T031603380Z-js_exec_rejection_catch`、
  `2026-07-27T031924866Z-js_exec_failure_no_retry`、
  `2026-07-27T032006223Z-js_exec_resume_no_replay`、
  `2026-07-27T032734725Z-js_exec_parallel_join`。
- 最终候选额外完成 43 个非 opt-in SDK 场景审计：41 个通过；其中本轮修复并复跑通过
  `apply-patch-absolute-path-failure`（`2026-07-27T074752038Z`）、
  `command-unicode-output`（`2026-07-27T074724173Z`）、
  `real-coding-modify`（`2026-07-27T074900322Z`）、
  `real-coding-unittest`（`2026-07-27T075223447Z`）和
  `command-missing-file`（`2026-07-27T075055289Z`），均为 `firstMismatch: null` / `baseline-drift`。
- SDK 差分发现并修复两个 Go 协议缺口：已解析但应用失败的 `apply_patch` 现在仍发出
  `file_change` started/completed failed 与 changes；Windows PowerShell 命令统一设置 UTF-8 输出编码，
  不再把中文和校验符解码为替换字符。对应 focused、runtime lifecycle、race 和真实 SDK 回归均通过。
- SDK 场景合同同时消除了两类误报：真实编码任务按 semantic tool lifecycle 比较，不要求模型生成源码
  哈希或 started/completed 交错完全一致；缺失文件错误按稳定文件名匹配，不依赖 Windows 显示语言。
- 其余 2 个非通过场景均有双端/上游证据：`resume-long-context-tools`
  （`2026-07-27T070529758Z`）在 Rust/Go 第五轮对称失败，属于 SDK/模型长上下文假设；
  `resume-tool-interrupted-command`（`2026-07-27T070952570Z`）仍由 Rust/Windows abort 后子进程写出 marker，
  Go 正确取消。二者不作为 Go 实现回归，也因此不推进正式 baseline。
- `js_exec_interrupt_once` 保留为未通过的 Rust/Windows baseline 诊断：Go 在 abort 后取消子进程，
  resume 得到 `MARKER_ABSENT`；Rust `18f50c9e62` 的子 PowerShell 继续运行并写出 marker，第二轮最终
  AbortError。artifact：`2026-07-27T032247216Z-js_exec_interrupt_once`。这不是 Go 修复目标，且不能宣称 parity。
- `session.LiveThread` / `LiveThreadInitGuard` 已落地；manager 对 paginated 和 legacy persisted thread
  都持有 live handle，paginated thread 额外持有 writer lock。appserver 组合 owner 串行化短生命周期
  rollout recorder，并在每次 append 后 flush/close，避免 `session`/`rollout` 包循环和 Windows 文件占用。
- `turn/interrupt` 的 terminal goroutine 已纳入 manager worker 计数；active turn 消费与 worker 登记原子化，
  shutdown 不再在 terminal persistence 前返回。
- Router 的 read/save/update/append 已统一走 live forwarding；active fork 也注册 live handle，失败时
  释放 handle、删除 record 和残留 rollout。
- `Store.PrepareFork`、latest/through/before boundary、reference-backed history 和 ephemeral fork 原已存在。
  本轮修复了无原生 turn snapshot 时 synthetic snapshot 被错误计入 source reference 的越界，并把
  active paginated turn 的 prompt/abort 保存为 target local delta，保持 source 不变。
- 官方 TypeScript SDK 当前没有 thread fork API，因此 fork 不伪装成 SDK 覆盖；本阶段使用 raw app-server、
  store/router 集成测试和 race test 验证 boundary、lineage、active-turn delta、回滚及无重复 replay。
- raw app-server paginated fork 审计已实现并通过，Rust/Go 的 source visibility、两个精确 invalid-request、
  pathless ephemeral fork 和 list 副作用完全一致，`firstMismatch: null`。artifact：
  `2026-07-27T075341889Z-raw_paginated_fork`（最终候选复跑）。
- P1 remote code-mode 的 WebSocket transport、host lifecycle、确定性 no-fallback、Lite tool-name metadata 和
  compatibility header 上限原已存在；本轮补齐公开 `--code-mode-host` CLI、`features.code_mode_host` 门禁、
  proxy-aware HTTP client、fallback 配置和 1024 个 pending delegate 的硬上限/恢复测试。
- MCP runtime coordinator 已覆盖 thread-scoped config、requirements/auth revision、显式 refresh、startup/prewarm
  交错、current authority、最终 HTTP client 和 allowlist 语义；`61a44880a8` 仅调整 Rust 编译期宏递归上限，
  Go 无对应移植项。
- Network approval、exec-server policy request 和 hook early-exit output 均已有完整实现；针对 cancel/late
  response/concurrency/amendment/recovery/output preservation 的 race 回归已通过。
- P2 的 system-skill watcher、executor skill tools/resource authority、metadata path aliases、Agent Plugin manifest、
  workspace publish capability、remote plugin ID、thread-scoped warnings、deferred namespace/source dedupe 和 host
  wait description 均已存在；12 个对应提交完成逐项代码审计，并通过 plugin/prompt/tool/turn/appserver focused/race。
- P3 的响应式 keymap、mention candidate freshness、`ent26`、Rust completion timing replay 和 ephemeral
  hook transcript/local-store 隔离均已完成；timing 同时覆盖 snake/camel 字段，旧 rollout 缺字段兼容。
- P2 最终验证额外通过 `go test ./... -count=1`、SDK comparator 8/8、raw fork protocol regression 和
  `go-rust` 双端 streaming smoke；live artifact 为 `2026-07-27T061105856Z-streaming-smoke`，
  `firstMismatch: null`，分类 `baseline-drift`；raw artifact 为 `2026-07-27T061209180Z-raw_paginated_fork`。
- 全量验证期间发现 exec-server long-poll 并发测试把 PowerShell 冷启动包含在 1 秒输出窗口内；测试已改为
  先读取 readiness marker，再验证 pending read 期间 write 不被串行阻塞，并通过普通 50 次和 race 10 次。
- `go test ./... -count=1`、
  `go test -race ./appserver ./session ./mcp ./execserver ./codemode ./tool -count=1`、
  SDK comparator 8 个 unit tests 和 `git diff --check` 已通过。全量测试首次运行暴露 MCP 并发启动测试
  依赖固定 100ms 进程启动窗口；已改为 observer/helper 显式握手，并额外通过普通 50 次和 race 10 次重复。
- 最终生产改动后再次通过 `go test ./... -count=1`、SDK comparator 8/8、
  `go test -race ./tool ./appserver ./rollout` 的本轮 focused 集、raw paginated fork 和 `git diff --check`。
- 正式 `parity.json` 保持 `0.145.0` / `9d82334302` 不变：最终 live SDK 场景仍按规则分类为
  `baseline-drift`，且 Rust/Windows `js_exec_interrupt_once` 的子进程残留诊断尚未解除。
- 本轮按用户要求不处理 macOS；macOS 不参与阶段退出条件或最终门禁。

## 3. 当前架构诊断

### 3.1 Go 当前状态

- `appserver/thread_manager.go` 已是在线线程 registry，统一管理 active turn/cancel handle、diff tracker、
  ephemeral record、connection subscription、writer lock、thread status 和 terminal claim。
- `RuntimeRouter.Close()` 已显式阻止新 turn、取消 active turn、等待 worker 并关闭共享 service；Rust
  `5a3808b77d` 的 in-process outbound channel 在 Go 没有对应通道 owner，因此按架构差异处理。
- `session.LiveThread` 已串行化 store read/save/append 与 close，paginated history 的跨进程 writer
  ownership 由同一对象释放；appserver `managedLiveThread` 负责 rollout recorder 的打开、flush、关闭和并发顺序。
- thread-local record persistence 已统一经 `LiveThread`；approval、MCP 和 pending exec 保持现有 service
  ownership，并已通过完整测试和 race test，后续只在对应 P1 协议变更中继续演进。

P0 剩余风险不再是未迁移 handler，而是官方 SDK 尚未暴露 fork API，以及 Rust/Windows interrupt baseline
自身的子进程取消问题。paginated fork 当前范围已有 store/router/raw 证据；待 SDK 暴露 fork API 后再增加
SDK 黑盒层，不因此阻塞 Windows/Linux 的 P1。

### 3.2 Rust 对齐方法

参考以下实现，不照搬 Rust 类型系统，但保持职责和状态转换一致：

- `codex-rs/core/src/thread_manager.rs`
  - `ThreadManager` 维护在线线程 registry 和共享服务
  - thread start/resume/fork 统一进入 manager
- `codex-rs/thread-store/src/live_thread.rs`
  - `LiveThread` 绑定线程存储、初始化、append、metadata 和关闭生命周期
- `codex-rs/core/src/session/session.rs`
  - session 创建/恢复时初始化 `LiveThread`
- `codex-rs/app-server/src/request_processors/thread_processor.rs`
  - paginated fork 先 `prepare_fork`，再按 boundary 创建目标线程

Go 的目标不是复制 Rust crate 边界，而是复制以下行为：单一 registry、单一资源 owner、初始化失败原子
回滚、terminal event 幂等、关闭顺序确定、持久化策略由线程来源和 history mode 决定。

## 4. 不可回归契约

以下契约在 Phase 0 先固化，之后每个阶段都必须通过：

1. fresh thread 在首次 user message 前不创建 rollout 或 persisted `thread/list` 记录。
2. `ThreadStartParams.Prompt` 只影响启动配置，不伪造成 user item。
3. ephemeral 或 non-local thread 不落入本地持久化列表。
4. 每个 command item 的 `started` 和 `completed` 各出现一次。
5. 每个 turn 的 final agent message 和 `turn/completed` 各出现一次。
6. final agent message 必须先于对应 `turn/completed`。
7. shell/tool rejection 必须以 JavaScript Promise rejection 返回，可被 `try/catch` 捕获。
8. JavaScript runtime 不得派发空 `exec`，也不得把失败调用静默重试成重复命令。
9. writer ownership 冲突必须在任何 rollout、DB、registry 副作用之前原子失败。
10. resume/fork 不重复 replay tool output、command completion 或 final message。
11. paginated cursor 与 non-paginated cursor 不混用；fork lineage 禁止错误的增量游标读取。
12. interrupt、disconnect 和 shutdown 竞争时，只能产生一个 terminal outcome。
13. hook 子进程在读取 stdin 前退出时，已经产生的 stdout/stderr 仍被保留。
14. item completion 的 start time 必须来自原始 started event，不能在 completion 时重新生成。

## 5. Phase 0：冻结基线和契约证据（P0）

### 工作项

- 为候选 Rust 和 Go 二进制记录 version、commit 和 SHA-256。
- 为现有 65 个 SDK 场景生成清单，保持 approved/opt-in 分层；新增场景不得默认扩大真实网络或费用。
- 扩展 normalizer，使 ID 映射保留引用关系，不删除 command/turn/item identity。
- comparator 首先报告 first mismatch，并区分：
  `go-bug`、`sdk-assumption`、`baseline-drift`、`model-nondeterminism`、
  `platform-difference`、`infra-failure`。
- 建立 raw app-server event audit，专门检查 SDK 归一化前的 notification 次数和顺序。
- 将第 4 节契约变成 table-driven Go 测试和可离线 replay 的 SDK fixtures。

### JavaScript 工具执行场景

以下 prompt 使用固定本地命令和固定输出，不依赖模型自由发挥：

| 场景 ID | Prompt 核心要求 | 断言 |
| --- | --- | --- |
| `js_exec_single_trace` | 使用 JavaScript 调用一次 `exec`，输出固定文本 `JS_EXEC_OK`，随后直接回答结果 | 一次 started、一次 completed、一次 final、一次 turn completed |
| `js_exec_two_commands` | 在同一 JavaScript cell 中顺序执行两个固定命令，输出 `STEP_1`、`STEP_2` | 两个不同 item，各自完整且不重复 |
| `js_exec_rejection_catch` | 在 `try/catch` 中调用确定会被策略拒绝的 shell，并输出 `CAUGHT` | rejection 可捕获；无进程副作用；turn 正常完成 |
| `js_exec_failure_no_retry` | 执行一个固定非零退出命令，报告 exit code，不允许重试 | 只有一次命令调用；completed 含失败状态 |
| `js_exec_no_empty_dispatch` | JavaScript 只进行本地值计算并返回 `42`，不得调用 shell | raw event 中没有空或任意 exec |
| `js_exec_resume_no_replay` | 第一轮执行固定命令，resume 后只读取上一轮结论并回答 | 第二轮不重复第一轮 command events/tool output |
| `js_exec_interrupt_once` | 启动可控长命令后 interrupt | cancel、command terminal、turn terminal 均最多一次 |
| `js_exec_parallel_join` | 并发执行两个有不同固定延迟的命令并 `await Promise.all` | item 可交错，但每个 item 因果顺序完整，final 只出现一次 |

“查询云南各城市今天的天气”保留为 opt-in 网络诊断场景，用来检查 web/network tool 的过程是否可见、
是否重复和是否完整。它依赖实时数据、城市口径和外部服务，不作为 deterministic parity release gate。

### 退出条件

- 所有新增场景都能在保存的 raw artifacts 上离线重跑 comparator。
- `run-manifest.json` 包含 OS/arch、Node/Go/Rust、双方 commit、SDK version、binary hashes、模型和场景。
- 报告能指向第一条不一致 raw event，而不是只报告最终文本不同。

## 6. Phase 1：补齐 LiveThread 并完成现有 ThreadManager 迁移（P0）

### 目标结构

现有 `appserver.ThreadManager` 不重建 registry；只增加缺失的 session 资源 owner，并逐步让 manager
持有它。目标结构如下，命名可在实现时按 Go 包边界微调：

```text
appserver.ThreadManager
  liveThreads[ThreadID] -> managedLiveThread
  ActiveTurn / Subscribers / DiffTracker / Status / TerminalLedger

appserver.managedLiveThread
  session.LiveThread
  Rollout serializer / short-lived Recorder

session.LiveThread
  Store / ThreadID / HistoryMode / optional WriterLock
  Init guard / Read / Save / Append / Close
```

### 迁移规则

- `ThreadManager` 是在线线程 registry 的唯一入口；禁止 handler 直接写多个 router map。
- `session.LiveThread` 是 persisted record store access 和 writer lock 的 owner；appserver 组合 owner 负责
  rollout recorder，避免 `rollout -> session -> rollout` 包循环。
- 使用 init guard：任一步初始化失败，逆序释放 recorder、writer、registry 和临时状态。
- manager registry 锁不包耗时 I/O；每 thread 的 store/rollout 资源锁覆盖必要 I/O，使 close 与 append 原子排序。
- `TerminalLedger` 以 `(threadID, turnID)` 记录 terminal outcome，notification sink 重试不能重复发送。
- `RuntimeRouter` 第一阶段保留同名 helper，但内部转发到现有 manager；禁止再引入第二套 registry。
- close 顺序固定为：阻止新 turn -> cancel/wait active turn -> flush terminal -> close recorder -> release writer ->
  remove registry -> notify status。

### 第一批迁移

1. `thread/start` 和首次 user message 的 lazy persistence。
2. `thread/resume` 的 writer ownership、record load 和 replay。
3. `turn/start`、`turn/interrupt`、turn completion。
4. router/connection shutdown。

### 当前状态

- `thread/start`、`thread/resume`、持久化 `thread/fork` 已注册 live handle；初始化失败会逆序释放并回滚。
- runtime record read/save/append 和 rollout append/turn terminal/compaction 已通过 live owner。
- archive/delete 会从 manager 移除并关闭 live handle；router close 按稳定 thread ID 顺序关闭剩余 owner。
- interrupt terminal worker 与 shutdown wait 已原子协调，定向 race 20 次及完整 race 套件通过。
- 本阶段已完成。ephemeral thread 继续使用 manager 内的独立内存 record，这是“不持久化、不进入 list”的
  契约实现，不是第二套 persisted owner；持久化 handler 已统一通过 live forwarding。

### 退出条件

- 旧的 `active`、`diffs`、`ephemeralThreads`、`threadSubscriptions` 至少不再被首批 handler 直接修改。
- start/resume/turn/interrupt/shutdown 全部通过 race test。
- 注入每个初始化步骤失败时，无残留 lock file、rollout、registry entry 或 terminal notification。

## 7. Phase 2：迁移完整线程生命周期（P0）

### 当前状态

已完成。read/save/update/append、list/read、fork、archive/unarchive/delete/rollback 和 metadata/name 路径均
通过统一 helper 与 live owner；fork 在创建 target 前预占 source writer，失败会删除 target record、rollout
和 live handle。fresh/name-only、ephemeral、non-local 的列表可见性和持久化策略已有回归测试。

### 工作项

- 迁移 `thread/read`、`thread/list`、`thread/turns/list`、`thread/items/list`。
- 迁移 `thread/fork`、archive/unarchive/delete/rollback 的资源预占和释放。
- archive/delete 对 descendant tree 按稳定顺序批量获取 writer ownership；任一冲突则整批无副作用失败。
- 统一 fresh、persisted、ephemeral、non-local、imported thread 的 persistence policy。
- hook transcript 不为 non-local thread 创建本地 session。
- metadata update、name update 和 item append 通过 `LiveThread` 排序，不直接改 record 后另行落盘。
- item start time 在 started 时进入 turn timing state，在 completion projection 中读取。

### 退出条件

- 所有 thread handler 只通过 manager 获取 live state。
- list/read 不会暴露 fresh、ephemeral 或 non-local 本地记录。
- concurrent resume/fork/archive/delete 的成功/冲突行为与 Rust raw JSON-RPC 对齐。

## 8. Phase 3：Paginated fork 与 replay 对齐（P0）

### 当前进度

已完成。Go 的 `Store.PrepareFork`、三类 boundary、reference-backed `HistoryBase`、ephemeral pathless fork、
source deletion protection 和对应 store/router tests 已闭环。本轮修复 active turn local delta、synthetic
snapshot lineage、重复 turn ID 的 newest-boundary 选择和 cold-resume replay，并覆盖 source writer 冲突与
target 初始化失败回滚。官方 TypeScript SDK 没有 fork API，因此当前门禁以 raw app-server、store/router
集成测试和 race test 为准，不重写现有 fork store。

### Rust 参考

- `05f000263b`：paginated thread fork
- `1811b67a84`：ephemeral fork of paginated thread
- `codex-rs/thread-store/src/local/paginated_fork.rs`
- `codex-rs/app-server/tests/suite/v2/thread_fork.rs`

### 工作项

- 在 Go thread store 增加 `PrepareFork`，输入 boundary：
  `Latest`、`ThroughTurn(turnID)`、`BeforeTurn(turnID)`。
- `PrepareFork` 返回已验证的 model context、terminal prefix、source metadata 和 rollout lineage；handler
  不自行拼接 JSONL。
- active paginated turn 被 fork 时，source snapshot 只能落为一个 interrupted terminal state。
- paginated fork 使用 reference-backed lineage，禁止伪装成普通 rollout 后继续使用 incremental cursor。
- ephemeral paginated fork 要求 `excludeTurns=true`，保持 pathless、不进入 list，并可继续 turn。
- 保留 approval reviewer、thread name、CWD、model context、config snapshot 和合法父子关系。
- fork replay 以 `(turnID, itemID)` 去重，保留 creation ordinal，仅推进 update ordinal。

### 必测边界

- latest、through named turn、before named turn。
- compacted history、缺失 boundary、active turn、空 history。
- persisted 与 ephemeral 目标。
- source writer 冲突、target 初始化失败、fork 后 resume。
- paginated/non-paginated cursor 误用必须返回明确 invalid request。

### 退出条件

- Rust `thread_fork.rs` 中可映射的行为测试在 Go 有对应 table cases。
- raw app-server 场景中无重复 user/tool/final item，fork 后工作区副作用只发生一次；SDK 增加 fork API 后，
  再补同一组 SDK 黑盒场景，当前不把不存在的 SDK surface 计为缺失实现。

## 9. Phase 4：Remote code-mode host 与 metadata（P1）

### 当前判断

已完成并审计。Go 原有 Sobek JavaScript engine、in-process/remote session host、WebSocket transport、
app-server host lifecycle、no-fallback plumbing、实际 tool-name metadata 和 bounded compatibility headers。
本轮补齐公开 CLI/config 入口、feature gate、proxy-aware HTTP client 和 1024 个 pending delegate 上限；
overflow 返回确定错误且连接保持可用。

### 工作项

- 对齐 `0dfa778dae` 的 WebSocket transport：frame limit、close、ping/pong、协议错误和断线传播。
- 对齐 `f61b51ddd9`：app-server 接收 remote host 配置，按 thread/session 创建连接并传给 code-mode runtime。
- 对齐 `cba0e2701c`：支持显式关闭 in-process fallback；remote 不可用时返回确定错误，不静默换执行引擎。
- 对齐 `25b6fc9bbc`：Responses Lite metadata 包含实际 code-mode tool names。
- 对齐 `5c36e869c1`：compatibility metadata headers 有稳定上限和确定截断策略。
- remote disconnect、turn interrupt 和 app-server shutdown 都通过 `ManagedThread` 关闭，不另建旁路 registry。

### 验证场景

- `code_mode_remote_ws_success`
- `code_mode_remote_ws_disconnect`
- `code_mode_remote_no_fallback`
- `code_mode_metadata_tool_names`
- `code_mode_metadata_bounded`
- 第 5 节全部 JavaScript tool execution 场景

## 10. Phase 5：MCP runtime refresh 与 authority（P1）

### 当前判断

已完成并审计。`mcpRuntimeCoordinator` 已覆盖线程级独立配置、requirements/auth revision 触发、显式 refresh
重连、启动期 refresh/prewarm 协调、原子 runtime 替换和 current authority；OAuth 使用最终 runtime HTTP
client，无 allowlist 时跳过 plugin filtering。相关 startup/close/auth/prewarm 交错均有 race 回归。

`61a44880a8` 只提高 Rust crate 的编译期宏递归上限，不是运行时 MCP 递归协议，因此归类为 `no-port`。

### 工作项

- 在 `ThreadManager` 建立 MCP refresh coordinator，对活跃线程快照后逐线程更新，不持有 registry 锁做网络 I/O。
- thread startup 期间的 refresh 与新 thread 初始化协调，不能漏更新或重复发布 catalog。
- auth revision 变化、managed MCP requirements 变化、独立 thread config 变化均触发正确范围的 refresh。
- background prewarm 完成后原子替换 runtime；失败保留旧健康 runtime 并报告 warning。
- 显式 `mcpServer/refresh` 强制 reconnect；普通无身份变化 refresh 继续复用健康连接。
- elicitation review 在决策时读取 current MCP authority，不使用 turn start 时的过期 snapshot。
- OAuth discovery 和 token 操作使用 runtime 最终 HTTP client，继承 proxy、CA、timeout 和 policy。
- 无 allowlist 时不执行 plugin MCP filtering；有 allowlist 时维持现有 fail-closed 行为。
- 审计 Rust MCP recursion limit 的层级；确认 `61a44880a8` 为编译期宏设置后按 `no-port` 记录。

### 退出条件

- 多线程同时 start、auth change、explicit refresh 时，每个 runtime 只发布一次最终 catalog。
- Apps catalog、资源和工具在可复用连接 refresh 后不回退。
- 本地 MCP fixture 覆盖 stdio/HTTP、断线、auth revision 和递归边界。

## 11. Phase 6：Network approval、exec-server 与 hooks（P1）

### 当前判断

已完成并审计。现有实现覆盖 approval 单次决议、同主机合并、不同主机并发、waiter/owner 独立取消、turn
terminal/connection close 和 late response；exec-server 覆盖 allow/deny/ask、超时、容量、重复/非法 ID 和
连接关闭；hook runner 在提前退出时仍保留 stdout/stderr。本轮 race 门禁通过，`09241ae4db` 保持 `no-port`。

### 工作项

- 对齐 network approval state machine：pending、resolved、cancelled、turn terminal，只有一个状态可获胜。
- 同一 turn 的并发 network denials 可独立等待，不复用错误的 request/response channel。
- interrupt/disconnect 时取消所有未决 approval，late response 被忽略且不改变 terminal outcome。
- exec-server client 接收 network policy server request，并把 decision 返回原连接；恢复/重连不能丢 pending request。
- 保持 network policy amendment 的 Rust snake_case wire shape。
- hook runner 同时 drain stdout/stderr 和写 stdin；子进程先退出时仍保存已输出内容。
- `09241ae4db` 的 reqwest 解耦属于 Rust 内部边界，Go 只审计外部 HTTP/policy 行为，不复制 crate 重构。

### 退出条件

- `go test -race` 覆盖 approval cancel/resolve/interrupt/shutdown 交错。
- raw events 中 approval request、resolution、command completion 和 turn terminal 均不重复。

## 12. Phase 7：Skills、plugins 与 deferred tools（P2）

### 当前判断

已完成并审计，无新增生产代码缺口。Go 已有 Agent Plugins 1.0 schema 与 legacy overlay、authority-bound
executor skill list/read、resource traversal/UTF-8/size 防护、metadata 压力下的 `rN` host path aliases、
workspace `canPublishToWorkspace` wire fields、remote plugin ID 的 skill/script/telemetry 传播、每线程 warning
限长去重、system scope watcher 排除、deferred namespace world state/source 去重，以及有总大小回退的 host
`wait_for_environment` descriptions。相关负向、schema、focused 和 race tests 均通过。

### 工作项

- 支持 Agent Plugin manifest，并与现有 plugin manifest 明确版本和 namespace 规则。
- executor skills 通过 skill tools 暴露，显式 skill 可读取授权范围内 resource。
- resource read 保持 provider authority，不允许将任意 host path 当作 skill resource。
- 暴露 workspace plugin publish capability 和 app-server wire fields。
- remote plugin ID 贯穿 skill metadata、script attribution 和 analytics，但不得泄漏本地绝对路径。
- skills watcher 忽略生成的 system skills，避免 self-trigger refresh loop。
- extension warnings 按所属 thread 发送一次，并进入 terminal ledger 去重路径。
- host skill path 在 metadata 压力下按 Rust 策略压缩，引用仍可解析。
- deferred tool search 记录 namespace，合并 sources 时去重。
- host 可自定义 `wait_for_environment` description，默认描述保持兼容。

### 退出条件

- manifest、authority、resource traversal、warning duplication 和 remote ID 都有负向测试。
- app-server schema snapshot 与 Rust 对应字段一致。

## 13. Phase 8：低优先级用户可见同步（P3）

### 当前判断

已完成实现和审计。keymap action menu 已按终端宽度切换单列描述与双列布局，并覆盖 48/64/96 列；
unified mention popup 每次刷新都会重建 skill/plugin/file candidates，以 generation 和 query 丢弃过期
文件搜索结果；`ent26` 已贯穿 auth/backend plan wire value、Enterprise 显示以及 business/workspace
分类。Rust `item_completed` 的 `started_at_ms`/`startedAtMs` 现可完整回放到 item timing data，旧 rollout
缺字段仍兼容；ephemeral（Go 中与 non-local transcript 语义对应）SessionStart hook 返回 nil transcript，
且不写入本地 thread store。macOS 发布工作不在本轮范围内，也不作为任何退出门禁。

### 工作项

- `20dafe201d`：keymap action menu 响应式布局。
- `322d5b96cf`：mention popup 保持结果新鲜。
- `544007d006`：`ent26` plan type 的协议和显示。
- `af7f6f4d34`：保留 Rust completion event 的 item start/completion timing。
- `99744cfe04`：非本地/ephemeral thread 不因 hook transcript 触发本地持久化。
- tracing、analytics、Bazel 和 Rust test helper 仅在 Go 有对应外部行为或测试价值时吸收。

### 退出条件

- TUI 窄/宽布局、候选刷新与 stale generation 均有 focused regression。
- plan type 的 auth/backend wire 和 workspace/business 分类一致。
- rollout completion timing 同时覆盖 snake_case、camelCase 和旧字段缺失。
- ephemeral hook transcript 为 nil，且本地 store 不存在该 thread。

## 14. 每阶段统一验证门禁

每个 implementation slice 必须依次执行，不能只写“测试通过”：

1. Focused unit tests

   ```powershell
   go test ./<changed-package> -run '<focused-pattern>' -count=1
   ```

2. Package tests

   ```powershell
   go test ./appserver ./session ./rollout ./codemode ./mcp ./execserver ./plugin ./skillprovider -count=1
   ```

3. Race tests（涉及 lifecycle、approval、refresh 时强制）

   ```powershell
   go test -race ./appserver ./session ./mcp ./execserver -run '<focused-pattern>' -count=1
   ```

4. SDK deterministic differential scenario

   ```powershell
   npm --prefix sdktests run test:parity -- --scenario <scenario> --rust <rust-codex> --go <go-codex> --sdk <rust-sdk-typescript>
   ```

5. Raw app-server event audit

   必须保存 request/notification 原文，检查 command/item/turn terminal 次数、顺序和关联 ID。

6. Full Go suite

   ```powershell
   go test ./... -count=1
   ```

7. Phase completion report

   必须包含 `run-manifest.json`、双方 binary hashes、raw artifacts、normalized diff、workspace diff、
   first mismatch、失败分类、实际命令和未执行项原因。

## 15. SDK / CI 场景矩阵

| 阶段 | Approved deterministic | Raw app-server | Opt-in |
| --- | --- | --- | --- |
| 0-1 | JS single/two/failure/no-empty/resume | lazy persist、single terminal、writer conflict | 无 |
| 2-3 | resume/fork no replay | fork boundary、ephemeral、cursor rejection | 大型历史压力 |
| 4 | in-process JS tool matrix、metadata | remote WS lifecycle | remote host 环境 |
| 5 | local stdio MCP refresh | multi-thread refresh、authority | OAuth/remote HTTP |
| 6 | hook early exit | approval cancel/resolve races | 真实 network proxy |
| 7 | local plugin/skill fixtures | schema/capability/warnings | remote publish |
| 8 | 无网络 CLI smoke | 无 | 天气网络诊断 |

CI 分层：

- 每个 PR：Go focused/package/full、normalizer/comparator、offline replay。
- lifecycle PR：额外 `-race` 和 raw app-server audit。
- 定时或受控任务：approved live SDK matrix，默认并发 1，Rust/Go 顺序交替。
- opt-in：OAuth、真实网络、remote publish；失败不能被归一化为 pass。

## 16. 48 个 Rust 提交映射

状态含义：`missing` 需要新增；`partial` 已有相邻能力但语义未完整对齐；`done/audit` 已有行为，需回归
审计；`no-port` 为 Rust 内部构建、测试或 crate 重构。这里是计划入口分类，不是 parity 完成声明。

| Commit | 状态 | Go 处置 |
| --- | --- | --- |
| `18f50c9e62` model/personality world state | done/audit | 已持久化 section snapshot，以独立 developer item 发送 diff，并覆盖首次、变化和 model switch |
| `0896bf6fc0` skip quiet inactive TUI requests | done/audit | 后台线程聚合只读取有 pending input/approval 的 store |
| `9ca6df68da` preserve replayed terminal errors | done/audit | replay 保留 message/codex error info，late completion 不覆盖 active turn |
| `5a3808b77d` explicit in-process router shutdown | no-port | Go 无对应 outbound channel；现有 router 已显式 cancel/wait/close，并有 shutdown 回归测试 |
| `61a44880a8` MCP recursion limit | no-port | 仅提高 Rust 编译期宏递归上限，Go 无对应 crate/macro 限制 |
| `62fd410384` ignore generated system skills | done/audit | system scope 不注册 watcher，生成目录变化不触发 refresh，并有用户目录对照测试 |
| `20dafe201d` responsive keymap menu | done/audit | action menu 使用响应式描述布局，覆盖 48/64/96 列且断言不越界 |
| `322d5b96cf` fresh mention results | done/audit | popup 每次刷新重建 candidates，generation/query 拒绝 stale file results |
| `5c36e869c1` bounded code-mode headers | done/audit | compatibility headers 已有稳定上限、确定截断和边界测试 |
| `af7f6f4d34` item start times | done/audit | 本地 tool timing 已持久化；Rust completion 的 snake/camel start/completion timing 可回放，旧记录兼容 |
| `3a08af44b2` exec-server network requests | done/audit | client 已处理 allow/deny/ask、取消、超时、容量和非法/重复 request ID |
| `4c43465133` skip filtering without allowlists | done/audit | 无 allowlist 跳过 filtering，有 allowlist 保持 fail-closed，并有回归测试 |
| `c3e926e61c` trace remote exec setup | no-port | 不复制 tracing 内部结构；保留诊断字段 |
| `25b6fc9bbc` code-mode names in Lite metadata | done/audit | Lite metadata 使用实际 code-mode tool names，并有矩阵测试 |
| `63fe5a6b71` approval cancellation/concurrency | done/audit | 单次决议、coalescing、waiter/owner cancel、late response 和不同主机并发均有 race tests |
| `cba0e2701c` disable in-process fallback | done/audit | CLI/config feature gate 和确定错误已补齐，显式 remote 不静默回退 |
| `a453588416` sign macOS helpers | no-port | 本轮明确排除 macOS，不作为 Go 对齐门禁 |
| `0d2a0aa76b` remote plugin analytics | done/audit | skill invocation telemetry 已包含 `remote_plugin_id`，本地绝对路径不进入事件 |
| `07fd04abb1` remote plugin metadata | done/audit | remote ID 已贯穿 installed metadata、skill roots、script attribution 和 suggestions |
| `32329b289d` workspace plugin publish | done/audit | share context/save/list 与 app-server schema 已暴露 nullable `canPublishToWorkspace` |
| `1811b67a84` ephemeral paginated fork | done/audit | 已有 pathless fork、`excludeTurns`、list 隔离和继续 turn 测试 |
| `89a3b89c4c` MCP auth via runtime HTTP | done/audit | OAuth discovery/token 使用最终 runtime HTTP client，并继承 proxy policy |
| `544007d006` ent26 plan | done/audit | auth/backend wire value、Enterprise 显示、business/workspace 分类和 JSON schema tests 已覆盖 |
| `99744cfe04` avoid non-local persistence | done/audit | ephemeral hook transcript 为 nil，record 仅在 manager 内存中且不写本地 store |
| `05f000263b` paginated fork | done/audit | 已有 `PrepareFork`/三类 boundary/reference lineage，并修复 synthetic 与 active-turn delta |
| `58b4277228` independent thread MCP config | done/audit | coordinator 按 thread key/config revision 管理独立 runtime |
| `a177013eb0` managed MCP requirements refresh | done/audit | requirements revision 触发 refresh 并替换旧 runtime |
| `000d2540ad` current MCP authority | done/audit | elicitation 在决议时查询当前 authority |
| `3645a4397c` refresh across startup | done/audit | startup 中 invalidation、coalescing 和并行线程均有竞态测试 |
| `fe8500c0a0` executor resource reads | done/audit | 显式 executor skill 注入 resource access；read 绑定 authority/package 并拒绝 traversal |
| `5f6a2c3adb` deterministic Apps test | done/audit | 只吸收 fixture 稳定性 |
| `634a998d8a` hook early-exit output | done/audit | captured stdout/stderr 在子进程提前退出时保留，并有跨平台回归测试 |
| `7c71783135` executor skills via tools | done/audit | `skills.list/read` 暴露 executor provider，支持分页并隐藏 explicit-only catalog entry |
| `1a817bb95d` resume test wait | no-port | Go 对应测试不得留下后台 worker |
| `5a1c54fc21` compact host skill paths | done/audit | 仅在 metadata 压力下启用 `rN` aliases，支持 marketplace 单例折叠和混合 authority 回退 |
| `5dd992acd3` extension warnings to threads | done/audit | warnings 限 4 条/256 bytes，携带 threadId 并按线程/消息去重 |
| `c8957bbf0f` MCP refresh coordination | done/audit | coordinator 合并同线程 refresh，不同线程并行执行 |
| `f201c30c52` reconnect explicit refresh | done/audit | 显式 refresh 强制新 generation，普通 refresh 可复用健康 runtime |
| `6c729ef1c1` refresh on auth changes | done/audit | auth revision 变化替换旧 runtime，并处理 prewarm 期间变化 |
| `ef2d3edb95` background prewarm | done/audit | prewarm coalescing、原子发布、close/startup 交错均有测试 |
| `81da9deb06` custom wait description | done/audit | host 可配置 tool/argument descriptions；组合或 serialized spec 超限时回退默认值 |
| `a28374e0db` Agent Plugin manifests | done/audit | 支持 Agent Plugins 1.0 root manifest、严格 schema/name/path 与 legacy Codex overlay |
| `f61b51ddd9` remote code-mode host | done/audit | app-server lifecycle 已存在，本轮补齐 CLI/config/feature gate 和 proxy-aware client |
| `0dfa778dae` code-mode WebSocket | done/audit | binary frame、ping/pong、size/truncation/text rejection、close/disconnect 均有测试 |
| `f47f28cd0d` Bazel platform data | no-port | Go 无对应 Bazel target |
| `3947f0d0c3` dedupe deferred sources | done/audit | tool search sources 按名称去重并保留首个非空 description；world state 开启时不重复嵌入 |
| `1d4b58f32d` deferred namespaces | done/audit | registry namespace 快照持久化到 world state，支持 initial/add/remove/resume 无重复 delta |
| `09241ae4db` decouple reqwest types | no-port | Phase 6 只审计外部 HTTP 行为 |

## 17. 推荐实施切片

为控制 review 和回滚范围，按以下顺序提交独立变更；每个切片必须自行通过第 14 节门禁：

1. Contract fixtures、raw audit、JS runtime deterministic scenarios。
2. `session.LiveThread` + init guard，不迁移 handler。
3. `ThreadManager` registry + start/resume/turn/interrupt/shutdown 迁移。
4. read/list/archive/delete/fork handler 迁移，删除相应旧 map 直写。
5. paginated `PrepareFork`、lineage、ephemeral fork。
6. remote code-mode WebSocket、fallback、metadata。
7. MCP refresh coordinator、triggers、authority、HTTP client。
8. network approval/exec-server/hook race fixes。
9. skills/plugins/deferred tools 协议。
10. P3 用户可见同步。

禁止把 2-5 合成一次大提交。统一状态对象只有在实际 handler 迁移并删除旧旁路后才算完成，单纯增加一个
wrapper 不算解决缺口。

## 18. 最终完成标准

- Go 的所有在线 thread 由一个 manager registry 管理，资源由一个 live-thread owner 释放。
- 不存在同一 thread 的旧 map/new manager 双写。
- 第 4 节全部契约通过 focused、race、SDK 和 raw-event 四层验证。
- 48 个 Rust 提交均有 `done`、`intentional_difference` 或有证据的 `no-port` 结论。
- approved SDK matrix 双端成功，报告无未解释 first mismatch。
- 更新 `parity.json` 到新 Rust baseline 时，同时提交 binary hash、manifest、场景报告和完整测试记录。
