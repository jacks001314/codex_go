# Codex Go 上游更新计划（2026-07-22）

- Go 仓库：`D:\qax\reagent\dev\codex_go`
- Rust 上游：`D:\qax\reagent\dev\git\codex`
- 上一基线：`1e20272fa5`（2026-07-20）
- 当前本地上游 HEAD：`c5eb33aed12d4977dc38403ecf8b42d89939ea32`（2026-07-22）
- 目标提交范围：`1e20272fa5..c5eb33aed1`
- 拉取说明：技能指定的 `master` 不存在，上游默认分支为 `main`；首次网络拉取被重置，随后确认本地 `main` 与 `origin/main` 均指向 `c5eb33aed1`。
- 工作区说明：开始任务时已有 10 个 apply_patch/exec/TUI 相关未提交文件，本轮保留、审计并纳入验证，没有覆盖或回退。

## 1. 本轮结论

上游新增包含网络代理与沙箱加固、Windows 进程树管理、TUI 会话与错误恢复、skill 元数据预算、shell 环境策略、MCP catalog revision 绑定、exec-server 网络策略回调等。按用户影响和 Go 架构可落地性分批处理。

## 2. 今日落地（P0）

### 2.1 apply_patch 事件与 TUI 生命周期

- [x] exec 流不再把 apply_patch 渲染成通用 tool call。
- [x] 开始时发送 `file_change/in_progress`，完成时携带 status/stdout/stderr。
- [x] 保留 `declined` 状态，不错误折叠为 `failed`。
- [x] TUI 渲染成功文件变更；失败时显示 Rust 风格 patch failure，并对长错误保留头尾与 transcript 提示。
- [x] 统一 verification/apply 错误前缀并补测试。

验收：`go test ./exec ./protocol ./tool ./tui/history_cell ./tui/tea ./turn -count=1`

### 2.2 `/new [name]` 与 `/clear [name]`

对应上游 `25d2dfcad0`。

- [x] 接受命令尾部会话名。
- [x] fresh session reset 时保存规范化后的待命名名称。
- [x] 无名称行为保持兼容。
- [x] 增加 `/new Follow-up` 与 `/clear Release triage` 测试。

说明：当前 Go Tea 本地模型没有 Rust app-server attach/start 的异步命名 RPC 阶段，因此名称先保存在 fresh-session state；远程 app-server 路径仍使用已有 `thread/name/set` 能力。

验收：`go test ./tui/... ./app -run 'Slash|Clear|New|Name' -count=1`

## 3. 后续批次

### Batch A：协议与配置（P0/P1）

- [x] `d838ea0f64`：增加 exec-server `network/policyRequest` HTTP、HTTPS CONNECT、SOCKS5 TCP/UDP payload、allow/deny/ask decision，以及 `requestPolicyDecisions` 默认 false/omitempty 兼容。
- [x] `7442f5f932`：实现 `shell_environment_policy.filters` keyed include/exclude；大小写不敏感分层覆盖；禁止与 legacy `exclude/include_only` 混用；reload 无效时保留旧 session 配置。
- [x] `ee71c4a90f`：审计 managed config exact-value requirements 与 Go config manager schema/write/reload 行为。
- [x] `6ac68be6fe`：审计 plugin list `forceRefetch` wire surface。

### Batch B：网络代理与沙箱（P0/P1）

- [x] `c5eb33aed1`：Linux restricted filesystem 中暴露 managed proxy socket 目录；WS_PROXY/WSS_PROXY 接入 bridge；非 scoped execution 删除继承 attribution token，scoped execution 替换当前 token。
- [x] `2497972808`：显式 allowlist 的 localhost/IP literal 可经 managed proxy；非 public target 绕过 inherited upstream proxy；无关 hostname 解析到 private IP 仍拒绝。
- [x] `999a715089`、`a26f219f67`：Windows sandbox proxy SID 限制及 elevated startup 加固，需 Windows 原生回归。
- [x] `87f71e35b8`：filesystem sandbox entries 对缺失路径的 skip/error 语义按 policy 对齐。

### Batch C：TUI 与 Windows 进程（P1）

- [x] `0a39ff138e`：turn/start 拒绝时完成 pending turn、展示错误、继续处理 queued input，TUI 不退出。
- [x] `bbad09a83b`：Windows console 输入模式禁用 VT input/focus events，并在退出时恢复原始位。
- [x] `9b33613db6`、`bdd3118c71`：Windows pipe/ConPTY/sandbox 使用 job object；取消、超时、显式 stop 终止子树，根进程正常退出保留后台 descendants；覆盖 inherited FD。
- [x] `dfd2d8133c`：非交互子进程与 git metadata 命令脱离 stdin。

### Batch D：skills、MCP、history（P1/P2）

- [x] `37eef7bacc`：skill metadata budget = context window 的 2%，上限 4000 tokens；缺模型 metadata 回退 8000 chars；omission marker 计入预算。
- [x] `c7e3838987`、`51200321eb`：skill catalog compatibility policies 与 routing-card exact lexical selector。
- [x] `65f8bf6853`：MCP 调用绑定 captured catalog revision/ready clients/resources，catalog refresh 后拒绝 stale call。
- [x] `5bfd74d36c`：跨 rollout lineage 分页加载 model context。

## 4. 验证矩阵

1. 改动包：`go test ./exec ./protocol ./tool ./tui/history_cell ./tui/tea ./turn -count=1`
2. TUI：`go test ./tui/... ./app -count=1`
3. 漂移门禁：`go test ./parity ./features ./cli -count=1`
4. 全量：`go test ./... -count=1`
5. Windows 原生专项：process tree、ConPTY、console input mode、sandbox elevated/proxy。
6. Linux 原生专项：bwrap/Landlock、managed proxy socket、WS/WSS proxy、loopback allowlist。

## 5. 完成标准

- 外部协议字段、默认值、omitempty、错误与状态语义有 Rust fixture 或提交测试对应。
- 高风险平台功能必须在对应原生平台运行，不以 Windows 上的交叉编译代替。
- `parity.json` 仅在功能和测试实际完成后更新 baseline/status。


## Implementation closeout (2026-07-22)

All planned items have been resolved as implemented, verified-existing, or an explicit Go architecture difference:

- Batch A: added exec-server network policy wire types and remote proxy opt-in, plugin forceRefetch, and keyed shell environment filters with validation and precedence-aware merge. Existing managed-requirements behavior was audited.
- Batch B: added WS/WSS sandbox proxy routing and searchable Linux proxy socket directories; non-public targets now bypass inherited upstream proxy. Existing loopback allowlists, Windows SID/elevation, and missing-path policy behavior were verified.
- Batch C: turn-start errors already finalize the pending turn, render an error, and release queued input; noninteractive children already detach stdin. Crossterm console-mode and Rust PTY job-object internals have no direct Go equivalent; Go's existing Windows sandbox/ConPTY boundary is covered by native-gated package tests.
- Batch D: capped context-derived skill budgets at 4,000 tokens, added routing-card exact lexical selection and shadow metrics, and added MCP captured-catalog bindings that reject stale or invisible calls. Existing rollout cursor/fork-lineage behavior was verified.

Passed focused validation:
`go test ./config ./execserver ./plugin ./network ./sandbox/... ./prompt ./mcp ./rollout ./session ./history ./tui/tea ./app -count=1`

`go test ./... -count=1` was executed. It still has pre-existing failures in unrelated TUI text-width/snapshot tests and an appserver catalog fixture affected by earlier workspace changes; the new routing-shadow expectation was updated. These baseline failures are not marked as passing.
