# Codex Go upstream alignment plan - 2026-07-30

## Baselines

- Go repository baseline: `1ceca7dacc48c4fb59410304688376958e5281b3`
  (`feat: align with Codex upstream through fa1d4c40d0`).
- Pre-existing Go worktree changes are retained and are outside this alignment
  unless a required fix overlaps them: `.agents/skills/update/SKILL.md`,
  `tui/app/history_ui.go`, `tui/app/history_ui_test.go`, and the untracked image
  and temporary artifacts reported by `git status`.
- Rust repository: `D:\qax\reagent\dev\git\codex`.
- Previous audited and formal parity baseline:
  `fa1d4c40d0e63eef2e0ba8a9e004ccd0a80b77f5`.
- Current Rust head after the user completed `git pull origin main`:
  `25eecb071e7f551516d63fe41c922678d51889c2` (`Harden the file change
  approval integration test`). The pull transcript is retained in the Rust
  repository as the untracked `git.txt`.
- Current incremental range: `fa1d4c40d0..25eecb071e` (82 non-merge commits,
  532 changed files, 19,799 insertions, and 4,875 deletions).
- `parity.json` remains at `fa1d4c40d0` until the applicable implementation,
  focused tests, full Go gates, and current-source Rust/Go differential evidence
  all pass.

## Required work

### A. Protocol and user-visible behavior

- [x] `8f00b9a04c`: tag feedback reports with the selected turn's model and
  reasoning effort rather than only the current session defaults.
- [x] `12b961d4c5`, `3a797496f1`, `28f3f1f9ef`: expose plugin eligibility and
  installation metadata and keep recommended plugins independent from tool
  suggestions.
- [x] `03edf16f0b`: accept plaintext collaboration messages without losing the
  existing structured message contract.
- [x] `101d6b8cb2`, `8707a35113`: preserve foreign background-terminal paths and
  normalize Windows namespace paths when converting paths to URIs.
- [x] `9f23e97797`: include source session titles in external-agent import
  histories.
- [x] `0a6616f4cf`, `6c13b113a3`, `250de82bfb`, `fe01054a28`: identify the
  primary environment, update readiness in place, use step environments for MCP
  file uploads, and inherit ready environments when spawning agents.
- [x] `b96ebfb312`, `d06c7ac055`, `e1895710ad`, `1e3c0042eb`, `a1286d12a2`:
  align blocked-goal display, side-conversation cleanup, startup input queuing,
  title-only reasoning summaries, and fork naming in the TUI.
- [x] `9a6668f674`: report direct-input capability when listing subagents.
- [x] `ad6fc66b6d`: add durable manual ordering for thread sections and the
  associated app-server protocol, listing, search, and metadata semantics.
- [x] `7ec480dda5`: distinguish unknown MCP authentication status from explicitly
  unsupported authentication.
- [x] `406dc92394`: retain MCP read-only hints on app-server tool-call items and
  use them for safe parallel dispatch.
- [x] `88ec932e96`: preserve fractional rate limits in thread-history projection
  (already equivalent through raw JSON decoding; covered by rollout replay).
- [x] `6219b7c40f`: correlate image generation/edit requests with their active
  turn and record only accepted explicit client interrupts.
- [x] `1c5f336c40`: remove the obsolete `codex exec --full-auto` compatibility
  path.

### B. Runtime, transport, and security behavior

- [x] `9ea975a2dc`, `709283b432`, `8bbdf6c8f9`, `155c3e299c`, `09cf609218`,
  `5989dcc470`, `7d5253d2b0`: route OAuth and remaining network callers through
  configured shared HTTP clients without bypassing proxy or policy behavior.
  MCP runtime traffic and every OAuth phase now reuse the configured client,
  CLI discovery honors `respect_system_proxy`, and Ollama readiness reuses one
  injected client for version and model probes. The Go TUI currently has no
  online update/announcement fetch or pet download implementation, and Go
  protocol errors perform no HTTP requests, so those Rust call-site migrations
  are not applicable until the corresponding network features exist.
- [x] `438c9e98db`: route WebRTC sideband joins to the Realtime API endpoint.
  Not applicable to the current Go runtime: `realtime.Manager` is an in-memory
  protocol/state implementation and emits the synthetic SDP answer without an
  HTTP or websocket sideband connection, so it cannot derive a join URL from a
  model provider.
- [x] `1def0a8925`: propagate trusted parent-turn identity through spawned-agent,
  follow-up input, and Guardian nested Codex requests, include it in canonical
  Responses metadata, and reject client metadata spoofing of the field.
- [x] `c550cb3e01`: remove pending MCP elicitation state on cancellation. Go's
  generic `ServerRequestBroker` already unregisters the exact request ID when
  its context is cancelled and rejects a late response. Added a two-pending-
  request regression proving cancellation does not disturb the other request.
- [x] `9f4c20aadc`: tolerate legacy MCP discovery prevalidation failures where the
  modern fallback remains available. HTTP 400/null-ID/`-32000` discovery errors
  now downgrade only with exact missing-session or exclusively legacy-version
  evidence, independent of content type; the negotiated legacy version is kept
  for subsequent request headers, while unrelated/correlated/modern/unknown
  cases remain rejected.
- [x] `166658a34a`: terminate remote exec servers when their parent stdin closes.
  Added the opt-in flag/environment control, stdin EOF cancellation for remote
  servers, session/process shutdown on remote exit, and removal of the
  parent-lifetime variable from child environments. Local mode remains
  independent when the environment value is false or unset.
- [x] `fcd2273de7`: resolve imported connectors by MCP server name. Claude
  transcript attribution is now correlated with session manifests by either
  exact UUID or trimmed case-insensitive server name; connector display names
  are deduplicated, sorted, and persisted in the external-session ledger.
- [x] `fbf666fa98`, `85c082cccc`, `9a46fd33a0`: share optional MCP startup grace
  across connection sets and wait only in the runtime that owns a requested
  tool, allowing unrelated tools to proceed. Exact executors now expose a
  readiness hook; MCP waits happen outside the response execution gate, while
  ordinary serial calls preserve their order. Optional deadlines are shared by
  server connection identity and cleared when inventory publishes; explicitly
  required servers continue waiting past the grace period.
- [x] `ddf33ea802`: bound generated MCP namespace descriptions. Model-facing MCP
  namespace/source descriptions are truncated at a valid UTF-8 boundary within
  1,000 bytes, aggregate `tool_search` source listings retain names within 4
  KiB, and stored MCP metadata remains unchanged.
- [x] `7579a2b413`: resolve MCP upload paths in the selected environment's native
  path syntax. Upload arguments now resolve through a directory `PathURI` using
  the primary environment's Windows/POSIX convention, derive names from the URI,
  and stream metadata/content through that environment's exec-server connection;
  local uploads use the same URI contract before converting to host paths.
- [x] `78a61de904`: include cloud-managed MCP servers in CLI list/get/login/logout
  flows. Read-only/OAuth commands now bootstrap the configured auth store,
  fetch and cache the identity-scoped cloud bundle through the configured HTTP
  policy, and resolve managed server definitions. Add/remove retain their local-
  config-only load path, so managed definitions are never copied or deleted.
- [x] `9cf6b3905c`: close stdio app-server processes when the input connection
  reaches EOF. Already equivalent in Go's transport architecture: EOF ends the
  sole stdio read loop, waits for accepted requests, closes the connection, and
  defers full router shutdown regardless of remote-control state. A regression
  verifies the remote-control backend store is closed on empty-stdin EOF.
- [x] `3834c47ccb`: fail closed when a network allow amendment cannot be applied.
  A persistence failure now returns deny immediately, records a policy-denial
  outcome for the owning command, emits no saved-rule fragment, and leaves the
  host uncached so a retry prompts again.
- [x] `3e3ae08839`: cap MCP catalog pagination and reject repeated or excessive
  cursors deterministically. HTTP and stdio tools/resources/templates now share
  one collector limited to 100 pages, 1,024 items, and 64 KiB cursors, reject
  nonconsecutive cursor reuse, and use one configured tool-timeout deadline (30
  seconds by default) across initialization and every page.
- [x] `b1ccaa0e08`: never overwrite symlinked external-agent migration targets.
  Missing-or-empty target checks now use link metadata and accept only regular
  files, so existing and dangling `AGENTS.md`/`hooks.json` symlinks are skipped
  during both detection and explicit import without changing their targets.
- [x] `3d805abdf0`, `5decb399ae`, `bdda5da56c`: executor skill discovery and
  resource reads now carry the turn's per-environment filesystem sandbox through
  every walk/metadata/read request, fail closed for missing contexts and disabled
  Windows sandboxing, bound content before and after reads, and key caches by the
  sandbox context. Capability discovery advertises sandbox support, accepts 128
  roots per request, and the client batches larger sets. `bdda5da56c` is not an
  applicable runtime branch: Go's `apply_patch` is host-in-process and has no
  executor attempt/materialized-profile path; the shared context constructor is
  nevertheless regression-tested to keep canonical permissions separate from
  attempt workspace roots.
- [x] `b293412c24`, `89a0eed93c`, `c126f206da`: tool/runtime assembly now
  uses one ordered registry. Trusted host registrations remain strict, external
  MCP/extension/dynamic collisions retain the first runtime without aborting the
  turn, and synthetic `tool_search`/`exec`/`wait` runtimes replace colliding
  client definitions only when enabled. Code mode consistently selects the
  first normalized JavaScript identifier for execution, Responses Lite metadata,
  prompt declarations, and direct schema augmentation while leaving shadowed
  tools directly dispatchable. Hosted web-search fallback now follows the
  standalone `web.run` runtime that actually won registration.
- [x] `4f6d06d485`: remote compaction now retains bounded non-completion
  structured agent messages using the upstream 10k-per-message and 64k-total
  token budgets, estimates encrypted payloads by their model-visible plaintext
  size, preserves raw agent-message content through compact/session conversion,
  and restores initial context before the latest real user or agent message.
  `FINAL_ANSWER` child completions are excluded, and full-history exec forks
  strip inherited parent agent messages before adding the child's own task.
- [x] `5ad367fb95`: not applicable to the current Go runtime. Go exposes the
  `memories` feature/config surface, memory reset compatibility, and Rust SQLite
  cleanup only; it has no stage-one/phase-two memory writer, job claimant or
  lease, consolidation agent, or thread-manager lifecycle that could release a
  job before agent shutdown. No synthetic worker was introduced solely to mimic
  an unavailable Rust subsystem.

### C. Architecture, performance, and upstream-internal commits

- [x] Classify Rust-only release/Bazel/dependency work (`dd6b880353`,
  `12b3e88028`, `b9b7c21821`, `00cb5c465b`, `df326d31cd`, `a05bcda3db`,
  `a5082373f1`) and test-only hardening (`4f6eaf7af9`, `a4d2f31022`,
  `1da9f846b3`, `1dad11f818`, `aa06446345`, `9588f660be`, `5a1097ed26`,
  `25eecb071e`) without manufacturing Go behavior changes. Release workflows,
  Bazel/rusty_v8 metadata, the bundled Rust OpenAI-docs skill, and Rust-only
  test harness changes have no Go runtime contract. The Ratatui upgrade's
  terminal-cell width behavior is covered by the shared TUI width work below.
  The rmcp 3.0 audit did expose one contract: OAuth discovery now uses an
  isolated client which rejects cross-origin redirects, preserves same-origin
  caller redirect policy, and returns transient 408/425/429 failures unchanged.
  Go already tolerates missing MCP `serverInfo` while retaining the configured
  server name.
- [x] Audit SQLite centralization, response boxing/serialization, rollout-scan
  optimizations, analytics allocation changes, and source-only module moves
  (`50a7328f50`, `07490c7523`, `1ae2b9880e`, `c41a38dd10`, `88d6c2b2b4`,
  `f9b18d04ba`, `6493417150`, `7b93b3bf9c`, `ff352fab62`, `410c22b30e`) for any
  observable Go contract. SQLite lint centralization, boxed events, allocation
  reductions, module moves, dependency cleanup, and local-variable edits are
  internal-only. Go has no Statsig tool-call metric exporter to filter. Fork
  scans already reject missing and synthetic noncanonical `beforeTurnId`
  boundaries, now with an explicit regression. The response refactor did expose
  a remote-control error-path gap: response serialization failures now emit a
  JSON-RPC `-32603` error with the original request ID; notification failures
  remain unreported as required.
- [x] Recheck cross-platform TUI width and hyperlink behavior represented by
  `cef3910ea4` and `3725f02cf3` without overwriting the user's current
  `history_ui` changes. Shared width, wrap, truncation, cursor, notice, session,
  request-input, and diff rendering now preserve grapheme clusters and count
  halfwidth Japanese sound marks consistently. Go has no Ratatui cell-buffer
  diff/forced-width phase; its hyperlink metadata/string path therefore needs
  no synthetic equivalent. The pre-existing `tui/app/history_ui*` edits were
  left untouched.

## Verification gates

1. Focused deterministic tests for every applicable row.
2. Race tests for shared MCP startup/catalog, environment, tool registry, and
   thread ordering state.
3. Native Windows package and PTY tests for affected CLI/TUI paths.
4. `go test ./... -count=1`, `go vet ./...`, and `git diff --check`.
5. Build a Rust binary at `25eecb071e` and a Go candidate, record both hashes,
   then run the approved official TypeScript SDK smoke and differential matrix.

### Verification evidence

- Focused package tests passed for every applicable row, followed by
  `go test ./mcp ./session ./appserver ./tui/... -count=1`.
- `go test -race ./mcp ./tool ./session ./appserver ./execserver -count=1`
  passed. Native Windows ConPTY and unified-exec packages passed; the real
  focus-input ConPTY smoke and the current Rust/Go `/status` plus `/mcp` slash
  comparison passed. The separate terminal-restore smoke skipped because this
  host's ConPTY output probe returned no readable bytes.
- `go test ./... -count=1`, `go vet ./...`, and `git diff --check` passed. The
  diff check reported only the repository's existing LF-to-CRLF warnings.
- Rust `25eecb071e` built successfully after preserving the verified
  `target/debug/gn_root` junction required by `v8 150.4.0`; `codex-cli 0.0.0`
  SHA-256 is `4b060c4af8b3ebe57dcbcdd542bdb1e1cdcb5461b17b8346e6e89562ccfb7775`.
  The Go candidate reports `codex-cli 0.0.0`; SHA-256 is
  `7c3e748f3eb074afe37e59905a2998154c4f8a54557c54be407d50f504b2aa97`.
  The rebuilt official TypeScript SDK SHA-256 is
  `0970cdd99525e6675804b81a3dc7197a682cfeda140a929839bdbfd3b6f02434`.
- SDK harness unit tests passed 37 with one expected Windows signal skip. The
  live smoke passed, the approved differential matrix passed all 42 of 42
  scenarios with no mismatch or infrastructure failure, and the model-free raw
  paginated fork audit passed. Suite evidence is
  `sdktests/artifacts/suites/2026-07-30T164951300Z-parity/suite-summary.json`.

## Completion rule

Every row must end as implemented, already equivalent, or not applicable with
specific source and test evidence. The formal parity baseline advances only when
all applicable rows and differential gates pass; a commit-title audit alone is
not sufficient.
