# Codex Go update alignment plan - 2026-09-25 (8ae55c863d)

Continues `update/plan_2026_09_20.md` (sync134-sync137 + the tui-align pass).
Upstream was fast-forwarded `5c5308fc9a..8ae55c863d` (**358 commits, 2183 files,
+105,300 / -28,905**) and this pass triages the range, re-pins the static
parity layer, lands the core deltas that the re-pin exposes, and records the
remaining lane.

Follow-up pass (same day): upstream advanced one more commit,
`8ae55c863d..d0a64e91ae` (#48015), which is landed as sync144 below.

## Baseline

- Rust checkout `D:\qax\reagent\dev\git\codex` at `8ae55c863d` (2026-09-25,
+#48004); previous pass pinned `5c5308fc9a` (2026-09-20, #46752).
- Go repo at `8a1db43d` (`release: v1.0.4`, 2026-09-20 23:02) before this pass.
- `go test ./parity/` failed at the new checkout with 15 drift failures; it is
  green at the new pin after this pass.

## What the range contains

358 commits. Classified by the paths each commit touches (`.tmp/triage358.py`):

| Bucket | Commits |
|---|---|
| core (non-TUI packages) | 257 |
| TUI-only (`codex-rs/tui`, `ext/*` UI assets) | 72 |
| docs/build/CI/scripts only | 29 |

Theme counts over the commit subjects (`.tmp/themes358.py`, a commit may match
several themes): multi-agent 64, TUI 62, model catalog 31, MCP 29,
config/features 29, guardian 23, auth/login 23, network policy/proxy 22,
exec-server/exec 20, protocol/app-server 19, telemetry/analytics 16,
Windows sandbox 11, voice/realtime 11, skills/plugins 10, image 4.

The lane is dominated by five coherent programs:

1. **Agent control + agent message board** (#46959-#47952): the
   `AgentControl`/`LocalAgentControl` refactor, the shared message-board
   extension (SQLite and in-memory storage, paging, tools, subscriptions), and
   the multi-agent spawn metrics.
2. **Application network policy** (#47389-#47968): one policy threaded through
   HTTP, WebSocket, bootstrap, MCP, AWS auth, telemetry, remote control, and
   the network proxy, plus MITM CA support and Btrfs/daemon-socket masking.
3. **Guardian v2 authorization context** (#47030-#47947): ordered assistant
   context, retained user goals, environment-bound scores, extra policy
   configuration, and the retirement of the old context gates.
4. **Gateway / executor authentication** (#47158-#48004): gateway OAuth
   sign-in and status RPCs, WebSocket auth extraction into
   `codex-websocket-auth`, executor bearer tokens, and prewarm reuse.
5. **Windows sandbox transport + MXC** (#47361-#47989): chunked environment
   transport for large launch payloads, PID-namespace inheritance, the local
   MXC backend preference, and the retired world-writable audit code.

## Landed in this pass

### sync138: the static parity layer is re-pinned to `8ae55c863d`

The L0 layer failed on the new checkout in 15 places. All are reconciled and
the corresponding upstream commits are named in the code:

- **Inventory**: `core-plugin-common` and `websocket-auth` joined the top-level
  directories (116 -> 118); the workspace members grew by
  `core-plugin-common`, `ext/agent-message-board` and `websocket-auth`
  (149 -> 152).
- **Critical file hashes**: `Cargo.toml` (#47447/#47683/#47748),
  `exec/src/lib.rs` (#47411), `core/src/client.rs`
  (#47745/#47758/#47957), `app-server-protocol/src/protocol/common.rs`
  (#47207/#47248/#47377/#47648), `app-server/tests/suite/v2/mod.rs`
  (#46917/#47028/#47207/#47407), `core/tests/suite/mod.rs`
  (#47407/#47679/#47819/#47820/#47879).
- **Fixture roots**: `cli/tests` 31 -> 38 (exec-server PID-namespace and
  WebSocket-auth suites, doctor path-safety snapshots),
  `app-server/tests/suite/v2` 149 -> 154, `core/tests/suite` 215 -> 256,
  `tui/src/chatwidget/tests` 112 -> 123 and its snapshots 63 -> 70,
  `tools/src` 39 -> 41.
- **TUI snapshot manifest**: total 1285 -> **1320**; the analytics dashboards
  were dropped (#46861) while the transcript view, markdown rendering, prompt
  suggestions, effect toggles and warnings surfaces grew. Three new directories
  are required: `tui/src/analytics/activity_chart/snapshots`,
  `tui/src/status_indicator_widget/snapshots`, `tui/src/tooltips/snapshots`;
  the retired `fresh_thread_with_draft` snapshot is replaced by
  `fresh_thread_header` (#47954).
- **Unified-exec sandbox manifest**: `windows_sandbox.rs` renamed
  `windows_elevated_unified_exec_enforces_managed_deny_reads` ->
  `..._large_recursive_deny_reads` (#47919).
- **Contract oracle path**: `session-startup-prewarm` now points at
  `core/src/session/startup_prewarm.rs` (the prewarm module moved under
  `core/src/session/`).
- **Vendored precomputed exports**: re-vendored byte-identical to Rust and the
  artifact SHA-256 pins re-frozen (`appserver/schema_test.go`).
- **Config surface**: `ConfigToml`/`config.schema.json` gained the `cloud`
  table (#47074) - handled by sync141 below.

### sync139: the feature registry follows #47179/#47686/#47799/#46849

- `daemon_auto_start` is **stable and enabled by default** (#47179); the
  experimental menu entry and announcement are gone.
- `write_stdin_approval` is **stable and enabled by default** (#47799).
- `guardianv2.thread_context` is **removed** (#47686/#47688/#47689/#47690):
  thread-owned Guardian context capture is unconditional, so the Go gate in
  `resetGuardianAfterParentCompaction` is fixed on instead of consulting the
  retired flag.
- `transcript_v2` is **deprecated** (#46849; the fullscreen transcript moved to
  TUI configuration).
- New keys registered: `defer_mailbox_preemption` (#47913),
  `agent_message_board` (#47946), `prefer_mxc` (#47375).

Two Go-visible default flips follow from this and are pinned by tests: an
eligible interactive launch now starts the shared background server and
requires the managed standalone install (Rust #47179 does the same), and an
escalated `write_stdin` requires review unless the feature is explicitly
disabled.

### sync140: Flex capacity failures are a distinct terminal error (#47967)

- `codexapi` gains `ErrorFlexUnavailable` with Rust's display text
  ("Flex capacity unavailable.") and the matching response-debug telemetry
  message.
- The model layer recognises `flex_unavailable` in HTTP 429 bodies, in a
  standalone streamed `error` event, and in `response.failed`, and treats it as
  non-retryable.
- The app-server maps it to `flexUnavailable` with `httpStatusCode: 429`
  (Rust `CodexErrorDetails::http_status_code_value`), and the rollout reader
  accepts the new classification.

### sync141: the cloud config table and cloud skills gate (#47074)

- `cloud` joins the recognized top-level config keys with strict sub-field
  validation mirroring `CloudToml` (`cloud.skills.enabled`).
- `Config.CloudSkillsEnabled()` reads `cloud.skills.enabled` defaulting to
  true; `orchestrator.skills` is retained as a legacy no-op, matching Rust's
  provider replacement. Go's gate therefore follows the cloud setting.
- `image_gen.imagegen` accepts the new `transparent_background` boolean and
  always sends an explicit `transparent`/`opaque` background, replacing the
  previous `auto` value (#47484); the vendored description gained the matching
  guideline.
- Guardian extra policy (#47125): the managed `guardian_extra_policy`
  requirement and `[auto_review] extra_policy` fill the template's
  `{{ extra_policy }}` slot, with Rust's two-pass substitution so
  placeholder-like text inside either policy stays literal.

### sync142: explicit gateway sign-in RPCs (#47170/#47207)

- `account/gatewayOAuth/read|login|cancel` are advertised in the JSON and
  TypeScript client surfaces with the `account/gatewayOAuth/changed`
  notification (`GatewayOAuthChangedNotification`).
- `auth.GatewayAuthManager` gained a per-CODEX_HOME login control: status
  (`notReady`/`started`/`succeeded`/`failed`), status subscriptions, the
  one-sign-in-at-a-time guard, and `LoginWithBrowser` with a caller-owned URL
  handoff. The read RPC reports provider policy and readiness without
  credentials; cancellation is bound to the initiating connection and is
  acknowledged only after the login releases its slot.

### sync144: the tool-suggestion app link validates its install URL (#48015)

- Static re-pin for the single new commit: the TUI snapshot total is 1321 and
  `tui/src/chatwidget/snapshots` grows to 314 with the new
  `declined_tool_suggestion` snapshot as a required entry.
- `bottompane` now parses the form-mode tool suggestion
  (`codex_approval_kind: tool_suggestion` with `tool_type`/`suggest_type`/
  `suggest_reason`/`tool_id`/`tool_name`/`install_url`), mirroring Rust's
  `parse_tool_suggestion_request`, and `AppLinkParamsFromToolSuggestion` builds
  the app link params through the shared external-URL validator (credential-free
  HTTPS with a host).
- The tea TUI follows the validated outcome: an invalid install URL declines the
  elicitation on the requesting thread without a popup, a valid URL renders the
  app link popup (link screen → confirmation screen) whose host actions —
  opening the URL — are forwarded through the new `Options.OnAppLinkAction`,
  and completing the flow resolves the elicitation as accepted. The app wiring
  opens the URL with `auth.OpenBrowser`, mirroring Rust's
  `AppEvent::OpenUrlInBrowser`.

### sync145: MCP OAuth authorization endpoints and the file blob upload retries

- **#47326**: MCP OAuth discovery rejects an authorization endpoint whose scheme
  is not `http`/`https` before client registration or the authorization URL is
  returned, mirroring Rust's
  `validate_authorization_server_endpoints` (`mcpOAuthWebEndpoint` in
  `mcp/oauth_discovery.go`). The URL matrix (https/http accepted; `file`,
  `custom`, malformed and scheme-relative rejected) is pinned by
  `TestOAuthDiscoveryRejectsNonWebAuthorizationEndpointsLikeRust`.
- **#47122/#47393/#47926**: the OpenAI file blob PUT gets its own 5-minute
  deadline (`defaultOpenAIFileBlobUploadTimeout`) shared by up to five attempts,
  retries the transient `502`/`503`/`504` responses and transport failures with
  a fresh Azure client request ID per attempt, honors the
  `x-ms-retry-after-ms`/`Retry-After` hints, and never retries terminal statuses
  (403/409/500) or a delay that cannot fit the remaining budget. The Rust retry
  matrix is mirrored by
  `TestOpenAIFileBlobUploadRetryBehaviorLikeRust`/`...RetriesTransportFailuresLikeRust`.

### sync147: `codex doctor` reports only safe config-load metadata (#46962)

- The config loader now wraps TOML failures in a typed
  `config.ConfigLoadError` (path plus 1-based line/column), which diagnostics
  can inspect without touching the offending values.
- `doctor`'s `config.load` failure check reports the typed location
  (`error: invalid configuration`, `file`, `line`, `column`), an I/O error kind
  (`entity not found`, `permission denied`, `invalid data`), or the generic
  `error: configuration load failed` — never the raw error text, which can echo
  credentials from `config.toml`.
- Audited N/A in the same lane: `#47038` (cache OS discovery for user agents and
  telemetry) caches Rust's `os_info::get()`, which can spawn subprocesses on
  Linux; Go reads `runtime.GOOS`/`runtime.GOARCH` compile-time constants and
  never shells out for OS metadata, so no Go surface changes.

### sync148/sync149: the exec-server authentication program (#47447/#47601/#47648)

The shared WebSocket authentication policy now lives in `codex_go/websocketauth`
(`websocketauth.Policy`/`Settings`/`NewPolicy`), which is the Go counterpart of
Rust's `codex-websocket-auth` crate extraction (#47447); `appserver` keeps its
existing names as aliases so the app-server listener behavior is unchanged.

- **#47648 (client side)**: `environment/add` accepts the optional
  `authBearerToken`, which requires a `wss://` or loopback `execServerUrl`
  (rejected otherwise, with no token text in the error). The registration stores
  the resulting `Authorization: Bearer <token>` header and every executor dial
  sends it — environment info/status, remote skills, the OpenAI-file executor
  client, and the unified-exec path — including automatic reconnects, because
  the header is threaded through `tool.UnifiedExecEnvironment`,
  `ShellRequest.UnifiedExecRemoteHTTPHeaders` and
  `execserver.DialClientOptions.HTTPHeaders`. `execserver` validates the header
  set (connection-controlled names and `sec-websocket-*` rejected, invalid names
  or values rejected, non-empty sets require `wss://` or loopback) and
  `RedactedExecServerHeaders` renders names with `[redacted]` values.
- **#47601 (listener side)**: `codex exec-server` accepts the shared `--ws-*`
  flag family (`cli.WebSocketAuthOptions`, also used by `codex app-server`), and
  `execserver.Server.SetWebSocketAuthPolicy` validates
  `Authorization: Bearer` before every WebSocket upgrade, answering 401 for
  missing or invalid credentials. Listener authentication is rejected together
  with `--remote`, `forward`, and a stdio listener, mirroring Rust.

Tests mirror the Rust CLI suite: capability-token and signed-JWT (issuer,
audience, expiry) listener coverage, 401 rejection, reconnects re-sending the
token, the URL/header validation matrices, CLI flag parsing and the
remote/forward/stdio rejections, and an end-to-end `Run(["exec-server", ...])`
test that proves the wiring from flags to 401 enforcement.

Remaining in this program: the `environments.toml` executor provider
(`exec-server/src/environment_toml.rs`) is not ported at all in Go, so the
`auth_bearer_token` URL-entry half of #47648 lands with that provider.

### sync151: the environments.toml provider and the stdio client transport

The executor environment configuration layer now exists in Go, mirroring
Rust's `environment_toml.rs` / `environment_provider.rs`:

- `execserver.ParseEnvironmentsTOML` / `LoadEnvironmentsTOML` decode
  `environments.toml` (top-level `default` and `include_local`, one
  `[[environments]]` table per environment with `id`, exactly one of `url` or
  `program`, optional `auth_bearer_token`, `args`, `env`, `cwd`,
  `connect_timeout_sec`, `initialize_timeout_sec`), reject unknown fields, and
  never echo configuration values in failures — syntax errors report a line and
  column, unknown fields report the field name.
- `NewEnvironmentProviderSnapshot` reproduces Rust's validation messages
  verbatim: reserved/whitespace/invalid-character/overlong ids, duplicate ids,
  `must set exactly one of url or program`, empty program, `args/env/cwd
  require program`, `auth_bearer_token requires url`, `connect_timeout_sec
  requires url`, non-websocket and malformed urls, config-dir-relative stdio
  cwd resolution, `default = "none"` disabling the default, and a local default
  rejected when local is disabled.
- `DefaultEnvironmentProviderSnapshot` mirrors the `CODEX_EXEC_SERVER_URL`
  fallback (`remote` environment, or `none` disabling the provider), and
  `EnvironmentProviderFromCodexHome` selects the file when present.
- The stdio transport is implemented: `execserver.DialClientOptions.StdioCommand`
  spawns the configured program and speaks line-delimited JSON-RPC over its
  stdin/stdout (reusing the shared initialize/session machinery). Mirroring
  Rust, a stdio connection registers no reconnect strategy, so a dropped
  connection fails instead of resuming a session the child cannot know about,
  and a stdio command conflicts with an exec-server URL.

Remaining for this program: the host-side wiring — `appserver`'s environment
manager still learns about environments only from RPC registrations, so the
app-server must pre-register the provider snapshot (remote entries with their
bearer headers, stdio entries with their command threaded through the
unified-exec and remote-skills dial paths).

### sync152: the app-server serves the configured executor environments

- `EnvironmentManager.ApplyProviderSnapshot` pre-registers a provider snapshot:
  remote entries become records with their URL, bearer headers and connect
  timeout, stdio entries become records carrying the program command, and the
  provider's default selection is retained. The local environment stays
  implicit.
- The app-server startup loads the provider from CODEX_HOME, so a malformed
  `environments.toml` fails startup with the parse error and never echoes the
  configuration values.
- Stdio records are reachable through every executor path: environment
  info/status, remote skill discovery/reads, the OpenAI-file executor client and
  unified exec all select the stdio transport (`execServerClientOptions`,
  `tool.UnifiedExecEnvironment.ExecServerStdioCommand` →
  `ShellRequest.UnifiedExecRemoteStdioCommand` → `execserver.DialClientOptions`),
  and the "remote environment" decisions in the shell executor treat a stdio
  command as remote so the local sandbox path is not used.
- A turn that selects no environment now uses the provider default
  (`DefaultEnvironmentID` → `unifiedExecEnvironmentsForTurn`), replacing the
  implicit local environment when `environments.toml` points `default` at a
  configured executor; `default = "none"` or a local default keeps tools local.

Remaining for full fidelity: Rust builds the same environment manager from
CODEX_HOME in `codex exec`, the TUI and the worktree paths, while Go's
non-app-server hosts still run turns locally without consulting
`environments.toml`.

### sync153: the shared discussion board (agent message board, in-memory)

The first slice of the largest unported program (upstream `#46959-#47028`,
`#47946`, `#47952`) now exists in Go as `codex_go/agentboard`:

- `types.go` / `api.go` / `host.go` port Rust's `types.rs`, `api.rs` and
  `host.rs`: post metadata/previews/content, thread and channel summaries,
  subscription state, `Page[T]` (serialized as `results`/`n_returned`/
  `has_more`/`next_cursor`), the board operations and the host capabilities
  (agent path, agent resolution, caller clock, notification admission).
- `inmemory.go` ports `in_memory.rs` + `in_memory/queries.rs`: tree-scoped state
  shared by every handle for one session identity with registry pruning when the
  last handle is released, per-caller request-id idempotency (a reused id with
  different input fails), atomic recipient selection that always excludes the
  author, channel subscriptions for new roots plus thread subscriptions for
  replies (an explicit unsubscribe survives re-posting), notification fanout
  bounded to 16 concurrent deliveries that tolerates host failures and skips
  inactive agents, Unicode case-folded search (`Straße` matches `STRASSE`), the
  50-item page cap with base64url offset cursors, the 20k-character preview
  budget split across a page and Unicode-character `read_post` offsets.
- Validation mirrors Rust's messages and limits (channel names, post text,
  request ids, recipient counts, unknown channels/threads, thread ids that must
  identify a top-level post), and the behavior tests replay Rust's
  `local_board.rs` scenarios for the in-memory arm.

Remaining for this program: the collaboration tool layer (9 tools with their
schemas, argument validation and response byte budgets), the SQLite (`local`)
backend with lifecycle/paging/compaction behavior, and the runtime wiring behind
the `agent_message_board` feature gate.

### sync150: the portable project trust lookup (#47620)

- `config.ProjectTrustLookup` mirrors Rust's `ProjectTrustLookup`: the ordered
  project-map keys for one working directory, with cwd keys before the
  repository root's and each executor-normalized canonical spelling before its
  original spelling. It matches the merged projects map exactly first, then
  case-insensitively on Windows with a deterministic (lowest-key) tie-break, and
  an entry that omits `trust_level` still wins — which is the precedence the
  Rust change preserved.
- `ProjectTrustLevelForTarget` now resolves through the lookup, so trust entries
  written with either the canonical or the original spelling are honored, and
  `ProjectConfigEnabled`'s ancestor walk stays as the separate onboarding
  (`directory_trust_persisted`) check.

## Verification

- `go test ./parity/ -count=1` (the L0 static layer) is green at `8ae55c863d`.
- `go test ./... -count=1` is green (see the session log
  `.tmp/fullsuite_0925b.log`).
- Targeted landing tests: `go test ./features ./config ./state ./model
  ./turn ./rollout ./auth ./appserver -count=1` plus the per-item tests named
  above.

## Queued lane (next passes, in workflow order: core before TUI)

Audited gates recorded while triaging this pass (each needs its predecessor
program before it can land, so none was stubbed):

- `#47947` (root authorization context 8 -> 16 messages) raises
  `LocalAgentControl::MAX_ROOT_MESSAGES`; Go's `agent/control.go` is the
  execution limiter only, so the root-context projection arrives with the
  `AgentControl` port below.
- `#47340` (`interrupt_if_no_pending_input`) depends on the Rust
  `CodexThread`/session submission-queue internals; Go's equivalent seams
  (`RuntimeRouter` interrupt + queued submissions) need the same turn-identity
  and pending-input bookkeeping first.
- `#47620` (portable project trust lookup with executor path conventions) needs
  the Go project-trust lookup to be split out of `config` the way Rust's
  `ProjectTrustLookup` was.
- `#47899` (`McpAttribution.error_reason`) extends the cumulative
  model-invisible `McpAttribution` carried on Responses requests; Go has no such
  surface yet (`grep mcp_attribution` finds none), so the reason enum lands with
  that field.

1. **Agent message board + AgentControl** (#46959-#47952): the extension was
   built from scratch upstream (shared interfaces, SQLite and in-memory
   storage, paging, collaboration tools, subscription persistence) and the
   multi-agent runtime now routes through `AgentControl`. Go needs the board
   store/tools behind the `agent_message_board` gate and the controller seam
   before the multi-agent session surface can be re-pinned.
2. **Application network policy** (#47389-#47968): one policy threaded through
   every Codex-owned transport. Go has the proxy/tunnel primitives
   (`network.ProxyPolicyDecision`/`ProxyDecisionSource`/`ProxyDecision` and the
   exec-server decision protocol); the enforcement points are unported.
   Scoped commit list for the next pass, in dependency order:
   `888e02db34` (#47389, HTTP and WebSocket requests; the `http-client` crate's
   `request_builder.rs`/`route_aware_client_pool` rework),
   `22a3f6d5d8` (#47407, app-server requests),
   `8f8ace78cb` (#47408, AWS auth and telemetry),
   `db3cb33eca` (#47410, remote control and remote-execution recovery),
   `9d4d34d436` (#47411, embedded Codex startup; `exec/src/lib.rs` is a pinned
   critical file),
   `8e17909b27` (#47663, route-aware transports),
   `d04b0561d3` (#47703, ChatGPT backend requests),
   `1d87af5faa` (#47742, history notes and image generation).
   The shared payload type is `codex_protocol::network_policy::NetworkPolicyDecisionPayload`
   (camelCase: decision, source, optional protocol/host/reason/port) with
   `is_ask_from_decider`.
3. **Guardian v2 authorization context** (#47030-#47947): ordered assistant
   context, retained user goals, environment-bound scores and the extra-policy
   classifier append. The extra-policy half landed in sync141; the context
   ordering/retention half is queued.
4. **Cloud skills provider** (#47074 second half): the host-supplied cloud
   provider replacing the built-in orchestrator provider, `cloud` authority and
   `c` root aliases in skill tools and prompts, per-turn cloud discovery and
   auth-scoped catalog caching.
5. **Windows sandbox transport + MXC** (#47361-#47989): chunked environment
   transport for large launch payloads, PID-namespace inheritance, the MXC
   launcher, and the retired world-writable audit code.
6. **exec-server authentication** (#47601/#47648/#47447): WebSocket auth,
   executor bearer tokens and the `websocket-auth` crate extraction.
7. **TUI lane** (#46832-#47954): prompt suggestions, transcript search/copy,
   effect toggles, markdown list rendering, mermaid parsing and the settings
   surfaces. Landed last, after the core lane above.

---

## Follow-up pass (same day, second pull)

Upstream advanced twice more while this pass was being finished:
`d0a64e91ae..d7b07d4551` (#48017) and `d7b07d4551..4b1c0c30da` (**#48035**).
The checkout is now pinned at `4b1c0c30da`.

### sync154: the static parity layer is re-pinned to `4b1c0c30da` (#48035)

`go test ./parity/` failed in exactly one place at the new checkout,
`TestPrecomputedAppServerExportsMatchRustTarget`, because #48035 rewrote the
vendored protocol exports:

- `PluginSummary.extensions` and the extension declaration types
  (`PluginExtensions`, `PluginEntrypoint`, `PluginQuickAction`,
  `PluginQuickActionTarget`, `PluginIcon`, `PluginSearchProvider`,
  `PluginSearchProviderCall`, `PluginSettings`) are gone from the v2 protocol,
  from the JSON schemas and from the TypeScript/Python bindings.
- The two precomputed exports were re-vendored byte-identical to Rust
  (`appserver/schema/precomputed/app-server-exports-{stable,experimental}.json.zst`)
  and the artifact SHA-256 pins in `appserver/schema_test.go` were re-frozen:
  stable `ed9e34b3...6732e` (155,868 -> 154,524 bytes), experimental
  `42590bd0...14e1` (161,278 -> 159,712 bytes). The vendored TypeScript tree
  loses the eight deleted `v2/Plugin*.ts` files.
- Fixture-root counts are unchanged by #48035 (the v2 suite edits
  `plugin_list.rs`, it does not add a file), and so are the pinned Rust critical
  file hashes, so no other parity constant moved.

Audited N/A in the same commit, with the reason recorded:

- `includeExtensions=true` was never sent by Go's installed-plugin fetch
  (`appserver/plugin_remote_installed.go` builds `scope`, `includeDownloadUrls`
  and `pageToken` only), so there is nothing to stop sending.
- Go never modelled the remote-plugin extension metadata:
  `plugin.PluginSummary` (`plugin/api.go`) and the remote installed/catalog
  models have no `extensions` field, and
  `plugin.InstalledPluginMetadataEqual` already excludes display-only metadata.
  The Rust change is therefore a protocol/schema delta with no Go behaviour to
  delete.
- `d7b07d4551` (#48017) is a Rust-only app-server integration test
  (`app-server/tests/suite/v2/request_permissions.rs` grew same-cell
  `request_permissions` + `strictAutoReview` coverage). It adds no Go surface;
  Go already covers the request/response flow
  (`TestRuntimeRouterRegistersRequestPermissionsToolWhenFeatureEnabledLikeRust`,
  `TestRuntimeRouterPermissionsApprovalEmitsReviewAnalyticsLikeRust`). The
  stricter assertion it adds - an awaited grant applying to the next command in
  the same code-mode cell, and `strictAutoReview` reviewing a command that needs
  no extra access - is queued with the code-mode lane below.

### sync155: the protocol agent path (#15313/#18353)

`codex_protocol::AgentPath` now exists as the Go `agent.AgentPath` protocol
layer (`agent/path.go`), mirroring `codex-rs/protocol/src/agent_path.rs`:

- `AgentPathRoot`/`AgentPathMorpheus` plus their string values, `String`,
  `IsRoot` and `AgentName` (the last segment, or `root`).
- `NewAgentPath`/`ValidateAgentPath` mirror `validate_absolute_path` exactly,
  including the `morpheus` early return, the `/root` prefix requirement, the
  trailing-slash rejection and per-segment name validation. The unreachable
  "absolute agent path must not be empty" branch is not reproduced, because
  Rust's `split('/')` can never yield zero segments: `/` reports the
  "must start with `/root` or be `/morpheus`" error in both languages.
- `ValidateAgentName` mirrors `validate_agent_name` (empty, reserved `root`,
  reserved `.`/`..`, embedded `/`, then the lowercase/digit/underscore charset).
- `Join` and `ResolvePath` mirror `join`/`resolve`: `/root` is special-cased,
  a `/`-prefixed reference is an absolute path, a relative reference is
  validated (`validate_relative_reference`) and then appended, and an **empty
  reference is rejected** with Rust's "agent path must not be empty". (The
  first draft of this file returned the caller's own path for an empty
  reference; Rust has no such rule and the message-board tools never use one,
  so it was corrected before landing.)
- `agent.rootAgentPath` is now an alias of `AgentPathRoot` so the spawn
  reservation key and the protocol validator cannot drift.

Behaviour is pinned by `agent/path_test.go`, which replays every Rust
`agent_path.rs` test plus the `/`, empty-relative-reference, `a/b`, morpheus
and empty-segment matrices.

### Verification (second pass)

- `go test ./parity/ -count=1` is green at `4b1c0c30da` after the re-vendor.
- `go test ./... -count=1` is green.
- `go test ./agent/ ./appserver/ -count=1` green for the new path layer and the
  re-pinned schema/exports.

### sync156: the message-board collaboration tools

The second slice of the message-board program is the model-facing tool layer,
`codex_go/agentboard/tools.go`, porting `tools.rs`, `tools/spec.rs` and
`tools/arguments.rs`:

- `NewMessageBoardTools`/`NewMessageBoardToolsWithOverrides` build the nine
  tools (`create_channel`, `get_channels`, `list_threads`, `search_posts`,
  `read_thread`, `read_post`, `subscribe`, `unsubscribe`, `post`) bound to one
  caller and its `agent.AgentPath`. The host supplies the namespace and its
  description; an empty namespace publishes plain function tools, matching
  Rust's `Option<&str>`. Only the five read tools declare parallel support.
- The parameter schemas are transcribed from `spec.rs`, including the bundled
  descriptions, the `minimum` bounds, the `sort` enum, the notify-recipient
  array and the `additionalProperties: false` envelope. A catalog description
  override replaces only the description, because Rust keeps the bundled
  parameters for these tools (Go ignores a catalog parameter override, as
  Rust warns and does).
- Arguments decode with `deny_unknown_fields` semantics through the new
  `tool.Invocation.DecodeStrictArguments` (Rust's `tools/arguments.rs` structs
  all deny unknown fields). Rust's `NonZeroU32`, `u32`, `Uuid` and enum fields
  are validated after decoding, and Rust's non-`Option` fields are pointers so
  a missing field is rejected. Behaviour (reject) matches serde; the message
  text is Go's, like every other `DecodeArguments` failure in this port.
- The executors mirror `tools.rs`: the requested read is tried first and the
  page/preview limits halve only when the serialized result exceeds the
  response budget (`bounded_read`), mutations reserve 2048 bytes plus the
  caller and target paths before writing (`check_mutation_budget`), the request
  ID is `turn:call` for direct calls and `turn:cell:runtime_call` for Code Mode
  calls, and every model error is truncated to 512 characters.
- `tool.Invocation.ResponseByteBudget` ports `ToolCall::response_byte_budget`
  with `codex_utils_output_truncation::with_serialization_allowance` (new on
  `utils.TruncationPolicy`): a direct call is bounded by
  `min(max_response_bytes, policy * 1.2)` and a Code Mode call only by the
  tool's own limit. The policy is threaded end to end -
  `turn.RuntimeOptions.TruncationPolicyForModel` resolves it from the effective
  model, `Runtime.Run` puts it on `ToolDispatcherOptions.Truncation`, and
  `addInvocationContext` stamps it onto every `tool.Invocation`; the app-server
  builds the resolver from `modelInfoForRuntimeWithConfig`.

Tests: `agentboard/tools_test.go` pins every schema literally, runs the
channel/thread/search/read/subscribe scenarios over the in-memory board,
replays the strict-argument and mutation-budget rejections, and proves the
request-ID and bounded-read behaviour (including that a rejected mutation
leaves the board unchanged). `tool/response_budget_test.go` pins the allowance
rounding and the direct/Code Mode budget matrix.

Remaining for this program: the SQLite (`local`) backend with its
lifecycle/paging/compaction behavior, the runtime registration behind the
`agent_message_board` feature gate (the Multi-Agent V2 `delegation` namespace,
the catalog description overrides, and `MultiAgentToolVersion` gating), and the
`contains_external_context` marker Rust sets on message-board results
(`JsonToolOutput::with_external_context`), which needs `tool.Output` to carry the
flag into the standalone memory-pollution check.

### Verification (third pass)

- `go test ./... -count=1` is green with the tool layer, the response-budget
  seam and the truncation-policy wiring in place.
- `go test ./agentboard/ ./tool/ -count=1` covers the new specs, scenarios and
  budget matrix.

### sync157: the SQLite (local) message-board backend

The third slice of the message-board program is `codex_go/agentboard/local.go`,
porting `local.rs` plus `local/{queries,paging,lifecycle}.rs`:

- The schema is copied verbatim, and the process keeps one SQLite pool per
  canonical database path with reference counts so live handles cannot open
  duplicate or stale pools (Rust's `POOLS` plus `Weak<SqlitePool>`; Go's
  `state.SqliteConfig.OpenReadWrite` already supplies the host's connection and
  journal policy).
- Every write runs in a `BEGIN IMMEDIATE` transaction that first checks the
  `deleted_boards` tombstone, so a permanently deleted board's root cannot be
  resurrected. `DeleteLocalBoards` writes the tombstone, deletes the board's
  subscriptions, opt-outs, posts and channels, and recovers an unopenable
  corrupt database by backing it up and recreating it
  (`state.IsSQLiteCorruptionError` + `state.BackupDBFilesForFreshStart`).
- `create_channel`, `post`, `set_subscription`, `read_post`, `list_channels`,
  `list_threads`, `search_posts` and `read_thread` mirror Rust's SQL, including
  the `request_id = "<caller>:<request id>"` idempotency column, the
  `subscription_opt_outs` rule that keeps an explicit unsubscribe until the
  agent subscribes again, the `instr(<folded search>, <folded query>)>0`
  matching, the `MATERIALIZED` page CTE for activity-sorted threads, and the
  per-query preview budgets (`MAX_READ_CHARS / (2 * n)`, `/(n + 1)`).
- Posts are written on `context.WithoutCancel`, mirroring Rust's detached
  `tokio::spawn(...).await` so a cancelled tool caller cannot abort an accepted
  write and its one-shot fanout; a failed notice never fails the commit.

The stored format is byte-compatible with Rust's serde shapes, which
`TestLocalBoardStoresRustCompatibleJSON` pins directly against the database: the
`payload` column holds `{"metadata":...,"text":...}` (with chrono's serde
`...Z` timestamp), the `request` column holds `serde_json`'s `PostRequest`
(`{"request_id":...,"destination":{"Channel":"work"},...}`), subscription target
keys hold serde's externally tagged `SubscriptionTarget`
(`{"Channel":"work"}` / `{"Thread":"<uuid>"}`), and `channels.created_at` holds
`chrono::DateTime::to_rfc3339`'s numeric-offset form (`+00:00`, which Go needs
the `-07:00` layout to emit).

Shared with the in-memory arm in the same pass: `validateChannelName`,
`newBoardPostID` (now `uuid.NewV7`, matching Rust's `Uuid::now_v7`; the
in-memory arm used v4), and the cursor/paging helpers
(`boardWindow`/`finishBoardPage`/`decodePageCursor`/`base64PageCursor`) factored
out of `pageItems` so both backends page identically.

Tests: `agentboard/local_test.go` proves persistence across handle
close/reopen, request-id idempotency after reopen, the channel/thread/author/
after search filters, cursor paging with invalid-cursor rejection, channel and
thread subscription opt-outs (including that posting again does not undo a
thread unsubscribe), permanent deletion with a surviving tombstone and an
untouched sibling board, and the collaboration tools running over the SQLite
board.

Remaining for this program: the runtime registration behind the
`agent_message_board` + `multi_agent_v2` gates (the host-supplied `delegation`
namespace and `MULTI_AGENT_V2_NAMESPACE_DESCRIPTION`, the
`multi_agent_v2.message_board_in_memory` switch, and the `LocalBoardHost`
adapter, which needs the `AgentControl::resolve` seam and a running-turn
notification injection), plus the `contains_external_context` marker.

### Verification (fourth pass)

- `go test ./agentboard/ -count=1` is green for both backends and the tool
  layer.
- `go test ./... -count=1` is green with the SQLite backend in place.

### sync158: the external-context marker on tool outputs

Rust's `handle_any_tool` marks a thread's memory mode polluted when a successful
tool output declares `contains_external_context()` and
`memories.disable_on_external_context` is enabled
(`codex-rs/core/src/tools/registry.rs`). Go only had the separate #39791
standalone-item rule, so a tool that returned external context was unnoticed.

- `tool.Output.ContainsExternalContext` mirrors Rust's
  `ToolOutput::contains_external_context()`; it is never serialized.
- Producers: the message-board JSON output (Rust's
  `JsonToolOutput::new(result).with_external_context()`) and the standalone web
  search output (Rust's `SearchOutput`, which already carried the marker as a
  `Data` key in the Go port).
- Consumers: `turn.ToolDispatcherOptions.OnToolOutputExternalContext` runs after
  a successful handler return and never on the responder-error or fatal paths,
  matching Rust's `?`; `turn.RuntimeOptions.OnToolOutputExternalContext` binds
  the turn's thread ID so the host does not read it back out of the invocation
  context; the app-server applies the `memories.disable_on_external_context`
  gate and marks the thread polluted.
- The gate and the mark now live in one helper
  (`markThreadMemoryPollutedIfExternalContextDisabled`) shared by the tool-output
  rule and the pre-existing standalone-item rule.

Tests: `turn/tool_output_external_context_test.go` pins the callback matrix
(marker, plain, non-fatal failure, responder error, fatal error) and the
thread-ID binding; `agentboard/tools_test.go` asserts the collaboration tools
declare the marker; `appserver` proves the gate is inert until
`disable_on_external_context` is enabled.

Remaining for this marker: Rust also sets it on cloud-authority skill outputs
(`SkillToolAuthoritySelector::Cloud`), which lands with the cloud skills
provider program.

### Verification (fifth pass)

- `go test ./... -count=1` is green with the external-context marker wired.

### sync159: the message-board runtime registration

The last structural piece of the message-board program: the board is now opened
for a turn and its tools are registered, behind Rust's gates, with the
host-side adapter that bridges storage to the agent tree, the clock and the
running-turn services.

- **Config**: `features.multi_agent_v2.message_board_in_memory` (default false,
  Rust `MultiAgentV2Toml::message_board_in_memory`) joins
  `config.MultiAgentV2Config`.
- **Namespace**: `agent.MultiAgentV2NamespaceDescription` now carries Rust's
  `MULTI_AGENT_V2_NAMESPACE_DESCRIPTION` once, shared by the V2 delegation tools
  (`agent/tools_v2.go`) and the discussion tools.
- **Notification fragment** (`agentboard/notification.go`): Rust's
  `AgentMessageBoardNotification` - role `assistant`, no markers, content kind
  `agent_message_board.notification`, the `CHANNEL_POST` body with the
  `[Use read_post for the rest.]` suffix when the preview was truncated, and the
  legacy type markers kept for matching older rollouts. (It lives in
  `agentboard` rather than `context` because `context` must not import the
  package that owns `PostPreview`.)
- **Registration** (`turn.MessageBoardOptions` + `registerMessageBoardTools`):
  the host supplies the caller-bound board, the caller's authoritative path, the
  configured namespace and its description, and the active model catalog's
  per-tool description overrides (Rust `MultiAgentToolMessages::by_name`, which
  the board tools look up by their own names, not just the six V2 names).
- **Gates** (`appserver`): `messageBoardFeatureEnabled` requires both
  `agent_message_board` and `multi_agent_v2`, and
  `messageBoardEnabledForTurn` mirrors Rust's `install_agent_message_board`:
  an ephemeral runtime must not open durable storage unless
  `message_board_in_memory` is set. The board is registered independently of
  `agents.enabled`, matching Rust (the board does not depend on the delegation
  surface).
- **Host adapter** (`appserver/message_board.go`): the tree identity comes from
  the agent tree root and the caller path from the tree's registry
  (`runtimeAgentIdentity`/`runtimeAgentRegistry`); the SQLite home resolves as
  `sqlite_home` then `CODEX_SQLITE_HOME` then the codex home
  (`state.SqliteConfigForCodexHomeWithOverride`, Rust `config.sqlite_config()`);
  `Notify` follows Rust's order and outcomes - a missing thread is skipped, a
  recipient in another tree is an error, a path the agent does not own is an
  error, and an idle recipient is skipped with nothing queued for a later turn -
  and delivers through the existing agent-message input item on the recipient's
  active turn.

Tests: the config default/override, the fragment body and suffix, the nine-tool
registration (namespace, description and catalog override) and the empty-board
no-op, the gate matrix (both features, ephemerality, in-memory override, missing
V2 config), and the host's tree membership plus notify semantics (idle skip with
no queued input, cross-tree rejection).

### sync160: permanent thread deletion drops the board

Rust attaches the board's permanent deletion to the local thread store as its
`thread_data_cleanup` callback (`thread_manager.rs` ->
`LocalAgentMessageBoard::delete_boards`), so removing a thread's data also
removes the boards owned by those thread roots. Go now mirrors that:

- `appserver.Router` gained a `SetMessageBoardCleanup` hook, installed by
  `NewRuntimeRouter` next to the other thread-store callbacks. It runs at the
  end of `handleThreadDelete` (after the records, history, rollouts and names
  are removed) and a failure is surfaced as the delete's error, as Rust's
  thread store does.
- `RuntimeRouter.messageBoardCleanup` resolves the same SQLite home as the open
  path (`sqlite_home` -> `CODEX_SQLITE_HOME` -> codex home) and calls
  `agentboard.DeleteLocalBoards` with the deleted thread IDs. A child's ID does
  not match its parent's board, so deleting a subagent leaves the tree's board
  alone; an unresolvable SQLite home means there is no durable board to drop, so
  the delete still succeeds.
- Erasure stays out of `unload` and `archive`, and an ephemeral thread delete is
  already rejected before this point, matching Rust's contract for this
  callback.

Test: `TestThreadDeleteRemovesOwnedMessageBoardsLikeRust` deletes a subagent
through the runtime router (the tree's board survives), then the root (the board
is tombstoned and the tombstone survives a reopen).

Remaining for this program: releasing a live board handle when a thread unloads
(Rust drops the handle; Go keeps one process-lifetime handle per tree, which is
functionally equivalent but retains state for deleted trees until the process
exits), and an end-to-end app-server suite case for the ephemeral in-memory
board (#47946).

### Verification (sixth pass)

- `go test ./config/ ./agentboard/ ./turn/ ./appserver/ -run 'MessageBoard'
  -count=1` is green.
- `go test ./... -count=1` is green apart from two known load-sensitive flakes
  in `appserver`/`app` (`TestOtelProviderReloadsAfterAccountChange`,
  `TestRuntimeRouterDynamicToolCallEmitsAnalyticsLikeRust`,
  `TestAppServerListenOffUsesConfiguredRemoteControlURL`), each of which passes
  when re-run in isolation (`-count=2`/`-count=3`).

### Verification (seventh pass)

- `go test ./appserver/ -count=1` is green on its own (183s), including the new
  deletion test and every existing thread-lifecycle test.
- `go test ./... -count=1` is green apart from the same known load-sensitive
  flakes, each verified green in isolation.

### sync161: the application network policy owner and the app-server enforcement

The first slice of the application-network-policy program (#47389-#47968). Go
already had the policy types (`network.DestinationPolicy`/`NetworkPolicy`/
`NetworkPermit`/`NetworkPolicyController`, ported from
`http-client/src/network_policy.rs`) and the managed requirements
(`config.ApplicationNetworkRequirements`), but nothing consumed them: no owner
published a policy and no transport enforced one.

- **Composition** (`appserver/application_network.go`):
  `destinationPolicyFromApplicationRequirements` mirrors Rust's
  `app-server/src/application_network.rs::destination_policy` - a missing,
  empty or disabled `application.network` requirement leaves traffic
  unrestricted; an enabled one restricts it to its explicitly allowed domains.
- **Owner**: the router now owns a `network.NetworkPolicyController`
  (Rust `ConfigManager`'s controller). `refreshApplicationNetworkPolicy`
  composes the policy from the effective managed requirements
  (`requireConfig().Requirements().Application`), publishes it under a
  revision-guarded lock, and returns the read handle plus the composed policy;
  `invalidateApplicationNetworkPolicy` revokes outstanding operations for an
  account change. Because the policy is re-derived whenever a transport is
  built, a request can never run under a superseded policy (Rust publishes at
  startup and at explicit reloads).
- **Enforcement** (`network.PolicyHTTPDoer`): the managed transport execution
  from #47389 - the destination is checked before route resolution, every
  redirect destination is checked before it is followed, and the permit stays
  valid until the response body is closed, so a revocation aborts an in-flight
  read and surfaces as `network.PolicyError` (Rust `TransportError::Policy`).
  `appserver.httpClientForConfig` now binds the policy, so all thirteen
  app-server client call sites (model sampling, realtime, web search, image
  generation, plugin and MCP-adjacent requests) are covered. An unrestricted
  policy returns the client unwrapped, so the common no-requirements case is
  unchanged and the shared-client reuse stays intact.

Tests: `network/policy_http_test.go` mirrors the Rust policy execution cases
(fail-closed while unpublished, rejection before connecting, unmanaged
pass-through, revocation aborting a body read, redirect destination checks) and
`appserver/application_network_test.go` pins the composition matrix plus an
end-to-end router case (allowed destination reaches the transport, denied
destination is rejected before connecting, and removing the requirement at a
reload restores access).

Remaining for this program: the bootstrap/endpoint scoping
(`NetworkPolicy::restrict_to_endpoints`, absent in Go) and the transports that
still build their own clients (`realtime_runtime.go`, `app/mcp.go`,
`app/app.go`, `exec/exec.go` - the #47411 embedded-startup path), the WebSocket
permit guarding from #47389 (realtime setup/read/write), AWS auth and telemetry
(#47408), remote control (#47410), the ChatGPT backend (#47703) and history
notes/image generation (#47742).

### Verification (eighth pass)

- `go test ./appserver/ ./network/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync162: endpoint scoping and the restricted cloud bootstrap

The second slice of the application-network-policy program: Rust's
`NetworkPolicy::restrict_to_endpoints` was missing from Go, so the bootstrap
clients that Rust pins to one exact URL were unconstrained here.

- **`network.NetworkPolicy.RestrictToEndpoints`** mirrors the Rust method: it
  installs (or intersects with) an exact-URL scope, it can never grant access a
  destination policy denies, and - as in Rust - the scope is enforced even
  without a policy owner. An empty set denies every destination, and
  `AcquireForUnsupportedSDK` fails once a scope exists. `IsScoped` reports
  whether a policy constrains destinations through either mechanism, and
  `PolicyHTTPDoer` now uses it, so an unmanaged-but-scoped policy is enforced
  rather than passed through.
- **`config.LoadCloudConfigBundle`** scopes its primary and system-proxy
  fallback clients to the exact config bundle endpoint
  (`<base>/wham/config/bundle`), mirroring Rust's
  `ConfigManager::replace_cloud_config_bundle_loader` and
  `in_process_bootstrap.rs`, whose comment states the intent: the local policy
  limits cloud bootstrap. `CloudConfigFetchOptions.NetworkPolicy` lets a caller
  (the app-server's owner from sync161, once the embedded path is wired) add its
  destination policy on top; the endpoint scope still cannot be widened.

Tests: the network endpoint-scope matrix (enforced without an owner, intersects
on re-restriction, empty set denies all, destination rules still bind) plus the
doer-level enforcement, and a config test proving the bootstrap client reaches
only the bundle endpoint and that a managed policy denying the endpoint still
denies it.

Remaining for this program: the app/exec-level policy owner that composes the
CLI host's requirements and binds them to the remaining transports
(`app/app.go`, `app/mcp.go`, `exec/exec.go`; Rust #47411), the bootstrap auth
routing from `login/src/outbound_proxy.rs` (`AuthRouteConfig`'s local bootstrap
factory), WebSocket permit guarding (#47389), AWS auth/telemetry (#47408),
remote control (#47410), the ChatGPT backend (#47703) and history
notes/image generation (#47742).

### Verification (ninth pass)

- `go test ./config/ ./network/ ./app/ ./appserver/ ./exec/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync163: transport-level policy enforcement for concrete clients

`sync161` bound the policy through a `Do` decorator, which the realtime runtime
silently dropped: it asserted `httpClientForConfig(cfg).(*http.Client)`, and a
policy-bound `model.HTTPDoer` is not an `*http.Client`, so the assertion failed
and the code fell back to a fresh, unwrapped client - losing enforcement on the
realtime path (Rust guards WebSocket setup with the same permit, #47389).

- **`network.PolicyRoundTripper` / `PolicyHTTPClient`** enforce the policy at the
  client's transport seam: the destination is checked before the request is
  issued (so WebSocket dialing and HTTP requests are both covered), the permit
  stays valid for the response body, and revocation surfaces as `PolicyError`.
  An unscoped policy returns the caller's client unchanged, so shared clients
  stay shared.
- **`appserver.policyHTTPClientForConfig`** builds the concrete policy-bound
  client, and `realtime_runtime.go` now uses it instead of a failing type
  assertion, so the app-server's realtime transport (WebSocket dial plus HTTP
  call setup) is enforced too.

Tests: `network/policy_round_tripper_test.go` (identity when unscoped, denial
before the transport is reached, revocation aborting a body read) and an
extended app-server case asserting the concrete client carries a
`PolicyRoundTripper` while restricted and is left unwrapped when unrestricted.

### Verification (tenth pass)

- `go test ./network/ ./appserver/ ./realtime/ ./config/ ./app/ ./exec/
  -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync164: the config carries the policy and `codex exec` publishes it

Rust's `EmbeddedNetworkPolicy::activate` publishes the policy composed from a
config's managed requirements and *binds it to that config*, so every client
built from the config shares one policy generation. Go had no such carrier, so
the non-interactive runner (`codex exec`, which Rust wires in
`exec/src/lib.rs`) still built unconstrained clients.

- **`config`**: the composition now lives with the requirements type
  (`config.DestinationPolicyForApplicationRequirements`, the one place managed
  application requirements become a destination policy - the app-server's
  Rust-named `destination_policy` wrapper delegates to it), and `config.Config`
  carries the live policy (`ApplicationNetworkPolicy`, never serialized) with
  `NetworkPolicy`/`SetNetworkPolicy` and
  `BindApplicationNetworkPolicy`, which creates a controller, publishes the
  composed policy and binds it - the Go form of `EmbeddedNetworkPolicy::activate`.
- **`exec`**: the run publishes the policy right after loading its effective
  config, and `Runner.httpClientForConfig` binds it, so the run's model, web
  search and image-generation clients share the restricted generation. An
  unpublished policy leaves the client untouched.

Tests: the carrier (unbound config is unmanaged; a bound config is scoped and
unrestricted without requirements; an enabled requirement restricts to its
allowed domains and denies denied/unlisted ones) and the runner end to end -
a `requirements.toml` with `[application.network.domains]` flows through the
config load into enforcement, with an allowed host reaching the transport and a
denied host rejected before connecting.

Remaining for this program: the CLI/TUI host owner (Rust #47411's
`EmbeddedNetworkPolicy::load/bind` in `tui/src/lib.rs`, `startup_orchestration`,
`worktree_startup`, `config_persistence`). The app host loads configs at roughly
a dozen sites (`app/app.go` 546/582/760/774/809/1211/1432/1588/1735/1767/1805,
`app/agents.go` 91, `app/mcp.go`) and builds clients at nine of them; the host
must own **one** controller pair (effective plus local bootstrap) and bind it to
every config it produces, not create a controller per client. Then the bootstrap
auth routing (`login/src/outbound_proxy.rs`'s local bootstrap factory), the
remaining unguarded transports (AWS auth and telemetry #47408, remote control
#47410, ChatGPT backend #47703, history notes and image generation #47742), and
WebSocket permit guarding beyond the transport check (#47389).

### Verification (eleventh pass)

- `go test ./config/ ./exec/ ./appserver/ ./network/ ./app/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync165: the CLI host binds its application network policy

The CLI host (`app/`) builds the transports Rust's embedded policy covers but had
no owner at all, so its cloud-config, MCP, exec-server and code-mode-host clients
ignored managed application restrictions.

- **`app/application_network.go`**: `publishApplicationNetworkPolicy(cfg)` is the
  host's `EmbeddedNetworkPolicy::activate` (compose from the config's managed
  requirements, bind the generation to the config) and `policyHTTPClient(cfg,
  client)` binds it to a concrete client, publishing first when the config
  carries none.
- **Cloud bootstrap**: both loaders (`sandboxCloudConfigBundleForRun`,
  `mcpCLIStore.LoadManaged`) now pass the host policy in their fetch options, so
  the bootstrap GET is both endpoint-scoped and destination-checked (Rust's
  `replace_cloud_config_bundle_loader` plus the local policy).
- **Host clients**: the MCP store's three loads and all five
  `loadedConfig`-derived clients in `app/app.go` (exec-server forward and
  listener, remote exec-server registration, code-mode host) are bound.
- **Only bind what restricts**: `config.Config` now also records the composed
  destination policy (`RestrictsApplicationTraffic`), and the app, exec and
  app-server paths return the caller's client untouched when the generation is
  unrestricted - preserving client identity and shared transports.

Fixing this exposed a real hazard in sync161/sync163's enforcement: replacing
`response.Body` broke upgraded WebSocket connections, whose concrete
`io.ReadWriteCloser` type is what lets the WebSocket client take over the
stream. Both enforcement seams now hand a hijacked stream to the caller
unchanged (the destination check that authorized the request still applies).
`TestExecServerRemoteValidationLikeRust` caught this; the new
`TestPolicyHTTPDoerKeepsHijackedResponseBodiesLikeRust` pins it.

Tests: the CLI host's published policy (allowed, denied and unlisted domains;
unrestricted without requirements), the MCP store's client denying a denied host
before connecting, and the hijacked-stream preservation.

Remaining for this program: the CLI host's bootstrap auth routing
(`AuthRouteConfig`'s local bootstrap factory from
`login/src/outbound_proxy.rs`), stream-level permit guarding for upgraded
WebSockets (Rust's split reader/writer wakeups, #47389), and the transports
Rust's later commits cover: AWS auth and telemetry (#47408), remote control
(#47410), the ChatGPT backend (#47703) and history notes/image generation
(#47742).

### Verification (twelfth pass)

- `go test ./network/ ./app/ ./exec/ ./config/ ./appserver/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync166: ChatGPT backend requests preserve the account network policy

Rust #47703 ("Preserve account network policy for ChatGPT backend requests")
covers the app-server's ChatGPT backend routes: `app/read`, the plugin
processors, the connectors client and the account-scoped default client. Go's
equivalent routes went through `RuntimeRouter.accountHTTPClient`, which returned
the injected account client or `http.DefaultClient` **without** the application
network policy, so managed restrictions did not cover the account, plugin and
cloud-config backends.

- `accountHTTPClient` now binds the router's published policy to the account
  client (the two call sites are the installed-plugin fetch in
  `plugin_remote_installed.go` and the cloud/account client built in
  `runtime_router.go`), and returns the client untouched when the generation is
  unrestricted, preserving the injected account transport.

Test: with managed requirements restricting to one host, the account client
reaches the injected transport for an allowed backend host and rejects a denied
one before connecting; without requirements the injected client is returned
unchanged.

Audited in the same pass: the other half of #47742 (history notes and image
generation) is already covered in Go - the history-notes backend is built with
`r.httpClientForConfig(cfg).Do` and image generation with
`r.httpClientForConfig`/the run's policy-bound client - so no change was needed
there beyond recording it.

### Verification (thirteenth pass)

- `go test ./appserver/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync167: OTLP exports are disabled under a restricting policy

The telemetry half of Rust #47408: "Guard OTLP log, trace, and metric exports
with revocable permits, disable them under destination restrictions, and make
managed HTTP exports cancellable." The config now carries the published policy
(sync164), so `otelinit.BuildProvider` can apply the disable rule Rust states:
when a managed policy restricts application destinations, the log, trace and
metric exporters are all set to `none`, because an export endpoint is not
necessarily one the policy allows.

Test: a config with analytics enabled and an OTLP/HTTP metrics endpoint builds
no enabled metrics exporter once its requirements enable
`application.network`, while the same config without the restriction keeps its
OTLP pipeline.

Audited in the same pass, no change needed: the analytics events client already
sends through `r.httpClientForConfig(cfg)`, so the "send analytics through the
authenticated account's HTTP client factory" half of #47408 is covered since
sync161.

Remaining for #47408: the AWS credential/region requests and Bedrock
authentication (Rust's new `aws-auth/src/transport.rs`, which checks the signing
destination before loading credentials and requires an unrestricted policy for
credential exporters and reauthentication), permit-guarded and cancellable
managed OTLP HTTP exports, and the Statsig-settings suppression while a managed
policy is active.

### Verification (fourteenth pass)

- `go test ./otelinit/ ./telemetry/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

---

## Follow-up pass (same day, third pull): `86be5320b0` (#48060)

Upstream advanced one commit, `4b1c0c30da..86be5320b0` (41 files, +928/-193):
"Deduplicate retained instructions across Guardian reviews".

### sync168: the static parity layer is re-pinned to `86be5320b0`

`go test ./parity/` failed in exactly one place at the new checkout
(`TestPrecomputedAppServerExportsMatchRustTarget`). Only the **stable** export
changed (154,524 -> 154,755 bytes; the experimental one is byte-identical), so
the vendored copy was refreshed and its SHA-256 pin re-frozen to
`0be68c20a72e9fdde88a7fa46fcf0ac3b91d0b1a0c655a2824170b0e6166e845`. Every other
pinned constant (inventory, workspace members, critical file hashes, fixture
roots, TUI snapshot totals) is unchanged by #48060; the full parity suite is
green again at the new pin.

### #48060 triage: no independently landable Go delta

The commit is a refinement *inside* the queued Guardian v2 authorization-context
program (queued item 3 below), not a new surface:

- `guardian-context/src/retained_instructions.rs` (+119),
  `composition.rs`, `enforcement.rs`, `verified_answers.rs`, and the new
  `history/src/retained_source.rs` add source identity/revision/completeness
  tracking so a follow-up review omits retained instructions only while the same
  complete revision is still in reviewer history, restores them once compaction
  drops them (inside the request budget, classified as context rather than new
  user authorization), and keeps source-order labels stable across eviction.
- `core/src/context/guardian_retained_instructions.rs` (new) is the
  `guardian.retained_instructions` user-role fragment for restored evidence, and
  `contextual_user_message.rs` now also classifies a user message as Guardian
  context when its `content_item_kinds` metadata is exactly that kind - the
  fragment type-marker path Go's `context` package does not model.
- `core/src/guardian/{input_budget,request_budget}.rs` and
  `context_manager/history.rs` carry the dedup/restore accounting.

Go has none of that machinery: `state/guardian.go`'s prompt builder documents
that "the sections Go does not model (retained instructions, trusted answers,
permissions, previous reviews, trusted tool/skills) come from the review's
inherited context instead", and greps for `retained_source`,
`retained_instructions`, `InputBudget` and `RequestBudget` find no Go surface.
Landing #48060's dedup semantics without the retained-context program would be
unreachable code, so it is recorded here as a required part of that program:
the retained-source metadata, the omit/restore rules, the
`guardian.retained_instructions` fragment kind and the metadata-based context
classification all land together with the guardian-context/retained-context
port.

### Verification (fifteenth pass)

- `go test ./parity/ ./appserver/ -run 'Precomputed|ExportsMatch' -count=1` is
  green at the new pin.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync169: remote-control requests honor the application network policy

Rust #47410 ("Honor network policy in remote control and recover remote
execution") routes remote-control enrollment and server requests through the
shared application policy. Go's remote-control backend was built with
`options.RemoteControlServerAPIOptions` as-is, and the package's own default is
`http.DefaultClient`, so a managed restriction did not cover it.

- `RuntimeRouter.remoteControlHTTPDoer` returns the router's policy-bound doer
  (the `network.PolicyHTTPDoer` from sync161) when the published generation
  restricts destinations, and nil otherwise so the package keeps its default.
- `remoteControlManagerBackend` now clones the caller's `ServerAPIOptions`
  (no caller mutation) and installs that doer when no explicit client was
  supplied, so enrollment and every server API request are checked before
  connecting.

Test: with managed requirements restricting to one host, the built backend
carries a client that rejects a denied host before connecting; without managed
requirements the backend keeps the package default (nil) doer.

Still open for #47410: the remote-control *WebSocket* dial
(`websocket.DialOptions.HTTPClient`, which the manager's default dial leaves to
the websocket package) and the exec-server remote-execution recovery work in the
same commit (`client_recovery.rs`, `remote/direct.rs`, the route-aware client
pool changes).

### Verification (sixteenth pass)

- `go test ./appserver/ ./remotecontrol/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync170: the remote-control WebSocket dial carries the policy

Completes the remote-control half of #47410 begun in sync169. Go's remote-control
WebSocket dial built `websocket.DialOptions{HTTPHeader: ...}` with no client, so
the connection used the websocket package default and a managed application
restriction did not apply to it (Rust guards the WebSocket transport with the
same policy).

- `remotecontrol.ManagerBackendOptions` gained `WebsocketHTTPClient`, and
  `ConnectWebsocketContext` installs it on the dial options when set (nil keeps
  the websocket default, so proxy behaviour is unchanged when unrestricted).
- The app-server's policy helper is factored into
  `remoteControlRestrictedPolicy` plus the two adapters
  (`remoteControlHTTPDoer` for the server API, `remoteControlWebsocketHTTPClient`
  for the dial), and `remoteControlManagerBackend` sets both.

Tests: `remotecontrol` captures the dial options through an injected dial and
asserts the backend client is passed (and that the default stands when the
backend supplies none); the app-server test asserts the built backend's
WebSocket client rejects a denied host before connecting and stays nil without
managed requirements.

Still open for #47410: the exec-server remote-execution recovery work in the same
commit (`client_recovery.rs`, `remote/direct.rs`, and the route-aware client pool
changes), which the plan tracks with the exec-server program.

### Verification (seventeenth pass)

- `go test ./remotecontrol/ ./app/ ./network/ -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync171: AWS credential and region requests accept the application client

The first part of the AWS half of #47408 ("Route AWS credential and region HTTP
requests through the shared HTTP client, ... check the signing destination
before loading credentials"). Rust adds `aws-auth/src/transport.rs`, a Smithy
connector that runs every credential/region request through the application-owned
client so a policy denial surfaces as a connector error instead of a network
failure. Go's `auth` package called `awsconfig.LoadDefaultConfig` with the SDK's
own client in three places, so managed restrictions never applied to AWS
credential discovery.

- `auth.AWSAuthLoadOptions{HTTPClient *http.Client}` carries the host's
  application client, and three new variants accept it:
  `LoadAWSAuthContextWithOptions`, `LoadAWSAuthContextWithProviderAndOptions`
  (which also covers the credential-exporter provider path) and
  `ResolveAWSRegionWithOptions`. The existing exported functions delegate with
  nil options, so behaviour is unchanged where no client is supplied.
- The client is installed with `awsconfig.WithHTTPClient`, so the SDK's
  credential and region requests go through `network.PolicyHTTPClient` and a
  managed restriction is checked before the request leaves the process.

Test: with a restricted policy (one allowed host), AWS credential discovery
fails with the application network policy denial instead of reaching the
metadata service - the SDK's IMDS request is rejected before connecting. Note
that Go's `NewAWSAuthContext` (static access keys with an explicit region)
already skips the config chain, which is the "skip discovery for static access
keys" half of the Rust change.

Remaining for the AWS half: pass the client from the hosts - `model/provider.go`
(439/441 for `LoadAWSAuthContextWithProvider`/`LoadAWSAuthContext`, 502 for
`ResolveAWSRegion`) and `model/responses_agent.go` (3104/3106 for the refresh
path) need an `*http.Client` threaded from the app-server
(`policyHTTPClientForConfig`) and the exec runner's policy-bound client; then the
account-scoped policy for Bedrock signing, the unrestricted-policy requirement
for credential exporters and reauthentication commands, and the Statsig global
settings suppression Rust states (Go has no global Statsig settings object, so
that item needs an audit rather than a new token).

### Verification (eighteenth pass)

- `go test ./auth/ ./model/ -count=1` is green.

### sync172: the AWS client is installed by the hosts and used by the model layer

Finishes the wiring sync171 left open: the AWS loaders accepted the host's
application client, but nothing passed one, so Bedrock credential discovery still
used the SDK default.

- **`model`**: `SetAWSHTTPClient`/`AWSHTTPClient` hold the process-scoped client
  (AWS credential discovery is itself process-scoped - environment, the shared
  config chain, the instance metadata service), and all five Bedrock auth call
  sites now pass it: `provider.go` 439/441 (`LoadAWSAuthContextWithProviderAndOptions`
  for the credential-exporter path and `LoadAWSAuthContextWithOptions`), 502
  (`ResolveAWSRegionWithOptions`), and `responses_agent.go` 3104/3106 (the
  post-refresh reload).
- **Hosts**: the app-server installs the policy-bound client whenever
  `refreshApplicationNetworkPolicy` publishes a restricting generation and clears
  it otherwise, so the AWS client always follows the published policy; the exec
  run does the same in `publishApplicationNetworkPolicy`.

Tests: the model round trip (uninstalled → nil options; installed → the exact
client; cleared → nil) and an end-to-end denial - with a restricted client
installed, `auth.LoadAWSAuthContextWithOptions(..., awsAuthLoadOptions())` fails
with the application network policy denial instead of reaching the metadata
service. The app-server case asserts the client is installed while restricted and
cleared when the requirement goes away.

Remaining for the AWS half of #47408: the account-scoped policy for Bedrock
signing, the unrestricted-policy requirement for credential exporters and
reauthentication commands, and the audit of Rust's Statsig global-settings
suppression (Go has no global Statsig settings object).

### Verification (nineteenth pass)

- `go test ./model/ -run 'AWS' ./appserver/ -run 'ApplicationNetwork' -count=1`
  is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync173: AWS signing and credential exporters enforce the policy like Rust

The remaining behavioural half of #47408's AWS work. Rust's `AwsAuthContext`
now carries the network policy: `sign()` acquires a permit for the *signing
destination* before `provide_credentials()`, so credentials are never loaded for
a denied destination, and the caller-supplied credential exporter goes through
`acquire_for_unsupported_sdk`, which requires an unrestricted policy because its
I/O runs outside the shared AWS transport; `permit.run(...)` cancels the work on
revocation and policy denials become `AwsAuthError::Policy` (non-retryable).

- **`auth`**: `AWSAuthLoadOptions` carries the `network.NetworkPolicy` the
  resolved context enforces. `AWSAuthContext.SignAt` acquires a permit for the
  request URL before retrieving credentials and runs the retrieval under that
  permit, so a denied destination fails before credentials are touched and a
  revocation cancels an in-flight fetch. `awsCredentialsProviderAdapter` holds
  the policy, calls `AcquireForUnsupportedSDK` (the exporter gate) and runs the
  provider under the permit. New `ErrAWSAuthPolicy` classifies these denials
  while still matching the `network` sentinels through a double `%w`.
- **`model`**: the installed client is now installed together with its policy
  (`SetAWSApplicationClient`/`AWSNetworkPolicy`), and `awsAuthLoadOptions()`
  passes both, so the gates apply to the production Bedrock paths; the app-server
  and exec hosts install the pair.

Tests: signing a denied destination returns the AWS policy denial and the
credentials provider is never consulted; an allowed destination signs and
consults it exactly once; a restricted exporter is refused without running; an
unrestricted one runs; and revocation makes further exports fail.

Remaining for #47408: Rust's "cancel active work when permission is revoked"
for a signing operation already in flight (Go checks at permit acquisition and
retrieval), and the Statsig global-settings suppression audit - Go has no global
Statsig settings object, so that item is recorded as N/A rather than stubbed.

### Verification (twentieth pass)

- `go test ./auth/ -run 'AWSAuth' -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

---

## Follow-up pass (same day, fourth pull): `69fa3881cb` (#48069)

Upstream advanced one commit, `86be5320b0..69fa3881cb`: "Handle early command
yields in the Guardian network approval test" - a single-file change to
`codex-rs/core/tests/suite/network_approval.rs` (+71/-25).

### sync174: parity is verified at `69fa3881cb`

`go test ./parity/` is green at the new checkout with no constant changes
required: the commit only edits an existing Rust test file, so the fixture-root
counts, critical file hashes, inventory, workspace members and vendored
precomputed exports are all untouched. Recorded rather than re-pinned silently so
the next pass knows the checkout moved.

### #48069 triage: no Go delta

The change hardens Rust's own Guardian network-approval test against an early
command yield (ordering between the approval request and the command's output).
Go's equivalent surface is `appserver/network_approval_test.go` plus the network
approval service, which already exercises the approval/command ordering; there is
no production behaviour in the commit to port.

### Verification (twenty-first pass)

- `go test ./parity/ -count=1` is green at `69fa3881cb`.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync175: managed OTLP HTTP exports are cancellable

The last bullet of #47408's telemetry work: "Guard OTLP log, trace, and metric
exports with revocable permits, ... and make managed HTTP exports cancellable."
sync167 disabled the exporters under a *restricting* policy; a managed but
unrestricted policy still exported without any policy attached, so a later
revocation could not cancel them.

- **`telemetry`**: the single `buildOTLPHTTPClient` helper (used by the metrics,
  logs and traces OTLP HTTP exporters) now wraps the client's transport with
  `network.PolicyRoundTripper` whenever an export policy is scoped, and
  `NewOtelProvider` installs `OtelSettings.NetworkPolicy` into that
  process-scoped slot before building the pipelines. Both metrics-driven and
  revocation-driven denials surface as policy errors instead of transient
  transport failures.
- **`otelinit`**: `Options.NetworkPolicy` is passed into `OtelSettings`.
- **Hosts**: the app-server supplies `applicationNetworkPolicyHandle()` and the
  exec run supplies `cfg.NetworkPolicy()` (the policy it published before
  building the OTEL provider).

Test: an endpoint-scoped export policy denies an out-of-scope export destination
before connecting; a managed policy makes a revoked export fail with the policy
denial; an unscoped policy leaves the export client unwrapped.

The suite caught one real defect while landing this: the app-server first passed
`applicationNetworkPolicyHandle()`, whose controller-backed handle is
*unpublished* at startup, so every export failed closed and twelve app-server
OTEL/span/tool-record tests broke. The app-server now publishes through
`refreshApplicationNetworkPolicy()` before building the provider, so the handle
it hands over is usable; the exec run already published before building its
provider.

### Verification (twenty-second pass)

- `go test ./telemetry/ -run 'BuildOTLPHTTPClient' -count=1` is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

---

## Follow-up pass (same day, fifth pull): `ca60d6fa72`, `60713126ee`

Upstream advanced two commits, `69fa3881cb..60713126ee`:

- `ca60d6fa72` (#48072) "Skip message-board notification previews when there are
  no recipients" - `ext/agent-message-board/src/in_memory.rs` (+3) and the
  board scenario test.
- `60713126ee` (#48073) "Avoid stdin approval for runtime-only permission
  grants" - `core/src/unified_exec/stdin_approval.rs` (+6/-1), its tests, a
  snapshot, and the plugin suite.

`go test ./parity/` is green at the new checkout; no pinned constant moved (the
fixture roots and the vendored exports are untouched).

### sync176: the empty-recipient board post skips its preview (#48072)

Rust's in-memory `post` returns as soon as it knows there are no recipients,
before building the notification preview. Go computed the preview
unconditionally and then handed it to `notifyRecipients`, which iterated an empty
set. `agentboard/inmemory.go` now returns after the thread subscription when the
recipient set is empty, matching Rust's early return; the message-board suite is
green.

### #48073 triage: needs the internal-permissions model first

Rust now compares the terminal's launch policy against
`effective_permission_profile(baseline, self.internal_permissions)` instead of
`effective_permission_profile(baseline, None)`, so a launch whose only extra
grants are runtime-internal is `UseDefault` (no review) while agent-requested
grants stay `WithAdditionalPermissions`. Go's counterpart
(`appserver/runtime_router.go`'s `terminalWriteReviewRequirement`) compares
`request.PermissionProfile` with the current profile and then consults
`request.AdditionalPermissions` - and `tool.WriteStdinApprovalRequest` has no
`InternalPermissions` field at all, so the runtime's own launch grants cannot be
distinguished from agent-requested ones. Landing the rule therefore needs the
unified-exec launch to record its internal grants and carry them on the approval
request; recorded here rather than guessed at, since Go has no internal-grant
model to compare against yet.

### Verification (twenty-third pass)

- `go test ./agentboard/ -count=1` is green with the early return.
- `go test ./parity/ -count=1` is green at `60713126ee`.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync177: the message-board handle is per thread and released on unload

Closes the last lifecycle gap the message-board program recorded: Go opened a new
board handle on every turn's tool-registry build and never released it, so the
refcounts grew without bound and an unloaded thread kept its tree's in-memory
state. Rust opens one handle with the thread's runtime and drops it when the
runtime unloads.

- The router keeps one caller-bound handle per thread
  (`messageBoardHandles`) and `messageBoardHandleForThread` reuses it, so a
  thread's turns share a single handle while the tree's state stays shared across
  the tree's threads.
- `closeMessageBoardHandle` pops and closes the handle, and `markThreadUnloaded`
  calls it next to the other per-thread lifecycle cleanup, so the last handle of a
  tree releases the shared in-memory state and the SQLite pool reference exactly
  as Rust's `Arc` drop does. Archive and delete already unload through the same
  path, and permanent deletion still tombstones the durable board (sync160).

Test: a thread reuses one handle across two builds, unloading it drops the live
board count to zero, and a thread loaded again afterwards opens a fresh handle.

### Verification (twenty-fourth pass)

- `go test ./appserver/ -run 'ThreadUnloadReleasesTheMessageBoardHandle' -count=2`
  is green.
- `go test ./... -count=1` is green apart from the known load-sensitive flakes.

### sync178: audits that change the next action (no code)

Two remaining items were re-scoped to the *real* prerequisite after inspecting
both sides, so the next pass implements the actual end state instead of a
plausible-looking subset.

**#48073 (stdin approval for runtime-only grants) is inert in Go, and the reason
names the real gap.** Rust's rule only matters because Rust *produces*
runtime-internal grants in the unified-exec launch:

```
let internal_permissions = merge_permission_profiles(sidecar_permissions, snapshot_permissions);
let additional_permissions = merge_permission_profiles(req.additional_permissions, internal_permissions);
```

where `sidecar_permissions` is `PluginMetricsSidecar::additional_permissions` and
`snapshot_permissions` is `snapshot_read_permissions(...)` for the shell snapshot
when `CREDENTIAL_BROKER_ACTIVE_ENV_KEY=1`
(`core/src/tools/runtimes/unified_exec.rs`). Go has neither: `plugin.PluginMetricsSidecar`
exports no permission profile, `shell/snapshot.go` only manages snapshot files
(no read-permission snapshot), and `tool/unified_exec.go` records the launch's
`additionalPermissions` straight from the launch request. With
`internal_permissions == nil`, Rust's comparison is
`effective_permission_profile(baseline, None)`, which Go's
`terminalWriteReviewRequirement` already reproduces. So the newly queued item is:
model the sidecar's and the credential-broker snapshot's permission grants, merge
them into the launch, carry them on `tool.WriteStdinApprovalRequest` as
`InternalPermissions`, and then apply Rust's comparison. Adding the field first
would be an unreachable no-op.

**Bootstrap auth routing needs the local policy, not the effective one.** Rust's
`bind_bootstrap_auth` binds `EmbeddedNetworkPolicy::local` - a *second* controller
published from `load_local_application_requirements` - and scopes it per endpoint
through `AuthRouteConfig::authentication_factory(endpoint)`. Go has a single
policy published from the *effective* (managed) application requirements, so
binding it to the OAuth/device-login clients (the router's
`accountOAuthOptions`, `app/app.go`'s device-code and browser-login options)
would be stricter than Rust and could lock out the login endpoints a managed
allow-list does not mention. The queued item is therefore: add the local
requirements load and publish it into its own controller, then scope the auth
factory per endpoint. No policy was bound to the auth clients in this pass so the
login paths keep working until that pair exists.

### Verification (twenty-fifth pass)

- No code changed; the worktree is clean and the previous full-suite run stands
  (`go test ./... -count=1`, 98 packages, zero failures, at `4cdedc7a`).
