# codex_go 对齐 Rust Codex `5157493c23` 更新计划

> 日期：2026-08-03  
> 上一对齐基线（Rust）：`bbbf396839b3bab872291354878980ba82ad4aee`  
> 上一对齐基线（Go）：`02b7c37`（Align Go Codex with Rust bbbf396839）  
> Rust 目标：`5157493c23`  
> 变更范围：32 个提交，317 个文件，+13,012 / -4,903  
> 目标：将 Go 版 Codex 同步至上游 `5157493c23`，功能对齐并通过测试验证

## 一、上游变更概况（bbbf396839..5157493c23）

### 1. 桌面协议 v2 / app-server
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `5157493c23` | Expose onboarding hints in login completion notifications | appserver/protocol、auth |
| `c42ea41ee0` | Add thread section management APIs | appserver、state、session |
| `64b2a3008e` | Declare the experimental plugin search API | appserver、plugin |
| `d62353a312` | Load local session pickers from the state DB first | appserver、session、state |

### 2. 插件体系
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `2b5bdcf675` | Support portable Agent Plugins throughout installation | plugin、install |
| `feee0b07c7` | Increase remote plugin bundle size limits | plugin |
| `a850875a8e` | Implement remote plugin search | plugin |
| `da2c7ca8d1` | Migrate Cursor-managed skills into Codex | skillprovider、systemskills |

### 3. MCP
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `5825699981` | Raise the MCP catalog item limit to 2,048 | mcp |
| `287e1020ae` | Add strict automatic review for MCP elicitations | mcp、tool |
| `385fe95ce1` | Use MCP bindings as the step tool catalog | tool、turn、agent |
| `d97cb0dcad` | Consolidate MCP config editing in codex-core | config、mcp |

### 4. TUI 交互
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `1e85ca099e` | Support two-stroke TUI key chords | tui/tea、config/tui_keymap |
| `7dc1856685` | Avoid querying terminal size on every TUI redraw | tui |

### 5. 工具执行与用户输入
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `66ebeb7037` | Use the step-scoped router for tool execution | tool、turn |
| `775fb21d2a` | Keep effective tool exposure in the registry | tool/registry、model |
| `a1dd74b535` | Retain attempted tool metadata across prompts | turn、rollout |
| `bf7804c254` | Add acknowledged user message submission to core | agent、session |
| `e2c0837923` | Make user input blocking behavior explicit | protocol、tool/request_user_input |
| `b7a6106608` | Add an `--approve-for-me` CLI flag | cli、execpolicy |

### 6. Realtime
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `6751b54cae` | Add a realtime delegation acknowledgement control | realtime、appserver |
| `670f69416b` | Allow custom Codex instructions for realtime transitions | realtime、context |

### 7. 线程 / 持久化 / state
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `dc60dadce6` | Enforce single-writer ownership for all thread histories | state、session、history |
| `332eac4b85` | Load turn summaries with paginated queries | state、rollout |
| `9949245d1d` | Preserve SQLite thread metadata during goal mutations | state、rollout |

### 8. exec-server
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `ee0247f95a` | Extract exec-server request dispatching | execserver |
| `4c219fdb1a` | Use Git repositories as pre-tool hook test markers | exec、hook |

### 9. code mode / 沙箱 / 性能
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `2e32d95894` | Enable sandboxed V8 for code mode | codemode、sandbox（评估等价性） |
| `003ec63bba` | Avoid redundant filesystem probes | filesearch、sandbox |

### 10. 遥测与构建（含 Rust-only）
| 提交 | 主题 | Go 对应组件 |
|---|---|---|
| `1bef168976` | Track image preparation details in turn analytics | telemetry、rollout |
| `0d109f097c` | Move skill catalog rendering out of core | skillprovider、prompt |
| `845497f483` | Run native Windows Bazel tests with MSVC | Rust-only（不落地，仅记录） |

## 二、本地 codex_go 当前状态

- 最新对齐提交：`02b7c37`（Align Go Codex with Rust bbbf396839），其后另有 `eda207d`、`856346d` 两个功能提交。
- 工作区存在大量未提交修改（agent/app/appserver/exec/model/rollout/session/state/tool/tui/turn 等 40+ 文件）与未跟踪文件（tui 多代理、SDK 差分、vscode/tui 技能等），需先收敛提交，再开始本轮对齐。
- 本轮共 32 个上游提交，其中约 30 项涉及 Go 功能域，2 项（MSVC CI、sandboxed V8 构建）主要为 Rust/构建侧。

## 三、对齐计划（阶段）

| 阶段 | 范围 | 状态 | 验证 |
|---|---|---|---|
| P0 | 收敛当前 WIP，提交并记录 parity/baseline | 待开始 | git 状态干净、go test 基线 |
| P1 | 协议 v2：thread sections API、onboarding hints、plugin search API、session picker 状态库优先 | 待开始 | appserver/state/session 单测 |
| P2 | 插件与 MCP：portable agent plugins、远程插件搜索、MCP 目录上限 2048、elicitation strict review、MCP bindings 目录 | 待开始 | plugin/mcp/tool 单测 |
| P3 | 工具执行与输入：step-scoped router、tool exposure registry、attempted tool metadata、user input blocking、`--approve-for-me` | 待开始 | tool/turn/agent 单测 |
| P4 | realtime：delegation ack control、realtime transition instructions | 待开始 | realtime/appserver 单测 |
| P5 | 持久化：single-writer thread history、分页 turn summaries、SQLite goal mutation 元数据 | 待开始 | state/session/rollout 单测 |
| P6 | TUI：两键 chord 支持、redraw 终端尺寸查询优化 | 待开始 | tui 单测 + parity |
| P7 | exec/codemode：request dispatcher 对齐、git repo hook 标记、sandboxed V8 等价性评估 | 待开始 | execserver/exec 单测 |
| P8 | 全量验证：go test ./...、race、vet、SDK 差分矩阵、协议 schema 校验、更新 parity.json | 待开始 | 全量测试报告 |

## 四、风险与阻塞

1. 当前 WIP 未提交，先收敛避免混入本轮变更。
2. sandboxed V8（`2e32d95894`）依赖 Rust/构建侧，Go 侧评估行为等价性即可，不强行移植。
3. `845497f483` 为 Rust-only CI 变更，不进入 Go 功能对齐。
4. SDK/TUI 差分验证依赖模型 provider 容量，按既有 `--infra-retries`/`--fail-fast-infra` 策略执行。

## 五、后续执行顺序

1. P0：提交/整理当前 WIP，记录本轮起始 parity 基线。
2. 按 P1→P7 逐阶段落地，每阶段跑对应 `go test -count=1`。
3. P8：全量 `go test ./...`、`-race`、`go vet`、SDK 差分矩阵与 TUI parity。
4. 更新 `parity.json`、提交清单并输出对齐总结。
