# Codex Go update alignment plan - 2026-08-10

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Previous aligned Rust commit: `c5d9431971`
- Audited Rust commit: `f344a80a3b`
- Range: 44 commits (`c5d9431971..f344a80a3b`)
- Go baseline: `c7789c1` (clean worktree on `main`)
- The Rust checkout was fast-forwarded from `c5d9431971` to `f344a80a3b` on 2026-08-10.

## Upstream audit

### Implement in this update

1. `208f05b233` (`auto_review.required_on_models`): add managed auto-review model requirements, union model slugs across requirement layers, expose through `configRequirements/read`, and force `on-request` + `auto_review` approvals for listed models.
2. `2e3a1702c2` (`auto_review.ignore_rules`): expose `ignoreRules` in `AutoReviewRequirements` returned from `configRequirements/read`.
3. `e734a1a5c1` (cyber exec policy): filter saved `allow` prefix rules from the execution policy for cyber-specialized models and models listed in `auto_review.ignore_rules`, preserving prompt/forbidden/network rules.
4. `4ca25a2c4e` (sandbox mode in turn metadata): add `sandbox_mode` (effective permission profile) to turn metadata for regular/prewarm/compaction/memory requests and reserve it against client overrides.
5. `248d8c0e22` (MCP call IDs): include the tool call ID in `_meta.callId` for every MCP tool request; rename `features.tool_registry.include_tool_metadata` to `turn_metadata_includes_tool_info`.
6. `3aae5d885b` (execution mode in hook listings): verify and complete `executionMode` (`sync` default) in `hooks/list` responses and TUI hooks browser display.
7. `50ef7395fa` (I/O subtypes): append a stable `std::io::ErrorKind`-style category to `failed_to_load_session_config` import subtypes (`invalid_data`, `not_found`, `permission_denied`, ...).
8. `f344a80a3b` (bounded Cursor path resolution): probe a bounded set of path candidates with common separators instead of recursively walking directories (max 128 probes, reject ambiguous/unsafe components, parse Windows drive prefixes).
9. `c4513cb982` (launch context): strip `OPENAI_FEDERATION_RULE_ID` / `OPENAI_IDENTITY_TOKEN_FILE` (case-insensitive) from child-process environments across exec, MCP, hooks, Git helpers, and remote helpers.
10. `646f7c0a91` (environment config read support): add `environmentConfigRead` to exec-server environment capabilities, advertised for local executors, defaulting false for legacy responses.
11. `92fb33b758` (temporary directories): add optional `temporaryDirectories` file URIs to exec-server `EnvironmentInfo`, populated from `TMPDIR`/`TEMP`/`TMP`.
12. `509565820f` + `c2bcb9a26b`: register feature keys `code_mode_interrupt` and `guardian_reuse_parent_compaction` (under development) in the feature registry.
13. `208f05b233` + `5729546839`: register `model_auto_review` and `server_diagnostics` modules in the Rust app-server v2 suite manifest; update `hooks_list`, `config_rpc`, `plugin_install`, `external_agent_config`, `executor_skills`, `command_exec`, and `process_exec` manifest focus lines.

### Already equivalent or covered by the Go architecture

- `8e4b10446e` / `2b1811e562` (legacy `code_mode_tool_names` removal, opt-in `tool_namespaces_info`): Go removed the unbounded inventory and keeps the opt-in path scoped to the renamed `turn_metadata_includes_tool_info` setting.
- `6db53df37f` (preserve child waiters): Go PTY process handles keep waiters detached on termination.
- `a875dd6b22` (generalized hook handler execution): Go already routes handlers by kind (`command`, `prompt`, `agent`, `mcp_tool`) through the hooks engine; `null` MCP tool input rejection for TOML trust hashing is covered by Go's JSON/TOML argument handling.
- `f65ea998c7` (external agent detection queue): Go gives `externalAgentConfig/detect` its own serialization path that does not block config reads/writes.
- `420accf199` (step context approval prefixes): Go resolves approval prefix rules from the active turn/step context at approval request construction.
- `4ca25a2c4e` note: Go memory/prewarm paths already reuse the turn metadata builder.
- `94937de51b` (plugin install analytics subtypes): Go plugin install runtime reports stable low-cardinality failure subtypes.
- `45f8cafa4e`, `3b366654f1`, `33e365b19e`, `beac16cccd`, `b3278e96cb` (core-skills removal, skill config in codex-config): Go has no `core-skills` crate; Go skill loading/config rules live in `skillprovider`/`config` and already match the consolidated behavior.
- `62b7386b07` (diagnostic payload limits): Go limits high-volume payload logging in feedback and SQLite log sinks.

### Track as separate follow-up work

- Async command hooks (`6f647caa9b`): Go currently emits `skipping async hook ... async hooks are not supported yet` during discovery. Full background scheduling with a per-session concurrency limit, result injection at turn boundaries, config-reload preservation, and shutdown abort needs lifecycle changes across `hooks_runner` and `turn_runtime`. Design and landing are tracked as the next batch.
- Hook process-tree termination (`dd916428cd`): Go terminates hook commands on timeout but the dedicated process-group/job-object tree termination path is tracked with the async-hook batch.
- gRPC code-mode host service (`8073dbb20b`, `61a3dd4387`, `c0ad3ab014`) and WebSocket `TCP_NODELAY` (`abc5d0b552`): Go code-mode uses WebSocket/stdio transports; a gRPC transport is a new subsystem requiring protocol codegen and host lifecycle work.
- MCP event discovery/subscriptions (`41014b11bd`): `events/stream` lifecycle notifications require rmcp-client streamable-HTTP support not present in Go.
- Workload identity token exchange (`936f5eb3ee`): a new auth crate; Go auth has agent-identity endpoint overrides but no file-backed JWT assertion exchange/rotation/caching.
- Skill locator aliasing (`ce22ea9712`, `ba94150c2a`): context-budget aliasing for executor/orchestrator/host skill catalogs needs Go skill renderer changes.
- Guardian compaction reuse (`c2bcb9a26b`) and step-environment reviews (`dd43a9967f`): Go review sessions do not yet restart on parent compaction or pair Guardian with the step environment snapshot.
- Response stream reconnect on connection failures (`5a0d0929e2`): Go keeps the bounded retry budget without the 5-60s exponential reconnect window.
- TUI composer whitespace wrapping (`a16863f870`): Go TUI textarea wraps full lines but the grapheme-safe breakable-whitespace reflow is pending.
- Buffered turns in prompt editing (`266c6920d9`): Go prompt-edit fork reads the persisted thread; replay-buffer reconstruction is pending.
- Remote process sandbox delegation (`511262b984`): Go resolves sandboxing on the host side; executor-native sandbox intent for remote exec is pending.
- App-server diagnostics (`5729546839`, `27e4a05cd3`): `server/diagnostics` experimental API and lifecycle gauges are tracked for the app-server diagnostics batch.
- Interrupt code-mode cells with turn (`509565820f` runtime): the feature key is registered; the interrupt propagation runtime is tracked with code-mode lifecycle work.
- The remaining commits are Rust build/test infrastructure, analytics, schema regeneration, and internal refactors without an additional Go surface.

## Verification

- Focused unit tests for each synchronized protocol/config behavior.
- `gofmt` on all touched Go files.
- `go test ./config ./execpolicy ./appserver ./mcp ./codexapi ./features ./execserver ./turn` for the touched packages.
- `go vet` on touched packages and `git diff --check`.
- App-server v2 suite manifest drift check against the updated Rust suite.
- Feature-key parity check for the newly registered keys.

## Completion record

### Implemented (2026-08-10)

1. **AutoReview requirements (config surface)** (`208f05b233`, `2e3a1702c2`):
   - `config/api.go`: `AutoReviewRequirements{RequiredOnModels, IgnoreRules}` added to
     `ConfigRequirements` with JSON/omitempty + clone support, plus
     `ConfigRequirements.AutoReviewRequiredForModel` with exact provider-alias
     matching (Rust parity).
   - `config/requirements_file.go`: parse `auto_review` from requirements files/maps.
   - `config/cloud_config.go`: layer merge — `required_on_models` unions across
     layers; `ignore_rules` first-wins (Rust parity).
2. **AutoReview enforcement** (`208f05b233`):
   - `appserver/turn_runtime.go` + `runtime_router.go`:
     `turnApprovalPolicyForTurn` / `turnApprovalsReviewerForTurn` force
     `on-request` + `auto_review` for protected models; Full Access is downgraded
     to workspace-write at turn configuration.
3. **Cyber exec policy** (`e734a1a5c1`):
   - `appserver/runtime_router.go`: `modelIgnoresAllowPrefixRules` (cyber
     specialty or `auto_review.ignore_rules`); `shellApprovalForTurn` skips
     reusable exec-policy amendment proposals for those models (one-time decisions).
4. **sandbox_mode turn metadata** (`4ca25a2c4e`):
   - `codexapi` (`ResponsesMetadata` + `ClientMetadata`) reserve and emit
     `sandbox_mode`; `turn` metadata plumbing; `permissionProfilePolicyTag`
     mirrors Rust `permission_profile_policy_tag`; wired for regular turns
     (`appTurnRunConfig`) and detached memory metadata.
5. **MCP call IDs + config rename** (`248d8c0e22`):
   - `mcp` `MCPToolCallParams.CallID` → `_meta.callId` for every MCP tool request;
     `features.tool_registry.include_tool_metadata` renamed to
     `turn_metadata_includes_tool_info` (strict validation rejects the old key).
6. **Hook execution mode** (`3aae5d885b`):
   - `HookMetadata.ExecutionMode` (`sync` default) in `hooks/list`.
7. **Session import I/O subtypes** (`50ef7395fa`):
   - `sessionImportIOCategorySubtype` appends stable `std::io::ErrorKind`
     categories (`not_found`, `permission_denied`, `invalid_input`, ...) to the
     session-parse failure subtype.
8. **Bounded Cursor path resolution** (`f344a80a3b`):
   - `config/external_session_detect.go` rewritten to probe a bounded set of
     separator candidates (max 128 probes), reject unsafe components and
     ambiguous matches, and parse Windows drive prefixes.
9. **Launch-context env scrubbing** (`c4513cb982`):
   - New `envutil` package (`OPENAI_FEDERATION_RULE_ID` /
     `OPENAI_IDENTITY_TOKEN_FILE`, case-insensitive) applied across exec-server
     `childEnv`, MCP stdio, hooks runner, git helpers, code-mode host process,
     and appserver command/process exec.
10. **Exec-server capabilities/environment** (`646f7c0a91`, `92fb33b758`):
    - `environmentConfigRead` capability (local executors advertise `true`);
      `temporaryDirectories` file URIs from TEMP/TMP/TMPDIR (dedup, relative
      Unix resolution).
11. **Feature keys** (`509565820f`, `c2bcb9a26b`): registered
    `code_mode_interrupt` and `guardian_reuse_parent_compaction`
    (under development, disabled).
12. **App-server v2 suite manifest** (`208f05b233`, `5729546839`): registered
    `model_auto_review` and `server_diagnostics` modules; updated focus lines
    for `hooks_list`, `config_rpc`, `plugin_install`, `external_agent_config`,
    `executor_skills`, `command_exec`, and `process_exec`.

### Verification

- `go build ./...` clean; `go vet` clean on touched packages; `git diff --check`
  clean; `gofmt` clean on touched LF files (CRLF files keep their baseline
  line endings).
- Unit tests added: auto-review merge/layer semantics, provider-alias model
  matching, `sandbox_mode` metadata reservation/emission, permission-profile
  policy tag, MCP `callId`, hooks/list execution mode, session-import I/O
  subtypes, bounded Cursor path resolution (separators/ambiguity/unsafe/Windows
  drive), launch-context env scrubbing, exec-server capabilities +
  temporary directories, app-server v2 suite manifest drift.
- `go test` passes for config, codexapi, turn, mcp, features, envutil, gitutil,
  codemode, execserver and the app-server suite except three pre-existing
  baseline failures (verified via `git stash`):
  `TestRouterForkFromPath`, `TestRouterResumeFromPath`,
  `TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread`
  (rollout item-shape drift in the plain `Router`, unrelated to this update).
- Upstream pull to github.com failed this session (network unreachable); the
  local Rust checkout is already at the audited baseline `f344a80a3b`.

## Second pull (2026-08-10, manual `git pull origin main`)

### New upstream range

- Audited Rust commit: `c9c6c0daa9` (2 new commits after `f344a80a3b`)
- Range: `f344a80a3b..c9c6c0daa9`

### Implement in this update

1. `21aa552e87` (`apply_patch` line-ending preservation mode): add opt-in
   `PreserveLineEndings` update mode threaded through patch verification and
   application. Untouched/context lines keep their original endings (including
   mixed-ending files); inserted/replaced lines use the file's first line
   ending; patch context is tracked separately from identical changed lines so
   preservation does not alter replacement ordering or matching. The
   standalone `apply_patch` CLI reads `CODEX_APPLY_PATCH_PRESERVE_LINE_ENDINGS`.
2. `c9c6c0daa9` (feature flag): register `apply_patch_preserve_line_endings`
   (under development, disabled by default) and apply it consistently to the
   built-in apply_patch tool and patches invoked through shell, unified-exec,
   and app-server command execution. Active feature configuration is
   authoritative over inherited, shell snapshot, and client-provided
   environment values (`inject_apply_patch_env`).

### Go implementation plan

1. `applypatch` package: `PreserveLineEndingsEnvVar` + `FileUpdateMode`
   (NormalizeToLF default / PreserveLineEndings) + `FileUpdateModeFromEnv()`;
   `ApplyOptions.FileUpdateMode` threaded through verify/apply; `updateChunk`
   extended with oldLines/newLines/contextIndices/changeContext/isEndOfFile;
   new `line_endings.go` with `sourceFile` parser (retains LF/CRLF/CR endings,
   first ending preferred), `applyReplacements` (context lines stay in place,
   inserted lines use preferred ending), `seekSequence` tolerance, and
   `computeReplacementsPreserving`.
2. `applypatch/cli.go`: `RunCLI` selects mode from the env var.
3. `features/features.go`: register `apply_patch_preserve_line_endings`
   (under development, disabled).
4. `tool/apply_patch_executor.go`: `ApplyPatchExecutorOptions.PreserveLineEndings`
   flows into `ApplyOptions.FileUpdateMode`.
5. `envutil`: `InjectApplyPatchEnv(env, preserve)` removes the stale var
   (case-insensitive) and sets `=1` when enabled.
6. Thread from config: `runtime_router.go` (apply-patch tool + shell executor),
   `command_exec.go` + `process_exec.go` (child env injection), `handleCommandExec`
   / `handleProcessSpawn` read the feature from `r.services.Config`.

### Verification

- `gofmt` on touched files; `git diff --check`; `go vet` on touched packages.
- `go test ./applypatch ./features ./envutil ./tool ./appserver` focused tests.
- Unit tests: preserve CRLF/CR/mixed endings, repeated context lines, trailing
  blank lines, EOF overlap rejection, legacy LF normalization default,
  `FileUpdateModeFromEnv`, `InjectApplyPatchEnv` staleness, feature registry,
  executor option flow.

## Second pull completion record

### Implemented (2026-08-10, after manual pull)

1. **apply_patch line-ending preservation** (`21aa552e87`):
   - `applypatch/line_endings.go` (new): `PreserveLineEndingsEnvVar`,
     `FileUpdateMode` (NormalizeToLF default / PreserveLineEndings),
     `FileUpdateModeFromEnv()`, `sourceFile` parser (retains LF/CRLF/CR endings,
     first ending preferred), `applyReplacements` (context lines stay in place,
     inserted lines use preferred ending, trailing newline preserved),
     `seekSequence` (exact/rstrip/trim/unicode-normalize tolerance, eof anchor),
     `computeReplacementsPreserving`, and the preserve-mode apply path.
   - `applypatch/applypatch.go`: `ApplyOptions.FileUpdateMode` threaded through
     `Verify/ApplyVerified/preflight/applyCommitted/applyChange/applyUpdate/
     applyUnifiedDiffToContent`; `updateChunk` extended with `oldLines/newLines/
     contextIndices/changeContext/isEndOfFile`; `parseUpdateChunks` records
     context lines and `@@` context; NormalizeToLF now drops `\r` from CRLF
     endings during matching (Rust parity).
   - `applypatch/cli.go`: `RunCLI` reads the env var via `FileUpdateModeFromEnv()`.
2. **Feature flag** (`c9c6c0daa9`):
   - `features/features.go`: registered `apply_patch_preserve_line_endings`
     (under development, disabled); parity test `TestRustFeatureKeySurfaceAgainstGo`
     passes.
   - `tool/apply_patch_executor.go`: `ApplyPatchExecutorOptions.PreserveLineEndings`
     flows into `ApplyOptions.FileUpdateMode`.
   - `tool/shell_executor.go`: `ShellExecutorOptions.PreserveLineEndings` injects
     the rollout env var into `validation.Env` at Execute.
   - `envutil`: `InjectApplyPatchEnv(env, preserve)` removes stale values
     (case-insensitive) and sets `=1` when enabled.
   - `appserver/command_exec.go` + `process_exec.go`: child env injection after
     launch-context scrubbing; `CommandExecOptions` / `ProcessSpawnOptions`
     carry the rollout state.
   - `appserver/runtime_router.go`: `handleCommandExec` / `handleProcessSpawn`
     read the feature from config (`applyPatchPreserveLineEndingsFromConfig`);
     `toolRouterForTurnContext` wires the in-process apply_patch tool and the
     shell executor from `cfg.FeatureSettings()`.

### Tests added

- `applypatch/line_endings_test.go`: `FileUpdateModeFromEnv`, CLI env selection,
  preserve CRLF / CR / mixed endings, repeated context lines, trailing blank
  line append, EOF overlap rejection, legacy LF normalization default,
  verify-then-apply parity, chunk parsing (context indices / eof / change
  context).
- `envutil/envutil_test.go`: `InjectApplyPatchEnv` follow/override semantics,
  differently-cased stale key removal, nil map handling.
- `tool/apply_patch_executor_test.go`: default vs preserve mode via executor
  option.
- `appserver/command_exec_test.go`: command/exec env injection overrides the
  client-provided value when enabled and removes the stale value when disabled.

### Verification

- `go build ./...` clean; `go vet` clean on touched packages; `git diff --check`
  clean; gofmt clean on all touched files (CRLF files keep baseline endings).
- `go test ./applypatch ./features ./envutil ./tool ./appserver` passes except
  three pre-existing baseline failures (`TestRouterForkFromPath`,
  `TestRouterResumeFromPath`,
  `TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread`).
- `parity` package: `TestRustFeatureKeySurfaceAgainstGo` passes; four other
  parity drift tests (`TestRustCriticalFileHashDrift`, precomputed app-server
  exports, TUI snapshot manifest, unified_exec sandbox manifest) are
  pre-existing drift between the Go snapshots and the Rust checkout (Cargo.toml
  is unchanged across `f344a80a3b..c9c6c0daa9`), tracked as separate follow-up
  snapshot regeneration.

## 总结（2026-08-10 收尾）

对齐范围：Rust c5d9431971..c9c6c0daa9（两轮，共 46 个提交），Go 基线 c7789c1。

落地 13 项功能对齐（含两轮）：
- 配置面：uto_review.required_on_models / ignore_rules（configRequirements/read）、cyber 执行策略过滤、pply_patch_preserve_line_endings 特性开关。
- 协议面：turn 元数据 sandbox_mode + permission-profile policy tag、MCP _meta.callId、hooks/list executionMode、session-import I/O 子类型、exec-server environmentConfigRead + 	emporaryDirectories。
- 实现面：有界 Cursor 路径解析（≤128 探测、Windows 盘符）、launch-context 环境变量擦除（新 envutil 包）、apply_patch 行尾保留模式、code_mode_interrupt / guardian_reuse_parent_compaction 特性键注册、app-server v2 套件清单（model_auto_review、server_diagnostics）。

验证：
- go build ./...、go vet（12 个受影响包）、git diff --check、gofmt 全部通过。
- 受影响包单测全绿（applypatch / features / envutil / tool / config / codexapi / turn / mcp / gitutil / codemode / execserver）。
- appserver 全量仅剩 3 个既有基线失败（TestRouterForkFromPath / TestRouterResumeFromPath / TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread，git stash 复核与本更新无关）。
- 特性键面 parity（TestRustFeatureKeySurfaceAgainstGo）与 app-server v2 清单 drift 检查通过。
- 已提交：1542a8f（工作区干净）。

后续批次（已在计划中登记）：异步命令钩子、钩子进程树终止、gRPC code-mode 主机传输、MCP 事件订阅、工作负载身份令牌交换、技能定位器别名、Guardian 压缩复用、响应流重连、TUI 换行回退、缓冲 turns、远端沙箱委托、app-server diagnostics、code-mode 中断传播。


## 13 ??????????2026-08-10 ????

### P1+P2 ?????? + ????????6f647caa9b / dd916428cd?
- `appserver/hooks_async.go`?per-thread ?????????? 8??? channel?done ???WaitGroup ????`scheduleAsyncHook` ?????`DrainAsyncResults` turn ?????`ShutdownAsync` ???
- `hooks_runner.go`?`Run` ? async handler ?????SessionEnd ??????`runCommand` ? `hookProcessTree`?Unix ??? Setpgid + kill(-pid)?Windows JobObject + taskkill ??????/??????????????
- `hooks_discovery.go`?async hook ?????`hookDiscoveryExecutionMode` ??????
- `async_hook_turn.go`?turn ??? drain ???? inputItems?before user prompt??

### P8 ??????5a0d0929e2?
- `model/responses_stream.go`?`isResponsesConnectionFailure`?dial/connect/ECONNREFUSED/EHOSTUNREACH/ETIMEDOUT/TLS ??????sampling ????? 5?60s ??????????????? `Reconnecting... waiting for network` ????Bedrock ???????

### P10 ?????? turns?266c6920d9?
- `tui/app/thread_routing.go`?`ThreadEventStore.TurnsIncludingBuffered`?? replay buffer ?? live turns/items?????????????`ApplyPromptEditWithStore`?

### P12 app-server diagnostics?5729546839 / 27e4a05cd3?
- `appserver/server_diagnostics.go`?`server/diagnostics` ??? API??? ID???????????gauges?in-flight ????? turn?MCP ????experimentalApi ???

### P13 code-mode ???????509565820f?
- `tool/code_mode_exec.go`?`CodeModeRuntime.InterruptActiveCells`??? cancel + ?? Terminate??? session??`finishTurnInterruptedAnalytics` ? `code_mode_interrupt` feature ????

### P5 ???????????936f5eb3ee?
- `auth/workload_identity.go`?`WorkloadIdentityExchange`????? ?16KiB ???token URL ?? HTTPS/loopback???+??+??+???????????

### P11 ?????????511262b984?
- `tool/shell_executor.go`?`rejectRemoteApplyPatch`??? unified-exec ? fs ????????? apply_patch?full-disk ????

### P9 TUI ???????a16863f870?
- `tui/bottom_pane/textarea.go`????????????????????/?????????????????????

### P7 Guardian ???? + ???????c2bcb9a26b / dd43a9967f?
- `guardian_reviewer.go`?`guardianSessionRunner.ResetAfterParentCompaction`?????? review session ? compaction response ID ???????????? reviewer??`resetGuardianAfterParentCompaction` ? `compactThread` ??????feature ????

### P6 ????????ce22ea9712 / ba94150c2a?
- `prompt/skills_render.go`?`buildExtensionAliasPlan`?executor/orchestrator `e` ?????+ `extensionSkillAliasRoot`?URI scheme ????

### P4 MCP ????/???41014b11bd?
- `mcp/events.go`?`ListMCPEvents`?events/list?+ `OpenMCPEventStream`?events/stream?+ `MCPEventDefinition`/`MCPEventNotification`?

### P3 gRPC code-mode ???? + TCP_NODELAY?8073dbb20b / 61a3dd4387 / c0ad3ab014 / abc5d0b552?
- `codemode/grpc/`?? Rust proto ?? `codex.code_mode.v1` Go gRPC ???CodeModeHost ?? + ?? `Transport` ?????
- `codemode/grpc_transport.go`?`GrpcTransport` ?? `remoteTransport`?framing JSON ??? FramedMessage??
- `codemode/websocket_transport.go`?`disableWebSocketNagle`??? response body ? TCP ???? Nagle??

### ??
- `go build ./...`?`go vet`?`git diff --check`?gofmt ?????
- ?????????tool / model / auth / mcp / prompt / codemode / features / envutil / turn / compact / tui/...??
- appserver ???? 3 ????????TestRouterResumeFromPath / TestRouterForkFromPath / TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread??
