# Codex Go update alignment plan - 2026-08-13 sync batch 2

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Audited Rust commit: `e766f75989` (`origin/main` after `git pull origin main`)
- Previous Go alignment commit: `37226be` (Rust `95aada11c4`, #38205)
- Go worktree is shared with another in-progress follow-up batch, so this plan
  commits only the files listed below.

## Implemented in this batch

1. **`retain_client_developer_messages` feature flag** (`1ad4397821`, #38227)
   - Added to `features.Registry` as `StageUnderDevelopment`, disabled by
     default.
   - Added a focused feature test that asserts the Rust-compatible stage and
     default.

2. **Latest network-review rejection precedence** (`020f6c963e`, #38256)
   - Explicit Guardian and policy outcomes now replace the previously recorded
     outcome for an execution instead of being discarded when an earlier
     outcome exists.
   - Generic blocked-request fallback still cannot overwrite an explicit
     outcome.
   - Added regression tests for Guardian latest-wins, policy latest-wins, and
     explicit-outcome precedence over blocked-request fallback.

3. **Protocol fixture synchronization for thread usage**
   (`f1a1fce26a`/`842fae26c9`, #38281/#38270)
   - Added `account_thread_usage` to the Rust app-server v2 suite manifest.
   - Added `auth.GetAccountTokenUsageParams`, `auth.ThreadUsage`, and
     `auth.ThreadUsageBreakdownGroup` to the Go protocol type surface.
   - Kept the stable JSON method signature params-absent while emitting
     `GetAccountTokenUsageParams` in the TypeScript client surface.
   - Updated the TypeScript fixture parser to recognize optional `params?`
     entries.

## Verification

- `go test ./features -count=1`
- `go test ./auth -count=1`
- `go test ./appserver -count=1`
- `go vet ./features ./auth ./appserver`
- `git diff --check` on the files in this batch

## Deferred

The remaining upstream commits in `91d6f48992..e766f75989` are covered by
`update/plan_2026_08_13_followup.md` and subsequent dedicated batches; they
include thread-usage backend/TUI behavior, turn-input unification, plugin
metrics, gRPC reconnect, MCP HTTP headers, and skills.read alias/root
resolution.
