# Codex Go Rust 对齐开发 TODO

本文档是后续开发的唯一进度账本。每一轮继续开发前，先阅读并更新本文档；每一轮完成后，也要把完成项、遗留问题、下一步如实写回本文档。

### rust基础版本
  基于codex rust版本进行编码和修改，最终以于Rust 版本codex功能对齐为目标
  目录：D:\qax\reagent\dev\codex-main
  
## 使用规则

- 先更新文档，再继续编码。
- 每次完成代码变更后，在“工作日志”追加记录。
- 每次完成一个 todo 后，把对应 checkbox 改成 `[x]`。
- 遇到阻塞不要删除 todo，在旁边标记 `BLOCKED:` 并记录原因。
- 中途允许为加速定位运行窄范围 `go test -run ...`；全量 `go test ./...` 仍留到模块收口后统一集成测试和修复。
- 中途必须用 `go list -buildvcs=false ./...` 做包加载检查。
- Go 代码里结构体参数和结构体返回值尽量使用指针。
- 同类功能继续归入现有包，不继续制造大量目录。
- 心脏阶段（阶段 1 核心 Turn Runtime、阶段 6 App-Server V2 RPC）要求 100% 高保真对齐 Rust；未达到 Rust 行为、wire shape、状态语义、错误码、持久化/rollout 语义一致前，不得按“基本完成”处理。
- 心脏阶段编码时必须同步补齐直接依赖：model/provider streaming、tool dispatcher、session/rollout、protocol item/notification、config/auth、MCP/plugin/skills 被调用面、sandbox/approval hook 等依赖不能留空壳或静默降级。

## 编码规范

- 参考 Rust 版本逐模块迁移，一个功能模块一个功能模块写 Go 代码和单元测试。
- 单元测试文件可以同步写；遇到 P0/P1 行为边界时允许边写边跑窄范围 `go test -run ...`，等所有模块写完后再统一集成测试和集中修复。
- 对于结构体，尽量用指针传参数、返回结果、保存依赖和组合服务；尤其是 provider、runtime、manager、options、request、response、config、tool、agent、session、thread 这类对象。
- 简单标量、不可变小值、枚举型别名可以继续按 Go 惯例传值；但如果是结构体，默认优先考虑 `*Type`。
- 代码组织按功能归类，相关源码放在同一个包里；不要因为 Rust 有很多 crate 就在 Go 里制造大量细碎目录。
- 包归类参考 Rust 的功能边界，例如 agent 相关操作归 `internal/agent`，tool 相关归 `internal/tool`，turn/runtime 相关归 `internal/turn`，app-server 相关归 `internal/appserver`。
- 如果发现同类功能散在多个目录，优先归并到已有功能包，而不是新增包。
- 优先选择成熟合适的第三方库，不要用标准库手搓一切；例如 TOML/YAML/JSON schema、CLI、协议解析、测试辅助、压缩/归档、终端/PTY、云 SDK/签名等场景，应先评估并采用稳定库。
- Rust 版本如果已经使用第三方库解决某类问题，Go 版本必须优先寻找并采用对应领域的成熟 Go 第三方库；不要因为 Go 标准库能拼出来就重新造轮子。
- 如果已有设计文档或 Rust 参考明确推荐库，优先按推荐库实现；只有在依赖不可用、场景很小或引入成本明显不合理时，才保留轻量自实现，并在计划里记录原因。
- Windows 终端/PTY 使用 `golang.org/x/sys/windows` 的 ConPTY 能力；不要用标准库或裸 syscall 重新拼一套伪终端实现。
- 先把核心代码整体写完整，再统一跑测试、修编译、修行为差异。
- 对阶段 1/6 的任何编码改动都要按“依赖闭环”推进：发现依赖缺失就直接补依赖，不能只写调用点、不能假实现、不能把必需 Rust 行为长期记为 TODO。
- 每次继续开发都基于本文档 todo 推进，并把真实进度写回本文档。
- 不要只做目录合并或结构调整偏离主线；当前主线是继续把 Rust Codex 的后续功能写出来。

## 当前总览

- 当前阶段：策略调整为心脏阶段优先收口。阶段 1 核心 Turn Runtime 与阶段 6 App-Server V2 RPC 必须 100% 对齐 Rust；阶段 9/10/7/8 继续推进，但凡被阶段 1/6 调用到的依赖必须同步补齐。
- 当前估算完成度：全量 Rust Codex CLI 高保真对齐约 70% 左右；若按完整测试通过和所有 Rust 边界一致计算仍保守约 65-70%。
- 最近一次包加载检查：`go list -buildvcs=false ./...` 已通过。
- 最近一次局部编译检查：`GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver` 已通过。
- 最近一次全量测试：`go test ./...` 已通过。
- Rust 参考目录：`D:\qax\reagent\dev\codex-main\codex-rs`。
- Go 工作目录：`D:\qax\reagent\dev\codex_go`。

## 已完成基础

- [x] 建立 Go module 和 `cmd/codex` 入口。
- [x] 建立主要包边界：`config/auth/model/tool/turn/exec/appserver/mcp/agent/session/rollout/sandbox/plugin`。
- [x] 完成基础 CLI/config/auth/feature/exec/review/session 骨架。
- [x] `internal/model` 支持 Responses request/response 基础结构。
- [x] `internal/model` 支持 function/custom/tool_search call item marshal/unmarshal。
- [x] `internal/model` 支持 tool specs 转 Responses tools。
- [x] `internal/tool` 支持 shell/apply_patch/tool_search/MCP/agent 工具注册骨架。
- [x] `internal/tool` 支持 model visible/deferred/hidden tool exposure。
- [x] `internal/tool` 支持 Responses API tool name 映射。
- [x] `internal/turn` 支持 tool registry、tool dispatcher、agent loop、runtime wrapper。
- [x] `internal/exec` 已接入 `turn.Runtime`，能记录 tool call/tool output 到事件和 session。
- [x] `internal/protocol` 增加 tool call/tool output thread item。
- [x] `internal/appserver` 已有 JSON-RPC router、通知、hooks/fs/process/thread/config 基础服务。
- [x] `internal/appserver` 已有 tool hook adapter。
- [x] `internal/mcp` 已有 tool executor、tool runtime、client/resource/OAuth 骨架。
- [x] `internal/agent` 已有 multi-agent tool handlers 骨架。
- [x] `internal/codemode` 已支持 freeform tool kind 和 tool surface 过滤。

## 近期最高优先级

1. [x] `internal/appserver` 接入 `turn.Runtime`，让 `turn/start` 不只是登记状态，而是真正启动异步 turn。
2. [x] HEART 阶段 1/6 100% 对齐审计：已逐项对照 Rust turn/session/app-server protocol，并将“基本可用但未完全一致”的项拆解、编码、补回归源码；最新收口 fork/resume/rollout/thread-list/metadata/rollback 边界。
3. [ ] app-server turn 执行期间发送开始、tool call、tool output、assistant output、完成、错误通知。IN_PROGRESS：已发送 turn started/completed/error、thread status、item completed、assistant delta，并已补 interrupt/cancel、同 thread 防并发和 turn/steer active mailbox；剩余 streaming delta 精细化。
4. [ ] `internal/model` 增加 Responses streaming runner，让 runtime 能消费流式事件。IN_PROGRESS：已支持 SSE parser、streaming request、assistant/tool item 聚合、completed/error、delta handler，并已接入 app-server streaming notifications。
5. [ ] 统一 `internal/protocol` 与 `internal/appserver` 的 assistant/tool/thread item 映射。
6. [ ] 完整化 `internal/session`/`internal/rollout` 的 thread history 和 resume/fork 输入恢复。
7. [ ] 完成 `internal/tool` approval/sandbox/hook 闭环。

## 阶段 1：核心 Turn Runtime

目标：对齐 Rust `core/src/session`、`codex_thread.rs`、`thread_manager.rs` 的基本 turn 执行模型。

心脏阶段门禁：本阶段必须 100% 对齐 Rust。完成标准不再是主流程可运行，而是 turn loop、tool ordering、fatal/non-fatal error、usage/token timing、compact/budget、diff、rollout、session persistence、interrupt/cancel/continue 状态语义全部可与 Rust rollout/protocol 对应。编码时同步补齐 `internal/model`、`internal/tool`、`internal/session`、`internal/rollout`、`internal/protocol` 依赖。

- [x] 建立 `turn.Runtime`，统一 agent runner、tool router、hooks。
- [x] 建立 `AgentLoop`，支持多轮 tool call。
- [x] 建立 `ToolDispatcher`，把 model tool call 转 tool output item。
- [x] `exec` 默认走 `turn.Runtime`。
- [x] tool execution 进入 exec event 和 session item。
- [x] `AgentLoop` 补齐 usage/metadata/token timing。已聚合多轮 usage，记录 turn timing profile，Responses 非流/流式 request id、server model、turn state、models ETag、rate limits、response headers、reasoning included 和 timing_metrics 已写回 `AgentResponse` 并贯通 exec/app-server session items；streaming delta 会通过 request stream handler 记录 turn TTFT，避免等完整 response 返回才近似记录。
- [x] `AgentLoop` 补齐 fatal/non-fatal tool error 与 Rust 行为一致。已区分模型可修正 tool 调用错误、`RespondToModel`、`Fatal`、取消/超时和普通运行时错误。
- [x] `Runtime` 支持 cancellation/interrupt。
- [x] turn runtime 接入 diff tracker。已支持 apply_patch/fileChange 成功输出聚合为 turn-level unified diff，发送 `turn/diff/updated`，并在成功、失败、中断、取消路径清理 active diff tracker。
- [x] turn runtime 接入 compact/token budget。已按 token usage 更新 thread metadata token status，达到阈值时触发自动 compact，手动/自动 compact 共用 compact helper 并追加 rollout compacted marker。
- [x] turn runtime 接入 rollout recorder。exec 新建/恢复与 app-server thread start/turn 完成/user shell/compact/rollback/inject 均会创建或追加 Rust 兼容 rollout JSONL。

验收：

- [x] `codex exec`、`codex review`、app-server turn 共用同一条 runtime 链。
- [x] tool call、tool output、assistant output 顺序与 Rust rollout 可对应。exec/app-server session/rollout 与 exec final JSON events 已按多轮 response 顺序输出，并对同一 response 内 parallel tool calls 采用全部 call 后全部 output 的顺序。
- [x] 失败、中断、继续运行的状态语义与 Rust 一致。失败会发 error/failed completion 并进入 systemError，下一次 turn/start 清理 systemError 并恢复 idle；中断会取消 active runtime、发 interrupted completion 并允许后续 turn。

## 阶段 2：App-Server Turn Runtime

目标：对齐 Rust `app-server/src/request_processors/turn_processor.rs` 和 protocol v2 turn notifications。

- [x] 阅读 Rust `turn_processor.rs`、`thread_state.rs`、`thread_status.rs`、`outgoing_message.rs`。
- [x] 在 `internal/appserver` 增加 turn runtime builder，复用 `turn.Runtime`。
- [x] 给 app-server runtime services 增加可注入 `model.AgentRunner` 或 runtime factory。
- [x] `turn/start` 创建 active turn 后启动 goroutine 执行 runtime。
- [x] turn 执行开始时发送 running/status notification。
- [x] assistant message 输出映射为 app-server item notification。
- [x] tool call 映射为 app-server tool call item notification。
- [x] tool output 映射为 app-server tool output item notification。
- [x] runtime error 映射为 turn error/completed notification。
- [x] 支持 turn cancel/interrupt。
- [x] 支持 thread idle/running 防并发。
- [x] app-server turn 结果写入同一套 session/rollout。

验收：

- [x] app-server `turn/start` 是真实异步执行。
- [x] SDK/IDE 能收到 turn 生命周期通知。
- [x] thread 状态从 running 回到 idle/completed/error。

## 阶段 3：Responses Streaming 与 Provider

目标：对齐 Rust `core/client.rs`、`codex-api`、`model-provider`、`chatgpt`。

- [x] 实现 Responses streaming event parser。
- [x] 支持 output_text delta。
- [x] 支持 function_call 增量。
- [x] 支持 custom_tool_call/freeform 增量。
- [x] 支持 tool_search_call。
- [x] 支持 response completed/error。
- [x] 支持 usage、request id、metadata headers。
- [x] 支持 rate limit/model metadata 事件。
- [x] 支持 retry/timeout 基础。已支持 Responses 非流/流式请求按 provider retry 配置重试 429、5xx、transport error，支持 `Retry-After` 与指数 backoff，支持 streaming idle timeout；更细的流中断后续续连语义待后续补。
- [x] 支持 `store` 和 `previous_response_id` 基础。已在 `AgentRequest`/`AgentLoopRequest`/Responses request 中贯通 `store`、`previous_response_id`、`service_tier`、`prompt_cache_key`、`client_metadata`；session resume 自动提取 previous response id 后续在 session 阶段补。
- [x] 支持 response debug context 基础。已用 `ResponsesAPIError` 保留 status、body/message、request id、cf-ray、authorization error、`x-error-json` code，并让错误字符串包含关键 debug context。
- [x] provider catalog 对齐 Rust bundled/remote models 基础。已支持解析 Rust `models-manager/models.json` 形状、优先加载本地 Rust bundled catalog、HTTP `/models` remote refresh、ETag 触发刷新、`online_if_uncached` 内存刷新策略、provider 默认 remote manager；磁盘 cache 与 auth-aware picker 细分后续补。
- [x] OpenAI provider 接入 config/auth 基础。已接入 API provider/auth headers、provider `auth.command`、401 command auth refresh、ChatGPT auth 远端模型 catalog 权威策略、ChatGPT refresh token 401 刷新并更新本次请求 headers；更完整的 AuthManager/permanent refresh failure 分类后续在阶段 7 深化。
- [x] OSS provider 接入基础。已支持 built-in OSS provider 走 Responses base URL、无需 OpenAI auth、默认静态模型 catalog。
- [x] Ollama provider 接入基础。已支持默认 provider/base URL/default model、Responses 版本检查和静态模型 catalog；真实 pull/运行时 setup 细节后续补。
- [x] LM Studio provider 接入基础。已支持默认 provider/base URL/default model 和静态模型 catalog；真实 download/load 细节后续补。
- [x] Bedrock/API key auth 接入基础。已支持 `auth.json` 中托管 `bedrock_api_key` 与 `AWS_BEARER_TOKEN_BEDROCK` 环境变量注入 `Authorization: Bearer ...`；AWS credential/SigV4 仍在阶段 7 补齐。
- [x] `exec --output-schema` 接入真实 structured output 约束基础。已读取 schema 文件 JSON 并贯通 exec/turn/model，Responses 请求输出 Rust 同形状 `text.format` 的 `json_schema`、`strict: true`、`name: codex_output_schema`。

验收：

- [x] `codex exec` 可真实流式请求模型。
- [x] streaming 事件可同时驱动 exec JSONL 和 app-server notification。
- [ ] provider/model 选择与 Rust CLI 行为一致。

## 阶段 4：Tool Runtime、Hooks、Approval、Sandbox

目标：对齐 Rust `core/src/tools`、`function_tool.rs`、`mcp_tool_call.rs`、`shell.rs`、`apply_patch.rs`。

- [x] shell tool spec 和 executor 基础。
- [x] apply_patch freeform spec 和 executor 基础。
- [x] tool_search executor 基础。
- [x] hook runner adapter 基础。
- [x] shell executor 补齐 cwd/env/timeout/exit status 细节。已支持工具调用参数 `cwd`/`workdir`、`env`、`timeout_ms`，相对 cwd 按会话 cwd 解析，env 覆盖会话环境，runner 输出保留 exit_code/timed_out/duration/stdout/stderr。
- [x] shell executor 补齐 Windows PowerShell/cmd 安全规则。已在 shell request 构造阶段拦截 PowerShell 管道到 `cmd /c` 的破坏性删除/移动、`Start-Process` 缺少 `-WindowStyle Hidden`、递归 `Remove-Item`/`Move-Item` 未使用 `-LiteralPath` 或目标越出 cwd 的情况。
- [x] shell executor 补齐 stdout/stderr 截断与 Rust 输出格式。已把 stdout/stderr 同时存在时的模型输出改为分段文本，保留原始 stdout/stderr 数据，新增 `stdout_truncated`、`stderr_truncated`、`hook_response_truncated` 元数据，并继续使用统一 truncation warning。
- [x] apply_patch executor 补齐 Rust freeform grammar/错误格式。已复用 `internal/applypatch` 的 Lark grammar/parser，新增错误分类/格式化 helper，executor 先 parse 再 apply，空 patch、grammar error、apply error 都以稳定 model response 返回。
- [x] hooks 补齐 before/after/payload rewrite/blocking/fatal。已增强 tool dispatch hook 链：pre/post hook error 分类为 model-visible/fatal，pre 支持 fatal 与 additional context，post 支持 fatal、blocking、feedback 覆盖与 additional context 合并，appserver adapter 映射 stopped/context 到 tool outcome。
- [x] approval policy 接入 tool dispatch。已让 shell executor 在 `require_escalated`/`with_additional_permissions` 且未预批准时返回结构化 approval request，不实际执行命令；`never` 继续拒绝，预批准后正常执行。
- [x] sandbox profile 接入 shell executor。已把 effective `ShellSandboxProfile` 接入 `ShellRequest`，包含 policy tag、network、writable roots 与 additional permissions，并在 approval request data 中返回，后续平台 sandbox runner 可直接消费。
- [x] escalation request 和批准后重试。已在 shell approval request data 中返回 `retry_context.permissions_preapproved=true`，ShellExecutor 可从 invocation context 识别批准后的重试并跳过 approval gate 执行原命令。
- [x] MCP required server 缺失时按 Rust exit 行为返回。已把 `required` 接入 MCP server config/service，required server 未启用、缺失或 failed/stopped 时 tool/resource 调用返回明确错误；skill runtime dependency 生成的 server config 默认 required。
- [x] MCP elicitation/resource read/file conversion 完整化。已补 elicitation schema normalize/validate，resource read response 到 payload content map 转换，OpenAI file rewrite 支持数组对象中的 `path`/`file`/`file_path` 字段。
- [x] hosted/discoverable/deferred tools 语义完整化。已明确 registry 的 model-visible 与 discoverable 分流，tool_search 只索引 discoverable specs，搜索结果转为 model-visible 形态，Responses tools 继续跳过 hidden/discoverable。

验收：

- [ ] Rust exec suite 中 apply_patch/approval_policy/hooks/sandbox/MCP required 可翻译为 Go 测试。
- [ ] tool request/output/error 的 JSON 形状稳定。

## 阶段 5：Session、Thread、Rollout、Compact

目标：对齐 Rust `rollout`、`thread-store`、`state`、`compact`。

- [x] session store 基础。
- [x] exec/review prompt 和 assistant message 可持久化。
- [x] tool call/tool output 可持久化。
- [x] session meta 完整化。已扩展 `session.Metadata`/`MetadataPatch`，保留 response id、cli version、agent 信息、service tier、sandbox/approval、extra 等字段。
- [x] thread item schema 完整化。已扩展 `session.Item`，支持 content/data/raw/call id/name/namespace/response id，并让 app-server thread item builder 透传这些结构化字段。
- [x] rollout JSONL 与 Rust 字段对齐。已扩展 rollout `Line`/`Item`/`SessionMeta`，支持 timestamp/item_id/turn_id/role/response_id/data，并新增 JSONL 到 Responses input items 的重建路径。
- [x] archive/unarchive/delete。session store 已有生命周期操作；已补齐 rollout 文件级 `FindThreadPath`/`Archive`/`Unarchive`/`Delete`，并让 app-server archive/unarchive/fork/delete 同步移动、创建或清理对应 rollout。
- [x] resume last/session id/session name。已有 session id/last；本轮补 `--name` 选择器，支持按 session title/name 恢复、归档、删除、反归档、fork。
- [x] fork snapshot。fork 已复制结构化 history；本轮补 metadata.extra 中的 fork source/mode/item_count/snapshot_at/last_n 记录。
- [x] rollout reconstruction 到 Responses input items。已新增 rollout JSONL lines/items 到 Responses input items 的重建路径。
- [x] compact content items to text。`compact.Item` 已支持 content/data，新增统一 `ItemText` 提取并用于本地摘要。
- [x] compact token budget。新增本地 token 估算与 `TrimHistoryToTokenBudget`，`CompactLocally` 可按 `MaxHistoryTokens` 裁剪历史。
- [x] remote compact。`internal/compact` 已新增远端 compact runner 接口、远端结果过滤/本地兜底；RuntimeRouter 的手动 `thread/compact/start` 与自动 compact 已统一走同一条 compact helper，可注入 `compact.RemoteRunner`，并在支持 remote compact 的 Responses provider/真实 agent 场景下调用 agent 生成摘要；DefaultRuntimeRouter/turn runtime 已可按 config/auth/provider 自动构造 Responses runner，无 auth 且 provider 需要 OpenAI auth 时回退本地 stub；PreCompact/PostCompact hook 已接入 compact helper，PreCompact additionalContext 会作为 developer initial context 注入 compact history；remote compact responseId/model/provider/usage/source/status/trigger/reason/phase 会写入 thread metadata 和 `thread/compacted` 通知。普通 Router 仍保留本地 compact 兼容。
- [x] compact 后继续 turn。`thread/compact/start` 已从空操作改为本地 compact 落盘，写回 summary history 并追加 rollout，为后续 turn 继续使用压缩历史打基础。
- [x] turn timing/memory usage/session prefix。已补 session prefix 和 tool item timing metadata；memory usage 仍待后续结合系统/usage 事件深化。

验收：

- [ ] CLI `resume/fork/archive/delete/unarchive` 与 Rust 行为一致。
- [x] app-server thread list/history/summary/goal 使用同一套存储。summary/goal 已接 session store；history 分页、默认 desc turns、summary/notLoaded/full itemsView 与 initialTurnsPage 基础语义已补。

## 阶段 6：App-Server V2 RPC

目标：对齐 Rust `app-server` 和 `app-server-protocol`。

心脏阶段门禁：本阶段必须 100% 对齐 Rust app-server protocol。完成标准包括 RPC surface、params/result JSON shape、server request、notification、thread item union、错误码、lifecycle、transport、schema fixtures 与 Rust 一致。编码时同步补齐其调用到的 model/turn/session/MCP/plugin/skills/auth/config/process/fs/hook/sandbox 依赖，不允许静默兼容旧字段代替 Rust canonical 字段。

- [x] app-server router 基础。
- [x] protocol/common/thread item 基础。
- [x] fs/config/hooks/process/thread/status 基础服务。
- [x] protocol v2 turn 类型完整化。`Turn`/turn started/completed notification 已按 Rust v2 required nullable shape 输出 `items/itemsView/status/error/startedAt/completedAt/durationMs`，空 items 固定 `[]`，未设置 nullable 字段固定 `null`。
- [x] protocol v2 notification 类型完整化。已补结构化 token usage、context compacted 扩展字段，并让 RuntimeRouter compact start 发 `thread/compacted` 通知。
- [x] protocol v2 item builder/mappers 对齐 Rust。已扩展 app-server `ThreadItem`/payload builder，保留 content/raw/name/namespace/callId/status/responseId/data 等结构化字段；本轮继续补齐 Rust `ThreadItem` union 剩余分支的 wire shape 与 history 读回：`hookPrompt`、`collabAgentToolCall`、`subAgentActivity`、`webSearch`、`imageView`、`sleep`、`imageGeneration`、`enteredReviewMode`、`exitedReviewMode`、`contextCompaction`。
- [x] config manager service 完整化。已补 managed layer 合并、origin/layer 输出和写入后被高优先级层覆盖时的 `okOverridden` metadata。
- [x] thread lifecycle create/resume/fork/archive/delete。已有基础 RPC；本轮补 RuntimeRouter 对 start/archive/unarchive/delete/setName 的 lifecycle notifications。
- [x] thread goal/summary/history。`getConversationSummary` 已接入 session store；thread goal 已持久化到 session metadata.extra，并支持可选 objective、updated/cleared 通知；turn history 已补默认 desc、summary/notLoaded/full itemsView 和 resume initialTurnsPage 分页。
- [ ] account/environment/model/mcp/plugin/hook/process/fs/feedback RPC 补齐。IN_PROGRESS：`process/spawn` 与 `command/exec` 已支持 stdout/stderr outputDelta streaming；`command/exec` 流式请求现在按 Rust 语义延迟到进程退出后返回最终 response，streamed stdout/stderr 不重复进入最终 response；`command/exec/write` 在带完整 thread/turn/item 上下文时会按 Rust `TerminalInteractionNotification` 发送 `item/commandExecution/terminalInteraction`；`thread/shellCommand` 已补 userShell commandExecution 通知闭环，standalone 时发送 turn started/completed，active turn 时挂到当前 turn，完成后会把 `<user_shell_command>` user message 注入 active turn mailbox 或持久化到 store-backed thread history/rollout，commandExecution 不写入 thread history，运行期间会登记到 `thread/backgroundTerminals/list` 并在真实 shell process 启动后回写 `osPid`，完成后移除，terminate/clean 会触发运行中 shell command cancel；`process/spawn` streamed output 也不再重复进入 `process/exited` stdout/stderr，spawn 校验文案已对齐 Rust；`command/exec` 与 `process/*` 控制面错误文案和 JSON-RPC `invalid_request(-32600)`/`invalid_params(-32602)` 分界进一步对齐 Rust；`command/exec`/`process/spawn` 的 `tty` 和 resize 已接真实 PTY backend，Unix 使用 `github.com/creack/pty`，Windows 使用 `golang.org/x/sys/windows` ConPTY；hook state/trust 已应用到 project/user/plugin hooks，`bypass_hook_trust` 已按 Rust 语义保持 trustStatus 但允许 enabled untrusted hooks 执行，`hooks/list` 已支持从 user/project `config.toml` 的 `[hooks]`/`[[hooks.Event]]` 发现 hook，按 Rust 对每个请求 cwd 返回空 entry，并会从 enabled plugin 的 `hooks/hooks.json` 发现 plugin hooks、注入 `PLUGIN_ROOT`/`PLUGIN_DATA` 与 Claude 兼容 env、展开 `${PLUGIN_ROOT}` 等命令占位符；turn/steer 已补 active mailbox，成功 steer 的用户输入会进入下一轮 agent sampling；account login/logout 已发 completed/updated 通知；外部 `chatgptAuthTokens` 登录会持久化 auth、解析账号字段、更新 account state，并按 Rust 行为取消任何 active ChatGPT login；外部 ChatGPT tokens 激活时已禁止 API key/普通 ChatGPT/device-code 直接覆盖，只允许 chatgptAuthTokens 更新或 logout；Responses 401 外部 token refresh 已接入 app-server server request broker；feedback upload 已保留 prepared snapshot，classification 校验和 invalid_request 错误码已补；model/provider capabilities params 已收窄为空 struct、response 收窄为三字段，model service 参数已改指针风格；permission/collaboration list 参数已改指针风格，permission profile response 已去掉内部 sandbox/network 字段，bad cursor 映射 invalid_request，collaboration response required nullable shape 已补；skills service 参数已改指针风格，config/write 参数错误映射 invalid_params；plugin marketplace add/remove/upgrade service 参数已改指针风格，plugin list/read/install/share RPC 参数已改指针风格，参数错误映射 invalid_request；apps/features/misc 体验 RPC 参数已改指针风格，apps list JSON 去掉 legacy apps 字段，app/feature bad cursor 映射 invalid_request；remotecontrol pairing/client 与 connection file watch 参数已改指针风格；config read/write/batch/external agent config 参数已改指针风格；MCP status/oauth/resource 参数已改指针风格；hooks list 与 Windows sandbox setup 参数已改指针风格；account manager get/login/cancel/reset credit 参数已改指针风格；turn/review service 入口参数已改指针风格；model/plugin/mcp 已按 Rust v2 schema 收窄一批响应字段并保留 MCP tool content 未知 JSON 字段；plugin marketplace/share/skill read JSON 已继续去掉旧兼容字段并对齐 Rust fixture；MCP status/tool/resource/OAuth 参数错误与分页 cursor 错误已按 Rust 语义映射 JSON-RPC `invalid_request(-32600)`，且 MCP serverInfo/tool/resourceTemplate/resourceContent JSON 已继续贴近 Rust fixture；environment/add 已限制为 ws/wss exec-server URL，environment/info 已接真实 exec-server WebSocket initialize/initialized/environment/info 查询；fs/watch 已拒绝重复 watchId，FS service 参数已改指针风格，metadata 会尽量填充 createdAtMs；mcp 更深细节仍待补。
- [x] transport stdio。已支持 JSON line stdio、notification/server request 输出、客户端 server request response 分流，以及长耗时请求并发处理；`command/exec` deferred response 不再阻塞后续 `write/terminate` 请求。
- [x] transport Unix socket。已支持 `app-server --listen unix://` 使用默认 control socket，`unix://path` 使用显式 Unix socket；每个连接独立 JSON-line runtime router，Windows 返回明确 unsupported。
- [x] transport WebSocket。已使用 `github.com/coder/websocket` 接入真实 WebSocket JSON-RPC listener，支持 initialize gate、notification/server request 双向消息、health/ready endpoint 和 auth policy。
- [x] auth gate/capability token/signed bearer token。已补 WebSocket auth policy 基础：capability token SHA-256/token file、signed bearer HS256 JWT exp/nbf/iss/aud 校验、非 loopback 未鉴权检测，以及 CLI `--ws-auth` 参数互斥/必填校验；真实 WebSocket listener 仍在 transport WebSocket 项下。
- [ ] schema fixtures 对齐。IN_PROGRESS：schema manifest 已从少量 method 清单扩展为 clientRequests/serverRequests/notifications 三类 RPC surface，覆盖 Go 当前声明的稳定与 experimental wire method，并保留旧 `methods` 字段兼容生成器；method entry 已补 params/result 类型名索引，并按 Rust `app-server-protocol/schema/typescript` 的导出类型名收紧 Apps/Mcp/WindowsSandbox/ExperimentalFeature/ThreadGoal/Chatgpt 等命名，且已处理一批 Rust `params: undefined` 方法；plugin/MCP/notification/thread item 部分 payload 已继续按 Rust fixtures 收窄；本轮按 Rust `schema/typescript` 对齐 `ThreadItem` 剩余 union 分支，隐藏 schema 中 Rust 未声明的旧 `mcpServer/oauth/cancel` 与 `mcpServer/refresh` client RPC，同时把 Rust `ServerNotification.ts` 稳定面已有的 process/realtime/threadSettings/turnModeration notifications 从 Go experimental 面移入 stable；后续继续做 Rust fixture 全量字段 diff。
- [x] MCP status resource wire shape 收口。`MCPServerStatus` 输出已使用 app-server 专用 wire 类型对齐 Rust v2：`resources` 不输出内部 `server`，但保留 Rust `Resource` 允许的 `icons/_meta`；`resourceTemplates` 不输出 `server/icons/_meta`，只保留 Rust `ResourceTemplate` 字段；底层 MCP 工具 payload 仍保留 `server/_meta` 兼容全服务器列表语义。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] MCP OAuth completed notification wire shape 收口。`mcpServer/oauth/loginCompleted` 已按 Rust v2 schema 保持 `threadId` required nullable，同时把 `error` 改为 optional：成功时不再输出 `error:null`，失败时输出具体字符串。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] MCP elicitation `openai/form` union 收口。MCP client request 方法 `openai/form` 现在进入 app-server server request 时保持 Rust v2 `mode:"openai/form"`，不再降级成 `form`；`url` 模式仍按 URL 优先。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] command approval server request Rust 字段名收口。`CommandExecutionRequestApprovalParams` 已按 Rust v2 schema 输出 `proposedExecpolicyAmendment`，不再输出非 Rust 拼写 `proposedExecPolicyAmendment`。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] tool requestUserInput server request wire shape 收口。`ToolRequestUserInputQuestion` 现在按 Rust v2 必填输出 `isOther/isSecret` false，`ToolRequestUserInputOption` 输出收窄为 `label/description`，不再泄露内部兼容 `id` 字段。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] ChatGPT auth refresh response required-nullable 收口。app-server 导出的 `ChatGPTAuthTokensRefreshResponse` 已按 Rust v2 输出 required nullable `chatgptPlanType:null`，不再在 nil 时省略字段；runtime 实际使用的 auth response 类型继续保持同形。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] applyPatchApproval server request map shape 收口。`ApplyPatchApprovalParams.fileChanges` 现在按 Rust parent schema 必填 map 输出，nil 时固定 `{}` 而不是 `null`。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] thread settings stable notification wire shape 收口。`Settings.MarshalJSON` 已按 Rust v2 `ThreadSettings` 输出 required nullable 字段，`activePermissionProfile` 从内部 string 转 `{id,extends:null}`，`sandboxPolicy` 从常见 string 转 Rust tagged object，`collaborationMode` 补 default/settings，且不再输出内部 `multiAgentMode`。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] thread goal required-nullable wire shape 收口。`Goal.MarshalJSON` 已按 Rust v2 `ThreadGoal` 固定输出 `tokenBudget`，nil 时为 `null`，避免 goal/read、goal/update notification 和持久化读回时省略必填 nullable 字段。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] terminal interaction notification wire shape 收口。`TerminalInteractionNotification` 已按 Rust v2 只输出 `threadId/turnId/itemId/processId/stdin`，内部兼容 `data` 不再出现在 stable notification payload。同步补单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。

验收：

- [ ] Python/TypeScript SDK 所需 RPC 方法可用。
- [ ] app-server notification 与 Rust schema fixtures 对齐。

## 阶段 7：配置、认证、账号

目标：对齐 Rust `config`、`core/src/config`、`login`、`keyring-store`、`aws-auth`。

- [x] `-c key=value` override 基础。
- [x] feature list/enable/disable 基础。
- [x] auth.json 和环境变量认证基础。
- [x] 外部 ChatGPT auth tokens 基础。已支持 `chatgptAuthTokens` 精确 auth mode、JWT claims 解析、account/read 账号状态、auth.json 持久化、logout 删除、默认 app-server 启动恢复账号状态，以及 OpenAI provider/backend auth mode 区分。
- [x] app-server auth status/account read 基础。`getAuthStatus` 现在读取 auth store/env、遵守 `includeToken`、根据 active provider 返回 `requiresOpenaiAuth`，并保留测试快照 fallback；`CODEX_ACCESS_TOKEN`/personal access token 已按 Rust 行为不泄露 token，`account/read` 会通过 AuthAPI `whoami` 水合 ChatGPT account，支持 `email:null` 和 plan type；Amazon Bedrock provider 会返回 `amazonBedrock` account 并区分 `awsManaged/codexManaged`；JSON 输出已收窄到 Rust v1 schema 的 `authMethod/authToken/requiresOpenaiAuth`。
- [x] refresh token 永久失败分类基础。已解析 `refresh_token_expired`/`refresh_token_reused`/`refresh_token_invalidated`，记录 scoped in-memory permanent failure，避免同一坏 refresh token 反复刷新，并让 `getAuthStatus`/`account/read` 在永久失败后隐藏 token/account。
- [ ] TOML loader 完整化。
- [ ] profile v2。
- [ ] project `.codex` config。IN_PROGRESS：已支持按 CWD 向上读取多个 `.codex/config.toml` 项目层，父级低优先级、最近 CWD 高优先级，并接入 exec/doctor 配置加载；已过滤 project-local 不支持的敏感键（provider/base URL/profile/profiles/model_providers/otel/notify/respect_system_proxy 等）；已按用户层 `[projects."<path>"].trust_level = "trusted"` 控制项目层是否生效；`model_instructions_file` 会按对应 `.codex` 目录解析相对路径；`instructions`/`model_instructions_file` 已贯通到 exec/turn/model 请求和 Responses `instructions` body；`config/read` 带 cwd 时会暴露 project layer/origin；app-server turn start 会从 thread cwd 的 effective config 补默认 model/provider/instructions；hooks discovery 有 ConfigService 时只加载 trusted project layer 的 hooks；普通 repo/root marker 场景已按 active project root 收紧 trust target；linked Git worktree 已按 root checkout 作为 trust target，config 仍读 worktree-local `.codex/config.toml`，hooks folder 映射到 root checkout 对应 `.codex`，且无 config.toml 的 trusted `.codex/hooks.json` 也会被发现。
- [ ] managed config/layer/origin metadata。
- [ ] strict unknown key path。
- [ ] config edit 保留 TOML 格式。
- [ ] ChatGPT OAuth/device code。IN_PROGRESS：已对齐 Rust 的 authorize URL `allowed_workspace_id`、token exchange 后 forced workspace 校验、ID token `https://api.openai.com/auth` 嵌套 claims 解析；CLI 登录和 app-server 外部 auth tokens 已接入 `forced_chatgpt_workspace_id` 配置；`forced_login_method` 已接入 app-server login RPC，按 Rust 文案禁止 API/ChatGPT/外部 ChatGPT 不匹配登录；app-server 读取现有 auth 时也会执行 forced login/workspace 限制，存储凭据不合规时清除后视为未登录，环境变量不合规则返回错误；浏览器 login server 已补 `/success`、错误页、`/cancel`、端口占用时取消旧 server 并重试/回退端口。剩余 route config/proxy 与更完整端到端覆盖。
- [ ] access token refresh。IN_PROGRESS：托管 ChatGPT refresh token 已有 OAuth refresh、refresh URL override、backend error code 永久失败分类和 app-server 可见状态处理；外部 `chatgptAuthTokens` 401 refresh 已有 model 回调和 app-server JSON-RPC server request broker，后续继续补超时/取消细节和端到端覆盖。
- [ ] keyring 平台抽象。IN_PROGRESS：已对齐 Rust `AuthCredentialsStoreMode` 的 file/keyring/auto/ephemeral 基础语义，CLI/app-server/exec/auth refresh 关键路径会按 `cli_auth_credentials_store` 和 `secret_auth_storage` 选择 store；Go 先使用 auth 包内 keyring 抽象和进程内后端，后续再接 OS keychain/secrets 加密文件。
- [ ] AWS Bedrock credential/sigv4。IN_PROGRESS：已对照 Rust `model-provider/src/amazon_bedrock`，Go 已有 AWS env/shared credentials、SigV4 signer、snake_case header strip；已补 Bedrock Mantle supported region 白名单、unsupported region 显式错误、doctor reachability 不再回退默认 endpoint、account/read AWS-managed/codex-managed credentialSource，并补 Bedrock expired signature 401 的可操作错误提示；本轮已让 Bedrock SigV4 清理旧 `Content-Encoding`/`Content-Length` 后按最终 body/header 签名，并同步最终 `Host`；AWS credential chain 新增 shared config/credentials 的 `credential_process` 支持，修正 INI 值裁剪避免破坏带空格命令；provider/model 细节补齐 Rust Azure Responses endpoint markers；补充 auth/model 单元测试代码；下一步继续补 AWS SDK credential chain 仍未覆盖的 role/web identity/IMDS 边界或转入 app-server RPC 细节。

验收：

- [ ] CLI 和 app-server 读取到同一份 config snapshot。
- [ ] `login/status/logout`、`features`、`debug models` 与 Rust 行为一致。

## 阶段 8：CLI 命令树与非交互行为

目标：对齐 Rust `cli/src/main.rs` 和 `exec/src/cli.rs`。

- [x] exec/review/login/logout/features/debug prompt-input 基础。
- [ ] interactive root。IN_PROGRESS：已按 Rust `run_interactive_tui` 对齐 prompt CRLF/CR 归一化、remote endpoint 解析、`--remote-auth-token-env` 运行期校验与 token trim/missing/empty 错误、`TERM=dumb` fatal/confirm 退出语义；剩余远端 TUI app-server client 主循环和 local state DB recovery 真实接入。
  - [x] root prompt CRLF/CR 归一化为 LF。
  - [x] `--remote` 支持 Rust 同形 `ws://host:port`、`wss://host:port`、`unix://`、`unix://PATH` 解析与非法地址报错。
  - [x] `--remote-auth-token-env` 改为 interactive 运行期校验，支持 env trim、missing/empty 报错，并限制为 `wss://` 或 loopback `ws://`。
  - [ ] remote TUI app-server client 主循环：WebSocket/Unix socket 连接、initialize gate、远端 session/archive/resume/fork 命令复用。
  - [x] `TERM=dumb` 无 TTY fatal/有 TTY confirm 行为。
  - [ ] local state DB locked/auto-backup/recovery guidance。IN_PROGRESS：已补 Rust 同款 locked/diagnostic/auto-backup/fresh-start confirmation 文案 helper 与测试；剩余真实 TUI/app-server startup error 接入、自动 backup 调用和重试启动闭环。
    - [x] Rust `state_db_recovery.rs` guidance 文案 helper 已补：locked guidance、diagnostic guidance、auto-backup start、fresh-start rebuild confirmation 和 TTY/non-TTY Enter 行为。
    - [ ] 真实 interactive TUI/app-server startup 中捕获 local state DB startup error，按 locked/corrupt/blocking sqlite home 分支执行 guidance、backup 与重试。
- [x] mcp/plugin/mcp-server。已按 Rust `mcp_cmd.rs`/`plugin_cmd.rs`/`marketplace_cmd.rs` 补 `mcp add` transport parser 的 stdio/HTTP 参数互斥和 requires 语义、`mcp-server --strict-config` config loader/runtime snapshot、MCP CLI list/get 输出、TOML 读写保留、`mcp add/login` OAuth flow，以及 plugin CLI 真实 marketplace/install/remove/upgrade manager。
  - [x] `mcp add` 拒绝 stdio command 搭配 HTTP-only `--bearer-token-env-var`/`--oauth-client-id`/`--oauth-resource`，并拒绝 `--url` 搭配 stdio-only `--env`。
  - [x] `plugin add/remove` selector 按 Rust `PluginId` 校验 plugin/marketplace segment，只允许 ASCII 字母数字、`_`、`-`。
  - [x] `mcp add/list/get/remove/login/logout` 输出、config TOML edit、OAuth 自动登录与 Rust 完整一致。
    - [x] `mcp list/get` human 表格/详情拆分 stdio 与 streamable HTTP，按 Rust 脱敏 env/http_headers、显示 auth/status/cwd/timeouts/tool filters，并让 disabled get 输出单行。
    - [x] `mcp list/get --json` 输出 Rust transport shape：stdio `env/env_vars/cwd`、HTTP `bearer_token_env_var/http_headers/env_http_headers`、list timeouts 与 auth_status。
    - [x] MCP CLI TOML 读写保留 Rust 字段：`env_vars` 字符串/对象项、`cwd`、HTTP headers、nested `oauth.client_id`、`oauth_resource`、timeouts、tool filters、scopes、required、parallel tool calls 与 per-tool config；写回默认值按 Rust 省略。
    - [x] `mcp login` 无显式 `--scopes` 时使用配置 `scopes`，runtime config 同时兼容旧顶层 `oauth_client_id` 与 Rust nested `oauth.client_id`。
    - [x] `mcp add` 检测 streamable HTTP OAuth support 后自动启动 OAuth flow，并按 Rust 支持 discovered scopes / retry-without-scopes / 成功文案。
  - [x] `plugin add/list/remove/marketplace` 接入真实 marketplace snapshot/install/remove/upgrade manager，不再只写 Go 侧 `plugins.json` 兼容状态。
  - [x] `mcp-server --strict-config` 贯通 config loader，并对 MCP server runtime 使用同一份 strict config snapshot。
- [ ] app-server/remote-control。IN_PROGRESS：已按 Rust CLI/daemon startup 与 remote-control websocket transport 顺序补齐到 `--listen off` remote-control-only 基础 loop，并补外部 auth recovery bridge 与 websocket auth revision watch；剩余 UnauthorizedRecovery step/log 细节继续顺序扫。
  - [x] `app-server --remote-control`、`app-server daemon bootstrap --remote-control`、`enable-remote-control`、`disable-remote-control` CLI 解析与 PID 启动参数已对齐 Rust 基础形状。
  - [x] PID backend remote-control disabled env marker 使用 Rust 同款 `CODEX_INTERNAL_APP_SERVER_REMOTE_CONTROL_DISABLED`，enabled 启动不注入 disabled env。
  - [x] `remote-control start` 改走 Rust 同形 daemon startup 分支：未 bootstrap 返回 daemon `bootstrapped` 输出，已 bootstrap 返回 daemon `started/alreadyRunning` lifecycle 输出。
  - [x] `app-server --remote-control` 贯通到 app-server runtime startup mode，stdio/unix router 初始化时按 Rust `EnabledEphemeral` 进入 remote-control enabled 状态，并实现 daemon disabled env marker 读取/清除与 flag 覆盖。
  - [x] `app-server --listen` parser 对齐 Rust transport parser：`unix://PATH` 接受相对路径，unsupported URL 与 invalid websocket URL 文案拆分为 Rust 同款。
  - [x] app-server runtime 读取 root config/feature toggles 与 `requirements.toml`/`requirements.allow_remote_control`，显式 `--remote-control` 在 managed requirements 禁用时返回 `remote control is disabled by managed requirements`；requirements 状态会进入 stdio/unix/ws router、`configRequirements/read` 和 remote-control RPC gate。
  - [x] `--listen off` 无本地 transport 的错误分支按 Rust 区分：requirements 禁用时报 managed requirements，显式 remote-control 但无 sqlite state DB 时报 sqlite state DB unavailable，普通未配置保持 use --listen/enable remote control。
  - [x] daemon `enable-remote-control`/`disable-remote-control` 基础 lifecycle 对齐：未运行时输出 `backend:null`，运行中 PID backend 会持久化设置并刷新 running PID 状态。
  - [x] `remote-control` 前台模式改为真实临时 Unix socket app-server 生命周期、Ctrl-C/context 停止、ready timeout/retry 与 abort 等待语义；非 Unix 平台按 Rust daemon 边界返回 unsupported，不再用内存 manager 假成功。
  - [x] daemon `enable-remote-control`/`disable-remote-control` 对 alreadyEnabled/alreadyDisabled 的运行中 managed app-server 发送 socket RPC enable/disable，并补 unmanaged app-server probe 错误。
  - [x] daemon bootstrap/start/restart 接入真实 managed standalone、updater loop、readiness probe 与 unmanaged app-server probe 错误。
  - [x] daemon operation lock、PID reservation flock、stop grace/force kill 与 stale PID 刷新已按 Rust backend 顺序接入。
  - [x] daemon Unix 实进程集成测试源码已补：覆盖 flock busy、setsid 启动、SIGTERM/SIGKILL 停止和 stale PID 清理。
  - [ ] daemon Unix 实进程集成测试仍需在 Unix 环境实际执行。
  - [x] daemon readiness failure stderr tail context 已按 Rust `app_server_not_ready_context`/`read_stderr_log_tail` 补源码回归，覆盖 recent complete lines、missing/empty log、context append 与 `waitUntilReady` 失败路径。
  - [x] `--listen off` + persisted remote-control preference=true 已读取 Rust `state_5.sqlite` 的 `remote_control_enrollments.remote_control_enabled=1`，并按 Rust `websocket_url + account_id + app_server_client_name("")` 精确匹配后进入 remote-control-only context 保活；显式 `--remote-control` 在 state DB 可用时不再报 sqlite unavailable。
  - [x] remote-control status notification core lifecycle 已按 Rust `publish_status` 语义补齐：enable/disable 仅状态变化时广播 `remoteControl/status/changed`，重复 enable/disable 不重复通知，disable 清空 `environmentId`，并覆盖 opt-out 过滤。
  - [x] app-server transport initialize 后的 remote-control status snapshot 已按 Rust connection-scoped 行为补齐：stdio/json-line 与 websocket 在 initialize response 后向当前连接发送 `remoteControl/status/changed` 当前状态，并尊重 opt-out。
  - [x] remote-control protocol/enrollment/server API 基础已按 Rust `protocol.rs`/`auth.rs`/`enroll.rs`/`server_api.rs` 顺序补齐：URL 规范化、backend snake_case 请求/响应、ChatGPT account header 覆盖、server token refresh requirement、pair/pair-status backend 请求、enroll/refresh HTTP、Retry-After deferral、transient error 分类和敏感响应体预览脱敏均有源码回归。
  - [x] remote-control persisted enrollment SQLite store 已按 Rust `state/src/runtime/remote_control.rs` 补齐：`state_5.sqlite` 路径、`remote_control_enrollments` schema/旧表补列、`None` client name 空字符串 key、get/upsert/set-enabled/delete、upsert 冲突不覆盖 `remote_control_enabled`、以及 load/update persisted enrollment helper 均有源码回归。
  - [x] remote-control durable enable/disable 已接入 app-server manager：非 ephemeral `remoteControl/enable` 会复用/创建 enrollment、写 `remote_control_enabled=1`、返回 Rust backend loop 前的 `connecting + environmentId`，非 ephemeral disable 写 `remote_control_enabled=0`，ephemeral daemon probe 保持 runtime-only 不改 DB，router/server 退出会释放 SQLite store。
  - [x] remote-control pairing/status 的 refresh/re-enroll 路径已接入 app-server manager：pairing start/status 会复用 current/persisted enrollment，缺 token 或 proactive/required refresh 时先调用 server refresh，refresh 404 时替换 enrollment，pair 401/403 清 token 后 refresh 重试，router pairing RPC 也改为 Context 版本传播 backend 错误。
  - [x] remote-control auth recovery 基础已接入 app-server manager：enroll/refresh 遇到 401/403 会调用 recovery hook，CLI app-server 默认复用 `auth.RefreshChatGPTTokens` 从 codexHome 刷新 ChatGPT tokens，同 account recovery 后重试，account 改变会清 token 并返回 pairing unavailable。
  - [x] remote-control 外部 auth server-request recovery 已接入 app-server backend：当当前 `auth.json` 为 `chatgptAuthTokens` 时，enroll/refresh 401/403 会优先通过 `account/chatgptAuthTokens/refresh` server request 获取新 token、更新 account/auth store 并用新 Authorization 重试；普通 ChatGPT 仍走本地 `auth.RefreshChatGPTTokens`，并给外部 recovery 加超时保护。
  - [x] remote-control websocket auth revision watch 已接入 loop：backoff 等待中 auth revision 变化会唤醒重连并重置 reconnect attempt；recovery 自身单次 revision 可标记已消费，两个以上竞态 revision 会保留给外层 reconnect loop；app-server login/logout/external refresh/managed refresh 会 bump revision，`--listen off` remote-control-only 自动注入 router revision。
  - [x] remote-control `UnauthorizedRecovery` state machine 已按 Rust 建模：managed 从 `reload` 进入 `refresh_token` 再到 `done`，external 从 `external_refresh` 到 `done`，并暴露 `mode_name`/`step_name`/`unavailable_reason`/`auth_state_changed`，覆盖 reload changed/no-change、account mismatch、external refresh success/error 回归；app-server 默认 recovery 构造已切到该 state machine。
  - [x] remote-control `UnauthorizedRecovery` controller/reset 生命周期已接入 websocket loop：successful websocket connect 与 auth-change wake 会 reset recovery state；controller observer 事件携带 `mode/step/auth_state_changed/unavailable_reason/error`，覆盖 reset 后重新从 `reload` 开始与 auth-change wake reset 回归。
  - [x] remote-control recovery revision mark 已收窄到 Rust `auth_state_changed=true` 语义：只有 recovery step 明确报告 auth state changed 时才消费单次 auth revision；connect 期间普通外部 auth revision 会唤醒 reconnect，recovery 自身 revision 不唤醒，竞态后续 revision 仍唤醒。
  - [x] `--listen off` remote-control-only `UnauthorizedRecovery` tracing 已接入实际结构化日志：默认 app-server recovery controller observer 会用 `slog` 输出成功 `mode/step/unavailable_reason/auth_state_changed` 与失败 `mode/step/unavailable_reason/error` 字段，自定义 recovery hook 保持不干预。
  - [x] remote-control websocket wire protocol 基础已按 Rust `protocol.rs` 补齐：`ClientId`/`StreamId`、client/server envelope、client message/chunk/ack/ping/closed、server message/chunk/ack/pong、snake_case tagged JSON、chunk `segment_id=0` 保留与 server chunk segment helper 均有源码回归。
  - [x] remote-control websocket seq/ack buffer 基础已按 Rust `WebsocketState`/`OutboundBuffer` 行为补齐：server envelope per `(client_id, stream_id)` 连续 seq id、outbound buffer 按 stream 保留、ack 按 `(seq_id, segment_id)` cursor 裁剪，chunk ack 不误删同 seq 非 chunk envelope。
  - [x] remote-control clients list/revoke backend API 已按 Rust `clients.rs` 补齐：`remoteControl/client/list|revoke` 在 backend 模式下调用 `/wham/remote/control/environments/{environment_id}/clients` 的 GET/DELETE，带 ChatGPT account header、cursor/limit/order query、last_seen_at Rfc3339 转 Unix 秒、401 recovery 重试一次，以及 limit 1..100 校验。
  - [x] remote-control client tracker 基础已按 Rust `client_tracker.rs` 补齐：initialize 打开 per `(client_id, stream_id)` connection、legacy 无 stream_id fallback、同 stream initialize 先 close old connection、inbound seq 去重只在成功投递后推进、ping active/unknown pong、client_closed/idle close 与 outbound writer 转 server_message envelope 均有源码回归。
  - [x] remote-control segment split/reassembly 已按 Rust `segment.rs` 补齐：client_message_chunk base64 顺序重组、stream/client invalidate、stale/duplicate chunk 不破坏新 assembly、非法/乱序/超限 chunk 丢弃，以及大 server_message 按 100KiB target/150KiB max 拆成 server_message_chunk。
  - [x] remote-control websocket state 的 client observation/delivery 已按 Rust `WebsocketState` 补齐：observe 阶段串起 segment reassembler、oversized wire drop、已完成 seq drop、ack 前置 ignore；delivery 阶段更新 subscribe cursor、记录完成 seq、按 ack 裁剪 outbound buffer，并支持 stream/client invalidate。
  - [x] remote-control websocket connect request 基础已按 Rust `websocket.rs` 补齐：握手 request 设置 `x-codex-server-id`、base64 `x-codex-name`、`x-codex-protocol-version=3`、Bearer server token、installation id、subscribe cursor；补 missing remote app-server 404 detail 识别、connect error body/header 预览和 reconnect backoff cap/reset。
  - [x] remote-control websocket enrollment prepare 已按 Rust `prepare_remote_control_enrollment` 补齐基础路径：state DB 中只持久化 server/environment/name、无 server token 时会 refresh；refresh 404 会重新 enroll 并保留原 enabled preference；account 变化会清 in-memory enrollment/environment。
  - [x] remote-control websocket connect client 基础已按 Rust `connect_remote_control_websocket` 补齐：真实 `coder/websocket` dial 使用准备好的 enrollment/header，HTTP 401/403 会清匹配 enrollment 的 server token，明确 missing remote app-server 404 会替换 stale enrollment 并持久化 enabled preference，错误信息继续带 header/body 预览。
  - [x] remote-control websocket connection worker 基础已按 Rust `run_server_writer_inner`/`run_websocket_reader_inner` 补齐：writer 重放 outbound buffer、给 server envelope per-stream 分配 seq、split/chunk 后写 websocket 并记录 buffer；reader 读取 client envelope、走 segment reassembly、投递 client tracker、处理 ack cursor 和 closed/idle invalidation。Go `coder/websocket` 使用 `Conn.Ping` 处理 ping/pong。
  - [x] remote-control websocket loop 基础已按 Rust `RemoteControlWebsocket::run/connect/run_connection` 接入：复用 `WebsocketState`/`ClientTracker` 跨 reconnect，按 status enabled/disabled 控制连接 cycle，connect 失败进入 errored+backoff，连接 worker 停止后重连，并保留 subscribe cursor/outbound buffer 重放。
  - [x] app-server remote-control transport bridge 已接入 Rust `TransportEvent` 同形行为：remote client opened/incoming/closed 转 RuntimeRouter connection，initialize 后回发 status snapshot，router response/notification/server-request 写回 remote client writer，并支持 targeted server request response 解析。
  - [x] `--listen off` remote-control-only 已从占位保活改为真实 websocket backend 连接循环：命中 persisted enabled 或显式 `--remote-control` 且 state DB 可用时启动 `ServeRemoteControlOnly`，无本地 listen 也会通过 remote-control websocket/transport bridge 长期运行并随 context 停止。
  - [x] websocket/pairing 侧 proactive refresh throttle 已按 Rust 回归补齐：proactive refresh transient 失败继续用旧有效 token 连接 websocket，required refresh 失败写回 `NextRefreshAt` 并阻止 websocket connect，websocket refresh 的 `Retry-After` 会节流后续 pairing refresh。
- [ ] completion/update/doctor/sandbox。IN_PROGRESS：doctor 已补 provider reachability plan/probe、credentials store mode 诊断、network_proxy 诊断、custom CA env (`CODEX_CA_CERTIFICATE`/`SSL_CERT_FILE`) 可读性诊断、overall fail 时 CLI exit 1 且不重复打印错误，并继续补齐 human renderer Notes/idle summary；completion/update/sandbox 仍继续顺序扫。
  - [x] `update` 命令树收紧为 Rust 同形裸子命令，不再接受 Go 侧额外 `--json`。
  - [x] `completion` 输出改为覆盖 Rust clap completion surface，而不是只补顶层静态命令。
  - [x] `doctor` human output 分组顺序对齐 Rust：Environment/Configuration/Updates/Connectivity/Background Server，并把 mcp/sandbox、updates、app-server 归入 Rust 对应分组。
  - [ ] `doctor` human/JSON output、check id/category/details 与 Rust doctor fixtures 对齐。
  - [x] `doctor` 默认 report check 集合与顺序按 Rust `build_report` 成功路径收敛为 18 项：移除默认输出中的 Go-only `config.features`/`state.threads`，按 `updates/network/websocket/mcp/sandbox/.../state/threads/app-server/reachability` 顺序输出。
  - [x] `doctor` `config.load` details 收紧为 Rust 标签：`CODEX_HOME`/`cwd`/`model`/`model provider`/`log dir`/`sqlite home`/`mcp servers`/feature flags/`config.toml` parse，不再输出 Go-only `config path`、`top-level keys`、CLI override 计数。
  - [x] `doctor` `auth.credentials` status/detail 基础对齐 Rust：输出 `auth storage mode`/`auth file`/聚合 env vars/stored auth mode/API key/ChatGPT tokens/agent identity，无凭据为 fail，provider env_key 缺失/存在走 provider-specific auth summary/remediation。
  - [x] `doctor` `auth.credentials` stored auth incomplete issue 对齐 Rust `stored_auth_issues`：API key/ChatGPT/external ChatGPT/agent identity/PAT/Bedrock mode 均输出 `stored auth issue: ...`，无 env 时 fail，有 env 兜底时 warning，summary/remediation 按 Rust 分级。
  - [x] `doctor` `updates.status` detail 去除 Go-only `update command`，保持 Rust `update action`、version cache、latest version/status/probe shape。
  - [x] `doctor` `updates.status` npm-managed 安装已对齐 Rust `npm_global_root_check`：输出 npm update target、running/npm package root mismatch details、missing package root/npm unavailable warning 与 remediation，并忽略 cargo target/debug|release 继承的 managed env。
  - [x] `doctor` `runtime.provenance`/installation npm-managed proof 已同步 Rust installation check：installation 输出 current executable/install context/managed env/package root/PATH codex entries，并复用 `npm_global_root_check` target/mismatch/missing/unavailable 分级。
  - [x] `doctor` `network.env` details 对齐 Rust：proxy env vars 聚合为 `proxy env vars present: ...` 单行，summary 使用 `network-related environment looks readable`，移除 Rust doctor 没有的 `[network_proxy]` 配置诊断 details。
  - [x] `doctor` `network.websocket_reachability` handshake details 对齐 Rust 第一层：发送 `OpenAI-Beta: responses_websockets=2026-02-06`，成功输出 `handshake result: HTTP ...`、reasoning/models etag/server model presence，失败输出 Rust fallback summary 与 `handshake API/transport error`。
  - [x] `doctor` `network.websocket_reachability` 已补 Rust DNS address family details 与 immediate-close grace/code/reason 检测：endpoint 后输出 `DNS: ...`，handshake 后等待 250ms 捕获立即 close 并降级 warning。
  - [x] `doctor` `mcp.config` status/detail 对齐 Rust 主路径：transport counts 只输出已配置类型，`disabled_reason` 参与 disabled 计数和跳过诊断；streamable HTTP 检查 bearer/header env vars 并执行 HEAD/GET reachability probe，required/optional 分别输出 `required/optional reachability failed`；stdio 检查 server `cwd`、`env_vars` remote/local source、server `env.PATH` command resolution，且 `environment_id = "local"` 按本地处理。
  - [x] `doctor` `mcp.config` remote stdio cwd URI 判定已贴齐 Rust `to_inferred_path_uri`：接受 POSIX/Windows/UNC 原生绝对路径拼写，拒绝相对路径与 `file://` URI；同时应用 `requirements.mcp_servers` allowlist，未命中 server 写入 `disabled_reason` 并在 doctor 中计 disabled/跳过诊断。
  - [x] `doctor` `sandbox.helpers` details/status 对齐 Rust 主路径：`approval policy` 输出 Debug 风格 `Never/OnRequest/UnlessTrusted`，`network sandbox` 输出 `enabled/restricted`，移除 Go-only workspace writable roots details，helper 缺失 detail 使用 `none`，且 execve wrapper 缺失不再让 check warning。
  - [x] `doctor` `sandbox.helpers` helper path 来源已改为从 Rust 同形 `Arg0DispatchPaths`/dispatch alias 传入：入口保留 alias guard paths，doctor 不再用 PATH/LookPath 推断 helper，Linux helper 无 alias 时按 Rust fallback 到当前 exe。
  - [x] `doctor` `terminal.env` detail/issue 主体对齐 Rust：补 `stdin/stdout/stderr is terminal`、`terminal size`、dimension env、`color output: enabled/disabled (reason)`、Rust remote env presence 列表、`LINES` narrow row warning，`TERMINFO` readiness 改为 `dir/file/missing/...` 语义。
  - [x] `doctor` `terminal.env` 已补 Rust 同形真实 terminal size 检测、supports-color 探测、tmux diagnostic details 与 Windows console code page/mode details：真实 check 采集 TTY size/color support/tmux options/Windows codepage 和 console mode，单测路径使用可注入 inputs 固定 shape。
  - [x] `doctor` `git.environment` 已按 Rust `doctor/git.rs` 顺序复核：selected git、PATH candidates、version/exec path/build options、repo root/`.git` entry、branch normalization、core.fsmonitor、Windows old Git warnings 的 detail/status/issue 已同形，无需本轮改代码。
  - [x] `doctor` `terminal.title` 已按 Rust `doctor/title.rs` 顺序复核：source/items/activity、invalid item de-dup/quote、project-name/current-dir aliases、summary/status/issue/remedy 与 project title truncation 主体已同形。
  - [x] `doctor` `terminal.title` 真实 check 已从 config layer stack 识别 project config root：非 git 场景通过 `ConfigService.Read(IncludeLayers)` 读取 trusted project layer 的 `.codex` folder，即使没有 `config.toml` 也按 Rust fallback 到 `project config` root，未 trusted 时回到 cwd。
  - [x] `doctor` `state.paths` 对齐 Rust 主体：输出 `CODEX_HOME`/`log dir`/`sqlite home` path readiness、`state/log/goals/memories DB` path readiness 与 SQLite `PRAGMA integrity_check`/missing skipped details，补 active/archived rollout stats 和 standalone release cache detail，summary/remediation 按 integrity fail 分级。
  - [x] `doctor` `state.rollout_db_parity` 对齐 Rust 主体：改为读取 `state_5.sqlite` 的 `threads` audit 行，输出 default provider、rollout scan、missing state DB/read error、active/archived/missing/stale/archive mismatch/duplicate DB paths、model provider/source count summary，scan cap 时 stale/archive mismatch 显示 skipped，并按 Rust issue/remediation 文案分级。
  - [x] `doctor` `state.rollout_db_parity` path key 已贴齐 Rust `normalize_for_path_comparison` 主语义：仅 canonicalize/EvalSymlinks 成功时使用绝对解析路径并应用 WSL normalize，失败时保留原始 path；Windows 不再无条件 lowercase，prefix 判断改为 path-aware。
  - [x] `doctor` `app_server.status` 对齐 Rust `doctor/background.rs` 主体：保留 daemon state/settings/pid/update-loop/control socket/mode details，socket 存在时通过 bounded initialize probe 获取 app-server version，probe 失败输出 `app-server version: unavailable (...)` 并截断/脱敏 socket path，summary/status/remediation 按 Rust `SocketStatus` 分级。
  - [x] `doctor` `network.provider_reachability` 按 Rust 顺序复核并补齐 query 细节：active provider/auth mode、ChatGPT/API key/provider auth endpoint plan、Bedrock/Azure route probe skip、base URL HEAD probe、`/models` route GET probe、404 fail/401-403 OK/warning 分级和 issue/remediation 已同形；`provider_url_for_path` 改为按 Rust 原样拼接 query params，不再 URL encode。
  - [x] `doctor` JSON support report 固定字段与 redaction 对齐 Rust：`remediation`/issue nullable 字段不再省略，空 `fields` 保持数组，JSON 与 human 输出共用 detail/issue/remedy 脱敏。
  - [x] `doctor` check id/report 前缀顺序补齐 Rust 第一段：安装检查 JSON key 使用 Rust `installation`，report 前四项按 `system.environment`、`installation`、`runtime.provenance`、`runtime.search` 构建。
  - [x] `doctor` human/JSON fixture shape 继续收紧：JSON check 的空 `issues` 固定输出数组，human summary 不再多一个空行，未知 category 进入 human `Other` 兜底分组，并用全量 check id/category 顺序回归替代只测前缀。
  - [x] `doctor` human footer/overall label 对齐 Rust renderer：warning overall 显示 `degraded`，详细输出 footer 同时提示 `--summary`/`--all`/`--json`，summary 输出 footer 增加 `--all`/`--json` 提示。
  - [x] `doctor` human Notes/idle summary 对齐 Rust renderer：有 update/rollout/sandbox/non-ok/mixed-auth 时先输出 `Notes` 区块，summary 计入 notes，app-server `status: not running` 以 idle `[--]` 展示并从 ok 计数中移出。
  - [x] `doctor` human row description 对齐 Rust renderer：system/runtime/install/search/git/terminal/title/state/config/mcp/sandbox/network/websocket/app-server 会从 details 提炼 Rust 同形短摘要，非 ok issue/remediation 仍优先展示可操作原因。
  - [x] `doctor` human detail issue metadata 对齐 Rust renderer：issue `fields` 匹配 detail label 后以 `>` 行展示 measured/expected，issue remedy 以 `->` 输出并去重，不再输出 Go 旧的 `issue/expected/remedy` 三行 dump。
  - [x] `doctor` human detail value humanization 对齐 Rust renderer：路径值支持 HOME `~` 缩写与中间截断，UTC ISO timestamp 显示为 `YYYY-MM-DD HH:MM UTC`，继续只影响 human 输出不改 JSON。
  - [x] `doctor` human PATH entries 折叠对齐 Rust `--all` 语义：`PATH git #`/`PATH codex #` 默认只显示前三条并提示 `--all`，带 `--all` 时展开完整列表。
  - [x] `doctor` feature flags detail 与 human `--all` 对齐 Rust：feature flag 明细并入 `config.load`，detail 使用 `feature flags enabled`/`feature flag overrides` 标签，human 输出折叠为 feature flags 摘要，`--all` 时显示 enabled flags 全量列表。
  - [x] `doctor` config legacy feature flag usage detail 已接入 Rust `legacy feature flag: alias -> canonical` 追踪：config/feature loader 会 canonicalize legacy aliases、保留 usage，并在 `config.load` details 输出。
  - [x] `doctor` state detail humanization 对齐 Rust renderer：state/log/goals/memories DB path 与 integrity 合并展示，active/archived rollout 文件统计格式化为 count/bytes/avg 摘要。
  - [x] `doctor` human header suffix 对齐 Rust renderer：runtime check 带 `platform` detail 时 header 显示 `Codex Doctor vX | platform`。
  - [x] `sandbox` parser 对齐 Rust requires：`-C/--cd` 与 `--include-managed-config` 需要 `--permission-profile`，sandbox-state readable root/disable-network 需要 `--sandbox-state-json`。
  - [x] `sandbox` parser 对齐 Rust conflicts：`--sandbox-state-json` 与 `--permission-profile`/`--cd`/`--include-managed-config` 互斥，且 conflict 优先于 requires 报错。
  - [x] `sandbox` parser 对齐 Rust `AbsolutePathBuf` 参数：`--sandbox-state-readable-root` 与 `--allow-unix-socket` 相对路径按当前目录解析为绝对路径。
  - [x] `sandbox setup` parser 对齐 Rust ArgGroup/requires：`--user`/`--current-user` 二选一，`--user` 需要 `--codex-home`，二者冲突。
  - [x] `sandbox` sandbox-state runtime readable-root 合并对齐 Rust `with_additional_readable_roots`：追加前按 cwd 解析特殊路径并检查有效读权限，避免 `:project_roots`/`:root` 已覆盖时重复追加 concrete path。
  - [x] `sandbox` runtime 对齐 Rust sandbox-state helper override：`codex_linux_sandbox_exe` 会覆盖 Linux sandbox wrapper argv[0]，默认仍走 `codex-linux-sandbox` PATH alias。
  - [x] `sandbox` runtime 对齐 Rust sandbox-state cwd：`sandbox_cwd` 必填且必须是当前 host native absolute path，并总是覆盖运行计划 cwd。
  - [x] `sandbox` root config/profile 合并对齐 Rust：root `-c/--config` 会 prepend 到 sandbox 子命令 overrides 前、sandbox 子命令 `-c/--config` 保持更高优先级，root `--profile` 会在 sandbox 未显式指定 `--profile` 时作为 config profile fallback，sandbox 显式 `--profile` 保持优先。
  - [x] `sandbox` legacy config 默认 profile 对齐 Rust：无 `default_permissions` 且无本次 CLI `sandbox_mode` override 时，`codex sandbox` 默认注入 legacy `read-only`，不再继承用户配置里的 ambient `sandbox_mode` 或裸跑。
  - [x] `sandbox` runtime 对齐 Rust `use_legacy_landlock`：无 sandbox-state 时从 config feature 读取，有 sandbox-state 时按 state 字段/默认值覆盖，并传入 Linux sandbox wrapper。
  - [x] `sandbox` child env 对齐 Rust `shell_environment_policy`：运行前按 inherit/core/default excludes/exclude/set/include_only 构造 env，随后注入 runtime `CODEX_PERMISSION_PROFILE`，直通执行与 Windows sandbox wrapper 共用该 env。
  - [x] `sandbox` 平台参数 surface 对齐 Rust：`--allow-unix-socket` 与 `--log-denials` 只在 macOS seatbelt 命令树可见/可解析，Linux/Windows 按 unknown sandbox option 拒绝。
  - [x] `sandbox` root feature toggles 对齐 Rust：root `--enable/--disable` 会折成 config overrides 传入 sandbox，顺序保持 root `-c` < root toggles < sandbox `-c`。
  - [x] `sandbox --include-managed-config` 运行时已接入 managed config file layer：sandbox config load 会在显式 include 时读取 `managed_config.toml` 并让其覆盖 user/profile/project config，不再把 include-managed-config 作为 sandbox plan unsupported。
  - [x] `sandbox` Linux managed proxy containment 参数对齐 Rust 小步：run plan 支持把 active managed network proxy 映射为 `--allow-network-for-proxy`，且 proxy routing 模式下不再同时传 legacy landlock。
  - [ ] `managed config/layer/origin metadata` 全体系仍需继续补齐：system/MDM `requirements.toml`、legacy `managed_config.toml` requirements reinterpret、cloud enterprise managed layers、ConfigService/LoadEffective 共享 origin stack 与 protected fields 还未完全同 Rust。
  - [ ] `sandbox` debug/run 参数、sandbox-state 运行时合并、profile/config 行为继续对齐 Rust。
  - [ ] `sandbox` managed network proxy runtime 仍需继续补齐：从 Rust `config.permissions.network` 同源配置启动/刷新 proxy、向 child env 注入 proxy 变量、把 managed MITM CA trust bundle 加入 readable roots、macOS extra unix sockets 与 Windows proxy port allowlist 还未全链路同 Rust。
- [ ] execpolicy/apply。IN_PROGRESS：已按 Rust `apply` 单一 optional PATCH positional 收紧 parser；`execpolicy check` 已补 matchedRules 空数组/decision 省略、exact match 优先于 host executable fallback，并补齐 prefix_rule/host_executable/network_rule 核心规则校验；Starlark 字符串 escape、按 statement 顺序的简单常量变量、string/list `+` 拼接、基础 f-string 插值已补，CLI 错误顺序和完整 Starlark 表达式继续顺序扫。
  - [x] `apply` parser 对齐 Rust：最多接受一个 PATCH positional；带空格的 patch 作为单个 shell 参数仍可用，多余 positional 直接报错。
  - [x] `execpolicy check` JSON 输出对齐 Rust 空匹配形状：无匹配时固定 `matchedRules: []` 且省略 `decision`。
  - [x] `execpolicy check --resolve-host-executables` 匹配顺序对齐 Rust：先按 exact command token 匹配，只有 exact 未命中时才走 basename/host_executable fallback，避免同一命令同时命中 exact 与 fallback 规则。
  - [x] `execpolicy` `prefix_rule` parser 对齐 Rust 核心校验：空 `pattern`、空 alternatives、空白 `justification` 直接报错；`match`/`not_match` 支持 token 数组和 shell 字符串，并在加载后按 Rust 语义验证例子。
  - [x] `execpolicy` `host_executable` 语义对齐 Rust：name 必须是 bare executable，paths 必须绝对且 basename 匹配，重复 path 去重，后定义覆盖前定义，显式空 allowlist 禁止 basename fallback。
  - [x] `execpolicy` `network_rule` parser 对齐 Rust 核心校验：协议 alias/deny decision、host 规范化、scheme/path/wildcard/whitespace 拒绝和空白 justification 拒绝，不再静默忽略 invalid network rule。
  - [x] `execpolicy check` CLI trailing command 对齐 Rust clap：未知 hyphen token 作为 `COMMAND` 起点处理，命令可直接以 `--...` 开头；出现在 `--rules` 前时按 Rust 顺序导致 required rules 缺失。
  - [x] `execpolicy` 规则 call scanner 补齐 Rust 主要边界：支持函数名与 `(` 之间空白，未知 policy 函数不再静默忽略，未知 named argument 会报错，`host_executable(paths=...)` 改为必填。
  - [x] `execpolicy` builtin 参数绑定继续对齐 Rust：`prefix_rule`/`network_rule`/`host_executable` 支持 positional 参数与 named 混用，拒绝重复参数、named 后 positional、过多 positional；pattern/decision/path/example 等直接值必须是字符串字面量，不再把未加引号 token 当字符串吞掉。
  - [x] `execpolicy` Starlark 字符串字面量 escape 小步对齐 Rust：规则 token、justification、example 等字符串会解析 `\"`/`\\` 等转义，非法 escape 不再按普通文本吞掉。
  - [x] `execpolicy` 简单常量变量对齐 Rust statement 顺序：支持前置 string/list 常量在 pattern、decision、match/not_match、host_executable、network_rule 参数中引用，后定义不 retroactively 影响前面的 builtin 调用。
  - [x] `execpolicy` Starlark 基础表达式拼接小步对齐 Rust：支持 string/list 常量和值使用顶层 `+` 拼接，覆盖 pattern、match/not_match、decision、justification 的常见静态表达式。
  - [x] `execpolicy` Starlark f-string 小步对齐 Rust：支持 `f"{NAME}"`/静态字符串表达式插值与 `{{`/`}}` 字面大括号，非法/未闭合插值不再静默吞掉。
  - [x] `execpolicy check` CLI JSON fixture 对齐 Rust：app 层测试按 Rust `cli/tests/execpolicy.rs` 精确比较 `decision/matchedRules/prefixRuleMatch`，并覆盖 absent justification 时不输出字段。
  - [ ] `execpolicy check` CLI 错误顺序、规则语言解析与 Rust fixture 继续对齐。
  - [ ] `execpolicy` 完整 Starlark 语义仍需继续补齐：load/函数或推导式等完整求值、标准 Starlark 诊断位置与精确类型名错误尚未完全同 Rust。
- [ ] resume/archive/delete/unarchive/fork。IN_PROGRESS：基础 session picker、archive/delete/unarchive/fork store 操作已具备；本轮补齐 delete force/confirm 文案与交互确认、Rust 成功输出、positional UUID/name target 解析，并按本地 Rust 收回 Go port 额外 `--name`/`fork` snapshot CLI 扩展，后续继续对齐真正 resume/fork 交互 runtime、prompt handoff 和 remote app-server 执行路径。
  - [x] `fork` CLI surface 按 Rust `ForkCommand` 收敛：只接受 `SESSION_ID`、`--last`、`--all`、prompt positional、remote/config/shared flags；拒绝 Go port 旧的 `--history-mode`、`--last-n`、`--last-turn-id`、`--ephemeral`，runtime 默认全量 fork。
  - [x] `delete --force` 对齐 Rust `delete_action`：强制删除只接受 UUID target，`--name` 或非 UUID target 返回 `--force requires a session UUID; names must be confirmed interactively`，避免名字绕过确认。
  - [x] `delete` 无 `--force` 在非交互路径返回 Rust 同款确认失败文案：`cannot confirm session deletion without an interactive terminal; rerun with --force and a session UUID`。
  - [x] `archive`/`unarchive`/`delete` 成功 human 输出对齐 Rust `success_message`：按 `Archived|Unarchived|Deleted session {name} ({id}).` 或 `{id}.` 输出，不再沿用 Go port 的 JSON/descendant count 临时形状。
  - [x] `resume`/`fork`/`archive`/`unarchive` positional `SESSION` target 解析对齐 Rust：先识别 UUID 作为 thread id，非 UUID 按 exact session name 查找，不再把任意字符串都当 id。
  - [x] `resume/archive/delete/unarchive/fork` session name selector surface 按 Rust 收敛：移除 Go port 额外 `--name` flag，统一使用 positional `SESSION`/`SESSION_ID` 承载 UUID 或 exact name。
  - [x] `resume`/`fork` TUI-only parser surface 对齐 Rust `SessionTuiCli`：支持 `--ask-for-approval/-a`、`--search`、`--no-alt-screen`，保留 `--ask-for-approval` 与 `--dangerously-bypass-approvals-and-sandbox` 同实例冲突，且不泄漏到只 flatten `SharedCliOptions` 的 archive/delete/unarchive。
  - [x] `archive`/`unarchive` exact-name lookup 对齐 Rust interactive scope：只命中 cli/vscode 等可交互 session，exec/appserver 等非交互记录不再被 mutation name target 选中。
  - [x] `resume`/`fork` prompt positional parser 对齐 Rust `SessionTuiCli`：允许 `SESSION PROMPT` 与 `--last PROMPT`，拒绝 `--last SESSION PROMPT` 冲突。
  - [x] `delete` 非 `--force` 补齐 Rust 交互确认流程：先 resolve target 并读取 session name，stdin/stderr 非 TTY 返回同款错误；TTY 下输出 `Permanently delete...`/`Continue? [y/N]:`，`y/yes` 删除，其它输入输出 `Delete cancelled.`。
  - [x] `resume/archive/delete/unarchive/fork` runtime config load 对齐 Rust 小步：root/session `-c` 按顺序并入 effective config，root/session `--strict-config` 任一开启都会在运行期触发未知字段校验，不再被 placeholder session runtime 静默忽略。
  - [ ] `resume`/`fork` 当前仍只输出 summary/picker，尚未复用 Rust interactive TUI 的真实会话恢复/fork 与 prompt handoff。
  - [ ] `resume/archive/delete/unarchive/fork` JSON 输出、剩余错误文案、remote app-server 执行路径继续按 Rust fixture 顺序扫。
- [ ] cloud/responses-api-proxy/stdio-to-uds/exec-server。IN_PROGRESS：exec-server stdio/ws listen 已有真实 initialize/config 服务，responses-api-proxy/cloud 基础已补；`stdio-to-uds` 现在在 Unix 平台会真实桥接 stdin/stdout 到 Unix domain socket，在 Windows 平台会真实桥接 stdin/stdout 到 `\\.\pipe\...` named pipe；本轮补 `cloud status` parser 错误顺序、`exec-server --strict-config` 运行时严格加载与本地 ws transport；cloud/exec-server 剩余 remote/runtime 细节继续扫。
  - [x] `cloud status` parser 按 Rust/其它 cloud 子命令顺序收紧：未知 flag 优先报 `unknown cloud status option`，多余 positional 报 exactly one TASK_ID，缺失仍报 requires TASK_ID。
  - [x] `exec-server --strict-config` 运行时接 config loader，root `--strict-config` 与子命令 `--strict-config` 都会对 config 文件和 root `-c` override 执行未知顶层字段校验；非 strict 本地 listen 仍按 Rust 忽略 config 加载失败。
  - [ ] `exec-server` remote/environment/name/auth 启动输出与真实 remote registration 行为继续对齐。
    - [x] parser 对齐 Rust clap：`--listen` 与 `--remote` 冲突，`--remote` requires `--environment-id`，`--use-agent-identity-auth` requires `--remote`。
    - [x] 本地 `exec-server --listen` transport 对齐 Rust：未传时默认 `ws://127.0.0.1:0`，显式空值进入 unsupported URL 错误；只接受 `stdio`/`stdio://` 与 `ws://IP:PORT`，`ws://localhost:...` 按 Rust 文案拒绝。
    - [x] 本地 WebSocket exec-server 不再输出 Go port 占位 JSON，启动后 stdout 打印实际 `ws://host:port`，提供 `/readyz`，拒绝带 `Origin` header 的浏览器请求，并复用现有 JSON-RPC initialize/environment/process/fs/http 处理。
    - [x] remote runtime 早期校验对齐 Rust：trim base URL/environment id，缺 auth、unsupported auth、agent identity 缺 `CODEX_ACCESS_TOKEN`、API-key remote host 限制均返回 Rust 同款文案；校验通过后不再输出 Go port 假 JSON 成功。
    - [ ] 真实 remote environment registry 注册、Noise rendezvous 连接与长期服务循环仍待移植。
  - [x] `responses-api-proxy` stdin API key 读取对齐 Rust buffer/trim/validation：只裁剪结尾 CR/LF，空输入、非法字符和 1024-byte buffer 超长错误文案对齐。
  - [x] `responses-api-proxy` 启动输出/server-info/URL 校验对齐 Rust：启动只写 stderr listen 行，stdout 不再输出 Go port 调试 JSON；server-info 保持单行 `{"port","pid"}` JSON；relative upstream URL 与缺 host URL 错误文案对齐 Rust 边界。
  - [x] `responses-api-proxy` 转发 headers/content-length 对齐 Rust：请求上游只跳过 `Authorization`/`Host` 并保留其它 header，响应只跳过 Rust 管理的 `content-length`/`transfer-encoding`/`connection`/`trailer`/`upgrade`，再按 upstream `Content-Length` 显式设置响应长度。
  - [x] `responses-api-proxy` `/shutdown` 对齐 Rust 进程语义：HTTP 200 空响应后调用进程 exit hook；测试中用 hook 接住并 fallback 关闭 server，生产路径保持 `os.Exit(0)`。
  - [x] `responses-api-proxy` dump response drop 写入对齐 Rust：响应体边转发边收集，正常 EOF 或客户端提前断开都会写出已读 response dump，写入失败只记 stderr。
- [x] root strict-config 限制。
  - [x] root `--strict-config` 只允许 Rust 同形会加载严格 config 的入口：interactive、exec/review、mcp-server、exec-server、doctor、app-server runtime 和 session 命令；`mcp`、`cloud`、`app`、`features`、`sandbox`、`completion/update/debug/apply/execpolicy/responses-api-proxy/stdio-to-uds/remote-control` 继续按 Rust 拒绝。
  - [x] `app-server` 工具子命令在解析后继续拒绝 root `--strict-config`，保留 `codex app-server daemon bootstrap` 这种完整 subcommand 错误文案。
- [ ] remote flag 限制。IN_PROGRESS：root `--remote/--remote-auth-token-env` 只放行 interactive 与 Rust 带 `InteractiveRemoteOptions` 的 session 命令；本轮补齐 session 子命令自身的 remote flags。
  - [x] `resume/archive/delete/unarchive/fork` 子命令接受 `--remote` 与 `--remote-auth-token-env`，并补 completion/parser 回归；子命令 remote 后续按 Rust 语义优先于 root remote。
  - [x] session runtime 合并 root/subcommand remote，按 Rust 优先级让子命令 remote 覆盖 root remote，并复用 interactive endpoint/auth-token 校验；remote 模式下不再静默回退本地 session store。
  - [ ] remote TUI/app-server client 主循环尚未接入 interactive/resume/archive/delete/unarchive/fork 的真实远端执行，当前返回明确 unsupported。
    - [ ] 需要先补 outbound `AppServerSession`/JSON-RPC client：`initialize`、`thread/list/read/archive/delete/unarchive/fork/resume`、notification/response 分流、websocket bearer token 注入。
    - [ ] 再接 TUI bootstrap 与 archive/delete/unarchive/fork CLI 的 remote endpoint launch/reuse 逻辑，替换当前 unsupported 返回。
- [ ] app-server listen/auth 参数。IN_PROGRESS：listen/off、WebSocket auth parser、managed requirements gate 与 remote-control-only websocket loop 已补；剩余 WebSocket auth/listen 细节继续顺序扫。
  - [x] `app-server --listen off`、`--stdio`/`--listen` 冲突、非法 listen scheme 和 WebSocket auth flag 依赖已补 parser/app 回归。
  - [x] WebSocket auth CLI `try_into_settings` 边界对齐 Rust：`--ws-token-file`/`--ws-shared-secret-file` 必须为绝对路径，`--ws-token-sha256` 在 parser 阶段校验 64 hex，signed bearer issuer/audience 按 Rust trim/空值收敛，显式空 mode-specific flag 仍要求 `--ws-auth`。
  - [x] requirements `allow_remote_control=false` 语义已接入 app-server startup 与 remote-control RPC：显式 `--remote-control`、`--listen off` fallback 和 runtime RPC 均返回 Rust 同类 managed requirements 错误；`allow_remote_control=true` 不会主动启用也不阻断 remote-control。
  - [x] `--listen off` + persisted remote-control enable 已接入真实 remote-control-only 长期运行：读取 Rust state DB enrollment preference 后启动 websocket loop 与 remote transport bridge，不再只是占位 context 保活或返回 no transport。
- [x] session 命令参数冲突。
  - [x] `resume/fork --last PROMPT` 保持可用，`resume/fork SESSION --last` 按 Rust 重解释为 prompt；`resume/fork --last SESSION PROMPT` 或 `SESSION --last PROMPT` 在 parser 层报冲突。
  - [x] `--name` selector 已按 Rust 收敛移除，parser/completion 不再暴露；positional `SESSION`/`SESSION_ID` 按 UUID 或 exact name 解析，错误文案和 mutation target lookup 已补回归。
- [ ] human output 与 JSONL output 完整对齐。IN_PROGRESS：`exec --json` lifecycle/error 基础已补；本轮收窄 `turn.completed.usage` schema。
  - [x] `exec --json` 的 `turn.completed.usage` 按 Rust `Usage` schema 去掉 Go-only `total_tokens`；保留 input/cached input/output/reasoning output 四字段。
  - [x] protocol 层补 Rust 同形 `turn.failed` JSONL event helper，供后续真实 failed-turn notification 路径接入。
  - [x] JSONL reasoning item 映射补齐 Rust 边界：只输出 summary 文本，空/空白 summary 或仅 raw content 的 reasoning 不再输出，streaming `output_item.added` 也不为 reasoning 生成 started item。
  - [x] direct exec JSONL 将 `update_plan` 工具输出映射为 Rust `todo_list` item.completed，计划项只保留 text/completed，且不再同时输出普通 `tool_call/tool_output`。
  - [x] human exec 结束时按 Rust blended total 语义向 stderr 输出 `tokens used`，stdout 继续只承载 final message。
  - [x] human final message 按 Rust TTY 规则分流：stdout/stderr 同为 TTY 时写 stderr `codex` 区块，否则写 stdout；tokens used 保持 stderr。
  - [ ] human “已渲染 final message 不重复打印”仍需真实 streaming human renderer/app-server notification 路径接入后补。
  - [ ] JSONL item mapping 仍需继续扫 Rust `EventProcessorWithJsonOutput`：todo list started/updated lifecycle 仍需 app-server `TurnPlanUpdated` notification 管道，MCP/collab/web-search/file-change 细项和 failed turn 来源。
- [ ] exit code 语义完整化。IN_PROGRESS：doctor overall fail 已 silent exit 1；exec JSON 模式失败时已输出结构化 error event 并继续返回 error 让 CLI exit 1；sandbox/child process exit passthrough 已补；后续继续按 Rust CLI 顺序扫其它命令退出路径。
  - [x] `sandbox` 普通子进程与 Windows sandbox wrapper 非零退出按 Rust `handle_exit_status` 语义保留原始 exit code，并通过 silent `app.ExitError` 避免额外错误打印。
  - [x] `login status` not-logged-in 与 `logout` not-logged-in 已复核 Rust exit 语义：前者 silent exit 1，后者成功退出 0。
  - [x] `login status` 存储读取错误按 Rust `Error checking login status: ...` 前缀返回 printable exit 1；login/logout config/storage 错误路径开始收敛到固定前缀。
  - [x] `login --with-api-key --with-access-token` 与废弃 `--api-key` 分支按 Rust direct exit 文案返回 printable exit 1，不再误走 API key stdin 分支。
  - [x] `login --with-api-key/--with-access-token` stdin secret 边界对齐 Rust：TTY stdin 输出 pipe guidance 后 silent exit 1；非 TTY 先向 stderr 输出 Reading 提示；空输入输出固定错误后 silent exit 1。
  - [x] `login` forced-method 与 API key/access-token/device/browser flow 错误路径收敛为 Rust 对应固定前缀的 printable exit 1，成功路径保持现有输出兼容。
  - [x] `logout` config load 错误按 Rust `Error loading configuration: ...` 前缀返回 printable exit 1，not-logged-in 仍保持成功退出 0。
  - [x] interactive startup fatal（`TERM=dumb` 拒绝、remote auth 参数错误等）按 Rust `handle_app_exit` 输出 stderr `ERROR: ...` 后 silent exit 1，不再作为裸 error 交给顶层重复打印。
  - [x] `update` 退出路径已复核：Rust update action 非零通过 error 退出 1，Go `install.RunUpdate` 已保持同类错误退出，无子进程原始 exit code passthrough 需求。
  - [x] Unix sandbox/child process signal exit 按 Rust `handle_exit_status` 映射为 `128+signal`；Windows/非 Unix 保持标准 process exit code。
  - [ ] 继续顺序复核 Rust `debug_sandbox.rs` 中 `codex sandbox` runner 的平台细节（managed proxy/env/denial logging/Windows session 边界等）；exit passthrough 主路径已补。
  - [x] Rust CLI 其它直接 `std::process::exit` 分支已按当前 Go 已实现入口复核：doctor fail、interactive fatal、login/logout/update 与 sandbox passthrough 均有 Go 顶层 `ExitCode` 映射。

验收：

- [ ] Rust CLI parser tests 可翻译到 Go。
- [ ] help、错误消息、别名、隐藏命令行为一致。

## 阶段 9：MCP、插件、Skills、Connectors

目标：对齐 Rust `mcp-server`、`codex-mcp`、`plugin`、`core-plugins`、`skills`、`connectors`。

- [x] MCP config/client/resource/OAuth 基础。
- [x] plugin manifest 基础。
- [x] skills/appserver 基础。
- [x] MCP server stdio。
- [ ] MCP client stdio/http 完整 runtime。IN_PROGRESS：stdio/http initialize 已统一使用 `2025-03-26` 和共享 client capabilities；stdio client 等待目标响应时已能处理服务端 `roots/list`、`elicitation/create`、`openai/form` 请求并继续读取目标响应；`roots/list` 已从固定空数组升级为基于 thread/active turn workspace roots 返回 MCP `file://` roots，支持 stdio 和 HTTP SSE；tools/resources/resourceTemplates inventory 已支持 `nextCursor` 分页并防重复 cursor；`mcpServerStatus/list` 的 `detail` 省略/params nil 已按 Rust README 默认 full，会拉取 tools/resources/resourceTemplates；stdio `elicitation/create`/`openai/form` 已支持注入 `MCPElicitationHandler`，无 handler 时保持 cancel；HTTP streamable SSE 现在能处理服务端 JSON-RPC request，调用同一 elicitation/root handler，并用后续 POST 回写 client response 后继续等待目标 response；stdio/HTTP SSE 已能识别 `notifications/progress` 并通过 `MCPProgressHandler` 回调，tool runtime 会把 thread/turn/item context 带到底层；`MCPService` 已对 HTTP streamable client 做 server/config 级 session cache，status/tool/resource 复用 initialize/session，`Refresh`/config change/runtime reload 会先对旧 HTTP streamable session 发送 best-effort `DELETE` 后重建；HTTP 带 session request 遇到 404/410 会清理 stale session、重新 initialize/initialized 并重试当前请求一次；stdio client 已补 server/config 级 session cache，status/tool/resource 复用同一个 initialized stdio 进程，`Refresh`/config change/runtime reload 会关闭旧进程，单次 request 仍保留 timeout 并在超时/transport 错误后重建；stdio JSON-RPC remote error 不再关闭 session，transport 错误会关闭坏 session 并对当前请求做一次重建重试；`RuntimeConfigFromValues` 已能从 config map 解析 `mcp_servers`、apps feature、ChatGPT base URL 和 apps product SKU，`MCPService.ApplyRuntimeConfig`/`config/mcpServer/reload` 已接入 app-server，`Refresh` 会清理资源缓存；stdio 并发 request multiplex 已补，更多 Rust 边界仍待补。
- 本轮补充：`RuntimeConfigFromValues` 现在兼容 app-server/JSON 风格 camelCase alias，`mcpServers`、`chatgptBaseUrl`、`appsMcpProductSku`、`connectorIds`、`availableEnvironment` 以及 server 级 `bearerTokenEnvVar`/`oauthClientId`/`oauthResource`/`environmentId` 不再被静默忽略；补 MCP config 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`RuntimeConfigFromValues` 的 server `env` 现在兼容 `map[string]string` 与 `map[string]any`，并裁剪 env key/value，避免 Go/app-server 内部构造 runtime map 时 stdio MCP env 被静默丢弃；补 MCP config 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：stdio client 已从单请求等待改为 pending map + read loop 分发 response，`writeMu` 串行写 frame，server request/progress 使用活跃 pending call 上下文，timeout/transport error 会关闭坏 session 并唤醒 pending；补充并发 out-of-order tool call 单元测试代码。
- 本轮补充：disabled MCP runtime server 不再进入 `mcpServerStatus/list` 的 ready status 面，避免未启用 server 看似可用；required+disabled 仍保留调用时 `required MCP server ... is not enabled` 错误语义；补 mcp service 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：动态 `SetServerConfig` 禁用 server 时会清掉旧 ready status；`config=nil` 仍表示移除，`enabled=false|required=true` 会保留 required 标记并在调用时返回 not enabled；补 mcp service 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：动态 `SetServerConfig` 启用 server 时会同步生成 ready status、server info 与 auth status，新启用 MCP server 立即出现在 `mcpServerStatus/list`；补 mcp service 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：动态 `SetServerConfig` 新增 HTTP MCP server 时会继承 service OAuth store 的 CodexHome，status auth 与实际 HTTP client bearer token 使用同一 token store；补 HTTP MCP 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [ ] MCP OAuth 完整流程。IN_PROGRESS：已补 streamable HTTP OAuth discovery，支持 `/.well-known/oauth-authorization-server/<path>`、protected resource metadata `WWW-Authenticate resource_metadata` 跳转、`scopes_supported` 去重归一化和 `registration_endpoint`；`mcpServer/oauth/login` 在带 `timeoutSecs` 时优先用发现到的 authorization endpoint，并已改用 `golang.org/x/oauth2` 生成 authorization URL；已补基于 `golang.org/x/oauth2` 的 authorization code exchange 与 refresh token 底层能力，按 Rust token 存储形状生成 `OAuthTokenSet`；MCP HTTP client 请求前已支持过期 token 自动 discovery/refresh/save 并使用新 bearer，未过期 access token 收到 401 时会强制 refresh 并重试当前请求一次，HTTP SSE server request 的 client response POST 遇到 401 也会强制 refresh 并重试；refresh 返回 `invalid_grant`/`invalid_client`/`unauthorized_client` 时会删除旧 token，避免坏 refresh token 反复重试；已补 OAuth login session helper，包含 PKCE/state、callback path/id、callback parse/provider error/state 校验和 code exchange 串联；动态 client registration helper 已支持 registration endpoint JSON POST、public client 默认 metadata、注册后生成 PKCE login session；browser callback server 已支持本地 callback、success/cancel、code exchange、token store 保存；`MCPService.OauthLogin` 已在有 discovery/codexHome 时启动本地 callback server并返回真实 authorization URL；app-server 已能把 OAuth login 完成/失败映射为 `mcpServer/oauthLogin/completed`；`mcpServer/oauth/cancel` 已接入 service/app-server/schema，取消 active login 后走同一 completed 失败通知，runtime reload/server config change 会取消旧 active login；CLI `codex mcp login` 已复用 `auth.OpenBrowser` 自动打开 authorizationUrl 并保留 headless fallback 文案；后续继续扫 OAuth Rust 细节差异。
- 本轮补充：MCP OAuth dynamic registration 的 `client_secret` 现在会随 token set 持久化，并在 `refresh_token` 请求中传递；动态注册返回 secret 后后续 OAuth refresh 不再丢 confidential client 凭据；补 OAuth store/token/http refresh 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：已安装远端 marketplace 插件在 `DiscoverableInstallCandidates` 路径上也会对 enabled placeholder 做懒 materialize，避免重启或缓存目录丢失后插件提供的 app/template connector 候选从推荐工具里消失；补 plugin discoverable candidates 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：discoverable plugin 过滤的 configured/disabled/loaded app connector ID 现在会统一 trim 并跳过空值，候选自身 connector ID 比较时也会 trim，避免 TOML/JSON 输入带空白导致推荐插件或 connector 候选漏过滤；补 plugin helper 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`mcpServerStatus/list` 显式空白 cursor 现在返回 `invalid cursor`，不再静默当作省略处理，保持 list/page 类 RPC 的 invalid request 行为一致；补 MCP status pagination 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`mcpServerOpenaiFormElicitation` capability 切换时现在会同时清理 MCP resource cache，避免 initialize capability 改变后继续复用旧 session 下缓存的 resource read 结果；补 HTTP MCP resource cache 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin/uninstall` 现在会在入口统一裁剪 `pluginId` 并用规范 ID 做 installed map 查询、config 删除、materialized cleanup 和内存状态更新，避免带空白 ID 时状态分裂；补 plugin install persistence 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin/read` 现在会对 `pluginName`、`remotePluginId`、`remoteMarketplaceName`、`marketplaceName`、`marketplacePath` 做裁剪后再匹配，并在 response 中回填裁剪后的 marketplacePath，避免 JSON-RPC 参数带空白时读不到已有插件；补 plugin read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin/skill/read` 现在会规范化 `remoteMarketplaceName`、`remotePluginId`、`skillName` 后再匹配/读文件/fallback，避免带空白参数时真实 skill 文件读不到并退回占位 Markdown；补 plugin skill read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：plugin share principal/target 输出现在会裁剪 `principalType`/`principalId`/`role`/`name`，避免带空白字段泄漏到 Rust wire shape；补 plugin share wire 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`PluginService.AddPlugin` 注入 detail 时会裁剪 summary 的 ID/name/displayName/marketplaceName/remotePluginId，并规范 detail marketplaceName，避免带空白标识直接成为内存 map key 或后续匹配字段；补 plugin AddPlugin 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`marketplace/upgrade` 的可选 `marketplaceName` 指针参数现在会先 trim 再比较，避免带空白时目标 marketplace 被静默跳过；补 marketplace upgrade 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：MCP status inventory 的 ready/failed/error/auth 快照现在会同步回 `MCPService.servers`，required server 在 status/list 失败后会阻断后续 tool/resource 调用，恢复成功后解除阻断；补 HTTP required server 回归测试，已运行窄范围 `go test -run TestHTTPMCPRequiredServerStatusFailureBlocksToolCall ./internal/mcp`、`gofmt` 与 `go list -buildvcs=false ./...`。
- 本轮补充：plugin hook sources 现在与 capability/skill roots 一致，要求插件 `installed && enabled` 才会暴露 hooks，避免未安装插件仅靠 `Enabled:true` 被 hooks/list 发现执行；补 plugin 负例和 app-server hooks/list 集成 fixture。为让窄测试可运行，同步补回 `model.AgentItem.Status`、修 app-server 测试编译漂移（fixedTime/writeJSON helper、指针参数、导入/类型转换）及几个非测试编译/vet 小口子；已运行 `go test -run "TestEnabledHookSources(UsesPluginRoot|SkipsUninstalledPlugins)$" ./internal/plugin`、`go test -run TestRuntimeRouterHooksListIncludesPluginHooks ./internal/appserver`、`gofmt` 与 `go list -buildvcs=false ./...`。
- 本轮补充：explicit skill linked mention parser 现在同时支持 `[$skill](skill://...)` 与 Rust/Markdown 常见的 `[skill](skill://...)`，修正远端 environment skill link 无 `$` 时不会注入 `<skill>` 内容的问题；补 prompt parser 回归并跑通 app-server 远端 skill root 集成测试；`gofmt`、`go test -run TestCollectExplicitSkillMentions ./internal/prompt`、`go test -run TestRuntimeRouterTurnStartUsesRemoteEnvironmentSkillRoot ./internal/appserver` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：plugin/app mention token parser 现在允许 `@` 出现在 token 内，修正 canonical plugin id 如 `sample@debug` 通过 `@plugin://sample@debug` 显式提及时被截断成 `sample`、导致 plugin-associated instructions 不注入的问题；补 plugin explicit mention 回归；`gofmt`、`go test -run TestCollectExplicitPluginMentions ./internal/plugin` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：MCP inventory 失败时现在会清空本次 status 的 tools/resources/resourceTemplates，并同步清掉 service 内部旧 inventory，避免 server 已 failed 但 app/list/MCP accessible 继续从 stale tools 暴露 connector 或让 required gate 看到旧工具；扩展 HTTP MCP required gate 回归覆盖“成功后失败”场景；`gofmt`、`go test -run TestHTTPMCPRequiredServerStatusFailureBlocksToolCall ./internal/mcp` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：`request_plugin_install` 的 connector accept 完成验证现在会用 `app/list forceRefetch` 查询 accessible apps，避免用户刚授权 connector 后仍命中旧 cache 导致 `completed=false`；补 app-server plugin install runtime 回归；`gofmt`、`go test -run TestPluginInstallRuntimeConnectorAcceptForceRefetchesAccessibleApps ./internal/appserver` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：`request_plugin_install` 的 codex-tui plugin install 禁用现在在 candidate 匹配后按实际 `tool_type` 再校验，避免模型省略 `tool_type` 时用 plugin `tool_id` 绕过 TUI 限制并触发 runtime；补 tool 层回归；`gofmt`、`go test -run TestRequestPluginInstallHandlerBlocksTUIPluginInstallWithoutToolType ./internal/tool` 与 `go list -buildvcs=false ./...` 已通过。
- 本轮补充：外围 MCP/plugin/apps/prompt/tool/appserver 已通过测试编译闸门 `go test -run '^$' ./internal/mcp ./internal/plugin ./internal/apps ./internal/prompt ./internal/tool ./internal/appserver`，确认本轮窄测暴露的测试编译漂移已收口；仍未运行全量测试。
- 本轮处理中：继续按 P0/P1 收口闸门扫 plugin/skills/apps 与 MCP 剩余状态边界，只修会影响状态、配置、调用路径或 Rust wire 语义的问题，暂不继续追低风险 trim/展示细节。
- [ ] MCP resource cache/elicitation/error mapping。IN_PROGRESS：资源读取缓存已补，`MCPService.ReadResource` 会按 server/uri 走 TTL/LRU cache 并在 server/config 变化时清理；HTTP/stdio JSON-RPC error 已结构化为 `MCPRemoteError`，保留 code/message/data/method；stdio/HTTP SSE elicitation 已能解析请求并通过 handler 返回 accept/decline/cancel；app-server 已把 `MCPElicitationHandler` 接到 `mcpServer/elicitation/request` server request broker，能把 accept/decline/cancel 回写给 MCP client；stdio/HTTP MCP tool/resource 调用已把 threadId 深传到 elicitation request；URL elicitation 会输出 `mode=url`，并支持 JSON-RPC id 兜底为 elicitationId；MCP progress notification 已保留 server/thread/turn/item/progress/total/message/原始 params 并回调 handler，app-server 会映射为 `item/mcpToolCall/progress`；stdio/HTTP 底层 resource read helper 已补 nil/空 URI 防护并裁剪 URI；resource template JSON 输出已透传 `icons` 与 `_meta` 并在 status clone 中深拷贝；本轮补充：app-server JSON-RPC 远端错误数据已透传 `type/method/code/message/data`，MCP server 返回的结构化 error 不再只剩 `-32603` 文案；本轮补充：resource read cache key 已加入规范化 `threadId`，避免不同 thread/workspace roots 共享同一 server+uri 缓存，并补 HTTP service 回归源码；本轮补充：app-server `item/mcpToolCall/progress` payload 已透传 `serverName/progressToken/progress/total/params`，不再只剩 message；本轮补充：runtime JSON-RPC 现在保留 MCP remote error code，不再把远端 `-320xx/-326xx` 一律映射为 `-32603`；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`；更多错误映射仍待补。
- 本轮补充：MCP resource read cache key 继续扩展到 workspace roots fingerprint，同一 thread 的 roots 变化后会重新读取 resource，同 roots 后续读取仍走缓存；补 HTTP resource cache 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`mcpServerStatus/list` 的 status wire 现在输出 `state/error`，默认空 state 归一为 `ready`，inventory 失败标记为 failed 后客户端也能看到错误消息；补 MCP status wire 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`MCPServerStatusDetail{}` direct/service 语义已与 Rust string wire 的 `toolsAndAuthOnly` 对齐，零值 detail 会包含 tools 但不拉 resources/resourceTemplates；旧 `{includeTools:false}` 对象输入仍保持不包含 tools 的兼容语义；补 MCP status detail 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [ ] plugin install/uninstall/marketplace。IN_PROGRESS：marketplace add 已补 Rust 风格 source 解析，支持 GitHub shorthand、Git URL/ref、SSH URL、本地目录，拒绝 file URL、本地文件、local+sparse；git marketplace 已支持可注入 materializer，默认按 Rust 走 `git clone`/`checkout`/sparse-checkout 并安装到 `.tmp/marketplaces/<name>`；add/remove 已能结构化读写 `config.toml` 的 `[marketplaces.<name>]`，`SetCodexHome`/`SetMarketplaceConfigPath` 会 best-effort reload 已保存 marketplace；upgrade 已补 git `ls-remote` revision resolver、revision 未变跳过和 `last_revision` 写回；plugin list/read 已能从 marketplace root 的 `.agents/plugins/marketplace.json`、`.agents/plugins/api_marketplace.json`、`.claude-plugin/marketplace.json` 发现本地插件并读取 `.codex-plugin/plugin.json`、真实 `skills/**/SKILL.md` 列表与 MCP server 摘要；plugin read/install 已支持 Rust canonical `marketplacePath + pluginName` 匹配，并能把 discovered marketplace plugin 安装回内存状态；plugin install 已补本地/远端来源互斥校验、安装策略拒绝、`ON_INSTALL` appsNeedingAuth 和 uninstall 禁用状态；远端 marketplace 插件现在会保留 git source 占位信息，显式 read/install 时按需 materialize 到 marketplace install root 的 `plugins/<marketplace>/<plugin>`，再读取真实 `.codex-plugin/plugin.json`、skills 与 MCP server 摘要；installed plugin 会写入 `config.toml` 的 `[plugins.<id>]`，重启后可恢复 installed/enabled、skills 与 MCP 摘要，uninstall 会清理配置；remove marketplace 会同步删除该 marketplace 的 installed plugin 配置、内存状态和已 materialized 插件目录；`plugin/skill/read` 已能从 installed plugin 的 `skills/` 目录读取真实 `SKILL.md`，找不到时保留旧兼容占位；PluginInterface 读取已兼容 Rust marketplace/cache 的 snake_case interface 字段和 default_prompt/default_prompts；marketplace 与 `.codex-plugin/plugin.json` 的 `apps`、`appTemplates`/`app_templates` 已能进入 detail，app ID、template canonical connector 和 materialized app IDs 会回填 `PluginSummary.AppConnectors`；本轮补充：单独 `plugin/uninstall` 现在会删除远端 marketplace 插件的 materialized 目录，避免只删配置不删缓存；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`；后续继续补 marketplace upgrade 对 installed/materialized 插件的刷新和 Rust 细节差异。
- 本轮补充：`plugin/skill/read` 已对远端 marketplace placeholder 补按需 materialize，未安装远端插件也能先拉到 marketplace install root 再读取真实 `SKILL.md`，找不到 skill 时仍保留旧占位 Markdown；补远端 marketplace skill read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin/skill/read` 现在在 `remoteMarketplaceName` 已限定时允许 `remotePluginId` 按 plugin name 匹配，兼容客户端把插件名放入 remotePluginId 的 Rust wire 形态，同时不放开裸 plugin name 的跨 marketplace 歧义；补 plugin skill read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：marketplace upgrade 成功后会清理同 marketplace 下未安装远端插件的 materialized cache，已安装插件仍走既有 refresh/rematerialize；避免只 read 过但未 install 的远端插件在 marketplace 更新后复用旧目录；补未安装远端 plugin upgrade cache 清理回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：远端 marketplace plugin 非 force materialize 现在会先复用已存在的 materialized `.codex-plugin/plugin.json`，重复 `plugin/read`/`plugin/skill/read` 不再反复调用 materializer；marketplace upgrade 仍通过 force 删除并重拉，未安装 cache 清理语义保持有效；补 plugin materialized cache 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：已安装远端 marketplace plugin 在重启后若 materialized manifest/目录丢失，`plugin/read` 会重新 materialize；旧配置里记录的绝对 materialized plugin root 会折回相对 subdir，避免被误当 marketplace source path；`plugin/skill/read` 也会在 root 存在但文件丢失时重拉；补 plugin rematerialize 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：已安装远端 plugin 缺失 materialized root 的 lazy rematerialize 已扩到 enabled capabilities、skill roots 和 hook sources；重启后直接 turn/app-list 也会恢复真实 MCP/skill/app capability，并把恢复后的 detail 写回内存；补 plugin enabled capability 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin/install` 已有 detail 快路径现在会对远端 placeholder/缺失 manifest detail 执行 materialization 后再写 installed config，避免 remote plugin 被直接标记 installed 却缺真实 skills/MCP 摘要；补 plugin install 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：plugin share 的 `remotePluginId` 现在在 save/update/delete/checkout 中统一裁剪；带空白 ID 时 update/delete 能命中同一 share，checkout response/path 也输出干净 ID；补 plugin share 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：收紧 `request_plugin_install` tool spec 的并行语义，和文案中的 “DO NOT call this tool in parallel” 保持一致；补 tool 层回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：继续收紧 `request_plugin_install` 的模型可见结果，补回 runtime response action、持久禁用标记、elicitation 是否发送和 suggestion id；补 tool 层 decline metadata 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：继续把 `tool_suggest.disabled_tools` 和已 accessible app connector 接入 app-server turn 的 plugin install 候选过滤，补齐 `persist=always` 后下一轮不再建议的闭环；补 app-server runtime 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：继续收窄 app-server turn 的 `<recommended_plugins>` 与 recommendation-context `request_plugin_install`，只使用 plugin 候选，避免 connector-only 候选出现在 plugin_id 工具无法接受的推荐面；补 app-server turn 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：`plugin marketplace upgrade` 成功后会刷新同 marketplace 下已安装插件；本地插件重新读取升级后的 manifest/skills/MCP 摘要，远端 marketplace 插件会强制清理旧 materialized 目录并重新 materialize，随后更新 `[plugins.<id>]` 配置和内存 installed 状态；enabled skill/hook roots 也改为优先使用插件根目录而不是 marketplace 根目录。
- [ ] plugin mentions/tool exposure。IN_PROGRESS：enabled installed plugin 已能生成 Rust `PluginCapabilitySummary` 风格 capability，显式 `plugin://<plugin id>` mention 可注入 plugin-associated skills/MCP/apps developer instructions，app-server turn runtime 已加入 available plugin instructions；显式 `app://<connector id>` mention 已能通过 plugin `AppConnectors` 反向触发关联 plugin instructions；显式 plugin instructions 现在按本轮真实 MCP status tools 与 app enabled 状态取交集，不再仅按 manifest 声明列出不可用 MCP/app，且只在存在显式 plugin/app mention 时拉取 MCP/app 状态；未安装 marketplace 插件会作为 `<recommended_plugins>` user context 注入 turn，并按推荐上下文暴露 `request_plugin_install` 工具；非推荐上下文也已提供 `list_available_plugins_to_install` 与 `request_plugin_install` handler；`request_plugin_install` 已能构造 Rust 同形 elicitation metadata，经 app-server `mcpServer/elicitation/request` 请求用户确认，accept 后调用 `PluginService.Install` 完成插件安装验证，decline + `persist=always` 会写入 `tool_suggest.disabled_tools`；legacy list-tool 路径已支持 connector 候选、`tool_type=connector`、`install_url` metadata 和 connector accept 不误装 plugin；已安装且 enabled 的插件现在会把 app/template connector 作为 discoverable connector 候选暴露给 `list_available_plugins_to_install`，list 输出也带 `install_url`；connector accept 后会通过 `AppService` 检查对应 app 是否 accessible 来设置 completed；MCP runtime tool 已能携带 plugin provenance，并按 Rust 文案把 “This tool is part of plugin(s) ...” 注入 tool description；本轮补了 Rust `codex-connectors` 风格 app list 合并核心：directory provider/cache、accessible provider/cache、plugin connector placeholder、install URL slug、accessible merge/filter、`[apps]` enabled 覆盖，`app/list` 会注入 enabled plugin 的 app connector IDs 和当前 config；插件 detail 的 app/template 名称、描述、install URL、logo 与 plugin display name 现在也会进入 app placeholder，多个插件关联同一 connector 会合并 pluginDisplayNames；ChatGPT directory provider 已按 Rust 路径分页拉取 `/connectors/directory/list`、workspace 追加 `/connectors/directory/list_workspace`、过滤 hidden、合并重复 app，并接入 app-server ChatGPT/Codex backend auth；codex_apps MCP accessible provider 已接入，会从 MCP status tools 的 connector metadata 生成 accessible apps，并兼容 `codex-apps`/`codex_apps` server 名和 Rust `_codex_apps` metadata key，且会透传 icon/color/homepage/docs 等 branding metadata 到 `app/list`；后续继续补真实 MCP hard refresh/长连接 runtime。
- 本轮补充：`list_available_plugins_to_install` connector 候选现在携带并合并来源 `plugin_display_names`，同一 connector 由多个已安装插件提供时不会只保留第一个来源；`request_plugin_install` elicitation metadata 也会透传来源 plugin 名称；补 plugin/tool 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：AppTemplate 派生 connector 候选现在会生成默认 `install_url`（`https://chatgpt.com/apps/<slug>/<connector_id>`），`list_available_plugins_to_install` 与 `request_plugin_install` 对 template connector 和 app connector 的 metadata 不再不一致；补 plugin connector 候选回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：apps `MergeConnectors` 现在在 accessible connector 命中 plugin/directory placeholder 时以 accessible install URL 为准；accessible 未提供时重新生成 ChatGPT apps URL，不再保留插件安装 URL；补 apps merge 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：apps enabled config fallback 现在从 connector 当前状态开始，只有 `_default.enabled` 或 app 级 enabled 明确存在时才覆盖；`[apps]` 存在但无 `_default`/无 app 单项时不会把 disabled connector 翻成 true；补 apps config 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [ ] skills discovery/metadata/toggle。IN_PROGRESS：已补 `SKILL.md` YAML frontmatter 解析、frontmatter scalar repair、`agents/openai.yaml` interface/dependencies/policy metadata 解析，Go 侧按 Rust `serde_yaml` 对应采用 `gopkg.in/yaml.v3`，旧纯 Markdown skill 保持兼容；`skills/list` 顶层 JSON 已收紧为 Rust `data` shape，`skills/config/write` 输出 `effectiveEnabled` 并要求 path/name 恰好一个，`skills/extraRoots/set` 已支持 Rust `extraRoots`；app-server turn runtime 已读取 enabled skills 并转成 prompt metadata，且会检查 enabled skill 的 MCP dependencies，当前 runtime config 未配置对应 MCP server 时发 `warning` notification；installed/enabled plugin 的 `skills/` root 已合并进 turn skills render，并按插件显示名加前缀；policy 保持内部字段不外露；默认 codex roots（`$CODEX_HOME/skills`、`$HOME/.agents/skills`、`$CODEX_HOME/skills/.system`）、repo `.agents/skills` root、`[[skills.config]]` 持久化写入/启用删除 override、`skills/changed` 通知和配置读取过滤已补；selectedCapabilityRoots 中本地可读的 environment capability root 已能作为 standalone skill root 进入 turn skills render；bundled system skills 会从安装资源目录 `codex-resources/skills/.system` 作为 system scope root 发现；本轮补充：`skills/list` 插件 skill 的 JSON 现在透传 `pluginId`，避免来源信息在 API 层丢失；本轮补充：skill MCP dependency 底层判定只把 enabled runtime server 视为已满足，disabled 同配置 server 仍会触发 missing；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`；后续继续扫 skill telemetry/Rust 边界差异。
- 本轮补充：skill frontmatter/openai.yaml metadata 现在兼容 snake/camel/kebab 字段 alias，`shortDescription`、`displayName`、`iconSmall`、`brand-color`、`defaultPrompt`、`allowImplicitInvocation` 等输入形态不会再静默丢失；本地与 remote environment skill resolver 共用同一 alias helper；补 app-server skills 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：skill dependency metadata 现在兼容 `kind/name/server/mcp_server/mcpServer` alias，本地与 remote environment skill metadata 共用解析，MCP dependency warning 不会再因常见字段名不同而漏报；补 app-server skills 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：skill MCP dependency runtime 现在归一化 `streamable-http`/`http`/`sse`/`streamableHttp` 等 URL 型 transport alias，并对 command-only dependency 推断 stdio；missing dependency 与 installed MCP server canonical key 能正确匹配，不再静默跳过 URL 型别名；补 MCP runtime 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：skill MCP dependency missing map 现在统一用裁剪后的 dependency name 做 key 与 warning 格式化，dependency value 带前后空白时不会污染缺失提示或重复缺失项；补 MCP runtime 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：runtime prompt skill metadata 现在透传插件 skill 的 `PluginID`，避免从 `skills/list` 进入 turn metadata 后丢失来源，为后续 telemetry/去重/调试保留插件归属；补 app-server 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：turn runtime 的 model-visible skills prompt 现在优先使用 `shortDescription`，缺失时再回退到长 `description`，减少 skills prompt 预算压力并保留 UI metadata 语义；补 app-server 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：selected capability root 指向已登记 remote environment 时，turn runtime 会通过 exec-server WebSocket 复用 `fs/walk`/`fs/readFile` 读取远端 `SKILL.md` 和可选 `agents/openai.yaml` metadata，解析 frontmatter/interface/dependencies/policy 后注入 available skills；prompt 中远端 skill locator 标记为 `environment:`，本地 `file://`/本地路径保持原 behavior。
- 本轮补充：remote environment skill metadata 的 `iconSmall/iconLarge` 现在会归一为 `environment://<id>/...` locator，不再把原始 `file://` asset path 暴露给客户端；补 remote skill metadata 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [ ] implicit skill invocation。IN_PROGRESS：prompt 层 `InstructionsSkillMetadata` 已带 `AllowImplicitInvocation` 策略并在隐式检测和 turn skills render 时过滤显式禁用的 skill；显式 skill selection/injection 支持结构化 `type=skill` 输入、`$skill-name` 和 `[$skill-name](skill://path)` 解析，app-server turn 会读取选中 `SKILL.md` 并注入 Rust 同形 user `<skill>` fragment；shell `exec_command` 执行后已接隐式 skill 检测，命中时会在下一轮 sampling input items 注入同形 `<skill>` fragment，并发送 `warning` notification，显式/已隐式注入过的 skill 会去重；后续继续补 telemetry 细分字段与 Rust 边界差异。
- 本轮补充：隐式 skill invocation 的工作目录 containment 改为按路径段判断，避免 skill 内部以 `..` 开头的子目录被误判为逃出 skill 根目录，同时保留外部 sibling 目录拒绝；补 prompt 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [ ] prompt injection/budget。IN_PROGRESS：已在 `internal/prompt` 增加 available skills render/budget 基础，支持 Rust 风格 scope/name/path 排序、描述 1024 字符预截断、2% token 预算默认值、预算内描述裁剪与超预算省略报告；app-server turn runtime 已接入 plugin available/explicit instructions，并把普通 skills 与 plugin-prefixed skills render 结果注入 turn 初始 instructions；本轮已把 skills render warning 映射为 app-server `warning` notification；后续继续补 telemetry。
- [ ] connectors/apps mentions/render/tool exposure。IN_PROGRESS：显式 `app://<connector id>` mention 现在会在 turn input items 注入 Rust 风格 `<app>` user fragment；已知 app/connector 会带 id/name/description/installUrl/accessibility/enabled/pluginDisplayNames，未知 connector 会给出基于 ChatGPT apps URL 的占位 installUrl；app-server turn router 已从 MCP status inventory 构造真实 MCP/codex_apps runtime tools，按 `tool_search` discoverable/deferred 表面暴露，保留 inputSchema/readOnlyHint，并用 app enabled/config 状态过滤 disabled connector、synthetic link 与 `modelVisible=false`；后续继续补真实 connector runtime 细节。
- 本轮补充：codex_apps MCP connector metadata 现在解析 `enabled/isEnabled/is_enabled`，MCP 侧声明 disabled 的 connector 会在 app/list 中保持 `isEnabled:false`，并在 apps merge、explicit app instructions 和 turn tool exposure 中作为显式 enabled 状态传播，不会被 legacy `enabled` 默认值翻回 true；补 mcp/apps/app-server 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：codex_apps MCP connector metadata 现在会从 tool `annotations` 读取 fallback，并由 `_meta` 同名/嵌套字段覆盖；connector id/name/branding/plugin_display_names 不再因 server 把扩展 metadata 放在 annotations 而漏掉；补 MCP connector metadata 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：codex_apps synthetic link 过滤现在复用 connector metadata bool 解析，兼容 `codexApps.syntheticLink` 与字符串 true/false，避免 synthetic connector 因字段别名进入 app/list；补 MCP connector 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- 本轮补充：app-server runtime 读取 app enabled 状态已统一尊重 `EnabledExplicit`，explicit app instructions、plugin app injections 和 MCP runtime connector 过滤不会再被 legacy `enabled` 字段把显式 disabled 状态翻回 true；补 app-server 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。

验收：

- [x] MCP/plugin/skills 在 model-visible/discoverable/deferred 三种表面行为一致。
- [x] 继续阶段 9：收口 MCP/plugin/skills 三表面一致性。已把 `internal/turn` 的 MCP runtime tools 接上 `mcp.BuildRuntimeExposure`：`tool_search` 开启时有效 MCP 工具进入 discoverable/deferred，关闭时进入 model-visible direct；`modelVisible=false` 与未启用 connector 的 `codex_apps` 工具不进入任何表面；同时补回归锁住 plugin install 建议工具保持 model-visible、不混入 discoverable，skills prompt/explicit/implicit 表面沿既有测试覆盖。`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：把 app-server turn 真实入口接入 MCP/plugin/skills 三表面。已沿 `RuntimeRouter.toolRouterForTurn` 接入 MCP status inventory、codex_apps connector enablement 和 plugin provenance；app-server turn 不再只依赖预注入 `ToolRouter` 才能看到 MCP discoverable/direct 工具，MCP runtime tool 会保留 `inputSchema`、`readOnlyHint`、`modelVisible` 和 synthetic link 过滤；同步补 app-server router 回归源码，覆盖普通 MCP、hidden tool、enabled/disabled codex_apps connector 与 plugin provenance；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。

## 阶段 10：Sandbox、Exec-Server、Network Proxy

目标：对齐 Rust `sandboxing`、`exec-server`、`execpolicy`、`network-proxy`、`windows-sandbox-rs`、`linux-sandbox`。

- [x] permission profile 基础。
- [x] execpolicy 基础。
- [x] network proxy 骨架。
- [x] Linux/Windows sandbox 外围入口与 CLI 安全边界。已补 arg0 helper alias、`codex-linux-sandbox`/`codex-execve-wrapper` dispatch stub、`codex sandbox` full-access 直通与 read-only/workspace 明确拒绝真实后端缺失，避免静默全权限执行。
- [x] Windows sandbox Rust 对应 Go 目录骨架。已按 `windows-sandbox-rs/src` 新建 `internal/sandbox/windowssandbox` 及 `bin/setup_main`、`bin/command_runner`、`conpty`、`elevated`、`unified_exec/backends`、`wfp` 子包，入口先保持 explicit unsupported stub，方便逐模块移植。
- [x] Windows sandbox 纯逻辑移植首批。已补 wrapper 参数构造/解析、permission profile 到 token mode/网络策略/写根决策、deny-read exact/glob 快照展开、Windows argv quoting、setup_error 报告读写与用户名脱敏、canonical path key；真实 token/ACL/WFP/native spawn 仍保持 explicit unsupported。
- [x] Windows sandbox setup/helper/logging 基础。已对齐 `.sandbox`/`.sandbox-bin`/`.sandbox-secrets` 布局、setup marker/sandbox users 路径、helper materialization copy/reuse/freshness/resource lookup、daily sandbox log、git safe.directory 注入。
- [x] Windows sandbox setup payload 纯逻辑。已补 proxy env loopback port 解析、offline/online sandbox identity、setup marker proxy mismatch、read/write root gather/effective filtering、profile root 展开与敏感目录过滤、SSH config dependency parser、setup complete marker/users 版本检查。
- [x] Windows sandbox DPAPI/SID/token 基础。Windows 上已接 `CryptProtectData`/`CryptUnprotectData` round-trip、SID 字符串和原始字节互转、当前进程 token 打开/关闭；非 Windows 平台继续返回 explicit unsupported。
- [x] Windows sandbox ACL/capability/deny-read/desktop 基础。已接 capability SID JSON 持久化、workspace/root scoped SID、Windows DACL allow/deny ACE、NUL device allow、deny-read ACL apply/materialize/state reconcile、workspace `.codex`/`.agents` deny-write、private desktop create/grant/close；Rust `serde_json`/`rand` 对应 Go 侧已改用 `github.com/goccy/go-json` 与 `github.com/google/uuid`。
- [ ] read-only/workspace-write/full-access 完整策略。
- [x] Windows restricted token/native spawn/WFP。restricted token 创建已按 Rust flags 接入 `CreateRestrictedToken`、logon/everyone/capability restricting SIDs、default DACL 和 `SeChangeNotifyPrivilege`；当前测试机对 `WRITE_RESTRICTED` 返回 `ERROR_INVALID_PARAMETER(87)` 时按 host unsupported 路径显式跳过 restricted 真机验证。native `CreateProcessAsUserW` spawn/stdio pipes、WFP setup、elevated setup refresh、command-runner IPC/ConPTY 与真机 elevated smoke 已接通。
- [ ] Linux bwrap/landlock。
- [ ] exec-server protocol。IN_PROGRESS：`fs/copy` 现在使用 `Lstat` 判别源类型，目录复制保留 symlink、跳过特殊文件，单独复制特殊文件返回 Rust 同类错误，避免跟随 symlink 复制实际内容；`fs/remove` 默认 `recursive=true/force=true`；`fs/writeFile` invalid base64 文案已对齐；`fs/walk` 空结果输出 required empty arrays；`process/start` 已支持 `arg0` 覆盖子进程 argv[0]；真实 sandbox/managed network backend 接入前，`process/start` 会拒绝对应请求而不是静默全权限执行；`http/request` 会跳过非法 header name/value 并稳定排序响应 headers，`streamResponse` 在真实通知流接入前显式拒绝；`fs/getMetadata` 在 Windows 会填充 `createdAtMs`，Unix 保持 0；JSON-RPC request/notification 判定改为顶层 `id` envelope，unknown method/decode params 已映射 `-32601/-32602`；继续扫 process/http/fs 剩余协议差异。
- [ ] PTY/process lifecycle/cancel/stdout/stderr streaming。IN_PROGRESS：stdout/stderr streaming、command stdin/write/terminate 与最终 response 基础已对齐；真实 PTY backend/resize 已接入，Unix 使用 `github.com/creack/pty`，Windows 使用 `golang.org/x/sys/windows` ConPTY；本轮已补 Windows Job Object 进程树清理和 PTY size 溢出保护；剩余继续扫 sandbox/exec-server 深层联动。
- [ ] execpolicy command check/warning/error 格式。
- [ ] network credential broker/MITM/upstream/certs/policy reload。

验收：

- [ ] sandbox/approval 与工具执行链真实联动。
- [ ] 平台差异封装在现有包内。

## 阶段 11：TUI 与交互体验

目标：对齐 Rust `tui` 的主要交互流程。

- [ ] 选择 Go TUI 技术栈。
- [ ] app shell。
- [ ] chat composer。
- [ ] bottom pane。
- [ ] status/footer。
- [ ] message queue/pending input。
- [ ] approval modal。
- [ ] MCP elicitation。
- [ ] model picker。
- [ ] session picker。
- [ ] hooks browser。
- [ ] feedback view。
- [ ] slash commands。
- [ ] image attachment。
- [ ] interrupt/cancel。
- [ ] snapshot/golden 测试。

验收：

- [ ] 交互 CLI 默认入口可用。
- [ ] 快捷键、状态提示、队列、审批流程与 Rust 保持一致。

## 阶段 12：Daemon、Remote Control、SDK 集成

目标：对齐 Rust app-server daemon、remote control、SDK 集成边界。

- [x] appserverdaemon 基础。
- [x] remotecontrol 基础。
- [ ] daemon bootstrap/start/restart/stop/version。
- [ ] remote-control enable/disable。
- [ ] control socket/connection file watch。
- [ ] pairing。
- [ ] capability token。
- [ ] signed bearer token。
- [ ] Python SDK contract。
- [ ] TypeScript SDK contract。
- [ ] desktop app launch/install hook。

验收：

- [ ] 本地 app-server 可被 SDK/IDE 稳定启动、连接、控制。

## 阶段 13：统一测试和修复

注意：这一阶段之前不运行 `go test`。

- [ ] `go list -buildvcs=false ./...`。
- [ ] `gofmt` 全仓格式化。
- [ ] `go test ./...`。
- [ ] 分包修复编译错误。
- [ ] 分包修复单测错误。
- [ ] 对照 Rust fixtures 补黄金测试。
- [ ] CLI 端到端测试。
- [ ] app-server 端到端测试。
- [ ] Windows 专项：PowerShell、路径、sandbox、ACL、WFP。
- [ ] mock server 网络测试。

## 工作日志

### 2026-07-02

- [x] 继续阶段 9：补 MCP OAuth discovery 与 login URL 对齐 Rust 行为，优先发现 authorization server metadata，支持 protected resource metadata 跳转和 scopes 归一化；本轮同步写单元测试代码，已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth authorization code exchange 与 refresh token 底层能力，按 Rust token 存储形状生成 `OAuthTokenSet`；新增 `golang.org/x/oauth2 v0.30.0` 依赖，authorization URL/code exchange/refresh 均走 OAuth2 库语义；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：把 MCP HTTP client 请求前 OAuth refresh 接入真实调用链，过期 token 有 refresh token 时自动发现 token endpoint、刷新、保存并使用新 access token；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth login session helper，包含 PKCE/state authorization URL、callback parse/path/id helper、callback state 校验和 code exchange 串联；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP stdio/http client initialize 参数统一和 stdio server request responder，stdio 等待响应时可处理 roots/list、elicitation/create、openai/form 并继续读取目标响应；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 补充编码规范：Rust 版本使用第三方库的场景，Go 版本必须优先寻找并采用对应成熟 Go 第三方库，避免重新造轮子；OAuth2 后续改用 `golang.org/x/oauth2` 这类成熟库推进。
- [x] 继续阶段 9：补 MCP resource read TTL/LRU 缓存与远端 JSON-RPC error 结构化映射。新增 `MCPResourceCache`，`MCPService.ReadResource` 按 server/uri 命中缓存并深拷贝响应，server/config 变化时清缓存；HTTP/stdio RPC error 统一返回 `MCPRemoteError`，保留 method/code/message/data；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP stdio elicitation handler 注入链路。新增 `MCPElicitationHandler`/request/response/action 类型，stdio client 收到 `elicitation/create` 或 `openai/form` 时可交给 handler 返回 accept/decline/cancel，无 handler 保持 cancel；`MCPService` 可设置 handler 并传入 stdio tool/resource 调用；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace source 解析。新增 `ParseMarketplaceSource`，支持 GitHub `owner/repo` shorthand、`@ref`/`#ref`、Git URL/SSH URL、本地目录解析，拒绝 file URL、本地文件和 local+sparse；`AddMarketplace` 使用解析结果生成规范 source/root/ref/sparse metadata；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin install/uninstall 策略细节。`plugin/install` 在非 `pluginId` 兼容路径下要求 marketplace/local 与 remote 来源恰好一个，`InstallBlocked` 插件拒绝安装，`ON_INSTALL` 返回 bundled appsNeedingAuth，未知插件返回 invalid plugin request；`plugin/uninstall` 同步清 installed/enabled；同步写单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills YAML discovery metadata。`SkillsService` 现在解析 `SKILL.md` YAML frontmatter、兼容 Rust 的 scalar repair、读取可选 `agents/openai.yaml` 的 interface 与 dependencies；新增 `gopkg.in/yaml.v3 v3.0.1` 依赖，按 Rust 第三方库语义不手搓 YAML；同步写 app-server skills 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills policy 与隐式调用过滤基础。`agents/openai.yaml` 的 `policy.allow_implicit_invocation`/`products` 已解析为内部 `SkillPolicy`，`skills/list` 顶层 JSON 去掉旧兼容 `skills` 字段只输出 Rust `data`，prompt 隐式 skill 检测会跳过显式关闭 implicit invocation 的 skill；同步写 app-server/prompt 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 skills RPC wire shape。`skills/extraRoots/set` 参数支持 Rust `extraRoots` 并保留旧 `roots` 兼容；`skills/config/write` 改为 `enabled` 标量、path/name 恰好一个，并返回 Rust `effectiveEnabled`，旧 `updated` 仅保留 Go 内部字段不出 JSON；同步更新单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills prompt render/budget 基础。`internal/prompt` 新增 available skills 渲染、scope/name/path 排序、描述默认 1024 字符上限、2% context token 预算、预算内描述裁剪、超预算省略报告与 warning 文案；同步写 prompt 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace git materializer 基础。新增 `MarketplaceMaterializer` 抽象和默认 `GitMarketplaceMaterializer`，git source 会按 Rust 语义调用 git CLI clone/checkout/sparse-checkout 到 `.tmp/marketplaces/<name>`，测试改用 fake materializer 避免公网依赖；同步写 plugin/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace config 持久化基础。`PluginService.SetCodexHome` 会设置 `.tmp/marketplaces` install root 与 `config.toml`；`marketplace/add` 成功后写入 `[marketplaces.<name>]` 的 `last_updated/source_type/source/ref/sparse_paths`，`marketplace/remove` 同步删除配置；默认 app-server router 已把 codex home 接入 plugin service；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace upgrade revision 基础。新增 `MarketplaceRevisionResolver` 与默认 git `ls-remote` 实现，`UpgradeMarketplace` 现在只选择 git marketplace，revision 未变时跳过，revision 变化时通过 `MarketplaceUpgrader`/materializer 刷新 root 并写回 `last_revision`；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace root 插件发现基础。`plugin/list` 和 `plugin/read` 现在会从 marketplace root 的 `.agents/plugins/marketplace.json`、`.agents/plugins/api_marketplace.json`、`.claude-plugin/marketplace.json` 发现插件，读取本地 `.codex-plugin/plugin.json`、skills 目录和 `.mcp.json` server 摘要，并合并到现有 stored plugin 结果；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplacePath install/read 与 runtime plugin instructions。`plugin/read`/`plugin/install` 现在支持 Rust canonical `marketplacePath + pluginName`，install 可把 marketplace manifest 中发现的本地插件写回 installed/enabled 状态；enabled capability 的 `configName` 对齐为本地 plugin id；app-server turn runtime 会注入 Rust 风格 available plugin instructions，并在显式 `plugin://<plugin id>` mention 时加入 plugin-associated skills/MCP/apps developer instructions；同步写 plugin/context/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills render 接入 app-server turn runtime。`InstructionsSkillMetadata` 补回 description 字段，app-server turn 会从 `SkillsService.List` 读取 enabled skills，转换为 prompt metadata，按模型 context window 的 2% 预算渲染 Rust 风格 `## Skills` instructions，并过滤 `allow_implicit_invocation=false` 的 skill；同步写 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin skill roots 注入 skills render。`PluginService.EnabledSkillRoots` 会返回 installed/enabled 插件的 `skills/` root，app-server turn runtime 复用 skills discovery 合并这些 root，并给 skill 名称加插件显示名前缀后进入 `## Skills` prompt；同步扩展 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补推荐未安装插件与 `request_plugin_install` 工具基础。`PluginService.DiscoverableInstallCandidates` 会从 stored/marketplace details 合并可安装候选；app-server turn runtime 会注入 Rust 风格 `<recommended_plugins>` user context，并在 tool registry 中暴露推荐上下文 `request_plugin_install`；普通 registry 也支持 `list_available_plugins_to_install` + `request_plugin_install` 基础 handler；同步写 plugin/context/tool/turn/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 `request_plugin_install` elicitation/install 闭环基础。tool handler 现在生成 Rust 同形 `tool_suggestion` metadata、`request_plugin_install_<call_id>` suggestion id 和 requested schema；app-server runtime 通过 `mcpServer/elicitation/request` 发往 `codex_apps`，收到 accept 后调用 `PluginService.Install`，decline 会识别 `persist=always` 但配置持久化仍待后续；同步写 tool/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 `request_plugin_install` decline persist。app-server plugin install runtime 在用户 decline 且 `_meta.persist=always` 时通过 `ConfigService.BatchWrite` 去重写入 `tool_suggest.disabled_tools = [{ type = "plugin", id = ... }]`，不手写 TOML；同步扩展 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 `request_plugin_install` connector 候选基础。`DiscoverableInfo` 增加 `toolType/installUrl`，list-tool 输出支持 `tool_type=connector`，request handler 会校验传回的 tool_type 并生成 connector `install_url` metadata；app-server runtime 对 connector accept 只返回 `user_confirmed=true`、不调用 plugin install，等待后续真实 connector refresh/verification；同步写 tool/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP runtime tool plugin provenance 底层。`RuntimeToolInfo` 增加 `pluginDisplayNames`，新增 `AnnotateRuntimeToolsWithConnectorPluginProvenance`/`AnnotateRuntimeToolWithPluginProvenance`，会去重排序插件名并按 Rust 文案追加到 tool description；`ConnectorToolInfoFromRuntimeTools` 可把 provenance 回灌到 connector 可访问状态；同步写 MCP 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 `request_plugin_install` connector completed 验证入口。app-server plugin install runtime 在 connector accept 后会查询 `AppService`，若同 ID app 已 accessible 则返回 `completed=true`，否则保持 false；同步扩展 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 app mention 到 plugin instructions 的反向关联。`CollectExplicitPluginMentions` 现在会收集显式 `app://<connector id>` mention，并通过 enabled plugin capability 的 `AppConnectors` 匹配关联 plugin，从而触发 plugin-associated instructions；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills roots 与 config toggle 持久化。`ConfigService` 新增 `WriteSkillConfig`，能按 Rust 语义写入/删除 `[[skills.config]]` name/path override，并让 TOML 渲染器支持数组表；`SkillsService` 新增 pointer options、默认 codex/user/system roots、repo `.agents/skills` roots、持久化 config 读取过滤和 `skills/changed` 通知；同步写 config/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补显式 skill selection/injection。`internal/prompt` 新增 Rust 风格 skill mention selector，支持结构化 `type=skill`、`$skill-name`、`[$skill-name](skill://path)`，避免重复名和 connector slug 冲突；`internal/context` 新增 user-role `<skill>` fragment；app-server turn runtime 会读取选中 `SKILL.md` 并作为 input item 注入模型请求；同步写 prompt/context/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 skills prompt budget warning notification。app-server turn runtime 现在会把 `RenderAvailableSkills` 产生的超预算/描述裁剪 warning 映射为 Rust app-server `warning` notification，并带上 threadId；同步写 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 selectedCapabilityRoots 本地 skill root 接入。app-server turn runtime 会从 thread metadata 解析 `selectedCapabilityRoots`，对本地可读的 environment capability root 复用 `SkillsService` discovery，加入 available skills render，并处理 Windows `file:///D:/...` URI 归一化；远端 environment-native FS 读取仍留后续；同步写 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 Codex Apps/app exposure 合并核心。`AppService` 现在支持 directory/accessible provider 缓存、`forceRefetch` 绕过缓存、plugin connector placeholder、Rust 风格 connector install URL、accessible merge/filter 和 `[apps]` enabled 覆盖；`app/list` 会把 enabled plugin 的 `AppConnectors` 与当前 config 注入 AppService；同步写 apps 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 ChatGPT directory connector 拉取。新增 `ChatGPTDirectoryProvider`，按 Rust 路径分页读取 `/connectors/directory/list?external_logos=true`，workspace 账号追加读取 `/connectors/directory/list_workspace?external_logos=true`，过滤 `visibility=HIDDEN`、合并重复目录 app、规范化 name/description/installUrl；app-server `app/list` 会在 ChatGPT/Codex backend auth 可用时配置该 provider 并复用现有 HTTP client；同步写 apps/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 codex_apps MCP accessible provider。`app/list` 现在会把 MCP status 中 `codex_apps`/`codex-apps` tools 的 connector metadata 转成 accessible apps，走现有 `AppService` 合并 directory/plugin/config 状态；新增 MCP metadata 解析 helper，支持 nested `codex_apps`、camelCase/snake_case 字段、plugin display names 和 synthetic link 过滤；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP runtime config reload 基础。新增 `mcp.RuntimeConfigFromValues` 解析 `[mcp_servers]`、`features.apps`、`apps.enabled`、`chatgpt_base_url`、`apps_mcp_product_sku`，默认按 Rust stable feature 启用内置 `codex_apps`；`MCPService.ApplyRuntimeConfig` 可整体替换 server/config/required 状态，`config/mcpServer/reload` 会重新读 config 并应用，`mcpServer/refresh`/forceRefetch 会清资源缓存；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 app-server MCP elicitation bridge。新增 `appserverMCPElicitationHandler`，把 MCP stdio/http client 的 elicitation 请求转换成 Rust app-server `mcpServer/elicitation/request` server request，经 broker 收到 accept/decline/cancel 后映射回 `mcp.MCPElicitationResponse`；`requireMCP` 会自动安装该 handler；同步写 app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP elicitation thread context 深传。`MCPElicitationRequest` 增加 threadId/turnId，stdio MCP client 会通过 context 把 app-server `mcpServer/tool/call`/resource read 的 threadId 带到服务端 elicitation 请求，app-server bridge 优先使用 request 上的 thread/turn context，meta 仅作兼容兜底；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 `_codex_apps` metadata 与 URL elicitation 兼容。connector accessible 解析已兼容 Rust `_codex_apps` key 并继续支持 synthetic link 过滤；app-server MCP elicitation params 对 URL 请求固定输出 `mode=url`，缺少 elicitationId 时从 JSON-RPC id 兜底；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 HTTP streamable MCP SSE server request 处理。HTTP client 现在在 SSE `data:` 中识别带 method/id 的 JSON-RPC server request，复用 `mcpClientRequestResult` 处理 roots/list 和 elicitation，并向同一 MCP endpoint POST JSON-RPC response，再继续读取原目标 response；HTTP tool/resource 调用会传入 serverName/threadId/elicitation handler；同步写 HTTP MCP 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP progress notification 通路。新增 `MCPProgressHandler`/`MCPProgressNotification`，stdio 与 HTTP SSE client 等待目标响应时会识别 `notifications/progress`，携带 serverName/threadId/progressToken/progress/total/message/原始 params 回调上层，并继续读取原请求结果；同步写 MCP 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP HTTP streamable session 复用。`MCPService` 新增 server/config fingerprint 级 HTTP client cache，`mcpServer/status/list` inventory、`mcpServer/tool/call` 和 `mcpResource/read` 会复用同一 initialized session；每次调用用 pointer options 注入 thread/elicitation/progress context，`mcpServer/refresh` 与 config reload/change 会清空旧 session；同步写 MCP 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP progress app-server bridge。`MCPToolCallParams` 增加可选 turnId/itemId，turn registry 会把 thread/turn context 传给 MCP tool executor，executor 用 invocation callId 作为 itemId；stdio/HTTP progress notification 会带上 thread/turn/item，app-server `requireMCP` 安装 progress handler 并映射为 `item/mcpToolCall/progress`；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth dynamic client registration 底层。OAuth discovery 现在保留 `registration_endpoint`；新增 `OAuthClientRegistrar`，按动态注册 endpoint POST public client metadata（redirect_uris、grant_types、response_types、scope、token_endpoint_auth_method），解析 client_id/client_secret；`NewOAuthLoginSessionWithClientRegistration` 可在缺少 client_id 时先注册再生成 PKCE authorization URL；同步写 MCP OAuth 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth browser callback server 核心。新增 `StartOAuthLoginServer`，会绑定本地 127.0.0.1 callback listener、按真实端口生成 redirect URL、构造/动态注册 PKCE login session、处理 callback code/provider error/state 校验、调用 token exchange、保存 `OAuthStore`，并提供 `/success` 与 `/cancel`；同步写 MCP OAuth 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：把 MCP OAuth login server 接到 `MCPService.OauthLogin`。当 server config 有 URL/codexHome 且 OAuth discovery 成功时，`OauthLogin` 会启动本地 callback server、返回带真实 redirect_uri/state/PKCE 的 authorizationUrl，callback 完成后保存 token 并清理 active login/HTTP session cache；条件不足时保持旧 fallback URL；同步写 MCP OAuth service 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth login completed notification。新增 `MCPOAuthLoginCompletionHandler`，`MCPService` 在本地 callback server 完成/失败后带 name/threadId/success/error 回调；app-server `requireMCP` 安装 completion handler，并映射为 Rust wire 名 `mcpServer/oauthLogin/completed`；同步写 MCP/app-server 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP CLI login 打开浏览器。`codex mcp login` 现在复用已有 `auth.OpenBrowser` 打开 `MCPService.OauthLogin` 返回的 authorizationUrl，失败时保留手动打开 URL 文案；测试通过可替换 opener 避免真实 GUI；同步更新 MCP CLI 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补远端 marketplace 插件 materialize/read。marketplace loader 会保留远端插件 git source 占位信息；`PluginService.Read`/`Install` 在遇到远端插件时通过可注入 `MarketplacePluginMaterializer` materialize 到 install root 下的 `plugins/<marketplace>/<plugin>`，再读取真实 `.codex-plugin/plugin.json`、skills 与 MCP server 摘要；默认 git materializer 复用现有 clone/checkout 逻辑；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin install 状态持久化基础。`Install` 会把 installed/enabled、marketplace/name/path/source/ref/sha 写入 `config.toml` 的 `[plugins.<id>]`，`Uninstall` 会清理对应配置；`SetCodexHome`/`SetMarketplaceConfigPath` 会 reload 已保存 marketplace 与 installed plugin，并从本地 materialized plugin root 重新读取 manifest/skills/MCP 摘要；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 remove marketplace 的 plugin cleanup。`RemoveMarketplace` 现在会同步删除同 marketplace 的内存 installed plugin、`config.toml` `[plugins]` 条目，并清理 install root 下已 materialize 的远端插件目录；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP stdio client session 复用。`stdioClient` 现在 initialize 后保持进程，`MCPService` 增加 stdio client cache，status/tool/resource 会复用同一 stdio session；`Refresh`、runtime reload 和 server config change 会关闭旧 stdio 进程；单次调用仍有 15s timeout，超时会 kill 当前进程并让下次调用重建；同步写 MCP stdio 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP stdio transport error recovery。stdio JSON-RPC remote error 现在保持 session 不关闭；IO/坏帧/进程退出等 transport 错误会关闭坏 session，并对当前请求尝试一次重新 initialize 后重试；timeout/cancel 不重试；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth cancel RPC。新增 `MCPService.OauthCancel` 与 app-server `mcpServer/oauth/cancel`，取消 active OAuth login 会关闭本地 callback server，并复用已有 `mcpServer/oauthLogin/completed` 失败通知；schema/alias 已同步；补充 MCP OAuth 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth active login 生命周期清理。`ApplyRuntimeConfig` 和 `SetServerConfig` 现在会取消旧 active OAuth login，避免 runtime reload/config change 后留下旧 callback server；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin/skill/read 真实文件读取。`PluginService.ReadSkill` 现在会从已安装/stored/marketplace plugin detail 定位 plugin root，在 `skills/<name>/SKILL.md`、`skills/SKILL.md` 或嵌套 skill 文件中读取真实内容；未找到时保留旧占位响应兼容；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 9：补 marketplace plugin skills 真实列表。marketplace loader 现在扫描 `skills/**/SKILL.md`，优先使用 skill frontmatter `name`，为每个 skill 返回 path/enabled，而不是只返回一个粗略 plugin-root skill；同步写 plugin 单元测试代码；已运行 `gofmt` 和带工作区 `GOMODCACHE`/`GOCACHE` 的 `go list -buildvcs=false ./...`，按要求未运行 `go test`。

### 2026-07-01

- [x] 继续阶段 6：补 plan mode `<proposed_plan>` streaming parser 与最终 plan item history；新增 `internal/appserver/plan_stream.go`，plan mode 下支持 proposed_plan tag 跨 SSE delta 拆分、剥离 agent delta 中的 plan 块、发送 plan item started 和 `item/plan/delta`，turn 完成后保存 `plan` item 与可见 agent message；补充 app-server plan mode streaming 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 Responses stream 到 app-server raw/reasoning/plan notification 贯通；`internal/model` 识别 raw `output_item.done`、reasoning summary/text delta、reasoning summary part added 和直接 plan delta，`internal/appserver` 发送 `rawResponseItem/completed`、`item/reasoning/*`、`item/plan/delta`，reasoning item 的 data/raw 进入 app-server 与 exec session 持久化；补充 app-server streaming 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/settings/update` 成功后的 `thread/settings/updated` notification，ThreadExtraService 增加 settings 快照读取并由 RuntimeRouter 发送 Rust 同形通知；同步补 appserver 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 app-server `initialize` 的 `configWarning` notification 发送路径，给 `ConfigService` 增加启动 warning 存储/克隆接口，RuntimeRouter 初始化后发送 Rust 同形 config warning；同步补 config/appserver 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `update_plan` 工具到 Rust `turn/plan/updated` 通知的 runtime 映射，收紧 `TurnPlanUpdatedNotification` payload 为 `threadId/turnId/explanation/plan[]`，`PlanHandler` 输出结构化 plan 元数据，runtime 根据 tool execution 发送 `turn/plan/updated`；同步补 tool/appserver 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/realtime/*` RPC 的 runtime notification 转发，按 Rust app-server 把 realtime manager 事件映射到 `thread/realtime/started`、`sdp`、`closed` 等通知；有 thread store 时 realtime RPC 会先校验 thread，未知线程按 Rust 返回 `thread not found`/`-32004`；同步补 runtime router 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 MCP streamable HTTP 初始化握手，HTTP client 先发送 `initialize`，携带 `Mcp-Session-Id` 发送 `notifications/initialized` 和后续请求；补 HTTP MCP 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 2/6：补 app-server runtime 的 `turn/diff/updated` 发送路径：`apply_patch` executor 现在输出 committed `appliedChanges` old/new 内容，runtime 复用已有 `internal/runtimeutil.DiffTracker` 按 active turn 聚合并在成功 fileChange tool output 后发送 turn-level diff；补 applypatch/tool/appserver 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补 Responses streaming metadata 到 app-server model 通知映射：model SSE `response.metadata` 会解析 reroute、verification、turn moderation metadata、safety buffering，并由 RuntimeRouter 发送 `model/rerouted`、`model/verification`、`turn/moderationMetadata`、`model/safetyBuffering/updated`；补 model/appserver 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 MCP streamable HTTP 的 `text/event-stream` JSON-RPC response 解析，支持从 SSE `data:` 中读取匹配 request id 的 result/error；补 HTTP MCP 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 `mcpServer/oauth/login` 的 `threadId` 门禁，提供未知线程时按 Rust 返回 `thread not found`/`-32004`；补 runtime router 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 `mcpServerStatus/list` 的 `threadId` 门禁，提供未知线程时按 Rust 返回 `thread not found`/`-32004`；补 runtime router 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 app-server MCP tool/resource RPC 的 `threadId` 门禁，提供 threadId 时先验证线程存在，未知线程按 Rust 返回 `thread not found`/`-32004`；补 runtime router 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 MCP tool call response 的 `isError` optional bool 语义，保留 Rust/JSON wire 上明确的 `false`，并同步更新 tool executor 判断与单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：补齐 MCP `mcpServer/tool/call` 对 Rust 的 `_meta.threadId` 合入与 HTTP/stdio 转发语义；`MCPService.CallTool` 已改为指针参数，HTTP/stdio tools/call wire params 会携带合并后的 `_meta`，并补充 MCP meta/HTTP/stdio 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 5/6：本轮修正 fork dynamic tools override 与 metadata raw JSON 深拷贝；rollout/history 恢复出的 Rust union `mcpToolCall`、`dynamicToolCall`、`fileChange`、`commandExecution` 会归一化为 app-server thread item mapper 可识别的 marker/data，并补充 rollout/appserver/session 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 fileChange streaming patchUpdated 的 diff 语义，Go runtime 的 apply_patch 增量通知现在对 delete hunk 输出空 diff，保留最终 apply_patch tool output 的 delete 内容 diff；补充通知层单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。
- [x] 继续阶段 6：对齐 `environment/add` 与 `environment/info` 的 JSON-RPC 错误码，环境管理层错误现在按 Rust processor 映射为 `invalid_request(-32600)`；补充 runtime router 单元测试代码；已运行 `gofmt` 和 `go list -buildvcs=false ./...`，按要求未运行 `go test`。

### 2026-06-30

- [x] 根据用户要求，把原来的路线图式 `plan.md` 重做为 TODO 进度账本文档。
- [x] 把已完成基础能力、近期最高优先级、13 个阶段的待办和验收项写入文档。
- [x] 明确后续每轮开发必须更新本文件。
- [x] 追加用户强调的编码规范：结构体优先指针、模块化迁移、先写完整再统一测试、按功能归包、不偏离继续写代码主线。
- [x] 开始阶段 2：app-server turn runtime 接入。先阅读 Rust/Go turn 相关实现，再实现 Go 侧 runtime builder 和异步执行。
- [x] 在 `internal/appserver` 新增 `turn_runtime.go`，把 `turn/start` 后台接入 `turn.Runtime`，写入 session item，并发送 turn/item/thread status 通知。
- [x] 扩展 `RuntimeServices`，支持注入 `model.AgentRunner`、`tool.Router`、`turn.Runtime` 和 `ThreadStatusManager`。
- [x] 为 app-server 异步 turn 补充单元测试文件内容，验证 runtime 落盘和通知；按要求未运行 `go test`。
- [x] 本轮运行 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 开始继续阶段 2：补 app-server active turn 跟踪、interrupt 取消后台 runtime、同 thread 防并发。
- [x] 完成 app-server active turn registry：`turn/start` 先占位防并发，runtime 正式启动后绑定 cancel，`turn/interrupt` 取消后台 context 并发送 interrupted completed。
- [x] 补充 app-server interrupt/concurrent start 单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 开始阶段 3：在 `internal/model` 增加 Responses SSE streaming runner 基础，默认不改变非流式路径。
- [x] 新增 streaming SSE parser、streaming `Run` 分支、stream event handler，并补充本地 SSE 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 把 Responses streaming handler 接入 app-server turn runtime：`output_item.added` 映射 `item/started`，`output_text.delta` 映射 `item/agentMessage/delta`，tool input delta 初步映射为 tool progress notification。
- [x] 补充 app-server streaming delta notification 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 给 exec JSONL 协议增加 `item.delta` 事件，并把 Responses streaming output/tool input delta 收集进 exec 结果事件序列。
- [x] 补充 protocol delta JSON shape 与 exec stream collector 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 给 Responses streaming runner 增加 `response.headers` 事件，解析 `x-request-id`、`openai-model`、`x-codex-turn-state`、`x-models-etag` 并保留规范化 header map。
- [x] 补充 Responses streaming header metadata 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 把 `codex exec --json` streaming events 改为通过 exec event sink 边接收边写 stdout，同时继续保留 `Result.Events` 收集。
- [x] 补充 `codex exec --json` streaming delta 即时输出单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 给 Responses streaming runner 增加 server model、rate-limit snapshots、models etag、server reasoning included 专用事件，并支持从 `response.metadata` headers 去重更新 server model。
- [x] 补充 Responses streaming rate-limit/model metadata 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 推进 provider catalog 对齐：`internal/model` 可解析 Rust `models-manager/models.json` 的模型字段形状，兼容 `visibility=list/hide`、reasoning level 对象、service tier 对象，并优先加载本机 Rust bundled catalog。
- [x] 补充 Rust catalog JSON shape 与 picker 默认模型选择单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 provider catalog remote 基础：新增 `ModelsEndpoint`、`HTTPModelsEndpoint`、`RemoteModelsManager`，支持 `/models`、`client_version` query、auth/provider headers、ETag、304、`online_if_uncached` 内存刷新、远程模型按 slug 覆盖/追加。
- [x] `ConfiguredProvider.ModelsManager` 默认接入 remote manager；显式 config catalog 仍使用 static manager，Bedrock 继续使用 Bedrock static catalog。
- [x] 补充 remote manager、HTTP `/models` endpoint、provider manager 选择单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 Responses retry/timeout 基础：`APIProvider` 带上 `StreamMaxRetries`，`ResponsesAgentRunner` 非流/流式请求统一重建 request 并重试 429、5xx、transport error，支持 `Retry-After`、指数 backoff 和 streaming idle timeout reader。
- [x] 补充 Responses retry 与 streaming retry/idle timeout 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 Responses `store`/continuation 字段基础：`AgentRequest` 与 `AgentLoopRequest` 增加并透传 `Store`、`PreviousResponseID`、`ServiceTier`、`PromptCacheKey`、`ClientMetadata`，Responses JSON body 输出 `store`、`previous_response_id`、`service_tier`、`prompt_cache_key`、`client_metadata`。
- [x] app-server turn runtime 已把 `serviceTier` 参数传入模型请求；补充 Responses request body 与 AgentLoop 透传单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 Responses response debug context 基础：新增 `ResponsesAPIError`，HTTP 错误保留 request id、cf-ray、authorization error、`x-error-json` code 和 body/message，非流与流式错误路径共用该错误构建。
- [x] 补充 API error debug context 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 推进 OpenAI provider config/auth：`APIProvider` 现在携带 provider command auth 配置，`ResolveProviderAuth` 支持 `auth.command`，可解析纯文本 token、`authorization`、`token`、`access_token`、`bearer_token` JSON 输出；Responses 401 retry 前会重新执行 command auth 刷新 Authorization。
- [x] 补充 provider command auth 与 401 refresh 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 OSS/Ollama/LM Studio provider 基础接入：新增 OSS provider ID 判断与静态 OSS 模型 catalog，`CreateRuntimeProviderForID` 保留 providerID，exec Responses runner 创建 runtime provider 时传入 providerID，OSS provider 默认不走 `/models` remote manager。
- [x] 补充 OSS runtime provider 静态 catalog 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 Bedrock/API key auth 基础接入：`AmazonBedrockProvider.APIAuth` 现在会保留 mantle client header，并从托管 `bedrock_api_key` 或 `AWS_BEARER_TOKEN_BEDROCK` 生成 bearer Authorization；AWS credential/SigV4 后续在阶段 7 补。
- [x] 补充 Bedrock managed API key 与环境变量 bearer fallback 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 `exec --output-schema` structured output 基础接入：`loadOutputSchema` 返回 JSON 值，`agentRunConfig`/`AgentLoopRequest`/`AgentRequest` 贯通 schema，Responses request 输出 `text.format` 的 Rust 同形状 json_schema 约束。
- [x] 补充 Responses text.format、AgentLoop 透传、exec 读取 schema 透传单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 推进 OpenAI provider config/auth：`RemoteModelsManager` 新增指针式 options 和 ChatGPT auth 远端 catalog source-of-truth 策略；ChatGPT/个人访问 token auth 且远端返回 `visibility=list` 模型时使用远端模型列表替代 bundled，API key/hidden-only 远端仍保持 merge。
- [x] 补充 remote catalog source-of-truth、hidden-only merge、provider auth 模式策略单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 完成 OpenAI provider config/auth 基础闭环：新增 ChatGPT refresh token 刷新函数与持久化合并逻辑；Responses runner 在 401 retry 前会优先刷新 ChatGPT auth，并更新 Authorization/ChatGPT-Account-ID 后重试。
- [x] 补充 ChatGPT token refresh 与 Responses 401 refresh retry 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 开始阶段 5：补 `session`/`rollout` 的 meta、thread item schema 和 JSONL 字段对齐；本轮先做结构化历史持久化与 resume input 重建，不运行 `go test`。
- [x] 扩展 `session.Metadata`/`session.Item`，支持 response id、cli version、agent 信息、service tier、sandbox/approval、content/data/raw/call id/name/namespace 等 Rust 对齐字段。
- [x] 新增 `session` 历史重建 helper，把 session items 转为 Responses input items；`exec resume` 已改为传结构化历史，并使用 `last_response_id` 作为 `previous_response_id` 基础。
- [x] 扩展 `rollout.Line`/`rollout.Item`/`rollout.SessionMeta`，保留 timestamp/item_id/turn_id/role/response_id/data，并新增 rollout JSONL 到 Responses input items 的重建 helper。
- [x] 本轮运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补齐 rollout 文件级生命周期：新增按 thread id 查找 rollout path、unarchive 和 delete；保留 archive 移动行为，并补充对应单元测试代码，未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 `codex resume/fork/archive/delete/unarchive --name` 解析和按 session title/name 选择；同名时按 recency 选择最新记录。
- [x] 补 fork snapshot metadata，记录 forked_from_id、fork_mode、fork_item_count、fork_snapshot_at 和 fork_last_n，并确认结构化 item 字段 clone 不共享。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 compact content item 文本抽取与 token budget 基础：`compact.Item` 支持 content/data，摘要使用 `ItemText`，并加入本地 token 估算和预算裁剪。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 接入 rollout recorder 到 exec/app-server 结果路径：新增 session item 到 rollout item/line 的转换，exec 新建/恢复会创建或追加 rollout，app-server thread start 创建 rollout，runtime turn 完成后追加 rollout。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 turn timing/session prefix 基础：新增 `session_prefix`，exec/app-server/rollout meta 写入 session prefix，tool call/output session item metadata 写入 started/completed/duration 毫秒字段。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 compact 后继续 turn 基础：`thread/compact/start` 现在会读取 session history，本地 compact 成 summary history，保存回 session，并追加 rollout；远端 compact 留到模型调用阶段继续深化。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 开始阶段 6：补 app-server notification payload 对齐，新增结构化 token usage、context compacted summary/item count/window 字段，并在 RuntimeRouter compact start 后发送 compacted notification。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 app-server item builder/mappers：`ThreadItem` 和 item notification payload 现在保留 content/raw/name/namespace/callId/status/responseId/data，减少 session/rollout 到 RPC 的信息损失。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 config manager service：`ConfigService` 支持 managed layers，读取时按 layer precedence 合并并输出 origins/layers；写入后若被高优先级 layer 覆盖，返回 `okOverridden` 和 overriding metadata。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 thread lifecycle notifications：RuntimeRouter 现在对 thread start/archive/unarchive/delete/setName 发对应通知，普通 Router 仍保持纯 store 操作。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 `getConversationSummary` 真实摘要路径：RuntimeRouter 配置 ThreadRouter 时会从 session store 读取 thread history，优先返回 compact metadata 中的 summary，否则复用 compact 本地摘要逻辑生成 summary。
- [x] 补充 conversation summary 单元测试代码，覆盖普通历史摘要与 compact summary 优先级；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 thread goal 持久化：RuntimeRouter 配置 ThreadRouter 时把 goal 存入 session metadata.extra，同一套 session store 可跨 router 读取；无 ThreadRouter 时保留内存 fallback。
- [x] 对齐 Rust goal 协议：`GoalSetParams.objective` 改成可选指针，新建 goal 必须有 objective，已有 goal 可只更新 status/tokenBudget；set/clear 会发送 `thread/goal/updated` 和 `thread/goal/cleared` 通知。
- [x] 补充 goal 持久化、可选 objective、clear 不丢 thread items 的单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 thread history shape：`thread/turns/list` 默认按 Rust 语义使用 desc + summary items，支持 notLoaded/full，`thread/resume.initialTurnsPage` 现在真正按 limit/sort/itemsView 分页，即使 `excludeTurns` 也能单独返回 initial page。
- [x] 补充 turns 默认 desc summary、notLoaded/full 和 resume initialTurnsPage 的单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 `process/spawn` stdout/stderr streaming：`streamStdoutStderr` 不再被拒绝，进程输出会同步发送 `process/outputDelta`，退出通知仍保留 stdout/stderr 与 capReached。
- [x] 补充 process outputDelta 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 `command/exec` streaming 最小闭环：支持 `streamStdoutStderr` 发送 `command/exec/outputDelta`，支持 `streamStdin` 后续 `command/exec/write`，支持 active command terminate；PTY resize 仍按非 PTY 返回错误。
- [x] 补充 command/exec streaming stdout 与 stdin 写入单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 account lifecycle 通知：同步完成的 `account/login/start` 现在发 `account/login/completed` 与 `account/updated`；取消 pending login 发失败 completed；logout 发 `account/updated`。
- [x] 补充 RuntimeRouter account login/cancel/logout 通知单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 补 feedback upload 准备结果留存：`FeedbackSnapshot.PrepareUpload` 现在保存 cloned `LastPrepared`，后续 uploader/测试可读取 tags、attachments、attachment paths，避免 runtime handler 准备后丢状态。
- [x] 补充 feedback LastPrepared clone 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续补阶段 6 model/plugin/mcp RPC schema 对齐：`model/list` JSON 只输出 Rust v2 的 `data/nextCursor` 与 Model 字段；`modelProvider/capabilities/read` JSON 收窄为 `namespaceTools/imageGeneration/webSearch`；保留 Go 内部兼容字段供服务代码使用。
- [x] 继续补阶段 6 plugin RPC schema 对齐：`plugin/list`/`plugin/installed` 不再输出顶层 legacy `plugins`；`plugin/install` 不再输出 legacy `pluginId`；`PluginSummary`、`PluginDetail`、`AppSummary`、`AppTemplateSummary` JSON 输出收窄到 Rust v2 字段集。
- [x] 继续补阶段 6 mcp RPC schema 对齐：`mcpServerStatus/list` 不再输出 legacy `servers`；`mcpServer/oauth/login` 不再输出 legacy `url`；`MCPToolCallContent` 现在可保留未知 JSON 字段并继续提供 typed `Text` 访问。
- [x] 补充 model/plugin/mcp JSON shape 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续补阶段 6 environment/fs 行为细节：`environment/add` 现在只接受 `ws://`/`wss://` exec-server URL；`fs/watch` 重复 `watchId` 会按 Rust 行为返回请求错误而不是覆盖旧 watch。
- [x] 补充 environment/fs 对应单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 认证/account/provider 对齐：新增 `chatgptAuthTokens` 精确 auth mode、BackendMode、JWT claims 解析、外部 ChatGPT tokens auth snapshot 构造，account/read 可从 access token/显式参数解析 email/plan/account。
- [x] app-server `account/login/start` 现在会把 API key 与外部 ChatGPT tokens 写入 `auth.json`，`account/logout` 删除 `auth.json`，默认 app-server 启动会从 auth store/env 恢复 AccountManager 状态。
- [x] OpenAI provider 现在把外部 `chatgptAuthTokens` 视作 ChatGPT backend auth：使用 ChatGPT Codex base URL、支持 attestation、远端 model catalog source-of-truth，并在 API headers 中注入 Authorization/ChatGPT-Account-ID。
- [x] 新增 Responses 401 外部 auth refresh 回调和 app-server server request broker；stdio transport 可输出 `account/chatgptAuthTokens/refresh` server request，并把客户端同 id response 分流回 broker，刷新成功后更新 auth.json 与 account state。
- [x] 补充 auth/model/appserver/server request broker 对应单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 auth status/account read 对齐：`getAuthStatus` 现在从 auth store/env 推导真实状态，支持 `includeToken` 默认不泄露 token，并按 active provider 返回 `requiresOpenaiAuth`；保留 `MiscService` 快照入口兼容旧测试。
- [x] 继续阶段 7 permanent refresh failure：新增 refresh token backend code 分类、scoped permanent failure cache、`CODEX_REFRESH_TOKEN_URL_OVERRIDE`，`getAuthStatus(refreshToken=true)` 和 `account/read` 在永久失败后隐藏 ChatGPT token/account，避免重复刷新同一坏 token。
- [x] 补充 auth/appserver 对应单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 ChatGPT OAuth/device code 对齐：补 ID token 嵌套 `https://api.openai.com/auth` claims 解析，device code/browser callback 在 token 落盘前执行 forced workspace 校验，authorize URL 过滤空 workspace 并用逗号拼接。
- [x] CLI `login` 现在读取 `forced_chatgpt_workspace_id`（含根级/子命令 `-c` override）并传入 OAuth/device code；app-server 外部 `chatgptAuthTokens` 登录也按同一配置拒绝 workspace mismatch。
- [x] 补充 auth/config/app/appserver 对应单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 keyring/credential store 语义：`auth.Store` 新增指针式 `StoreOptions`，支持 file/keyring/auto/ephemeral 后端，keyring 保存会删除 fallback `auth.json`，auto 会从 keyring 优先读取并在失败/空时回退文件，ephemeral 只保存在进程内存。
- [x] `config` 新增 `cli_auth_credentials_store` 与 `features.secret_auth_storage` 读取；CLI login/status/logout、app-server auth status/account/login/logout/external refresh、exec Responses runner 和托管 ChatGPT refresh 已接入同一 store options。
- [x] 补充 auth/config/app/appserver keyring store mode 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor 对齐：新增 provider reachability plan/probe，按 active provider/auth 选择 ChatGPT/API key/provider auth 探测，支持 `/models` route probe、Azure/Bedrock 跳过 route probe、Bedrock 实际 region base URL。
- [x] doctor auth 检查现在读取 `cli_auth_credentials_store` 和 `features.secret_auth_storage`，按 file/keyring/auto/ephemeral store mode 解析凭据，并在诊断 details 中输出 store/backend。
- [x] 补充 doctor reachability、route 401/404、Bedrock region、keyring store mode 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 route config/proxy：新增 `network.ProxyConfigFromConfigValues`，从 `[network_proxy]` 读取 enabled/proxy_url/socks/mode/upstream/domains/unix sockets 等 Rust 同名字段，并复用现有 runtime 校验。
- [x] doctor `network.env` 现在输出 `network_proxy` enabled/mode/http/socks/upstream/allowed/denied/runtime bind 诊断；无效 proxy 配置会给 warning 和修复提示。
- [x] 补充 network proxy config values 与 doctor network proxy 诊断单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 8 responses-api-proxy 对齐：代理转发路径改为边读 upstream 边写客户端并 flush，避免 SSE/长流式 Responses 被整包缓冲；dump 仍保留完整 response body，dump 错误写 stderr。
- [x] `/shutdown` 返回明确文本后关闭 server；补充 Responses proxy streaming 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 route config/proxy：新增 `Config.RespectSystemProxyEnabled()` 和 `network.NewHTTPClient`，exec Responses runner 会按 `features.respect_system_proxy` 决定是否使用系统代理环境；显式注入 HTTPClient 仍优先。
- [x] 补充 respect_system_proxy config 与 HTTP client proxy 开关单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 7 doctor 退出语义：新增 app `ExitError`/`ExitCode`/`ShouldPrintError`，`cmd/codex` 按错误退出码退出，`doctor` 在 report overall fail 后返回 silent exit 1，避免 JSON/human report 后额外打印错误。
- [x] 补充 doctor fail silent exit 与 exit helper 单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 Bedrock/SigV4 对齐：新增 Bedrock Mantle supported region 校验，`RuntimeBaseURL`、`APIProvider`、env bearer token auth、doctor reachability plan 都会拒绝不支持的 region，并补充 provider/doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 Bedrock 错误映射：Responses 非流/流式 HTTP error 现在按 provider name 做 Bedrock expired signature 401 映射，输出 Rust 同义可操作提示；其他 Bedrock 401 保持泛化错误，并补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 8 exec 失败事件：`exec --json` 在 turn started 后遇到 agent/runtime error 会输出 `error` event，同时 Run 保持返回 error，CLI 仍 exit 1；补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 project config：新增 `LoadOptions.CWD`/`EffectiveOptions.CWD`、`ProjectConfigPath(s)`，配置加载会按 CWD 向上合并 `.codex/config.toml`，CLI overrides 仍最高优先；exec/doctor 已传入 CWD，并补充 config/exec 单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 project config 安全过滤：项目本地 `.codex/config.toml` 现在忽略 Rust 明确列出的敏感/全局键，保留普通 `model` 与普通 feature；补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 project config trust gate：项目本地配置现在需要用户级 `[projects."<path>"].trust_level = "trusted"` 才参与合并，unknown/untrusted 会忽略项目层；补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 project config 相对路径：项目本地 `model_instructions_file` 现在会按来源 `.codex` 目录解析相对路径，最近项目层优先；补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮包加载检查：`go list -buildvcs=false ./...` 通过。
- [x] 继续阶段 7 project config base instructions：新增 `AgentRequest`/`AgentLoopRequest.Instructions` 并贯通 exec -> turn runtime -> model runner；exec 会读取 `model_instructions_file`（空文件/读取失败报错）或内联 `instructions`，Responses request body 使用该值覆盖默认 base instructions；补充 turn/runtime/model/exec 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 app-server config snapshot：`ConfigService.Read` 在传入 `cwd` 时会单独构造 user/project/profile/managed layers，project config 以独立 `LayerSourceProject` 暴露 origins/layers，managed layer 仍能覆盖 project；补充 config service 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 app-server turn config snapshot：`turn/start` 会从 thread record 补 CWD/model/provider，runtime 执行前按该 CWD 读取 effective config，补默认 model/provider/base instructions 并传给 AgentLoop；显式 turn params 仍优先；补充 runtime router 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 project hooks config folder：`HookDiscoveryService` 可注入 `ConfigService`，带 config 时从 trusted project layers 的 `.codex` 目录发现 hooks，未信任项目不再回退加载 `cwd/.codex/hooks.json`；DefaultRuntimeRouter/requireHooksDiscovery 会共享 ConfigService；补充 hooks discovery 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 project trust root：新增 active project root/trust target helper，普通 git repo 信任只看当前 repo root，父目录 trusted 不再误信任 nested git project；ProjectConfigPaths 只收集 active root 到 cwd 的项目层；补充 config 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 Bedrock/SigV4 最终 body 对齐：`AuthHeaders.SignRequest` 现在可返回签名后实际发送的 body，Responses runner/models endpoint 会保持签名所见 body 与发送 body 一致；Bedrock Mantle SigV4 签名前会移除 snake_case headers 和 `Content-Encoding`，对齐 Rust Bedrock auth provider 的“准备 body 后关闭请求压缩”语义；补充 Responses signer body 与 Bedrock content-encoding 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 project `.codex` linked worktree hooks：新增 project `.codex` folder helper、linked worktree `.git` pointer 解析和 root checkout hook folder override；`config/read` 现在对 trusted `.codex` 即使没有 config.toml 也输出空 project layer，hooks discovery 使用内部 hooks folder override 读取 root checkout hooks；补充 config/config service/hooks discovery 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3/7 Responses originator header：新增 `AgentRequest`/`AgentLoopRequest.Originator`，turn runtime/app-server runtime 会从 thread metadata 透传 originator；Responses runner 在 originator 非空且 header-safe 时发送 `originator` header；补充 model/turn/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 provider config 校验：`ConfiguredProviderMap` 现在按 provider id 校验 AWS 配置，custom provider 配 `aws` 会按 Rust 文案拒绝，`amazon-bedrock` 只允许覆盖 `aws.profile`/`aws.region`，避免通用 AWS 冲突校验误伤 Bedrock override；补充 provider config 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 command/process RPC 对齐：`command/exec` 流式请求现在延迟到进程退出后返回最终 response，支持执行期间继续 `command/exec/write`，streamed stdout/stderr 不重复进入最终 response；`process/spawn` streamed stdout/stderr 不再重复进入 `process/exited`；补充 command/process/protocol JSON shape 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 stdio transport 对齐：`StdioServer.Serve` 现在并发派发 JSON-RPC 请求并串行写回 stdout，长时间运行的 `command/exec` deferred response 不会阻塞后续 `command/exec/write`；对流式 command 按 processId 注册后再放行控制请求，补充 stdio 并发 command/write 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 hook state/trust 对齐：project hooks 现在和 user hooks 一样应用 `hooks.state` 的 enabled/trusted_hash；`bypass_hook_trust` 从 config 贯通到 hook discovery/turn adapter，列表仍显示原始 trustStatus，执行时允许 enabled untrusted hooks；补充 hook discovery/runner 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7/8 doctor network 诊断：`network.env` 现在按 Rust 行为检查 `CODEX_CA_CERTIFICATE` 和 `SSL_CERT_FILE` 是否指向可读文件，目录/不可读路径降级为 warning 并给出明确 detail；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3/7 OpenAI provider config/auth 对齐：`ProviderInfo.BuildHeaderMap` 现在按 Rust 行为跳过非法 header name/value，避免无效静态/env header 泄露到 HTTP 请求；补充非法 header 与 `auth + env_key` 配置拒绝单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3 Responses retry 对齐：HTTP Responses 请求现在按 Rust provider retry config 不再重试 429，只保留 401 auth recovery、5xx 与 transport retry；补充 429 不重试单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 Unix socket transport：新增 app-server Unix socket JSON-line listener，`app-server --listen unix://` 现在使用默认 control socket，`unix://path` 使用显式 socket；补充 path 解析与 Unix socket 通信单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 WebSocket auth gate 基础：新增 capability token/signed bearer token auth policy，支持 bearer header 解析、SHA-256 常量时间比较、HS256 JWT claim 校验和非 loopback 未鉴权检测；CLI 解析 `--ws-auth` 相关参数并校验 Rust 同类互斥/必填规则；补充 appserver/cli 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 Unix socket path 细节：显式 `unix://PATH` 现在按 Rust 行为解析为当前工作目录下的绝对 socket path，默认 `unix://` 仍使用 Codex home control socket；补充单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 personal access token/account 对齐：新增 AuthAPI `whoami` metadata 客户端，`account/read` 在 `CODEX_ACCESS_TOKEN`/`personal-access-token` 模式下水合 ChatGPT account，`getAuthStatus(includeToken=true)` 不返回 PAT；补充 auth/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 getAuthStatus schema 对齐：`AuthStatusResponse` 保留内部 legacy 字段但 JSON 输出只包含 Rust `GetAuthStatusResponse` 的 `authMethod/authToken/requiresOpenaiAuth` 三个字段；补充 JSON shape 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account login 细节：外部 `chatgptAuthTokens` 登录和 logout 现在会取消所有 pending ChatGPT login，后续取消原 loginId 返回 `notFound`，对齐 Rust `set_auth_token_cancels_active_chatgpt_login`；补充 auth/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 forced login method：新增 `Config.ForcedLoginMethod()`，app-server `account/login/start` 现在按 Rust 行为拒绝 `forced_login_method = "chatgpt"` 下的 API key 登录，以及 `forced_login_method = "api"` 下的 ChatGPT/外部 ChatGPT 登录；补充 config/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 Bedrock account/read：`account/read` 现在会从 active provider 的 account state 返回 Amazon Bedrock account；AWS credentials provider 返回 `awsManaged`，托管 Bedrock API key 返回 `codexManaged`，并把旧 `apiKey` credentialSource 改成 Rust schema；补充 auth/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account login 外部 auth 保护：当当前 auth 是 `chatgptAuthTokens` 时，API key、普通 ChatGPT、device-code 登录现在按 Rust 文案拒绝，避免覆盖外部托管凭据；新的 `chatgptAuthTokens` 仍可更新；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 login restriction enforcement：新增 app-server `resolveAuthWithLoginRestrictions`，`getAuthStatus`/`account/read`/默认 router 启动恢复账号会执行 `forced_login_method` 与 `forced_chatgpt_workspace_id` 检查；存储 auth 不合规时清除并按未登录处理，环境变量 auth 不合规时报错；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 CLI login 对齐：`codex login` 现在按 Rust 文案执行 `forced_login_method` 限制；`--with-access-token` 按 `at-` 前缀区分 PAT/JWT，PAT 会先请求 AuthAPI `whoami` 并校验 `forced_chatgpt_workspace_id`，最终 auth.json 只写 `personal_access_token` 以保持 Rust 回滚兼容；补充 app 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 CLI login/logout 可见行为：`codex login` API key/access token 成功文案统一为 Rust 的 `Successfully logged in`，`login status` 未登录返回 silent exit 1，API key status 使用 Rust `safe_format_key` 形状，`logout` 输出 `Successfully logged out`；新增 best-effort OAuth revoke 并让 ChatGPT browser/device 登录前清理旧 auth；补充 auth/app 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity account 基础：新增 `AgentIdentityAuthRecord` Go 结构和 agent identity JWT/record 本地解析，`AccountFromAuth` 与 workspace restriction account id 提取现在能识别 agent identity；补充 auth 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity provider 行为：provider account state 现在把 agent identity 呈现为 ChatGPT account/email/plan；`AuthHeadersFromAuth(agent-identity)` 不再生成空 bearer，而是明确返回 Rust 同义错误，避免静默无鉴权请求；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 7 CODEX_ACCESS_TOKEN 分类：新增 `FromCodexAccessToken`，环境变量 `CODEX_ACCESS_TOKEN` 现在按 Rust `at-` 前缀区分 personal access token 与 agent identity JWT，CLI `--with-access-token` 复用同一分类 helper；补充 auth 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 account logout 对齐：app-server `account/logout` 现在走 `auth.LogoutWithRevoke`，会 best-effort revoke 存储的 ChatGPT refresh/access token，并同时清理 ephemeral 与配置存储；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account usage RPC shape：`account/usage/read` 的 `dailyUsageBuckets` 现在保留 Rust `Option<Vec>` 语义，nil 序列化为 `null`、显式空列表序列化为 `[]`；manager clone 不再把 nil 改成空列表；补充 auth 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 account backend read gate：`account/rateLimits/read`、`account/usage/read`、`account/workspaceMessages/read` 现在按 Rust 行为要求存在 Codex 后端登录；未登录返回 `codex account authentication required...`，API key/Bedrock API key 返回 `chatgpt authentication required...`，ChatGPT/external/PAT/agent-identity 允许读取本地快照；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account sendAddCreditsNudgeEmail 对齐：新增 `chatgptapi.CloudClient.SendAddCreditsNudgeEmail` 和 HTTP status error，app-server 现在从当前 auth 取 ChatGPT/PAT 后端 bearer，按 Rust 文案拒绝未登录/API key，POST `chatgpt_base_url` 后端，429 映射 `cooldown_active`，其他失败返回 `failed to notify workspace owner`；补充 chatgptapi/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account backend reads 对齐：`account/rateLimits/read` 现在 GET `/api/codex/usage` 并映射 primary/secondary/additional/spend control/reset credits；`account/usage/read` 现在 GET `/api/codex/profiles/me` 并保留 `dailyUsageBuckets` 可空语义；`account/workspaceMessages/read` 现在 GET `/api/codex/workspace-messages`，RFC3339 时间转 Unix seconds，404 映射 feature disabled；补充 chatgptapi/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 fs/copy RPC 细节：`FSService.Copy` 现在按 Rust exec-server 语义使用 `Lstat` 判类型，目录复制要求 `recursive=true`、拒绝复制到自身/子目录，递归复制保留 symlink 并跳过 FIFO 等特殊文件，独立特殊文件返回 `fs/copy only supports regular files, directories, and symlinks`；`fs/writeFile` invalid base64 文案对齐 Rust；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 process/writeStdin RPC 细节：参数校验现在按 Rust 文案返回 `process/writeStdin requires deltaBase64 or closeStdin` 与 `invalid deltaBase64: ...`，保留 invalid params error code；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 model/list RPC 细节：分页现在按 Rust 语义处理 `limit=0` 为至少 1 条，非法 cursor 返回 `invalid cursor: ...`，cursor 超过总数返回 `cursor X exceeds total models Y`，并通过 `model.ErrInvalidModelRequest` 映射为 JSON-RPC invalid request `-32600`；补充 model 与路由层单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity 深化：`internal/agent` 现在补齐 Rust `agent-identity` 的核心 HTTP/data shape：Ed25519 PKCS#8 key material 生成、公钥 SSH 编码、AgentAssertion/任务注册签名、agent identity 注册、task 注册、JWKS fetch、RS256/JWK JWT 验证入口、plan alias 归一化与 retryable registration error 判断；补充 agent 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account rate limit reset backend：新增 `chatgptapi.CloudClient.ConsumeRateLimitResetCredit`，app-server `account/rateLimitResetCredit/consume` 现在按 Rust 行为要求 ChatGPT/Codex backend auth，POST `/api/codex/rate-limit-reset-credits/consume`，发送 `redeem_request_id` 并映射 `reset/nothing_to_reset/no_credit/already_redeemed`；空 `idempotencyKey` 和未登录/API key 文案对齐 Rust；补充 chatgptapi/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity provider auth：新增 `auth.AgentIdentityRecordFromAuth`，`model.AuthHeadersFromAuth(agent-identity)` 对完整 registered record + task id 生成 Rust 同义 `AgentAssertion` Authorization，并保留 `ChatGPT-Account-ID`/FedRAMP routing header；裸 JWT/缺 task id 仍不暴露 bearer；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 personal access token backend routing：`model.AuthHeadersFromAuth(personal-access-token)` 现在会从 PAT metadata 带上 `ChatGPT-Account-ID` 与 `X-OpenAI-Fedramp`，account backend client 在发起 rate/usage/workspace/reset/nudge 等后端请求前会水合 PAT `whoami` metadata；补充 model/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 managed agent identity bootstrap：新增 ChatGPT managed agent identity resolver，可从 managed ChatGPT auth 注册/复用 agent identity record、注册缺失 task、持久化 record 到 auth store，并在 `features.use_agent_identity` 打开时让 CLI exec / app-server 注入的 Responses runner 把 bearer auth 升级为 AgentAssertion；bootstrap unavailable 会按 Rust 语义退回本会话 ChatGPT bearer；补充 auth/model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity clone 边界：清理 managed bootstrap 重复测试定义，`cloneAuthDotJSON` 与 Responses runner snapshot clone 现在会深拷贝 `AgentIdentityAuthRecord`，避免注册 task 时通过浅拷贝污染调用方 snapshot；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 account backend client 复用和超时：`account/usage/read`、`account/workspaceMessages/read`、rate limit reset consume 现在按 Rust 语义加 10s 请求超时和同义超时文案；rate limit reset 与 sendAddCreditsNudgeEmail 统一复用 `accountBackendClient`，因此 PAT `whoami` 水合后的 account/FedRAMP routing header 覆盖所有 account backend RPC；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 provider models endpoint 对齐：`HTTPModelsEndpoint` 现在无论 provider 是否已有 query params 都会追加 `client_version=0.0.0`，`RemoteModelsManager` 远端刷新加 5s timeout，对齐 Rust `MODELS_REFRESH_TIMEOUT` 与 `/models` client_version 行为；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity request-time signing：`model.AuthHeadersFromAuth(agent-identity)` 现在把 AgentAssertion 放到 `SignRequest` 中每次请求动态生成，避免 runner 初始化时固化 assertion timestamp；补充 auth managed bootstrap 与 model auth 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity bootstrap cooldown：managed ChatGPT bootstrap 现在按 Rust 行为对同一 account/Auth API base URL 的 retryable 注册失败记录 1 小时共享冷却，冷却期内直接返回 bootstrap unavailable，成功或非 retryable 失败会清理冷却；补充 auth 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity provider catalog：`authHasChatGPTAccount` 现在把 `agent-identity` 视为 Codex backend/ChatGPT account auth，远端模型目录在 agent identity 下也作为 source of truth，对齐 Rust `uses_codex_backend`；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/7 account backend error wrapping：rate limits、token usage、workspace messages、rate limit reset consume、sendAddCreditsNudgeEmail 的 backend client 构造失败现在统一包成 `failed to construct backend client: ...`，对齐 Rust account processor；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 agent identity telemetry 承载：`AuthHeaders` 现在携带 `codexapi.AgentIdentityTelemetry` 的 agent/task id，agent identity provider auth 与 managed bootstrap 后的 Responses runner 会保存这份元数据，为 Rust `ResolvedProviderAuth.agent_identity_telemetry`/models endpoint request telemetry 对齐提供同源入口；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor JSON shape：`doctor.Run(... JSON:true)` 现在输出 Rust 同构的结构化支持报告，`checks` 按 check id 建 map，detail 字符串转换为 key/value，重复 detail 保留数组，非结构化 detail 进入 `notes`，editor/pager 类值在 JSON 中脱敏为 `set`；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor terminal diagnostics：新增 `terminal.env` 检查并纳入 human Environment 分组，复用 `internal/shell` 终端识别，报告 TERM/TERM_PROGRAM/multiplexer/color/locale/远程终端 presence/TERMINFO，并对 `TERM=dumb`、窄 `COLUMNS`、非 UTF-8 locale、不可读 TERMINFO 给出 Rust 同义 issue/remedy；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor git diagnostics：新增 `git.environment` 检查并纳入 human Environment 分组，对齐 Rust doctor/git 的 selected git、PATH 候选、git version/exec-path/build-options、repo root、`.git` entry、branch、core.fsmonitor 与 Windows 旧 Git 警告；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor terminal title diagnostics：新增 `terminal.title` 检查并纳入 human Environment 分组，从 `[tui].terminal_title` 读取配置，支持 Rust 同义默认项、禁用、别名归一化、未知项 warning、git/project/cwd 项目名来源和 24 字符截断；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor updates diagnostics：新增 `updates.status` 检查并纳入 human Connectivity 分组，报告 `check_for_update_on_startup`、安装来源对应 update action、`version.json` cache、latest probe/status/update command；latest probe 失败按 Rust doctor 降级 warning；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor background app-server diagnostics：新增 `app_server.status` 被动检查并纳入 human Connectivity 分组，报告 daemon state/settings/pid/update-pid/control socket、socket status 与 persistent/ephemeral mode；普通 socket 占位/异常降级 warning 并给出 daemon version remedy；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor MCP diagnostics：新增 `mcp.config` 检查并纳入 human Connectivity 分组，从 `mcp_servers` 配置解析 stdio/streamable_http server，报告 configured/disabled/transport counts，对 stdio 空 command/不可解析 command/cwd/env key 与 HTTP bearer token env 缺失做 required fail、optional warning 分级；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor sandbox diagnostics：新增 `sandbox.helpers` 检查并纳入 human Connectivity 分组，报告 approval policy、filesystem sandbox、network sandbox、workspace writable roots，并检查 `codex-linux-sandbox`/`codex-execve-wrapper` dispatch helper 是否可解析；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor websocket diagnostics：新增 `network.websocket_reachability` 检查并纳入 human Connectivity 分组，报告 active provider、wire API、supports websockets、proxy env、connect timeout、auth mode 与 WebSocket endpoint；当前 Go 端尚无真实 Responses WebSocket handshake client，因此支持 WebSocket 的 provider 按 warning 标注 handshake probe 未实现；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor websocket auth context：`network.websocket_reachability` 现在读取当前 auth store snapshot 后再构造 runtime provider，使 ChatGPT/PAT/agent identity 场景下的 endpoint 与 auth mode 更接近 Rust runtime provider 行为；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 account sessions RPC 细节：新增 `account/sessions/add|list|logout|switch` method 常量与 runtime handlers，补齐 `AccountSessions*` 参数/响应别名和 AuthManager 内存 session list/switch/logout 语义，切换时维护 `activeSessionId`、`isActive`、`selectedWorkspaceAccountId` 和 `lastUsedAt`，登出 active session 时回退到剩余 session；补充 auth/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 server request wire shape：`ServerRequestBroker` 发给客户端的 server request 不再主动携带 `jsonrpc` 字段，对齐 Rust `ServerRequest` 序列化；保留响应解析兼容客户端回 `jsonrpc:"2.0"`；补充 ChatGPT auth refresh server request params shape 测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 protocol wire name 兼容：将 thread elicitation 主 wire 名对齐 Rust 的 `thread/increment_elicitation` / `thread/decrement_elicitation`，保留旧 camelCase 常量兼容；补齐 experimental server request `currentTime/read` 的 method 常量和 params/response 类型；补充协议 shape 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 notification 命名兼容：在 appserver notification 常量层补齐 Rust 命名别名（Guardian/Mcp/Fs/Sdp），统一指向已有 wire 值，避免后续按 Rust 名称接事件时缺符号；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 current time server request 桥接：新增 runtime `requestCurrentTime`，通过 `currentTime/read` server request 向客户端读取 Unix 秒时间，做 10s timeout、threadId/currentTimeAt 校验并返回 UTC time；补充 runtime 桥接单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 配置解析第三方库化：采纳“优先成熟第三方库”规范，引入 `github.com/pelletier/go-toml/v2`，将 `config.toml` 文件读取从手写 TOML parser 切换为库解析；补 `current_time_reminder` Rust 同名结构配置读取（bool 简写和 `[features.current_time_reminder]` 表、clock_source/delivery_mode/sleep_tool/interval），并补充配置单元测试代码；按要求未运行 `go test`。
- [x] 本轮使用工作区 `GOMODCACHE/GOCACHE` 拉取依赖并运行 `go mod tidy`、`go list -buildvcs=false ./...`，已通过；临时 `.gomodcache/.gocache` 已清理；未运行 `go test`。
- [x] 继续阶段 6 current_time_reminder 注入：app-server turn runtime 现在会在启用 `features.current_time_reminder` 时把 `<current_time>` 片段注入 turn instructions；`clock_source=external` 通过 `currentTime/read` server request 取客户端时间，`system` 使用本地 UTC；补充 runtime 注入单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 WebSocket transport 基础：引入成熟三方库 `github.com/coder/websocket`，`app-server --listen ws://...` 现在可启动真实 WebSocket JSON-RPC transport，支持 `/readyz`/`/healthz`、连接级 initialize gate、server request/notification 双向文本帧、auth policy 校验和非 loopback 未鉴权启动拒绝；补充 transport 单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `go mod tidy`、`gofmt` 和 `go list -buildvcs=false ./...`，已通过；临时 `.gomodcache/.gocache` 已清理；未运行 `go test`。
- [x] 继续阶段 7 AWS Bedrock credential/SigV4：按“优先合适第三方库”规范引入 AWS SDK v2（`config`/`credentials`/`aws/signer/v4`），`internal/auth` 的 AWS auth 保留现有 `AWSAuthContext`/`SignAWSRequest` 接口但内部改用 SDK 默认凭据链、shared config/profile/credential_process 和官方 SigV4 signer；Bedrock provider 的 request-time signing 继续复用同一接口；补充/保留 env、profile、credential_process、静态凭据签名单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `go mod tidy`、`gofmt` 和 `go list -buildvcs=false ./...`，已通过；临时 `.gopath/.gocache/.gomodcache` 已清理；未运行 `go test`。

- [x] 继续阶段 6 hook/process RPC 细节：`hooks/list` 现在按 Rust 对每个请求 cwd 返回 entry，支持从 user/project `config.toml` 的 `[hooks]`/`[[hooks.Event]]` 发现 command hook，保留 `hooks.json` 兼容路径；hook key/sourcePath/hash/timeout=0 归一语义进一步贴近 Rust；`process/spawn` 校验文案对齐 Rust；补充 hooks/process 单元测试代码；按要求未运行 `go test`。
- [x] 本轮编译链整理：修复 appserver 局部编译路径上的 Go 编译阻塞（tool apply_patch executor 字段/方法同名、agent enum receiver、mcp/appserver 重复 helper、指针别名 marshal、ThreadStartedNotification 指针传递）；`gofmt`、`go list -buildvcs=false ./...` 与 `GOCACHE=$TEMP/codex_go_gocache go build -buildvcs=false ./internal/appserver` 已通过；未运行 `go test`。
- [x] 继续阶段 6 plugin hooks：`hooks/list` 现在通过 `PluginService.EnabledHookSources()` 接入 enabled plugin 的 `hooks/hooks.json`，plugin hook key/source/pluginId 按 Rust 形态生成，命令展示与执行前会展开 `${PLUGIN_ROOT}`/`${PLUGIN_DATA}`/Claude 兼容占位符，HookRunner 执行子进程时注入对应 env；补充 plugin/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 与 `GOCACHE=$TEMP/codex_go_gocache go build -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 6 command/process 控制面错误语义：新增窄范围 `ErrJSONRPCInvalidRequest`，让 command/process 中 Rust `invalid_request` 场景映射为 JSON-RPC `-32600`，同时保留现有 `ErrInvalidRequest` 兼容；`command/exec` 参数冲突、base64、size、streaming processId、duplicate/no-active 和 `process/*` duplicate/no-active/stdin/resize 文案进一步对齐 Rust；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 与 `GOCACHE=$TEMP/codex_go_gocache go build -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 6 MCP RPC 错误语义：新增 `mcp.ErrInvalidMCPRequest`，让 status list 非法 cursor、cursor 超界、OAuth 空 name、resource/tool 缺少必要字段、required server 不可用映射到 JSON-RPC `invalid_request(-32600)`；补充 mcp/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 与 `GOCACHE=$TEMP/codex_go_gocache go build -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 6 PTY RPC：按“优先成熟第三方库”规范引入 `github.com/creack/pty` 和 `golang.org/x/sys/windows`，`command/exec` 与 `process/spawn` 的 `tty` 现在接真实 PTY backend，Unix 使用 creack/pty，Windows 使用 ConPTY，支持 outputDelta streaming、final response 不重复输出、stdin 写入/关闭、terminate/kill 和 resize；补充 appserver PTY 单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt`、默认平台 `go list -buildvcs=false ./...` 与 `GOOS=linux GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，均已通过；未运行 `go test`。
- [x] 继续阶段 6 复杂 ThreadItem union：`apply_patch` 成功输出现在携带 `fileChange` marker、`changes`、`status`，delete 会在应用前读取旧内容作为 diff；app-server runtime 会把 apply_patch call/output 映射为 Rust v2 `fileChange` union，`PatchChangeKind::Update` 输出保留 `move_path:null`，并补充工具和协议单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 fileChange streaming：Responses streaming handler 现在会记录 `apply_patch` custom tool call，累积完整 patch 输入并复用 `internal/applypatch` 解析后发送 `item/fileChange/patchUpdated`，补充 runtime router 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 fileChange approval：`FileChangeRequestApprovalParams` 已收窄到 Rust v2 schema 字段 `threadId/turnId/itemId/startedAtMs/reason/grantRoot`，移除旧扩展 `changes/action` 输出，并补充 server request marshal 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 fileChange notification：`item/fileChange/patchUpdated` 现在复用 `ThreadItem` 的 file change 归一化逻辑，`PatchChangeKind::Update` 会按 Rust schema 保留 `move_path:null`，补充 notification marshal 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3/OpenAI provider config：新增 `responsesapi_client_metadata` 配置读取，复用 turn metadata 过滤规则，并在 exec 与 app-server turn runtime 中透传到 Responses `client_metadata`；补充 config/exec/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3/turn metadata：`turn.FilterClientMetadata` 现在复用 codexapi reserved metadata key 列表，过滤 `x-codex-*`/`x-openai-subagent`/内部 lineage 字段，避免 `responsesapi_client_metadata` 覆盖核心请求 metadata；补充 turn 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 本轮继续阶段 3/6 turn metadata：补 app-server `turn/start`、`turn/steer` 的实验字段 `responsesapiClientMetadata`，配置层与请求层 metadata 会合并后进入 `client_metadata["x-codex-turn-metadata"]` JSON blob，保留字段继续过滤且不作为顶层 flat key 泄漏；补充 turn/exec/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 3 Responses metadata header 投影：Responses runner 现在会从 `client_metadata` 派生 Rust 兼容 HTTP headers（`x-codex-window-id`、`x-codex-turn-metadata`、`x-codex-parent-thread-id`、`x-openai-subagent`），普通 extra metadata 不会上 header；补充 Responses runner 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 1/6 usage metadata：`AgentLoopResult` 现在聚合多轮 Responses usage，no-router runtime 也会保留 usage；exec JSONL turn.completed 改用聚合 usage；app-server turn 完成时发送 `thread/tokenUsage/updated`，assistant session item metadata 写入 `tokenUsage/usage` 与 response id；补充 turn/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 1/5 compact token status：app-server turn 完成后会根据聚合 usage 评估 `compact.TokenStatus`，写入 thread metadata 的 `token_status` 与 `last_token_usage`；per-turn tool router 现在带 thread-specific `ContextStatus`，`get_context_remaining` 可读取最近 token 状态；真正自动 inline compact/remote compact 执行仍保留为后续 todo；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 request_user_input server request 桥接：app-server turn 的 core tool router 现在会把 `request_user_input` 通过 `item/tool/requestUserInput` server request 发给客户端，转换客户端 `answers` 回 core tool JSON，同时在请求期间发送 `waitingOnUserInput` thread status；`UserInputResponder` 已按指针风格调整；补充 server request shape 与 runtime 桥接单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 1/5 自动 compact 本地闭环：app-server turn 完成后如果 metadata 中配置的 `auto_compact_token_limit`/`tokenLimit` 等触发 `compact.TokenStatus.ShouldCompact`，会自动执行本地 compact、写回 `compaction_summary`/`compaction_trigger`/`compaction_reason`/`compacted_turn_id`，追加 rollout，并发送 `thread/compacted` 通知；默认无 token limit 时不改变现有行为；补充 runtime 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 1/6 turn active 状态收口：新增 `TurnService.Complete(*TurnCompleteParams)`，app-server runtime 成功、失败、中断结束时同步清理 TurnService active turn，避免已完成 turn 继续被 `turn/steer` 命中；补充 turn/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 turn/steer 错误语义：`TurnService.Steer` 新增 Rust 风格错误分类（no active turn、expected mismatch、empty input），app-server `turn/steer` 将这些场景映射为 JSON-RPC `invalid_request(-32600)`，并对齐 `expectedTurnId must not be empty` / `input must not be empty` 等文案；补充 turn/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 turn/steer 输入持久化：app-server `turn/steer` 成功后会把 steered user input/legacy prompt 追加到 thread session history，保留 `clientUserMessageId`、`steered` 标记和 `additionalContext` metadata，追加 rollout，并发送 `item/completed` userMessage 通知；这先补齐输入可追踪性，完整 active-turn mailbox 交付仍待后续深化；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6 additionalContext 注入：新增 `context.AdditionalContextFragment`，app-server `turn/start` 会把 `AdditionalContextApplication` 渲染进 developer instructions，把 `AdditionalContextUntrusted` 渲染为额外 user input item，初步对齐 Rust AdditionalContextStore 的角色语义；跨 turn 去重/store 仍待后续深化；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 5/6 app-server turn 历史恢复：`turn/start` 现在会从 thread session store 读取已有 history，复用 `session.InputItemsFromRecord` 转成 Responses input items，并把 `lastResponseId/previousResponseId` 作为 `PreviousResponseID`、`Store=true` 传给 agent；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 5/6 app-server response id 延续：turn runtime 完成后会把 agent `ResponseID` 写回 thread metadata 的 `last_response_id`，下一轮 `turn/start` 可作为 `previous_response_id` 继续 Responses 对话；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6/10 Windows PTY：确认 `command/exec` 与 `process/spawn` 的 Windows TTY backend 使用 `golang.org/x/sys/windows` ConPTY；本轮补 Windows Job Object 包裹 ConPTY 子进程，terminate/timeout 时尽量清理整棵命令进程树，并保留受限宿主下的安全降级；补 PTY terminal size 上限校验，避免 `uint16` 到 Windows `int16` 坐标溢出；补充 appserver 参数单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 1/6 turn/steer active mailbox：新增 `turn.SteerMailbox`，`AgentLoop`/`Runtime` 会在每轮 agent sampling 前 drain steer input items；app-server `turn/steer` 在保留 session/rollout/item notification 的同时，把 user input 与 additionalContext 转成 Responses input items 投递到 active turn，完成/失败/中断时清理 mailbox；补充 turn/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 1/6 steer mailbox 边界收口：修正首轮 sampling 前已有 steer input 时原始 prompt 仍会写入 loop history，避免后续 tool-loop history 丢失初始用户消息；补充 turn 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5/6 session history replay：`session.InputItemsFromRecord` 现在会把历史中的 `image`、`inputImage`、`localImage`、`local_image` 和空 type 但带 `image_url` 的用户内容统一恢复为 Responses 可接受的 `input_image` blocks，并保留 `detail`；补充 session 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 3/5/6 Responses history order：`ResponsesAgentRunner` 现在会先发送恢复出来的 history/input items，再把本轮 prompt 追加为最新 user message，修正 app-server resume/history 场景中新问题排在历史之前的问题；补充 model 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5 exec resume history：`codex exec resume` 不再把历史文本拼进 prompt；历史只通过 `session.InputItemsFromRecord` 作为结构化 input items 恢复，本轮 prompt 只保留新的用户消息，避免 Responses 请求重复历史；删除旧 `buildResumePrompt` 拼接路径并更新 exec 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5 remote compact 基础：新增 `compact.RemoteRunner`/`CompactRemotely`，远端结果会过滤成可继续 turn 的 compact history，失败或无 runner 时可本地兜底；RuntimeRouter 手动/自动 compact 共用 helper，支持注入远端 runner，支持 provider remote compaction capability 判断；补充 compact/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5/6 app-server 默认 Responses agent 装配：RuntimeRouter 在未显式注入 Agent 时，会读取 effective config、auth store/login restriction、provider config，并用 `model.NewResponsesAgentRunnerFromRuntimeProviderWithAuth` 构造真实 Responses runner；支持 `RuntimeServices.HTTPClient` 注入，streaming handler/external auth refresh/store options/agent identity 仍按 turn 注入；无 auth 且 provider 需要 OpenAI auth 时回退本地 stub；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5 remote compact hooks：RuntimeRouter compact helper 现在会发现/执行 PreCompact 与 PostCompact hooks，复用 HookRegistry/HookDiscovery/bypass trust 规则；PreCompact additionalContext 转为 developer compact initial context，hook stop 会中止 compact 并返回错误；补充 appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 5 remote compact metadata：`compact.Result` 现在承载 source、responseId、model、providerId、usage；agent remote compact runner 会从模型响应填充这些字段；RuntimeRouter 会把 compaction source/status/phase/response/model/provider/token usage 写入 thread metadata 并扩展 `thread/compacted` 通知；补充 notification/appserver 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 6 complex ThreadItem history replay：ThreadItem union mapper 现在不只识别 `tool_output`，也会识别 `function_call_output`、`custom_tool_call_output`、`tool_search_output` 和 `tool_search_call`，让历史恢复出的 MCP/dynamic/fileChange 工具输出继续按 Rust `mcpToolCall`/`dynamicToolCall`/`fileChange` union 输出，不退回旧 envelope；补充协议单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt`、`go list -buildvcs=false ./...` 和 `GOOS=windows GOARCH=amd64 go list -buildvcs=false ./internal/appserver`，已通过；未运行 `go test`。
- [x] 继续阶段 6 Windows ConPTY 输入对齐：确认 Windows PTY backend 使用 `golang.org/x/sys/windows` ConPTY，本轮新增 Windows TTY input normalizer，按 Rust `WindowsTtyInputNormalizer` 语义把 LF 转 CR、折叠跨 write 的 CRLF、把 Backspace `0x08` 转 DEL `0x7f`；`command/exec` 与 `process/spawn` 的 TTY stdin 共享该行为；补充 Windows-only 单元测试代码；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 5/7 thread archive 与 rollout parity：`thread/archive`/`thread/unarchive` 现在会 best-effort 移动对应 rollout 到 `archived_sessions`/`sessions`，避免 thread store 与 rollout 文件位置长期分裂；新增 doctor `state.rollout_db_parity` 检查，对比 Go JSON thread store 与 rollout 文件，报告缺失记录、stale record、归档位置不一致、重复 rollout/thread record、扫描错误和样本；补充 appserver/doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 7 doctor system/runtime/search 诊断细节：`system.environment` 新增 Rust 同名风格的 `os type`/`os version`/`os language`、editor/pager env 细节；`runtime.provenance` 新增 install method 描述和 build commit；`runtime.search` 新增 bundled/system search provider，并按 provider 输出 OK summary；补充 doctor 单元测试代码；按要求未运行 `go test`。
- [x] 本轮再次运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 5/6 thread lifecycle rollout 同步：`thread/fork` 现在为 fork 出的新线程创建 rollout，`thread/delete` 删除子树时会 best-effort 清理 active/archived rollout，避免 JSON thread store 删除后残留孤立 rollout；补充 appserver 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6/7 account rate limit wire shape：`RateLimitWindow.usedPercent` 在 app-server v2 account 响应层改为 Rust schema 的 `int32`，后端浮点值按 Rust 行为 round；`rateLimitResetCredits` 现在只在后端返回 summary 时输出对象，否则输出 `null`；补充 login response union 与 rate limit JSON shape 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6/7 account invalid_request 语义：`account/rateLimits/read`、`account/usage/read`、`account/workspaceMessages/read`、`account/rateLimitResetCredit/consume`、`account/sendAddCreditsNudgeEmail` 的 auth/参数拒绝现在返回 Rust v2 的 JSON-RPC `invalid_request(-32600)`，错误文案去掉旧 `invalid account request:` 前缀；更新对应单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6/7 account ChatGPT login URL：app-server `account/login/start` 的 ChatGPT 登录响应现在按 config `forced_chatgpt_workspace_id` 注入 `allowed_workspace_id` query；单 workspace 输出单值，allowlist 输出逗号拼接值，对齐 Rust v2 account 测试；补充 runtime router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread lifecycle 子树语义：`thread/archive` 现在按 session ParentThreadID 子树归档全部后代并移动 rollout；RuntimeRouter 对 archive/delete 按 Rust v2 接近顺序发送每个后代的 `thread/archived`/`thread/deleted`，`thread/fork` 也发送 `thread/started`，且 started 通知清空 copied turns；补充 runtime router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread fork `lastTurnId`：`ThreadForkParams` 新增 Rust v2 `lastTurnId`，session fork 会先按 turn id 截断到目标 turn 末尾，再应用 history mode/lastN；目标 turn 不存在时返回 `ErrInvalidThreadID`；补充 session/appserver 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 command/exec 与 process/spawn 的连接级 session 隔离和连接关闭清理：请求现在携带 transport connection id，`command/exec` 和 `process/spawn` active session 按 connection+process key 隔离，同一 processId/processHandle 可在不同连接并存；stdio/websocket 连接关闭会取消对应连接的长进程；补充连接隔离/清理单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `command/exec` request-level `sandboxPolicy`/`permissionProfile` 解析与安全执行分界：`internal/sandbox.SandboxPolicy` 已支持 Rust v2 `dangerFullAccess/readOnly/externalSandbox/workspaceWrite` JSON 和 legacy restricted read 拒绝；`command/exec` 已支持无需平台隔离的 `dangerFullAccess`/full-access permission profile，并注入 `CODEX_PERMISSION_PROFILE`；read-only/workspace/external 等需要真实隔离的策略在 Go sandbox runner 接入前返回明确 invalid_request；补充单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread fork session id 对齐：session store fork 默认 `SessionID` 改为新 fork thread id，仍保留显式 `ForkOptions.SessionID` override；app-server fork response 增加新 session id 断言；补充 session/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread fork ephemeral：`ThreadForkParams` 新增 Rust v2 `ephemeral`，session fork 支持返回不落盘的 ephemeral record，app-server ephemeral fork 不创建 rollout、不写 thread store，response thread 标记 `ephemeral:true` 且无 path；补充 session/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread fork `path` 入口：rollout 包新增 `ThreadIDFromPath` 解析 transcript meta，`thread/fork` 支持 path-only 请求并复用 session store fork；同时修复 ephemeral fork 带 `threadSource` 时误落盘的问题，fork rollout 创建失败不再静默吞掉；补充 rollout/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread resume `path` 与 response path 对齐：`thread/resume` 现在支持非空 transcript path 优先于 threadId，空 path 按未传处理；thread start/read/resume/fork/unarchive/metadata/list/search/rollback response 的 `thread.path` 统一输出 rollout `.jsonl` 路径而不是 Go thread store `.json` 路径；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread resume `history` wire/基础语义：`ThreadResumeParams.history` 从错误的 history-mode 枚举改为 Rust v2 `ResponseItem` raw slice；新增 history-to-session item 映射，支持 message/reasoning/tool call/output 常见项，`thread/resume` 有 history 时返回不落盘 ephemeral thread，preview 取首个 user 文本，并透传 model/modelProvider/cwd/serviceTier override；补充协议/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread fork config overrides 与 response shape：`ThreadForkParams` 补 Rust v2 的 model/modelProvider/cwd/serviceTier/permissions/config/runtimeWorkspaceRoots/approval/sandbox 等基础字段；fork 后写入 session metadata/extra，`ThreadForkResponse` 补齐和 start/resume 同类的顶层 model/modelProvider/cwd/serviceTier/approval/sandbox/instructionSources 字段并固定 JSON shape；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only thread read/resume：rollout parser 兼容 Rust `payload` 形态的 `session_meta`/`response_item` 行，rollout 包新增 transcript 到 `session.Record` 的反向映射；`thread/read` 和 `thread/resume` 在 Go JSON thread store 不存在时可 fallback 读取 rollout `.jsonl` 历史与 metadata；补充 rollout/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only thread fork：session store 新增 `ForkRecord(*Record, options)`，`Store.Fork` 复用该核心逻辑；`thread/fork` 在 JSON thread store 缺失时可从 rollout record fork 并持久化新 fork/thread rollout；补充 session/router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 thread resume response runtime settings：store/rollout-backed `thread/resume` 顶层 response 现在输出 model/modelProvider/cwd/serviceTier/approval/sandbox，且请求 override 优先于 record metadata；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only thread list/search：session store 新增 `AllRecords`/`ListRecords` 复用过滤排序分页；app-server list/search 合并 JSON thread store 与 rollout-only records，按 thread id 去重后统一过滤分页，避免旧 transcript 会话在列表和搜索里缺失；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only thread detail APIs：`thread/items/list` 和 `thread/turns/list` 在 JSON thread store 缺失时 fallback 读取 rollout record，保证 list/search 看到的旧 transcript 详情可打开；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only archive/unarchive/delete：`thread/archive`/`thread/delete` 在 JSON thread store 缺失但 rollout 存在时退化为单线程 rollout 移动/删除；`thread/unarchive` 支持 archived rollout-only thread 移回 active 并返回 thread；补充 router 单元测试代码，`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only thread name/metadata repair：`thread/name/set` 与 `thread/metadata/update` 在 JSON thread store 缺失但 rollout 存在时，会先从 `.jsonl` 重建并保存完整 `session.Record`，再复用现有 metadata patch 流程；补充 router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout rollback/inject parity：rollout 现在支持 Rust 标准 `event_msg`/`thread_rolled_back` marker，`RecordFromPath` 与 `InputItemsFromLines` 会在重建历史时应用 rollback；`thread/rollback` 会 best-effort 追加 rollback marker，`thread/inject_items` 会把注入项追加到 rollout，并且 rollout-only thread 会先 repair 到 JSON store 再执行；补充 rollout/router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only mutable lifecycle repair：`thread/increment_elicitation`、`thread/decrement_elicitation`、`thread/memoryMode/set`、普通 Router `thread/compact/start` 在 JSON thread store 缺失但 rollout 存在时，会先 repair 到 store 再复用原逻辑；补充 router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout compact replacement history：rollout 现在支持 Rust 标准 `compacted` item 的 `replacement_history`，`RecordFromPath` 与 `InputItemsFromLines` 重建时会用 replacement history 替换旧历史；普通 Router 与 RuntimeRouter compact 不再把压缩后的整段 history 当普通 items 重复追加，而是追加 compacted marker；补充 rollout/router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6 thread rollback notification parity：RuntimeRouter 收到 `thread/rollback` 时会先发送 Rust v2 `deprecationNotice`，summary 为 `thread/rollback is deprecated and will be removed soon`，再执行 rollback；补充 runtime router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5 rollout session_meta parity：rollout session meta 现在写入并读取 `forked_from_id`、`thread_source`、`originator`、`memory_mode`、`base_instructions` 等 Rust 字段；`createThreadRollout` 会从 `session.Record` 传入这些字段，`RecordFromPath` 会恢复到 `session.Record` lineage/metadata；补充 rollout 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only RuntimeRouter extra RPC：RuntimeRouter 的 `thread/goal/*` 与 conversation summary 现在在 JSON thread store 缺失时会从 rollout repair/fallback，保持旧 `.jsonl` transcript 与普通 Router 一致；补充 runtime router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6 rollout-only runtime turn：`turn/start`、runtime history replay、compact token status/manual compact 现在通过统一 `threadRecord` 从 rollout-only thread repair/fallback；rollout `session_meta.extra.previous_response_id/last_response_id` 会恢复到 session metadata，让旧 `.jsonl` transcript 可以直接继续执行新 turn；补充 rollout/runtime router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5 rollout session_meta 高保真字段：已补 Rust `SessionMeta` 的 `agent_path`、`dynamic_tools`、`selected_capability_roots`、`multi_agent_version`、`context_window` 透传；复杂字段用 JSON raw 保真保存，`thread/start` 会把 dynamic tools/capability roots 写入 session metadata 与 rollout meta，rollout-only repair 后 `turn/start` 也能从 metadata 恢复 dynamic tools；补充 rollout/router 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5 rollout turn_context/world_state 保真：rollout parser 现在识别 Rust `turn_context` 与 `world_state` rollout 行，保留 raw JSON 到 session metadata，并把 turn context 的 cwd/model/approval/sandbox/multi-agent/network/permission profile 等关键字段合并到 metadata/extra；补充 rollout 单元测试代码，`gofmt` 和 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。

## 下一步

- [x] 继续阶段 2：补 `turn/interrupt` 对后台 runtime 的取消控制。
- [x] 补 thread idle/running 防并发，避免同一 thread 同时启动多个 turn。
- [x] 继续阶段 3：把 Responses streaming delta 映射到 app-server notifications。
- [x] 继续阶段 3：补 Responses request id/metadata headers。
- [x] 继续阶段 3：把 `codex exec --json` streaming events 改成边接收边写 stdout，而不是 turn 结束后统一输出。
- [x] 继续阶段 3：补 Responses rate limit/model metadata 事件。
- [x] 继续阶段 5：补 DefaultRuntimeRouter/turn runtime 的真实 Responses agent 装配，让 app-server 默认能按 config/auth/provider 走远端模型，而不只是 LocalAgentRunner。
- [x] 继续阶段 5：补 PreCompact/PostCompact hook 接入 compact helper，并把 hook 输出/阻断语义对齐 Rust。
- [x] 继续阶段 3：补 provider catalog remote/在线刷新与 provider/model 选择基础。
- [x] 继续阶段 3：补 Responses retry/timeout。
- [x] 继续阶段 3：补 Responses `store` 和 `previous_response_id`。
- [x] 继续阶段 3：补 response debug context。
- [x] 继续阶段 3：继续补 OpenAI provider config/auth 基础。
- [x] 继续阶段 4：补 shell executor cwd/env/timeout/exit status 细节。
- [x] 继续阶段 4：补 shell executor Windows PowerShell/cmd 安全规则。
- [x] 继续阶段 4：补 shell executor stdout/stderr 截断与 Rust 输出格式。
- [x] 继续阶段 4：补 apply_patch executor Rust freeform grammar/错误格式。
- [x] 继续阶段 4：补 hooks before/after/payload rewrite/blocking/fatal。
- [x] 继续阶段 4：补 approval policy 接入 tool dispatch。
- [x] 继续阶段 4：补 sandbox profile 接入 shell executor。
- [x] 继续阶段 4：补 escalation request 和批准后重试。
- [x] 继续阶段 4：补 MCP required server 缺失时按 Rust exit 行为返回。
- [x] 继续阶段 4：补 MCP elicitation/resource read/file conversion 完整化。
- [x] 继续阶段 4：补 hosted/discoverable/deferred tools 语义完整化。
- [x] 继续阶段 5：补 session meta/thread item schema/rollout JSONL 字段对齐。
- [x] 继续阶段 5：补 rollout archive/unarchive/delete 与 CLI session 命令细节。
- [x] 继续阶段 5：补 resume last/session id/session name 与 fork snapshot。
- [x] 继续阶段 5：补 compact content items to text 与 compact token budget。
- [x] 继续阶段 5：补 turn runtime 接入 rollout recorder/compact 后继续 turn。
- [x] 继续阶段 5：补 turn timing/memory usage/session prefix。
- [x] 继续阶段 5：补 compact 后继续 turn/remote compact 基础。
- [x] 继续阶段 6：补 app-server protocol v2 turn/notification 类型完整化。
- [x] 继续阶段 6：补 protocol v2 item builder/mappers 对齐 Rust。
- [x] 继续阶段 6：补 config manager service 和 thread lifecycle RPC 细节。
- [x] 继续阶段 6：补 thread lifecycle RPC 通知和 goal/summary/history 细节。
- [x] 继续阶段 6：补 thread summary RPC 接入 session store。
- [x] 继续阶段 6：补 thread goal 持久化。
- [x] 继续阶段 6：补 thread history 游标/shape 细节。
- [ ] 继续阶段 6：补 account/environment/model/mcp/plugin/hook/process/fs/feedback RPC 细节。IN_PROGRESS：account sessions RPC、server request wire shape、thread elicitation Rust wire 名兼容、notification Rust 命名别名、`currentTime/read` server request/桥接和 current_time_reminder 配置读取/turn 注入已补；配置文件解析已改用 `github.com/pelletier/go-toml/v2`；MCP invalid_request 错误语义已补；environment/info 已接真实 exec-server WebSocket initialize/initialized/environment/info 查询；FS service 参数已改指针风格并尽量填充 metadata createdAtMs；feedback upload classification 校验和 invalid_request 错误码已补；model/provider capabilities params 已收窄为空 struct、response 收窄为三字段，model service 参数已改指针风格；permission/collaboration list 参数已改指针风格，permission profile response 已去掉内部 sandbox/network 字段，bad cursor 映射 invalid_request，collaboration response required nullable shape 已补；skills service 参数已改指针风格，config/write 参数错误映射 invalid_params；plugin marketplace add/remove/upgrade service 参数已改指针风格，plugin list/read/install/share RPC 参数已改指针风格，参数错误映射 invalid_request；apps/features/misc 体验 RPC 参数已改指针风格，apps list JSON 去掉 legacy apps 字段，app/feature bad cursor 映射 invalid_request；remotecontrol pairing/client 与 connection file watch 参数已改指针风格；config read/write/batch/external agent config 参数已改指针风格；MCP status/oauth/resource 参数已改指针风格；hooks list 与 Windows sandbox setup 参数已改指针风格；account manager get/login/cancel/reset credit 参数已改指针风格；turn/review service 入口参数已改指针风格；继续扫 app-server protocol 请求枚举和路由差异。
- [x] 继续阶段 6：补 command/exec streaming process 管理与 deferred final response。
- [x] 继续阶段 6：补 account login/logout 通知。
- [x] 继续阶段 6：补 feedback upload prepared snapshot。
- [x] 继续阶段 6：补 stdio transport 并发请求处理。
- [x] 继续阶段 6：补 Unix socket transport 基础。
- [x] 继续阶段 6：补 WebSocket transport 基础，优先采用成熟第三方 WebSocket 库承载 JSON-RPC 双向消息。
- [x] 继续阶段 6：继续补 hook/PTY RPC 细节。plugin hooks 基础已补；真实 PTY backend/resize 已接入，Unix 使用 `github.com/creack/pty`，Windows 使用 `golang.org/x/sys/windows` ConPTY；Windows TTY stdin 已按 Rust 归一化 LF/CRLF/Backspace；`command/exec` 与 `process/spawn` 的 TTY 输出继续复用现有 outputDelta/final response 语义。
- [x] 继续阶段 6：补 app-server schema RPC surface 清单：schema manifest 现在区分 client request、server request、notification，覆盖 account/model/plugin/mcp/fs/process/command/realtime/currentTime 等已声明 method，并按 Rust experimental/internal 面分组；method entry 已补 params/result 类型名索引，稳定面按 Rust `ClientRequest.ts` 收紧，类型名按 Rust fixture 的 `Apps*`/`Mcp*`/`WindowsSandbox*` 等命名输出；补充 schema 单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 schema fixture 签名差异：`configRequirements/read`、`externalAgentConfig/import/readHistories`、`memory/reset` 按 Rust 输出无 params；`thread/goal/*` 使用 `ThreadGoal*` 类型名；`account/chatgptAuthTokens/refresh` 使用 Rust fixture 的 `Chatgpt*` 大小写；按要求未运行 `go test`。
- [x] 本轮运行 `gofmt` 和 `go list -buildvcs=false ./...`，已通过；未运行 `go test`。
- [x] 继续阶段 6：继续抽查 model/plugin/mcp 剩余 payload schema。model list/capabilities 输出与 Rust fixture 基本一致；plugin marketplace add/remove/upgrade、share list/share context、skill read 响应继续收窄 legacy JSON 字段，并新增对应单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 6：继续抽查 MCP status/tool/resource payload schema。`McpServerInfo` JSON 不再输出内部 command/args，`Tool.inputSchema` 为空时输出 `{}`，`ResourceContent` 空 text 仍输出 `text:""`，`ResourceTemplate` 不再输出 `_meta`；补充 MCP 单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 6：继续抽查 app-server notification payload schema 与 turn/item payload 差异。先收紧 `serverRequest/resolved` payload：按 Rust fixture 输出 `threadId` 和 `requestId`，不再输出旧 `outcome` 字段；补充 notification 单元测试代码；按要求未运行 `go test`。
- [x] 继续阶段 6：补 Responses stream 到 app-server raw/reasoning/plan notification 贯通。已让 Responses SSE parser 识别 raw `output_item.done`、reasoning summary/text delta、reasoning summary part added 与直接 plan delta；RuntimeRouter 会发送 `rawResponseItem/completed`、`item/reasoning/*`、`item/plan/delta`；reasoning item 的 data/raw 会进入 app-server/exec session 持久化；补充 app-server streaming 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 plan mode `<proposed_plan>` streaming parser 与最终 plan item history。已新增 app-server 内部 proposed_plan stream parser，支持 tag 跨 delta 拆分和 EOF 闭合；plan mode 下剥离 proposed_plan 块、发送 plan item started 与 `item/plan/delta`，最终 session/history 保存 `plan` item 并让 agent message 只保留可见前后文；补充 parser/app-server plan mode streaming 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：继续抽查 thread item payload/schema 与 turn read/list payload 差异。`ThreadItem` 已新增渐进式 Rust union JSON：普通 user message、agent message、plan、reasoning 先按 Rust `ThreadItem` fixture 输出，item notification payload 复用同一 shape；复杂工具项暂保留旧兼容 envelope，后续逐类补 commandExecution/mcpToolCall/dynamicToolCall；补充协议单元测试代码；按要求未运行 `go test`。
- [ ] 继续阶段 6：继续补复杂 thread item union：commandExecution、fileChange、mcpToolCall、dynamicToolCall。IN_PROGRESS：`commandExecution` 已先补 shell/exec_command 基础映射，app-server runtime 写入 command/cwd/output/exitCode/duration data，`ThreadItem` JSON 对可识别 shell 工具输出 Rust `commandExecution` union；MCP executor 输出新增 `mcpToolCall` marker，app-server runtime 写入 server/tool，`ThreadItem` JSON 对 MCP call/output 输出 Rust `mcpToolCall` union；未知 function_call 保持旧 envelope 兼容；`dynamicToolCall` 已补 runtime executor、`item/tool/call` server request、Responses content_items 回灌、thread-start dynamicTools 持久化恢复、Rust union item started/completed shape 和单元测试代码，并补旧 transcript 中 `input_image`/`image_url`、`input_text` 等 content item 别名归一；`fileChange` 已补 apply_patch changes metadata、delete 内容 diff、app-server runtime marker、Rust `fileChange` union JSON、完整/未闭合 patch 输入流的 `item/fileChange/patchUpdated` notification、同 item changes 去重、Rust 同形 `item/fileChange/requestApproval` params、map 形态 changes 与 path/diff/kind/movePath 别名归一和单元测试代码；剩余继续扫 thread history replay 差异；按要求未运行 `go test`。
- [x] 继续阶段 6：补 `command/exec/write` 到 Rust `item/commandExecution/terminalInteraction` 通知映射。`CommandExecParams` 增加可选 thread/turn/item 上下文指针，active command 保存 terminal interaction context 和通知回调，stdin 写入成功后发送 Rust 同形 `TerminalInteractionNotification`；补充 command exec 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/shellCommand` 的 Rust userShell command execution 通知闭环。新增 appserver shell command runner：请求立即返回，后台用系统 shell 执行命令，standalone 时发送 `turn/started`/`turn/completed`，active runtime turn 存在时挂到当前 turn，并发送 Rust 同形 `commandExecution` item started、`item/commandExecution/outputDelta`、item completed；active turn 完成后会把 `shell.CommandRecord` 渲染出的 `<user_shell_command>` user message enqueue 到 `SteerMailbox`，供下一轮 agent sampling 读取；commandExecution 不写入 thread history；补充 runtime router 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6：补 `thread/shellCommand` 的 user shell record history 持久化。store-backed thread 完成 shell command 后持久化 `shell.CommandRecord.Render()` 生成的 `<user_shell_command>` user message，metadata 标记 `kind=user_shell_command`/`turnId`/`command` 并追加 rollout，但继续排除 commandExecution item；补充 runtime router 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/backgroundTerminals/list` 与 user shell command runner 的运行中终端联动。`ThreadExtraService` 新增指针参数的 `AddBackgroundTerminal` 和 remove helper，shell command 开始时登记 background terminal，完成时移除，保持现有 clean/list/terminate API 形状；补充 thread extra 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/backgroundTerminals/terminate` 对运行中 user shell command 的真实 cancel。background terminal registry 保存 cancel func，`thread/backgroundTerminals/terminate` 和 clean 会触发运行中命令 cancel，remove 会清理取消句柄，shell runner 使用 `context.WithCancel` 驱动 `exec.CommandContext`；补充 thread extra 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `thread/backgroundTerminals/list` 的真实 OS pid 更新。让 user shell command 启动后把 `cmd.Process.Pid` 回写到 background terminal，同时不丢失 terminate/clean cancel 句柄；补充 thread extra/runtime router 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补复杂 `ThreadItem` history replay 的 fileChange/dynamic 别名归一。让 Rust/旧 transcript 的 map 形态 changes、unifiedDiff/content diff、movePath/move_path、dynamic input_image/image_url 等字段恢复后继续输出 Rust union shape；补充 protocol/rollout 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 apply_patch streaming partial patch parser。完整 patch 继续用 `internal/applypatch` parser，未闭合 patch 用轻量 partial parser 提前生成 `item/fileChange/patchUpdated` changes，并对同一 item 去重；补充 appserver 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 MCP v2 参数/状态 wire shape。按 Rust v2 schema 以 `name/server/tool` 为 canonical JSON 输出，保留 `serverName/toolName` 输入兼容；`mcpResource/read` 也只输出 canonical `server`；`mcpServer/status/updated` 的 startup status 只输出 Rust 允许值，避免内部 `stopped` 泄露到 wire；补充 MCP/appserver JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 MCP `ThreadItem` history replay 的 result/error 形状。把旧/Rust 历史里的 `result.content` 补齐为 Rust v2 `McpToolCallResult{content,structuredContent,_meta}`，并保留对象形态 `error.message`；补充 protocol JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 `commandExecution.commandActions` Rust union shape。把 Go fallback 的旧 `exec` action 改为 Rust `unknown`，并让历史恢复中的 read/listFiles/search/unknown action 对象数组保留字段；`CommandAction` JSON 输出按 Rust union 字段收窄；补充 protocol JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 warning/deprecation/goal notification wire shape。`warning.threadId` 与 `thread/goal/updated.turnId` nil 时输出 JSON null，`deprecationNotice` 去掉旧兼容 `message` 字段；补充 notification JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 deprecated `ContextCompactedNotification` wire shape。Rust v2 notification 只输出 `threadId/turnId`，Go 内部保留 summary/tokenUsage 等扩展字段但不再放到该 notification JSON；补充 notification JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 `ThreadTokenUsageUpdatedNotification` wire shape。按 Rust v2 输出 `tokenUsage.total`、`tokenUsage.last`、`tokenUsage.modelContextWindow`，同时保留 Go 内部 flat token 字段供运行时代码继续使用；补充 notification JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 `McpServerOauthLoginCompletedNotification` nullable shape。让 `error` 和 `threadId` 一样按 Rust v2 输出 required nullable 字段，nil 时为 JSON null；补充 notification JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 server request wire shape。修正 command execution approval 的 `proposedExecPolicyAmendment` 大小写，并让 dynamic tool call server request 只输出 Rust canonical `tool/arguments`，保留旧 `toolName/input` 输入兼容；补充 server request JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 permissions approval response shape。`AdditionalFileSystemPermissions.read/write` 按 Rust required array 输出，nil 时编码为空数组而不是 JSON null；补充 server request JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6/7：收紧 account rate limits response shape。`rateLimitsByLimitId` 按 Rust required map 输出，nil 时编码为空对象 `{}`，同时保留 `rateLimitResetCredits:null` 语义；补充 auth JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3/7：补 provider auth command 错误细节。auth command 执行失败时保留 stderr 摘要进入错误信息，stdout 仍只用于解析 bearer token，避免排障时丢失 provider 返回的真实原因；补充 provider auth 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 7：补外部 ChatGPT auth tokens 的账号状态、provider 识别、auth.json 持久化和 Responses 401 外部刷新 server request broker。
- [x] 继续阶段 7：补 AuthManager/permanent refresh failure 分类与 app-server 可见状态处理。
- [x] 继续阶段 3：补 OSS/Ollama/LM Studio provider 接入基础。
- [x] 继续阶段 3：补 Bedrock/API key auth 接入基础。
- [x] 继续阶段 3：补 `exec --output-schema` structured output 请求约束。
- [x] 继续阶段 7：补 doctor provider reachability plan/probe，并让 doctor auth 检查尊重 `cli_auth_credentials_store`/`secret_auth_storage`。
- [x] 继续阶段 7：补 `network_proxy` config 读取与 doctor proxy 诊断摘要。
- [x] 继续阶段 7：补 doctor overall fail 的 CLI exit 语义。
- [x] 继续阶段 7：补 AWS Bedrock credential/SigV4 与 provider runtime 剩余差异。
- [x] 继续阶段 7：继续收紧 project `.codex` config 的 project root/trust 语义与 linked worktree hooks folder 差异。
- [ ] 继续阶段 7：继续扫 auth/account/doctor 剩余差异，补 route config/proxy 与诊断细节。IN_PROGRESS：personal access token 的 `whoami` 水合、PAT backend routing header、`account/read` 和 CLI `login --with-access-token` forced workspace 限制已补；CLI login status/logout/revoke 和登录前清理旧 auth 已补；agent identity 本地 account/限制解析、provider account/error 基础、`CODEX_ACCESS_TOKEN` 分类、Ed25519 key/JWKS/registration 基础、registered record 的 AgentAssertion provider auth、managed ChatGPT bootstrap/record persist/runner 接线、agent identity snapshot 深拷贝、bootstrap 1 小时 shared cooldown、provider 远端模型目录 source-of-truth、agent/task telemetry 承载已补；app-server account/logout revoke 与 account sessions add/list/logout/switch RPC 基础已补；account usage `dailyUsageBuckets` Rust 可空 shape、account backend read RPC 鉴权门、rate/usage/workspace 后端读取、sendAddCreditsNudgeEmail 后端调用、rate limit reset backend consume、account backend timeout、backend client 构造错误包装与 PAT routing 全覆盖已补；provider models endpoint client_version/timeout 已补；doctor JSON 支持报告结构化输出、terminal.env 诊断、git.environment 诊断、terminal.title 诊断、updates.status 诊断、app_server.status 被动诊断、mcp.config 本地诊断、sandbox.helpers 诊断、network.websocket_reachability 配置诊断已补；`state.rollout_db_parity` 已改为按 Rust state DB `threads` audit 行与 rollout 文件对比，missing/read error/count summary/scan cap 语义已补，并修复 archive/unarchive 移动 rollout；`system.environment`/`runtime.provenance`/`runtime.search` 细节继续贴近 Rust；下一步继续扫 doctor/account 剩余边界。
- [ ] 继续阶段 3：继续补 OpenAI provider config/auth 细节。
- [x] 继续阶段 6：补 `ThreadItem` MCP/dynamic arguments history alias 归一。把历史回放中的 `input`、`rawArguments`、`raw_arguments` 收束到 Rust canonical `arguments`，避免旧 transcript 或 Responses raw item 重放时输出 null/empty arguments；补充 protocol history replay 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 7：收紧外部 ChatGPT auth refresh server request shape。`account/chatgptAuthTokens/refresh` 的 `previousAccountId` 按 Rust nullable 字段输出，空值不省略；补充 server request JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 7：收紧 account workspace messages backend timeout。对齐 Rust `ACCOUNT_WORKSPACE_MESSAGES_FETCH_TIMEOUT=1000ms`，保留 token usage 10s；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：收紧 provider `wire_api="chat"` 迁移错误文案。对齐 Rust 的 How to fix / More info 提示，补充 provider 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：补 legacy `ollama-chat` provider 拒绝入口。`model_provider` 和显式 `--local-provider` 遇到 `ollama-chat` 时返回 Rust 同文案迁移提示；补充 provider/exec 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：补 `--oss` 模式下的 `oss_provider` config 解析。对齐 Rust 显式 `--local-provider` 优先、否则读取 config `oss_provider`、再 fallback `ollama` 的 provider 选择语义；补充 exec 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：收紧 unknown model provider 错误文案。`ProviderForConfigID` 未找到 provider 时对齐 Rust `Model provider \`id\` not found`；补充 provider 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：补 provider auth 配置默认值与相对路径解析。`auth.timeout_ms` 默认 5000ms 且显式 0 报错，`auth.refresh_interval_ms` 默认 300000ms 且显式 0 保留为禁用 proactive refresh，`auth.cwd` 在 config.toml/profile 加载时按配置文件目录解析为绝对路径；同时把 `wire_api` 默认值对齐为 `responses`，允许 Amazon Bedrock override 携带默认 `wire_api = "responses"`；补充 config/provider 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3：补 provider command auth 运行时缓存/刷新语义。Responses runner 现在会在首个请求前解析 command auth，按 `refresh_interval_ms` 复用或过期重跑命令，`refresh_interval_ms=0` 时禁用 proactive refresh 但 401 recovery 仍会强制刷新；provider auth command 带路径时按 `auth.cwd` 解析；补充 Responses command auth 缓存/过期刷新单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 3/7：收紧 provider account state 的 ChatGPT plan 默认值。ChatGPT/外部 ChatGPT token 缺少 plan metadata 时现在返回 `unknown`，对齐 Rust `ProviderAccount::Chatgpt { plan_type: Unknown }` 和 app-server required `planType` 语义；补充 provider 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 7：收紧 doctor `runtime.search` 路径命令可执行性诊断。路径形式 `rg` 现在在 Unix 要求 executable bit，目录/不可执行文件返回 warning，Windows 保持存在性判断；补充 doctor 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5：补 session `--name` 执行层解析。`resume/fork --name` 不再误走 picker，`archive/delete/unarchive` 也不再只读 `Target`，会按名称解析 session；`archive --name` 优先 active，`unarchive --name` 优先 archived，`delete --name --force` 可覆盖 active/archived；补充 app 层端到端单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 1：收紧 AgentLoop 多轮 Responses continuation。tool loop 每次 sampling 后会用上一轮 `response_id` 更新后续请求的 `previous_response_id`，避免 store/Responses 多轮工具调用一直挂在初始 response 上；补充 AgentLoop 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 1：收紧工具执行 fatal 边界。`tool.Router.Dispatch` 现在会把 executor panic 转成 fatal `FunctionCallError`，上层仍按 fatal tool error 处理但不会让工具 panic 打穿进程；补充 tool 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 1：补 AgentUsage `total_tokens` 贯通。Responses 非流式/流式 parser 现在保存 API 返回的 `total_tokens`，AgentLoop 聚合、app-server token usage notification、compact 阈值和 session metadata 都使用统一 total helper；无 total 的本地/旧路径仍回退 input+output；补充 model/turn 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 connection file watch notification shape。`ConnectionFileChangedNotification.changedPaths` 现在按 required array 输出，nil 时编码为空数组而不是 JSON null；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 1/8：补 exec JSONL usage total。`turn.completed.usage` 现在输出 `total_tokens`，优先使用 Responses API total，没有时回退 input+output；补充 exec JSON streaming 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 `environment/info` 真实 exec-server 查询。`EnvironmentManager` 现在通过 WebSocket 对远端 exec-server 执行 Rust 同序列 `initialize`、`initialized`、`environment/info`，解析远端 shell/cwd，未知环境错误文案对齐 Rust；`EnvironmentAddParams`/`EnvironmentInfoParams` 调用改为指针风格；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 FS service 指针参数与 metadata 时间。`FSService` 的 request 结构体参数已改为指针风格并补 nil 校验；`fs/getMetadata` 现在会在平台可用时填充 `createdAtMs`，Windows 使用文件创建时间，其他平台不可用时仍按 Rust 注释返回 0；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：补 feedback upload 请求校验。`FeedbackUploadParams` 新增指针校验，空 classification 返回 `ErrInvalidFeedbackRequest`，RuntimeRouter 映射为 Rust 同类 `invalid_request(-32600)`，prepared snapshot/extraLogFiles 行为保持；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 model/provider capabilities 参数和响应形状。`ProviderCapabilitiesReadParams` 现在是 Rust 同形空 struct，`ProviderCapabilitiesReadResponse` 收窄为 Rust 暴露的 `namespaceTools/imageGeneration/webSearch` 三字段，`ModelService.List`/`ProviderCapabilities` 改为指针参数并保留 nil 默认值；补充/调整 model 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 plugin marketplace service 参数。`AddMarketplace`/`RemoveMarketplace`/`UpgradeMarketplace` 改为指针参数并补 nil/必填校验，新增 `ErrInvalidPluginRequest`，RuntimeRouter 映射为 `invalid_request(-32600)`；补充 plugin 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 skills service 指针参数。`SkillsService.List`/`SetExtraRoots`/`WriteConfig` 改为指针参数，`WriteConfig`/`SetExtraRoots` 补 nil/必填校验并新增 `ErrInvalidSkillsRequest`，RuntimeRouter 映射为 `invalid_params(-32602)`；补充 app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 permission/collaboration list wire shape。`PermissionProfileService.List` 与 `CollaborationModeService.List` 改为指针参数；permission profile JSON 只输出 Rust v2 的 `id/description/allowed`，不再泄露内部 `sandboxMode/network`；collaboration mode 输出 required nullable `mode/model/reasoning_effort`，response 空 data 为 `[]`；permission bad cursor 文案改为 `invalid cursor: <value>` 并映射 `invalid_request(-32600)`；补充 app-server/sandbox 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 apps/features/misc 体验 RPC 参数与 wire shape。`AppService.Add/List`、`FeatureService.List/SetEnablement` 和 `MiscService` 的 auth/git/fuzzy/search-session 方法改为指针参数；`apps/list` JSON 只输出 Rust `data/nextCursor`，保留内部 `Apps` 字段但不再暴露 legacy `apps`；app/feature invalid cursor 文案带原始值并映射 `invalid_request(-32600)`；补充 apps/features/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 plugin 非 marketplace RPC 参数。`PluginService.List/Installed/Read/ReadSkill/Install/Uninstall/SaveShare/UpdateShareTargets/ListShares/CheckoutShare/DeleteShare` 改为指针参数；空 install、skill read、share 等必填错误统一包 `ErrInvalidPluginRequest` 并通过 RuntimeRouter 映射 `invalid_request(-32600)`；补充 plugin/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 remotecontrol 与 connection file watch 参数。`Manager.StartPairing/PairingStatus/ClaimPairing/ListClients/RevokeClient` 改为指针参数，CLI pairing 与 RuntimeRouter 同步传指针；`ConnectionFileWatchManager.Watch/Unwatch` 改为指针参数并补 nil 参数覆盖；补充 remotecontrol/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 config service 参数。`ConfigService.Read/WriteValue/BatchWrite/DetectExternalAgentConfig/ImportExternalAgentConfig` 改为指针参数，RuntimeRouter、CLI、hooks discovery、turn runtime 和 auth status 读取同步传指针；补充 config nil 参数单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 MCP service 参数。`MCPService.ListStatus/OauthLogin/ReadResource` 改为指针参数，RuntimeRouter 同步传指针；nil OAuth/resource 参数保留 Rust 同形 invalid MCP request 文案；补充 MCP 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 hooks 与 Windows sandbox 参数。`HookRegistry.List` 改为指针参数并支持 nil 默认空 filter，`WindowsManager.StartSetup` 改为指针参数并复用 nil 校验；RuntimeRouter 与 compact hooks 同步传指针；补充 hooks/sandbox 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 account manager 参数。`AccountManager.GetAccount/Login/CancelLogin/ConsumeResetCredit` 改为指针参数，pending login 保存值副本，RuntimeRouter 与 app-server/account 单元测试同步传指针；补充 nil login/cancel/reset credit 参数覆盖；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 turn/review service 参数。`TurnService.Start/Steer/Interrupt` 与 `review.Service.Start` 改为指针参数，RuntimeRouter 同步传指针；补充 nil 参数单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 thread extra 零值与指针调用边界。`GoalStore`/`ThreadExtraService` 增加懒初始化，零值实例可安全执行 goal/settings/shell/background terminal 写路径；`thread/shellCommand` standalone turn start 改为传 `*TurnStartParams`；补充零值单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 realtime manager 零值与指针参数边界。`Manager` 增加懒初始化，零值实例可安全执行 start/append/stop/state；`SetClock` 支持 nil receiver no-op，`Start` 先走参数归一/校验再报 manager nil；补充零值 lifecycle 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6/7：收紧 account manager 零值与 session 参数边界。`AccountManager` 增加统一懒初始化，零值实例可安全执行登录 pending map、rate reset、workspace messages 与 sessions 写路径；补充 account session nil 参数和零值 manager 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 6：收紧 remotecontrol manager 零值与指针遗留。`Manager` 增加懒初始化，零值实例可安全执行 enable/pairing/client list/revoke；nil receiver 对无 error 状态入口返回默认空结果，有 error 入口返回 `ErrInvalidRequest`；修正 pairing lookup 中旧的 `&params` 遗留；补充零值 lifecycle 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 5/6：收紧 rollout recorder 构造参数。`rollout.NewRecorder` 改为接收 `*CreateParams` 并补 nil 参数错误；exec/app-server/router/doctor 与 rollout 测试调用点同步传指针；补充 nil 参数单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 7/8：补 doctor Responses WebSocket handshake probe。`network.websocket_reachability` 现在会基于 active provider 构建 wss endpoint，合并 provider/auth headers，用 `github.com/coder/websocket` 在配置 timeout 内真实握手；成功返回 OK，失败记录 status/error 作为 warning；补充成功/失败单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 8：把 `codex mcp-server` 从假 ready notification 推进为真实 MCP stdio server 骨架。新增 `internal/mcp` stdio frame server，支持 `initialize`、`ping`、`shutdown/exit`、`tools/list`、`tools/call`、空 resources/prompts list；`initialize` 对齐 Rust 的 `codex-mcp-server`/`Codex`/`tools.listChanged` shape，`tools/list` 输出 Rust 同款 `codex` 与 `codex-reply` 工具 schema，`tools/call` 返回标准 MCP content/structuredContent/isError 形状并收紧 unknown field；CLI `mcp-server` 改为接 stdin/stdout；补充 mcp/app 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 8：把 MCP `codex`/`codex-reply` 工具接入现有 exec/turn runtime。`internal/mcp` 保持 runner 接口避免包循环，`internal/app` 新增 runner adapter：`codex` 转 `codex exec` 请求，`codex-reply` 转 `exec resume`，并把 model/cwd/sandbox/config/base instructions/developer instructions/compact prompt/approval policy 映射到现有 CLI/config override；工具结果返回真实 exec runner 的 `ThreadID` 与 `LastMessage`，错误作为 MCP `isError` tool result；补充纯映射单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 8：收紧 MCP streamable HTTP client wire 细节。HTTP 初始化协议版本改为 Rust 当前使用的 `2025-03-26`，所有请求补 `MCP-Protocol-Version` header，非 2xx 响应会保留最多 4KB body 到错误信息；补充 HTTP MCP header/error body 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 marketplace upgrade 后 installed/materialized plugin 刷新。`UpgradeMarketplace` 成功更新 git marketplace 后会重新加载升级后的 marketplace manifest，刷新同 marketplace 已安装插件的 manifest、skills、MCP 摘要和 `[plugins.<id>]` 配置；远端 marketplace 插件会先删除旧 materialized 目录再重新 materialize，避免 revision/source 变化后继续使用旧内容；enabled skill/hook roots 改为优先使用插件根目录；补充本地/远端升级刷新单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 selected capability remote environment skills loading。新增远端 skill discovery helper，turn runtime 在 capability root 指向已登记 environment 时通过 exec-server WebSocket 调用 `fs/walk`/`fs/readFile`，读取远端 `SKILL.md` 与 `agents/openai.yaml`，解析 frontmatter/interface/dependencies/policy 并以 `environment:` locator 注入 available skills；本地路径 fallback 保持不变；补充远端 environment skill root 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补远端 explicit skill invocation 注入。`SkillsListEntry`/`InstructionsSkillMetadata` 新增内部 contents 承载，远端 environment skill discovery 会缓存 `SKILL.md` 内容；显式 skill mention 现在优先用缓存内容生成 Rust 同形 `<skill>` input item，URI path 归一化会保留 `environment://`/`file://`，隐式 shell skill 检测跳过非本地 URI；补充 prompt/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP stdio client 并发 request multiplex。`stdioClient` 改为 pending map + read loop 分发 response，`writeMu` 串行写 frame，server request/progress 使用活跃 pending call 上下文，timeout/transport error 会关闭坏 session 并唤醒 pending；补充并发 out-of-order tool call 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth refresh 永久失败清理。HTTP MCP client 使用 `golang.org/x/oauth2` 的 `RetrieveError` 识别 `invalid_grant`/`invalid_client`/`unauthorized_client`，刷新失败时删除旧 store token，让后续状态回到 notLoggedIn，避免反复使用坏 refresh token；`ListStatusChecked` 会在 inventory 调用后重算 authStatus，确保同一轮 status 响应不保留刷新前旧状态；补充 invalid_grant 删除 token 与 status 重算单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 shell 命令触发的 implicit skill invocation。`turn.ToolDispatcher` 新增 post-tool input item hook，AgentLoop 会在工具输出后把追加 input items 带入下一轮 sampling；app-server skills context 基于当前 enabled skills 构造 shell 后置检测器，`exec_command` 命中本地 skill 时读取 `SKILL.md` 并注入 `<skill>` fragment，同时发送 warning，并对显式/重复隐式 skill 去重；补充 runtime router 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP HTTP OAuth 401 强制 refresh 重试。HTTP MCP request 若携带 OAuth bearer 但服务端返回 401，会基于已有 refresh token 强制刷新并用新 access token 重试当前请求一次；补充未过期旧 token 被拒后 refresh/retry initialize 的单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补显式 app mention 的 model-visible context。新增 `<app>` context fragment，turn runtime 会收集 `app://<connector id>` mention，从当前 AppService 合并目录/accessible/plugin connector 状态取 app 元数据并注入 input item；未知 connector 会生成 ChatGPT apps install URL 占位；补充 app mention runtime 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 bundled system skills root 发现。`install.InstallContext` 新增 bundled resource directory 查询，`SkillsService` 默认 roots 会在可用时纳入发行包资源目录 `codex-resources/skills/.system`，以 system scope 暴露 bundled skills；补充 install/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP HTTP session 失效恢复。HTTP client 的非 2xx 错误已结构化为 `mcpHTTPStatusError` 且保留原错误文案/body；带 session 的 request 遇到 404/410 会清空 stale session、重新 initialize/initialized，并对当前请求重试一次；补充 session expired retry 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP `roots/list` workspace roots。新增 `MCPRoot` 与 roots provider，MCP stdio/HTTP options 会携带 roots，服务端请求 `roots/list` 时返回当前 thread/active turn 的 `runtimeWorkspaceRoots` 或 CWD 转成的 `file://` roots；app-server `requireMCP` 注入基于 active turn params、thread metadata 和默认 CWD 的 provider；补充 roots/list 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP inventory pagination。HTTP/stdio inventory 的 `tools/list`、`resources/list`、`resources/templates/list` 现在按 MCP `nextCursor` 拉取所有页，并对重复 cursor/超大页数做保护；资源和模板列表仍保持现有容错语义；补充 HTTP inventory 分页单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace interface snake_case 兼容。`PluginInterface` 新增自定义 JSON Unmarshal，兼容 Rust remote marketplace/cache 常见的 `display_name`、`short_description`、`developer_name`、`logo_url`、`logo_url_dark`、`screenshot_urls`、`default_prompt`、`default_prompts` 等字段；`default_prompts` 优先于 legacy 单值 prompt，输出仍保持 app-server Rust v2 camelCase；补充 plugin interface 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：修正 MCP status 默认 detail。`ListStatusChecked(nil)`/省略 detail 现在按 Rust README 默认 full，不再因为 params nil 跳过真实 inventory；会拉取 tools/resources/resourceTemplates，并保留 toolsAndAuthOnly 的收窄语义；补充 HTTP MCP status 默认 full 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 MCP resource read 底层 helper。HTTP/stdio resource read helper 现在对 nil 参数或空 URI 返回同类 `invalid MCP request`，并在发起 `resources/read` 前裁剪 URI；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP HTTP SSE client response OAuth 401 refresh。HTTP SSE 处理服务端 `elicitation/create`/`roots/list` 等 JSON-RPC request 后回写 client response 时，如果旧 OAuth bearer 收到 401，会通过已有 refresh token 强制刷新并用新 access token 重试一次；普通 HTTP RPC 的 401 retry 也避免在 refresh 不可用时提前关闭错误响应体；补充 SSE elicitation 回写 refresh 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth dynamic client registration 服务级覆盖。`MCPService.OauthLogin` 在 server 未配置 `oauth_client_id` 且 discovery 返回 `registration_endpoint` 时，会通过本地 callback server 主路径完成 dynamic client registration、authorization URL 生成、callback code exchange 和 token store 保存；补充服务级单元测试代码锁定 dynamic client id/secret 使用；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin marketplace apps/appTemplates 读取。`AppSummary`/`AppTemplateSummary` 现在兼容 Rust marketplace 常见 snake_case 输入字段；marketplace manifest 和 `.codex-plugin/plugin.json` 的 `apps`、`appTemplates`/`app_templates` 会进入 `PluginDetail`，并把 app IDs、template canonical connector 和 materialized app IDs 回填到 `PluginSummary.AppConnectors`，materialized 远端插件刷新时不丢失这些元数据；补充 marketplace manifest apps/templates 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 plugin app/template metadata 到 app/list。`CapabilitySummary` 现在携带 installed plugin 的 app/template detail；app-server 会把这些元数据转换成 `apps.PluginConnector`，app placeholder 保留名称、描述、installUrl、logo 和 pluginDisplayNames；`MergePluginConnectors` 会按 connector ID 合并多个插件名并用插件元数据填补 directory 缺口；补充 apps/app-server 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP resource template metadata 透传。`MCPResourceTemplate` 的自定义 JSON 输出现在保留 MCP schema 中的 `icons` 与 `_meta`，`cloneMCPServerStatus` 也会深拷贝 template icons/meta，避免 status/list 或 app-server 响应丢 metadata；同步更新资源模板 wire-shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 installed plugin connector discoverable 候选。`PluginService.DiscoverableInstallCandidates` 现在对已安装且 enabled 的插件生成 app/template connector 候选，未安装插件仍只生成 plugin 候选以保持“优先安装插件”的语义；`list_available_plugins_to_install` 的 connector 条目会输出 `install_url`，并继续把 install URL 写入 request elicitation metadata；补充 plugin/tool 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP HTTP streamable session cleanup。HTTP client 新增 `Close`，在 `Refresh`、runtime reload、server config change 或同名 server cache key 变化丢弃 cached client 时，会在锁外 best-effort 发送带 `Mcp-Session-Id` 的 `DELETE` 释放服务端 session；补充 refresh 与 cache key 替换删除旧 session 的单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 codex_apps MCP accessible connector metadata。MCP connector parser 现在兼容 `_codex_apps`/`codex_apps` 中的 icon/logo/color/homepage/docs 字段，`app/list` 的 accessible merge 会保留 logo、branding 和 metadata；补充 MCP parser 与 app-server app/list 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 8：补 `stdio-to-uds` Unix 真实桥接。新增平台拆分实现，Unix 使用 `net.Dial("unix")` 把 stdin/stdout 双向转发到 socket，现有 Windows named pipe 路径保持兼容兜底；补充 Unix socket 桥接单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：补 exec-server `fs/copy` symlink/特殊文件语义。`copyPath` 改用 `os.Lstat`，目录复制保留 symlink 并跳过特殊文件，单独复制非 regular/dir/symlink 源返回 `fs/copy only supports regular files, directories, and symlinks`；补充 symlink 复制单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：补 exec-server `process/start` 的 `arg0` 语义。启动子进程时若请求携带 `arg0`，会覆盖 `exec.Cmd.Args[0]`，保持执行路径与 argv[0] 分离；补充子进程 helper 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：收紧 exec-server `http/request` header 语义。请求 headers 现在会跳过非法 header name/value，保留 tab 合法值；响应 headers 按 name/value 稳定排序，避免 map 顺序影响 JSON-RPC 输出；补充 HTTP 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：补 exec-server `fs/getMetadata.createdAtMs`。新增平台拆分的 creation time helper，Windows 读取 `Win32FileAttributeData.CreationTime`，Unix 保持 0；补充 Windows metadata 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：收紧 exec-server JSON-RPC 协议错误码。unknown method 现在返回 `method not found(-32601)`，params JSON 解码错误返回 `invalid_params(-32602)`，未分类执行错误继续保留 `internal error(-32603)`；补充 stdio 协议错误单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：收紧 exec-server sandbox/managed network 安全边界。`process/start` 收到 `sandbox`、`managedNetwork` 或 `enforceManagedNetwork` 时，在真实 backend 接入前返回 `invalid_request(-32600)`，避免静默全权限执行；补充 stdio 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：对齐 exec-server `fs/remove` 默认值。未显式传 `recursive/force` 时默认递归删除并忽略不存在路径，和 app-server/Rust FS 语义保持一致；补充默认删除目录树和 missing path 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：对齐 exec-server `fs/writeFile` invalid base64 文案。解码失败时返回 `fs/writeFile requires valid base64 dataBase64: ...`，和 app-server/Rust 文案保持一致；补充单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：收紧 exec-server `http/request.streamResponse` 边界。真实流式 response notification 尚未接入前，`streamResponse=true` 返回 `invalid_request(-32600)`，避免被静默降级为整包 body；补充 stdio 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：收紧 exec-server `fs/walk` wire shape。空目录或无错误时 `entries/errors` 现在编码为 `[]` 而不是 `null`；补充 JSON shape 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 10：修正 exec-server JSON-RPC request/notification framing。现在先解析顶层 envelope 判断是否存在 `id`，params 中带 `id` 的 notification 不再被误判为 request；补充 notification 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 策略调整：心脏阶段（阶段 1 核心 Turn Runtime、阶段 6 App-Server V2 RPC）完成标准改为 100% Rust 高保真对齐；编码阶段同步补齐直接依赖，不再允许阶段 1/6 的必需依赖长期空壳、假实现或静默降级。已更新使用规则、编码规范、当前总览、近期最高优先级和阶段 1/6 门禁。
- [x] 本轮 HEART 阶段 1/6 100% 对齐审计已收口：已沿 turn runtime、AgentLoop、app-server notification/thread item、session/rollout 依赖闭环持续补齐直接缺口，最新一轮完成 fork/resume/rollout/thread-list/metadata/rollback 剩余边界收口；按要求本轮仅运行 `gofmt` 和 `go list -buildvcs=false ./...`，未运行 `go test`。
- [x] 继续 HEART 阶段 1：收紧 no-router `turn.Runtime` 单轮结果语义。无 tool router 路径现在和 `AgentLoop` 一样把 prompt user message 与 assistant response item 写入 `AgentLoopResult.InputItems`，并在 agent 返回 tool call 但无 dispatcher 时返回明确错误，避免 session/rollout 依赖链丢用户输入或静默吞 tool call；补充 runtime 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/4：补 Rust `parallel_tool_calls` 语义。Responses 请求体现在接收 `AgentRequest.ParallelToolCalls`，exec/app-server 会按模型 `supports_parallel_tool_calls` 且非 Responses Lite 传入该开关；`ToolDispatcher` 按 tool spec 把连续 parallel-safe 调用并发执行，non-parallel 调用按模型顺序串行执行，结果按原顺序回灌；补充 Responses request 与 dispatcher 并发/串行单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补 token usage 通知的模型 context window。`RuntimeRouter` 发送 `thread/tokenUsage/updated` 时会根据当前模型 catalog 填充 Rust schema 的 `tokenUsage.modelContextWindow`，并新增 `ModelService.Info(*ModelInfoReadParams)` 供 runtime/app-server 查询模型能力；补充 runtime router 断言代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 3 直接依赖：补 Responses 请求体 Rust wire parity 的 `tool_choice:"auto"`。`ResponsesAgentRunner` 现在和 Rust client 一样在请求体中带 `tool_choice`，并更新 request body 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6/9：统一 app-server skill context budget 的模型来源。`instructionsWithSkillsContext` 不再用 fallback `ModelInfoFromSlug` 推断窗口，而是复用 runtime 的 `ModelService.Info`/`modelContextWindowForModel`，让 skill metadata budget 与当前模型 catalog 保持一致；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 3/6：对齐 app-server Responses 请求体 `store` 语义。`appTurnModelProviderConfig` 现在解析 provider info，并仅在 Azure Responses endpoint/provider 时把 `AgentLoopRequest.Store` 设为 true；普通 OpenAI app-server turn 不再无条件开启 store；同步更新 session/rollout restore 测试源码和 Azure provider store 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：对齐 Rust `build_reasoning` 请求体语义。`AgentRequest`/`AgentLoopRequest` 已贯通 `ReasoningEffort`/`ReasoningSummary`，exec 从 config reasoning effort 读取，app-server 从 turn/start `effort`/`summary` 读取；`ResponsesAgentRunner` 现在基于 provider/model catalog 判断 `supports_reasoning_summaries`，发送 `reasoning`、`include:["reasoning.encrypted_content"]`，支持 `ultra -> max`、summary `none` 省略、Responses Lite `context:"all_turns"`；runner 支持注入 provider `ModelsManager` 并在 runtime provider 构造时复用 provider catalog；补充 request body 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：对齐 Rust Responses `text.verbosity` 请求体语义。`AgentRequest`/`AgentLoopRequest` 已贯通 `ModelVerbosity`，exec/app-server 从 `model_verbosity` 配置读取；Responses request builder 会在模型 `support_verbosity` 为 true 时发送显式 verbosity 或模型 `default_verbosity`，并继续和 structured output schema 共用 `text` 对象，模型不支持时不发送；补充 request body 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：补 Responses Lite 请求体与 header 语义。`ResponsesAgentRunner` 会按 `ModelInfo.UseResponsesLite` 把 tools/instructions 移入 input 前缀的 Rust canonical `additional_tools` 和 developer message，顶层 `tools`/`instructions` 省略，parallel tool calls 强制关闭，reasoning context 使用 `all_turns`，input image `detail` 会在 Lite 请求中剥离；HTTP 请求补 `x-openai-internal-codex-responses-lite:true`，`BuildResponsesClientMetadata` 支持顶层 Lite metadata key；补充 request body/header/metadata 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1：补 AgentLoop turn timing profile 基础闭环。`AgentLoop`/no-router `Runtime` 现在会记录 turn started、sampling phase、tool blocking phase、sampling request count、TTFT/TTFM 近似点，并把 `TimingProfile *Profile` 放入 `AgentLoopResult`；`TimingGuard` 支持注入时间关闭，runtime 会把现有 `Now` 传入 loop；补充 loop/no-router timing profile 单元测试代码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/5/6：补 timing profile 持久化。exec 与 app-server 在生成 assistant/plan/reasoning 等模型响应 session items 时，会把 `AgentLoopResult.TimingProfile` 写入 metadata 的 `timingProfile` 与 `timing_profile`，后续 rollout/history/诊断可复用；补充 exec/app-server session persistence 单元测试断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3：补 Rust Responses timing metrics 请求开关。`include_timing_metrics` 已按 Rust session 级开关贯通 exec/app-server -> turn runtime -> Responses request，支持 snake/camel 与 responsesapi 前缀配置键；Responses HTTP 请求在开启时发送 `x-responsesapi-include-timing-metrics:true`，并补充 request header 单元测试源码断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3：补 Responses `x-codex-turn-state` sticky routing。`ResponsesAgentRunner` 现在会从非流式/流式 response header 捕获 `x-codex-turn-state`，并在同一 runner 后续 Responses 请求中回放该 header，对齐 Rust 同一 turn 内多次 sampling/compact 请求的 sticky routing 语义；补充 turn-state replay 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：补 `x-codex-beta-features` Responses header。新增 `features.ModelClientBetaFeaturesHeader`，按 Rust 规则把启用的 experimental feature 与默认/显式启用的 `remote_compaction_v2` 拼成 model client header；exec/app-server 从当前 config feature settings 贯通到 AgentLoop/Responses 请求，HTTP 层发送 `x-codex-beta-features`；补充 feature helper 与 Responses header 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/5/6：对齐 Rust `item_ids_enabled` 请求语义。`features.item_ids` 现在从 exec/app-server config 贯通到 AgentLoop/Responses request；Responses builder 默认在非 store 请求中清除历史 input item 顶层 `id`，仅在 `item_ids` 开启或 Azure/store 请求时保留，匹配 Rust `prepare_response_items_for_request`；补充默认剥离、item_ids 保留、store 保留的 request 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：补 Rust 默认 `prompt_cache_key` 贯通。exec 与 app-server turn 现在默认把当前 `threadID` 作为 `AgentLoopRequest.PromptCacheKey`，Responses 请求体会稳定携带该 key，对齐 Rust `ModelClient::prompt_cache_key()` 的 session/thread scoped 行为；补充 exec/app-server agent request 单元测试源码断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：对齐 Rust `service_tier` 请求过滤语义。新增模型 catalog service tier helper，`fast` 请求值会规范化为 `priority`，`default` sentinel 或模型不支持的 tier 不发送；exec/app-server 仅在 `fast_mode` 有效时从 turn/config 贯通 tier 到 Responses request；补充 catalog 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/6：补 Rust Responses zstd 请求压缩。引入 `github.com/klauspost/compress/zstd` 直接依赖，`enable_request_compression` 按 feature 默认/覆盖贯通 exec/app-server 到 `ResponsesAgentRunner`；仅 OpenAI provider 且 ChatGPT/外部 ChatGPT tokens/PAT/agent identity 等 Codex backend auth 时，对 JSON request body zstd 压缩并设置 `Content-Encoding:zstd`，签名器接收压缩后的 body；补充压缩与 API key 不压缩分支单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1：收紧 tool error 的 fatal/non-fatal 语义。`ToolDispatcher` 现在把未知工具、无效工具调用和 JSON 参数解码错误转成 failed tool output 回灌给模型，保留 `tool.Fatal`、context cancel/deadline 和普通运行时错误为致命错误，避免模型可修正错误直接中断整个 turn；补充未知工具与坏 JSON 参数单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3：修正流式 Responses `response_id` 闭环。streaming accumulator 现在把 `response.created/completed` 捕获的 response id 写回最终 `AgentResponse.ResponseID`，让 AgentLoop 工具调用后的下一轮 sampling 能正确设置 `previous_response_id`；补充流式 runner 单元测试断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/5/6：补多轮 sampling 的中间模型 response 持久化。`AgentLoopResult` 新增 `ModelResponses()` 统一输出去重后的模型响应序列；exec 与 app-server session item 生成改为遍历全部 response，保留第一轮 reasoning/assistant 等非工具 item，并把每个 item 的 `response_id` 写入 session/rollout/ThreadItem metadata；同时修正 app metadata 误读不存在的 `AgentResponse.ID`，改为 `ResponseID`；补充 exec/app-server session item 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 3/6：补 streaming notification 的 response id 透传。Responses stream accumulator 现在把当前 `response_id` 附到 output added/done/text delta/tool delta/plan delta/reasoning delta 事件；app-server `item/started` 在普通 streaming 和 plan-mode synthesized agent/plan item 中都会带 `responseId/response_id`，与最终 session item/ThreadItem 关联一致；补充 model/app-server streaming 单元测试断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/5/6：修正 session/rollout item 顺序。exec 与 app-server 生成 turn session items 时不再先写完全部 tool executions 再写模型 response，而是按 `AgentLoopResult.ModelResponses()` 顺序遍历，遇到 tool call item 时消费对应 tool execution，保证 reasoning/assistant/tool call/tool output/final assistant 的持久化顺序与 AgentLoop 实际执行顺序一致；补充顺序单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/5：修正 exec JSON final event 顺序。CLI exec 的最终事件 emitter 改为接收完整 `AgentLoopResult`，按模型 response 顺序输出非工具 item completed，遇到 tool call 时输出对应 tool started/completed/output，再输出后续 assistant 与 turn completed；非流式多轮 sampling 不再丢中间 reasoning/assistant item，事件顺序与 session/rollout 同步；补充 exec event 顺序单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/5/6：对齐 parallel tool calls 的持久化与事件顺序。对同一模型 response 内多个 tool call，exec/app-server session 生成现在先写全部 tool call item，再写全部 tool output item；exec JSON final emitter 同样先输出全部 tool call started/completed，再输出 outputs，避免 call/output 成对穿插破坏 Rust/AgentLoop 的并行工具顺序；补充双 tool call 顺序断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：收紧失败/中断 turn 的 diff tracker 生命周期。`finishTurnWithError` 与 `finishTurnInterrupted` 现在都会清理 active diff tracker，避免运行时错误、取消或中断后遗留 diff 状态污染同 thread 后续 turn；补充 failed/interrupted 清理断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3/5/6：补 AgentResponse 请求级 metadata 持久化。`AgentResponse` 新增 request id、server model、turn state、models ETag、response headers、rate limits 和 reasoning included 字段；Responses 非流/流式 runner 从 HTTP headers 写回这些字段；exec/app-server session item 生成把 metadata 贯通到 assistant/reasoning/tool call/tool output，避免多轮 sampling 丢第一轮请求诊断信息；补充 model/exec/app-server 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/3：补 streaming TTFT 与 Responses timing_metrics。`model.AgentRequest` 支持 request-level stream handler，Responses runner 会合并 runner/request handler；AgentLoop/no-router Runtime 用 stream delta/output item 记录 turn TTFT；stream accumulator 捕获 Rust `responsesapi.websocket_timing` 的 `timing_metrics` 并写入 `AgentResponse` metadata；补充 turn/model 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补失败后 active state 恢复语义覆盖。新增 app-server runtime 失败后恢复源码断言：agent 首次报错时 turn failed、thread status 进入 systemError；随后同 thread 再次 `turn/start` 可正常执行完成并回到 idle，确保失败路径不会遗留 active turn 或阻塞继续运行；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 Turn wire shape fixture。补 `Turn` marshal 单元测试源码，锁定 Rust v2 required nullable shape：空 items 输出 `[]`，默认 itemsView 为 `full`，`error/completedAt/durationMs` 未设置时输出 `null`；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 app-server `ThreadItem` union 与 schema stable surface。`ThreadItem.MarshalJSON` 现在补齐 Rust v2 剩余分支：`hookPrompt`、`collabAgentToolCall`、`subAgentActivity`、`webSearch`、`imageView`、`sleep`、`imageGeneration`、`enteredReviewMode`、`exitedReviewMode`、`contextCompaction`；`sessionItemFromResumeHistory` 同步支持这些 Rust union 读回，避免 resume/fork/history 丢语义；schema manifest 按 Rust `ClientRequest.ts` 隐藏旧兼容 `mcpServer/oauth/cancel` 与 `mcpServer/refresh`，并按 Rust `ServerNotification.ts` 把 process/realtime/threadSettings/turnModeration notification 放回 stable 面；补充 protocol/schema 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 MCP status resource schema 尾巴。`MCPServerStatus` 输出新增专用 resource/template wire 类型，按 Rust v2 `Resource`/`ResourceTemplate` 去掉 status 内部 `server` 和 template 的非 schema 字段，同时保留底层 MCP 工具 payload 兼容字段；清理 `MCPResourceTemplate` 残留 `Icons` 引用；补充 mcp 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 MCP OAuth completed notification。`MCPServerOauthLoginCompletedNotification` 的 `error` 字段改为 Rust optional 语义，成功完成只输出 `name/threadId/success`，失败时才输出 `error`；补充 notification 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 MCP elicitation union mode。`appserverMCPElicitationParams` 现在把 MCP `openai/form` client request 映射成 Rust v2 `McpServerElicitationRequestParams` 的 `mode:"openai/form"` 分支，保留 `threadId/turnId/serverName/_meta/message/requestedSchema` 形状；补充 runtime router 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 command approval server request 字段名。按 Rust v2 `CommandExecutionRequestApprovalParams.ts` 的精确 wire shape，把 proposed exec policy amendment 字段输出为 `proposedExecpolicyAmendment`；补充 server request broker 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 `item/tool/requestUserInput` server request。question 结构按 Rust 必填 bool 输出 `isOther/isSecret`，option 结构收窄为 Rust 的 `label/description`，内部 `id` 不出 wire；补充 server request broker 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 ChatGPT auth refresh server request response。app-server 包内 `ChatGPTAuthTokensRefreshResponse` 的 `chatgptPlanType` 改为 required nullable，和 Rust `ChatgptAuthTokensRefreshResponse.ts` 及 `internal/auth` 实际类型保持一致；补充 server request broker 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 legacy `applyPatchApproval` server request。`fileChanges` nil 时输出 `{}`，保持 Rust `ApplyPatchApprovalParams.ts` 的 required map 语义；补充 server request broker 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 `thread/settings/updated` notification。`ThreadSettings` wire shape 去掉内部 `multiAgentMode`，补 required nullable 字段，active permission profile 与 sandbox policy 转 Rust schema 对象；补充 thread extra 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 `ThreadGoal` required nullable 字段。`tokenBudget` nil 时输出 `null` 而不是省略，对齐 Rust `ThreadGoal.ts`；补充 thread extra 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 terminal interaction notification。`item/commandExecution/terminalInteraction` payload 去掉非 Rust `data` 兼容字段，保留 Rust schema 五字段；补充 notification 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：审计 command/process RPC schema 尾巴。`command/exec` response、`command/exec/outputDelta`、`process/outputDelta`、`process/exited` 与 Rust v2 schema 对齐；保留 Rust experimental `permissionProfile` 和 Go 内部 terminal interaction context 的兼容读入，不扩大响应 wire 面；按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 app-server stable RPC surface manifest。修正 `hooks/list`、`account/rateLimitResetCredit/consume` 的 params 类型名，补 `app/list/updated` 与 `configWarning` notification params，新增 `HooksListParams/HooksListResponse` alias；用 Rust `ClientRequest.ts`、`ServerRequest.ts`、`ServerNotification.ts` 对照后 stable method/params diff 归零；同时修复本轮暴露的硬编译问题（network proxy addr、daemon lifecycle、MCP discovery/stdio、appserver runtime helper、CLI 指针调用）；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补 schema manifest 回归断言源码。`schema_test.go` 已锁定 `HooksListParams`、`ConsumeAccountRateLimitResetCreditParams`、`AppListUpdatedNotification`、`ConfigWarningNotification` 四个 Rust 类型名，防止 stable surface manifest 回退；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口剩余 turn/thread/server-request payload 尾巴。补 `TurnError` required nullable 字段，规范 `mcpToolCall.appContext` 完整 nullable shape，收紧 `tool_search_call/tool_search_output` required 字段，修正 `UserInput.text_elements` 与 `TextElement.placeholder` Rust union shape，给 thread start/resume/fork response 补非 null 默认 `approvalPolicy/approvalsReviewer/sandbox`，移除 `Thread` wire 上内部 `extra/historyMode`，让 `RequestPermissionProfile` 输出 required nullable `network/fileSystem`，并限制 `CommandExecutionRequestApprovalParams` 不泄露 Go 内部字段；补充对应单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 realtime/background terminal payload 尾巴。对照 Rust `ThreadRealtimeStartedNotification.ts`/`ThreadRealtimeOutputAudioDeltaNotification.ts`/`ThreadRealtimeAudioChunk.ts`，app-server `thread/realtime/started` wire 字段改为 `sessionId`，`thread/realtime/outputAudio/delta.audio` 收紧为非 null 对象并保留 `itemId/samplesPerChannel` required nullable；`thread/backgroundTerminals/list` response 增加 `MarshalJSON`，空 data 固定输出 `[]`、`nextCursor` 固定 nullable；补充 notification/schema/thread-extra 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 `thread/name/updated` notification required-nullable 尾巴。`ThreadNameUpdatedNotification.threadName` 去掉 `omitempty`，空名称时按 Rust schema 输出 `threadName:null` 而不是省略字段；补充 protocol 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：修正 stable notification surface 尾巴。对照最新 Rust `ServerNotification.ts`，Go 不再声明或发送 Rust 未声明的 `model/safetyBuffering/updated`；保留 model 层 safety buffering metadata 解析作为内部事件，但 app-server runtime 忽略该事件，schema manifest 加 absent 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：修正 stable client RPC surface 尾巴。对照最新 Rust `ClientRequest.ts`，`account/workspaceMessages/read` 当前不是 stable client request，Go schema manifest 已移出该方法并加 absent 回归断言；runtime 兼容 handler 暂留但不再对外宣称；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：继续修正 stable notification surface 尾巴。对照最新 Rust `ServerNotification.ts`，Go schema 不再声明 Rust 未导出的 `guardianWarning` 和 `item/autoApprovalReview/{started,completed}`；移除对应 notification method 常量和 schema signature，保留内部结构体不对外暴露；补 absent 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：按用户指定改为只对照本地 Rust `D:\qax\reagent\dev\codex-main\codex-rs`，不再使用 GitHub/线上源码。本地 `ClientRequest.ts`/`ServerRequest.ts`/`ServerNotification.ts` stable surface diff 已归零；据本地 Rust 恢复 `account/workspaceMessages/read`、`model/safetyBuffering/updated`、`guardianWarning`、`item/autoApprovalReview/{started,completed}` stable schema，并补 `ModelSafetyBufferingUpdatedNotification` payload。继续收口 account/plugin/skills/apps payload：`rateLimitsByLimitId` nil 按 Rust required nullable 输出 `null`；`PluginSharePrincipal` 不再泄露 legacy `type/id`；`SkillsListEntry` 分组 entry 不再泄露 skill metadata 字段；`AppInfo` 不再泄露 legacy `enabled/lightIcon/darkIcon`。阶段 1 runtime 尾巴同步复核了 history rebuild、previous response id、parallel tool call 持久化顺序，当前未发现需立即改动点。`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收窄 turn request params stable marshal。`TurnStartParams`/`TurnSteerParams` JSON 输出现在按本地 Rust `TurnStartParams.ts`/`TurnSteerParams.ts` 只保留 stable 字段，legacy `Prompt` 会合成为 Rust `input` text item，`responsesapiClientMetadata/originator/runtimeWorkspaceRoots/additionalContext/config/dynamicTools` 等 Go 扩展不再从 stable params marshal 泄漏；保留解码/运行时内部字段兼容；补充 turn request shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 config/import payload 尾巴。按本地 Rust serde 运行时形状修正 `config/read`：`Config` 补齐 required nullable 字段和 `sandbox_workspace_write/tools.web_search/analytics/apps` 默认展开，`layers` 仅在 includeLayers 时输出，`ConfigLayerSource` 按 tagged union variant 精确输出；`ConfigRequirements` 及 `computerUse/models/network/hooks` 嵌套对象补 required nullable/空数组语义；external agent import/detect 已复核保持 required array/null shape；补充 config wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 thread lifecycle 与 review/turn response payload 尾巴。`thread/start|resume|fork` response 按本地 Rust 运行时结构补 `runtimeWorkspaceRoots` 空数组、`activePermissionProfile` nullable、`multiAgentMode:"explicitRequestOnly"`，`thread/resume` 补 `initialTurnsPage:null`；路由返回尽量贯通请求里的 runtime roots/permissions；`turn/start` response 的 `TurnRecord` 不再泄露内部 `threadId/prompt/input`，`startedAt` 按 Rust 初始 turn response 输出 `null`；review commit target 的 `title` 改为 required nullable，review turn 也按 Rust response 输出 nullable时间字段；补充 appserver/turn/review 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 account/apps/plugin/skills payload 第二轮。按本地 Rust `D:\qax\reagent\dev\codex-main\codex-rs` 复核 account token usage，确认 `dailyUsageBuckets` 是 `Array | null`，当前 nil->null 语义无需改动；`AppInfo` 的 `branding/appMetadata` 对外 JSON 现在会把 map 来源归一化为 Rust `AppBranding/AppMetadata` required nullable shape，并丢弃 `iconUrl/color/docsUrl` 等非 schema 扩展；`PluginSource` 改为 Rust tagged union marshal，`git/npm` nullable 字段输出 `null`，内部 `suggestion` source 对外折叠为 `remote`；`PluginInterface.defaultPrompt` nil 输出 `null` 且不再泄露 legacy `icon`；`PluginSkill.interface` 改为 required nullable，`PluginHookSummary` 不再泄露 `enabled`；`SkillToolDependency` 的 `type/value` 改为 required 字段；补充 apps/plugin/skills wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 plugin marketplace/share params 第二轮。`PluginShareSaveParams` 和 `PluginShareUpdateTargetsParams` 拆成不同 Go 类型，stable marshal 分别对齐 Rust 的 optional/required `shareTargets`，legacy `targets` 只作为输入兼容且不再输出，share target 不再携带 principal `name`；`PluginListParams` 补 Rust `cwds` 并隐藏 legacy `includeInstalled`；`PluginReadParams` 补 `remoteMarketplaceName` 解码/匹配，stable marshal 不再泄露 legacy `marketplaceName/remotePluginId`；`PluginInstallParams` stable marshal 不再泄露 `pluginId/marketplaceName`；`PluginMarketplaceEntry.interface` 会归一化为 Rust `MarketplaceInterface` 并丢弃扩展字段；本地 Rust v2 未导出 account sessions 相关 schema，workspace messages 已复核保持 `messages:[]` 与 nullable 时间字段；补充 plugin wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 plugin marketplace add/remove params 与 app/list notification 尾巴。`MarketplaceAddParams`/`MarketplaceRemoveParams` stable marshal 现在只输出本地 Rust v2 的 `source/refName/sparsePaths` 与 `marketplaceName`，legacy `name/url` 只保留输入兼容不再泄露；`AppListResponse` 增加内部完整列表，`app/list` 成功后发送 `app/list/updated`，响应仍按 cursor/limit 分页但 notification 使用完整合并列表，对齐 Rust `send_app_list_updated_notification` 与 `paginate_apps` 分工；补充 plugin/app-server wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 marketplace params required-nullable 细节。按本地 Rust `MarketplaceAddParams`/`MarketplaceUpgradeParams` 序列化测试，`marketplace/add` 的 `refName/sparsePaths` nil 时输出 `null`，`marketplace/upgrade` 的 `marketplaceName` nil 时输出 `null`，legacy 字段仍只作为输入兼容不出 stable wire；补充 plugin 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 account login params union shape。`LoginAccountParams` 增加 Rust union-aware marshal，`apiKey/chatgpt/chatgptDeviceCode/chatgptAuthTokens` 分支只输出对应字段，`chatgptAuthTokens.chatgptPlanType` nil 时输出 `null`；复核 account sessions 仅在 internal schema，不进入 stable `ClientRequest`，保持本地 Rust stable surface 一致；补充 auth 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补 app/list forceRefetch cached update 语义。`AppService` 新增只读 cache 合并入口，不触发 provider 调用；`RuntimeRouter.handleAppList` 在 `forceRefetch=true` 且 cache 非空时先发 cached `app/list/updated`，刷新后再发最终完整列表，若和 cached 完全一致则避免重复通知；这补齐 Rust cached/interim notification 的高价值可见行为，完整异步分阶段加载仍作为后续深水区；补充 app-server 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 plugin params optional-nullable 细节。按本地 Rust `PluginInstalledParams`/`PluginReadParams`/`PluginInstallParams`/`PluginShareSaveParams` 序列化测试，Go 现在输出 `cwds:null`、install suggestion names 数组或 null、read/install 的 `marketplacePath/remoteMarketplaceName` required nullable、share save 的 `remotePluginId/discoverability/shareTargets` required nullable；legacy forceRemoteSync、pluginId、marketplaceName、targets 等仍只作为输入兼容不出 stable wire；补充 plugin 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 plugin share checkout response。`PluginShareCheckoutResponse` 增加 stable marshal，按本地 Rust 只输出 `remotePluginId/pluginId/pluginName/pluginPath/marketplaceName/marketplacePath/remoteVersion`，内部兼容 `localPath` 不再泄露，`remoteVersion` nil 时保持 required nullable `null`；补充 plugin 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：复核 account stable response/notification 与 plugin share list/detail。account login completed/rate limit update/nudge response 已是本地 Rust required nullable/enum 形状；account sessions 继续只在 internal schema；`PluginSummary`/`PluginShareContext`/`PluginDetail` 现有 marshal 已覆盖 `remotePluginId/localVersion/shareContext/interface` nullable、`keywords/skills/hooks/apps/appTemplates/mcpServers` 空数组，以及 share context `sharePrincipals:null` 语义，本轮无需代码改动；按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 model/list params。`ModelListParams.IncludeHidden` 改为 `*bool` 三态指针，stable marshal 按本地 Rust 输出 `cursor/limit/includeHidden` required nullable，并隐藏内部 `refreshStrategy`；model list response/provider capabilities/permission profile summary 对照本地 TS 已复核保持 Rust shape；补充 model 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：收口 experimentalFeature RPC。`FeatureEntry` stable marshal 不再泄露 legacy `key`，`displayName/description/announcement` nil 时输出 `null`；`FeatureListParams` 输出 `cursor/limit/threadId` required nullable；`FeatureEnablementSetParams` 按本地 Rust 改为 `enablement` map stable shape，同时保留 legacy `enabled/disabled` 输入兼容但不再输出；`FeatureService.SetEnablement` 支持新 map；补充 features 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：复核 collaborationMode/hooks 轻量 RPC。`CollaborationModeMask` 已按 Rust 输出 `mode/model/reasoning_effort` nullable；`HooksListParams` 的 `cwds` 保持空时省略，`HooksListEntry` 的 hooks/warnings/errors 空数组和 `HookMetadata` 字段集与本地 Rust TS 一致；schema manifest 的 feature/hooks/collaboration 类型名已复核；本轮无需代码改动；按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：对照本地 Rust v2 payload，收口 `fs/watch`、`fs/readDirectory`、`fs/getMetadata`、`fuzzyFileSearch`、`feedback/upload` 等剩余 stable RPC 的 params/response required-nullable 语义和 legacy 字段泄漏；`FuzzyFileSearchParams` stable marshal 只输出 `query/roots/cancellationToken`，隐藏 `cwd/limit/exclude`，roots nil 输出 `[]`、cancellationToken nil 输出 `null`，空 roots 返回空 files；`fuzzyFileSearch/sessionUpdate` response 改为 Rust 空对象，并在当前同步实现中发送 `sessionUpdated/sessionCompleted` notifications；`FeedbackUploadParams` 按 Rust serde 输出 nullable `reason/threadId/extraLogFiles/tags`，`includeLogs=false` 省略；补 FS/fuzzy/feedback/router wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：完成本地 Rust v2 payload diff 的当前重点簇，覆盖 fs/watch/readDirectory/getMetadata、fuzzyFileSearch、feedback/upload 的 params/response nullable 和 legacy 字段泄漏。
- [x] 本轮继续 HEART 阶段 6：继续本地 Rust v2 payload 全量字段 diff，收口 `fs/readFile`、`fs/writeFile`、`fs/createDirectory`、`fs/remove`、`fs/copy`、`fs/unwatch`、`getAuthStatus`、`gitDiffToRemote`、`getConversationSummary` 的 params/response nullable、空数组、错误码和 legacy 字段泄漏；`CreateDirectoryParams`/`RemoveParams` stable marshal 现在按 Rust `Option<bool>` 输出 required nullable，`CopyParams` false 继续省略 recursive；`GitDiffToRemoteParams` 不再泄露内部 remote；`ConversationSummaryParams` 输出 Rust untagged `conversationId`/`rolloutPath`，runtime 同时支持新字段读取线程摘要，response stable marshal 改为 Rust `summary: ConversationSummary` 对象并保留内部字符串摘要兼容；补充 FS/misc/runtime wire shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：拆解并推进本地 Rust v2 payload 深层行为差异，`getConversationSummary.rolloutPath` 与 `fs/watch/unwatch` 连接级作用域已分别完成，`fuzzyFileSearch` 后台索引/取消和 remaining stable schema manifest 复扫继续留作下一步；继续只按用户要求运行 `gofmt` 与 `go list -buildvcs=false ./...`，不运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：补 `getConversationSummary.rolloutPath` 的深层 Rust 行为，支持相对 `codex_home` 路径解析、rollout 直读 summary、path 分支优先于 conversationId、store 缺失时修复保存，并保持 `ConversationSummary` 响应对象一致；补充 runtime rolloutPath 行为测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：补 `fs/watch/unwatch` 连接级作用域。`FSService` 的 watch 表改为 `(connectionID, watchID)` 作用域，runtime `fs/watch`/`fs/unwatch` 传入请求连接，同名 watchId 可跨连接共存，同连接重复仍返回 invalid request，`ConnectionClosed` 会清理该连接的 watch；补充 FS 与 runtime router 连接作用域测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：补 `fuzzyFileSearch` 后台索引/取消的 Rust 深层行为。一次性搜索现在支持 cancellation token 取消同 token 的 pending 搜索，空 query/空 roots 返回空 files，limit 固定为 Rust `MATCH_LIMIT=50`；session start/update/stop 改为带 latest query、generation 与 cancel 状态，stop 或过期 query 不再发送 stale updated/completed notification；补充 misc 与 runtime router 行为测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：复扫 remaining stable schema manifest 与本地 Rust TS surface。对照本地 `ClientRequest.ts`、`ServerRequest.ts`、`ServerNotification.ts` 后，Go stable client request/server request/notification method 集合与 params 类型名 diff 均为 0；`currentTime/read` 仅保留 experimental server request 分支；`go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：修正 `turn/started` notification 的 Rust `itemsView` 语义。Go app-server 启动 turn 时现在和本地 Rust 一样发送空 `items` 且 `itemsView:"notLoaded"`，避免启动事件暗示已加载完整 turn items；补充 runtime router notification 断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 active `thread/fork` 无 `lastTurnId` 时的 Rust interrupted snapshot 语义。Runtime fork 现在会在仅 fork 快照中追加当前 prompt 与 `<turn_aborted>` marker，不污染 source thread，并为 fork rollout 按 turn 顺序写入 `task_started`/items/`turn_aborted`，让 rollout rebuild 继续恢复 active turn 的 interrupted 状态；补充 runtime router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 Rust 匿名 `turn_aborted` rollout 边界 replay。Rust fork/resume history 可能写出无 `turn_id` 的 interrupted boundary，Go 现在会把它绑定到最近 replay 的 history item turn，并且局部 `RolloutTurns` snapshot 只覆盖已有 turn 状态、不再重排 restored thread turns；补充 rollout/protocol 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 history `thread/resume` 的 Rust `initialTurnsPage` 语义。Rust 即使 `excludeTurns=true` 也会用传入 history 构造初始 turns page；Go history resume 分支现在同样返回分页，并套用 resume payload redaction；补充 history resume 分页回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 history `thread/resume` 持久化语义。Rust 对传入 history 的 resume 会作为 forked initial history 创建正常 thread/rollout 并返回 path；Go 现在从 ephemeral-only 改为保存 record、创建 rollout、允许后续读取/继续 turn；更新 history resume 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补普通 `thread/fork` 保留 rollout turn snapshot/lifecycle。Rust fork 基于 rollout history，interrupted/failed/completed 边界会进入新 thread；Go 现在会 fork 前附加 source snapshots、过滤保留 turn 的 snapshots，并在新 rollout 写 lifecycle，避免 fork 后或 rebuild 后 turn 状态丢成 completed；补充 fork rollout rebuild 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 history resume 顶层 `turnId`/`turn_id` replay。Rust ThreadItem history 可直接携带顶层 turn id；Go 现在优先读取 payload 顶层 turn id，再回退到 passthrough metadata 和 item id，避免 resume/fork history turn 分组偏移；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 stale in-progress rollout turn 的 restored interrupt 语义。Rust cold resume/read 会在 thread 已 idle 时把 rollout 尾部未完成 turn 显示为 interrupted；Go `BuildThread` 与静态 `BuildTurnsResponse` 现在会把 stale `inProgress` 归一化为 interrupted，避免 restored thread 误显示仍运行中；补充 protocol 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 Rust `token_count` rollout replay 与 resume/fork token usage 恢复通知。Rust resume/fork includeTurns 会在响应后向连接补发 `thread/tokenUsage/updated`；Go 现在从 rollout 恢复 latest token usage/owner turn，并在 RuntimeRouter resume/fork 后发送同形状通知；补充 rollout/runtime router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 synthetic item-only history fork 的 Rust interrupted tail 语义。Rust `ForkSnapshot::Interrupted` 会把没有 lifecycle boundary 的 response-item history 尾部视为 interrupted；Go 普通 fork 现在会在 source 没有 `RolloutTurns` 时为 forked items 合成 turn snapshots，最后一个 turn 标 interrupted 并写入 fork rollout lifecycle；补充 rollout source fork 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：对齐 fork 显式 thread name 继承。Rust fork 会继承 source 的显式 name，不追加 `" (fork)"` 后缀；Go `Store.ForkRecord` 现在保留原 title，仍允许显式 `ForkOptions.Title` 覆盖；补充 store/router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：修正 app-server fork 新 thread source 继承。Rust app-server fork 生成的是新的 app-server 会话，不继承 source rollout 的 `cli` source；Go 普通 fork 与 active fork 现在都会把 forked record source 设为 app-server 侧来源，并继续保留 caller-supplied `threadSource`；补充普通 fork 与 active fork 响应断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：对齐 `thread/turns/list` summary 视图。Go 现在和 Rust 一样取第一条 user message 与最后一条 agent message，尾部工具/上下文 item 不再误入 summary；补充 turns summary 测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 `thread/turns/list` 运行中 turn 合并语义。RuntimeRouter 现在对 `thread/turns/list` 使用专用 handler，按 Rust 在分页前合并 active turn snapshot，并复用初始 user item 生成逻辑；补充 active turn 分页测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：修正 `thread/turns/list` cursor 语义。Go turns 分页现在按 Rust 使用 JSON `{turnId,includeAnchor}` anchor cursor，next 跳过 anchor、backwards 包含 anchor，limit 默认 25 且最大 100；items/list 数字 cursor 保持不变；补充 cursor 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 6：复核并补齐 `thread/turns/list` 默认 limit=25 与 max=100 的 Rust 分页细节，补充默认分页回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补真实 Rust rollout `event_msg` replay 的核心路径，支持 `turn_started/turn_complete/turn_aborted/user_message/agent_message` 生成 session items 与 turn snapshots，`thread/turns/list`/`BuildThread` 会优先保留 rollout turn 状态、时间和显式空 turn；补充 rollout/appserver 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补真实 Rust rollout `event_msg user_message` 在线程列表 preview/search 轻量路径中的识别；补充 rollout 列表回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：修正 rollout repair/store 命中后 `RolloutTurns` 内部 snapshot 丢失的问题；普通 Router 与 RuntimeRouter 都会按需从 rollout 附加 snapshot，连续 `thread/turns/list` 仍保留 interrupted/时间；补充 runtime router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 Go runtime 写入 Rust 风格 rollout turn lifecycle 边界。Go app-server runtime 现在会在 rollout 中写入 Rust 风格 `event_msg` lifecycle（`task_started/task_complete/turn_aborted`），让后续 replay 能恢复 completed/interrupted 状态、时间字段与显式空 turn；补充 completed/interrupted runtime rollout 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 Rust `error` + `turn_complete` 失败 turn 的 rollout 写入与 replay。Go runtime 失败路径现在写入 `error` 后接 `task_complete`，rollout replay 会把 error 绑定到当前 turn、保留真实 turn id、恢复 `failed` 状态、错误消息和时间边界；补充 rollout 与 runtime writer 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补中断 turn 的 Rust `<turn_aborted>` history marker 写入。Go app-server runtime 现在会在 config 构建后、agent 运行前持久化本轮用户 prompt，避免中断/失败丢 prompt；中断时在 `turn_aborted` lifecycle 前追加模型可见 `<turn_aborted>` user marker，并让成功完成路径过滤已提前落盘的 prompt 防重复；补充 interrupted rollout marker 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 本轮继续 HEART 阶段 1/6：补 Rust `item_completed` rollout event replay 基础，把 `payload.item` 中的 `Sleep`/`CommandExecution` 等 Rust TurnItem 恢复为 Go session item，支持 Rust PascalCase TurnItem 类型、命令数组 shell 展示、PathUri 字符串提取、Duration 毫秒换算与 parsed_cmd commandActions 基础归一化，避免 rollout-only thread 的工具/状态 item 在 restored turns 中丢失；补充 rollout 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：修正 `thread/resume.path` 与 `thread/fork.path` 的 Rust source 优先级。Go 现在只要请求显式带 path，就直接按该 rollout path 读取 record/history，而不会被同 ID store 记录抢先命中；`initialTurnsPage` 也复用同一路径源；补充同 ID store/rollout 内容冲突的 resume/fork 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 `thread/resume.path` 与 `thread/fork.path` 的目录错误语义。Go app-server 现在会在读取 rollout 前检测 path 是否为目录，并返回稳定的 invalid request 文本 `path is a directory`，避免泄漏底层系统错误 `Is a directory`；补充 resume/fork 目录 path 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐当时 Rust `thread/items/list` 暂未支持语义。Go 当时保留 schema 声明但 processor 返回 method-not-found，错误码 `-32601`、文本 `thread/items/list is not supported yet` 与 Rust v2 一致；旧成功路径测试改为断言 unsupported；后续 HEART 阶段 6 已重新补齐 rollout/store-backed 支持；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 failed turn 后 `thread/read` 的 Rust status 语义。RuntimeRouter 现在专门处理 `thread/read`，委托 ThreadRouter 读取后用 ThreadStatusManager 覆盖 response thread status，失败后读回 `systemError`，恢复成功后读回 `idle`；includeTurns 时同步用 loaded status 归一化 turns；补充 runtime router 失败/恢复 read 断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 paginated rollout 的 Rust legacy history path 拒绝语义。Go 从 rollout 文件恢复的 transient record 现在带 `FromRollout` 标记；当 rollout metadata 为 `historyMode:paginated` 时，`thread/read includeTurns`、`thread/turns/list`、`thread/resume` 以及 RuntimeRouter turns/list 都返回 `-32601 paginated_threads is not supported yet`，metadata-only read 仍允许；补充 rollout-only paginated 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐空 `thread/start` 的 Rust rollout 延迟物化语义。Go 现在为空 prompt thread 预计算并返回 rollout path，但不创建空 jsonl；path 保存在内部 metadata，`thread/read` 物化前仍返回同一路径；第一次 `turn/start` 写 rollout 时会按 store record 创建文件再追加 lifecycle/items；补充 Router 未物化 path 与 RuntimeRouter 首 turn 物化回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补未物化 loaded thread 的 Rust read/list 拒绝语义。空 `thread/start` 在首条用户消息前若调用 `thread/read includeTurns=true` 或 `thread/turns/list`，Go 现在返回 JSON-RPC invalid request，并带 Rust 文本 `includeTurns is unavailable before first user message` / `thread/turns/list is unavailable before first user message`；RuntimeRouter turns/list 也同步拦截；补充空 start 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补未物化 thread fork 的 Rust 拒绝语义。空 `thread/start` 在首条用户消息前调用 `thread/fork` 时，Go 现在返回 JSON-RPC invalid request，并带 Rust 可见文本 `no rollout found for thread id`，避免 fork 出空历史 thread；补充空 start fork 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 lifecycle 响应的 Rust loaded status 语义。Go `thread/start`/`thread/resume`/`thread/fork` 响应现在把返回 thread 标为 `idle`；RuntimeRouter 成功 start/resume/fork 时会无通知地 upsert loaded status，保证后续 `thread/read` 在物化前读回 `idle`，同时不破坏 Rust fork 先 `thread/started`、不先发 `thread/status/changed` 的通知顺序；补充 start/fork/active-fork/read 状态断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `turn/start` 空 input 仍运行模型请求的语义。Go `AgentLoop` 不再要求 prompt 或 input items 非空，空 `turn/start` 会进入 agent/model sampling；补充 AgentLoop 空输入与 RuntimeRouter 空输入 turn 回归断言源码，确保 agent request 的 prompt/input 均为空但 turn 正常 completed；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `turn/start`/`turn/steer` 用户文本输入最大长度限制。Go 已按 Rust `MAX_USER_INPUT_TEXT_CHARS = 1 << 20` 统计 prompt 与 text input 的字符数，mention/localImage 等非文本项不计入；长度错误返回 Rust 文案、`-32602` JSON-RPC code 与 `input_error_code/max_chars/actual_chars` data；补充参数校验与 runtime router 错误 shape 单元测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start` settings override 互斥校验。Go 现在会在 app-server runtime 入口拒绝同时携带 `permissions` 与 `sandboxPolicy` 的 turn/start，请求在 turn 启动和通知前返回 invalid request 文案 `` `permissions` cannot be combined with `sandboxPolicy` ``；补充 runtime router 断言源码确认 `-32600` 且无通知；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust app-server 远程图片 URL 拒绝。Go 现在会在 `turn/start`、`turn/steer` 与 `thread/inject_items` 入口拒绝 `http(s)` 图片 URL，返回 invalid request 文案 `remote image URLs are not supported; use an inline data URL instead`，data URL 与 localImage 继续允许；补充 runtime/router 断言源码覆盖大小写不敏感 scheme 与 injected tool output；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust image input 默认 detail。Go app-server 现在会在含 image/localImage 的 `turn/start` 中把当前用户输入作为结构化 user message 传给 agent/model，为未显式设置 detail 的 `input_image` 补 `detail:"high"`，显式 detail 继续透传，并同步持久化 image content；补充 runtime agent request 断言源码覆盖 text+image+localImage；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补 Rust localImage 模型输入语义。Go app-server 现在会在 `turn/start`/`turn/steer` 结构化输入中读取 localImage 文件并转换为 data URL（使用标准库 MIME 探测），读失败时生成模型可见 input_text 占位错误而不是把本地路径当 image_url 发送；localImage 同步包裹 `<image ...>`/`</image>` 文本，并避免 mention/skill path 被误当图片；补充 runtime agent request 断言源码覆盖成功和读失败；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust dynamic tool 远程图片输出错误文案。Go dynamic tool 输出含远程 `inputImage` 时现在返回模型可见 `remote image URLs are not supported; use an inline data URL instead`，并保持 failed/success=false 语义；补充 dynamic tool runtime 断言源码覆盖 app item 与模型 content_items；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start.environments` unknown id 拒绝。Go app-server runtime 现在会在 turn 启动前校验请求里的 environmentId，未知 id 返回 invalid request 文案 `unknown turn environment id \`...\``，避免静默忽略 environments override；补充 runtime router 断言源码确认 `-32600` 且无通知；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start.environments.cwd` 绝对路径校验。Go app-server runtime 现在会在 environments 前置校验里拒绝非绝对 POSIX/Windows cwd，返回 Rust 文案 `invalid cwd for environment ... path ... does not use absolute POSIX or Windows path syntax`；补充 runtime router 断言源码确认相对路径先于 unknown id 报错；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/settings/update` permissions+sandboxPolicy 互斥错误。Go runtime router 现在会在调用 ThreadExtraService 前返回 invalid request `-32600` 和精确文案 `` `permissions` cannot be combined with `sandboxPolicy` ``；补充 runtime router 断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/settings/update.serviceTier:null` default sentinel 语义。Go 现在把 null 更新保存/通知为 `"default"`，并继续依赖请求层过滤 default，不向模型发送 service_tier；补充 thread extra/runtime router 断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/settings/update` 影响后续 `turn/start` 的语义。Go runtime 现在把已接受的 thread settings 合并为下一次 turn 的默认 model/cwd/serviceTier/effort/summary/personality，显式 turn override 继续优先；补充 runtime router 断言源码覆盖 updated cwd 参与项目配置加载与 agent request 字段；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start` settings override 发 `thread/settings/updated` 并成为后续默认值的语义。Go runtime 现在把显式 turn/start model/cwd/serviceTier/effort/summary/personality 写入 ThreadExtraService、发送 settings notification，并保留当前 turn 显式 override 优先级；补充 runtime router 断言源码覆盖通知与后续 turn 继承；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start.serviceTier:null` 双层 Option 语义。Go `TurnStartParams` 现在区分未传与显式 null，null 会写入/通知 `"default"` sentinel，当前 turn 也不再回落到 config `service_tier`；补充 turn 解码与 runtime router 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust thread lifecycle `serviceTier` 双层 Option 语义。Go `thread/start|resume|fork` 现在区分未传与显式 null，null 归一为 `"default"` sentinel 并贯通 metadata/response，fork active/runtime 路径同样返回 effective service tier；补充协议解码与 Router lifecycle 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/goal/set.tokenBudget` 双层 Option 语义。Go `GoalSetParams` 现在区分 omitted=keep、null=clear、number=set，并在 goal builder 中按字段出现语义清除已有预算；补充参数解码与 GoalStore 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/metadata/update.gitInfo` 空 patch 拒绝语义。Go 现在在省略 `gitInfo` 或传 `{}` 时返回 invalid request 文案 `gitInfo must include at least one field`，字段级 null/string 三态继续保留；补充 metadata patch 回归断言源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/unsubscribe` connection 订阅状态语义。Go RuntimeRouter 现在记录 start/resume/fork 成功后的连接订阅，`thread/unsubscribe` 对 loaded thread 返回 `unsubscribed`/`notSubscribed`，store-only cold thread 返回 `notLoaded`，连接关闭会清理订阅；补充 runtime router 回归测试源码覆盖 start/resume/fork/connection closed/cold loaded 状态；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/loaded/list` 内存 loaded thread 语义。Go RuntimeRouter 现在从 ThreadStatusManager 的 loaded 状态列线程，store-only cold 记录不再被误报 loaded，并按字典序分页、cursor 插入点语义、limit=0 至少返回 1 条；补充 runtime router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust archive/delete 成功后的 loaded teardown 语义。Go RuntimeRouter 成功 `thread/archive`/`thread/delete` 后会清 ThreadStatusManager loaded 状态和 connection subscription，`thread/loaded/list` 与 `thread/unsubscribe` 不再把已移除 thread 当 loaded；补充 runtime router 回归测试源码覆盖 archive/delete；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust app-server ephemeral thread runtime 语义。RuntimeRouter 现在为 `thread/start.ephemeral` 与 `thread/fork.ephemeral` 保存 live-only 内存 record，不落 session store/rollout、不暴露 path、不进入 `thread/list`，但继续支持 loaded/read/turn 写入；`thread/read includeTurns`、`thread/turns/list`、`thread/delete`、goal、metadata update 对 ephemeral 返回 Rust 文案；补充 runtime router 回归测试源码覆盖 start/fork/pathless/unlisted/delete/read/list/goal/metadata/turn 写入；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/archive` 对未物化 rollout 的拒绝语义。空 `thread/start` 在首条用户消息前 archive 现在返回 invalid request，文案包含 `no rollout found for thread id`，物化后 archive 仍成功；补充 runtime router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.historyMode=paginated` 拒绝语义。`legacy` 继续允许，`paginated` 在普通 start 与 RuntimeRouter ephemeral start 入口都会返回 method-not-found `-32601 paginated_threads is not supported yet`；同步修正旧测试不再把 paginated start 当成功路径，并补 plain/runtime 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.environments` 前置校验。RuntimeRouter 的 thread/start 现在像 turn/start 一样拒绝 unknown environment id、相对 cwd 和缺失 environmentId，返回 Rust invalid request 文案；补充 runtime router 回归断言覆盖 unknown id 与相对 cwd；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6/1：对齐 Rust `thread/start` instructionSources 基础语义。RuntimeRouter start 响应现在返回非空 global/project AGENTS.md 来源，显式 `environments: []` 时只保留 global；加载到的 AGENTS 文本写入 thread metadata，并让后续 turn/start 在无显式 baseInstructions 时继承；补充 runtime router 回归断言覆盖 response sources 与模型 instructions 继承；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start` CWD effective config response 基础语义。RuntimeRouter start 成功后按请求 CWD 读取 effective config，填充响应/record 的 model、modelProvider、reasoningEffort、cwd，确保 project config 中 `model_reasoning_effort` 会出现在 ThreadStartResponse，并让后续 turn 继承；补充 runtime router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.serviceTier` 模型支持过滤语义。请求显式 `"default"`/`null` 保留 default sentinel，模型支持的 tier 保留，未知 tier 不写入 response/metadata；普通 Router、RuntimeRouter effective config 与 ephemeral start 路径已统一过滤，并补充普通/runtime 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.runtimeWorkspaceRoots` effective roots 语义。无显式 roots 时 response/record extra 使用有效 CWD 的绝对路径；显式 roots 保留绝对 roots、去重，且不把 profile workspace roots 混入 runtimeWorkspaceRoots；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust thread lifecycle `permissions` 与 `sandbox` 互斥校验。`thread/start`、`thread/resume`、`thread/fork` 在启动/物化/通知前拒绝二者同时出现，返回 `-32600` 与 Rust 文案 `` `permissions` cannot be combined with `sandbox` ``；普通 Router 与 RuntimeRouter start/fork 旁路均已接入，并补充回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.allowProviderModelFallback` 的 Bedrock 静态 catalog 语义。provider 为 `amazon-bedrock` 且允许 fallback 时，unsupported model 回落到 Bedrock 默认模型、supported Bedrock model 保留；非 Bedrock/dynamic provider 不做 fallback；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start` elevated sandbox 持久化 project trust 语义。显式 `workspace-write`/`danger-full-access` sandbox 且 CWD 尚无 trust_level 时写入 trusted，nested git CWD 信任 git root；read-only 不写，已有 untrusted/trusted 不覆盖；补充 RuntimeRouter 回归测试源码，并验证后续 start 可加载 project config；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust loaded thread 的 `runtimeWorkspaceRoots` 传播语义。`turn/start` 带 runtime roots 后会持久化去重后的 effective roots，后续 `thread/resume excludeTurns` 读回；`thread/resume` 显式 roots 会更新 record extra，`thread/resume`/`thread/fork` response 与 fork record extra 已统一走 roots 归一逻辑；补充普通 Router 与 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust loaded thread 的 cached `instructionSources` 语义。`thread/start` 发现的 AGENTS 来源现在持久化到 thread record extra，`thread/resume` 会回放缓存 sources；AGENTS.md 后续被删除也不影响 resume 响应；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume` running thread 禁止 `history` override 的语义。loaded thread 存在 active turn 时，带 `history` 的 resume 会在普通恢复前返回 invalid request，错误文本包含 cannot resume/thread/with history/running；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust running thread `thread/resume.path` stale path 拒绝语义。loaded thread 正在运行且请求带 path 时，path 必须指向该 thread 的当前 rollout；否则返回 invalid request，文本包含 stale path；保留当前 rollout path rejoin 成功路径；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust running thread resume rejoin override mismatch 语义。loaded thread 正在运行时，`thread/resume` 的 model/cwd override 不再覆盖正在运行的 thread config，`initialTurnsPage` 会用 live in-progress turn 重建；补充 RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume` 按 ID 拒绝 archived session 的语义。请求未显式 path 且目标 session/rollout 已 archived 时，会返回 invalid request，并提示先 `codex unarchive <thread>`；补充 Router 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork` 继承 source session `source` 的语义。普通、ephemeral、active fork 不再无条件把 `metadata.source` 改成 appServer，而是保留源 thread/rollout 的 source；`threadSource` 仍按请求覆盖；同步调整并补充 Router/RuntimeRouter 回归测试源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补 Rust `thread_start_response_excludes_empty_project_instruction_source` 回归保护。RuntimeRouter 的 `thread/start` instruction source 测试现在覆盖项目 `AGENTS.md` 为空时只返回 global source，避免空项目 source 混入响应；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list sourceKinds=[]` 默认 interactive-only 语义。Go `BuildListOptions` 现在默认筛选 cli/vscode/appServer 交互来源，空 sourceKinds 不再列出 exec；补充 protocol options 与 Router list 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` 最大 limit 语义。Go `BuildListOptions` 现在把超过 Rust 最大页大小的 limit clamp 到 100，Router list 会返回 100 条并保留 nextCursor；补充 protocol/router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` invalid cursor 错误语义。Go 现在在 app-server 参数层严格校验 cursor 为非负数字，错误返回 `-32600` 与 `invalid cursor: ...` 文案，避免 session 层宽松解析 `12abc`；补充 protocol/router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/metadata/update.gitInfo` 空 patch 错误 wire 语义。空/缺失 gitInfo 字段现在返回 JSON-RPC invalid request，响应 code 为 `-32600`，消息精确为 `gitInfo must include at least one field`，不再暴露内部 `invalid app-server request` 前缀；补充 protocol/router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/rollback` 对 codex-tui suppress deprecation notice 语义。RuntimeRouter 现在在 initialize 时按 connection 记录 clientInfo，connection closed 时清理；rollback 对 `clientInfo.name == "codex-tui"` 不发送 `deprecationNotice`，其他连接保留原通知；补充 RuntimeRouter 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start`/`thread/start` 忽略废弃 `multiAgentMode` 参数语义。Go 现在显式接收旧客户端的 `multiAgentMode` 字段，但 stable `turn/start` marshal 不输出它，runtime 不读取它、不会写入 thread settings，`thread/start` response 仍输出 `multiAgentMode:"explicitRequestOnly"`；补充 turn/protocol 解码与 RuntimeRouter start/turn 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust personality model instructions 基础语义。`model.ModelInfo` 现在提供 Rust `get_model_instructions` 等价 helper，模板存在时总是替换 `{{ personality }}`，`none` 会清空占位；model list 也会填充 `supportsPersonality`。app-server turn 在未显式覆盖 base instructions 时按当前模型 template 与 turn/settings/config personality 生成 instructions，JSON 请求显式传 `personality` 时额外注入 Rust 风格 `<personality_spec>` developer 更新块；同时补 `thread/start` 显式空 `baseInstructions` suppress 默认模型 instructions 的语义，避免空 override 又回退到 model template；补充 model/turn/RuntimeRouter 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start` originator 继承语义。RuntimeRouter 现在在 `thread/start` 后把请求 `serviceName` 或连接 `initialize.clientInfo.name` 写入 thread metadata，后续 `turn/start` 会继承为 `AgentRequest.Originator`，从而让 Responses runner 发送对应 originator header；补 RuntimeRouter 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/start` userMessage item/started 的 `clientId` 与 `text_elements` 语义。app-server 现在会在持久化用户输入后发送 userMessage `item/started`，payload 携带 `clientId`，并通过 session item data 保留原始 V2 user input content（含 `text_elements`）到 thread item wire shape；补 RuntimeRouter 回归断言覆盖 `byteRange`/`placeholder`；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `turn/steer` userMessage `item/started` 的 `clientId` 与 V2 input content 语义。steer 追加用户输入后现在会先发送 userMessage `item/started`、再保持原有 completed 通知，并和 `turn/start` 一样把 V2 content（含 `text_elements.byteRange`/`placeholder`）保存在 session item data 供 thread item wire shape 还原；补 RuntimeRouter 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` relation filter ThreadId 校验。`parentThreadId`/`ancestorThreadId` 现在会在进入 session 过滤前按 Rust UUID ThreadId 语义拒绝无效字符串，返回 JSON-RPC invalid request，并保留 hyphenated/32-hex/`urn:uuid:`/braced UUID 输入；补协议层回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` relation filter 省略 `sourceKinds` 的来源默认语义。relationship-filtered list 现在在 `sourceKinds` 省略/null 时包含所有来源，显式 `sourceKinds: []` 仍保持普通 interactive-only 默认；补协议层回归断言覆盖两条分支；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list.limit=0` clamp 语义。显式 `limit: 0` 现在返回最小页大小 1，而不是回落到默认页大小；补协议与 Router 回归断言覆盖 option 和实际返回数量；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume` 按 ID 拒绝 archived session 的错误文案。archived resume 现在返回 `session ... is archived. Run \`codex unarchive ...\` to unarchive it first.` 的 Rust 句式；补 Router 精确文案回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/search` 空 searchTerm 错误文案。空白 searchTerm 现在返回 JSON-RPC invalid request 文案 `thread/search requires a non-empty searchTerm`；补 Router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补齐普通 Router `thread/turns/list` 参数校验。普通 Router 现在与 RuntimeRouter 一样在读取 thread 前拒绝空 `threadId` 或负 `limit`，返回 JSON-RPC invalid request；补 Router 回归断言覆盖负 limit 优先于未物化 thread 错误；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/turns/list.limit=0` clamp 语义。显式 `limit: 0` 现在返回最小页大小 1，而不是回落到 turns 默认页大小；补协议层回归断言覆盖默认、0、超大 limit；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/rollback.numTurns=0` 错误文案。无效 numTurns 现在返回 `numTurns must be >= 1`；补 Router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `Thread.historyMode` wire shape。Thread JSON 现在必带 `historyMode`（默认 `legacy`），只继续隐藏内部 `extra`；补协议层回归断言，并移除旧测试里不存在的 `ThreadHistoryTurnBased` 期望；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.cwd` 可省略与 effective cwd 语义。Go 不再拒绝省略 cwd 的 `thread/start`；普通 start、RuntimeRouter start 与 ephemeral start 都会把 effective cwd 写入 response/thread/record，并用它计算 runtimeWorkspaceRoots 与默认 project instructions/config CWD；补 Router/RuntimeRouter 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.dynamicTools` 前置校验与 legacy normalization。`thread/start` 现在会在保存 raw dynamicTools 前按 Rust 规则区分 canonical/legacy，拒绝混用格式，legacy `namespace`/`exposeToContext` 会归一为 canonical namespace/function，并复用校验返回 JSON-RPC invalid request `-32600`；同时补齐 namespace 至少包含一个 tool 的规则；补 app-server/turn 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume.history=[]` 拒绝语义。`ThreadResumeParams` 现在记录 history 字段出现态，`history:null` 仍按 omitted 处理，显式空数组返回 JSON-RPC invalid request `history must not be empty`；Router history resume 分支按出现态进入；补协议层与 Router 回归断言；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/start.sessionStartSource` 与 session-start hook 语义。校验 `startup`/`clear` 枚举，把 startup/clear pending source 持久到新 thread，并在首个 `turn/start` 前一次性运行 SessionStart hook、把 additional context 注入本轮；补协议/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/start.experimentalRawEvents` raw response 通知开关。接收并持久化 start 参数，默认不发送 `rawResponseItem/completed`，显式 opt-in 时才发送；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/resume.path` 与 `thread/fork.path` 空字符串反序列化语义。Go 协议层把空 path 归一为 nil，保持空 path 等同省略；补协议回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `runtimeWorkspaceRoots` 绝对路径输入语义。`thread/start`、`thread/resume`、`thread/fork` 与 `turn/start` 拒绝相对 roots，不再静默按进程 cwd absolutize；补协议/turn 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：收紧 Rust `ThreadSourceKind` 闭合枚举语义。`thread/list` 与 `thread/search` 拒绝未知 `sourceKinds`，避免未知值被 Go 映射成空来源过滤并扩大查询；补协议回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/list.useStateDbOnly` 跳过 rollout repair/scan 语义。Go `thread/list` 在 `useStateDbOnly:true` 时只列 session store，不扫描 JSONL rollout；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/list.cwd` filter 路径归一语义。Go `BuildListOptions` 对 cwd filter 执行环境路径清洗/相对路径归一，避免 `.`、冗余分隔或相对 cwd 与已存绝对 cwd 不匹配；补协议回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust rollout-only `thread/unarchive` 触碰 restored rollout mtime 语义，确保反归档响应的 `updatedAt` 随恢复动作刷新且 `recencyAt` 仍由最后一条历史 item 决定；补 rollout/router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust archive/delete 使用 spawned descendant subtree 的 lifecycle 语义。Go ThreadRouter 增加可选 agent spawn graph，archive/delete 合并 graph descendants 与 session lineage，并让 RuntimeRouter 通知使用同一组 IDs；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/metadata/update` 全清 gitInfo 后 `Thread.gitInfo:null` 的 wire 语义。Go Thread builder 在 sha/branch/originUrl 全空时返回 nil gitInfo；补 protocol/router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 5/6：对齐 Rust `thread/metadata/update` 将 gitInfo 追加写回 rollout 的持久化语义。Go metadata update 在 rollout 文件存在时追加最新 session_meta，rollout replay 采用最后一条 session_meta 的 metadata 但保留首条 createdAt；补 rollout/router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume` 拒绝未物化 thread 的语义。空 `thread/start` 首条用户消息前按 ID resume 返回 invalid request，文案包含 `no rollout found for thread id`，包括 `excludeTurns` cheap path；补 Router/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume.path` 指向 archived rollout 时仍拒绝恢复的语义，沿 path 分支返回同 ID 分支 archived 错误并补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork` 从 archived source 读取时复用 resume 拒绝语义，按 ID/path 都返回先 unarchive 的 invalid request 并补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/set_name` 与 `thread/memory_mode/set` 不更新 archived thread 的语义，改为 includeArchived=false 并补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork.lastTurnId` 截断语义，缺失 lastTurnId 返回 Rust invalid request 文案，合成 turn snapshots 时 terminal prefix 全部为 completed，并补 Router/Session 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork.lastTurnId` 指向 in-progress turn 的拒绝语义，利用 rollout turn snapshot 状态返回 Rust invalid request 文案并补回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork.lastTurnId` 拒绝合成 legacy turn id 的语义，要求 lastTurnId 命中显式 turn metadata 并返回 canonical turn 错误文案；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 RuntimeRouter ephemeral fork 的 `lastTurnId` 错误映射，复用普通 Router 的 Rust invalid request wire shape；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/fork` 读取 paginated rollout history 时的 unsupported 语义，普通/ephemeral/active fork 都会在 fork record 前返回 `-32601 paginated_threads is not supported yet`，补充普通 ID/path 与 RuntimeRouter ephemeral 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 RuntimeRouter `thread/fork` 从 archived source 读取时的 Rust resume-read 拒绝语义，ephemeral ID/path 与 active runtime fork 都会返回先 unarchive 的 invalid request；补 RuntimeRouter ephemeral 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/rollback` 只作用于 loaded thread 的语义，RuntimeRouter notLoaded/cold store rollback 现在返回 `thread not found: <id>` 且不改持久化，同时保留非 TUI deprecation notice 时机；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/compact/start` 只作用于 loaded thread 的语义，RuntimeRouter notLoaded/cold store manual compact 现在返回 `thread not found: <id>` 且不改持久化、不发 compact 通知；补 RuntimeRouter 成功路径 loaded 标记与拒绝回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust live-thread-only RPC 入口，RuntimeRouter 对 elicitation increment/decrement、guardian approve、settings/update、shellCommand/backgroundTerminals 在 notLoaded 时返回 `thread not found: <id>`；补成功路径 loaded 标记与表驱动拒绝回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/inject_items` 只作用于 loaded thread 的语义，RuntimeRouter notLoaded 时会先返回 `thread not found: <id>` 且不改持久化；补入 live-only 表驱动回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/backgroundTerminals/list.limit=0` clamp 语义，显式 0 现在返回 1 条并给出 anchor nextCursor，而不是回落全量；补分页回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/resume` remote client redaction 名称匹配语义，clientName 现在精确匹配 ChatGPT remote 名称而不是 trim 后匹配；补协议层回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/rollback` deprecation notice 的 codex-tui clientName 精确匹配语义，带空白的名称不再 suppress notice；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补 `memory/reset` 的 Rust 文件侧语义，清空 `codex_home/memories` 内容并保留 threads/sessions；Go 暂无 sqlite memories 表，未伪造 DB 清理；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/settings/update` 错误优先级，notLoaded 线程现在先返回 `thread not found`，loaded 线程仍返回 permissions/sandboxPolicy 冲突；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/turns/list` enum 参数闭合语义，拒绝未知 `sortDirection` 与 `itemsView`，避免静默按 asc/summary 处理；同时让 `thread/resume.initialTurnsPage` 提前复用同一 enum/limit 校验，保持参数错误优先级；补协议回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.capabilities.optOutNotificationMethods` 基础语义，Go RuntimeRouter 现在按 initialize 注册通知 opt-out，并在当前 sink 模型下过滤 `thread/status/changed` 等已 opt-out 方法，连接关闭会清理；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/archive` spawned descendant 部分失败语义，descendant rollout archive 失败时整体请求仍成功但只通知/标记实际 archive 成功的线程，并回滚失败 descendant 的 store archived 标记以保留活跃状态；补 RuntimeRouter partial-failure 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/memoryMode/set` 将 memory mode 追加写回 rollout 的持久化语义，避免 store 重建/rollout-only repair 丢失最新 memory mode；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.capabilities.experimentalApi=false` 的实验 RPC 门禁，RuntimeRouter 在具体 handler 前拒绝实验方法与 `thread/start` 实验字段，返回 Rust `requires experimentalApi capability` 文案；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.clientInfo.name` header 校验与 initialize user-agent originator 语义。Go 现在会在提交 initialize 状态前拒绝非法 header value 的 `clientInfo.name`，返回 Rust 同形状 `-32600` 文案；initialize response 的 `userAgent` 改为 `<originator>/0.0.0 ...` 形状，支持真实客户端名、probe/backend 保持 `codex_cli_rs`、`CODEX_INTERNAL_ORIGINATOR_OVERRIDE` 优先，并补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust 同一 connection 重复 `initialize` 返回 `Already initialized` 的会话状态语义。RuntimeRouter 现在对每个 connection 只接受一次 initialize，重复请求返回 JSON-RPC invalid request `Already initialized`，不同 connection 仍可初始化，`ConnectionClosed` 后同 ID 可重新初始化；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust 显式连接未 `initialize` 的普通 RPC 返回 `Not initialized` / `-32600` 语义，并修正 websocket 外层错误码。RuntimeRouter 现在对显式 `ConnectionID` 的非 initialize 请求先执行 initialized 门禁，再执行 experimental 门禁；websocket pre-initialize 错误码从 `-32000` 改为 Rust invalid request `-32600`；补 RuntimeRouter/websocket 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.capabilities` 省略/null 时 `experimentalApi` 默认关闭的实验 RPC 门禁语义。RuntimeRouter 现在会把 nil capabilities 记录为 experimentalApi=false，而不是无记录放行；补 RuntimeRouter 回归源码覆盖省略 capabilities 与显式 null 两种初始化形状；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.capabilities.mcpServerOpenaiFormElicitation` 下游 MCP initialize extension 广告语义。RuntimeRouter 现在按连接记录 openai/form elicitation capability，并同步到 MCPService；MCPService 切换该能力时会清掉 stdio/HTTP MCP client session cache，后续 initialize 在 opt-in 时携带 `capabilities.extensions["openai/form"]={}`，省略/false 时不广告；补 MCP initialize params 与 RuntimeRouter 贯通回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `initialize.capabilities.requestAttestation` app-server attestation provider 与 Responses `x-oai-attestation` header 语义。RuntimeRouter 现在按连接记录 requestAttestation capability，并按 loaded thread subscriber 稳定选择可请求连接；ServerRequestBroker 增加兼容旧 sink 的可选 targeted sink，attestation provider 会向选中的 connection 请求 `attestation/generate` 并生成 Rust 同形 `{"v":1,"s":...}` envelope；turn runtime 将 provider 传入 model request，Responses runner 在 provider/auth 支持时写入 `x-oai-attestation`；补 broker/model/turn/appserver provider 与 RuntimeRouter 贯通回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `currentTime/read` app-server external clock provider 语义，要求线程订阅客户端唯一且向该连接定向请求，避免多客户端时静默广播/任选外部时钟；补 RuntimeRouter 回归源码覆盖定向请求、多订阅拒绝、等待订阅超时；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `[features.current_time_reminder]` 工具暴露语义，只有启用该 feature 时暴露 `clock.curr_time`，并且仅在 `sleep_tool=true` 时暴露 `clock.sleep`；external clock 工具调用复用 app-server current-time provider；补 tool/turn/appserver 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust 线程绑定 server request 解析通知语义，在 request_user_input/permissions/MCP elicitation/dynamic tool/审批类请求被响应或因上下文清理时发送 `serverRequest/resolved`；补 broker/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust current-time reminder 文本格式，`<current_time>` 内容改为 `It is YYYY-MM-DD HH:MM:SS UTC.`，避免 Go/Rust 提示词形状分叉；补 context/appserver 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust current-time reminder 配置默认值，`reminder_interval_seconds` 省略/布尔开启时默认为 1 秒而不是 0；补 config 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list.sourceKinds` 对 exec/appServer/unknown 与 subAgent 细分的 post-filter 语义，避免只传 subAgent 时退化成无过滤；补协议/Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust app-server `thread/list` 省略 `modelProviders` 时默认按当前配置 provider 过滤、显式空数组表示所有 provider、relation filter 省略 provider 默认的语义；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list.cwd` 过滤的路径规范化比较语义，让记录 CWD 与请求 filter 在 clean/abs 形状不同但指向同一路径时仍匹配；补 session store 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/search.searchTerm` trim 语义，搜索路径先裁剪前后空白再交给 store/snippet，避免 `" term "` 在 Go 侧无法命中；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐普通 Router `thread/loaded/list` 分页语义，按 ID 排序、cursor 使用插入点、显式 `limit:0` clamp 为 1，保持和 Rust/RuntimeRouter 一致；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：补普通 Router `thread/items/list` 的 rollout/store-backed 实现，对齐 Rust 默认 asc、limit clamp 1..100、turnId filter 与未物化/paginated rollout 错误边界；同时补 RuntimeRouter ephemeral 边界；补 Router/Protocol/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/items/list` 与 `thread/turns/list` 缺失 thread/history 的 invalid request 错误映射，items 返回 `no rollout found for thread id ...`，turns 返回 `thread not loaded: ...`，避免泄漏通用 `session.ErrThreadNotFound`；补 Router/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/loaded/list.cursor` ThreadId 校验语义，非法 cursor 返回 `invalid cursor: ...`，同时保留 ID 排序分页行为与 Go app-server 既有 `thread-*` cursor 兼容；补 Router/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list`/`thread/search` 显式空白 cursor 拒绝语义，cursor 不再 trim 后当作省略处理；补协议/Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust list/page 类 RPC 负 `limit` 的 JSON-RPC invalid request wire shape，统一返回 `-32600` 与 `limit must be non-negative` 文案；复用已有 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust request params 反序列化失败的 invalid request wire shape，`DecodeParams` 的 JSON 类型错误返回 `-32600 Invalid request: ...`，避免落成 internal error；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/rollback.numTurns=0` invalid request wire shape，返回 `-32600 numTurns must be >= 1` 而非 invalid params；复用已有 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/set_name` 空名字 invalid request wire shape，返回 `-32600 thread name must not be empty`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/inject_items` 允许空 items no-op 的语义，Go 不再拒绝空数组；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/shellCommand` command trim 语义，Go RuntimeRouter 传给 thread extras/history 与实际运行命令都使用 trim 后字符串；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/metadata/update.gitInfo` 字段空字符串 invalid request wire shape，返回 `-32600 gitInfo.<field> must not be empty`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` parent/ancestor relation filter 互斥错误 wire shape，返回 `-32600 parentThreadId and ancestorThreadId are mutually exclusive`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/search` 空 searchTerm invalid request wire shape，返回 `-32600 thread/search requires a non-empty searchTerm`；复用已有 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/items/list` 与 `thread/turns/list` invalid cursor 的 invalid request wire shape，返回 `-32600 invalid cursor...`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/list` 与 `thread/search` 未知 `sortKey`/`sortDirection` 的 invalid request wire shape，返回 `-32600 unsupported ...`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/items/list`、`thread/turns/list` 与 `thread/resume.initialTurnsPage` 未知 `sortDirection`/`itemsView` 的 invalid request wire shape，返回 `-32600 unsupported ...`；补 Router/协议回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/read` 缺失 thread/history 的 invalid request 错误映射，返回 `-32600 thread not loaded: ...` 而不是通用 not found；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/delete` 缺失根线程且无 spawn descendants 时的 invalid request 错误映射，返回 `-32600 thread not found: ...`，保留缺失根但有 descendants 的成功删除语义；补 Router/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/setName`、`thread/memoryMode/set` 与 `thread/metadata/update` 缺失线程写元数据的 invalid request 错误映射，返回 `-32600 thread not found: ...`，保留 rollout-only repair；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/memoryMode/set` 未知 `mode` 枚举参数的 invalid request wire shape，返回 `-32600 unsupported memory mode ...`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/start.historyMode` 未知枚举值的 invalid request wire shape，返回 `-32600 unsupported historyMode ...`，同时保留 `paginated` 的 method-not-found 语义；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/turns/list` 显式空白 cursor 的 invalid request 语义，省略 cursor 仍正常，`cursor:"  "` 返回 `-32600 invalid cursor:   `；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust `thread/approveGuardianDeniedAction` 缺失 event/actionId 的 invalid request wire shape，返回 `-32600 event is required`，保留 Go 的 `actionId` 兼容；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 6：对齐 Rust running `thread/resume` 的 invalid request 文案，history override 使用 `while it is already running`，stale path 使用 `cannot resume running thread ... requested ..., active ...`；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：收口 Rust `thread/list`/`thread/search` 对已加载线程的 runtime status overlay，失败 turn 后 list/search 与 read 同样返回 runtime `systemError`/active/idle 状态；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：收口 Rust `thread/fork` 不继承源线程 `name` 的 wire/metadata 语义，普通 fork、active-turn fork、ephemeral fork 与 `thread/started` 通知都输出 `name: null`；补 Router/RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补齐 Rust `thread/fork` 对显式 thread name 的 session-index 继承语义，`thread/setName` 标记显式名，fork 只继承显式名并继续清除普通/生成标题；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/fork` restored token usage replay 的通知顺序，fork response 后 `thread/tokenUsage/updated` 早于 `thread/started`；补 RuntimeRouter 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：对齐 Rust `thread/list` createdAt/updatedAt/recencyAt 分页 cursor 语义，`nextCursor/backwardsCursor` 使用毫秒 RFC3339 时间水位并保留旧数字 cursor 兼容；同步 `thread/search` backwards cursor；补 session/Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补齐 Rust `thread/metadata/update` archived rollout-only repair 边界覆盖，确保缺失 store/state row 的归档 rollout 仍可修复、写 gitInfo 并保留 archived/path/preview；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补齐 Rust `thread/resume` external rollout path 边界覆盖，确保 path 指向当前 codex home 之外的 rollout 时优先按 path 恢复、忽略占位 threadId 并返回原始 thread/path/preview；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：补齐 Rust rollout-backed `thread/read`/`thread/resume` 的 `threadSource` 恢复覆盖，确保 rollout `session_meta.thread_source` 在缺失 store 记录时仍回到 wire `threadSource`；补 Router 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续 HEART 阶段 1/6：已沿 Rust app-server/thread/session 路径收口 fork/resume/rollout/thread-list/metadata/rollback 剩余边界，本轮补齐 thread/list 时间 cursor、metadata archived rollout repair、resume external path、rollout-backed read/resume threadSource 覆盖，并复核 rollback/turns cursor/fork notifications/sourceKinds 无新增直接缺口；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP OAuth dynamic registration `client_secret` 持久化与 refresh 传递。`OAuthTokenSet`/fallback credentials 现在保存动态注册返回的 secret，authorization code exchange 与 refresh token 返回的 token set 会保留 secret，HTTP MCP 自动 refresh 会把 secret 作为 confidential client 凭据传给 token endpoint；补 OAuth store/token/http refresh/dynamic registration 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 installed remote marketplace plugin 的 discoverable connector 候选恢复。`DiscoverableInstallCandidates` 现在会对 installed+enabled 的远端占位 detail 复用 best-effort materialize，重启后 materialized 根目录缺失时仍能恢复插件提供的 app/template connector candidate；补 plugin 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 discoverable plugin filter ID 归一化。configured/disabled/loaded app connector ID 建集合时会 trim 并跳过空值，候选 connector ID 匹配也会 trim，避免配置值带空白时推荐插件或 connector 过滤失效；补 plugin helper 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 MCP status pagination cursor。`mcpServerStatus/list` 显式空白 cursor 现在按 invalid request 返回 `invalid cursor`，不再被当作 omitted cursor；补 MCP status pagination 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：补 MCP openai/form capability 切换清 resource cache。`SetOpenAIFormElicitationEnabled` 现在关闭旧 HTTP/stdio session 后也会清空 resource cache，避免 initialize capability 改变后沿用旧 session 下的 resource read 缓存；补 HTTP MCP cache 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：修正 `plugin/uninstall` pluginId trim 漏洞。Uninstall 入口统一裁剪 pluginId，并用规范 ID 做 installed map/config/materialized cleanup/内存状态更新，避免带空白 ID 时配置删不掉或状态分裂；补 plugin persistence 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：修正 `plugin/read` 参数 trim 漏洞。Read 匹配前会裁剪 pluginName/remotePluginId/marketplaceName/remoteMarketplaceName/marketplacePath，并在 response 回填规范 marketplacePath，避免 JSON-RPC 参数带空白时读不到插件；补 plugin read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：修正 `plugin/skill/read` 参数 trim 漏洞。ReadSkill 会规范化 remoteMarketplaceName/remotePluginId/skillName 后再匹配、读文件和生成 fallback Markdown，避免带空白参数时真实 skill 文件读不到；补 plugin skill read 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 plugin share principal/target wire 归一化。principal marshal 与 shareTargets 转换会裁剪 principalType/principalId/role/name，避免带空白字段泄漏到 Rust wire shape；补 plugin share wire 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：收紧 `PluginService.AddPlugin` 标识归一化。AddPlugin 会裁剪 summary ID/name/displayName/marketplaceName/remotePluginId 与 detail marketplaceName，避免带空白标识直接成为内存 map key；补 plugin AddPlugin 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过，按要求未运行 `go test`。
- [x] 继续阶段 9：修正 `marketplace/upgrade` marketplaceName trim 漏洞。UpgradeMarketplace 会裁剪可选 marketplaceName 指针后再筛选目标 marketplace，避免 `" debug "` 跳过 debug；补 marketplace upgrade 回归源码；`gofmt` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：修正 MCP required server inventory 状态不同步漏洞。`ListStatusChecked` 真实 inventory 后会把 ready/failed/error/auth 快照写回 `MCPService.servers`，required server 在 status/list 失败后会阻断 tool/resource 调用，恢复成功后解除阻断；补 HTTP MCP required gate 回归测试；窄范围 `go test -run TestHTTPMCPRequiredServerStatusFailureBlocksToolCall ./internal/mcp`、`gofmt` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 plugin hook source 暴露条件。`EnabledHookSources` 现在要求插件 installed+enabled，避免未安装插件仅靠 enabled 字段进入 app-server hooks/list 并被执行；补 plugin service 负例与 app-server hooks/list 已安装 fixture。为启用窄测试，同步修复 `AgentItem.Status` 缺字段、app-server 测试 helper/import/指针参数漂移、非测试代码重复 helper/函数比较/sort import/vet 错误；`go test -run "TestEnabledHookSources(UsesPluginRoot|SkipsUninstalledPlugins)$" ./internal/plugin`、`go test -run TestRuntimeRouterHooksListIncludesPluginHooks ./internal/appserver`、`gofmt` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：修正 explicit skill markdown link 漏匹配。`CollectExplicitSkillMentions` 的 linked mention parser 现在接受无 `$` 的 `[skill](skill://...)`，远端 environment skill link 能正确注入 `<skill>` input item；补 prompt parser 回归并跑通 `TestRuntimeRouterTurnStartUsesRemoteEnvironmentSkillRoot`；`gofmt`、窄范围 go test 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：修正 canonical plugin mention token 截断。plugin mention parser 现在允许 token 内含 `@`，`@plugin://sample@debug` 不再被截成 `plugin://sample`，显式 plugin-associated instructions 能命中 marketplace-qualified config name；补 `TestCollectExplicitPluginMentions` 回归；`gofmt`、窄范围 go test 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：修正 MCP failed status 泄漏旧 inventory。`ListStatusChecked` 在 runnable server inventory 失败时会清空 tools/resources/resourceTemplates，并通过 status 快照同步清掉内部旧 inventory，避免 failed `codex_apps` 仍通过 stale tools 暴露 accessible connector；扩展 HTTP MCP required gate 回归覆盖 success->failed 清 tools；`gofmt`、窄范围 go test 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：修正 connector install completed 验证命中旧 app cache。`pluginInstallRuntime` 在 connector accept 后用 `AppService.List(forceRefetch=true)` 重新拉 accessible apps，避免刚授权 connector 后仍返回旧缓存导致 `completed=false`；补 force-refetch 回归；`gofmt`、窄范围 go test 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 `request_plugin_install` codex-tui plugin 禁用门禁。handler 在 candidate 匹配后按实际 discoverable tool type 再拦截 plugin install，避免省略 `tool_type` 时用 plugin `tool_id` 绕过 TUI 限制；补 tool 回归；`gofmt`、窄范围 go test 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 URL-encoded explicit mention 归一化。`plugin://`/`app://` mention 会对 path id 做 URL path decode，`skill://` 本地路径会先 decode 再绝对化，避免 UI/Markdown 输出 `%40`、`%20` 后显式 plugin/app/skill 注入失效；补 plugin/prompt 回归；`go test -run "TestCollectExplicit(AppIDs|PluginMentions)$" ./internal/plugin`、`go test -run TestCollectExplicitSkillMentions ./internal/prompt`、MCP/apps 窄测试与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 apps list 显式空白 cursor。`AppService.List` 现在对 `cursor:"  "` 返回 `ErrInvalidAppRequest`，app-server 映射为 JSON-RPC invalid request，避免外围列表 RPC 和 thread/MCP/feature cursor 语义分叉；补 apps/app-server 回归，并顺手修复 `TestRuntimeRouterDispatchesExperienceAPIs` 旧测试漂移（使用真实 thread/start ID 与 blocking agent 验证 turn/steer/interrupt）；`go test -run TestListRejectsInvalidCursor ./internal/apps`、`go test -run TestRuntimeRouterDispatchesExperienceAPIs ./internal/appserver` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 plugin share principal 存储归一化。`SaveShare`/`UpdateShareTargets` 现在会把 legacy `type/id` 与带空白的 role/name 规范为 canonical `principalType/principalId/role/name` 后入内存，避免 Go response 结构和 Rust wire marshal 表示不一致；补 `TestPluginPayloadMarshalRustShape` 回归；`gofmt` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 MCP/tool exposure connector ID 归一化。legacy `BuildToolExposure` 与 runtime `BuildRuntimeExposure` 现在都会 trim enabled connector ID 和 tool connector ID，避免配置/远端 connector metadata 带空白时漏暴露已启用 connector 工具；输出保留 canonical connector ID；补 MCP exposure 回归；`go test -run "TestBuildToolExposure|TestBuildExposureDefersToolsWhenSearchEnabled" ./internal/mcp` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：收紧 MCP connector provenance 查询与 accessible connector ID。`ConnectorPluginProvenance.Names`、`ConnectorAccessibleFromMCPTools` 与 `ConnectorToolInfoFromRuntimeTools` 现在都会 trim connector ID，避免 runtime tool/provenance 带空白时 plugin source 注释丢失或生成带空白 ID 的 connector；补 runtime provenance 回归；`go test -run "TestAnnotateRuntimeToolsWithConnectorPluginProvenance|TestConnectorAccessible" ./internal/mcp` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 9：外围模块本轮窄验证收口。已重跑本轮实际改动覆盖的 mention/apps cursor/plugin share/MCP exposure/provenance 关键回归，补跑 `TestRuntimeRouterDispatchesExperienceAPIs`，并跑 `go test -run '^$' ./internal/mcp ./internal/plugin ./internal/apps ./internal/features ./internal/prompt ./internal/tool ./internal/appserver` 与 `go list -buildvcs=false ./...`，全部通过；按要求未运行全量 `go test ./...`。
- [x] 继续阶段 9：外围模块继续收口 plugin/skill/MCP/apps 剩余边界。`plugin/read` 的 `remotePluginId` 现在同时匹配 canonical `Summary.ID`，远端 environment skill locator 会对本地路径生成 URI-safe `%20` 形态并保持显式 mention 可命中，`NewMCPService` 会规范 runtime server name，`AppService` accessible provider cache 改为按 `threadId` 分桶避免 codex_apps connector 跨线程串味，同时修正 accessible connector name/installUrl merge 语义；补 plugin/prompt/appserver/MCP/apps 回归；`go test` 窄测、涉及包 `go test -run '^$' ./internal/mcp ./internal/plugin ./internal/apps ./internal/prompt ./internal/appserver` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 10：补 Linux/Windows sandbox 外围入口与 CLI 安全边界。普通 `cmd/codex` 启动会创建临时 PATH alias，直接以 `codex-linux-sandbox`/`codex-execve-wrapper` 调用时走明确 helper 分支；`codex sandbox` 现在复用统一 permission profile 解析，`:danger-full-access` 可直通并注入 `CODEX_PERMISSION_PROFILE`，read-only/workspace、sandbox state/network/socket/log-denial/config 驱动请求在真实平台后端接入前返回明确 unsupported，避免静默全权限执行；Windows `sandbox setup` 非 Windows 平台返回 unsupported。补 sandbox/app/cli/appserver 回归；`gofmt`、窄范围 `go test`、`go test -run '^$'` 编译闸门、`go list -buildvcs=false ./...` 与 Linux/Windows 交叉 `go list` 已通过；未运行全量 `go test ./...`。
- [x] 继续阶段 10：按 Rust `windows-sandbox-rs` 创建 Go 对应目录结构并接入 helper 入口。新增 `internal/sandbox/windowssandbox` 根包，镜像 `acl/allow/audit/cap/deny_read*/desktop/dpapi/elevated_impl/env/helper_materialization/hide_users/identity/logging/path_normalization/process/proc_thread_attr/resolved_permissions/setup/setup_error/spawn_prep/stdio_bridge/token/wfp/winutil/workspace_acl/wrapper` 模块，并补 `bin/setup_main`、`bin/command_runner`、`conpty`、`elevated`、`unified_exec/backends`、`wfp/filter_specs` 子包和 Rust test module 对应 Go 回归；`cmd/codex`/arg0 alias 现在识别 `codex-windows-sandbox-setup` 与 `codex-command-runner`，在真实 Windows API 后端移植前返回 explicit unsupported。`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、dispatch 窄测、`go list -buildvcs=false ./...` 与 Windows/Linux 目标 `go list` 已通过；全包 `go test ./internal/cli` 仍受既有 `TestRejectRootStrictConfigForUnsupportedSubcommand` 断言漂移影响，未作为本轮验收。
- [x] 继续阶段 10：移植 `windows-sandbox-rs` 首批纯逻辑。`wrapper` 已按 Rust flag 构造/解析 `--run-as-windows-sandbox` 请求并 round-trip `PermissionProfile`/env/roots/deny overrides/private desktop/proxy flags；`resolved_permissions` 复用 Go `sandbox.PermissionProfile` 生成 Windows 本地视图，判断 read-only vs writable-roots capability token、网络阻断和 TEMP/TMP 写根；`deny_read_resolver` 支持 exact missing path 保留、glob literal scan root、`**` 递归匹配和扫描深度；`winutil` 对齐 CRT quoting/argv command line；`setup_error` 补 Rust 错误码、`.sandbox/setup_error.json` 读写/清理和用户名路径脱敏；`path_normalization`/workspace root key 已收口。补 windowssandbox 回归；`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、dispatch 窄测、`go list -buildvcs=false ./...` 与 Windows/Linux 目标 `go list` 已通过；真实 restricted token/ACL/WFP/native spawn 仍未接。
- [x] 继续阶段 10：移植 `windows-sandbox-rs` setup/helper/logging 基础层。`setup` 已对齐 Rust `SETUP_VERSION=5`、`CodexSandboxOffline/Online`、`.sandbox`、`.sandbox-bin`、`.sandbox-secrets`、`setup_marker.json` 与 `sandbox_users.json` 路径，并补 setup marker 读写；`helper_materialization` 已支持 helper source resource lookup、materialized filename、dev/build suffix、copy-if-needed、freshness 检查和 cache；`logging` 已输出 `sandbox.YYYY-MM-DD.log` 并保留 UTF-8 安全 command preview；`sandbox_utils` 已支持 codex home 创建、git worktree root 查找和 `GIT_CONFIG_* safe.directory` 注入。补 windowssandbox 回归；`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、dispatch 窄测、`go list -buildvcs=false ./...` 与 Windows/Linux 目标 `go list` 已通过；真实 elevated setup/user provisioning/ACL/WFP 仍未接。
- [x] 继续阶段 10：移植 `windows-sandbox-rs` setup payload/identity/SSH 纯逻辑。`setup` 已补 `SandboxNetworkIdentity`、loopback proxy URL/端口解析、offline proxy settings、marker proxy mismatch reason、read roots/write roots gather、explicit read override 保留 helper/platform roots、write root user profile 展开、`.ssh/.aws/...` 与 SSH config dependency 过滤、CODEX_HOME/.sandbox/.sandbox-bin/.sandbox-secrets 写根保护，以及 deny-read/deny-write payload 构造；`ssh_config_dependencies` 已解析 `Include`、`IdentityFile`、`CertificateFile` 等 profile path directive，支持 quoted/escaped args、`~`/`%d`/`${HOME}` 和 include glob；`identity` 已补 setup marker + sandbox users 版本 readiness、users file 读写/删除。补 windowssandbox 回归；`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、dispatch 窄测、`go list -buildvcs=false ./...` 与 Windows/Linux 目标 `go list` 已通过；真实 DPAPI 解密、用户登录、elevated setup、ACL/WFP/native spawn 仍未接。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` DPAPI/SID/token 基础模块。`DPAPIProtect/DPAPIUnprotect` 在 Windows 上调用 `CryptProtectData/CryptUnprotectData` 并释放系统 buffer；`LocalSID` 现在保存 canonical SID 字符串和原始 SID bytes，`StringFromSIDBytes`/`SIDBytesFromString` 用 Windows SID API 互转；`GetCurrentTokenForRestriction` 打开当前进程 token，新增 `CloseTokenHandle` 释放句柄。补 Windows round-trip/token 回归和非 Windows explicit unsupported 回归；`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、Windows/Linux 目标 `go list` 与 Linux test package 交叉编译已通过；restricted token 创建、ACL/WFP/native spawn 仍未接。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` ACL/capability/deny-read/workspace ACL/desktop 基础模块，并按“Rust 用第三方则 Go 也用第三方”的要求新增 `github.com/goccy/go-json` 与 `github.com/google/uuid`。`cap` 已支持 Rust JSON/legacy 单 SID 迁移、workspace_by_cwd/writable_root_by_path 持久化和 root overlap helper；`acl` 已支持 Windows DACL deny-read/deny-write/allow-write、重复 ACE 检测、NUL device allow；`deny_read_acl/state` 已支持 plan、materialize missing path、apply deny-read ACE、state JSON reconcile 和 stale revoke；`workspace_acl` 已保护 `.codex`/`.agents`；`desktop` 已支持 default/private desktop、logon SID access grant 和 CloseDesktop。restricted token 创建已接 `CreateRestrictedToken` 完整 Rust flags/default DACL/privilege，当前 host 对 `WRITE_RESTRICTED` 报 87 时测试按 Rust unsupported host skip；`layout` 状态同步更新。`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、Windows/Linux 目标 `go list` 与 `go list -buildvcs=false ./...` 已通过；WFP/elevated setup/native process spawn 仍未接。
- [x] 继续阶段 10：继续消除 `windows-sandbox-rs` 明确 unsupported。`audit` 已实现 world-writable 候选收集、Everyone write DACL 检测和 capability deny-write 应用；`proc_thread_attr` 已封装 Windows `PROC_THREAD_ATTRIBUTE_LIST`、handle list 与 pseudoconsole 属性；`elevated/ipc_framed` 已对齐 Rust `version/type/payload` 扁平 JSON、8MiB frame 上限、小端长度前缀、spawn/stdin/output/exit/error/resize/terminate 消息和 base64 payload，并补 Rust-shaped permission profile wire encoder。`layout` 已同步标记 audit/proc_thread_attr/ipc_framed implemented；当前正在继续验证并推进 native process/runner 剩余模块。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` elevated runner 命名管道基础。`runner_pipe` 已支持 Rust 同名 pipe pair 生成、command-runner helper 定位、Windows `CreateNamedPipeW` + sandbox 用户 SID SDDL、`ConnectNamedPipe`/`GetNamedPipeClientProcessId` 校验和 CloseHandle；非 Windows 保持 explicit unsupported。补跨平台 pipe name/helper fallback、非 Windows unsupported、Windows 实 pipe 连接测试；`go test ./internal/sandbox/windowssandbox/elevated` 与 Windows/Linux 目标 `go list` 已通过；下一步继续接 `runner_client`/native process spawn。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` elevated runner client 基础。`runner_client` 已补 RunnerLogonError/RunnerStartupError、sandbox creds refreshable 判定、retry-once、transport send spawn_request/read spawn_ready/error、runner pipe client open、Windows `CreateProcessWithLogonW` 启动 command-runner、双命名管道连接超时清理、startup 失败 terminate runner 和 pipe ownership 转交；非 Windows 保持 explicit unsupported。补 refresh 判定、transport frame、非 Windows unsupported、Windows ConnectRunner pipe 测试；`go test ./internal/sandbox/windowssandbox/elevated` 与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` native process 基础。`process` 已补 Rust 对齐的 env block 排序/UTF-16 双 NUL、token-aware `CreateProcessAsUserWithToken`、显式/继承 stdio、private/default desktop、PROC_THREAD_ATTRIBUTE_HANDLE_LIST、anonymous pipe spawn、stdin/stderr mode、handle read loop、失败日志和 handle/desktop 清理；旧 `CreateProcessAsUser`/`SpawnProcessWithPipes` 在 Windows 用当前 token 包装，非 Windows 保持 explicit unsupported。补 env block、非 Windows unsupported、Windows stdio validation 测试；`go test ./internal/sandbox/windowssandbox` 与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 `windows-sandbox-rs` stdio bridge。新增 `SandboxSessionIO`/`SpawnedSandboxSession`，支持 stdin 分块转发、EOF close stdin、stdout/stderr channel 写出与 flush、interrupt 后 request terminate、exit code 等待和 5 秒输出 drain；替换原 explicit unsupported 占位。补 input/output forwarder、exit drain、interrupt terminate 回归；`go test ./internal/sandbox/windowssandbox` 已通过。
- [x] 继续阶段 10：实现 setup helper 的 read ACL mutex。`bin/setup_main/win/read_acl_mutex` 已支持 `Local\CodexSandboxReadAcl` 命名 mutex 存在性检查、CreateMutex initial-owner guard、已存在时跳过执行、ReleaseMutex/CloseHandle 清理；非 Windows 保持 explicit unsupported。补 nil guard、非 Windows unsupported、Windows acquire/exists 回归；setup_main/win 测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 command-runner cwd junction。`bin/command_runner/win/cwd_junction` 已支持 USERPROFILE 下 `.codex/.sandbox/cwd` 根目录、目标 cwd hash 命名、已有 reparse point 复用、异常占位移除、`cmd /c mklink /J` 创建 junction、日志记录和非 Windows explicit unsupported。补 helper、非 Windows unsupported、Windows 真实 junction/reuse 回归；command_runner/win 测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 Windows sandbox 环境变量处理。`env` 已支持 null-device env 归一化、非交互 pager 默认、PATH/PATHEXT 继承、no-network proxy/offline/Git/Pip/NPM/Cargo 环境注入、denybin stub 创建、PATH 前置和 PATHEXT 中 `.BAT/.CMD` 前置；按 Rust `dirs_next` 对应第三方要求新增 `github.com/mitchellh/go-homedir`。补 null device、pager、denybin/no-network 回归；`go test ./internal/sandbox/windowssandbox` 已通过。
- [x] 继续阶段 10：实现 hide users。`hide_users` 已支持 Windows Winlogon `SpecialAccounts\UserList` 隐藏新建 sandbox 用户、当前 USERPROFILE 目录 `HIDDEN|SYSTEM` 属性设置、日志记录和非 Windows explicit unsupported。补空用户 no-op、非 Windows unsupported、Windows HideDirectory 属性/缺失 profile 回归；根包测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 setup runtime bin ACL 刷新。新增通用 `PathMaskAllows` 与 `EnsureAllowMaskACEsWithInheritance`，`setup_runtime_bin` 已支持 LOCALAPPDATA/USERPROFILE fallback、扫描 `OpenAI/Codex/bin` 与 `runtimes`、检查 sandbox group 读/执行 mask、缺失时写入继承 allow ACE、refresh error/log 记录；非 Windows 通过 ACL stub 记录 unsupported。补 LocalAppData、缺失目录、非 Windows refresh error、Windows Everyone grant 回归；相关测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 identity 凭据读取。`identity` 已支持 setup marker/users 版本检查、offline/online sandbox user 选择、base64 DPAPI blob 解密、返回 `SandboxCredentials{Domain:"."}`，保留 setup incomplete 明确错误；非 Windows 在实际解密时返回 DPAPI explicit unsupported。补 setup complete、Windows DPAPI 凭据 round-trip、非 Windows unsupported 回归；根包测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 setup sandbox users 基础。`bin/setup_main/win/sandbox_users` 已支持 sandbox users group ensure、本地用户 create/update、group membership、well-known SID/name lookup、随机 24 位密码（按 Rust `rand` 对应新增 `github.com/sethvargo/go-password`）、DPAPI protect + base64 secrets 写入和非 Windows explicit unsupported；真实用户创建封装 netapi32 lazy proc，单元测试避免管理员副作用。补密码生成、Windows SID/secret round-trip、非 Windows unsupported 回归；setup_main/win 测试与 Windows/Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 Windows sandbox firewall/setup helper 主流程。`bin/setup_main/win/firewall` 已按 Rust 规则名/RemoteAddresses/LocalUserAuthorizedList/loopback proxy port complement 实现 Windows Firewall COM 配置，Go 侧按 Rust COM 依赖要求新增 `github.com/go-ole/go-ole`；`setup_main` 已支持 base64 JSON payload、`full/provision-only/read-acls-only` mode、setup marker prepare/commit、错误报告写入、sandbox 用户 provision/hide、offline firewall+WFP、deny-read 同步 ACL、后台 read ACL helper、write root allow ACE、deny-write ACE、runtime bin refresh 和 sandbox 持久目录 DACL 锁定。补 firewall 纯逻辑、payload 缺失、disabled wrapper 回归；`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：实现 setup orchestrator 与 wrapper 非 TTY 路径。`setup.go` 的 `RunElevatedSetup`/`RunElevatedProvisioningSetup`/`RunSetupRefresh` 已从 stub 改为生成 Rust 同形 payload，通过当前 `codex.exe --codex-run-as-windows-sandbox-setup` 启动 helper，必要时用 `ShellExecuteExW runas` 提权，并回读 `.sandbox/setup_error.json`；`CaptureRequest` 支持直接携带完整 `PermissionProfile`，wrapper 现在可解析后执行 disabled 直通、restricted-token capture 和 elevated adapter capture，并将 stdout/stderr/exit code 写回调用方。`layout` 状态同步更新。
- [x] 继续阶段 10：实现 ConPTY 与 elevated adapter 收口。确认 Go 侧第三方 `golang.org/x/sys/windows` 已提供 `CreatePseudoConsole`/`ResizePseudoConsole`/`ClosePseudoConsole` 和 `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` 后，补 `conpty` Windows 实现、legacy PTY capture、command-runner `tty=true` 分支和 resize frame；`unified_exec` 不再把 PTY 硬拒为 unconfirmed。同步拆除 `elevated` 子包对根包的反向依赖，把 helper materialization/argv quoting/credentials 本地化，让根包 `RunWindowsSandboxCaptureForPermissionProfileElevated` 真实走 runner，wrapper elevated level 不再回落 legacy；`sandbox setup --elevated` 入口现在会调用 provisioning setup；`layout` 已无 stub/scaffold 条目并新增测试守护。`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、`go test ./internal/app -run TestSandboxSetupCurrentUser` 与 Linux 目标 `go list` 已通过；`go test ./internal/app` 仍有既有 debug CLI 输出断言漂移，未作为本轮验收。
- [x] 继续阶段 10：补 elevated setup refresh 与 wrapper overrides 传递。`CaptureRequest` 现在承载 proxy/read-root/write-root/deny override，wrapper 解析后的 elevated capture 会把这些值传入 spawn prep；`RequireLogonSandboxCredsForPermissions` 已按 Rust 逻辑检查 setup marker/users、支持 preserve/reconcile proxy settings、必要时运行 full elevated setup，并且每次 elevated spawn 前通过 request 型 `RunSetupRefreshForRequest` 带当前 roots/deny/proxy 刷 ACL；runner logon retry 也会删除旧 users file 后按同一上下文刷新凭据。补 identity/wrapper/layout 回归；`go test ./internal/sandbox/windowssandbox/... ./cmd/codex` 与 Linux 目标 `go list` 已通过。
- [x] 继续阶段 10：收口 Linux sandbox 外围 stub。`codex-execve-wrapper` 已按 Rust `shell-escalation` 协议实现 DGRAM 握手、STREAM JSON frame、SCM_RIGHTS stdio FD 传递、Run/Escalate/Deny 分支和 Unix `execv`；Linux `codex-linux-sandbox` 已接 `pflag` CLI、Rust/Go permission profile 解析、pure-Go `github.com/elastic/go-seccomp-bpf` 网络 seccomp、`github.com/landlock-lsm/go-landlock` legacy filesystem Landlock、bubblewrap 默认外层、内层 seccomp re-entry、managed proxy UDS/TCP bridge，并让 `codex sandbox --permission-profile` 在 Linux 上真正包装 helper。补平台分叉测试并更新依赖；`go test ./internal/sandbox ./cmd/codex`、`go test ./internal/sandbox/windowssandbox/...`、Linux `go test -c ./internal/sandbox`、Windows/Linux `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 10：补完 permission profile runtime 与 Linux sandbox 目录归拢。`run` 现在会在进入 sandbox 前解析 config/custom/legacy permission profile，并把 Rust 同形 runtime JSON 传给平台 helper；`permissions` 已支持 custom profile、`extends`、workspace root placeholder、`glob_scan_max_depth`、deny-read glob materialization 和 legacy `sandbox_mode` 回退。Linux sandbox 主实现和 `codex-execve-wrapper` 已移入 `internal/sandbox/linuxsandbox`，父包仅保留薄 wrapper/stub；deny-read glob 按 Rust `globset` 对应要求在 Go 侧引入 `github.com/bmatcuk/doublestar/v4`，并在 bubblewrap 中用 `--tmpfs`/`--ro-bind-data` 屏蔽目录、文件和缺失路径。补 config/linuxsandbox 回归；`gofmt`、`go test ./internal/config -run TestResolveSandboxPermissionProfile`、`go test ./internal/sandbox ./internal/sandbox/linuxsandbox`、`go test ./internal/app -run TestSandbox`、Linux `go test -c` 与 Windows/Linux `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 10：Windows sandbox 真机 smoke。已新增 gated smoke 测试 `CODEX_WINDOWS_SANDBOX_SMOKE=restricted|elevated`，补 `cmd/codex --run-as-windows-sandbox` wrapper dispatch、wrapper main stderr 输出和 smoke 命令/超时诊断；restricted token 现在保持 Rust 的 `WRITE_RESTRICTED` flags，不再降级到普通 restricted token，当前宿主对该 flag 返回 `ERROR_INVALID_PARAMETER(87)` 时明确归类为 host unsupported 并 skip。elevated 路径修正 stale users secret 触发 setup 重建、materialized `codex-command-runner-<suffix>.exe` 显式带 `--codex-run-as-windows-command-runner` dispatch flag，真机 elevated smoke 已用 `.tmp/elevated-codex-home` 完整通过 setup refresh、runner、workspace 写入和 stdout capture。`gofmt`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、`go test ./internal/cli -run TestDispatchKind`、restricted smoke skip、elevated smoke pass、`go list -buildvcs=false ./...` 已通过；`go test ./internal/cli` 全量仍有既有 `--strict-config mcp` 报错顺序断言漂移，未作为本轮改动范围。
- [x] 继续阶段 10：接入 Windows sandbox 主运行路径。`BuildCommandRunPlan` 现在把 Windows 视为真实 permission profile sandbox 后端平台，Linux 仍在 plan 层包装 `codex-linux-sandbox`，Windows 则由 `internal/app` 在执行 `codex sandbox -P :workspace -- ...` 时把 plan 包装成 elevated `--run-as-windows-sandbox` 请求，传入绝对 command cwd/workspace root/CODEX_HOME、环境变量和 `CODEX_PERMISSION_PROFILE`，并直接走 wrapper exit-code 路径。补 Windows 分支 app 回归和 run plan 回归，修复 CLI `--strict-config mcp` 错误优先级漂移，以及 app debug JSON 断言漂移。`gofmt`、`go test ./internal/sandbox ./internal/app ./internal/cli`、`go test ./internal/sandbox/windowssandbox/... ./cmd/codex`、`go list -buildvcs=false ./...` 已通过；全量 `go test ./...` 仍按计划留到模块收口后统一跑。
- [x] 继续阶段 4/10：接入 shell/tool sandbox 执行链。`ShellRequest` 现在携带有效 `PermissionProfile`，`require_escalated` 获批后转 full access，`with_additional_permissions` 获批后合并额外网络和写根；`LocalShellRunner` 在 profile 非空时统一调用 `BuildCommandRunPlan`，Linux 走 `codex-linux-sandbox` helper，Windows 走 elevated capture，并注入 `CODEX_PERMISSION_PROFILE`，避免 sandboxed tool 命令裸跑。`codex exec` 和 app-server turn 工具注册已从 CLI/config/turn params 解析 permission profile 传给 shell executor；同时修复 Windows sandbox smoke test 的 auth/tool 导入环和 tool_search 断言漂移。`gofmt`、`go test ./internal/exec ./internal/tool ./internal/sandbox ./internal/app ./internal/cli ./internal/sandbox/windowssandbox/... ./cmd/codex`、`go test ./internal/appserver -run TestTurnSandboxPermissionProfilePreservesModePolicyFields`、`go list -buildvcs=false ./...` 已通过；`go test ./internal/appserver` 全包仍有多项既有 Windows/fixture 漂移失败，未作为本轮验收。
- [x] 继续阶段 6/10：接入 app-server `command/exec` buffered sandbox。`command/exec` 的 `permissionProfile`/`sandboxPolicy` 现在不再对 read-only/workspace 直接报平台 unsupported；非 TTY、非 streaming 请求会复用 shell sandbox runner 执行并保持 output cap，streaming/TTY sandbox 仍明确拒绝，避免交互路径裸跑。补 fake sandbox runner 回归和 streaming/TTY 拒绝回归；`go test ./internal/appserver -run "TestCommandExecSandbox"`、稳定相关包 `go test ./internal/exec ./internal/tool ./internal/sandbox ./internal/app ./internal/cli ./internal/sandbox/windowssandbox/... ./cmd/codex`、`go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 6/P0 schema fixture diff：新增 `TestBuildProtocolSchemaMatchesRustStableFixtures`，读取 Rust JSON stable `ClientRequest`/`ServerRequest`/`ServerNotification` fixtures 自动比对 Go schema 的 method+params surface，并校验 params/result 类型名存在于 Rust JSON fixture 树；按 Rust `export.rs` JSON 口径从 Go stable schema surface 移除 v1 legacy `getAuthStatus`/`getConversationSummary`/`gitDiffToRemote` 与 JSON 排除的 `rawResponseItem/completed`，router/运行时兼容入口保留。`gofmt`、`go test ./internal/appserver -run "TestBuildProtocolSchema(MatchesRustStableFixtures|IndexesRPCSurface)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...` 已通过；字段级 required/nullable/error code 与 TypeScript fixture diff 继续保留。
- [x] 继续阶段 6/P0 schema fixture diff：将首批 payload 护栏升级为 `TestProtocolPayloadsValidateAgainstRustSchemas`，直接读取 Rust JSON schema 校验 Go `InitializeResponse`、`ThreadStartResponse`、`ThreadStarted/ThreadNameUpdated/TurnStarted/TurnCompleted` notifications、`CommandExecResponse` 与 `CommandExecOutputDeltaNotification` 的真实 marshal 输出；测试内 schema validator 支持 required、nullable、array/object、enum、oneOf/anyOf/allOf、`$ref`/definitions 与 `additionalProperties:false`；`go test ./internal/appserver -run "Test(BuildProtocolSchema|ProtocolPayloadsValidateAgainstRustSchemas)" -count=1 -v` 已通过。
- [x] 继续阶段 6：修复 app-server 全包 Windows 断连清理抖动。`ProcessService.ConnectionClosed` 取消非 PTY process 时同时关闭 stdin pipe，避免 `cmd /c more` 子进程等待 stdin 导致 TempDir cleanup 失败；`go test ./internal/appserver -run TestProcessServiceConnectionClosedCancelsOnlyThatConnection -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 6/P0 TypeScript fixture diff：新增 `BuildTypeScriptProtocolSchema` 并让 `GenerateTypeScript` 走 Rust TS compatibility surface；JSON stable surface 继续排除 v1 legacy `getAuthStatus`/`getConversationSummary`/`gitDiffToRemote` 与 JSON 排除的 `rawResponseItem/completed`，TS surface 保留它们。新增 `TestBuildTypeScriptProtocolSchemaMatchesRustFixtures`，解析本地 Rust `ClientRequest.ts`、`ServerRequest.ts`、`ServerNotification.ts` union variants 比对 Go method+params；`go test ./internal/appserver -run "Test(GenerateTypeScriptWritesProtocolSchema|BuildProtocolSchema|BuildTypeScriptProtocolSchema|ProtocolPayloadsValidateAgainstRustSchemas)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 6/P0 错误码 diff：参考 Rust `app-server/src/error_code.rs` 新增标准 JSON-RPC 错误码常量 `-32600/-32601/-32602/-32603`，Router 与 RuntimeRouter 统一改用常量映射，并补 Router/RuntimeRouter mapper 与 decode params `Invalid request:` -> `-32600` 回归；同时修复 `TestRuntimeRouterDispatchesThreadExtrasAndRealtime` 对短命 user shell background terminal 数量的竞态断言，改为验证预置 terminal 可见性。`go test ./internal/appserver -run "TestRuntimeRouterDispatchesThreadExtrasAndRealtime|Test(JSONRPCErrorCode|RouterErrorCodeMapping|RuntimeRouterErrorCodeMapping|RuntimeRouterDecodeParamsError)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...` 已通过。
- [x] 本轮收尾全量 `go test ./... -count=1` 已通过，使用仓库内 `.gocache/.gotmp`。
- [x] 继续阶段 6/P0 payload schema diff：扩展 `TestProtocolPayloadsValidateAgainstRustSchemas`，新增 FuzzyFileSearch、ThreadGoal、ThreadSettings、ThreadTokenUsage、Warning/Deprecation、ModelSafetyBuffering、Realtime、ServerRequestResolved、MCP status、TerminalInteraction、WindowsSandboxReadiness 等 Rust JSON schema 样本，继续收紧 required/null/empty array/enum/additionalProperties；同时让 `TestRuntimeRouterTurnStartInjectsExternalCurrentTimeReminder` 与 `TestRuntimeRouterTurnStartPassesResponsesAPIClientMetadata` 等待 `turn/completed`，避免 Windows TempDir cleanup 撞上后台 session 写入。`go test ./internal/appserver -run "Test(ProtocolPayloadsValidateAgainstRustSchemas|RuntimeRouterTurnStartPassesResponsesAPIClientMetadata|RuntimeRouterTurnStartInjectsExternalCurrentTimeReminder)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 6：修复 app-server PTY 回归抖动。`waitForCommandExecOutputDeltaContaining` 不再抢读 final response channel，而是等待 output delta，避免全量压力下 final response 与 notification 可见性竞态误报；`go test ./internal/appserver -run "Test(CommandExecTTYStreamsAndResizes|ProcessServiceSpawnTTYStreamsAndResizes)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go list -buildvcs=false ./...`、全量 `go test ./... -count=1` 已通过。
- [x] 继续阶段 8：补 `stdio-to-uds` Windows named pipe 真实桥接。`runStdioToUDS` 不再对 `\\.\pipe\...` 做临时回显，Windows 平台改为 `CreateFileW` 打开 named pipe，并用 overlapped `ReadFile`/`WriteFile` 双向转发 stdin/stdout，避免 Go `os.File` 同 handle 读写锁造成死锁；新增 Windows 真 named pipe 回归测试，删除旧回显测试。`gofmt`、`go test -run TestStdioToUDS -timeout 20s -v ./internal/app`、`go test -run "Test(StdioToUDS|ExecServerConfigOutput|CompletionOutputsScript|ParseCloudAndTransportCommands)" ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8：补 Rust exec CLI parser 的 `--full-auto` 互斥语义。Go parser 现在在 `codex exec`/`exec resume` 上拒绝 `--full-auto` 与 `--dangerously-bypass-approvals-and-sandbox`/`--yolo` 同时出现，覆盖 root shared flag 与 exec shared flag 两种顺序，保留单独 `--full-auto` 的迁移 warning 路径；补 CLI parser 回归。`gofmt`、`go test -run "TestParseExec(RejectsFullAutoWithDangerousBypass|Resume|OutputFlags)|TestRejectRootStrictConfigForUnsupportedSubcommand" ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8：对齐 `codex sandbox setup` CLI 校验顺序。`runSandbox` 现在先执行 `SetupCommand.Validate`，再做 Windows 平台检查，确保缺 `--elevated`、缺 `--user/--current-user` 等 Rust CLI 语义不会被平台 unsupported 错误抢先覆盖；补跨平台 app 回归。`gofmt`、`go test -run "TestSandboxSetup(CurrentUser|ValidatesBeforePlatformCheck)" ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8：收紧 app-server listen 参数。CLI parser 现在接受 `--listen off`、拒绝 `--stdio` 与显式 `--listen` 同时出现，并对非法 listen scheme（如 `http://...`）返回 invalid listen address；`runAppServer` 把 `off` 当作合法无本地 transport，返回 Rust 同款 `no transport configured; use --listen or enable remote control`，不再报 unsupported。补 parser/app 回归。`gofmt`、`go test -run "TestParseAppServer(ListenValidation|WebSocketAuthFlags|ToolingSubcommands)" ./internal/cli`、`go test -run TestAppServerListenOffReturnsNoTransport ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8：对齐 `exec-server --remote` 缺环境参数错误文案。CLI 现在在设置 `--remote` 但缺少 `--environment-id` 时返回 Rust 同款 `--environment-id is required when --remote is set`；补 parser 回归。`gofmt`、`go test -run "TestParseExecServerAndStdioToUDS|TestParseAppServerListenValidation" ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `interactive root`。按 Rust `run_interactive_tui`/`resolve_remote_endpoint` 补 root prompt CRLF/CR 归一化、`--remote` 地址解析、`--remote-auth-token-env` 运行期校验、env token trim/missing/empty 错误和 `wss://`/loopback `ws://` auth 限制；Go 交互入口现在不再静默忽略 remote endpoint，远端 TUI 主循环作为 `interactive root` 子项留在阶段 8。`gofmt`、`go test -run "Test(ParseRemoteAuthEnvForInteractiveRoot|RejectRemoteForExec)|TestInteractive(RemoteAuthRequiresRemoteAtRuntime|PromptNormalizesCRLF|PromptUsesExecRunner|WithoutPromptRunsLineSession)|TestResolveInteractiveRemoteEndpoint|TestReadRemoteAuthTokenFromEnvVar" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `mcp/plugin/mcp-server` 第一段。按 Rust `mcp_cmd.rs` 的 transport group/requires 语义，Go parser 现在拒绝 `mcp add` stdio command 搭配 HTTP-only `--bearer-token-env-var`/`--oauth-client-id`/`--oauth-resource`，也拒绝 `--url` 搭配 stdio-only `--env`，避免静默丢弃不适用参数；补 parser 回归。`gofmt`、`go test -run "TestParseMCPAdd(Stdio|HTTP|TransportFlagValidation)" ./internal/cli`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `plugin` selector 校验。Go `plugin add/remove` 现在按 Rust `PluginId::parse/new` 校验 plugin name 和 marketplace name segment，只允许 ASCII 字母数字、`_`、`-`，并区分 `<plugin>@<marketplace>` 与 `PLUGIN --marketplace MARKETPLACE` 两条错误路径；补 app 回归。`gofmt`、`go test -run "Test(ParseMCPAdd|ParsePluginCommands)|Test(MCPAddListGetRemove|MCPHTTPLoginLogout|PluginMarketplaceLifecycle|PluginAddListRemove|PluginSelectionValidatesSegments)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `app-server/remote-control` 第一段。按 Rust `app-server-daemon::ensure_remote_control_started` 对齐 `remote-control start` 的 daemon startup 分支，Go 现在首次未 bootstrap 时返回 daemon `bootstrapped` 输出，已有 settings 时先持久化 remote-control enabled 再走 lifecycle start 输出；补 lifecycle/app 回归。`gofmt`、`go test -run "Test(LifecycleRunnerEnsureRemoteControlStarted|LifecycleRunnerBootstrapAndRemoteControlSettings|RemoteControlStartJSON|AppServerDaemonBootstrapAndVersion|ParseAppServerToolingSubcommands|ParseRemoteControl)" ./internal/appserverdaemon ./internal/app ./internal/cli`、`go test ./internal/appserverdaemon ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `app-server --remote-control` startup mode。按 Rust `app-server/src/main.rs` 的 `(remote_control, disabled_env)` 映射，Go 现在把 CLI flag 传入 app-server runtime router options，stdio/unix router 创建时进入 enabled ephemeral remote-control 状态，且 `CODEX_INTERNAL_APP_SERVER_REMOTE_CONTROL_DISABLED` 会被读取并清除、但被显式 flag 覆盖；补 stdio JSON-RPC status 回归。`gofmt`、`go test -run "Test(AppServerRemoteControlFlagEnablesStartupMode|AppServerRemoteControlFlagOverridesDisabledEnv|AppServerDaemonBootstrapAndVersion|RemoteControlStartJSON|LifecycleRunnerEnsureRemoteControlStarted|ParseAppServerToolingSubcommands)" ./internal/app ./internal/appserverdaemon ./internal/cli`、`go test ./internal/appserver ./internal/app ./internal/appserverdaemon ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 daemon remote-control lifecycle。按 Rust `set_remote_control_locked` 的 backend 语义，Go `SetRemoteControl` 现在未运行 managed daemon 时返回 `backend:null`，运行中 PID backend 切换 remote-control setting 时会持久化设置并刷新 running PID 状态，不再只写 settings 后固定输出 pid backend；补 lifecycle 回归。`gofmt`、`go test -run "Test(LifecycleRunnerBootstrapAndRemoteControlSettings|LifecycleRunnerSetRemoteControlRestartsRunningDaemon|LifecycleRunnerEnsureRemoteControlStarted|RemoteControlStartJSON|AppServerDaemonBootstrapAndVersion)" ./internal/appserverdaemon ./internal/app`、`go test ./internal/appserver ./internal/app ./internal/appserverdaemon ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 PID remote-control env marker。按 Rust `app-server-transport`/`app-server-daemon` 的 `REMOTE_CONTROL_DISABLED_ENV_VAR`，Go PID backend 现在禁用 remote-control 时注入 `CODEX_INTERNAL_APP_SERVER_REMOTE_CONTROL_DISABLED=1`，不再使用旧的 `CODEX_REMOTE_CONTROL_DISABLED`；补 PID backend args/env 回归。`gofmt`、`go test -run "Test(PIDBackendCommandArgsAndEnv|PIDUpdateLoopCommandArgsAndEnv|LifecycleRunnerSetRemoteControlRestartsRunningDaemon|AppServerRemoteControlFlagOverridesDisabledEnv)" ./internal/appserverdaemon ./internal/app`、`go test ./internal/appserver ./internal/app ./internal/appserverdaemon ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `completion/update/doctor/sandbox` 的 `update` 命令树。对照 Rust `Subcommand::Update` 无参数形态，Go parser 现在拒绝额外 `--json`，删除 Go 侧离线 JSON status 扩展入口，避免和 Rust CLI surface 分叉；补 parser/app 回归。`gofmt`、`go test -run "Test(ParseUpdate|GenerateCompletionBash|GenerateCompletionRejectsUnknownShell|CompletionOutputsScript|UpdateCommandRejectsJSONFlag)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` parser requires。按 Rust `HostSandboxArgs` 的 clap `requires` 语义，Go parser 现在拒绝缺 `--permission-profile` 的 `-C/--cd` 与 `--include-managed-config`，也拒绝缺 `--sandbox-state-json` 的 `--sandbox-state-readable-root`/`--sandbox-state-disable-network`；补 legacy `--permissions-profile` alias 回归。`gofmt`、`go test -run "TestParseSandbox|TestSandbox" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox setup` parser 依赖。按 Rust `sandbox_setup.rs` 的 ArgGroup/requires 语义，Go CLI parser 现在在 parse 层要求 `--user`/`--current-user` 二选一，拒绝二者同时出现，并要求 `--user` 搭配 `--codex-home`；`--elevated` 仍保持 Rust 运行层校验。`gofmt`、`go test -run "TestParseSandboxSetup|TestSandboxSetup" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `doctor` human output 分组。按 Rust `doctor/output.rs` 的 GROUPS，Go doctor human renderer 现在使用 Environment、Configuration、Updates、Connectivity、Background Server 五段，mcp/sandbox 归 Configuration，updates 独立，app-server 归 Background Server，Environment 中 git 先于 terminal；补渲染顺序回归。`gofmt`、`go test -run "TestRenderHuman(UsesRustDoctorGroups|SummaryASCII)|TestRunJSONUsesStructuredReport" ./internal/doctor`、`go test ./internal/doctor ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` path 参数解析。按 Rust `cli/src/lib.rs` 的 `parse_absolute_path`/`AbsolutePathBuf::relative_to_current_dir`，Go parser 现在会把 `--sandbox-state-readable-root` 与 `--allow-unix-socket` 的相对路径解析为当前目录下的绝对路径，避免后续 sandbox state/socket allowlist 收到相对路径；补 parser 回归。`gofmt`、`go test -run "TestParseSandbox|TestSandbox" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `completion` Rust 可见 surface。对照 Rust `MultitoolCli::command()`/`clap_complete::generate`，Go completion 从顶层静态命令列表改为命令树生成，覆盖可见子命令、alias、嵌套 subcommand 与 flags，补入 `cloud`、`exec-server`、`e`/`a`、mcp/plugin/app-server/sandbox 等嵌套 surface，并排除 Rust hidden 的 `execpolicy`、`responses-api-proxy`、`stdio-to-uds`、内部 schema/pid tooling；补五种 shell 生成回归。`gofmt`、`go test -run "TestGenerateCompletion|TestCompletionSpec" ./internal/cli`、`go test -run "Test(GenerateCompletion|CompletionOutputsScript|ParseCompletion)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `doctor` JSON/human redaction wire shape 第一段。按 Rust `JsonDoctorReport`/`redacted_json_check`，Go `doctor --json` 现在保留 `remediation:null`、issue `measured/expected/remedy:null` 与 `fields:[]`，并对 details、issues、remediation 统一执行 secret/url redaction；human renderer 也复用同一 redaction，避免明细行泄露 token、auth header 或 URL credential/path secret。`gofmt`、`go test -run "Test(JSONReportShapeStructuresAndSanitizesDetails|RenderHumanRedactsDoctorDetails|RunJSONUsesStructuredReport|RenderHumanUsesRustDoctorGroups|RenderHumanSummaryASCII)" ./internal/doctor`、`go test ./internal/doctor`、`go test ./internal/doctor ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `doctor` check id/order 第一段。按 Rust `build_report` 和 `installation_check`，Go report 现在把安装检查 id 从 Go 侧旧扩展 `install.identity` 改为 Rust 稳定 key `installation`，并将 report 构建前缀调整为 `system.environment`、`installation`、`runtime.provenance`、`runtime.search`；补 ID 和顺序回归。`gofmt`、`go test -run "TestReportBuildsLocalChecks|TestRunJSONUsesStructuredReport" ./internal/doctor`、`go test ./internal/doctor`、`go test ./internal/doctor ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` parser conflicts。按 Rust `SandboxStateArgs` 的 `conflicts_with_all`，Go parser 现在拒绝 `--sandbox-state-json` 与 `--permission-profile`/`--cd`/`--include-managed-config` 组合，并让 conflict 报错优先于 `-C/--include-managed-config` 的 profile requires；补 parser 回归。`gofmt`、`go test -run "TestParseSandbox|TestSandbox" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` sandbox-state runtime readable-root 合并。按 Rust `FileSystemSandboxPolicy::with_additional_readable_roots`，Go 现在追加 readable root 前会用 cwd 解析 `:root`、`:project_roots`/`current_working_directory`、`:slash_tmp`、`:tmpdir` 等特殊 entry，并按路径 specificity 与 `deny > write > read` 冲突优先级判断已有有效读权限，避免已被 `project_roots` 覆盖的 cwd 被重复追加 concrete path；补 runtime 回归。`gofmt`、`go test -run "TestBuildCommandRunPlan" ./internal/sandbox`、`go test ./internal/cli ./internal/app ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` runtime helper override。按 Rust `run_command_under_sandbox` 对 `SandboxState.codex_linux_sandbox_exe` 的覆盖逻辑，Go run plan 现在保存该字段并在 Linux sandbox wrapper 构造时替换 argv[0]；底层 `linuxsandbox.CreateCommandArgs` 保持默认 `codex-linux-sandbox`，新增 override API 供 run plan 使用。`gofmt`、`go test -run "Test(CreateCommandArgs|BuildCommandRunPlan)" ./internal/sandbox ./internal/sandbox/linuxsandbox`、`go test ./internal/sandbox ./internal/sandbox/linuxsandbox ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` root config/profile 合并。按 Rust `Subcommand::Sandbox` 的 `prepend_config_flags` 与 root profile fallback，Go `Run` 现在执行 sandbox 前会把 root `-c/--config` prepend 到 sandbox overrides 前，并在 sandbox 未显式 `--profile` 时继承 root `--profile`；补 root override/profile 端到端回归，确认 resolved permission profile 会注入子命令环境。`gofmt`、`go test -run "TestSandbox(HonorsRootConfigOverrides|HonorsRootProfile|FullAccessProfileRunsAndInjectsEnv)" ./internal/app` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` legacy config 默认 profile。按 Rust `load_debug_sandbox_config_with_codex_home`，Go `codex sandbox` 现在每次都会加载 debug sandbox config，并在 effective config 没有 `default_permissions` 且本次 CLI 没有显式 `sandbox_mode` override 时注入 legacy `read-only`，避免无 profile 时裸跑，也避免继承用户 ambient `sandbox_mode`；显式 `-c sandbox_mode=...` 仍按兼容路径生效。补默认 read-only 与 CLI legacy override 回归，并把执行测试改为显式 full-access 以避开默认平台 sandbox 依赖。`gofmt`、`go test -run "TestSandbox" ./internal/app`、`go test ./internal/cli ./internal/app ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` child env policy。按 Rust `exec_env::create_env`，`internal/exec` 的 env policy 现在支持 `inherit=all/core/none`、Rust default excludes、wildcard exclude/include、set 后再 include_only 和 Windows `PATHEXT` 兜底；`codex sandbox` 加载 config 时会解析 `shell_environment_policy`，直通执行和 Windows sandbox wrapper 都使用 policy env，并在最后注入 runtime `CODEX_PERMISSION_PROFILE` 覆盖配置值。补 env policy 单元测试与 sandbox 端到端 env 回归。`gofmt`、`go test -run "Test(CreateEnv|InjectPermissionProfile|VariablePattern|Sandbox)" ./internal/exec ./internal/app`、`go test ./internal/exec ./internal/cli ./internal/app ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` seatbelt-only 参数 surface。对照 Rust `SeatbeltCommand`/`LandlockCommand`/`WindowsCommand`，Go parser 和 completion 现在只在 darwin 暴露并接受 `--allow-unix-socket`、`--log-denials`；Linux/Windows 保持 Rust 同形命令树，遇到这两个参数返回 unknown sandbox option。补平台条件 parser 回归。`gofmt`、`go test -run "Test(ParseSandbox|GenerateCompletion|CompletionSpec)" ./internal/cli`、`go test ./internal/exec ./internal/cli ./internal/app ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` root feature toggles。按 Rust `feature_toggles.to_overrides()` 后追加到 root config overrides 的顺序，Go sandbox 入口现在把 root `--enable/--disable` 折成 `features.<name>=true/false` 并传入 sandbox config，且 sandbox 子命令自己的 `-c` 仍可覆盖 root toggle。补 run config 回归。`gofmt`、`go test -run "TestSandbox(InheritsRootFeatureToggles|ConfigOverrideWinsOverRootFeatureToggle|RunConfig)" ./internal/app`、`go test ./internal/exec ./internal/cli ./internal/app ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` runtime cwd 校验。按 Rust `SandboxState.sandbox_cwd.to_abs_path()`，Go 现在要求 sandbox-state JSON 必须带 `sandbox_cwd`，且必须是当前 host native absolute path；解析后总是覆盖 run plan cwd，避免缺失或相对 cwd 被静默当作当前目录运行。`gofmt`、`go test -run "TestBuildCommandRunPlan" ./internal/sandbox`、`go test ./internal/sandbox ./internal/sandbox/linuxsandbox ./internal/cli ./internal/app` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序加固 `sandbox` profile precedence 回归。补端到端测试确认 sandbox 子命令显式 `--profile` 会覆盖 root `--profile` fallback：root profile 指向 workspace 时不应抢走 sandbox profile 的 full-access。`gofmt`、`go test -run "TestSandbox(HonorsRootConfigOverrides|HonorsRootProfile|ProfileOverridesRootProfile|FullAccessProfileRunsAndInjectsEnv)" ./internal/app`、`go test ./internal/app ./internal/cli ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序加固 `sandbox` config override precedence 回归。补端到端测试确认 root `-c default_permissions=...` 会被 sandbox 子命令自己的 `-c default_permissions=...` 覆盖，保持 Rust `prepend_config_flags` 后子命令 override 后应用的优先级。`gofmt`、`go test -run "TestSandbox(HonorsRootConfigOverrides|ConfigOverridesRootConfigOverrides|HonorsRootProfile|ProfileOverridesRootProfile|FullAccessProfileRunsAndInjectsEnv)" ./internal/app`、`go test ./internal/app ./internal/cli ./internal/sandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox` runtime `use_legacy_landlock`。按 Rust debug sandbox 行为，Go 现在无 sandbox-state 时从 config `[features].use_legacy_landlock` 读取并传入 run request；一旦存在 sandbox-state，则由 state 的 `use_legacy_landlock` 值/默认 false 覆盖 config，并把最终值传给 Linux sandbox wrapper。`gofmt`、`go test -run "Test(BuildCommandRunPlan.*Legacy|SandboxRunConfigUsesLegacyLandlockFeature|Sandbox.*Legacy)" ./internal/sandbox ./internal/app`、`go test ./internal/app ./internal/cli ./internal/sandbox ./internal/sandbox/linuxsandbox` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `doctor` human/JSON fixture shape。JSON report 现在对无 issue 的 check 也固定输出 `issues: []`，human summary 行不再产生双换行；`TestReportBuildsLocalChecks` 改为锁全量 check id/category 顺序，避免只测前四个。`gofmt`、`go test -run "Test(ReportBuildsLocalChecks|JSONReportShapeStructuresAndSanitizesDetails|RenderHumanSummaryASCII|RenderHumanUsesRustDoctorGroups|RunJSONUsesStructuredReport)" ./internal/doctor`、`go test ./internal/doctor ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 加固 `doctor` human fixture 兜底。human renderer 现在会把未归入 Rust 已知分组的未来/未知 check category 放入 `Other` 分组，避免 summary 计数包含 warning/fail 但正文吞掉具体检查。`gofmt`、`go test -run "Test(RenderHuman|JSONReportShapeStructuresAndSanitizesDetails|ReportBuildsLocalChecks|RunJSONUsesStructuredReport)" ./internal/doctor`、`go test ./internal/doctor ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `apply` parser positional。按 Rust 单一 optional PATCH 参数语义，Go `codex apply` 现在最多接受一个 PATCH positional，不再把多个剩余参数拼成一个 patch；单个带空格 patch 参数保持可用。`gofmt`、`go test -run "Test(ParseApply|ParseExecpolicy|ApplyPatch|Execpolicy)" ./internal/cli ./internal/app ./internal/applypatch ./internal/execpolicy`、`go test ./internal/cli ./internal/app ./internal/applypatch ./internal/execpolicy` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `execpolicy check` Rust 匹配/JSON 形状。Go 现在无匹配时输出 Rust 同款 `matchedRules: []` 与 `decision:null`；`--resolve-host-executables` 先做 exact command token 匹配，只有 exact 未命中才走 basename/host_executable fallback，避免同时命中 exact 与 fallback 规则导致更严格 decision 抢占。`gofmt`、`go test ./internal/execpolicy ./internal/cli ./internal/app ./internal/applypatch` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `execpolicy` 规则 parser 核心校验。Go `prefix_rule` 现在拒绝空 pattern、空 alternatives、空白 justification，并支持 `match`/`not_match` token 数组和 shell 字符串例子验证；`host_executable` 现在要求 bare executable name、绝对 path、basename 匹配，重复 path 去重，后定义覆盖前定义，显式空 allowlist 禁止 basename fallback。`gofmt`、`go test ./internal/execpolicy`、`go test ./internal/cli ./internal/app ./internal/execpolicy ./internal/applypatch` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `execpolicy` `network_rule` parser 校验。Go 现在解析并验证 Rust 同形 `network_rule(host, protocol, decision, justification?)`，支持 `https_connect`/`http-connect` alias 和 `deny` decision，规范化 host/port/bracket IPv6，并拒绝 scheme/path/wildcard/whitespace host、非法 protocol 与空白 justification，不再静默忽略 invalid network rule。`gofmt`、`go test ./internal/execpolicy`、`go test ./internal/cli ./internal/app ./internal/execpolicy ./internal/applypatch` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `resume/archive/delete/unarchive/fork` 的 `fork` 快照参数。Go `codex fork` 现在暴露并执行 `--history-mode all|none|last-n`、`--last-n`、`--last-turn-id` 与 `--ephemeral`，复用 session store 已有 ForkOptions；`--last-n` 会默认切到 `last-n` 快照模式，ephemeral fork 返回 summary 但不落盘。`gofmt`、`go test -run "Test(ParseForkSnapshotOptions|ParseForkSnapshotOptionsRejectInvalidValues|ParseSession|ParseResume|ParseArchive|SessionForkSnapshotOptions|SessionForkByName|SessionArchiveUnarchiveForkAndDeleteFlow)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `delete --force` UUID 限制。按 Rust `delete_action`，Go parser 现在要求强制删除 target 必须是 UUID，`--force --name ...` 或 `--force thread-name` 返回 `--force requires a session UUID; names must be confirmed interactively`，不再允许名字绕过确认删除。`gofmt`、`go test -run "Test(ParseArchiveAndDelete|ParseSession|SessionArchiveUnarchiveForkAndDeleteFlow|SessionMutationsResolveNameSelector|SessionDeleteRequiresForce)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `delete` 非交互确认错误。Go 无 `--force` 删除现在返回 Rust 同款 `cannot confirm session deletion without an interactive terminal; rerun with --force and a session UUID`，去掉 Go port 临时文案。`gofmt`、`go test -run "TestSessionDeleteRequiresForce|TestSessionArchiveUnarchiveForkAndDeleteFlow|TestSessionMutationsResolveNameSelector" ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `cloud status` parser 错误顺序。Go 现在对 `cloud status --flag TASK` 先报 unknown option，对多个 TASK_ID 报 exactly one，缺失 TASK_ID 保持 requires 文案，避免旧的 `len(args)` 粗校验吞掉具体错误。`gofmt`、`go test -run "TestParseCloud(Commands|StatusRejectsExtrasInOrder)" ./internal/cli` 与 `go test ./internal/cli ./internal/app` 已通过。
- [x] 继续阶段 8 顺序补 `root strict-config` 与 `remote flag` parser surface。按本地 Rust `cli/src/main.rs` 复核，Go 已用表驱动锁定 root `--strict-config` 支持/拒绝矩阵与 `app-server daemon ...` 错误优先级；`resume/archive/delete/unarchive/fork` 子命令现支持 Rust 同形 `--remote`、`--remote-auth-token-env`，completion 同步补齐，远端 TUI runtime 仍留在阶段 8 未完成项。`gofmt`、`go test -run "Test(RejectRootStrictConfigForUnsupportedSubcommand|AllowRootStrictConfigForSupportedSubcommands|ParseSessionRemoteOptions|ParseSession|ParseForkSnapshotOptions|GenerateCompletion|CompletionSpec)" ./internal/cli` 与 `go test ./internal/cli ./internal/app` 已通过。
- [x] 继续阶段 8 顺序补 `session` `--last` 冲突。按 Rust `SessionTuiCli`，Go `resume/fork` 现在允许 `--last PROMPT`，也允许 `SESSION --last` 重解释为 prompt；但 `--last SESSION PROMPT` 或 `SESSION --last PROMPT` 会在 parser 层报 `--last conflicts with SESSION_ID and prompt`，避免旧实现把两个 positional 全部拼成 prompt。`gofmt`、`go test -run "Test(ParseResumeLastAndPrompt|ParseSessionLastRejectsExplicitSessionAndPrompt|ParseSession|ParseForkSnapshotOptions|SessionResume|SessionFork|SessionArchive|SessionMutations|SessionDelete)" ./internal/cli ./internal/app` 与 `go test ./internal/cli ./internal/app ./internal/session` 已通过。
- [x] 继续阶段 8 顺序补 `resume/archive/delete/unarchive/fork` Rust target/output 语义。Go 现在按 Rust `resolve_session_target` 先把 UUID positional 当 thread id，非 UUID positional 走 exact name；`archive/unarchive` mutation name lookup 只命中 cli/vscode 可交互 session；`archive/unarchive/delete` 成功输出对齐 `Archived|Unarchived|Deleted session {name} ({id}).`/`{id}.`。`gofmt`、`go test -run "TestSession(MutationsResolveNameSelector|MutationsResolvePositionalNameLikeRust|ResumeByName|ForkSnapshotOptions|ArchiveUnarchiveForkAndDeleteFlow)" ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 纠偏 `fork` CLI snapshot flags。重新按本地 Rust `ForkCommand` 对照后，Go parser/completion 已移除 `--history-mode`、`--last-n`、`--last-turn-id`、`--ephemeral` 的 fork CLI 暴露；`codex fork` 保持 Rust surface 的 `SESSION_ID`/`--last`/`--all`/prompt/remote/config，并默认全量复制历史。`gofmt`、`go test -run "Test(ParseForkRustSurface|ParseForkRejectsSnapshotOptionsLikeRust|GenerateCompletion|CompletionSpec|SessionForkPositionalNameCopiesAllHistory|SessionForkByName)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `delete` 非 force 交互确认。Go `codex delete SESSION` 现在按 Rust 顺序先 resolve/read target name，非 TTY 返回 `cannot confirm session deletion without an interactive terminal; rerun with --force and a session UUID`；TTY 下向 stderr 输出永久删除提示和 `Continue? [y/N]:`，`y/yes` 执行删除，其它输入输出 `Delete cancelled.`。`gofmt`、`go test -run "TestSessionDelete(RequiresForce|InteractiveConfirmation)|TestSessionArchiveUnarchiveForkAndDeleteFlow|TestSessionMutationsResolveNameSelector" ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 纠偏 session `--name` selector。按 Rust `ResumeCommand`/`SessionArchiveCommand` 复核，Go 已移除 `resume/archive/delete/unarchive/fork` 的额外 `--name` parser/completion surface，统一通过 positional `SESSION`/`SESSION_ID` 解析 UUID 或 exact name；`delete --force --name ...` 现在按未知 delete option 拒绝。`gofmt`、`go test -run "Test(ParseArchiveAndDelete|ParseSessionRejectsNameFlagLikeRust|ParseSessionRemoteOptions|ParseForkRustSurface|ParseForkRejectsSnapshotOptionsLikeRust|SessionMutationsResolveNameSelector|SessionMutationsResolvePositionalNameLikeRust|SessionResumeByName|SessionForkByName)" ./internal/cli ./internal/app`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `resume/fork` TUI-only flags。按 Rust `tui::Cli`/`SessionTuiCli`，Go root/resume/fork parser 现在接受 `--ask-for-approval/-a`、`--search`、`--no-alt-screen` 并记录到 shared options；archive/delete/unarchive 仍只接受 `SharedCliOptions`，遇到这些 TUI-only flags 返回 unknown。`gofmt`、`go test -run "Test(ParseTUIOnlyFlagsForRootResumeAndFork|ParseSessionMutationRejectsTUIOnlyFlagsLikeRust|ParseSessionRejectsNameFlagLikeRust|ParseSessionRemoteOptions|GenerateCompletion|CompletionSpec)" ./internal/cli`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 TUI-only approval conflict。按 Rust `TuiSharedCliOptions::mark_tui_args`，Go 现在拒绝同一个 root/resume/fork TUI shared scope 内同时传 `--dangerously-bypass-approvals-and-sandbox`/`--yolo` 与 `--ask-for-approval/-a`。`gofmt`、`go test -run "TestParseTUIOnly" ./internal/cli`、`go test ./internal/cli ./internal/app ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 本轮 session/parser/execpolicy/apply/cloud/strict-config 相关累计改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 `exec-server --strict-config` 运行时 config 校验。Go `config.LoadEffectiveWithOptions` 增加 strict 顶层未知字段校验，`codex exec-server --strict-config` 与 root `codex --strict-config -c foo=bar exec-server ...` 都会返回 `unknown configuration field`，非 strict 本地 exec-server 保持 Rust 同款忽略 config load failure。`gofmt`、`go test -run "Test(LoadEffectiveStrictConfig|ExecServerStrictConfigRejectsUnknownConfig|ExecServerConfigOutput|ParseExecServerAndStdioToUDS|AllowRootStrictConfigForSupportedSubcommands)" ./internal/config ./internal/app ./internal/cli` 与 `go test ./internal/config ./internal/cli ./internal/app ./internal/execserver` 已通过。
- [x] 继续阶段 8 顺序补 `responses-api-proxy` 运行输出/server-info/URL 校验。按 Rust `run_main`，Go proxy 启动不再向 stdout 写调试 JSON，只保留 stderr listen 行；server-info 锁定为单行 `port/pid` JSON；relative upstream URL 与缺 host URL 错误文案对齐。`gofmt`、`go test -run "TestResponsesAPIProxy" ./internal/app`、`go test -run "TestParseResponsesAPIProxy" ./internal/cli`、`go test ./internal/app ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `exec-server` remote parser/auth 安全边界。Go 现在拒绝 Rust clap 同款 `--listen`+`--remote` 与缺 remote 的 `--use-agent-identity-auth`；remote runtime 会先做 config/auth 校验、API-key remote host allowlist 和 agent identity env 校验，且校验通过后返回明确 unsupported，不再向 stdout 输出假注册 JSON。`gofmt`、`go test -run "Test(ExecServerRemoteValidationLikeRust|ExecServerStrictConfigRejectsUnknownConfig|ExecServerConfigOutput|ParseExecServerAndStdioToUDS)" ./internal/app ./internal/cli`、`go test ./internal/app ./internal/cli ./internal/config ./internal/execserver` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 session `--remote` runtime gate。`resume/archive/delete/unarchive/fork` 现在按 Rust 合并 root/subcommand remote，子命令 remote 覆盖 root remote，并复用 remote auth token 校验；remote 模式不会再误操作本地 session store，而是返回明确 unsupported。`gofmt`、`go test -run "TestSession(RemoteFlagsValidateBeforeLocalStore|ArchiveUnarchiveForkAndDeleteFlow|ResumeByName|ForkByName|MutationsResolve)" ./internal/app`、CLI remote 窄测、`go test ./internal/app ./internal/cli ./internal/session` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `app-server` listen/remote-control requirements gate。按 Rust `AppServerTransport`/`run_main_with_transport_options`，Go 现在接受 `unix://` 相对 socket path、区分 unsupported listen 与 invalid websocket 文案；app-server runtime 会加载 root config 和 `requirements.toml`，在 `allow_remote_control=false` 时拒绝显式 `--remote-control`、`listen off` 和 remote-control RPC，且 WebSocket router 也继承 startup options。`gofmt`、`go test -run "TestParseAppServer" ./internal/cli`、`go test -run "TestAppServer(ListenOff|RemoteControl|RootConfigOverride)" ./internal/app`、`go test -run "TestWebSocket(RouterFactory|ServerHealth|ListenAddress)" ./internal/appserver`、`go test ./internal/app ./internal/cli ./internal/appserver ./internal/config`、`go list -buildvcs=false ./...` 与二次 `go test ./...` 已通过；首次全量仅遇到 Windows TempDir cleanup 竞争，`./internal/appserver` 单包和全量重跑均通过。
- [x] 继续阶段 8 顺序补 `remote-control` 前台真实生命周期。按 Rust `remote_control_cmd.rs`，Go 前台模式不再直接 `manager.Enable()` 假成功；Unix 路径会创建私有临时 socket、启动 `appserver.ServeUnixSocket`、通过 JSON-line socket client 发送 initialize/initialized 与 `remoteControl/enable` ready probe，ready 后打印并等待 Ctrl-C/context 或 app-server 退出；Windows 按 Rust unsupported platform 返回。`gofmt`、`go test -run "TestRemoteControl" ./internal/app`、`go test -run "TestLocalSocketRemoteControlClientEnable" ./internal/appserverdaemon`、`go test ./internal/app ./internal/appserverdaemon ./internal/cli` 与 `go list -buildvcs=false ./...` 已通过；`./internal/appserver` 组合测试首次遇到 Windows TempDir cleanup 竞争，单包重跑通过。
- [x] 继续阶段 8 顺序补 daemon remote-control already/socket probe。按 Rust `set_remote_control_locked`，Go `SetRemoteControl` 现在会在 unmanaged app-server socket probe 成功时返回 `app server is running but is not managed by codex app-server daemon`；运行中 managed app-server 已经处于目标 enable/disable 状态时，会先 probe app-server version，再向控制 socket 发送 `remoteControl/enable`/`remoteControl/disable` RPC，并把 app-server version 带入 already 输出。补 JSON-line disable client 与 lifecycle 三分支回归。`gofmt`、`go test -run "Test(LocalSocketRemoteControlClient|LifecycleRunnerSetRemoteControl)" ./internal/appserverdaemon`、`go test ./internal/appserverdaemon ./internal/app ./internal/cli`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 daemon 真实 managed lifecycle。按 Rust `Daemon::start/restart/bootstrap/stop/version` 与 PID backend，Go 现在 daemon CLI 在 Windows 返回 unsupported；Unix PID backend 会从 managed standalone codex bin 启动 app-server/更新循环、写 PID record、stderr log、stop 时 SIGTERM/SIGKILL，并通过 socket readiness probe 返回 app-server version；bootstrap/restart/stop/set-remote-control 遇到 unmanaged socket 会报 Rust 同款错误；`remote-control start` 也改为通过 daemon control socket 发送 enable RPC，不再用内存 manager 假成功。补 lifecycle managed stub、unmanaged socket、platform boundary 和 update-loop 回归。`gofmt`、`go test -run "Test(LifecycleRunner|LocalSocketRemoteControlClient|UpdateOnce|UpdateModes|ShouldReexec|CurrentUpdaterIdentity)" ./internal/appserverdaemon`、`go test -run "TestAppServerDaemon|TestRemoteControl|TestAppServer(RemoteControl|ListenOff|RootConfigOverride)" ./internal/app`、`go test ./internal/appserverdaemon ./internal/app ./internal/cli`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 daemon operation/PID reservation locks。按 Rust `acquire_operation_lock` 与 PID backend reservation lock，Go 现在 start/restart/stop/bootstrap/set-remote-control 使用 daemon operation lock，update loop 的 try-restart 在锁忙时返回 `busy`；PID backend start 会持有 pid lock 后 reserve pid file，`IsStartingOrRunning`/stop 会识别活跃 reservation 与 stale 空 pid，stop 保留 grace 后 force kill 语义。新增 Unix/Windows lock helper，Windows daemon lifecycle 仍保持 unsupported。`gofmt`、`go test ./internal/appserverdaemon`、`go test ./internal/appserverdaemon ./internal/app ./internal/cli`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
- [x] 继续阶段 8 补 daemon Unix 实进程测试源码。新增 `!windows` 测试覆盖 PID backend 启动长驻 managed codex 脚本、stop 后进程退出、stale PID record 清理，以及 operation lock busy 时 try-restart 返回 `busy`；当前 Windows 本机不执行该文件，已跑本机 `go test ./internal/appserverdaemon`、`go list -buildvcs=false ./...` 与 `go test ./...` 通过。
- [x] 继续阶段 8 顺序补 `app-server --listen off` persisted remote-control preference。引入 Go 1.24 可用的 `modernc.org/sqlite v1.41.0`，Go 现在读取 Rust `state_5.sqlite` / `CODEX_SQLITE_HOME` 下 `remote_control_enrollments.remote_control_enabled=1`；`--listen off` + ResolvePersisted 命中 enabled 时进入 remote-control-only context 保活，persisted disabled/no row 仍返回 no transport，显式 `--remote-control` 在 state DB 存在时不再报 sqlite unavailable。补真实 SQLite fixture 回归；`gofmt`、`go test -run "TestAppServerListenOff" ./internal/app`、`go test ./internal/app ./internal/appserver ./internal/config ./internal/appserverdaemon ./internal/cli`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 `exec-server --listen` 本地 WebSocket transport。按 Rust `parse_listen_url`/`run_websocket_listener`，Go 现在只接受 `stdio`/`stdio://` 与 `ws://IP:PORT`，显式空 listen 返回 unsupported URL；未传 `--listen` 才默认 `ws://127.0.0.1:0`。本地 ws 启动会 stdout 打印实际 URL、提供 `/readyz`、拒绝 Origin header，并复用现有 exec-server JSON-RPC 处理，不再输出占位 JSON。`gofmt` 与 `go test -run "Test(ParseListenURLLikeRust|WebSocketTransportServesInitializeAndRejectsOrigin|ExecServerConfigOutput|ExecServerWebSocketListenStartsServer|ExecServerExplicitEmptyListenIsRejectedLikeRust|ExecServerStrictConfigRejectsUnknownConfig|ExecServerRemoteValidationLikeRust|ParseExecServerAndStdioToUDS)" ./internal/execserver ./internal/app ./internal/cli` 已通过。
- [x] 继续阶段 8 顺序补 `responses-api-proxy` 剩余 runtime 语义。Go 现在按 Rust 只过滤上游请求的 `Authorization`/`Host`，响应 header 过滤收窄到 Rust 管理集合并显式传播 upstream `Content-Length`；`/shutdown` 改为 HTTP 200 空响应后调用进程 exit hook；dump response 在正常 EOF 或客户端提前断开时都会写出已读内容并只把写入失败记 stderr。`gofmt`、`go test -run "TestResponsesAPIProxy" ./internal/app` 与 `go test ./internal/app ./internal/cli` 已通过。
- [x] 继续阶段 8 顺序补 `exec --json` Usage wire shape。Go `turn.completed.usage` 去掉 Rust schema 没有的 `total_tokens`，只保留 input/cached input/output/reasoning output 四字段；protocol 层补 `turn.failed` helper 供后续 failed-turn notification 接入。`gofmt` 与 `go test ./internal/protocol ./internal/exec` 已通过。
- [x] 继续阶段 8 顺序补 `app-server --listen off` persisted remote-control scope 精确匹配。按 Rust `resolve_persisted_preference`，Go 现在从当前 Codex backend auth 解析 account id，按 `chatgpt_base_url` 规范化 websocket URL，并用 `websocket_url + account_id + app_server_client_name("")` 精确查询 `remote_control_enrollments.remote_control_enabled=1`；补 account mismatch、非空 client name、custom `chatgpt_base_url` 与真实 SQLite/auth fixture 回归。`gofmt`、`go test -run "TestAppServerListenOff" ./internal/app`、`go test ./internal/app ./internal/appserver ./internal/config ./internal/appserverdaemon ./internal/cli`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 daemon readiness stderr tail context。按 Rust `app-server-daemon` 的 `wait_until_ready`/`app_server_not_ready_context`/`read_stderr_log_tail` 对照，Go 现在给 readiness 超时路径补可测钩子，并用回归锁定 stderr log tail 只保留最近完整行、missing/empty 返回 nil、context 先输出 daemon app-server 再输出 stderr tail。`gofmt`、`go test -run "Test(ReadStderrLogTail|PIDLogTail|LifecycleRunnerWaitUntilReady)" ./internal/appserverdaemon`、`go test ./internal/appserverdaemon`、`go test ./internal/appserverdaemon ./internal/app ./internal/cli`、`go list -buildvcs=false ./...` 与重跑 `go test ./...` 已通过；首次全量遇到一次 Windows TempDir cleanup 竞争，相关单测/单包/全量复跑均通过。
- [x] 继续阶段 8 顺序补 remote-control status notification core。按 Rust `RemoteControlStatusPublisher`/`RemoteControlHandle::publish_status` 语义，Go `remotecontrol.Manager` 现在重复 enable/disable 不返回 status-change notification，disable 会清空 `environmentId`；app-server router 在 enable/disable 状态变化时广播 `remoteControl/status/changed`，并沿用 notification opt-out 过滤。`gofmt`、`go test -run "Test(ManagerEnableDisableAndStatus|RuntimeRouterRemoteControlStatusChanged)" ./internal/remotecontrol ./internal/appserver`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app` 已通过。
- [x] 继续阶段 8 顺序补 initialize remote-control status snapshot。按 Rust `lib.rs` initialize 后向当前 connection 发送 `ServerNotification::RemoteControlStatusChanged(remote_control_status)`，Go stdio/json-line 与 websocket transport 现在在 initialize response 后发送当前 status snapshot，且 opt-out 时不发送；补 stdio/websocket 回归，并确认本地 socket remote-control ready probe 不受 notification 影响。`gofmt`、`go test -run "Test(StdioServerInitialize|WebSocketServerHealthAndInitialize|RuntimeRouterRemoteControlStatusChanged|ManagerEnableDisableAndStatus)" ./internal/appserver ./internal/remotecontrol`、`go test ./internal/appserver`、`go test ./internal/app ./internal/appserverdaemon ./internal/remotecontrol` 已通过。
- [x] 阶段 8 remote-control status lifecycle 本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control protocol/enrollment/server API 基础。按 Rust `protocol.rs`/`auth.rs`/`enroll.rs`/`server_api.rs` 对齐 Go `internal/remotecontrol` 的 URL 规范化、backend snake_case 协议结构、account header 覆盖、server token refresh requirement、pair/pair-status HTTP、enroll/refresh HTTP、Retry-After deferral、transient error 分类和敏感响应体脱敏；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control protocol/enrollment/server API 本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control persisted enrollment SQLite store。按 Rust state runtime 对齐 Go `EnrollmentStore`、record get/upsert/set-enabled/delete、load/update helper 和旧表补 `remote_control_enabled`；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/app ./internal/appserver ./internal/appserverdaemon`（首次遇到 Windows TempDir cleanup 竞争，`./internal/appserver` 单包重跑通过）与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control persisted enrollment SQLite store 本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control durable enable/disable manager 接入。Go app-server 启动会在 CLI 路径启用 remote-control backend，manager 非 ephemeral enable 复用/创建 enrollment 并写 enabled=true，disable 写 enabled=false，ephemeral probe 不落库；新增 router/server Close 释放 SQLite 句柄，避免 Windows state_5.sqlite cleanup 失败。`gofmt`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app`、`go test ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control durable enable/disable manager 接入本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control pairing/status refresh 接入 manager。Go manager 的 pairing start/status 现在会在 backend 模式下复用/刷新 persisted enrollment token，并在 stale enrollment 404 时走 replacement；app-server pairing RPC 改用 Context 方法。`gofmt`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app`、`go test ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control pairing/status refresh 接入 manager 本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control auth recovery 基础。Go backend manager 的 enroll/refresh 401/403 会走 `RemoteControlAuthRecovery` hook 并重试一次，app-server CLI 默认用 `auth.RefreshChatGPTTokens` 构造 codexHome recovery；补 durable enable recovery 与 pairing refresh recovery 回归。`gofmt`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app`、`go test ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control auth recovery 基础本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket wire protocol 基础。按 Rust `protocol.rs` 补 Client/Server envelope tagged JSON、ack/chunk/pong 结构和 chunk segment helper；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control websocket wire protocol 基础本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket seq/ack buffer 基础。按 Rust websocket state 补 per-stream seq id、outbound buffer 和 ack cursor 裁剪；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control websocket seq/ack buffer 基础本轮改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control clients list/revoke backend API。按 Rust `clients.rs` 给 Go backend manager 增加 client management HTTP GET/DELETE、auth recovery、query/last_seen_at/error kind/limit 校验，app-server RPC 改走 Context 路径；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control client tracker 基础。按 Rust `client_tracker.rs` 给 Go remotecontrol 增加 remote client stream tracker、transport events、outbound writer、seq 去重、legacy stream fallback、ping/idle close/connection close 回归；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control segment split/reassembly。按 Rust `segment.rs` 给 Go remotecontrol 增加 client chunk reassembler、server envelope splitter、segment 常量与 stale/duplicate/stream-change 回归；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket state client observation/delivery。按 Rust `WebsocketState` 接入 client segment observation、delivery seq/cursor 记录、ack 裁剪 outbound buffer 与 invalidate 回归；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 阶段 8 remote-control clients/tracker/segment/websocket state 本轮累计改动做全量回归，`go test ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket connect request 基础。按 Rust `websocket.rs` 补 websocket 握手 header、subscribe cursor、protocol version、server token 校验、missing app-server 404 detail 识别、错误 body/header 预览和 reconnect backoff cap/reset；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket enrollment prepare。按 Rust websocket prepare 路径补 manager backend 的 persisted enrollment load、missing-token refresh、refresh 404 replacement、account-change clear 与 enabled preference 保留；`gofmt`、`go test ./internal/remotecontrol` 已通过；`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 首次遇到 `TestCommandExecTTYStreamsAndResizes` PTY 输出抖动，单测重跑和组合重跑均通过，`go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket connect client 基础。按 Rust `connect_remote_control_websocket` 对齐 Go manager 的真实 websocket dial、401/403 清 server token、missing remote app-server 404 stale enrollment replacement 与真实握手回归；`gofmt`、`go test ./internal/remotecontrol`、`go test -run TestProcessServiceSpawnTTYStreamsAndResizes ./internal/appserver`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过；组合首跑仍遇到一次 Windows PTY 输出抖动，单测和组合重跑均通过。
- [x] 继续阶段 8 顺序补 remote-control websocket connection workers。按 Rust writer/reader worker 顺序接上 Go `WebsocketState`、`ClientTracker`、server envelope split、ack buffer 和 closed/idle invalidation；同时修正 state 只记录 chunk delivery cursor，普通 client_message 继续由 tracker 去重，重组 chunk 用内部 `json:"-"` 标记保持 Rust delivery 语义；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `--listen off` remote-control-only websocket loop/transport bridge。按 Rust `RemoteControlWebsocket::run` 与 app-server `TransportEvent` 主循环补 Go `RemoteControlWebsocketLoop`、appserver remote-control transport bridge、`ServeRemoteControlOnly`，使 persisted enabled 或显式 `--remote-control` 在无本地 listen 时启动真实 websocket backend 连接循环、client envelope 到 RuntimeRouter、router response/notification/server-request 回写 remote client，并随 context 停止；`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/appserver`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket/pairing proactive refresh throttle。按 Rust `websocket_refresh_tests.rs` 补 Go 回归：proactive refresh 502 继续用旧有效 token 建 websocket 并设置 `NextRefreshAt`，required refresh 502/Retry-After 不进入 websocket 且下一次 connect 返回 refresh deferred，websocket 侧 Retry-After 会节流 pairing refresh；修复 manager 在 required refresh 错误返回前未把 mutated enrollment/`NextRefreshAt` 写回 current 的差异。`gofmt`、`go test ./internal/remotecontrol`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control 外部 auth server-request recovery。按 Rust `UnauthorizedRecovery` 外层恢复意图补 Go app-server backend：remote-control backend 在当前 auth 是 `chatgptAuthTokens` 时，enroll/refresh 401/403 会通过 `account/chatgptAuthTokens/refresh` server request 获取新 token、更新 auth/account 状态并用新 token 重试；普通 ChatGPT 继续使用本地 refresh，空/custom loader 不默认改变 recovery 行为；外部 request 加 30s 超时。`gofmt`、`go test ./internal/appserver -run "TestRuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 已通过。
- [x] 继续阶段 8 顺序补 remote-control websocket auth revision watch。按 Rust `auth_change_rx.changed()`/`mark_recovery_auth_change_seen` 补 Go loop 的 `AuthRevision` 钩子：reconnect backoff 中 auth revision 变化会立即唤醒重连并把 reconnect attempt 清零；单次 recovery revision 可标记已消费，两个以上竞态 revision 保留给外层 loop；app-server 对 login/logout/external auth refresh/managed status refresh 统一 bump revision，`ServeRemoteControlOnly` 注入 router revision。`gofmt`、`go test ./internal/remotecontrol -run "TestRemoteControlWebsocketLoop" -count=1 -v`、`go test ./internal/appserver -run "TestRuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount)" -count=1 -v`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control `UnauthorizedRecovery` state machine。按 Rust `login/src/auth/manager.rs` 建模 managed `reload -> refresh_token -> done` 与 external `external_refresh -> done`，补 `mode_name`/`step_name`/`unavailable_reason`/`auth_state_changed`，reload account mismatch 使用 Rust 同款错误文案；app-server 默认 recovery 构造改走 state machine，external bridge 作为 `ExternalRefresh` step。`gofmt`、`go test ./internal/remotecontrol -run "TestUnauthorizedRecovery|TestManager(DurableEnableRecoversAuthOnPermissionDenied|DurablePairingRefreshRecoversAuthOnPermissionDenied|BackendClientManagementRecoversUnauthorizedOnce)" -count=1 -v`、`go test ./internal/appserver -run "TestRuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount)" -count=1 -v`、`go test ./internal/remotecontrol -count=1`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control `UnauthorizedRecovery` reset/observer 生命周期。Go backend options 新增 recovery reset hook，app-server 默认 recovery 通过 controller 同时提供 recover/reset；websocket loop 在 successful connect 与 auth-change wake 时 reset recovery state；controller observer 暴露 recovery `mode/step/auth_state_changed/unavailable_reason/error` 元数据。`gofmt`、`go test ./internal/remotecontrol -run "TestUnauthorizedRecovery|TestRemoteControlWebsocketLoop" -count=1 -v`、`go test ./internal/appserver -run "TestRuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount)" -count=1 -v`、`go test ./internal/remotecontrol -count=1`、`go test ./internal/appserver -run TestCommandExecTTYStreamsAndResizes -count=1 -v`、`go test ./internal/appserver -run "TestRuntimeRouter(SessionStartHookInjectsAdditionalContextOnce|TurnStartPreservesThreadOriginator)" -count=1 -v`、`go test ./internal/appserver -count=1`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 与 `go list -buildvcs=false ./...` 已通过；组合首跑遇到 Windows PTY/session 临时目录抖动，相关单测、appserver 包与组合包重跑均通过。
- [x] 继续阶段 8 顺序补 remote-control recovery revision mark 精确绑定。Go loop 现在只在 backend recovery controller 消费到 `auth_state_changed=true` 时调用 `markSingleRecoveryAuthChangeSeen`，避免把 connect 期间普通外部 auth 变化误当 recovery 自身变更吞掉；新增回归覆盖 connect 期间普通 revision 唤醒、recovery revision 不唤醒、后续竞态 revision 继续唤醒。`gofmt`、`go test ./internal/remotecontrol -run "TestRemoteControlWebsocketLoop|TestUnauthorizedRecovery" -count=1 -v`、`go test ./internal/appserver -run "TestRuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount)" -count=1 -v`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 与 `go list -buildvcs=false ./...` 已通过；组合首跑再次遇到 Windows PTY 抖动，单测与组合包重跑通过。
- [x] 继续阶段 8 顺序补 doctor human renderer fixture 差异。按 Rust `cli/src/doctor/output.rs` 对齐 warning overall label 为 `degraded`，详细输出 footer 增加 `--all expand truncated lists`，summary 输出 footer 增加 `--all`/`--json` 提示；补 renderer 回归。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor human Notes/idle summary。按 Rust `notes_for_report`/`StatusCounts::from_report`，Go human renderer 现在会在分组前输出 update/rollout/sandbox/non-ok/mixed-auth Notes，summary 增加 notes 计数，app-server `status: not running` 显示 idle `[--]` 且不再算 ok；未知 category 仍保留 Go 既有 `Other` 兜底。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(PromotesNotesAndIdleLikeRust|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过；首次未设置工作区 `GOCACHE/GOTMPDIR` 时命中默认 cache 权限问题，改用项目内 cache 后通过。
- [x] 继续阶段 8 顺序补 doctor human row description。按 Rust `row_description`/`display_summary`，Go 现在对 system/runtime/install/search/git/terminal/title/state/config/mcp/sandbox/network/websocket/app-server 行从 details 提炼短摘要，warning/fail 的 issue/remediation 仍优先显示；补多 category 摘要回归。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor human detail issue metadata。按 Rust `detail_lines` 的 `attach_issue_metadata`/`issue_remedies`，Go detailed human 输出现在把 issue `fields` 匹配到 detail label，使用 `>` 行携带 measured/expected，并以 `->` 输出去重 remedy，不再输出旧的 `issue/expected/remedy` dump 行；补 expected/measured/remedy 去重回归。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor human detail value humanization。按 Rust `humanize_value`，Go detailed human 输出现在对路径值做 HOME `~` 缩写与中间截断，对 ISO UTC timestamp 输出 `YYYY-MM-DD HH:MM UTC`；补路径/时间戳回归。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(DetailsHumanizePathAndTimestampLikeRust|DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor human PATH entries `--all` 展示。按 Rust `install_details`/`git_details` 的 PATH entries 折叠，Go detailed human 输出现在把 `PATH git #`/`PATH codex #` 合并为 `PATH entries (N)`，默认显示前三条并提示 `... (full list with --all)`，`--all` 展开完整列表。`gofmt`、`go test ./internal/doctor -run "TestRenderHuman(PathEntriesRespectAllLikeRust|DetailsHumanizePathAndTimestampLikeRust|DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor feature flags detail 与 `--all` 展示。Go `featureCheck` 现在输出 Rust renderer 使用的 `feature flags enabled`/`feature flag overrides` 标签，不再输出旧 `default enabled`；human renderer 会把 feature flags 折叠为摘要、展示 overrides 名称，并在 `--all` 时显示 enabled flags 全量列表。`gofmt`、`go test ./internal/doctor -run "Test(FeatureCheckReportsRustFeatureDetailLabels|RenderHuman(FeatureFlagsRespectAllLikeRust|PathEntriesRespectAllLikeRust|DetailsHumanizePathAndTimestampLikeRust|DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails))" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor state detail humanization。按 Rust `state_details`/`push_database_row`/`rollout_summary`，Go detailed human 输出现在合并 DB path 与 integrity，并将 active/archived rollout 文件统计格式化为 count/bytes/avg 摘要，不再显示 raw integrity/rollout bytes 行。`gofmt`、`go test ./internal/doctor -run "Test(FeatureCheckReportsRustFeatureDetailLabels|RenderHuman(StateDetailsMatchRustSummaries|FeatureFlagsRespectAllLikeRust|PathEntriesRespectAllLikeRust|DetailsHumanizePathAndTimestampLikeRust|DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails))" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor human header suffix。按 Rust `header_suffix`，Go human header 现在在 runtime check 提供 `platform` detail 时显示 `Codex Doctor vX | platform`；补 header 回归。`gofmt`、`go test ./internal/doctor -run "Test(FeatureCheckReportsRustFeatureDetailLabels|RenderHuman(HeaderIncludesPlatformLikeRust|StateDetailsMatchRustSummaries|FeatureFlagsRespectAllLikeRust|PathEntriesRespectAllLikeRust|DetailsHumanizePathAndTimestampLikeRust|DetailsAttachIssueMetadataLikeRust|PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries|SummaryASCII|DetailedFooterMatchesRustHints|UsesRustDoctorGroups|ShowsUnknownCategories|RedactsDoctorDetails))" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 remote-control `UnauthorizedRecovery` tracing 接入。默认 app-server recovery controller 现在把 observer 事件接入 `slog` 结构化日志：成功输出 `mode/step/unavailable_reason/auth_state_changed`，失败输出 `mode/step/unavailable_reason/error`；自定义 recovery hook 不强制包日志。`gofmt`、`go test ./internal/appserver -run "Test(RemoteControlAuthRecoveryLogObserverEmitsRustMetadata|RuntimeRouter(RemoteControlAuthRecoveryUsesExternalRefreshBridge|ExternalAuthRefreshBridgeUpdatesAccount))" -count=1 -v`、`go test ./internal/remotecontrol ./internal/appserver ./internal/app ./internal/appserverdaemon -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 interactive root `TERM=dumb` 行为。按 Rust `run_interactive_tui`，Go interactive 入口现在在 `TERM=dumb` 且 stdin/stderr 非 TTY 时返回同款 fatal 文案；TTY 下先输出 warning，再询问 `Continue anyway? [y/N]:`，只有 `y/yes` 继续，否则返回同款拒绝文案。`gofmt`、`go test ./internal/app -run "TestInteractive(DumbTerminalWithoutTTYFailsLikeRust|DumbTerminalTTYConfirmationLikeRust|WithoutPromptRunsLineSession|PromptNormalizesCRLF|PromptUsesExecRunner|RemoteAuthRequiresRemoteAtRuntime)" -count=1 -v`、`go test ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 local state DB recovery guidance helper。按 Rust `state_db_recovery.rs`，Go app 层新增 locked/diagnostic/auto-backup/fresh-start confirmation 文案 helper，复用已有 `internal/state` backup primitives，并覆盖 TTY 下 Enter 等待与非 TTY 自动继续文案；真实 TUI startup error 接入仍留在 interactive root 未完成项。`gofmt`、`go test ./internal/app -run "Test(LocalStateDB|Interactive(DumbTerminalWithoutTTYFailsLikeRust|DumbTerminalTTYConfirmationLikeRust|WithoutPromptRunsLineSession|PromptNormalizesCRLF|PromptUsesExecRunner|RemoteAuthRequiresRemoteAtRuntime))" -count=1 -v`、`go test ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `mcp-server --strict-config`。按 Rust `codex_mcp_server::run_main`，Go `mcp-server` 启动前会用 root `-c`/`--enable`/`--disable` 和 root/subcommand `--strict-config` 加载 effective config，未知字段立即失败；MCP runner 会复制 root options 并注入后续 `codex`/`codex-reply` exec request，避免启动配置输入在 runtime 丢失。`gofmt`、`go test ./internal/app -run "Test(MCPServerStrictConfigRejectsUnknownConfig|MCPServerInitialize|CodexToolExecRequestMapsConfig|CodexMCPRunnerAppliesRootOptionsToExecRequests)$" -count=1 -v`、`go test ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `mcp add/list/get/remove/login/logout` 的输出与 TOML 读写主体。按 Rust `mcp_cmd.rs`，Go `mcp list/get` 已拆分 stdio/streamable HTTP human 输出，env/http headers 脱敏，JSON transport 补齐 `env_vars/cwd/http_headers/env_http_headers` 与 list timeouts；disabled get 输出单行；MCP CLI store 会读取并写回 Rust 字段 `env_vars` 字符串/对象项、`cwd`、nested `oauth.client_id`、`oauth_resource`、timeouts、tool filters、scopes、required、parallel tool calls 与 per-tool config，写 TOML 时默认值省略且 inline table 数组不再误写成 array-of-tables；runtime config 同步兼容 nested OAuth，`mcp login` 无显式 scopes 时使用配置 scopes。`gofmt`、`go test ./internal/app -run "TestMCP" -count=1 -v`、`go test ./internal/mcp ./internal/config -count=1` 已通过；`mcp add` OAuth support 自动登录仍在同阶段未完成子项中。
- [x] 继续阶段 8 顺序补 `mcp add` OAuth 自动登录。按 Rust `oauth_login_support`/`resolve_oauth_scopes`/`perform_oauth_login_retry_without_scopes`，Go `mcp add` 现在仅对无 bearer token 的 streamable HTTP 做 OAuth discovery，成功时自动启动本地 callback flow、使用 discovered scopes、provider 拒绝 discovered scopes 时无 scope 重试并保存 token；`mcp login` 在无显式/配置 scopes 时也先 discovery scopes，支持回调完成后输出 Rust 同款成功文案，unsupported/unknown discovery 保留兼容 fallback URL。补 add 自动登录、unknown hint、login discovered scopes、retry-without-scopes 回归。`gofmt`、`go test ./internal/app -run "TestMCP" -count=1 -v`、`go test ./internal/mcp ./internal/config -count=1` 已通过。
- [x] 继续阶段 8 顺序补 `plugin add/list/remove/marketplace` 真实 manager。CLI 不再写 app 层临时 `plugins.json`，改用 `internal/plugin.PluginService` 读取/写入 `config.toml` 的 `[marketplaces]`/`[plugins]`，本地 marketplace 会读取真实 `.agents/plugins/marketplace.json` 与 `.codex-plugin/plugin.json`，plugin add/remove 走 install/uninstall，marketplace add/list/upgrade/remove 走 Add/List/Upgrade/Remove manager；测试改为本地 marketplace fixture 并验证不生成 `plugins.json`。`gofmt`、`go test ./internal/app ./internal/cli ./internal/plugin ./internal/config -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor 默认 report check 集合/顺序。按 Rust `doctor.rs build_report` 成功路径把 Go 默认 report 收敛为 18 个 check，移除默认输出里的 Go-only `config.features` 与 `state.threads`，并把 feature flag 明细并回 `config.load`，且按 effective config 计算 CLI enable/disable overrides；保留 legacy feature usage 追踪为未完成子项。`gofmt`、`go test ./internal/doctor -run "Test(ReportBuildsLocalChecks|ConfigCheckReportsRustFeatureDetailLabels|RenderHumanFeatureFlagsRespectAllLikeRust)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `config.load` details。按 Rust `config_check` 把 Go config details 改为 `CODEX_HOME`、`cwd`、`model`、`model provider`、`log dir`、`sqlite home`、`mcp servers`、feature flags 与 `config.toml` parse/missing/read 行，移除默认 JSON/human 中的 Go-only `config path`、`top-level keys`、CLI override 计数；相对 `CODEX_SQLITE_HOME` 按 doctor cwd 解析。`gofmt`、`go test ./internal/doctor -run "Test(ConfigCheckReportsRustFeatureDetailLabels|ReportBuildsLocalChecks|JSONReportShapeStructuresAndSanitizesDetails)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `auth.credentials` 基础 shape。按 Rust `auth_check` 改为 `auth storage mode`、`auth file`、聚合 `auth env vars present`、stored auth mode/API key/ChatGPT tokens/agent identity details；无 env/无 stored auth 从 warning 改为 fail 并输出 Rust remediation；自定义 provider `env_key` 缺失/存在走 provider-specific auth summary/remediation。`stored_auth_issues` 细分仍记为未完成子项。`gofmt`、`go test ./internal/doctor -run "TestAuthCheck" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/auth ./internal/model -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `updates.status` detail shape。按 Rust `doctor/updates.rs` 移除 Go-only `update command` detail，保留 `update action`、version cache、latest version/status/probe；npm global root target proof 仍列为未完成子项。`gofmt`、`go test ./internal/doctor -run "TestUpdates|TestUpdateAction" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/install -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `network.env` detail shape。按 Rust `network_check` 将 proxy env vars 改为聚合单行 `proxy env vars present: ...`，summary 改为 `network-related environment looks readable`，并移除 Rust doctor 没有的 `[network_proxy]` 配置 details；custom CA env 检查保留。`gofmt`、`go test ./internal/doctor -run "TestNetworkCheck" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `network.websocket_reachability` detail shape。按 Rust websocket doctor probe 发送 `OpenAI-Beta: responses_websockets=2026-02-06`，成功 detail 改为 `handshake result: HTTP ...`、reasoning/models etag/server model presence，失败 summary/detail 改为 `Responses WebSocket failed; HTTPS fallback may still work` 与 `handshake API/transport error`；DNS family 和 immediate-close grace 仍列为未完成子项。`gofmt`、`go test ./internal/doctor -run "TestWebsocketReachability" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/codexapi ./internal/model -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `mcp.config` detail/status shape。按 Rust `mcp_check_from_servers`，Go 现在按 configured/disabled/transport counts 输出，只输出存在的 transport；`disabled_reason` 会让 server 计入 disabled 并跳过后续诊断；streamable HTTP 检查 bearer token env 与 `env_http_headers`，并做 HEAD 后 GET 的 reachability probe，required/optional failure 分流；stdio 检查 server `cwd`、`env_vars` source、remote cwd、`environment_id = "local"` 与 server `env.PATH` command resolution；`internal/mcp.ServerConfig` 同步补齐 `env_vars/cwd/http_headers/env_http_headers/disabled_reason` 字段和 clone/runtime 解析。`gofmt`、`go test ./internal/doctor -run "TestMCP" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/mcp -count=1` 与 `go list -buildvcs=false ./...` 已通过；remote cwd URI 精确判定和 requirements disabled_reason 集成仍列为未完成子项。
- [x] 继续阶段 8 顺序补 doctor `sandbox.helpers` detail/status shape。按 Rust `sandbox_check`，Go `approval policy` 改为 Debug 风格，`network sandbox` 改为 `enabled/restricted`，移除 workspace writable roots details，helper 未解析时 detail 改为 `none`，execve wrapper 缺失不再导致 warning，只有 Linux sandbox helper 缺失才降级；补充 execve missing OK 与 Linux helper warning 单测。`gofmt`、`go test ./internal/doctor -run "TestSandbox|TestRenderHuman" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/sandbox ./internal/config -count=1` 已通过；Arg0DispatchPaths 精确来源仍列为未完成子项。
- [x] 继续阶段 8 顺序补 doctor `terminal.env` detail/issue shape。按 Rust `terminal_check_from_inputs`，Go 补 `stdin/stdout/stderr is terminal`、`terminal size` detail、dimension env detail、带原因的 `color output`、Rust remote env presence 列表、`LINES` narrow row issue；`TERMINFO`/`TERMINFO_DIRS` readiness 改为 `dir/file/missing/not a file or directory` 等 Rust 语义，并支持空 `TERMINFO_DIRS` 输出 `present`。`gofmt`、`go test ./internal/doctor -run "TestTerminalCheck" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/cli ./internal/shell -count=1` 已通过；真实 terminal size/supports-color/tmux/Windows console diagnostics 仍列为未完成子项。
- [x] 继续阶段 8 顺序复核 doctor `git.environment`。对照 Rust `cli/src/doctor/git.rs` 后，Go 已覆盖 selected git、PATH candidates、git version/exec-path/build-options、repo root、`.git` entry、branch `HEAD` normalization、core.fsmonitor、Windows old Git warning 的 detail/status/issue shape；本轮无需改代码。`go test ./internal/doctor -run "TestGitCheck" -count=1 -v` 已通过。
- [x] 继续阶段 8 顺序复核 doctor `terminal.title`。对照 Rust `cli/src/doctor/title.rs` 后，Go 已覆盖默认/禁用/配置 source、item alias、invalid item 去重与 quote、activity/project details、summary/status/issue/remedy 与 24 grapheme-ish truncation 主路径；本轮无需改主逻辑。`go test ./internal/doctor -run "TestTerminalTitle" -count=1 -v` 已通过；真实 config layer stack 的 project config fallback 仍列为未完成子项。
- [x] 继续阶段 8 顺序补 doctor `state.paths`。按 Rust `state_check`，Go 改为输出 `CODEX_HOME`/`log dir`/`sqlite home` readiness，补 `state DB`/`log DB`/`goals DB`/`memories DB` path readiness 和 SQLite `PRAGMA integrity_check`，缺失 DB 输出 `integrity: skipped (missing)`；active/archived rollout stats 移入 state check，并补 standalone release cache detail。`gofmt`、`go test ./internal/doctor -run "Test(State|Rollout|RenderHumanState)" -count=1 -v`、`go test ./internal/doctor ./internal/app ./internal/state ./internal/rollout -count=1` 已通过。
- [x] 继续阶段 8 顺序补 doctor `state.rollout_db_parity`。按 Rust `doctor/thread_inventory.rs`，Go doctor 现在读取 `state_5.sqlite` 的 `threads` audit 行而不是 JSON thread store，对比 rollout 文件 path/thread id/archive flag，补 missing state DB/read error、duplicate DB paths、model provider/source count summary、scan cap stale/archive skipped、Rust 同款 summary/issue/remediation；补 missing DB、scan cap、source summary 回归。`gofmt`、`go test ./internal/doctor -run "TestRolloutDBParity" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/app ./internal/state ./internal/rollout -count=1` 与 `go list -buildvcs=false ./...` 已通过；`normalize_for_path_comparison` 极端 path key 边界仍列为未完成子项。
- [x] 继续阶段 8 顺序补 doctor `app_server.status`。按 Rust `doctor/background.rs`，Go 现在 socket 存在时调用 app-server daemon initialize probe 读取 app-server version，running 输出 `app-server version: ...`，probe 失败输出脱敏/截断后的 `app-server version: unavailable (...)`，不再仅凭 socket 文件类型判断 running；补 running version、stale unavailable、probe error 脱敏截断回归。`gofmt`、`go test ./internal/doctor -run "TestBackground(Server|Probe)|TestRenderHuman(PromotesNotesAndIdleLikeRust|RowDescriptionsMatchRustSummaries)" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/appserverdaemon ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序复核 doctor `network.provider_reachability`。对照 Rust `provider_reachability_plan_from_parts`/`provider_reachability_check` 后，Go 已覆盖 auth mode endpoint plan、Bedrock/Azure route probe skip、base URL HEAD probe、`/models` GET route probe、warning/fail issue/remediation；本轮修正 `/models` route query 参数拼接，按 Rust 原样 `key=value` 拼接而不是 URL encode。`gofmt`、`go test ./internal/doctor -run "TestProviderReachability" -count=1 -v`、`go test ./internal/doctor -count=1`、`go test ./internal/doctor ./internal/model ./internal/config ./internal/auth -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `auth.credentials` stored auth issues。按 Rust `stored_auth_issues`，Go 现在校验 API key、ChatGPT tokens/refresh metadata、external ChatGPT account id/refresh metadata、agent identity auth material、personal access token、Bedrock API key，并把 issue 明细加入 details；stored credentials incomplete 无 env fail，有 env 兜底 warning。`gofmt`、`go test ./internal/doctor -run "TestAuthCheck|TestStoredAuth" -count=1 -v`、`go test ./internal/doctor ./internal/auth -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `updates.status` npm_global_root_check。按 Rust `doctor/updates.rs`，Go 现在仅对非 cargo target/debug|release 的 npm-managed binary 执行 `npm root -g` proof，输出 `npm update target`、mismatch 的 `running package root`/`npm package root`，missing package root/npm unavailable 降级 warning，mismatch 保持 fail 且 latest probe 不覆盖 summary；补 match/mismatch/missing/inherited cargo env 回归。`gofmt`、`go test ./internal/doctor -run "TestUpdates|TestUpdateAction" -count=1 -v`、`go test ./internal/doctor ./internal/install -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `installation` npm-managed proof。按 Rust `installation_check`，Go installation check 改为输出 `current executable`、`install context`、managed npm/bun env、managed package root、`PATH codex` entries 和 npm global root proof，移除 Go-only installation id/managed bin 诊断；`runtime.provenance` 也改用 doctor install context，cargo target/debug|release 继承的 managed env 会被忽略；managed env 判断同步 Rust `var_os` 存在语义。`gofmt`、`go test ./internal/doctor -run "Test(InstallCheck|Updates|UpdateAction|RuntimeCheck|RenderHumanRowDescriptionsMatchRustSummaries)" -count=1 -v`、`go test ./internal/doctor ./internal/install -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `network.websocket_reachability` DNS/immediate close。按 Rust `websocket_reachability_check`，Go 在 WebSocket endpoint 后追加 DNS address family detail，成功握手后等待 250ms 捕获服务端立即 close，输出 `immediate close code/reason` 并用 Rust 同款 warning summary/remediation；正常成功测试改为延迟 close，新增 immediate close 回归。`gofmt`、`go test ./internal/doctor -run "TestWebsocketReachability" -count=1 -v`、`go test ./internal/doctor ./internal/codexapi ./internal/model -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `mcp.config` remote cwd URI 与 requirements disabled_reason。按 Rust `to_inferred_path_uri` 收紧 remote stdio cwd 判定，拒绝 `file://` URI；Go doctor MCP config 解析新增 `requirements.mcp_servers` command/url exact/prefix/regex allowlist，未命中 server 设为 disabled 并写 `requirements (config)`，doctor 计 disabled 且跳过 required 输入/HTTP probe。`gofmt`、`go test ./internal/doctor -run "TestMCP" -count=1 -v`、`go test ./internal/doctor ./internal/config -count=1` 已通过。
- [x] 继续阶段 8 顺序补 doctor `sandbox.helpers` Arg0DispatchPaths 来源。按 Rust `arg0_dispatch_or_else`/`sandbox_check(config, arg0_paths)`，Go 入口现在把 `cli.DispatchPaths` 显式传到 app/doctor；doctor helper details 只使用传入路径，`none`/missing path/status 与 Rust `push_path_detail` 主体一致，不再扫描 PATH/LookPath，Linux helper 无 alias 时 fallback 到当前 exe。`gofmt`、`go test ./internal/doctor -run "TestSandbox|TestRenderHuman" -count=1 -v`、`go test ./internal/cli -run "Test(Dispatch|PrepareArg0|PathEnv)" -count=1 -v`、`go test ./cmd/codex ./internal/app ./internal/doctor ./internal/cli ./internal/sandbox ./internal/config -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `terminal.env` 真实探测。按 Rust `TerminalCheckInputs::detect`/`terminal_check_from_inputs`，Go terminal check 现在真实采集 terminal size、stream color support、tmux client/options diagnostics 和 Windows console code page/mode details；报告支持 `terminal size: COLSxROWS`、真实 size width/height issue、`terminal color support not detected` reason，tmux/Windows detail 非 fatal 注入。`gofmt`、`go test ./internal/doctor -run "TestTerminalCheck" -count=1 -v`、`go test ./internal/doctor ./internal/shell ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `terminal.title` project config root fallback。按 Rust `terminal_title_project_root(config, cwd)`，Go 真实 check 现在先取 git root，非 git 时通过 config layer stack 读取 trusted project layer 的 `DotCodexFolder`，`.codex/` 存在但没有 `config.toml` 也输出 `terminal title project source: project config`；未 trusted project 不越权使用该 layer，回退 cwd。`gofmt`、`go test ./internal/doctor -run "TestTerminalTitle" -count=1 -v`、`go test ./internal/doctor ./internal/config ./internal/app ./internal/cli -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `state.rollout_db_parity` path key。按 Rust `path_key -> normalize_for_path_comparison(path).unwrap_or_else(|_| path.to_path_buf())`，Go rollout parity key 现在复用 `utils.NormalizeForPathComparison`，canonicalize/EvalSymlinks 成功才使用绝对解析路径和 WSL normalize；失败时保留原始 path，不再 Abs/Clean，也不再 Windows 全 lowercase；archived/active root prefix 改为 path-aware `filepath.Rel` 判断。`gofmt`、`go test ./internal/doctor -run "TestRolloutDBParity|TestRolloutParity" -count=1 -v`、`go test ./internal/doctor ./internal/rollout ./internal/state ./internal/utils -count=1` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 doctor `config.load` legacy feature usage。按 Rust `Features::legacy_feature_usages`/`feature_for_key`，Go `internal/features` 现在把 legacy aliases canonicalize 到正式 key，忽略 Rust no-op removed keys，记录 `codex_hooks`/`web_search`/`experimental_use_unified_exec_tool` 以及强制 deprecated `features.web_search_request`/`features.use_legacy_landlock` usage；`config.FeatureSettings()` 保持旧 map API，新增 usage 元数据供 doctor 输出 `legacy feature flag: alias -> canonical`。`gofmt`、`go test ./internal/features -run Test -count=1 -v`、`go test ./internal/config -run "Test(FeatureSettings|LoadEffective|LoadParses)" -count=1 -v`、`go test ./internal/doctor -run "TestConfigCheck" -count=1 -v`、`go test ./internal/features ./internal/config ./internal/doctor -count=1`、`go test ./internal/app ./internal/exec ./internal/appserver -run "Test.*Feature|Test.*Config|Test.*Runtime|Test.*Turn" -count=1` 已通过。
- [x] 继续阶段 8 顺序补 `sandbox --include-managed-config` runtime。按 Rust `debug_sandbox` managed config/requirements 分支，Go sandbox 运行时现在把 `--include-managed-config` 传入 config loader；`LoadEffectiveWithOptions` 支持显式包含 managed config file layer，默认 Windows 从 `$CODEX_HOME/managed_config.toml`、非 Windows 从 `/etc/codex/managed_config.toml` 读取，测试可指定 path；sandbox plan 不再把 managed config 标为 unsupported，include 后 shell env policy 等配置会覆盖用户配置。`gofmt`、`go test ./internal/config -run "TestLoadEffectiveWithOptionsIncludesManagedConfigLayer|TestLoadEffective|TestLoadParses" -count=1 -v`、`go test ./internal/app -run "TestSandbox(IncludeManagedConfig|RunConfig|Defaults|Honors|Config|FullAccess|Workspace)" -count=1 -v`、`go test ./internal/sandbox -run TestBuildCommandRunPlan -count=1 -v`、`go test ./internal/cli ./internal/app ./internal/config ./internal/sandbox -count=1` 与 `go list -buildvcs=false ./...` 已通过；system/MDM requirements 和 origin stack 全量 parity 已另列后续条目。
- [x] 继续阶段 8 顺序补 `sandbox` managed proxy containment 参数。对照 Rust `debug_sandbox` 的 `allow_network_for_proxy(enforce_managed_network)`，Go `CommandRunRequest/Plan` 现在能表达 active managed proxy containment，并把它传到 Linux sandbox wrapper 的 `--allow-network-for-proxy`；底层 wrapper 在该模式下会抑制 `--use-legacy-landlock`。`gofmt`、`go test ./internal/sandbox -run "TestBuildCommandRunPlan(UsesRequestLinuxSandboxHelper|AllowsNetworkForProxy)|TestBuildCommandRunPlanAppliesSandboxStateReadableRootsAsReadOnly" -count=1 -v`、`go test ./internal/sandbox/linuxsandbox -run "TestCreateCommandArgs" -count=1 -v` 已通过；proxy 启动/env/MITM CA readable root 全链路已另列后续条目。
- [x] 继续阶段 8 顺序补 `execpolicy check` CLI 与规则 parser 边界。按 Rust `ExecPolicyCheckCommand` 的 trailing `COMMAND`/`allow_hyphen_values`，Go parser 现在把未知 hyphen token 当作命令起点；规则 parser 从字符串查找升级为轻量 call scanner，支持 `prefix_rule (` 空白形式，未知 policy 函数/未知 named argument 报错，`host_executable` 缺 `paths` 不再被误当空 allowlist。`gofmt`、`go test ./internal/cli -run "TestParseExecpolicyCheck|TestParseApply" -count=1 -v`、`go test ./internal/execpolicy -count=1 -v`、`go test ./internal/cli ./internal/app ./internal/execpolicy ./internal/applypatch -count=1` 已通过；完整 Starlark 求值语义已另列后续条目。
- [x] 继续阶段 8 顺序补 `execpolicy` builtin 参数绑定与类型校验。Go 现在按 Rust builtin 签名把 positional 参数绑定到 `prefix_rule(pattern, decision, match, not_match, justification)`、`network_rule(host, protocol, decision, justification)`、`host_executable(name, paths)`，拒绝重复/过多/顺序错误参数；直接 policy 值必须是字符串字面量，未加引号的 pattern/decision/path/example 不再被误接收。`gofmt`、`go test ./internal/execpolicy -count=1 -v`、`go test ./internal/cli ./internal/app ./internal/execpolicy ./internal/applypatch -count=1` 已通过；变量/f-string/escape/诊断位置仍列为完整 Starlark 后续项。
- [x] 继续阶段 8 顺序补 session 命令 runtime config load。按 Rust `SessionTuiCli`/`SessionArchiveConfigOverrides` 传入交互运行时的语义，Go `resume/archive/delete/unarchive/fork` 现在运行期加载 effective config，root `-c` 先于 session `-c`，root/session `--strict-config` 合并生效；严格 config/override 错误会先于 picker/target/store placeholder 流程返回。`gofmt`、`go test ./internal/app -run "TestSession(RuntimeLoads|Archive|Mutations|Resume|Fork|Delete|Remote)" -count=1 -v`、`go test ./internal/cli -run "TestParse(Resume|Archive|Fork|TUIOnly|RejectRootStrict|AllowRootStrict)" -count=1 -v` 已通过；真实 TUI resume/fork handoff 仍列为后续项。
- [x] 继续阶段 8 顺序补 CLI exit 语义。按 Rust `exit_status.rs`/`login.rs`/`main.rs`/`debug_sandbox.rs` 复核后，Go 现在保留 `sandbox` 普通子进程与 Windows wrapper 非零 exit code，Unix signal 按 `128+signal` 映射；`login` credential-source 冲突、废弃 `--api-key`、stdin secret TTY/empty、forced-method 与 flow error 均按 Rust 文案返回 printable/silent exit；`logout` config error、interactive startup fatal (`ERROR: ...` + silent exit 1) 也已对齐。`gofmt`、多组 `go test ./internal/app -run ... -count=1 -v`、`go test ./cmd/codex ./internal/app ./internal/cli ./internal/sandbox -count=1`、`go test ./...` 与 `go list -buildvcs=false ./...` 已通过。
- [x] 继续阶段 8 顺序补 `execpolicy` Starlark 字符串与简单常量语义。按 Rust `PolicyParser`/Starlark 求值顺序，Go policy scanner 现在按 statement 顺序处理前置常量赋值，支持 string/list 常量在 `prefix_rule`、`host_executable`、`network_rule` 的参数、pattern 元素和 match/not_match 例子中引用；字符串字面量改为解析 `\"`/`\\` 等 escape，非法 escape 报错，行尾注释不混入赋值值。`gofmt`、`go test ./internal/execpolicy -count=1 -v`、`go test ./internal/cli ./internal/app -run "Test(ParseExecpolicyCheck|Execpolicy|RunExecpolicy|SandboxExecpolicy)" -count=1 -v` 已通过；表达式拼接/f-string/load/精确 Starlark 诊断仍列为后续项。
- [x] 继续阶段 8 顺序补 `execpolicy` Starlark 基础拼接表达式。Go policy evaluator 现在支持顶层 string/list `+` 拼接，变量解析后也会继续求值，覆盖 pattern、match/not_match、decision、justification 的静态拼接场景；补 `BASE + [STATUS]`、`"pro" + "mpt"` 等回归。`gofmt` 与 `go test ./internal/execpolicy -count=1 -v` 已通过；f-string/load/函数/推导式和精确 Starlark error span 继续保留后续项。
- [x] 继续阶段 8 顺序补 `execpolicy` Starlark f-string 基础插值。对照 Rust `PolicyParser` 启用 `enable_f_strings` 后，Go 字符串 parser 现在支持 `f"{NAME}"`、静态字符串表达式插值以及 `{{`/`}}` 字面大括号；非法/未闭合插值返回错误。`gofmt`、`go test ./internal/execpolicy -run "TestCheck(ParsesSimpleStarlarkFStrings|RejectsInvalidStarlarkFString)" -count=1 -v` 与 `go test ./internal/execpolicy -count=1 -v` 已通过；load/函数/推导式与精确 Starlark diagnostic 仍列为后续项。
- [x] 继续阶段 8 顺序补 `execpolicy check` CLI JSON fixture。按 Rust `cli/tests/execpolicy.rs`，Go app 层测试现在解析 stdout JSON 并精确比较 `decision/matchedRules/prefixRuleMatch`，覆盖有/无 `justification` 两条 fixture，避免只用字符串 contains 放过 shape 偏差。`gofmt` 与 `go test ./internal/app -run "TestExecpolicyCheckCommand" -count=1 -v` 已通过；CLI 错误顺序与完整 Starlark diagnostic 继续保留后续项。
- [x] 阶段 8 `execpolicy` 本轮收口测试：`go test ./internal/cli ./internal/app ./internal/execpolicy ./internal/applypatch -count=1`、`go list -buildvcs=false ./...` 与 `go test ./...` 已通过。
