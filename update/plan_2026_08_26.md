# Codex Go update alignment plan - 2026-08-26 (sync26 #40196-#40866)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` frozen target (sync25, static baseline): `4582c0a498158063760309c48214a0416a81488a` (#40196)
- Rust new HEAD (sync26 final): `bde9db1375` (#40906)
- Go baseline: `defd2e2` (`main`, sync25 landed)
- Go baseline state: `go build ./...` clean.

Rust `git pull origin main` advanced `4582c0a498..f5420174da` (about **190 upstream commits**) on top of the sync25 target.

Post-freeze drift: `git pull origin main` then advanced `f5420174da..bde9db1375`
by **4 commits** (#40884, #40892, #40901, #40906) after the initial static
freeze to `f5420174da`. The static layer was re-frozen to `bde9db1375` (see the
"Late re-freeze" subsection below).

## Late re-freeze (static layer -> bde9db1375 / #40906) - DONE

Refreshed the L0 static layer over the 4 late-arriving commits
(10d5a603ae #40884, 62fb56ee56 #40892, d4998d611a #40901, bde9db1375 #40906).
These are all Guardian V2 risk-score / client-endpoint changes:

- `parity/rust_snapshot_test.go`: re-pinned the critical SHA for
  `core/src/client.rs` (`2c4221bc...` -> `751ddeea...`), reflecting the
  endpoint-selection and free-Guardian routing changes in #40892/#40906.
- `appserver/schema/precomputed/app-server-exports-stable.json.zst`:
  re-vendored byte-identical to the regenerated Rust blob (commit d4998d611a
  refreshed the stable exports for `security_risk` protocol additions).
- `config/` and `features/` surface unchanged (the new `features.guardianv2.free_guardian`
  field is a nested config key, not a top-level config key; the Go config
  surface test only validates top-level keys, and it remains green).

Verification after re-freeze:
- `go build ./...` -> clean.
- `go test ./parity -count=1` -> pass (all snapshot/manifest/surface gates).
- `go test ./features -count=1` -> pass.

The runtime behavior introduced by these commits (routing Guardian review and
classification through dedicated `/guardian` and `/guardian-classifier`
endpoints, recording reviewed actions with security risk scores) is landed in
the sibling `update/plan_2026_08_26_sync27_guardian_endpoints.md` as the sync27
Guardian-endpoints runtime batch (committed `42cf85f`). The remainder of the 18
`batch-guardian-v2` items below remain open for later passes.

## Static freeze status (L0 gate) - DONE

Re-froze the static layer to `f5420174da`. The parity package now passes clean
(`go test ./parity -count=1`). Changes:

- `parity/rust_snapshot_test.go`: re-pinned critical-file SHA256 hashes
  (`Cargo.toml`, `exec/src/lib.rs`, `core/src/client.rs`,
  `app-server-protocol/src/protocol/common.rs`,
  `app-server/tests/suite/v2/mod.rs`, `core/tests/suite/mod.rs`) and added Rust
  workspace members `agent-roles`, `worktree`.
- `parity/rust_tui_snapshot_manifest_test.go`: TUI snapshot total 691 -> 715;
  updated per-directory counts (`app/tests/snapshots` 4->7, `bottom_pane` 197->200,
  `bottom_pane/textarea` 4->8, `chatwidget` 230->238, `history_cell` 54->55,
  `snapshots` 128->132) and added the new `tui/src/markdown_render/snapshots`
  directory.
- `parity/rust_fixture_manifest_test.go`: fixture root counts
  `app-server/tests/suite/v2` 115->118, `core/tests/suite` 164->171.
- `parity/rust_inventory_test.go`: top-level directory inventory adds
  `agent-roles`, `worktree` (106 vs 104).
- `parity/rust_unified_exec_sandbox_manifest_test.go`:
  `core/tests/suite/unified_exec_process_events.rs` test cases 7->8.
- `appserver/schema/precomputed/app-server-exports-{stable,experimental}.json.zst`:
  vendored byte-identical to the new Rust blobs.
- `features/features.go`: registered 8 new Rust feature keys:
  `transcript_v2`, `content_item_kinds`, `code_mode_prewarm`,
  `skip_host_skill_discovery`, `in_app_local_automation`, `bedrock_setup_wizard`,
  `step_model_switching`, `compaction_image_budget` (stages/defaults mirrored
  from `features/src/lib.rs`).

## Runtime batch roadmap (sync26)

The range is dominated by: turn/step-scoped settings, realtime thread timeline +
thread artifacts, conversation recaps, Guardian v2 review, MCP OAuth/identity,
Windows sandbox hardening, Vim/TUI features, Bedrock onboarding, and content-kind
enumeration. Each batch is tracked by `status: pending` until its runtime behavior
and verifier land.

### batch-settings-steps (turn/step-scoped settings)
- `68301fa45f` #40651 Snapshot resolved settings for each model step
- `4e883ce36e` #40645 Keep settings operations bound to their committed snapshot
- `1d64085e67` #40647 Validate step settings against proposed permissions
- `ba6cf9c692` #40648 Make automatic turn admission atomic with settings updates
- `a10c8127f7` #40653 Add live turn settings updates
- `9695e71519` #40656 Add turn-scoped settings updates to the app server
- `a26f1806a4` #40821 Use issuing step settings for approval decisions
- `04907ab95a` #40807 Use step settings for MCP request metadata
- `538900ee76` #40665 Add turn trigger metadata
- `ed42068c45` #40616 Add a turn-scoped service tier override
- `e24190caa9` #40771 Use turn environment settings for sandbox execution
- `8444cf63b5` #40551 Derive sandbox contexts from turn environments
- `ba9567daf3` #40660 Propagate temporary directories into sandbox policy contexts

### batch-realtime-timeline
- `41ca88e95c` #40508 Persist realtime events in the thread timeline
- `9b2ef38f54` #40509 Add persisted thread artifact models
- `76d98a771e` #40315 Add task mentions to the TUI composer
- `a8468330bb` #40308 Add TUI tools for managing Codex tasks
- `34c5303f49` #40625 Ignore closed agents when changing directories
- `d21794d6ba` #40477 Reload Multi-Agent V2 children through their parent
- `4fa6ad1730` #40585 Track remaining multi-agent v2 tools in analytics
- `b705b6b076` #40437 Report completed sub-agent activity on parent turns
- `2126f93617` #40449 Route peer agent completion activity to the initiating turn
- `b6333bb1bb` #40464 Enforce subagent ownership across app-server inputs

### batch-recaps
- `7c1e36c23f` #40696 Prepare the TUI for conversation recaps
- `40ba7da7b4` #40697 Prepare automatic TUI recap generation
- `6988d390b3` #40705 Generate automatic and manual conversation recaps
- `5a51caf04d` #40495 Suggest conversation-based thread titles in `/rename`
- `b3c7e1a47f` #40492 Generate descriptive TUI thread titles
- `0c992c305d` #40494 Hide ephemeral system threads from TUI routing

### batch-guardian-v2
- `039eb58a0b` #40848 Give Guardian reviewers read-only access to parent history
- `9dea1f709f` #40846 Default Guardian v2 to computer-use reviews with images
- `a9ed4f154a` #40844 Refine Guardian predictive risk classification
- `dc08ace782` #40742 Prepare isolated Guardian reviewer sessions
- `8476434a97` #40497 Harden internal Guardian session isolation
- `523519d974` #40431 Render Guardian review evidence with async scorer truncation
- `c2904b9c5a` #40465 Record Guardian classifier input truncation metrics
- `60821c5043` #40420 Represent Guardian policy as a contextual fragment
- `a9e7920da1` #40393 Classify Guardian V2 risk with a single token
- `77b30a21e1` #40403 Prewarm more Guardian sampling WebSockets
- `1f5c9ba183` #40597 Optimize Guardian transcript window retention
- `076f17c114` #40594 Preserve conversation context in Guardian transcripts
- `da4cf1cdea` #40641 Preserve Windows proxy settings in Guardian sessions
- `c9b19deb09` #40221 Distinguish Guardian review threads from subagents
- `4b24638e1c` #40678 Prewarm code-mode host connections at session startup
- `c395364915` #40622 Keep the active goal clock current in the TUI
- `9c9675d3d0` #40528 Represent terminal input in approval reviews
- `3a469a297d` #40533 Route extension hints into context-window metadata
- `10d5a603ae` #40884 Persist Guardian V2 risk scores without restoring them
- `62fb56ee56` #40892 Route Guardian inference through dedicated endpoints
- `d4998d611a` #40901 Record reviewed actions with security risk scores
- `bde9db1375` #40906 Record actual Responses endpoints in tracing spans

### batch-mcp-oauth-identity
- `9b4a0f8a0a` #40739 Add enterprise IdP identity resolution for MCP OAuth
- `f6805328c4` #40722 Add enterprise ID-JAG exchange for MCP OAuth
- `9be8d6e1c3` #40691 Harden MCP OAuth callback handling
- `4213b38f3c` #40728 Honor attachment-owned permissions for MCP servers
- `75cb7c903d` #40737 Preserve MCP tool output as content items
- `2161ec272a` #40257 Support `cua_repl` as a Node REPL-backed MCP server
- `f5853cbe90` #40634 Reconnect MCP servers when authentication mode changes
- `20c3f9733f` #40636 Restart pending MCP connections when startup timeout changes
- `40b7560169` #40363 Forward local env vars to Agent Plugin MCP servers
- `2df6705423` #40343 Cache executor metadata from initialization
- `339751715c` #40368 Use snapshot session sources for MCP projection

### batch-windows-sandbox
- `21c58c90f2` #40808 Harden Windows sandbox helper cleanup
- `7e1ee6df6c` #40672 Reuse private desktops across Windows sandbox commands
- `3a211471d2` #40475 Request read control when updating Windows ACLs
- `a12c4f4c01` #40441 Preserve Windows root reads with deny-read rules
- `0d9bb6c34c` #40423 Harden Windows file URI conversion
- `09609ba414` #40422 Harden Windows updater command execution
- `d52478c52e` #40570 Preserve Unicode paths in Windows batch aliases
- `3470b807b4` #40438 Use rust-lld for Bazel Windows MSVC Rust links
- `d7510aa4b4` #40613 Preserve pending input after terminal turn errors
- `bf3eb2ec91` #40460 Prevent Unix PTY I/O from blocking runtime shutdown

### batch-vim-tui
- `98ee29c7cd` #40785 Add Vim character find and till motions
- `c1db22a3cd` #40521 Add dot-repeat to Vim mode
- `25a6e316c8` #40775 Make history and notes tools Bridge-compatible
- `a95391160a` #40644 Drive TUI stream animation from the foreground loop
- `6525b95dae` #40618 Refresh animated terminal titles without redrawing the TUI
- `2764e83626` #40751 Preserve transcript overlay state across updates
- `d6174a8798` #40720 Preserve composer hyperlinks across wrapped lines
- `bb8cada846` #40471 Render Markdown links as clickable labels in supported terminals
- `a63cb33e8d` #40629 Show reconnect guidance when disconnecting from tasks
- `cbfd999db7` #40511 Add hooks for interrupted turns

### batch-bedrock
- `f1a6f7c9b3` #40679 Add Amazon Bedrock setup to TUI onboarding
- `4f6d43c267` #40481 Support managed AWS access keys for Amazon Bedrock
- `13fe2bcb7a` #40706 Redact Bedrock API keys from debug output
- `c3d5e4418f` #40466 Add credential brokering to network proxy feature config
- `0280d73c1e` #40484 Broker credential aliases in child environments
- `0533f96c44` #40301 Label Business Pro Lite plans as Business Premium

### batch-content-kind-enable
- `4aa52f2cb2` #40450 Gate content item kinds behind a feature flag
- `afb797cae6` #40669 Enable content item kinds by default
- `479c8c8924` #40281 Preserve content kinds during image preparation
- `e21bc763a7` #40273 Normalize compacted user message annotations
- `184ff338ad` #40266 Preserve content annotations when filtering forked agent history
- `0a80fab240` #40264 Preserve content metadata when truncating messages
- `a70974c1a0` #40297 Preserve developer instruction annotations in subagent forks
- `bd0d4a23e3` #40271 Preserve content annotations when rolling back model switches
- `aec653daa9` #40277 Preserve annotations when omitting unsupported media

### batch-other
- `3e4707b34b` #40799 Support persistent reasoning effort
- `5ca4175295` #40709 Rename host instruction payload to `Instructions`
- `0cdb1f1c83` #40628 Harden goal continuation and remove duplicate prompt helpers
- `32ce703a80` #40688 Bound SQLite integrity checks for diagnostic attachments
- `c51e7b3736` #40724 Add plugin-attributed skill telemetry
- `02c9f83f74` #40735 Use the model info accessor for skill telemetry
- `a7b86b6201` #40523 Retry provider auth commands after initial failures
- `0b94751cc4` #40726 Add telemetry for SQLite log persistence
- `e16d098c00` #40620 Instrument exec-server shell snapshot capture
- `5ce04789fb` #40640 Honor managed plugin disablement for executor capabilities
- `a6e63f9f32` #40683 Respect config layers when removing plugin marketplaces
- `304c8de4c6` #40631 Allow extensions to skip host skill discovery
- `46aa019e80` #40604 Count serialized JSON bytes without allocating strings
- `e44bd9b64d` #40637 Retry timed-out cloud config loads in the background
- `f832b2fe7b` #40624 Add worktree settings parser
- `e3609f2d02` #40382 Expose invocation sources to extension tools

## Verification (this sync)

- `go build ./...` clean.
- `go test ./parity -count=1` pass (all static-freeze/surface gates green at
  `f5420174da`).
- `go test ./features -count=1` pass.
- Remaining: dynamic L1-L3 differential (record-replay, SDK) for the runtime
  batches above, driven as each batch lands.

## Prior status carry-over (from sync25 plan)

sync25 logged these as pending; they remain open and are folded into the above
batches where relevant:
- batch-suspend (`4f39251a01` #40038 unfinished root turn suspension) - app-server
  `thread/suspendTurn` + `SuspendTurnOutcome` wire enum still pending.
- batch-thread-tree (`a1d4aea265` #40179 archive descendant shutdown ordering) - pending.
- batch-tui cursor order (`1e6185e522` #40166 move cursor before showing) - pending.
- batch-content-kind user-input tagging + merged `build_rendered_message` - pending.
- batch-thread-source (`a73485dc76` #40161 exec `--thread-source`) - implemented.

## Status summary

Static layer re-frozen to `f5420174da` (L0 gate green). The runtime batch roadmap
above is the work queue; each batch needs its own differential verifier
(record-replay / SDK) before `parity.json` certification can advance.