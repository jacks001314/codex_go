# Codex Go update alignment plan - 2026-08-07

## Baseline

- Rust upstream checkout: `D:\\qax\\reagent\\dev\\git\\codex`
- Previous aligned Rust commit: `aac9f842473ac6a05d417dd76ce8b89bdb3b707d`
- Audited Rust commit: `c5d9431971`
- Range: 53 commits (`aac9f84247..c5d9431971`), including the latest plugin-loader commit after the prior continuation
- Go baseline: `017b826c6270c8aa03452ddc0a4e3bbc5fe0c295`
- The Rust checkout was successfully fast-forwarded from `a9d59d8b8e` to `c5d9431971` with `git pull --ff-only origin main` on 2026-08-07.
- The Go worktree already contained uncommitted self-update, TUI, branding, response-schema, and test changes. This run preserves and builds around those changes.

## Upstream audit

### Implement in this update

1. `4d7e3e9`: clamp a too-short `wait_agent` timeout to the configured minimum and disclose the adjustment in the result message.
2. `270d932`: send `x-codex-routing-hint` for first-party Codex-backend requests, including the model and optional service tier.
3. `912524d`: expose a minimal `/dev` in the full-filesystem bubblewrap path.
4. `81b9bc2`: recognize `mcp_tool` hook configuration and report an explicit unsupported warning instead of treating it as an unknown handler.
5. `d0c8f42`: make code-mode's omitted `exec.yield_time_ms` default to 30 seconds and support `features.code_mode.default_exec_yield_time_ms`.
6. `4ee4192` / `c87a218`: accept `features.tool_registry.include_tool_metadata` using the final upstream name.
7. `8425476`: derive connector install URLs from the configured ChatGPT origin.
8. `b94343a`: honor the agent-identity auth endpoint override used by upstream test and deployment environments.

### Already equivalent or covered by the Go architecture

- `547080e`: local session records already persist and restore their CWD.
- `70b4653`: archive status and archive events are already represented in the Go session/app-server paths.
- `82b17bc`, `69b6152`: full-history forks already inherit role/model context through Go's agent fork options.
- `57f42a8`: Go session reads do not clone Rust rollout metadata.
- `1efd8fc`, `0bdce9f`: the Go resume picker already lists archived sessions and supports restore.
- `9457f8f`: Go session APIs already accept caller-selected thread IDs.
- `5bdc889`: Go has product-specific TUI placeholders; literal Rust wording is intentionally not copied.

### Track as separate follow-up work

- Durable queued user-message dispatch (`bc8b25e`) and shared MCP handler reuse/recovery/settling (`74b8f8d`, `1151b23`, `e1831db`, `b3ffe3d`, `4ffeddc`, `9daa491`) require lifecycle changes across the agent and app-server runtimes.
- Unified image budgets and visualization content references (`0a0ebb8`, `29dce2d`) require protocol and rendering work.
- Remote plugin scope loading and plugin-root discovery (`1ae82ce`, `bfb6a6e`) depend on plugin distribution APIs not present in Go.
- Managed proxy MITM authorization (`7a0e974`) requires a security review of Go's proxy and sandbox boundary.
- App-server environment provisioning, account telemetry reload, routing environments, and rollout migration (`d9eac10`, `f8ac8fa`, `7257826`, `4bb7ee3`) are larger app-server/state changes.
- Inline viewport repaint and Markdown conversation export (`303a292`, `2801d12`) need dedicated TUI interaction tests.
- `4bb7ee3` rollout migration and the Rust CLI `migrate-rollouts` command remain separate follow-up work; the Go CLI surface test currently reports the missing `MigrateRollouts` command.
- The remaining commits are Rust build/test infrastructure, internal endpoint wiring, retry hardening, or first-turn rollback behavior without a direct Go surface.

## Existing self-update work

The pre-existing worktree changes remain part of today's update candidate:

1. Detect npm, bun, and pnpm ownership in the npm shim.
2. Restrict automatic updates to `@jacks001314/codex-go`.
3. Use cached update information on interactive startup and refresh stale cache data in the background.
4. Persist per-version dismissal and run the selected package-manager update after the prompt exits.

## Verification

- Focused unit tests for each synchronized protocol/config behavior.
- Existing self-update tests for `install`, `app`, `tui`, and `doctor`.
- `gofmt` on all touched Go files and JavaScript syntax validation for npm shims.
- Broader package tests where the dirty worktree and platform allow them.
- Linux-only bubblewrap behavior receives a build/static test on Windows and should also run in Linux CI.

## Completion record

- Implemented all eight items in the first synchronization batch: short-wait clamping, model routing hints, bubblewrap `/dev`, `mcp_tool` hook recognition, configurable code-mode exec yield, final tool-registry metadata config naming, configured connector origins, and the agent-identity auth API endpoint override.
- Aligned the feature registry surface by retaining `code_mode_buffered_exec` at the removed stage and adding the default-disabled `background_paginated_rollout_migration` and `unified_image_budget` keys; their larger runtime implementations remain in the follow-up groups above.
- Added the upstream `otel` app-server v2 suite module to the local parity manifest. Reload behavior itself remains in the larger app-server telemetry follow-up group.
- Kept the JWKS half of the agent-identity endpoint override out of production code because Go currently has no production JWKS verification caller; the active auth API path is covered.
- `go test ./config ./tool ./turn ./exec ./auth ./apps ./agent ./model ./codexapi` passed.
- Focused app-server tests for the Rust suite manifest and unsupported `mcp_tool` hooks passed.
- `go test ./sandbox/...` passed, including the `linuxsandbox` package.
- Existing self-update work passed `go test ./install ./tui ./app ./doctor`; both modified npm scripts passed `node --check`.
- `go vet ./agent ./apps ./appserver ./auth ./codexapi ./config ./exec ./features ./model ./parity ./sandbox/... ./tool ./turn` and `git diff --check` passed.
- The live Rust feature-key parity check passed against `c87a218bd3`. The complete `parity` package still reports the expected frozen-baseline drift for manifests, fixtures, generated exports, workspace membership, and TUI snapshots.
- The complete `appserver` package still has three stable rollout/history failures: `TestRouterResumeFromPath`, `TestRouterForkFromPath`, and `TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread`. They reproduce in isolation and are outside the files and execution paths changed by this synchronization batch.
- `parity.json` remains at `aac9f84247`: the range is audited through `c87a218bd3`, but the explicitly listed follow-up work prevents claiming full baseline parity.

## This continuation

- Implemented `80858a8` (`codex exec fork`): CLI parsing for IDs/names, promptless fork event behavior, image/output/ephemeral validation, copied history, persistent and ephemeral fork handling, and source immutability tests.
- Implemented `6622546` cold resume approval restoration: rollout persistence for `thread_settings_applied`, the Rust-compatible latest-policy selector including stale same-turn context handling, explicit resume override precedence, runtime config propagation, and cold-router regression tests.
- Verification: `go test ./exec ./rollout` passed; focused `appserver` approval/resume tests passed; `go vet ./cli ./exec ./rollout ./appserver` passed; `git diff --check` passed.
- The broader `go test ./cli ./exec ./rollout ./appserver` run passed for `exec` and `rollout`. It still reports the pre-existing three appserver rollout/history failures, and the CLI source-surface test reports Rust's newer `MigrateRollouts` subcommand, which is tracked above.

## Rust commits audited in this continuation

- `51e36d2`: implemented `model/list` exposure of normalized `multiAgentVersion` (`disabled`, `v1`, `v2`) and added wire/API coverage.
- `85e0661`: aligned the project-document setting description and aggregate byte budget semantics. Go now exposes the 32 KiB default/configured `project_doc_max_bytes` for local project instruction loading; selected remote executor filesystems still require the executor-local filesystem/config work tracked below.
- `964a227`: implemented durable base-instruction provenance. Go rollout/session metadata now reads and writes Rust's nested `{text, provenance}` form, persists custom/model provenance through thread creation, fork and recorder paths, infers model provenance for compatible legacy rollouts, and avoids restoring model-generated text on resume so model/personality changes recompute instructions.
- `a9d59d`: audited and tracked. Rust's plugin tool-suggestion change depends on the shared skill-root loader introduced by `a4b129e`/`e75a188`; Go has no equivalent executor-scoped loader in the current plugin suggestion path.
- `c5d9431`: implemented the Go-visible part of unified plugin skill discovery. Agent Plugins now report `hasSkills` and `plugin/read.skills` only for valid direct-child skills; nested-only Agent Plugins no longer produce a false plugin-level skill. Legacy recursive discovery and fallback behavior remain unchanged.
- `957f8eed`, `95c7265`, `a7dcd20`, `a4b129e`, `e75a188`, `e58d9ef`: audited and tracked as executor-local config/diagnostics/skills service work; no isolated Go surface was changed in this continuation.

## Continuation verification

- Passed: `go test ./model ./rollout ./session ./exec ./app`.
- Passed: focused `go test ./config ./appserver` for project-doc budget, instruction provenance, and model-list changes.
- Passed: `go vet ./model ./rollout ./session ./exec ./app ./appserver` and `git diff --check`.
- The complete `go test ./appserver` run still reports only the three previously documented rollout/history failures: `TestRouterResumeFromPath`, `TestRouterForkFromPath`, and `TestRouterInjectItemsAndRollbackRepairRolloutOnlyThread`.
- Latest plugin verification: `go test ./plugin`, plugin-focused `go test ./appserver -run 'Plugin|plugin|Skills|Skill'`, `go vet ./plugin ./appserver`, and `git diff --check` passed.
