# Codex Go update alignment plan - 2026-08-16

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` after `git pull origin main` (system proxy
  `127.0.0.1:7897` required):
  `9ded177ce7c1c0bd2047f902936c177612ab3434`
- Previous audited target: `85fc4def358b7df21883e72ae8dda43a0f572f32`
- Go baseline: `df0a351` (`main`, pushed)
- Audited upstream range: `85fc4def35..9ded177ce7` = 17 commits
- Method: commit-by-commit file/diff audit, mapped to Go domains and packages.

## Committed in this batch

1. #38767 Forward workload identity context during token exchange
   - `auth` reads `OPENAI_WORKLOAD_IDENTITY_CONTEXT`, forwards it unchanged as
     the `workload_identity_context` exchange field, and includes it in the
     workload identity session fingerprint so sessions with different values
     cannot share an exchange; `envutil` scrubs the variable from
     model-reachable child environments (Rust `NON_INHERITABLE_ENV_VARS`).
2. #38795 Add storage diagnostics to `codex doctor`
   - New `system.disk` check reports available space for `CODEX_HOME` and the
     active worktree (warning below 5 GiB, failure below 1 GiB, measured via
     `unix.Statfs` / `windows.GetDiskFreeSpaceEx`). New `git.worktree.dev_drive`
     check (Windows) reports whether the active Git worktree is on a trusted
     Dev Drive (IOCTL persistent-volume-state flags) with remediation.
     Feedback doctor-report workspace anchoring is N/A: Go has no feedback
     doctor attachment.
3. #38800 Route executor policy audits through log-only telemetry
   - Forwarded `codex.network_proxy.policy_decision` events carry
     `target=codex_otel.log_only`; the state `LogDBHandler` excludes that
     target from the persistent log while standard logging still receives it.
4. #38827 Add endpoint protection checks to `codex doctor`
   - New `security.endpoint` check detects CrowdStrike Falcon, BeyondTrust
     Privilege Management, Microsoft Defender and SentinelOne (Windows
     `sc.exe query`) plus Jamf Protect (macOS `systemextensionsctl list`),
     with product-specific exclusion remediation and
     complete/partial/unavailable inspection states.
5. #38840 Identify Mac mini hosts in remote control handshakes
   - Remote-control WebSocket handshakes send `x-codex-host-device-kind:
     mac_mini` on macOS when `system_profiler` reports a machine name of
     exactly `Mac mini` (2 s timeout, cached successful detection, header
     omitted on other platforms/failures).

## Full upstream audit: 17 commits

| # | Rust commit | Subject | Disposition |
|---|---|---|---|
| 1 | `3c7ae4a812` | #38743 Scope TUI app directory state to the active context | Implement |
| 2 | `a7edf37cb4` | #38746 Remove the TUI test dependency on `codex-cli` | N/A: Rust test-infra dependency removal; Go TUI tests have no such dependency |
| 3 | `12933b6955` | #38767 Forward workload identity context during token exchange | Implement |
| 4 | `c4941302c7` | #38774 Use paginated history for persistent exec threads | Implement |
| 5 | `00f6a8a60e` | #38785 Keep active-turn model settings stable across updates | Equivalent: Go snapshots model/provider/reasoning/service-tier/approval into `appTurnRunConfig` once per turn |
| 6 | `a95a6fe333` | #38788 Show resume and fork status during TUI startup | Equivalent |
| 7 | `6efcdad4c3` | #38795 Add storage diagnostics to `codex doctor` | Implement |
| 8 | `899d1715c8` | #38800 Route executor policy audits through log-only telemetry | Implement |
| 9 | `b3cc217378` | #38806 Add a health endpoint to the code-mode gRPC listener | N/A: Go provides no code-mode gRPC server (client only) |
| 10 | `5ba12929f8` | #38817 Add raw config overrides to the TypeScript SDK | N/A: Go repo has no TypeScript SDK mirror |
| 11 | `fcdae21073` | #38819 Support metadata staging for reserved thread IDs | N/A: Go appserver has no ThreadManager reserve API |
| 12 | `1ba9ce8910` | #38822 Avoid cloning TUI history span content | N/A: Rust allocation micro-optimization |
| 13 | `49db349ffd` | #38823 Avoid allocating per character when decorating hyperlinks | N/A: Rust allocation micro-optimization |
| 14 | `c7a95f84b3` | #38827 Add endpoint protection checks to `codex doctor` | Implement |
| 15 | `73abda8bfe` | #38830 Isolate external editor buffers from sandbox-writable paths | Implement |
| 16 | `9bfaf7a076` | #38837 Share editor keymaps across TUI composer components | N/A: Rust Arc allocation sharing; Go observable behavior identical |
| 17 | `9ded177ce7` | #38840 Identify Mac mini hosts in remote control handshakes | Implement |

## Verification

```powershell
gofmt -l (touched Go files)
go build ./...
go vet ./...
go test ./auth ./envutil ./remotecontrol ./execserver ./state ./exec ./doctor ./parity ./features -count=1
go test ./... -count=1
git diff --check
```

Parity re-baseline to `9ded177ce7` after the sync commits:

- Updated `parity/baseline.json`, `parity.json`, `parity/commits.json`,
  `parity/domains.json`, and `parity/contracts/manifest.json` to the current
  target and generated the 17-commit audit ledger.
- Refreshed the Rust critical-file hash snapshot (`Cargo.toml`,
  `exec/src/lib.rs`) and the TUI snapshot counts (663 total;
  `chatwidget/snapshots` 223, `tui/src/snapshots` 120) for the current
  `9ded177ce7` checkout.
- Updated the root README target pointer.

## Follow-up batch (same window) — all four deferred commits closed

1. #38774 Use paginated history for persistent exec threads
   - Fresh persistent `codex exec` threads now persist `history_mode:
     paginated`. The paginated encoder maps non-command tool calls/outputs to
     canonical `DynamicToolCall` thread items (Rust's protocol `TurnItem` has
     no plain `function_call` variants) and command tools to `CommandExecution`
     via JSON-argument command extraction; exec session-item building carries
     arguments/input in item data so every exec item is paginated-compatible.
   - Verification: `go test ./exec ./rollout ./session ./appserver -count=1`.
2. #38743 Scope TUI app directory state to the active context
   - The tea apps directory flow tags fetches with a scope generation,
     discards stale results after account/workspace/thread changes, and
     dismisses an open apps view on invalidation (resume/fork/agent
     switch/delete). `AppListUpdated` revalidation is N/A: Go fetches fresh on
     every apps open, so there is no cached directory content to revalidate.
   - Verification: `go test ./tui/tea -run
     'TestApplyAppListResultIgnoresStaleScopeLikeRust|TestInvalidateAppsScopeDismissesOpenAppsView'
     -count=1`.
3. #38788 Show resume and fork status during TUI startup
   - Equivalent: Go's session-picker modal is the startup surface (entries
     labeled with the Resume/Fork action) and session actions resolve
     synchronously with outcome notices; Rust's `StartupDraft` loading line
     is a cosmetic status rendered during an async startup window Go does not
     have.
4. #38830 Isolate external editor buffers from sandbox-writable paths
   - `EditorDirectory` resolves a protected `<home>/editor` directory
     (configured home, default home, workspace fallback), rejecting
     candidates exposed as writable by the effective filesystem policy or
     resolving through symlinks; the tea external editor creates buffer files
     there and surfaces an editor error when no protected directory is
     available; the interactive app wires the effective permission profile's
     sandbox policy.
   - Verification: `go test ./tui/tea -run TestEditorDirectory -count=1`.

All 17 commits in the `85fc4def35..9ded177ce7` window are now `complete` in
`parity/commits.json`.

## djalign cycle verification (L0/L1 green, no upstream drift) and new L2
facility

- Baseline re-check: Go `41929a5`, Rust `origin/main`
  `9ded177ce7c1c0bd2047f902936c177612ab3434` (fetched, no new commits), frozen
  target in `parity/baseline.json` matches.
- L0 static gate green: `go test ./parity ./features -count=1` (baseline,
  ledger, domains, contracts, CLI/exec surfaces, critical-file hashes, schema
  exports, feature keys, TUI snapshot manifest, unified-exec manifest, state
  migrations).
- L1 unit contracts green: `go test ./applypatch ./tool ./rollout ./session
  ./state ./appserver ./turn ./model ./config -count=1` plus the full-tree
  `go test ./... -count=1` and `go vet ./...` runs completed without failure.
- Upstream drift: none (`git fetch origin main` == frozen baseline), so the
  drift-driven dynamic selection is empty this cycle.
- New L2 record-replay facility built instead of being skipped: `recordreplay/`
  parses `tui/tests/fixtures/oss-story.jsonl` (8041 events) into a normalized
  contract view, freezes the structural digest as
  `recordreplay/testdata/oss-story-digest.json` (recording drift → golden
  break), and replays the recorded task lifecycle through Go's paginated
  `rollout.Recorder`, cross-checking persisted event sequence, item states and
  recoverability. Registered as contract `story-recording-replay` in
  `parity/contracts/manifest.json` and recorded in `parity.json`.
- Verification: `go test ./recordreplay ./parity -count=1`.

## Shared-fixture double-run over the Rust apply-patch corpus (new contract)

- New parity verifier `TestApplyPatchScenariosMatchRustFixtures` runs the 25
  committed Rust apply-patch scenarios (input/ + patch.txt + expected/) through
  Go's apply_patch CLI and byte-compares the resulting file trees with the
  Rust expected trees (mirroring Rust's own runner, which asserts final state
  only). Registered as contract `apply-patch-scenarios` in
  `parity/contracts/manifest.json`.
- The double-run surfaced four real divergences, now fixed:
  1. Parser whitespace leniency around patch markers (scenarios 017/018/020):
     Go now mirrors Rust's marker matching — full trim for begin/end/hunk
     headers outside update hunks, trailing-trim inside update hunks, and
     `StreamingPatchParser::finish`'s full trim of the final line; content
     lines keep their raw bytes.
  2. CLI partial success (scenario 015): `RunCLI` now applies hunks
     sequentially and leaves earlier changes on disk on failure, like Rust's
     `apply_patch_with_mode`; the app-server tool keeps its verify-first flow,
     matching Rust's `verify_apply_patch_args` (unit test
     `TestApplyVerifiesBeforeApplyingLikeRustTool` pins the split).
- Unit pins: `TestParseToleratesWhitespacePaddedMarkersLikeRust`,
  `TestRunCLILeavesPartialSuccessOnFailureLikeRust`.
- Verification: `go test ./parity -run TestApplyPatchScenariosMatchRustFixtures
  -count=1; go test ./applypatch ./tool ./appserver -count=1`.

## Tool input-schema policy double-run (new jsonschema package + MCP wiring)

- New `jsonschema/` package mirrors Rust `tools/src/json_schema.rs`
  (`parse_tool_input_schema`): sanitize (boolean-form coercion, `const`→`enum`,
  type inference, object/array child defaults), transitive pruning of
  unreachable `$defs`/`definitions` (URL-decoded local refs, both table
  spellings), dropping every field outside the Responses JsonSchema subset
  (`pattern`, `format`, `title`, `minimum`, ...), and oversized-schema
  compaction (5000-byte budget, depth 3, description strip → definition drop
  with local-ref rewrite to `{}` → deep-object collapse → composition prune).
- New parity verifier `TestToolJSONSchemaPolicyFixturesMatchRust` +
  `TestToolJSONSchemaPolicyOversizedCompaction` run the six committed Rust
  fixtures (`tools/tests/fixtures/json_schema_policy/`) through Go and match
  every expected_preserved / expected_pruned / expected_dropped_fields pointer
  assertion plus the oversized compaction byte/size and ref-rewrite
  assertions. Registered as contract `tool-schema-policy`.
- Wired into `mcp.ToolExecutor.Spec()`: the model-visible parameters of MCP
  tools now go through the policy, mirroring Rust's
  `mcp_tool_to_responses_api_tool`; the raw schema stays on the executor for
  calls. `go test ./mcp ./turn ./appserver ./model ./codemode ./agent ./tool`
  all pass.
- Verification: `go test ./jsonschema ./parity ./mcp ./turn ./appserver
  ./model ./codemode ./agent ./tool -count=1`.

## Imported-tool surfaces closed: dynamic tools + Agent Plugin cap

- Dynamic tools now normalize their input schemas through the jsonschema
  policy in `dynamicToolInputSchema` (turn/dynamic_tool_runtime.go), mirroring
  Rust `parse_dynamic_tool` (which calls `parse_tool_input_schema`).
- Agent Plugin v1 MCP tools get Rust's `MAX_SERIALIZED_MCP_TOOL_BYTES` (8000)
  fallback: when the normalized schema still exceeds the cap, the parameters
  degrade to `{"type":"object","additionalProperties":true}`, mirroring
  `agent_plugin_mcp_tool_to_responses_api_tool`. The `AgentPlugin` flag is
  threaded from `MCPServerStatus.PluginID` (Go's existing agent-plugin proxy)
  through `RuntimeToolInfo` / `ToolExecutorOptions` / `ToolExecutor.Spec`.
- Unit pins: `TestDynamicToolInputSchemaNormalizesLikeRust`,
  `TestAgentPluginOversizedSchemaDegradesToAcceptAnything`.
- Verification: `go test ./mcp ./turn ./plugin ./appserver ./agent ./model
  ./tool ./codemode -count=1`.

## Config surface verifier against core/config.schema.json

- New parity verifier `TestRustConfigSchemaSurfaceAgainstGo` diffs Go's
  recognized top-level config keys (`config.KnownTopLevelConfigFields()`, now
  exported) against the 94 properties of Rust `core/config.schema.json`.
- The diff surfaced 13 genuinely-supported keys missing from
  `knownTopLevelConfigFields` (auto_review, check_for_update_on_startup,
  default_permissions, goals, memories, orchestrator, permissions,
  plan_mode_reasoning_effort, plugins, projects, responses_api_metadata,
  skills, tool_suggest) — Go already read them but strict config validation
  would reject them; they are now recognized.
- 16 Rust-only unsupported properties and 6 Go extensions/legacy keys are
  explicitly allowlisted with auditable reasons; the verifier fails if the
  allowlists go stale in either direction.
- Registered as contract `config-schema-surface`.
- Verification: `go test ./parity -run TestRustConfigSchemaSurfaceAgainstGo
  -count=1; go test ./config -count=1`.

## Method-5 differential harness against the Rust apply_patch oracle

- Built the Rust `apply_patch` release binary from the frozen `9ded177ce7`
  checkout (`cargo build -p codex-apply-patch --release`,
  `target/release/apply_patch.exe`) as the oracle.
- New parity test `TestApplyPatchDifferentialAgainstRustOracle` generates a
  deterministic seeded corpus (default 200 cases, env-tunable count/seed) of
  random workspaces and patches (add/update/delete/move + deliberately invalid
  constructs), applies each with the Rust oracle and Go's `apply_patch` CLI,
  and requires the same exit status and byte-identical final file trees. It
  skips when the oracle binary is absent. 3000+ cases across two seeds pass.
- The harness immediately found a real divergence: Go accepted `*** Update
  File` + `*** Move to:` hunks with zero change lines (pure moves), while Rust
  rejects them (the spec requires `change+`; oracle reports "Update file hunk
  for path 'X' is empty"). `parseUpdate` now requires at least one chunk even
  for moves, and `TestApplyPureMovePreservesContent` (which encoded the
  divergent behavior) was replaced by
  `TestUpdateMoveRequiresChangeLinesLikeRust`.
- Registered as contract `apply-patch-differential-oracle`.
- Verification: `go test ./parity -run TestApplyPatchDifferentialAgainstRustOracle
  -count=1; go test ./applypatch -count=1; go test ./... -count=1`.

## Differential corpus extension + backend task-details fixtures

- Extended the seeded differential corpus to unicode content lines, CRLF
  workspaces, paths with spaces, add-overwrite of existing files, duplicate
  targets, `*** Environment ID:` headers, `*** End of File` markers, context
  lines and multiple `@@` chunks per update. 8000+ cases across four seeds
  match the Rust oracle; no new divergences found.
- New shared-fixture double-run `TestBackendTaskDetailsFixturesMatchRust`
  reads `backend-client/tests/fixtures/task_details_with_{diff,error}.json`
  through Go's `chatgptapi.CodeTaskDetailsResponse` and asserts the same
  diff-preference / fallback / messages / prompt / error behaviors as the Rust
  `types.rs` tests. Registered as contract `backend-task-details-fixtures`.
- Verification: `go test ./parity ./applypatch ./chatgptapi -count=1; go test
  ./... -count=1`.

## Rollout writer property test (method 5) + djalign skill versioned

- New `rollout/property_test.go` `TestRecorderRandomSequencesPreserveRustStructure`
  writes random turn/item operation sequences through the paginated Recorder
  and verifies Rust-compatible structure: contiguous ordinals, session_meta
  first line, canonical `event_msg` payload types
  (`task_started`/`item_started`/`item_completed`/`task_complete`), exact item
  byte round-trip, item lifecycle pairing and appendability after Resume.
  Pinned at 4 seeds x 30 sequences in CI; 450 high-intensity sequences also
  verified. The Rust rollout oracle path was rejected: it would require
  modifying the upstream checkout plus heavy state/protocol dependencies, so
  the Go-side property test enforces the pinned Rust layout contract instead.
- The `.agents/skills/djalign/` skill (the methodology driving this goal) is
  now versioned under `.agents/skills/` like its sibling skills.
- Verification: `go test ./rollout ./recordreplay ./parity -count=1; go test
  ./... -count=1`.

## CLI summary ordering aligned + verifier-existence audit

- The differential harness surfaced a CLI stdout divergence: Rust
  `print_summary` groups all added, then modified, then deleted, each in hunk
  application order; Go's `Summary()` path-sorted within each kind. Fixed:
  `Summary()` now mirrors Rust's grouping/order (verified against the oracle),
  and the differential harness compares stdout byte-exact on both success and
  failure paths (both implementations print nothing to stdout on failure).
  3500+ additional seeded cases pass with stdout comparison enabled.
- New `TestContractVerifiersExistInGoTestFiles` audits that every contract
  verifier name in `parity/contracts/manifest.json` maps to a real test
  function somewhere in the Go tree, so renamed/deleted verifiers cannot
  silently detach contracts from their evidence. goPaths remain the Go
  implementation paths (checked separately by
  `TestAlignmentContractInventoryIsTraceable`).
- Verification: `go test ./parity ./applypatch ./appserver ./tool -count=1;
  go test ./... -count=1`.

## L0 enum inventory: sandbox types + event names

- `TestRustProcessSandboxTypeSurfaceAgainstGo` extracts the exec-server
  `ProcessSandboxType` enum (serde camelCase) from protocol.rs and pins Go's
  `execserver` wire values: none / macosSeatbelt / linuxSeccomp /
  windowsRestrictedToken - exact match.
- `TestRustEventMsgWireNamesCoverRecordedSurface` extracts the app-server
  `EventMsg` wire names (serde snake_case + explicit renames) from protocol.rs
  and pins every codex_event msg.type in the oss-story recording to a current
  wire name or a documented historical rename (`agent_message_delta` ->
  `agent_message_content_delta`, `agent_reasoning_raw_content_delta` ->
  `reasoning_raw_content_delta`); 5/7 recorded names are current wire names.
  Future protocol renames now break the check instead of silently drifting.
- Verifiers attached to contracts `exec-server-protocol` and
  `story-recording-replay`.
- Verification: `go test ./parity -run 'TestRustProcessSandboxTypeSurfaceAgainstGo|TestRustEventMsgWireNamesCoverRecordedSurface' -count=1; go test ./... -count=1`.

## L0 enum inventory: error codes

- `TestRustErrorCodeSurfaceAgainstGo` pins every wire value of the app-server
  v2 `CodexErrorInfo` (17 values) and `ConfigWriteErrorCode` (7 values) enums
  to Go production code: CodexErrorInfo values are emitted by the appserver
  (10) or tracked, and ConfigWriteErrorCode values are constants in
  `config/api.go` — `configRequirementReadonly` was added to complete the
  7/7 surface.
- Two emission gaps are documented with auditable reasons and monitored by the
  verifier: `sessionBudgetExceeded` (Go rollout-budget exhaustion does not yet
  surface the codexErrorInfo) and `threadRollbackFailed` (Go thread/rollback
  failures return generic RPC errors). The verifier fails if Rust adds codes
  or Go starts emitting an allowlisted value without removing the entry.
- Verification: `go test ./parity -run TestRustErrorCodeSurfaceAgainstGo
  -count=1; go test ./config -count=1; go test ./... -count=1`.

## L0 enum inventory: model metadata

- `TestRustModelInfoFieldSurfaceAgainstGo` pins the wire fields of Rust's
  `openai_models::ModelInfo` (41 fields; the serde-skipped internal
  `used_fallback_model_metadata` is excluded) against Go's `ModelInfo` JSON
  surface (38 fields). Six Rust-only fields are allowlisted with auditable
  reasons (`availability_nux` lives on Go's `ModelSummary`;
  `supports_reasoning_summary_parameter` is Go's `supports_reasoning_summaries`
  naming variant; `apply_patch_tool_type` / `comp_hash` /
  `experimental_supported_tools` / `shell_type` not yet carried), and three
  Go-only fields are documented extensions.
- With this, all four named L0 enum-inventory items are pinned: sandbox types,
  event names, error codes, and model metadata.
- Verification: `go test ./parity -run TestRustModelInfoFieldSurfaceAgainstGo
  -count=1; go test ./model -count=1; go test ./... -count=1`.

## L3 sdktests facility audit + opt-in rollout-budget scenario

- Audited the L3 facility without burning tokens: sdktests has 88 scenarios
  (apply-patch failure modes, resume/recovery, MCP, code-mode, approval,
  sandbox, compaction, persistence), a documented runner producing
  run-manifest.json, raw audit scripts (fork / rolled-fork / session-compat /
  requirements / goal), and healthy unit tests (50/51 pass, free).
- Added opt-in scenario `rollout-budget-exhaustion`: enables
  `features.rollout_budget` with `limit_tokens = 1` and expects the turn to
  fail once the shared session budget is exhausted (`errorPattern: "budget"`
  matches Rust's "shared rollout token budget exhausted" and Go's "session
  budget exceeded"). Pending live-SDK validation during the next authorized L3
  run.
- `prompt.md` (Rust BASE_INSTRUCTIONS) vs Go's BaseInstructions audited: Go
  carries its own complete prompt ("You are gcode..."); model wording is
  explicitly out of the djalign contract (no byte comparison).
- Verification: `npm --prefix sdktests run test:unit`.

## sdktests normalization rules documented + generator no-drift verified

- New `sdktests/NORMALIZATION.md` documents every SDK event normalization rule
  with reason, example and field paths (UUID/identity mapped placeholders,
  RFC-3339 timestamps, session-temp paths, volatile numeric fields including
  exitCode at the raw layer, usage token counts), plus the contract fields
  asserted outside raw normalization (command exit codes/statuses, item-type
  sequences, workspace side effects, agent message contracts) and the
  event-sequence / command-output comparison modes. README links the doc.
- Verified the app-server precomputed-exports generator already satisfies the
  static-layer regeneration-no-drift requirement: `GenerateTypeScript` /
  `GenerateJSONSchema` output is byte-compared against the frozen artifacts
  (`TestPrecomputedExportsAreWrittenToDiskLikeRust`) and the artifacts are
  pinned to the target Rust commit hash
  (`TestPrecomputedExportArtifactsMatchTargetRustCommit`).
- Verification: `npm --prefix sdktests run test:unit; go test ./... -count=1`.

## show_raw_agent_reasoning config surface + raw reasoning gating

- Audit found a config-surface and behavioral gap: Rust's `show_raw_agent_reasoning`
  (default false) was missing from Go — strict config rejected it, and Go
  emitted raw chain-of-thought reasoning deltas
  (`item/reasoning_text.delta`) unconditionally while Rust suppresses them by
  default.
- Fix: the key is now recognized, `Config.ShowRawAgentReasoning()` reads it
  (default false), and the turn stream handler gates raw reasoning deltas
  behind it. The config-schema rust-only allowlist shrank from 16 to 15
  entries.
- Tests: `TestShowRawAgentReasoningAccessor` (defaults), the existing
  `TestRuntimeRouterResponsesStreamingEmitsDeltaNotifications` now enables the
  key, and `TestRuntimeRouterRawReasoningDeltaGatedByShowRawAgentReasoning`
  pins suppressed-by-default / enabled-emits semantics.
- Verification: `go test ./config ./appserver ./parity -count=1; go test
  ./... -count=1`.

## sqlite_home config surface + appserver wiring

- Audit found Go's state package already supports the `CODEX_SQLITE_HOME` env
  var and `SqliteConfig`, but the Rust `sqlite_home` config key was missing
  (strict config rejected it). Added the key,
  `Config.SQLiteHome()` accessor, and
  `state.SqliteConfigForCodexHomeWithOverride` (configured override wins, then
  env var, then codex home — mirroring Rust's managed-value precedence),
  wired into the appserver state runtime resolution
  (`prepareSharedStateRuntime` -> `resolveDefaultStateRuntime`).
- The config-schema rust-only allowlist shrank from 15 to 14 entries.
- `TestSqliteConfigForCodexHomeWithOverride` pins the precedence.
- Verification: `go test ./state ./appserver ./config ./parity -count=1; go
  test ./... -count=1`.

## log_dir config surface recognized

- Go's doctor already reads `log_dir` for log-path reporting, but strict
  config validation rejected the key (missing from knownTopLevelConfigFields).
  Added it so a configured `log_dir` is accepted and doctor-reported; the
  config-schema rust-only allowlist shrank from 14 to 13. Go's logging stays
  SQLite-based; a file-log redirect honoring `log_dir` is a tracked follow-up.
- Verification: `go test ./parity -run TestRustConfigSchemaSurfaceAgainstGo
  -count=1; go test ./config ./doctor -count=1; go test ./... -count=1`.

## feedback config surface recognized

- Go's interactive TUI already reads `feedback.enabled`
  (`interactiveFeedbackEnabledFromConfig` gates the TUI feedback prompt,
  default true like Rust), but strict config validation rejected the key.
  Added it plus `feedback.enabled` sub-field validation (deny_unknown_fields
  mirroring Rust `FeedbackConfigToml`); the config-schema rust-only allowlist
  shrank from 13 to 12.
- Verification: `go test ./parity -run TestRustConfigSchemaSurfaceAgainstGo
  -count=1; go test ./config ./app -count=1; go test ./... -count=1`.

## Reasoning-summaries catalog parse fixed (supports_reasoning_summary_parameter)

- Audit found a real gap: Go's ModelInfo parsed only the legacy name
  `supports_reasoning_summaries`, but the backend /models response sends
  Rust's serde name `supports_reasoning_summary_parameter` — the field was
  always false, so the catalog-driven request paths
  (responses_agent.go:558/863) never emitted the reasoning summary while Rust
  defaults it true (`#[serde(default = "default_true")]`).
- Fix: the ModelInfo struct tag now uses `supports_reasoning_summary_parameter`
  (Rust wire name wins on parse), the legacy `supports_reasoning_summaries`
  name remains a parse-time alias, and an absent field defaults to true.
- The L0 model-metadata verifier allowlist shrank to 1 Rust-only entry
  (`availability_nux` on ModelSummary) and 2 Go-only extensions
  (`base_instructions`, `supports_parallel_tool_calls`).
- `TestModelInfoReasoningSummariesParseMirrorsRustDefaultTrue` pins absent /
  Rust-name / legacy-alias semantics.
- Verification: `go test ./model -run 'TestModelInfoReasoningSummariesParseMirrorsRustDefaultTrue|TestModelInfoUnmarshalRustCatalogShape' -count=1; go test ./parity ./model -count=1; go test ./... -count=1`.

## Model metadata surface fully aligned

- Go's ModelInfo now carries the final missing Rust wire field
  `availability_nux` (`ModelAvailabilityNux{message}`), populated by the
  catalog UnmarshalJSON. All 41 Rust `openai_models::ModelInfo` wire fields
  are now carried; the L0 model-metadata verifier's Rust-only allowlist is
  empty and only 2 genuine Go extensions remain (`base_instructions`,
  `supports_parallel_tool_calls`).
- `TestModelInfoUnmarshalRustCatalogShape` pins the `availability_nux` parse.
- Verification: `go test ./parity -run TestRustModelInfoFieldSurfaceAgainstGo
  -count=1; go test ./model -count=1; go test ./... -count=1`.

## Error-code emission gaps closed: threadRollbackFailed + rollout budget

- `threadRollbackFailed`: rollback request validation failures (num_turns < 1,
  active turn) now surface a -32600 invalid-request RPC error carrying
  `codexErrorInfo: "threadRollbackFailed"` (new `threadRollbackFailedError`
  implementing `jsonRPCErrorDataProvider` and matching
  `ErrJSONRPCInvalidRequest` for the Rust `invalid_request` code), wired into
  both the legacy and runtime rollback handlers; unit-pinned by
  `TestRouterThreadRollbackValidationSurfacesThreadRollbackFailedLikeRust`.
- `sessionBudgetExceeded`: implemented the rollout budget feature end to end.
  Config: new `config/rollout_budget.go` `Config.RolloutBudgetConfig()`
  resolves `features.rollout_budget` with Rust's exact
  `resolve_rollout_budget_config` semantics (bare bool or table with `enabled`,
  required `limit_tokens`/`reminder_at_remaining_tokens` with positivity
  bounds, finite non-negative weights defaulting to 1.0) plus strict
  sub-field validation; 5 unit tests mirror Rust's config_tests. Runtime:
  `RuntimeRouter.rolloutBudgetForSession()` lazily configures a shared
  `runtimeutil.Budget` per session from the resolved config, `runTurnRuntime`
  charges each completed turn's usage (mirroring Rust
  `Session::record_rollout_budget_usage`) and fails the turn with the new
  `ErrSessionBudgetExceeded`, and `turnAnalyticsErrorFieldsFromError` maps it
  to the `sessionBudgetExceeded` codexErrorInfo; 2 unit tests wire the
  resolution/exhaustion path.
- `TestRustErrorCodeSurfaceAgainstGo` now passes with zero emission gaps: all
  17 CodexErrorInfo and 7 ConfigWriteErrorCode wire values are present in Go
  production code.
- Verification: `go test ./parity ./config ./appserver -count=1; go test
  ./... -count=1`.

## Rollout budget reminder delivery

- New `RuntimeRouter.rolloutBudgetFollowUp` returns a `turn.SamplingFollowUp`
  composed into the agent loop next to the auto-compact follow-up. Each
  completed model response is charged per-response (mirroring Rust
  `Session::record_rollout_budget_usage`) and, when a remaining-tokens
  threshold is crossed, the Rust-shaped `<rollout_budget>` developer message
  ("You have N weighted tokens left in the shared session token budget.") is
  injected exactly once per delivered index (mirroring Rust
  `maybe_record_reminder` / `pending_reminder` semantics, including the
  first-charge reminder and per-window dedup).
- Exhaustion detected in the follow-up is recorded on the router and fails the
  turn at completion with the `sessionBudgetExceeded` codexErrorInfo; turns
  without model responses are still charged at completion.
- Unit tests: `TestRuntimeRouterRolloutBudgetExhaustionSurfacesSessionBudgetExceeded`,
  `TestRuntimeRouterRolloutBudgetReminderInjectsDeveloperMessage` (80 -> 75 ->
  35 per-threshold sequence + duplicate suppression),
  `TestRuntimeRouterRolloutBudgetDisabledResolvesNil`.
- Verification: `go test ./appserver -run TestRuntimeRouterRolloutBudget
  -count=1; go test ./... -count=1`.

## Model metadata fields carried by Go ModelInfo

- Go's `ModelInfo` now carries four previously-missing Rust
  `openai_models::ModelInfo` wire fields: `shell_type` (ConfigShellToolType:
  default/local/unified_exec/disabled/shell_command), `apply_patch_tool_type`
  (freeform), `comp_hash`, and `experimental_supported_tools`. The catalog
  `UnmarshalJSON` populates them so the backend /models metadata round-trips
  identically.
- The L0 model-metadata verifier allowlist shrank from 6 to 2 Rust-only
  entries (`availability_nux` carried on Go's ModelSummary;
  `supports_reasoning_summary_parameter` is Go's `supports_reasoning_summaries`
  naming variant).
- `TestModelInfoUnmarshalRustCatalogShape` pins the parse of the new fields.
- Verification: `go test ./parity -run TestRustModelInfoFieldSurfaceAgainstGo
  -count=1; go test ./model -count=1; go test ./... -count=1`.
