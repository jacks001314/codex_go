# Codex Go update alignment plan - 2026-09-26 (a0b85c7a66)

Continues `update/plan_2026_09_25.md` (sync138-sync214).

## Baseline

- `git fetch origin main` + `git pull origin main` in
  `D:\qax\reagent\dev\git\codex` report **no new upstream commits**: the Rust
  checkout, `origin/main` and `git ls-remote origin main` all agree on
  `a0b85c7a664505a95ad0cfbaa2291ee34aa5133d` (#48158, the Guardian
  retained-context spacing/empty-assistant fix).
- Go repo was at `159bb7c1` (`sync214`, #48158's rendering half) before this
  pass, so the daily pull exposes no unlanded upstream commit.

Per the workflow's step 6 (re-check the recorded unlanded items), this pass
works the most recently recorded remaining lane instead of stopping: the
`history` crate's retained-context model. sync214 landed the *rendering* half of
#48158 (`state/guardian_root_messages.go`); the data model the renderer consumes
did not exist in Go.

## Why the model lives in its own package

Go's `history` package is the counterpart of Rust's **`message-history`** crate
(the append-only global `history.jsonl`: `history.go` / `message_history.go`
mirror `message-history/src/lib.rs`). Rust's **`codex-history`** crate
(`history/src/lib.rs` and its `retained_context` / `retained_source` /
`retained_assistant_messages` / `sender_user_messages` / `heartbeat` /
`reconciled_retained_context` modules) had no Go counterpart at all, so the port
lands as `codex_go/retainedctx` and the Go top-level directory inventory is
re-pinned (`parity/rust_inventory_test.go`).

Go also has no typed `CodexHarnessMetadata` (persisted items carry
`Metadata map[string]any`), so the package takes the typed view it needs
(`retainedctx.HarnessMetadata`: `user_input_order`, `inherited_user_message`,
`sender_user_messages`). Wiring the rollout/compacted-item wire to the typed
metadata is recorded below as the follow-up.

## sync215: the retained-context model (`codex-history`)

`retainedctx` ports the bounded, host-owned evidence families that live outside
the model's compaction contract, with Rust's admission rules intact:

- **`heartbeat.go`** - `HEARTBEAT_CONTENT_KIND`, `UserInputOrigin`
  (`FromTurnTrigger`, `FromMessage`, `IsUser`), and `Heartbeat`
  (`ParseHeartbeat`, `HeartbeatFromMessage`). `ParseHeartbeat` reproduces Rust's
  byte-budget guard (`len + lines*6 + 64 > 3600` with `str::lines()` counting),
  the single-trailing-newline strip, the exact envelope prefix/suffix, and the
  automation-id/timestamp byte-class limits. The Go zero value is normalized
  onto `User` so an unset origin never round-trips into a different stored value.
- **`retained_source.go`** - `ResponseItemID` (`NewResponseItemID("retained")`,
  UUIDv7), `RetainedSourceRole`, `RetainedSourceID`, `RetainedSource`, plus
  `RestoreSourceRevision` and `Source` (delivery proof requires both a message id
  and a recorded revision).
- **`retained_context.go`** - `VerifiedQuestionAnswer`, `VerifiedAnswer`,
  `RetainedUserMessage` (skips the ordinary-user origin on the wire, defaults it
  on decode), `RetainedInputSource` (`Local(order)` / `Inherited`),
  `RetainedContextOrder` (inherited prefixes order before local acceptance
  order), `RetainedContextEntry` / `OrderedEntry`, `RetainedContextEvent`
  (internally tagged `verified_answer`), and `RetainedContext` itself with
  Rust's serde layout: `verified_answers` + `incomplete`,
  `user_messages` + `user_messages_incomplete` (legacy default **true**),
  `assistant_messages` + `assistant_messages_incomplete`, `next_order`, and the
  empty-skipped `sender_deliveries`. Storage limits are `MAX_FAMILY_RECORDS = 8`,
  `MAX_RECORD_BYTES = 16_384`, `MAX_FAMILY_BYTES = 65_536`; `boundFamily` sorts
  by acceptance order (not persistence order) and evicts the oldest, marking the
  family incomplete; `RetainedUserMessage.bound` clears text/incompleteness and
  truncates ids at a char boundary; `RetainedContextEvent.Bound` clears an
  oversized answer's questions and truncates its ids.
- **`retained_messages.go`** - `RecordUserMessage` (heartbeat coalescing that
  compares only the latest version of the same automation, id-based replacement,
  inherited prefix order via `next_inherited_order`, revision minting,
  `verified_answers_incomplete |= inherited`), `RecoverUserMessageExcerpts`,
  `Record`, `Restore` (deep copy, re-bound every family, repair pre-adoption
  checkpoints, re-derive `next_order` from surviving item metadata), and
  `Rollback` (boundary by acceptance order or first removed message id; the
  legacy fallback by turn id, with inherited/local scoping).
- **`retained_assistant_messages.go`** - `RecordAssistantMessage`, including
  #48158's rules: an unsequenced `Local(None)` source is left to legacy
  transcript selection; a **complete, empty** assistant message is neither
  recorded nor a dedup hit (an id collision removes the stale record instead);
  an incomplete message keeps its omission notice; `HasOmittedAssistantMessages`
  reports storage loss or an undeliverable record.
- **`reconciled_retained_context.go`** - `LiveUserMessage`,
  `NewReconciledRetainedContext` (source-identity matching before ordering,
  conflicting/absent orders mark the result incomplete, latest ordered user turn
  tracking), `OrderedEntries` (with heartbeat-version coalescing after ordering,
  scoped per inherited/local prefix), and `UnmatchedUserMessages`.

Tests mirror Rust's suites: `retained_context_tests.rs` (order through recovery,
checkpoint and rollback; family storage limits without changing snapshots;
recovered excerpts obeying record/family limits; legacy checkpoints marking user
messages incomplete; surviving local sources advancing the counter; delayed
acceptance order across checkpoint replay plus `RestoreSourceRevision`; adopted
instructions preserving local order and rollback scope), the assistant rules
from #48158, `reconciled_retained_context_tests.rs` (source identity/acceptance
order/checkpoint gaps; missing and conflicting orders; heartbeat versions across
retention, restore and reconciliation), and `heartbeat_tests.rs` (exact
instruction body with and without a trailing newline, plus the rejection
matrix), with an added origin-classification and sender-budget matrix.

### Verification (fifty-fifth pass)

- `go test ./retainedctx/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `a0b85c7a66` (only the Go top-level
  directory inventory changed: `+retainedctx`).
- `go test ./... -count=1` reported only the documented load-sensitive
  `appserver.TestSkillInstructionsSelectAppsTheyMentionLikeRust`, which passes
  in isolation (`-run ... -count=1`) and as a full-package run
  (`go test ./appserver/ -count=1`, `ok ... 140.6s`).

## Remaining for the retained-context program (recorded)

- The rest of the `codex-history` crate domain that this slice does not yet
  model: `RetainedContext`'s typed home in the rollout wire
  (`ResponseItemEnvelope` + the typed `CodexHarnessMetadata` with
  `guardian_sources`, `guardian_source_order_guidance`, `retained_source`,
  `history_truncation_token_limit`, `delivered_assistant_message`,
  `harness_authored_configuration`, `compaction_model_hash`, `compaction_output`,
  `mcp_attribution`), `GuardianHistoryCheckpoint`, `CompactedItem`,
  `RolloutItem`/`RolloutLine`, and `CompactionCheckpoint` /
  `CompactionResumeMetadata`. Go persists these as untyped maps today, so the
  retained model has no consumer yet.
- `guardian-context`'s `composition.rs` / `retained_instructions.rs` sections
  (the retained-instruction fragment spacing, source-order guidance detection and
  empty-section dedup introduced alongside #48158). `state/guardian*.go` carries
  only the root-message renderer (sync214) and the bounded root authorization
  list.

The next pass continues with the typed harness-metadata/rollout wire that gives
`retainedctx` its consumer, then the guardian-context composition sections.

---

## Follow-up pass (same day, nineteenth pull): `44a9bfa145` (two commits)

`git fetch` advanced `a0b85c7a66..44a9bfa1456e51269fa824fa2f93b435e3e800ed`:

| Commit | Subject | Lane |
|---|---|---|
| `f6a4bd81e3` | Generate unique exec-server process IDs for every request (#48168) | unified-exec (core) |
| `44a9bfa145` | Preserve usage limit windows in turn and compaction analytics (#48174) | analytics + codex-api |

`go test ./parity/ -count=1` is green at the new pin: both commits modify
existing files (test suites and sources), so no fixture root, inventory,
critical-file hash, snapshot total or precomputed export changes.

### sync216: unique exec-server process IDs for every request (#48168)

Rust previously suffixed a fresh UUID onto the exec-server process id only when
the request carried sandbox or shell-snapshot settings; a thread sharing an
executor, or a sandbox retry, could reuse a public handle while the executor
still retained its previous process. The suffix is now unconditional.

Go's unified-exec remote start built `remoteID` as the bare stringified public
process id, i.e. exactly the pre-#48168 "no suffix" branch, so it had the same
collision window.

- **`tool/unified_exec.go`**: `remoteID` is now
  `"<public id>-<uuid v4>"`. The public id keeps addressing the session
  (`unifiedExecProcess.id`, the tool's process list, writes and signals), while
  the suffixed value is what the executor is addressed with: process-event
  subscription, network-policy controller registration, `ExecParams.ProcessID`
  and every later read/write/signal/terminate against that process. The trace
  event still reports the public id separately from the executor key.
- **`tool/unified_exec_spans_test.go`**: the process-start event's executor key
  is now asserted to be the public id plus a parseable UUID suffix, mirroring
  Rust's request-parameter test (`assert_regex_match` on the suffixed format and
  `assert_ne!` across repeated calls).

### sync217: usage-limit windows in turn and compaction analytics (#48174)

Rust preserves an optional `limit_window_minutes` from usage-limit error
responses on `UsageLimitReachedError`, accepting unsigned integers that fit in
`u16` and treating missing/null/malformed/out-of-range values as unknown, and
reports it as `usage_limit_window_minutes` in turn and compaction analytics
(`null` for every other error kind).

Go had no home for the value at all: its HTTP 429 with `error.type ==
"usage_limit_reached"` fell through to a plain `ResponsesAPIError`, so no
usage-limit classification (and therefore no window) reached the app-server.
This pass lands the classification together with the window.

- **`codexapi/error.go`**: `APIError` (and `APIErrorDetails`) gained
  `UsageLimitWindowMinutes *uint16`. `Details()` and `NewAPIErrorWithDetails`
  carry it on both the literal and the details-backed construction paths.
- **`model/responses_agent.go`**: `responsesAgentAPIErrorBody` gained the
  untyped `limit_window_minutes` member; `responsesHTTPError` classifies an HTTP
  429 whose `error.type` is `usage_limit_reached` as `ErrorRateLimit` (Rust's
  `CodexErr::UsageLimitReached`, checked before the quota codes as in
  `api_bridge.rs`) and attaches the window through
  `usageLimitWindowMinutesFromValue` (unsigned integers that fit in `u16` only).
  The wrapped-WebSocket `error` event path gained `websocketUsageLimitError`,
  which requires a numeric status and a `usage_limit_reached` error type before
  producing the same classified error with its window, mirroring Rust's
  `parse_wrapped_websocket_error_event` / `map_wrapped_websocket_error_event`
  followed by `map_api_error`.
- **`appserver/turn_runtime.go`**: `turnAnalyticsErrorFields` gained
  `UsageLimitWindowMinutes`, populated only for `codexapi.ErrorRateLimit`, and
  the value is threaded through `turnCompletionAnalyticsContext` into
  `emitCodexTurnAnalyticsEvent`. A status-only 429 (`ErrorAPI` /
  `ErrorRetryable`) keeps reporting `null`, as Rust does for other error kinds.
- **`appserver/compaction_analytics.go`**: the compaction event carries the same
  field from its error classification, so local and remote compaction failures
  report the window.
- **`telemetry/turn_event.go` / `telemetry/compaction_event.go`**: the turn and
  compaction event params gained `usage_limit_window_minutes *uint16` (always
  serialized, `null` when unknown) and the constructors copy it from the input.

Tests: the HTTP window matrix (five-hour 300, weekly 10080, missing, null,
malformed, out-of-range 70000), the unchanged quota classification without a
window, the wrapped-WebSocket mapping with and without a status, the
app-server analytics field matrix (usage limit with and without a window, and
another error kind that must report null), the turn and compaction event JSON
nullability, and the exec-server process-id suffix and uniqueness.

Recorded as remaining from #48174's lane: Rust's `UsageErrorBody` also models
`plan_type` and `resets_at`, and its 429 branch classifies `usage_not_included`
as `CodexErr::UsageNotIncluded`; Go's HTTP path still does not model those, so
the window is the only usage-limit detail Go currently preserves on the HTTP
error. Rust's `analytics` crate also folds the value through
`TurnCodexError`/reducer aggregation, which has no Go counterpart (Go's
telemetry events are the wire shape only).

### Verification (fifty-sixth pass)

- `go test ./codexapi/ ./telemetry/ ./retainedctx/ -count=1` is green.
- `go test ./model/ -run 'UsageLimit|ResponsesHTTPError|WebsocketUsageLimit'
  -count=1` is green.
- `go test ./appserver/ -run 'TurnAnalytics|Compaction|UsageLimit' -count=1`
  and `go test ./tool/ -run 'UnifiedExec' -count=1` are green.
- `go test ./parity/ -count=1` is green at `44a9bfa145`.
- `go test ./... -count=1` recorded below.

## Remaining after this pass

The recorded lanes from sync215 are unchanged (typed harness metadata / rollout
wire for `retainedctx`, the `guardian-context` composition and
retained-instruction sections), plus the #48174 items named above.

---

## Follow-up pass (same day, twentieth pull): `1d804e91b7` (one commit)

`git fetch` advanced `44a9bfa145..1d804e91b75454927fb4f51615b067ba6158c37c`:

| Commit | Subject | Lane |
|---|---|---|
| `1d804e91b7` | Protect `.aws` directories under sandbox writable roots (#48176) | sandbox (security) |

`go test ./parity/ -count=1` is green at the new pin: the commit modifies
existing sources and snapshots, so no fixture root, inventory, critical-file
hash, snapshot total or precomputed export changes.

### sync218: `.aws` joins the protected workspace metadata paths (#48176)

AWS profiles can select credential helpers that the application executes, so
granting write access to a containing directory must keep `.aws` protected.
Rust adds `.aws` to `PROTECTED_METADATA_PATH_NAMES`, to the default read-only
project-root subpaths of `FileSystemSandboxPolicy::restricted`, and to
`default_read_only_subpaths_for_writable_root` (where, like `.agents`, it is
materialized only when the top-level directory exists), preserving explicit user
rules that override the default protection.

Go carries the same metadata list in several places; all now include `.aws`
(Go rebrands the codex home directory to `.gcode`, which is unchanged):

- **`sandbox/policy.go`**: `WritableRoot.ProtectedMetadataNames` gains `.aws`
  (the name-based protection `IsPathWritable` and the seatbelt profile consume)
  and `protectedSubpaths` gains `<root>/.aws`.
- **`sandbox/run.go`**: `rustPermissionProfileWireFromPermissionProfile` adds the
  `.aws` read-only `project_roots` special for a workspace-write policy,
  mirroring `FileSystemSandboxPolicy::restricted`.
- **`config/permissions.go`**: the built-in `:workspace` profile builder gains
  the `.aws` read-only `project_roots` entry next to `.git`/`.agents`/`.gcode`.
- **`sandbox/linuxsandbox/linux.go`**: both the legacy workspace-write entry
  list and `protectedReadOnlySubpaths` (materialized only for existing
  directories) include `.aws`.
- **`sandbox/windowssandbox/resolved_permissions.go`**: the Windows deny
  subpaths gain `.agents` and `.aws`; the deny computation keeps only subpaths
  that exist, so the existing "only existing `.git`" behavior is unchanged.
- **`sandbox/windowssandbox/elevated/ipc_framed.go`**: the elevated
  workspace-write profile's protected project-root subpaths include `.aws`.

Tests: the workspace writable-root matrix (`.aws` in
`ProtectedMetadataNames` and `ReadOnlySubpaths`, and `.aws/credentials` staying
read-only), the built-in `:workspace` resolution asserting the `.aws` read-only
project-root entry, the Windows deny set for `.git`/`.agents`/`.aws`, and a
Linux-only case asserting an existing top-level `.aws` becomes a protected
read-only subpath.

Recorded as remaining for this lane: Rust's
`append_default_read_only_project_root_subpath_if_no_explicit_rule` applies the
default metadata carveouts to *custom* `[permissions]` profiles and skips them
when the profile already has an explicit rule for that subpath. Go's
custom-profile path (`compileConfigPermissionProfile`) applies no defaults at
all, so a custom profile that only writes `:workspace_roots` leaves the
metadata names writable - stricter in Rust than in Go. That divergence predates
#48176 and needs the "no explicit rule" predicate modelled before it can be
closed. Go also has no `bwrap` builder, so Rust's Linux synthetic mounts for
`.aws` have no Go counterpart (the Landlock/deny-policy path above is the Go
equivalent).

### Verification (fifty-seventh pass)

- `go test ./sandbox/... ./config/ -count=1` is green.
- `GOOS=linux CGO_ENABLED=0 go build ./sandbox/...` type-checks the Linux-only
  sandbox additions (the host has no Linux cgo toolchain for `go vet`).
- `go test ./parity/ -count=1` is green at `1d804e91b7`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `1d804e91b7`), so this
pass works the next recorded lane from sync215: the `guardian-context`
retained-instruction and verified-answer sections, which give `retainedctx` its
first consumers.

Rust's `guardian-context` composes the review prompt from independently scoped
section contributors. Go's `state.BuildPromptWithOptions` renders the same
prompt as one string, and previously modelled only the root conversation,
transcript, node-repl evidence and planned action; the retained snapshot had no
renderer at all.

### sync219: the retained-instruction, verified-answer and sender sections

- **`state/guardian_retained_context.go`** ports
  `guardian-context/src/retained_instructions.rs` and
  `guardian-context/src/verified_answers.rs`:
  - `HasLegacyRetainedOrder` (a repeated `RetainedContextOrder` means the
    checkpoint cannot order its records) and `RetainedSourceOrderLabels`, which
    render `Local(order)` labels, `inherited <order>` prefixes, or the legacy
    positional enumeration.
  - `RetainedAssistantMessage`, which selects a *whole* complete assistant
    message only when its rendered framing fits
    `ApproxBytesForTokens(900)` with the 32-byte order label reserved.
  - `RenderRetainedInstructions`, which emits one `RetainedInstructionFragment`
    per whole instruction in acceptance order, skips complete+empty assistant
    messages (#48158), keeps assistant context as optional (untrusted
    commentary) evidence, omits any record that cannot fit its budget instead of
    truncating it, and prepends the incomplete-assistant-context then the
    unavailable-instructions host notices.
  - `RetainedUserInstructionsSectionItems`, which returns the delivered user
    content: the modern `START` banner (with the inherited-prefix sentence) or
    the legacy `LEGACY_START` banner, the fragments, and the `END` footer, each
    with Rust's extra newline so blank lines separate the banner, fragments and
    footer (#48158).
  - `RenderVerifiedAnswer` / `RenderVerifiedAnswers`, which keep both sides of
    every question/answer pair within their own 900-token budget, mark the
    result incomplete when a record does not fit, and prepend the incomplete
    verified-answers host notice.
  - `SenderUserMessagesSectionItems`, the `SenderUserMessagesSection`
    counterpart: the delivery-bound host-rendered sender text, or nothing.
- **`state/guardian.go`**: `BuildPromptOptions` gained
  `RetainedContext *retainedctx.RetainedContext`, and the prompt now renders the
  sender evidence and then the retained-instruction section between the root
  conversation and the transcript, matching Rust's registry order. A nil
  context renders no section, so existing callers are unchanged.

Tests mirror Rust's `retained_instructions_tests.rs` cases that do not need the
ComposedContext/budget machinery: the order/whole-record matrix (an existing
900-token answer still fits, an oversized instruction is omitted with the notice
and never partially rendered), the role-labelling and assistant-context rules
(forged `user:` text inside an assistant message stays labelled `assistant:`, a
complete empty message renders nothing, an incomplete or oversized one reports
the omission notice first while the user instruction still renders), the legacy
positional labels, the section banners/blank-line spacing, the
verified-answer rendering and completion flag, the assistant-message selection
matrix, the sender section, and the prompt-level section order.

Recorded as remaining for this program: `composition.rs`'s
`ComposedContext`/`SectionDelivery` machinery (`retain_new_instructions`,
`deduplicate_transcript_instructions`, `retained_instructions()`,
`enforce_budget`, `into_annotated_messages`) and Rust's `SectionRegistry` /
`SectionContributor` abstraction; Go renders the sections directly rather than
through a registry. The remaining section contributors (`previous_reviews`,
`trusted_tool`, `trusted_skills`, `trusted_user_answers`, `permissions`,
`transcript`, `images`, `node_repl`, `action`) are still unmodelled or rendered
inline. Rust's shared `truncate_text` (`guardian-context/src/truncation.rs`) is
already implemented in Go as `context.guardianTruncateText`, so it needs no port.

### Verification (fifty-eighth pass)

- `go test ./state/ -count=1` is green.
- `go test ./state/ ./appserver/ ./app/ ./parity/ -count=1` is green.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-first pull): `782826663d` (two commits)

`git fetch` advanced `1d804e91b7..782826663df3e898d0c594a13f6f75cc2a498644`:

| Commit | Subject | Lane |
|---|---|---|
| `8f0b05910e` | Fix zsh alias quoting in sourced shell snapshots (#48187) | shell snapshots |
| `782826663d` | Bound agent message board SSE frames before parsing (#48190) | agent message board client |

`go test ./parity/ -count=1` is green at the new pin: both commits modify
existing sources and test suites, so no fixture root, inventory, critical-file
hash, snapshot total or precomputed export changes.

### sync220: zsh alias quoting in sourced snapshots (#48187)

A sourced snapshot's group is parsed before its restored options take effect, so
aliases serialized with `RC_QUOTES` could be misread by the replay shell. Rust
now serializes them with `NO_RC_QUOTES` in a subshell (no optional module), and
keeps the plain alias dump for the evaluating form.

- **`shell/snapshot_scripts.go`** is regenerated byte-for-byte from the Rust
  source with the existing extractor
  (`python scripts/extract_snapshot_scripts.py`): the zsh capture script's alias
  dump is now the `SNAPSHOT_ZSH_ALIASES` placeholder.
- **`shell/snapshot_capture.go`**: `snapshotCaptureScript` substitutes
  `\alias -L` for the evaluating form and `(\setopt NO_RC_QUOTES; \alias -L)`
  for the sourced form, next to the existing option-group substitutions.
- **`shell/snapshot_capture_test.go`**: the marker list covers the new
  placeholder, and the zsh case asserts the sourced form normalizes quoting
  while the evaluating form leaves shell options alone (and that no other shell
  sets zsh options).

### sync221: board SSE frames are bounded before parsing (#48190)

Checking the notification size after SSE parsing left oversized fields and
unterminated frames free to accumulate in the parser, and malformed UTF-8 could
be retained indefinitely. Rust now enforces its bound on the incoming frame
bytes before parsing, resets the count at blank lines (supporting LF, CR and
CRLF across chunks), validates UTF-8 incrementally, and stops reading the source
after an error.

- **`agentboard/remote.go`**: `remoteBoardFrameScanner` reproduces that state
  machine - per-frame byte counting with the blank-line reset and the deferred
  CR handling, plus incremental UTF-8 validation that only carries an incomplete
  code point (using `utf8.Valid`/`utf8.FullRune` for Rust's
  `error_len().is_some()`). `readRemoteBoardEvent` feeds every wire byte through
  it before interpreting the line and returns
  `board SSE frame exceeds the service limit` (Rust's message) or the
  malformed-encoding failure; the caller closes the stream, so reading stops
  after an error. `Next` drops the now-redundant post-parse length check, since
  the frame bound is strictly stronger.

Tests mirror Rust's `remote_board.rs` additions: a long heartbeat sequence using
all three line endings still delivers the notification (the bound is per frame,
not per connection), and an oversized readiness field, an unterminated trailing
field and an oversized multiline data frame are each refused with the
service-limit message before parsing; malformed UTF-8 in the readiness frame and
in a later frame is rejected incrementally.

### Verification (fifty-ninth pass)

- `go test ./shell/ ./agentboard/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `782826663d`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `782826663d`), so this
pass works the next recorded lane from sync219: `guardian-context`'s permission
evidence, which the sync review had no way to include.

Rust's `PermissionContextSection` is `SectionScope::Shared`, so the synchronous
reviewer receives the reviewed environment's denied read paths and globs and is
told not to approve an escalation whose purpose is to read them. Go built its
review prompt without any permission evidence at all.

### sync222: parent-turn permission evidence for the review prompt

- **`sandbox/unreadable.go`** ports
  `FileSystemSandboxPolicy::get_unreadable_roots_with_cwd` and
  `get_unreadable_globs_with_cwd` over Go's resolved profile wire:
  `UnreadableRootsWithCWD` lists the deny entries that are still unreadable
  (skipping the filesystem root, skipping entries a readable rule wins at, and
  deduplicating), and `UnreadableGlobsWithCWD` resolves the deny glob patterns
  against the cwd and returns them sorted and deduplicated. An unrestricted or
  disabled profile reports nothing, exactly as Rust's non-restricted kinds do.
- **`state/guardian_permission_context.go`** ports the section: the
  `PermissionContext` struct, Rust's body text (paths before globs, the
  environment-scoped scope sentence, the "no explicit denied-read
  paths/globs" sentence for an environment-only context), the marked items
  (`\n>>> PARENT TURN PERMISSION CONTEXT START\n` … `>>> PARENT TURN PERMISSION
  CONTEXT END\n`), the synchronous contribution, and the asynchronous rule that
  evidence over one thousand estimated tokens is an error so the async scorer
  falls back to synchronous review instead of dropping restrictions.
- **`state/guardian.go`**: `BuildPromptOptions` gained `PermissionContext`, and
  the prompt renders the section after the node-repl evidence and before the
  planned action, matching Rust's registry order; the marker's own leading
  newline is normalized away because the block separator already supplies that
  paragraph break.
- **`appserver/agent_runtime.go`**: `guardianTurnPermissionProfileForTurn`
  resolves the reviewed turn's own profile and cwd, and
  `guardianReviewPermissionProfileForTurn` (the read-only profile for the
  reviewer's session) now builds on it, so the two are no longer conflated.
- **`appserver/guardian_reviewer.go`**: the reviewer takes a
  `turnPermissionProfile` callback, derives the denied paths and globs, and
  passes the permission context into every prompt build, so a turn with denied
  read rules now reviews with that evidence.

Tests: the body matrix (no evidence, environment-only, paths before globs), the
section items (nothing for an empty context, marked section otherwise), the
asynchronous limit boundary (exactly 3,000 bytes delivered, one byte more
rejected), the sandbox enumerators (deny path and glob, relative glob resolution
against the cwd, duplicate removal, filesystem-root exclusion, and no
restrictions for read-only/full-access/nil profiles), the prompt-level section
order (after node-repl evidence, before the action), and the reviewer wiring
(the denied path and the restriction guidance reach the sampled prompt, and a
profile without deny entries adds no section).

Recorded as remaining for this lane: Rust's resolver also carries the reviewed
environment's *selection id* (so the body names the environment) and uses the
selected executor environment's own `permission_profile_with_workspace_roots`
when the turn runs there; Go currently reports the parent turn's profile with no
environment id, and does not consult an executor environment's own profile. Rust
additionally derives the evidence from executor path contexts
(`get_unreadable_roots_with_context` for remote environments); Go's enumerators
resolve against the host cwd only.

### Verification (sixtieth pass)

- `go test ./state/ ./sandbox/... ./appserver/ ./tool/ ./exec/ ./config/ ./mcp/
  -count=1` is green.
- `go test ./parity/ -count=1` is green at `782826663d`.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-second pull): `dd9a7101d9` (four commits)

`git fetch` advanced `782826663d..dd9a7101d9ddbaab68030e04d288d71bead68552`:

| Commit | Subject | Lane |
|---|---|---|
| `888948e522` | Optimize `blake3` in Bazel fastbuilds (#48197) | build only |
| `8f5387d104` | Honor execution environment proxy requirements (#48198) | network proxy |
| `7596ab19a6` | Keep archived threads with empty previews visible in listings (#48199) | state / rollout listing |
| `dd9a7101d9` | Extract Responses header conversion into a shared module (#48200) | codex-api refactor |

`go test ./parity/ -count=1` is green at the new pin: none of the four commits
touches a pinned artifact (no schema, precomputed export, inventory or snapshot
changes).

### Triage: `#48197` is build-only (N/A in Go)

The commit adds a Bazel fastbuild option for the `blake3` crate (`MODULE.bazel`
plus a comment). Go has no Bazel graph, so there is no Go surface to change; no
stub is added.

### sync223: the owner proxy requirement (#48198's data model)

An execution environment's enabled proxy configuration was reduced to traffic
restrictions, leaving restricted commands offline without a controller proxy or
an approved network grant. Rust carries `NetworkProxyConfig.enabled` into
`EnvironmentNetworkPolicy.requires_proxy` (serialized as `requiresProxy`, omitted
when false) and lets the orchestrator treat it as a managed-network activation
trigger alongside an enabled controller proxy.

- **`network/network.go`**: `EnvironmentNetworkPolicy` gained `RequiresProxy`
  with Rust's wire name and omission rule, and
  `EnvironmentNetworkPolicyFromConfig` now captures it from `Config.Enabled`, so
  the owner's enabled proxy configuration survives into the portable policy
  instead of being dropped with the other controller-owned settings. The doc
  comments record the new "proxy activation plus traffic restrictions" meaning.

Tests: the capture matrix (an enabled owner proxy produces a proxy requirement,
a disabled one does not, and the omitted controller allow-all flag still
resolves to false) and the serialized shape (`requiresProxy` emitted when true,
absent when false).

Recorded as remaining for this lane, with evidence: the *activation consumer*.
Rust's rule lives in `core/src/tools/orchestrator.rs`'s `managed_network_active`,
which reads the resolved environment config's `network_policy`. Go has neither
an `EnvironmentConfig` network-policy field
(`appserver/environment_config.go` carries `AllowLoginShell`,
`ShellEnvironmentPolicy`, `ExecPolicy`, `McpPolicy` and the permission profile
only) nor a production caller of `network.NewSpecForEnvironment` (it is
exercised by tests only), so no host currently constructs an
`EnvironmentNetworkPolicy`. That host path is the already-recorded "app/exec-level
policy owner that composes the CLI host's requirements and binds them to the
remaining transports" lane; once it lands, the activation rule can consume
`RequiresProxy` exactly as Rust does.

### Triage: `#48199` needs the frozen migration baseline and a listing filter Go does not have

The commit has three parts, none of which maps onto a Go edit:

1. **SQLite preview filter**: `push_thread_filters_with_preview` no longer adds
   `threads.preview <> ''` when `archived_only`. Go's thread listing does not
   have that filter at all: `session.matchesListOptions` selects by archive
   collection, section, provider, cwd, source, search and relation only, so an
   archived thread with an empty preview is already listed. Go's `state` layer
   exposes row queries (`state/thread_query.go`) without Rust's
   `ThreadFilterOptions`/`push_list_threads_query` predicate builder.
2. **Archive sort indexes** (`0058_threads_archive_sort_indexes.sql`): Go's
   migration inventory is pinned byte-for-byte to the certified baseline
   (`TestStateMigrationsMatchRustTarget`, `candidateRustTo`), which ends at
   `0050_threads_section_empty_preview_indexes.sql`; the same constraint already
   recorded `0052_projects_recency.sql` as a deferred pure-performance index.
   Rust's 0051-0057 migrations are likewise absent from Go, with their behavior
   implemented outside the state DB where Go needs it (for example
   `thread_attachments` lives in `session.Metadata.Extra`). Adding 0058 would
   break the pinned inventory and the file set it verifies against.
3. **Rollout-file listing**: `rollout/src/list.rs` now passes
   `PreviewFilter::IncludeEmpty` for the flat-layout traversals while the
   hierarchical created-at/updated-at traversals still require a preview. Go's
   `rollout.BuildThreadItem` has no preview or session-meta requirement at all
   (it accepts any loadable rollout with lines), so Go's rollout listing already
   includes empty-preview threads - but it also includes them where Rust still
   requires a preview.

The Go-visible remainder of this commit is therefore the *narrowing* half: Go's
listing never requires a discoverable preview, and Rust's active listing still
does. Pinning that difference needs Rust's `PreviewFilter` semantics (which
orderings require a preview and which include empty) plus a listing predicate
seam in `session.ListRecords`; it is recorded here rather than approximated.

### Triage: `#48200` is a Rust-internal refactor

The commit moves `json_headers_to_http_headers` from
`codex-api/src/endpoint/responses_websocket.rs` into a new crate-wide
`responses_headers` module and updates the WebSocket endpoint's imports; the
conversion behavior (string/number/bool values accepted, invalid names and
unsupported values ignored) is unchanged. Go has no crate boundary to align and
no shared-module extraction to perform.

Recorded as remaining from the same area (already noted under sync217): Go's
wrapped-WebSocket error path folds the transport directly into the classified
error instead of building Rust's `ApiError::Transport(Http { .. })`, so it never
converts the event's JSON `headers` map into HTTP headers at all. Go has no
`jsonHeadersToHTTPHeaders` equivalent; that belongs with the wrapped-transport
mapping when it is ported.

### Verification (sixty-first pass)

- `go test ./network/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `dd9a7101d9`.
- `go test ./... -count=1` recorded below.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `dd9a7101d9`), so this
pass closes the Go-visible half of `#48199` rather than leaving it recorded as
needed.

Rust's listing rules are: the SQLite query requires a non-empty `preview` for the
plain active collection and exempts the archived collection, relation listings
(`include_empty_preview = relation_filter.is_some()`) and specific-section
listings (`matches!(section, Some(Some(_)))`); the flat rollout traversal
includes empty previews while the nested one still requires them.

### sync224: the state-backed listing preview rule (#48199's part 1)

- **`appserver/router.go`**: `stateThreadListRequiresPreview` mirrors
  `push_thread_filters_with_preview`, and `listRecordsFromState` drops a state
  row with an empty (or absent) preview when it applies. Because Go's
  state-backed path is the counterpart of Rust's SQLite listing, this is the
  same rule in the same place; the state-less path (store records plus the
  rollout scan) keeps Rust's flat-traversal behavior of including empty
  previews.

Tests: the exemption matrix over a state-backed router - the plain active
listing and an *unsectioned* listing hide the two empty-preview threads, a
specific-section listing and a relation listing keep the empty-preview thread,
and archiving one keeps it in the archived collection while the active
collection stays filtered. A second test ports Rust's
`thread_list_keeps_archived_threads_without_previews` /
`list_archived_threads_without_db_keeps_threads_without_previews` end to end:
two archived rollouts with empty previews are listed one page at a time
(`limit = 1`, cursor pagination) reporting an empty preview, for both
`useStateDbOnly` values, and the active collection stays empty.

Recorded as remaining for this lane: Rust's rollout-file *nested* traversal
still requires a preview (`PreviewFilter::RequirePreview`), so a nested rollout
with session metadata but no preview is hidden from Rust's file-based listing
while Go's state-less scan shows it. Go's recorder writes the nested layout, and
Go's existing `TestRouterThreadListUseStateDBOnlySkipsRolloutScan` pins the
current behavior (an active rollout with no user message is listed), so adopting
the nested rule is a deliberate listing narrowing that needs the layout
distinction modelled (`ThreadListLayout::NestedByDate` vs `Flat`) rather than a
one-line filter. The archive sort indexes of Rust's `0058` migration remain
pinned out by the certified migration baseline (the same constraint that already
records `0052_projects_recency.sql`), and are a pure-performance addition.

### Verification (sixty-second pass)

- `go test ./appserver/ -count=1` is green (141.0s).
- `go test ./parity/ -count=1` is green at `dd9a7101d9`.
- `go test ./... -count=1`: two whole-suite runs each hit one of the documented
  load-sensitive flakes in a different package
  (`tool.TestUnifiedExecRecoversDisconnectedExecServerSessionLikeRust`, then
  `appserver.TestOtelProviderReloadsAfterAccountChange`, both documented in
  `plan_2026_09_25.md`); each package passes as a full-package run and the test
  passes in isolation with `-count=3` (`ok ... 0.047s` / `ok ... 0.280s`).

---

## Follow-up pass (same day, twenty-third pull): `f5ffa46959` (three commits)

`git fetch` advanced `dd9a7101d9..f5ffa4695995bb6664fe8754ecb5ed2973f2801e`:

| Commit | Subject | Lane |
|---|---|---|
| `f5ffa46959` | Preserve queued output for observers during code-mode termination (#48207) | code-mode (core) |
| `4193f1acfb` | Add a keep-and-next action to the warnings viewer (#48206) | TUI |
| `7703f8b354` | Dismiss viewed TUI warnings when closing the viewer (#48205) | TUI |

### sync225: the static layer is re-pinned to `f5ffa46959`

`go test ./parity/` failed on the new checkout in one place: the TUI snapshot
total was 1327 rather than 1326, because #48206 added
`tui/src/app/snapshots/codex_tui__app__warnings_tests__warnings_partial_dismissal.snap`.
The manifest now records total 1327, `tui/src/app/snapshots` at 74 files, and the
new snapshot as a required entry (the two #48205/#48206 snapshots that changed in
place - `warnings_page` and `warnings_narrow` - need no count change).

### sync226: code-mode termination preserves produced output (#48207)

Rust's cell actor recorded the active yield signal separately from the observer,
so termination could disable yielding while keeping the observer attached and
ignore the queued `Started`/`Pending`/`YieldRequested` events, letting output the
script had already produced reach the observer in `CellEvent::Terminated`.

Go's runtime has no observer/event model: each cell's script runs one
`Engine.Execute` call whose result (and therefore its output) is published once.
The equivalent bug was that a termination racing that publish dropped the
produced output:

- **`codemode/session_runtime.go`**: each running cell now has an `engineState`
  (its engine, a `wake` channel and the published outcome). The script's
  goroutine publishes the outcome, and the terminating caller and the completion
  goroutine each consume it at most once (`takeEngineOutcome`), so exactly one
  path delivers it. `Terminate` captures the cell's wakeup channel first, and
  when the cell was still running it mirrors Rust's `begin_termination`:
  interrupt the engine, wait (bounded by `terminationDrainGrace`) for the
  publish and for the finishing path's append, then drain anything left. The
  completion path keeps a terminated cell's status and attaches the produced
  output to it (`AppendTerminatedOutput`) instead of failing the append and
  losing the output.
- **`codemode/codemode.go`**: `CellStore.AppendTerminatedOutput` adds output the
  runtime had already produced to a terminated cell without changing its status.

Tests: a termination regression mirroring Rust's (an immediate yield timeout, a
canceled yield signal, and output the script had already produced) asserting
that both the termination caller and a later observer receive the queued output
in the terminated event, exactly once, repeated 50 times so both select outcomes
(the script's completion racing the termination) are exercised. The existing
preempt/yield suites and the tool and turn suites that drive code mode stay
green.

### Verification (sixty-third pass)

- `go test ./codemode/ -count=5` is green.
- `go test ./codemode/... ./tool/ ./turn/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `f5ffa46959`.

### Triage: `#48205`/`#48206` need a warnings viewer Go does not have

Both commits change the TUI warnings viewer, and that component is absent from
Go entirely:

- Rust's `bottom_pane/warnings_view.rs` (`WarningsView` with a frozen
  `Vec<WarningEntry>`, per-entry visited cells, paging and an intentional
  `close()` that sends the visited entries), `app_event.rs`'s
  `AppEvent::OpenWarnings` / `CopyWarning` / the new
  `UpdateWarnings { transcript, dismissed }`, and
  `chatwidget.warning_display_state` (`dismissed`, `synced_cells`, `transcript`)
  have no Go counterpart: `git grep` finds no `WarningsView`, no
  `open_warnings`/`CopyWarning`/`UpdateWarnings` surface, and no `WarningEntry`.
- Go's `tui/chatwidget/warnings.go` carries only `WarningDisplayState` with
  `fallbackModelMetadataSlugs` and `startupConfigWarnings`;
  `tui/history_cell/startup_warnings.go` renders the startup warnings *cell*
  but nothing opens a viewer over the diagnostics.

So #48205 (dismiss viewed warnings on intentional close; the cancel key no longer
closes the viewer) and #48206 (a keep-and-next action that advances to the next
warning without closing) require building the viewer first: the frozen entry
list with its pagination, the visited tracking driven by the render path, the
keymap action and its footer hint, the app-level `UpdateWarnings` event with the
transcript-identity guard and sync invalidation, and the snapshot surfaces the
parity manifest pins (`warnings_page`, `warnings_narrow`, and the new
`warnings_partial_dismissal`). That is the recorded TUI lane; the L0 manifest is
already re-pinned for its snapshots so the static layer stays honest while the
component is unported.

---

## Follow-up pass (same day, twenty-fourth pull): `58670eeac4` (two commits)

`git fetch` advanced `f5ffa46959..58670eeac4b0bdb9fcb86929d8631c14aee0d9f6`:

| Commit | Subject | Lane |
|---|---|---|
| `d5355e95ef` | Keep Codex visible during external editor handoff (#48211) | TUI |
| `58670eeac4` | Isolate executable fixture copies in CLI tests on Linux (#48213) | CLI tests (Rust infra) |

### sync227: the static layer is re-pinned to `58670eeac4`

`go test ./parity/` failed on the new checkout in two places:

- **Fixture root**: `cli/tests` grew to 39 files for the new
  `cli/tests/support/executable.rs` (#48213), recorded in
  `rust_fixture_manifest_test.go`.
- **TUI snapshot manifest**: the total is 1328 and
  `tui/tests/suite/snapshots` grew to 5, with
  `all__suite__focus_palette__external_editor__external_editor_visible_handoff.snap`
  recorded as a required entry (#48211).

### Triage: `#48213` is Rust test infrastructure (N/A in Go)

The commit adds `cli/tests/support/executable.rs`, which copies an executable
fixture through a separate process on Linux so a sibling test's inherited
writable descriptor cannot cause `ETXTBSY`, and routes the CLI suites through
it. Go's CLI tests have no such helper and no `ETXTBSY` handling (`git grep` for
`ETXTBSY`/`copyExecutable` finds nothing), so there is no Go surface to change;
no stub is added.

### Triage: `#48211` rides Bubble Tea's terminal handoff in Go

Rust threads a `TerminalHandoff::{Restore, KeepScreen}` through its own
`Tui::with_restored`: the alternate screen's input modes are released without
leaving the screen (`AlternateScreen::release_input` with a new
`keyboard_active` flag), the last frame is repainted so a separate-window editor
leaves Codex visible, and the screen is re-entered before the editor returns.

Go's TUI does not own that handoff: it has no alternate-screen enter/leave
machinery, no keyboard-mode stack and no `restore_keep_raw` equivalent
(`git grep` for those surfaces finds only job-control and resize-reflow
helpers). The external editor runs through `bubbletea.Exec`
(`tui/tea/external_editor.go`'s `defaultExternalEditor`), so Bubble Tea releases
and restores the terminal around the editor and repaints on return; the
`KeepScreen` repaint has no Go code to modify. The observed end state - Codex's
screen is restored after the editor exits - is therefore provided by the
framework rather than by Go, and the Rust snapshot that pins it is recorded in
the manifest.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass closes the remaining Go-visible half of `#48200` together with the
safety-buffering wire it feeds.

Tracing the Rust oracle showed that Go's safety buffering was wrong in three
ways: it read the wrong wire key, it let the payload control a flag Rust marks
`#[serde(skip)]`, and it never read the two response headers that supply the
fallback model. It also only looked for safety buffering on `response.metadata`
events, while Rust checks every event.

### sync228: the safety-buffering wire, visibility and header treatment

- **`model/responses_headers.go`** ports `codex-api/src/responses_headers.rs`
  (#48200): `jsonHeadersToHTTPHeaders` converts a Responses event's JSON
  `headers` object with RFC 7230 token names and Rust's
  `HeaderValue::from_str` value rules (string/number/bool values only,
  unsupported or invalid names and values dropped).
- **`model/safety_buffering.go`** ports `codex-api/src/safety_buffering.rs`: the
  `x-codex-safety-buffering-enabled` and
  `x-codex-safety-buffering-faster-model` headers and
  `safetyBufferingTreatmentFromHeaders` / `...FromJSONHeaders`, which report
  whether a treatment exists and what model it carries.
- **`model/responses_stream.go`**:
  - the payload's faster model now comes from Rust's wire name `retry_model`
    first (the server's actual key, previously ignored), with Go's
    `faster_model`/`fasterModel` aliases kept;
  - `use_cases` and `reasons` are required arrays of strings, mirroring Rust's
    `SafetyBuffering` struct, so a malformed payload produces no event and an
    empty pair still does;
  - `show_buffering_ui` is no longer read from the payload (Rust marks it
    `#[serde(skip)]`) and is always set, so a delivered payload always asks the
    UI to show;
  - when the payload omits `retry_model`, the header treatment's model is used
    (`retry_model: null` suppresses that fallback, as in the Rust matrix);
  - the treatment is threaded from the HTTP response headers for a streamed
    response (`parseResponsesStreamWithTreatment`) and refreshed from each
    WebSocket event's JSON `headers` before the event is applied;
  - safety buffering is now detected on *every* event (Rust's
    `safety_buffering_for_event`), not only on `response.metadata`, so the
    top-level `safety_buffering` field on an ordinary event reaches the
    app-server and TUI.

Tests: the header treatment matrix (neither/enabled-only/model-only/both/blank
model), the JSON-header conversion matrix (invalid names, object/array/null
values, control characters and padded values dropped; the converted headers then
feed the treatment lookup), Rust's retry-model matrix (omitted falls back to the
header, `retry_model` wins, explicit null suppresses the fallback, a non-string
value is rejected), the required-array rules, the forced visibility flag for both
payload forms, and an end-to-end WebSocket test mirroring Rust's
`websocket_safety_buffering_event_controls_visibility_when_header_disables_it`
(the event header supplies `gpt-fast-header` and the payload cannot disable the
UI).

### Verification (sixty-fourth pass)

- `go test ./model/ -count=1` is green.
- `go test ./appserver/ ./app/ ./tui/... ./exec/ ./turn/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `58670eeac4`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass closes the rest of the Go-visible `#48174`/api_bridge usage-limit contract
that sync217 left recorded.

### sync229: the usage-limit error's evidence and the analytics HTTP-status rule

- **The 429's rate-limit headers reach the client.** Rust's `map_api_error`
  parses the response's rate-limit headers into
  `UsageLimitReachedError.rate_limits`, and the turn loop then refreshes the
  session snapshot from them (`core/src/session/turn.rs`'s
  `sess.update_rate_limits`). Go returned the 429 before ever emitting
  header-derived events, so a usage-limit failure left the client's rate-limit
  display stale. `emitUsageLimitErrorHeaderEvents` now emits the same
  header-derived rate-limit events on the error path - for a usage-limit error
  only, as Rust attaches them only there - for both the streamed and the
  non-streamed request paths (the stream path now builds its handler before the
  status check).
- **The HTTP `usage_not_included` classification.** Rust's 429 branch classifies
  `error.type == "usage_not_included"` as `CodexErr::UsageNotIncluded`, the same
  kind Go's streamed `response.failed` path already reported for that code;
  `responsesHTTPError` now does too, with no HTTP status on the detail (Rust's
  `http_status_code_value` reports none for it).
- **The analytics HTTP status now follows Rust's rule.** Rust's
  `CodexErrorDetails::http_status_code_value` only reports a status for
  `RetryLimit`, `UnexpectedStatus`, `ConnectionFailed`, `ResponseStreamFailed`
  and `FlexUnavailable` (429); every other detail - usage limits, quota,
  usage-not-included, context window, policy refusals - reports `null`.
  `turnAnalyticsErrorFieldsFromAPIError` previously copied the error's HTTP
  status for every kind, so Go's turn and compaction analytics reported 400/429
  where Rust reports `null`. `analyticsErrorHTTPStatus` now applies the same
  rule, and the three test expectations that pinned the old behaviour
  (`ErrorInvalidRequest`, `ErrorMisalignmentPolicyViolation`, `ErrorBioPolicy`
  all carrying 400) were updated to the Rust-faithful `null`, with the reason
  recorded at each site.

Tests: the runner-level usage-limit case (a 429 `usage_limit_reached` with
primary-window headers emits one rate-limit event carrying the window while the
error keeps its classification; an unrelated 429 emits none), the
`usage_not_included` classification matrix, and the existing analytics matrices
under the corrected status rule.

### Verification (sixty-fifth pass)

- `go test ./model/ -count=1` is green.
- `go test ./appserver/ ./app/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `58670eeac4`.

### sync230: the wrapped-WebSocket usage limit refreshes the snapshot too

Rust's wrapped-WebSocket error mapping builds `ApiError::Transport(Http { status,
headers, body })`, so `map_api_error` reads the event's `x-codex-*` JSON headers
and a WebSocket-delivered usage limit refreshes the session's rate-limit snapshot
exactly like the HTTP path. Go classified the event but dropped its headers.

- **`model/responses_agent.go`**: `websocketEventHTTPHeaders` converts the event's
  JSON `headers` through the shared `jsonHeadersToHTTPHeaders`, and the
  usage-limit branch emits the header-derived rate-limit events before returning
  the classified error.

Test: a WebSocket `error` event with `status: 429`,
`error.type: usage_limit_reached` and primary-window headers returns the
usage-limit error with its window and emits exactly one rate-limit event carrying
the window's percent, duration and reset time.

### Verification (sixty-sixth pass)

- `go test ./model/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass re-verified two recorded items against the current source and landed the
WebSocket half of the turn-state contract.

### sync231: the WebSocket turn state

Rust's ModelClientSession keeps one turn state per turn: `connect_websocket`
records the handshake response's `x-codex-turn-state`, `ResponseEvent::turn_state`
records the one a `response.metadata` event carries in its JSON headers (the
`OnceLock` keeps the first value), and every later request's client metadata
sends it (`client.rs`'s `turn_state.get()`).

- **`model/responses_agent.go`**: after a successful WebSocket dial the runner
  remembers the turn state from the handshake response headers, and for a
  `response.metadata` event it remembers the state from that event's JSON
  `headers` (through the shared `jsonHeadersToHttpHeaders`). Go previously
  learned the state only from HTTP response headers, so a WebSocket-delivered
  state was dropped and the next dial in the turn carried nothing.

Tests: the handshake case (the first dial fails before any event, so the retry
dial must carry the handshake's state, and a later metadata event cannot replace
it - Rust's first-wins `OnceLock`), and the metadata case (a handshake without
the header followed by a `response.metadata` carrying it, so the next dial in the
turn sends the event's state).

### Correction: the custom-profile metadata carveouts are not a divergence

sync218 recorded that Rust applies `append_default_read_only_project_root_subpath_if_no_explicit_rule`
to custom `[permissions]` profiles while Go does not. Re-checking the current
source shows that record was wrong:

- `FileSystemSandboxPolicy::restricted(entries)` is a plain constructor
  (`protocol/src/permissions.rs`), and the four appends live only in
  `FileSystemSandboxPolicy::workspace_write(...)`, which is used by
  `BUILT_IN_WORKSPACE_PROFILE` (`core/src/config/permissions.rs`) and the legacy
  `SandboxPolicy::WorkspaceWrite` conversion -
  the two sites Go already mirrors (`builtinPermissionProfileBuilder(":workspace")`
  and `rustPermissionProfileWireFromPermissionProfile`).
- A custom profile compiles through
  `compile_resolved_permission_profile`, which starts from
  `restricted(Vec::new())` and appends only the profile's own entries, and
  `with_materialized_project_roots_for_path_uris` materializes existing entries
  only - so Rust's custom profiles carry no metadata carveouts either, exactly
  like Go's.

One unpinned nuance stays recorded: a custom profile with `extends = ":workspace"`
inherits the parent's *special* entries in Go (the builder clone), while Rust
projects the builtin into TOML and drops entries with `skips_missing_path()`
(which the metadata carveouts use). No Rust test pins that case, and Go's
behaviour is the stricter one, so it is left as is rather than weakened.

### Triage: `#48073`'s credential-broker snapshot grant has no consumer yet

Rust's unified-exec launch composes `internal_permissions` from the metrics
sidecar plus `snapshot_read_permissions(snapshot_path, profile, sandbox_cwd)`,
consulted only while `CREDENTIAL_BROKER_ACTIVE_ENV_KEY == "1"`: the replayed
snapshot gets a read-only grant unless the launch's profile can already read it
with the sandbox cwd. Go has the sidecar half (`tool/shell_executor.go`'s
`internalPermissions`) but not the snapshot half, because Go does not replay a
snapshot for brokered launches at all: `MaybeWrapShellLCWithSnapshot` returns the
command unchanged when the broker is active, with a comment recording that Rust
additionally wraps brokered captures and replays protected environment files
around them.

Landing the grant alone would widen the sandbox's readable set for a snapshot the
launch never reads, so the whole brokered-snapshot program stays recorded
together: the creation-side broker handling (`core/src/shell_snapshot.rs`'s
env-key insert/remove and the `active == "1"` branch), the wrap's brokered rules
(`maybe_wrap_shell_lc_with_snapshot`'s `-c` acceptance, session-shell reuse and
`-lfc`/`-fc` flags), and then the grant with its merge into the launch's internal
permissions.

### Verification (sixty-seventh pass)

- `go test ./model/ -count=1` is green.
- `go test ./... -count=1` is green (99 packages, zero failures).

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `58670eeac4`), so this
pass starts the recorded `codex-history` program: the retained-context data path
that gives sync215's model, sync219's renderer and sync222's prompt option a
production route.

Rust's retained evidence is host-owned: the session records it when the host
accepts an input, each compaction persists the bounded snapshot in its
`CompactedItem`, and both the transport and the Guardian sections read it back
from the checkpoint plus later evidence. Go had none of that - no
`retained_context` on the compacted record, no session-owned snapshot, and no
reviewer consumer - so the retained user-instruction section could never render.

### sync232: the retained-context checkpoint, store and review wiring

- **`rollout`**: `CompactedEvent` gained `retained_context` (Rust
  `CompactedItemWire::retained_context`),
  `Recorder.AppendCompactedWithContext` writes a checkpoint with the snapshot
  (with `AppendCompacted` kept as the no-snapshot form), and
  `CompactedRetainedContext(lines)` reads the newest checkpoint's snapshot - the
  evidence a resumed thread restores before later evidence is replayed.
- **`appserver/retained_context.go`**: a per-thread store,
  `RuntimeRouter.retainedContextForThread`, seeds the thread's context from its
  newest checkpoint and then records every accepted user message the thread's
  history still holds, in order. Re-recording is idempotent (Rust's id-based
  dedup), so the store can be refreshed on every resolution, a restarted process
  rebuilds the same evidence from the durable checkpoint, and a compaction can
  never drop it from the reviewer's view. Resolutions return a copy so a
  concurrent review renders while later resolutions keep recording; the thread's
  in-memory evidence is dropped with its runtime
  (`forgetThreadRetainedContext`), matching Rust's `Arc` drop.
- **`appserver/turn_runtime.go`**: `appendRuntimeCompacted` persists the store's
  snapshot with every checkpoint, so the evidence survives the history rewrite
  the same compaction performs.
- **`appserver/guardian_reviewer.go` / `agent_runtime.go`**: the model reviewer
  gained a `retainedContext` callback and passes the resolved snapshot into every
  prompt build (`state.BuildPromptWithOptions.RetainedContext`), so the review
  now renders the retained user-instruction section between the sender evidence
  and the transcript, as Rust's registry order requires.

Tests: the rollout round trip (a checkpoint with a snapshot reads back, and one
without carries none), the store (accepted user messages become ordered evidence,
assistant text never does, repeated resolution dedupes by message id, an unknown
thread has none), compaction survival (the history is replaced, the checkpoint
carries the snapshot, and a fresh router rebuilds the original instruction), and
the reviewer prompt (the marked section with the order label and role prefix, and
no section when a thread has no evidence).

Remaining for this program (recorded): the other producers (retained assistant
messages, verified answers recorded when `request_user_input` is accepted, sender
deliveries) and the sparse `RetainedContextEvent` rollout item Rust writes
between checkpoints; the source identities (`RetainedSource`, the
`retained_source`/`guardian_sources`/`guardian_source_order_guidance` harness
metadata) that carry delivery proof and drive redelivery/deduplication; the
`inherited_user_message` provenance for adopted parent instructions, which is why
the reviewer still resolves retained evidence only for non-subagent threads (a
worker keeps the root-conversation section rather than mislabeling inherited
instructions as its own); and the `CodexHarnessMetadata` type itself, since Go
persists harness metadata as an untyped map.

### Verification (sixty-eighth pass)

- `go test ./appserver/ ./rollout/ ./session/ ./compact/ ./exec/ ./app/ ./parity/ -count=1` is green.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-fifth pull): `63eb71c9da` (one commit)

`git fetch` advanced `58670eeac4..63eb71c9da95a6e4deaf34ee9340c88a7cac9a65`:

| Commit | Subject | Lane |
|---|---|---|
| `63eb71c9da` | Preserve late result metadata for truncated code-mode calls (#48222) | executed-tool-calls (core) |

`go test ./parity/ -count=1` is green at the new pin: the commit edits existing
sources and test suites, so no fixture root, inventory, critical-file hash,
snapshot total or precomputed export changes.

This lane was the oldest recorded Go-visible gap in the repo. Go's recorder
(`turn/executed_tool_calls.go`, last synced at `sync135`/#46712) kept attached
calls only in the *pending* inventory and dropped them on commit, so it modelled
neither Rust's `RetainedToolCalls` binding nor the origin/runtime cell split it
depends on. #48222 is the third Rust pass over that model (#47714/#47758/#47957
preceded it), so this pass lands the binding itself together with #48222's rules
instead of re-deriving them.

### sync233: the retained output binding and late truncated result metadata

- **Retained bindings (`retainedToolCallBinding`).** `AttachPendingToPrompt`
  now names the output it attaches to (`executedToolCallGroupAttachment`), and
  `CommitAttachment` moves that inventory into `retained[outputCallID]` instead
  of dropping it. Every later request re-emits the binding
  (`attachRetainedBinding`), so an output that was attached while its cell was
  still running, or before its result arrived, keeps its inventory instead of
  losing it on the next sampling request. Bindings carry Rust's two identities
  (`originCallID`, the exec/wait call ID written as the item's metadata
  `cell_id`; `runtimeCellID`, the Code Mode handle used for input matching) and
  the `callIndexByID` / `truncatedCallIndexByID` / `lateTruncatedIndices`
  indices Rust uses to address one call.
- **Late result backfill.** `RecordToolResultMetadata` first checks the pending
  inventory, then the retained bindings: a full-argument call found by
  `callIndexByID` is replaced in place, and a truncated call found by
  `truncatedCallIndexByID` is updated only when exactly one binding claims the
  ID, recording the index in `lateTruncatedIndices`. A contradictory second
  claim leaves the result unattributed (Rust
`record_tool_result_metadata`'s `candidates.next().is_some()` guard).
- **Ambiguity invalidation.** `invalidateUntrustedTruncatedBindings` runs before
  any retained calls are cloned into a request: a duplicated output, or an input
  that no longer identifies the same execution, marks the whole runtime cell's
  attribution ambiguous, clears its late-backfilled evidence
  (`clearLateTruncatedMetadata`) and stops admitting more. The same clearing
  happens on origin/cell invalidation, on a repeated nested invocation ID
  (`seenNestedIDs`), and when a new nested call arrives after the dispatch gate
  closed.
- **Cell lifecycle.** `StartCell` (the exec path) always observes the runtime
  handle, so a reused runtime ID releases the previous execution's pending
  calls, output mappings and late bindings (`dropReusedCell`); `RegisterCell`
  (the wait path) registers into an already started cell. `FinishCell` marks the
  dispatch gate closed and keeps a cell whose truncated calls are still eligible
  for backfill. A repeated nested invocation ID now replaces its pending attempt
  instead of consuming the pending budget twice, and truncation/overflow now
  records Rust's `CellCompletion::Incomplete` (a distinct `incomplete` flag)
  rather than revoking the cell's identity, so a truncated call's binding stays
  valid while the inventory is reported incomplete.
- **Metadata cell id corrected.** The item's metadata `cell_id` now names the
  originating exec/wait call, not the runtime handle, matching Rust
  (`seen_ids.rs`'s "Metadata cell_id identifies the originating exec call, not a
  runtime handle", `compact_tests.rs`'s `start_cell(&cell, "exec")` →
  `set_tool_call_cell_id("exec")`, and
  `code_mode_terminated_empty_cell_has_complete_tool_inventory`, where the
  `call-2` wait output reports `cell_id == "call-1"`). Go previously wrote the
  runtime handle; the recorder, dispatcher and capacity tests were updated with
  the Rust citations.

Tests mirror Rust's `request_metadata_tests.rs` additions: late metadata across
retries and compaction for two truncated calls in one cell; the eight-case
matrix over `result_before_wait` / `call_before_wait` / `close_before_wait` with
a following second wait; the ambiguity matrix (duplicate output, wrong input,
wrong sibling input, repeated ID, reused cell, dispatch after close); and the
reused-runtime-cell / repeated-pending-ID capacity behaviour.

Recorded as remaining for this lane: Rust's `retry_cache`
(`ExecutedToolCallCache`) that lets a failed sampling attempt replay the same
snapshot without re-draining the cell (`attach_to_prompt`'s `retry_cache`
parameter), the metadata-budget re-derivation in
`bound_executed_tool_calls_for_prompt_prioritizing_recent`
(Go bounds in `model.BoundExecutedToolCallsForPrompt` instead), and Rust's
`UsageErrorBody`-style request-level shedding of oversized late metadata before
it reaches the wire. Rust captures raw result metadata and bounds it at request
time; Go bounds it at capture (`model.NewToolResultMetadata`), so an oversized
late snapshot becomes the omission marker earlier than Rust's request budget
would shed it.

### Verification (sixty-ninth pass)

- `go test ./turn/ -count=1` is green, including the new #48222 regression suites.
- `go test ./model/ ./tool/ ./exec/ ./codemode/ -count=1` is green.
- `go test ./parity/ -count=1` is green at `63eb71c9da`.
- `go test ./... -count=1` recorded below.

---

## Follow-up pass (same day, twenty-sixth pull): `5f3180c793` (one commit)

`git fetch` advanced `63eb71c9da..5f3180c79374bd9440db733e23ead8bc9f62d64b`:

| Commit | Subject | Lane |
|---|---|---|
| `5f3180c793` | Preserve model and access program pairs during compaction (#48224) | turn-level access programs (core) |

### sync234: the static layer is re-pinned to `5f3180c793`

`go test ./parity/` failed on the new checkout in two places:

- **Fixture root**: `core/tests/suite` grew to 259 files for the new
  `core/tests/suite/compact_program_tests.rs` (#48224), recorded in
  `rust_fixture_manifest_test.go` (with the file added to the root's required
  list).
- **Vendored precomputed export**: `app-server-exports-stable.json.zst` changed
  (155387 → 155425 bytes) and was re-vendored from the checkout. Decompressing
  the old and new exports shows exactly one difference, in the **internal**
  schema's `RolloutLine.json`: the previous-turn settings definition now carries
  `cyber_access_program` next to `model` and `comp_hash`. The stable
  `json_schema` and `typescript` sets are byte-identical, so `turn/start` is
  unchanged. `appserver/schema_test.go`'s artifact pin moved to
  `8691e8ec516b44dcd86e495290a88e10c4447321e25baab4001304410aa05bc4`.

### Triage: `#48224` refines a lane Go has not ported

The commit has three functional halves, all downstream of **a turn carrying a
cyber access program**, which Go does not model:

- `session/turn.rs` / `session/rollout_reconstruction.rs` persist and restore
  `PreviousTurnSettings.cyber_access_program` (the new field in
  `history/src/compaction_resume_metadata.rs`).
- `maybe_run_previous_model_inline_compact` restores the previous turn's program
  after `with_model`, so compaction uses that turn's model/program pair.
- `compact.rs` passes the selected program into the local compaction prompt
  (`Prompt.cyber_access_program`).

Go's evidence of absence, checked against the current source:

- `appserver/protocol.go`'s v2 `TurnStartParams` has no `cyberAccessProgram`
  member (its `DaybreakEnabled` comment records the distinction explicitly:
  "It does not select a turn's cyber access program"), so no turn ever selects
  one.
- `runtimeTurnContext` / `rollout.TurnContextRecord` / `TurnContextSettings`
  carry only `model` and `comp_hash`; there is no program to persist or restore.
- `runPreviousModelInlineCompact` rebuilds the previous model's turn context
  without a program, and the local compaction prompt has no
  `cyber_access_program` field: `git grep` finds the access-program surface only
  in the catalog metadata (`model/catalog.go`, `model/api.go`) and the v2 suite
  manifest (`cyber_access_program`, owner "safety, appserver"), never in request
  building.
- The Responses request body has no `access_programs` field and there is no
  `cyber_access_program::for_auth` (ChatGPT-auth-gated) equivalent, so Rust's
  rejected model/program pair cannot be constructed by Go today.

`appserver/appserver_v2_suite_manifest_test.go` already records the
`cyber_access_program` module as a tracked but unported surface. The whole lane
stays recorded together - the v2 `turn/start` selection, the ChatGPT-auth gate
and request-body `access_programs`, the turn context field, the rollout
persistence this pass re-pinned, and the compaction program - because landing
only the persistence half would write a field nothing selects or reads. The
static layer is re-pinned now so the vendored export and fixture manifest stay
honest while the component is unported (the same treatment as the warnings
viewer in #48205/#48206).

### Verification (seventieth pass)

- `go test ./parity/ -count=1` is green at `5f3180c793`.
- `go test ./appserver/ -run 'Precomputed|Schema|Export' -count=1` is green.
- `go test ./... -count=1` recorded below.

### Full-suite confirmation (this pass)

- `go test ./... -count=1` is green across all 99 packages, zero failures, at
  `5f3180c793` and with `590dd1f0` pushed to `origin/main`.

## Unlanded work after this pass

The recorded lanes still open, in the order the workflow's step 6 would take
them:

1. The turn-level cyber access program lane (`#48224`'s subject, plus the v2
   `turn/start` selection, the ChatGPT-auth gate, the request-body
   `access_programs`, and the turn-context/rollout field).
2. The `codex-history` retained-context producers (assistant messages, verified
   answers, sender deliveries, source identities, typed `CodexHarnessMetadata`)
   left open by `sync232`/`sync215`.
3. The TUI warnings viewer (`#48205`/`#48206`) and the remaining TUI lanes.
4. The `#48174` usage-limit `plan_type`/`resets_at` fields.
5. The brokered-snapshot program recorded under `#48073`.

---

## Continuation pass (same day, no new upstream commits)

`git fetch` reports no new commits on `origin/main` (still `5f3180c793`), so this
pass closes recorded lane 1: the turn-level cyber access program. Go had the
catalog half of `#44893` (the `availableAccessPrograms` metadata and the
app-server summary) but nothing selected a program for a turn, so every
downstream field - including `#48224`'s model/program pair preservation - had no
Go surface.

### sync235: the turn-level cyber access program (#44893 and #48224)

- **The request body (`model`).** `model/access_programs.go` ports Rust
  `core::cyber_access_program::for_auth` and `codex_api::AccessPrograms`: the
  body field is `access_programs: {"cyber": "<core name>"}`, it is sent only for
  an authenticated human ChatGPT account, and an absent program preserves the
  backend's automatic behavior. The gate is Rust's
  `CodexAuth::is_chatgpt_auth` (`AuthMode::has_chatgpt_account`: `chatgpt`,
  `chatgptAuthTokens`, `personalAccessToken`), which is *not* the pre-existing
  `model.authHasChatGPTAccount` predicate (that one also accepts Agent Identity,
  i.e. `uses_codex_backend`), so the new gate is its own function. Known
  programs are the three catalog values; an unknown selection is dropped, since
  Rust's v2 enum is validated while deserializing. `responsesAgentRequest`
  gained `access_programs`, both runners (WebSocket and HTTP/SSE) populate it
  from `AgentRequest.CyberAccessProgram`, and both WebSocket payload builders
  emit it, mirroring Rust's `ResponsesApiRequest` /
  `ResponseCreateWsRequest` pair.
- **The turn selection (`turn`).** `TurnStartParams` gained the app-server wire
  selection `cyberAccessProgram` (camelCase: `standard`, `daybreakBlue`,
  `daybreakRed`) with `CoreValue()` performing Rust's explicit
  `impl From<…>` mapping onto the core snake_case program, plus an internal
  `CoreCyberAccessProgram` (json "-") for internally started turns. The turn
  loop's `AgentLoopRequest` and Rust's `Prompt` counterpart
  (`model.AgentRequest`) carry the core value.
- **The provider gate (`appserver`).** `appCyberAccessProgramForTurn` mirrors
  Rust's turn-context construction: the selection is recorded only when the
  turn's provider is the OpenAI provider. The regular turn's sampling request
  carries it; a review turn and the guardian reviewer's request do not, matching
  Rust (`session/review.rs` sets `cyber_access_program: None` for the review
  turn context and `guardian/review_session.rs` builds the reviewer from
  `new_default_turn`).
- **Persistence and restore.** `rollout.TurnContextRecord` gained
  `cyber_access_program` (snake_case, omitted when unset) and
  `TurnContextSettings` now returns it, so `runtimePreviousTurnSettings` reports
  the recorded model/hash/program triple. `recordRuntimeTurnContext` writes the
  resolved program with the model, which is what makes `#48224`'s
  resume/fork/rollback/checkpoint replay keep the pair.
- **Compaction.** `runtimeCompactRequest` gained an explicit program: an attempt
  that fixes it keeps it verbatim (including an explicitly absent one), while a
  nil value resolves the thread's latest turn context. The previous-model
  pre-turn compaction passes the previous turn's recorded program
  (`previousModelCompaction.PreviousProgram`) and the fallback retry passes the
  current turn's, mirroring `maybe_run_previous_model_inline_compact`'s
  `previous_model_turn_context.cyber_access_program = previous_turn_settings.…`;
  the post-turn, pre-turn and mid-turn auto compactions pass the current turn's,
  and `agentCompactRunner` puts the resolved program on the compaction
  `AgentRequest` (Rust's `Prompt.cyber_access_program` in `compact.rs`).
- **Subagents.** A spawned or continued child turn inherits the initiating
  turn's program (`runtimeAgentController.cyberAccessProgram` feeding the spawn,
  send-input and message param builders), gated again by the child's own
  provider, mirroring the `cyber_access_program` field Rust passes through
  `SpawnAgentOptions` / `SendInputOptions`.

Tests: the auth-gate matrix (chatgpt / chatgptAuthTokens / personalAccessToken
versus api-key / agent-identity / bedrock / no auth, unknown and absent
programs) plus an end-to-end HTTP request-body check that only a ChatGPT account
gets `access_programs`; the wire/core mapping matrix and the params JSON round
trip; the provider gate matrix including the inherited core value; the
compaction pair (fixed, explicitly absent, and recorded-turn resolution) and the
`agentCompactRunner` request; the spawned-child inheritance; and the rollout
round trip for a present and an absent program.

Recorded as remaining for this lane: Rust's daemon interrupted-turn snapshot
(`app-server-transport/src/daemon_recovery.rs`'s `InterruptedTurn.
cyber_access_program`, restored by `daemon_continuation`/`daemon_snapshot`). Go
has no `RecoverySnapshot`/`codex-interrupted-v1` mechanism at all (`git grep`
finds neither), so there is nothing to persist into yet; the whole daemon
recovery snapshot stays recorded with its lane. Go's `exec` CLI and TUI keep
sending no program, matching Rust (`exec/src/lib.rs` and
`tui/src/app_server_session.rs` both pass `None`).

### Verification (seventy-first pass)

- `go test ./appserver/ ./turn/ ./model/ ./rollout/ ./exec/ ./compact/ -count=1` is green.
- `go test ./appserver/ -count=1` is green in isolation (139.9s).
- `go test ./... -count=1` is green (99 packages, zero failures). One earlier
  99-package run reported the load-sensitive
  `appserver.TestOtelProviderReloadsAfterAccountChange`, which is green in
  isolation, in the full appserver package run, and in the later full-suite runs
  (it waits on a localhost OTLP collector while every package builds and runs in
  parallel).
