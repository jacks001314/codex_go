# Codex Go upstream alignment plan - 2026-07-29

## Baselines

- Go repository baseline: `3252456a54fda016ee6fcb85b05e2168439147a1` with the
  uncommitted `bd2de422aa..f2bee854a7` alignment slice retained in the worktree.
- Rust repository: `D:\qax\reagent\dev\git\codex`.
- Previous audited Rust head: `f2bee854a73666e1c3e922a853dda591b1a25fcf`.
- Current Rust head after `git pull origin main`:
  `fa1d4c40d0` (`Gate paginated thread history on the state database`).
- Current incremental range: `f2bee854a7..fa1d4c40d0` (11 non-merge commits).
- Formal `parity.json` advanced from Rust `9d82334302` to `fa1d4c40d0` only after
  the current Rust binary and the approved SDK differential matrix passed.

## Required work

### A. Close the 2026-07-28 deferred implementation

- [x] Replace the placeholder local OpenAI file uploader with authenticated,
  streaming, route-aware upload behavior and reuse its HTTP client.
- [x] Route curated plugin catalogs atomically by authentication/provider mode and
  refresh existing thread MCP runtimes after catalog identity changes.
- [x] Load cloud-managed sandbox profiles for `codex sandbox
  --include-managed-config` from the real cloud config source.
- [x] Align background subagent-picker refresh, concurrent MCP/plugin
  recommendations, and terminal-focus input preservation with deterministic TUI
  tests and PTY evidence where applicable.

### B. Align `f2bee854a7..fa1d4c40d0`

- [x] `8e271dc02b`: record locally observed executed tool calls in private response
  metadata, reject forged metadata, and enforce 8 KiB per-call and 32 KiB
  prompt-wide serialized bounds.
- [x] `d9e1c9cd55`: do not block a turn on optional MCP startup; wait only for
  servers required by explicit plugin/skill input and preserve cached catalogs.
- [x] `e597169e9a`: keep path-to-thread and thread-to-path agent registry identities
  bijective across resume, replacement, release, and root registration.
- [x] `a68d0a74bd`, `f6160ca5b3`, `03748ad5e1`: share one context-scaled skill
  metadata budget across host and executor catalogs and emit host pressure
  warnings from world state.
- [x] `7cde2323f3`: renew a matching models-cache ETag only after half the cache TTL
  has elapsed.
- [x] `84ccb2938b`: resolve MCP tool catalogs concurrently while retaining stable
  server order and required-server semantics.
- [x] `bb1af235ea`: load thread titles concurrently with instruction and skill/plugin
  session warmup, or prove that Go has no corresponding serial startup waits.
- [x] `cf7e9cfe6a`: support `self_serve_business_prolite` in auth, account, rate-limit,
  notification, display, and workspace-account behavior.
- [x] `fa1d4c40d0`: gate paginated history materialization, listing, fork, and delete
  behavior on the state database, or prove the Go store's architecture makes the
  state-database gate inapplicable without losing protocol behavior.

Evidence for completed rows: focused tests in `model`, `turn`, `mcp`, `agent`,
`prompt`, `appserver`, `auth`, and `tui/status`; race tests cover MCP startup,
agent registry identity replacement, and model-cache renewal. The MCP catalog
path already resolves all server inventories concurrently and restores results
by stable server index; the new optional-startup gate reuses that path.

The deferred OpenAI file slice is now implemented end to end: Codex Apps
`openai/fileParams` metadata is retained, raw provided-file schemas are converted
to model-visible local-path schemas, and declared arguments are uploaded through
authenticated create, streaming blob PUT, and retrying finalize requests using
the turn's shared HTTP client. Focused and race tests cover optional field
derivation, hook propagation, retries, and signed-URL-safe diagnostics.

The curated plugin slice now selects `openai-curated`,
`openai-curated-remote`, or `openai-api-curated` from the active auth mode and
configured provider, including ambient Bedrock and ChatGPT-authenticated
Bedrock. The projection is shared by listing, installation, capabilities,
hooks, skills, command attribution, and MCP contributions. Explicit API
marketplace manifests resolve local sources from the repository root, plugin
MCP definitions retain source attribution below user-config priority, and
curated/remote materialization clears skill caches and rebuilds already-loaded
thread runtimes. The end-to-end regression starts a thread before the API
bundle exists, materializes it asynchronously, and calls its MCP tool from that
same thread; focused, full-package, and race tests pass.

The cloud-managed sandbox slice now bootstraps the configured auth store and
ChatGPT backend only when both an explicit permission profile and
`--include-managed-config` are present. Eligible workspace auth fetches
`/wham/config/bundle` with the active account headers, validates structured TOML,
writes the Rust-compatible identity-scoped HMAC cache, merges enterprise config
and requirement layers, enforces `allowed_permission_profiles`, and compiles the
managed profile before sandbox execution. A real HTTP integration test verifies
the request headers, network-enabled profile, cache artifact, and cache hit on a
second load; the default path verifies zero cloud requests. Full `config`, `auth`,
and `app` package tests pass.

The remaining TUI/performance slice now shows cached subagents immediately,
coalesces one background refresh, preserves the selected agent, and rejects
stale results after thread/session replacement. Turn setup fetches suggested
plugins and prepares the MCP catalog concurrently under the active turn context;
interrupting during either preparation path emits an interrupted terminal state
without sampling the model. Real HTTP plus stdio-MCP integration tests prove both
branches overlap, and focused race tests cover the shared app-catalog dependency.
On native Windows, `TestRealPTYFocusGainedPreservesQueuedKey` sends blur, focus,
and `focus-key-preserved-7Q9` in one production ConPTY write and verifies the
complete text is rendered. Full `plugin`, `appserver`, `tui/tea`, and `tui/tui`
package tests pass.

`bb1af235ea` is structurally inapplicable: Go's `session.Record` stores `Title`
beside all other thread metadata, and thread start/resume performs one record
read before instruction and skill warmup; there is no separate title store or
serial title lookup to parallelize. `fa1d4c40d0` is likewise a dual-store Rust
guard: Go's paginated history references, item ordinals, and turn ordinals are
all durable fields in the same atomically written JSON store. The
`TestStorePaginatedHistoryNeedsNoSidecarStateDatabase` regression test exercises
fork, materialized reads, pagination, reference-safe deletion, and verifies that
no sidecar state artifact is created.

### C. Close historical evidence gaps

- [x] Re-audit every dated plan and classify every unchecked or deferred row as
  implemented, directly equivalent, or not applicable with source/test evidence.
- [ ] Resolve `parity.json`'s `cross-platform-release-gates` partial status using
  native platform evidence; cross-compilation alone is not sufficient.
- [x] Build or locate a Rust binary matching the current source head, record both
  binary hashes, and run the approved official TypeScript SDK differential matrix.
- [x] Re-run the slash-command PTY matrix whose current-source Rust baseline was
  blocked by Windows symlink privilege.

Historical audit closure: the only older unchecked implementation row was the
2026-07-24 Rust-internal analytics/crate/test-helper bucket, now classified not
applicable unless a future change gains a Go observable contract. The 2026-07-28
effect-only rows now have direct tests, and `/import` has response-first
background completion parity. The current-source ConPTY gate uses Rust
`fa1d4c40d0` and asserts `/status` plus `/mcp`, including model/reasoning,
permissions, collaboration mode, startup session, and command history fields.

The current Rust binary was built from `fa1d4c40d0` and reports `codex-cli
0.0.0`; its SHA-256 is
`ADD18D279C921FCE9A4538A05D11EC65E6AB72F9A1E4345FB4309C7D6CAFEBE6`. The final
Go candidate also reports `codex-cli 0.0.0`; its SHA-256 is
`6F8A0AF4AF30F759A2114AF534157A6D3BA8BAD754064E1418F0EDF287A1364A`.
The official TypeScript SDK distribution hash was
`0970CDD99525E6675804B81A3DC7197A682CFEDA140A929839BDBFD3B6F02434`.
The final approved matrix completed 42 of 42 scenarios with zero mismatches or
infrastructure failures; its suite summary is
`sdktests/artifacts/suites/2026-07-28T215319021Z-parity/suite-summary.json`.

The earlier long-context failure was a harness bug: both implementations emitted
the same structured JSON object with different key insertion order. The comparator
now uses structural equality, its regression test passes, and the real five-turn
scenario passes in the final matrix. `resume-tool-interrupted-command` remains an
explicit opt-in Windows diagnostic rather than a differential gate: current Rust
twice returned `AbortError`, allowed the canceled PowerShell child to create
`interrupted-marker.txt`, and could not resume, while Go canceled the descendant,
kept the workspace clean, and resumed successfully. Reproduction artifacts are
`sdktests/artifacts/2026-07-28T211638871Z-resume-tool-interrupted-command` and
`sdktests/artifacts/2026-07-28T214305841Z-resume-tool-interrupted-command`.

Local release evidence is current: native Windows platform packages pass, the
production ConPTY `/status` and `/mcp` comparison passes against Rust
`fa1d4c40d0`, focus input preservation passes, 47 relevant packages compile as
Darwin/arm64 test binaries, Darwin `network` and `sandbox` vet passes, and the
macOS workflow passes `actionlint`. The workflow now contains Darwin-only native
integration tests for Keychain root loading, a real Unix-socket HTTP round trip,
and seatbelt enforcement of workspace, outside-root, and metadata writes. The
cross-platform checkbox remains open until that workflow runs on a native macOS
runner; cross-compilation is recorded only as preflight evidence.

## Verification gates

1. Focused deterministic tests for every row above.
2. `go test -race` for shared registry, MCP startup/catalog, cache, and runtime
   refresh paths.
3. Native Windows platform tests plus the repository's Linux/macOS workflow gates.
4. `go test ./... -count=1`, `go vet ./...`, and `git diff --check`.
5. Official TypeScript SDK smoke followed by the approved parity matrix, using the
   same prompt, model, configuration, isolated homes, and pristine fixtures for
   Rust and Go.

## Completion rule

This plan is complete only when every checkbox has authoritative current-state
evidence. A skipped build, missing native runner, Go-only unit test, or inferred
architecture equivalence is not parity evidence.
