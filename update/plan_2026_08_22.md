# Codex Go update alignment plan - 2026-08-22 (sync23 #39847-#40031)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` frozen target: `bd19459358` (sync22) -> `970b7f2ff4` (52 new commits, #39847-#40031)
- Rust frozen SHA (previous): `bd19459358f534ed1cae464ec13d56600aeb45f2`
- Rust new HEAD: `970b7f2ff4f612b8e8cd340eb6b6d789d7141dd2`
- Go baseline: `5c25a72` (`main`, TUI markdown-rendering alignment landed after sync22)
- Go baseline state: `go build ./...` clean.

Rust `git pull origin main` reported "Already up to date" at `970b7f2ff4`.

## Drift report

52 new upstream commits between `bd19459358` (#39837) and `970b7f2ff4` (#40031).
Full per-commit ledger to land in `parity/commits.json`.

This is a large sync (348 files changed, ~16,185 insertions / ~1,798 deletions). It is
organized here into **analysis** (below) plus a **batching roadmap**. Live
implementation is landing in batches following the established
batch-in-sync pattern used in sync22 (batches 2-6 + follow-up).

### Static-layer (L0) drift already observed at the new target

`go test ./parity/... -count=1` currently fails at the previous frozen baseline because the
Rust checkout advanced past `bd19459358`. These are the re-freeze items that a completed
sync23 must regenerate/validate once behavior is aligned:

- `TestRustConfigSchemaSurfaceAgainstGo` - Rust `config.schema.json` top-level properties 93 -> 95.
- `TestRustGoldenFixtureRootsSnapshot` - app-server suite/v2 fixture file-count 114 -> 115.
- `TestRustWorkspaceMembersSnapshot` - workspace members 134 -> 135 (new `utils/redacted-string`).
- `TestRustCriticalFileHashesSnapshot` - critical file hashes moved (Cargo.toml etc.).
- `TestPrecomputedAppServerExportsMatchRustTarget` - vendored precomputed export changed.
- `TestRustFeatureKeySurfaceAgainstGo` - Go-only feature keys `guardian_ext`, `shell_snapshot_v2`.
- `TestRustConfigTomlTopLevelSurfaceSnapshot` - ConfigToml top-level fields 96 -> 98.
- `TestRustTUISnapshotManifestCoversPrioritySurfaces` - TUI snapshot total 684 -> 687.

All of these are expected mid-sync; they are resolved together by the re-freeze step.

## Per-commit catalog (classification + Go mapping)

Legend: **IMPLEMENT** = needs new/changed Go code; **N-A** = no corresponding Go surface
or Go already behaviorally equivalent; **TEST** = test-only; **INFRA** = dependency/tooling;
**PLATFORM** = platform-specific (not actionable on Windows host). Go package mappings are
the owning package(s) in `D:\qax\reagent\dev\codex_go`.

### MCP / executor wiring

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `970b7f2ff4` | #40031 | Preserve strict MCP auto-review outcomes | IMPLEMENT | `mcp/` + `appserver/` - keep a strict `auto_review` outcome across an MCP re-elicitation; do not downgrade to a softer review kind. |
| `e6a3877e95` | #40015 | Harden remote installed plugin cache reconciliation | IMPLEMENT | `plugin/` + `appserver/` - reconcile the installed-plugin cache from the remote source with stricter staleness/registry handling. |
| `ab8768306f` | #40012 | Preserve executor context for MCP stop hooks | IMPLEMENT | `mcp/` + `execserver/` - keep the originating executor context when running MCP stop hooks. |
| `8b61c50ebe` | #40009 | Run allowlisted executor plugin stop hooks | IMPLEMENT | `plugin/` + `execserver/` - run plugin stop hooks only for allowlisted plugins. |
| `8edb95f274` | #39979 | Preserve MCP compatibility with older executors | IMPLEMENT | `mcp/` + `execserver/` - negotiate MCP capabilities against older executors without breaking compatibility. |
| `054588acfe` | #39952 | Honor required MCP servers from selected executors | IMPLEMENT | `mcp/` + `appserver/` - merge `required` MCP servers contributed by the selected executor into the effective MCP server set. |
| `00a7b888b2` | #39941 | Discover HTTP MCP servers from selected executors | IMPLEMENT | `mcp/` + `appserver/` - discover HTTP MCP servers advertised by the selected executor. |
| `7f9832d0d0` | #39935 | Enforce issuer binding for MCP OAuth endpoints | IMPLEMENT | `mcp/` - bind the accepted OAuth issuer to the configured endpoint when validating issuer claims. |
| `9c3da20b3f` | #39930 | Track remote MCP header environment variables | IMPLEMENT | `mcp/` - carry remote MCP header env-var metadata so header secrets stay environment-resolved. |
| `93c54bca38` | #39926 | Resolve HTTP MCP bearer tokens in executor environments | IMPLEMENT | `mcp/` + `execserver/` - resolve HTTP MCP bearer tokens inside the executor env. |

### Browser / computer use (new config surface)

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `95118dff65` | #40018 | Add browser and computer use configuration | IMPLEMENT | `config/` - add browser/computer-use config keys and surface them. |
| `0f1a30b5c2` | #40000 | Expose browser and computer-use requirements through app-server | IMPLEMENT | `appserver/` - expose browser/computer-use requirements in the app-server config surface. |
| `950dd184a1` | #39995 | Expand browser and computer use requirements | IMPLEMENT | `config/` + `appserver/` - extend requirement fields for browser/computer use. |

### Guardian / review

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `50ea8fd411` | #40028 | Log Guardian V2 classification results | IMPLEMENT | guardian surface in `appserver/` + `state/` - log classification outcome. |
| `6143217c67` | #40021 | Cancel Guardian reviews with their tool calls | IMPLEMENT | guardian review teardown - cancel a review when its tool call is cancelled. |
| `9949c9eafa` | #40013 | Reuse Guardian reviews in async risk scoring | IMPLEMENT | reuse an in-flight Guardian review across concurrent async risk-score requests. |
| `dbe9dac1ae` | #40005 | Route escalated commands through synchronous Guardian review | IMPLEMENT | route an escalated command through a synchronous Guardian review before exec. |
| `56012fafb8` | #39994 | Add Guardian internal session support | IMPLEMENT | `appserver/` + `session/` - internal Guardian session identity. |
| `51ebf5b184` | #39985 | Truncate Guardian instructions after rendering the policy | IMPLEMENT | guardian instruction truncation after policy rendering. |
| `c517cc6d84` | #39981 | Bypass risk scoring for models that require automatic review | IMPLEMENT | `model/` + guardian - skip risk scoring when the model requires automatic review. |
| `d12a7f3fd8` | #39975 | Preserve root user authorization in subagent Guardian reviews | IMPLEMENT | guardian review - preserve root-user authorization for subagent reviews. |
| `e482cc66ae` | #39962 | Keep Guardian reviews isolated from executor MCP servers | IMPLEMENT | guardian review isolation from executor MCP servers. |

### Exec / sandbox / unified exec

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `9445ef227e` | #40024 | Honor granular sandbox approvals in unified exec | IMPLEMENT | `execserver/` + `execpolicy/` - honor granular (per-path) sandbox approvals in unified exec. |
| `f580dd886f` | #39980 | Enforce environment network policies for remote execution | IMPLEMENT | `execserver/` - enforce env network policy for remote exec. |
| `696b4502df` | #39976 | Allow semaphore limit queries in the macOS sandbox | PLATFORM | macOS seatbelt; N-A on Windows host. |
| `41ab01a2ea` | #39971 | Fix elevated Windows sandbox setup activation | IMPLEMENT | `sandbox/` (Windows) - elevated sandbox setup activation fix. |
| `4e6ef3b418` | #39958 | Stop advertising shell snapshots from local exec servers | IMPLEMENT | `execserver/` - stop advertising shell snapshots from local exec servers. |
| `3882ced09c` | #39957 | Add in-memory shell snapshots to unified exec | IMPLEMENT | `execserver/` - in-memory shell snapshots for unified exec. |
| `748d8ac834` | #39937 | Bound unified exec output delta frames | IMPLEMENT | `execserver/` - bound the size/count of unified exec output delta frames. |
| `ff0e95007c` | #39917 | Honor request PATH in exec-server shell snapshots | IMPLEMENT | `execserver/` - honor the request PATH in exec-server shell snapshots. |
| `2151d3a5b7` | #39852 | Reset registry retries when refreshing Noise bundles | IMPLEMENT | `execserver/` - reset registry retry state when Noise bundles refresh. |

### App-server protocol / schema / provider / model

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `e77c2a90af` | #40007 | Implement Amazon Bedrock setup in the app server | IMPLEMENT | `appserver/` + `model/` - Bedrock setup capability. |
| `79b7606803` | #39993 | Keep credentials out of app-server logs | IMPLEMENT | `model/` + `appserver/` - introduce `RedactedString` equivalent and stop logging credential-bearing payloads/headers/args/attestation tokens. |
| `be6ebb1f6d` | #40017 | Trace turn context creation and realtime state checks | IMPLEMENT | `appserver/` + telemetry - trace turn-context creation and realtime state checks. |
| `44e95c857f` | #39870 | Allow session configuration with `codex agents` | IMPLEMENT | `cmd/` + `codemode/` - session config via `codex agents`. |
| `27c05a52e0` | #39847 | Include context window IDs in response metadata | IMPLEMENT | `model/` - include context-window IDs in responses/response metadata. |
| `ad9e8097fd` | #40004 | Preserve managed deny-read rules across permission updates | IMPLEMENT | `config/` (Permissions) - keep managed `deny_read` across thread permission/profile updates; reject conflicts. |
| `d446960657` | #39953 | Support voice-aware configuration and version-skew builds | IMPLEMENT | `config/` - voice-aware config + version-skew guard. |
| `536f86e5cc` | #39876 | Support attaching to existing realtime calls | IMPLEMENT | `realtime/` + `appserver/` - attach to an existing realtime call. |
| `275ef855fa` | #39940 | Allow more time for local code-mode host startup | IMPLEMENT | `codemode/` - extend local code-mode host startup timeout. |

### TUI

| SHA | # | Subject | Class | Go mapping / action |
|---|---|---|---|---|
| `9cdc66904f` | #39999 | Hide Fast mode status for unsupported models | IMPLEMENT | `tui/chatwidget/status_controls.go` - hide fast-mode status when the model does not support Fast mode; keep it for supported/uncatalogued. |
| `df6a54ee85` | #39997 | Add a response target picker to `/copy` | IMPLEMENT | `tui/` - response-target picker for `/copy`. |
| `45a3edc02a` | #39992 | Keep keymap action descriptions stable while navigating | IMPLEMENT | `tui/keymap_setup` - stable binding-aware action descriptions. |
| `f6519a355a` | #39991 | Preserve TUI event ordering during active-thread draining | IMPLEMENT | `tui/` - preserve event ordering while draining an active thread. |
| `2aaefa32b0` | #39873 | Add keybindings for cycling TUI permission modes | IMPLEMENT | `tui/` + `config/` - keybindings to cycle TUI permission modes. |
| `9ab176f488` | #39864 | Limit pending input preview wrapping work | IMPLEMENT | `tui/` - bound pending input-preview wrapping work. |

### Test-only / infra

| SHA | # | Subject | Class | Action |
|---|---|---|---|---|
| `677cfee000` | #40020 | Add e2e tests for executor Stop hooks | TEST | Port test intent; no new product code. |
| `8b5beea5c0` | #40011 | Synchronize concurrent Git enrichment test explicitly | TEST | Test-only; N-A product change. |
| `51d8d12236` | #40006 | Synchronize Git enrichment tests explicitly | TEST | Test-only; N-A product change. |
| `8ce27647fb` | #39961 | Test browser MCP bearer tokens over executor WebSockets | TEST | Test-only; N-A product change (covered by #39926). |
| `16e2722c50` | #39969 | Consolidate code mode output helper tests | TEST | Test-only refactor. |
| `3432d3f2c9` | #39967 | Upgrade pnpm to 10.34.5 | INFRA | JS tooling; N-A for the Go tree. |

## Batching roadmap

sync23 will land in batches, mirroring the sync22 pattern. Suggested grouping:

1. **batch1 - Config/model/provider & credential redaction**: #40004, #39999, #39847,
   #39953, #39993 (RedactedString), #40018/#40000/#39995 (browser/computer use config).
2. **batch2 - MCP server discovery & auth**: #39941, #39952, #39926, #39930, #39935,
   #39979, #40012, #39962, #40031.
3. **batch3 - Executor/unified exec & sandbox**: #40024, #39980, #39957, #39958, #39937,
   #39917, #39852, #39971, #40009, #40015.
4. **batch4 - Guardian/review**: #40021, #40013, #40005, #39994, #39985, #39981, #39975,
   #40028.
5. **batch5 - App-server/CLI/realtime**: #40007, #40017, #39870, #39876, #39940.
6. **batch6 - TUI**: #39992, #39991, #39997, #39873, #39864.
7. **batch7 - Re-freeze static layer**: update `parityRustBaseline` to `970b7f2ff4`,
   regenerate schema surfaces, fixture/inventory/hash snapshots, workspace members,
   TUI snapshot manifest, precomputed exports; re-point `parity.json`,
   `parity/baseline.json`, `parity/domains.json`, `parity/contracts/manifest.json`,
   `parity/README.md`, and the `parity/commits.json` ledger.

## Verification

```powershell
go build ./...
go test ./appserver ./mcp ./model ./tool ./execserver ./execpolicy ./config ./tui/chatwidget ./tui ./parity
go test ./... -count=1
```

## Cycle status

- 2026-08-22: sync23 **analysis plan authored** (this file).
- **Batch 1 landed and pushed (`7a1d371`)**:
  - #39940 ("Allow more time for local code-mode host startup"): locally spawned
    code-mode host handshake timeout 10s -> 30s (mirrors Rust
    `LOCAL_HOST_STARTUP_TIMEOUT` for `ConnectionOwner::Process`, 10s retained for
    WebSocket). `codemode/process_provider.go`. Verified.
  - #39864 ("Limit pending input preview wrapping work"): pending-input preview
    wraps only the first `PREVIEW_LINE_LIMIT+1` source lines before truncation
    (mirrors Rust `.take(PREVIEW_LINE_LIMIT + 1)`).
    `tui/bottom_pane/pending_input_preview.go`. Verified.
  - Verified: `go build ./...` clean; `go test ./codemode ./tui/bottom_pane -count=1` PASS.
- **#39958 ("Stop advertising shell snapshots from local exec servers")** audited
  during batch 1 and classified **N/A-equivalent**: Go's
  `execserver.EnvironmentCapabilities` has no `shellSnapshotV2` field, so the
  local exec server never advertises it (JSON-absent => false).
- Static-layer drift at the new Rust target is documented above (expected mid-sync;
  resolved by the batch7 re-freeze).
- Remaining implementation lands via the **batch roadmap** (batches 1-6), followed by
  the **batch7** static-layer re-freeze to `970b7f2ff4`.
- Next action: continue with the remaining batch1 items (config/model/provider &
  credential redaction) and subsequent batches.
