# Codex Go update alignment plan - 2026-08-17

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main`: `9ded177ce7c1c0bd2047f902936c177612ab3434` (fetched;
  unchanged from the frozen baseline - no upstream drift this cycle)
- Go baseline: `df0a351` -> current `5263541` (this cycle's commits)
- `parity/baseline.json` target: `9ded177ce7c1c0bd2047f902936c177612ab3434`

## Drift report (junction point 1 input)

`git fetch origin main` == frozen baseline, so the static layer produced no
drift entries this cycle; the drift-driven dynamic selection is empty. Instead
of skipping the dynamic layer, the cycle extended the L2 record-replay
facility to drive Go's actual app-server core (see below) and closed the last
unwired config-requirements surface with a shared-fixture differential.

## L2 record-replay extended to core-level (dynamic verification infra)

The existing `recordreplay` facility only replayed the Rust recording through
the storage recorder (`rollout.Recorder`). This cycle added the dynamic half
of djalign method 2:

- `recordreplay.ReplayThroughCore` starts a paginated thread on
  `appserver.RuntimeRouter` and runs one turn per recorded task (3 tasks from
  `tui/tests/fixtures/oss-story.jsonl`) with a scripted agent reproducing the
  recording's per-task item surface (reasoning + agent_message presence;
  model text excluded per normalization rules).
- The observable contract is validated by `ValidateCoreReplayCrossCheck`:
  notification stream (turn/started, item/completed reasoning/agentMessage,
  turn/completed completed) plus the persisted paginated rollout (session_meta
  first, contiguous ordinals, task_started/item/task_complete lifecycle per
  turn), and `rollout.Resume` recoverability.
- Registered as verifier `TestReplayRustStoryRecordingThroughGoCore` on the
  `story-recording-replay` contract; token-free and CI-able.

## remote_sandbox_config shared-fixture differential (junction point 2)

Closed the last unwired config-requirements surface:

- Go parses `remote_sandbox_config` (hostname-pattern-scoped
  `allowed_sandbox_modes`) and applies the Rust
  `apply_remote_sandbox_config` semantics: hostname normalization
  (trim/trailing-dot/lowercase), first matching entry replaces the top-level
  modes, non-match preserves them, `WildMatchPattern` `*` / `?`
  case-insensitive matching.
- `ConfigRequirementsFromMapWithHostname` mirrors Rust's explicit hostname
  parameter (production paths keep the `os.Hostname()` fallback), so the
  differential is deterministic.
- `TestRustRemoteSandboxConfigSamplesRunInGo` drives the
  `config_requirements.rs` samples (`deserialize_remote_sandbox_config_requires_hostname_patterns_list`,
  `remote_sandbox_config_first_match_overrides_top_level`,
  `remote_sandbox_config_non_match_preserves_top_level`) with fn-name pinning;
  the fourth sample
  (`remote_sandbox_config_does_not_override_higher_precedence_sandbox_modes`)
  is pinned by name here and covered at the config unit level
  (`TestRemoteSandboxConfigDoesNotOverrideHigherPrecedenceModes`), which has
  access to `mergeConfigRequirements`.
- Registered as contract `remote-sandbox-config-shared-fixtures`.

## Verification

```powershell
go build ./...
go test ./recordreplay ./config ./parity -count=1
go test -race ./recordreplay -count=1
go test ./... -count=1        # full tree, 0 FAIL
go vet ./recordreplay/...
npm --prefix sdktests run test:unit   # 50 pass, 1 documented skip, 0 fail
```

## Committed

- `5263541` djalign: L2 core-level record-replay + remote_sandbox_config
  shared-fixture differential (recordreplay/core.go, config/
  remote_sandbox_config.go + tests, parity differential + manifest + parity.json
  + domains.json).

## Alignment state

- L0/L1/L2 gates: green at frozen `9ded177ce7`.
- L3 SDK differential: facility ready (89 scenarios + opt-in
  rollout-budget-exhaustion; unit suite 50/51 with 1 documented skip); live
  matrix remains authorization/provider-gated.
- L4 certification blockers unchanged: SDK differential re-certification
  against `9ded177ce7` (authorization/provider), native platform matrix
  (GitHub billing), `sdk-cancellation` (upstream Rust fix).

## Follow-up batch (same window): image-resize-notice closed

The last internally-closable `partial` parity item (`image-resize-notice-feature`)
was closed by implementing real image preparation:

- `eventmap` now performs actual dimension-changing image prep: decode
  PNG/JPEG/GIF/WebP, Rust `prompt_image_output_dimensions_for_limits` budget
  math (High 2048 max dimension / 2500 patches, Unified 6000 / 10000), resize
  (bilinear), re-encode (PNG lossless / JPEG quality 85). Images already
  within budget pass through byte-for-byte for PNG/JPEG/WebP (Rust
  `can_preserve_source_bytes`); GIF re-encodes as PNG. WebP resize re-encodes
  as PNG (Go stdlib/x-image has no WebP encoder) - a documented container
  difference on that rare path.
- Wired into the production turn-start input path
  (`preparedUserMessageInputItem` in `RuntimeRouter`): user images are resized
  before reaching the model regardless of the feature flag; when
  `image_resize_notice` is enabled (default off), an `ImageResizeNotice`
  developer fragment with Rust's exact wording is appended after the user
  message.
- Pinned by `TestRustImagePreparationDimensionSamplesRunInGo`
  (`detail_policies_apply_the_expected_budgets` table: 2048x2048->1600x1600
  high, 6401x100->6000x94 and 3201x3201->3200x3200 original), the eventmap
  pipeline tests, and end-to-end app-server tests
  (`TestRuntimeRouterTurnStartPreparesImagesAndEmitsResizeNoticeLikeRust`,
  `TestRuntimeRouterTurnStartResizeNoticeEnabledEmitsDeveloperFragmentLikeRust`).
- Registered as contract `image-preparation-dimensions`; parity.json item
  flipped to `done`; domains.json evidence updated.
- Verification: `go test ./eventmap ./appserver ./parity -count=1`, full tree
  `go test ./... -count=1` (0 FAIL), `go test -race ./eventmap -count=1`.

## Follow-up batch (same window): config-surface allowlist 5 -> 3

Closed two more Rust top-level config keys, shrinking the config-schema
rust-only allowlist from 5 to 3 entries:

1. `disable_paste_burst`
   - `Config.DisablePasteBurst()` accessor (default false), threaded through
     the interactive TUI `Options.DisablePasteBurst` into the tea `Model`.
   - When true the burst-paste detector is bypassed entirely: no composer
     paste-window extension on fast typed input and Enter submits instead of
     inserting a newline, mirroring Rust `chat_composer.rs`
     `set_disable_paste_burst` / `handle_non_ascii_char` /
     `append_newline_if_active` gates.
   - Pinned by `TestDisablePasteBurstAccessorLikeRust` and
     `TestModelDisablePasteBurstSubmitsOnEnterLikeRust` (existing
     `TestModelPasteBurstEnterInsertsNewlineWithoutSubmitting` still passes).
2. `audio` (`RealtimeAudioToml` microphone/speaker)
   - Recognized as a top-level key with serde `deny_unknown_fields` sub-field
     validation mirroring Rust `RealtimeAudioConfig`, accepted as a
     compatibility no-op so legacy config loads (unknown sub-fields rejected).
   - Pinned by `TestLoadEffectiveStrictConfigAcceptsRealtimeAudioLikeRust`.

Remaining 3 rust-only entries are genuinely unimplemented experimental
features with auditable reasons (`experimental_compact_prompt_file`,
`experimental_thread_config_endpoint`, `experimental_thread_store`).

- Verification: `go test ./config ./tui/tea ./parity ./app -count=1`, full
  tree `go test ./... -count=1` (0 FAIL).
