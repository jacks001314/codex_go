# Codex Go update alignment plan - 2026-08-22 (sync23 #39847-#40031)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust `origin/main` frozen target: `bd19459358` (sync22) -> `970b7f2ff4` (52 commits, #39847-#40031)
- Rust frozen SHA: `970b7f2ff4f612b8e8cd340eb6b6d789d7141dd2`
- Go baseline: `5c25a72` (`main`, prior sync22 follow-up + TUI work committed)
- Go baseline state: `go build ./...` clean before this cycle (verified at start).

## Drift report

New upstream commits since frozen `bd19459358`: **52 commits** (#39847-#40031).
The per-commit ledger is tracked in `parity/commits.json` (this cycle appends entries
with a `rustFromExclusive=bd19459358`, `rustToInclusive=970b7f2ff4` range).

Each commit below is classified as:
- **Implement**: requires Go code changes to preserve runtime/contract parity.
- **N/A (equivalent)**: Go already matches the observable behavior; no code change needed.
- **Test-only / tooling / platform**: no runtime Go behavior change (test infra, npm,
  macOS Seatbelt, version-skew build harness), documented as evidence.

### 1. Exec / unified exec cluster

| Commit | Subject | Go action |
|---|---|---|
| `9445ef227e` #40024 | Honor granular sandbox approvals in unified exec | **Implement** - unified-exec sandbox escalation must use the shared approval-policy check so granular `sandbox_approval` prompts when enabled (exec/execpolicy). |
| `4e6ef3b418` #39958 | Stop advertising shell snapshots from local exec servers | **Implement** - local exec-server `shellSnapshotV2` capability must advertise `false` (execserver). |
| `3882ced09c` #39957 | Add in-memory shell snapshots to unified exec | **Implement** - executor-managed in-memory snapshots for eligible direct login shells (execserver/unified-exec). |
| `748d8ac834` #39937 | Bound unified exec output delta frames | **Implement** - cap each `ExecCommandOutputDelta` frame at 8 KiB with a per-call quota and UTF-8 scalar preservation (exec protocol/execserver). |
| `ff0e95007c` #39917 | Honor request PATH in exec-server shell snapshots | **Implement** - apply per-request env overrides incl. `PATH` after restoring captured shell env; drop `runtime_path_prepends` from `ShellSnapshotRequest` (execserver). |
| `41ab01a2ea` #39971 | Fix elevated Windows sandbox setup activation | **Implement** - add `SEE_MASK_NOASYNC` to shell execution flags (Windows elevated sandbox IPC). |
| `275ef855fa` #39940 | Allow more time for local code-mode host startup | **Implement** - 30s handshake timeout for locally spawned code-mode hosts (10s retained for WebSocket) (codemode). |
| `f580dd886f` #39980 | Enforce environment network policies for remote execution | **Implement** - resolve each remote environment's network policy for the selected command and apply to execution-scoped proxy (execserver/network). |

### 2. MCP / executor cluster

| Commit | Subject | Go action |
|---|---|---|
| `00a7b888b2` #39941 | Discover HTTP MCP servers from selected executors | **Implement** - read `mcp_servers` config/requirements from selected remote executors, add eligible HTTP servers to the thread MCP runtime bound to the executor snapshot (mcp/execserver). |
| `054588acfe` #39952 | Honor required MCP servers from selected executors | **Implement** - preserve `required` on executor-discovered HTTP MCP servers; cover delayed startup availability (mcp). |
| `93c54bca38` #39926 | Resolve HTTP MCP bearer tokens in executor environments | **Implement** - preserve `bearer_token_env_var` for executor-owned HTTP MCP configs, resolve at request time (mcp/http_client). |
| `9c3da20b3f` #39930 | Track remote MCP header environment variables | **Implement** - track `env_http_headers` for remote HTTP MCP, keep `bearer_token_env_var` local-only (mcp). |
| `7f9832d0d0` #39935 | Enforce issuer binding for MCP OAuth endpoints | **Implement** - require advertised issuer to match origin serving auth metadata; validate auth/token endpoint origins (mcp/oauth). |
| `8edb95f274` #39979 | Preserve MCP compatibility with older executors | **Implement** - advertise `httpHeaderEnvVars` capability; fall back to host bearer resolution when executor lacks the capability (mcp). |
| `ab8768306f` #40012 | Preserve executor context for MCP stop hooks | **Implement** - scope executor stop-hook calls to the registered MCP server environment; forward turn metadata (mcp/hooks). |
| `8b61c50ebe` #40009 | Run allowlisted executor plugin stop hooks | **Implement** - discover inline hooks from executor plugin manifests, accept the bundled Computer Use `Stop` hook for `node_repl.turn_ended` (hookengine). |
| `677cfee000` #40020 | Add end-to-end tests for executor Stop hooks | **Test-only / N/A** - upstream test suite; Go hook engine behaviour covered by #40012/#40009 work. |
| `8ce27647fb` #39961 | Test browser MCP bearer tokens over executor WebSockets | **Test-only / N/A** - upstream executor MCP integration test scenario. |
| `e482cc66ae` #39962 | Keep Guardian reviews isolated from executor MCP servers | **Implement** - skip projecting executor MCP servers when the session source is a Guardian reviewer (guardian/mcp). |

### 3. MCP auto-review / Guardian cluster

| Commit | Subject | Go action |
|---|---|---|
| `970b7f2ff4` #40031 | Preserve strict MCP auto-review outcomes | **Implement** - strict auto-review must propagate canonical denial/timeout/abort responses instead of a generic decline (mcp/review). |
| `6143217c67` #40021 | Cancel Guardian reviews with their tool calls | **Implement** - propagate tool cancellation tokens into Guardian approval reviews and server-initiated MCP elicitation (review/mcp). |
| `dbe9dac1ae` #40005 | Route escalated commands through synchronous Guardian review | **Implement** - escalated command requests require synchronous Guardian review; bypass extension approval + Guardian V2 shortcuts (review). |
| `d12a7f3fd8` #39975 | Preserve root user authorization in subagent Guardian reviews | **Implement** - add bounded root conversation to Guardian prompts for worker reviews; only root user messages are authorization evidence (review). |
| `56012fafb8` #39994 | Add Guardian internal session support | **Implement** - extension API for host-owned internal sessions + `ThreadReadyInput` lifecycle callback + reviewer session scaffolding (guardian). |
| `51ebf5b184` #39985 | Truncate Guardian instructions after rendering the policy | **Implement** - keep classifier instructions intact until policy rendered, then apply the token cap once (review). |
| `c517cc6d84` #39981 | Bypass risk scoring for models that require automatic review | **N/A (equivalent)** - Go has no Guardian V2 async risk-classification path; the auto-review bypass is already the effective path. |
| `9949c9eafa` #40013 | Reuse Guardian reviews in async risk scoring | **N/A (equivalent)** - Go has no Guardian V2 async classifier evidence channel. |
| `50ea8fd411` #40028 | Log Guardian V2 classification results | **N/A (equivalent)** - Go has no Guardian V2 async classification log-event surface. |
| `ad9e8097fd` #40004 | Preserve managed deny-read rules across permission updates | **Implement** - retain managed deny-read rules separately and merge them into updated permission profiles; reject conflicting profiles (config/permissions). |

### 4. Config / browser-computer-use cluster

| Commit | Subject | Go action |
|---|---|---|
| `95118dff65` #40018 | Add browser and computer use configuration | **Implement** - typed `browser_use` + `computer_use` settings, app-server config reads, generated schemas (config/protocol/appserver). |
| `0f1a30b5c2` #40000 | Expose browser and computer-use requirements through app-server | **Implement** - expand `configRequirements/read` with Browser/Computer Use umbrella + full browser-use policy (appserver). |
| `950dd184a1` #39995 | Expand browser and computer use requirements | **Implement** - top-level `allow_browser_and_computer_use`, browser history/auto-review/persistent approvals, computer-use policies (requirements). |
| `79b7606803` #39993 | Keep credentials out of app-server logs | **Implement** - `RedactedString` for bearer tokens / headers / auth command args, redacted debug output (envutil/telemetry). |

### 5. Plugin cluster

| Commit | Subject | Go action |
|---|---|---|
| `e6a3877e95` #40015 | Harden remote installed plugin cache reconciliation | **Implement** - scope remote installed/loaded snapshots to active account, serialize reconciliation, cache generations, validate snapshot before materialization (plugin). |

### 6. Model / provider cluster

| Commit | Subject | Go action |
|---|---|---|
| `e77c2a90af` #40007 | Implement Amazon Bedrock setup in the app server | **Implement** - `account/bedrock/discover` + `account/bedrock/setup` reporting AWS profiles/environment credentials (auth/account). |
| `9cdc66904f` #39999 | Hide Fast mode status for unsupported models | **Implement** - hide Fast mode status value when current model is known unsupported (tui status/model). |
| `39981` (see Guardian cluster) | Bypass risk scoring | N/A. |

### 7. TUI cluster

| Commit | Subject | Go action |
|---|---|---|
| `df6a54ee85` #39997 | Add a response target picker to `/copy` | **Implement** - `/copy` picker over whole response + fenced blocks/blockquotes with language labels and previews (tui/chatwidget). |
| `45a3edc02a` #39992 | Keep keymap action descriptions stable while navigating | **Implement** - one binding-aware description per keymap action; no text swap while selection moves (tui/keymap). |
| `f6519a355a` #39991 | Preserve TUI event ordering during active-thread draining | **Implement** - bound active-thread draining to one target frame interval, prioritize queued app events (tui/tea). |
| `2aaefa32b0` #39873 | Add keybindings for cycling TUI permission modes | **Implement** - unbound `previous_permission_mode`/`next_permission_mode` actions in `tui.keymap.chat` (tui/keymap). |
| `9ab176f488` #39864 | Limit pending input preview wrapping work | **Implement** - limit pending steers/rejected steers/queued follow-ups to four source lines before wrapping (tui/bottom_pane/pending_input_preview). |
| `d446960657` #39953 | Support voice-aware configuration and version-skew builds | **N/A (tooling)** - `chat.toggle_voice_mute` keymap extension + Debian-like build harness; version-skew only. |
| `44e95c857f` #39870 | Allow session configuration with `codex agents` | **Implement** - accept interactive options (model/approval/sandbox/search/cwd/config overrides) when opening the agents dashboard (agent/app). |

### 8. Turn / metadata / realtime cluster

| Commit | Subject | Go action |
|---|---|---|
| `27c05a52e0` #39847 | Include context window IDs in response metadata | **Implement** - `context_window_id` in turn metadata for sampling/compaction; stable per context window, advances after compaction (turn/appserver). |
| `536f86e5cc` #39876 | Support attaching to existing realtime calls | **Implement** - `existingCall` transport to `thread/realtime/start` with client `callId` + optional `realtimeSessionId`; sideband attach without SDP (realtime). |
| `2151d3a5b7` #39852 | Reset registry retries when refreshing Noise bundles | **Implement** - reset registry retry count + operation deadline before requesting replacement bundle after 401 handshake (execserver/remote_harness). |
| `be6ebb1f6d` #40017 | Trace turn context creation and realtime state checks | **N/A (equivalent)** - Go has no OTLP trace-span surface; behavior unchanged. |

### 9. Platform / tooling / test-only (no runtime Go change)

| Commit | Subject | Go action |
|---|---|---|
| `696b4502df` #39976 | Allow semaphore limit queries in the macOS sandbox | **Platform / N/A** - macOS Seatbelt; Windows dev host. |
| `16e2722c50` #39969 | Consolidate code mode output helper tests | **Test-only / N/A**. |
| `3432d3f2c9` #39967 | Upgrade pnpm to 10.34.5 | **Tooling / N/A** - npm/pnpm package pinning. |
| `8b5beea5c0` #40011 | Synchronize concurrent Git enrichment test explicitly | **Test-only / N/A**. |
| `51d8d12236` #40006 | Synchronize Git enrichment tests explicitly | **Test-only / N/A**. |

## Implementation plan (this cycle)

This cycle ships the **plan document plus a focused, verifiable runtime batch**, then
updates the parity ledger for the commits completed. Given the scope (52 commits across
Guardian / MCP / config / plugin / TUI / exec / realtime), the batch is split and
committed incrementally, mirroring the sync22 batch pattern:

1. **Batch A (this turn):** two bounded runtime-parity commits with existing Go
   counterparts, verified with `go build ./...` and scoped package tests, then committed:
   - `275ef855fa` #39940 - code-mode local host startup handshake timeout 10s -> 30s
     (`codemode/process_provider.go`, mirroring Rust's `LOCAL_HOST_STARTUP_TIMEOUT`).
   - `9ab176f488` #39864 - pending input preview wraps only the first four source lines
     before truncation (`tui/bottom_pane/pending_input_preview.go`, mirroring Rust's
     `.take(PREVIEW_LINE_LIMIT + 1)`).
2. **Batch B-Later (deferred to subsequent cycles):** the remaining 50 commits. These
   include larger structural changes (Fast-mode status catalog threading, keymap action
   menu restructure + permission-mode cycling, MCP executor discovery/bearer/OAuth
   issuer-binding, Guardian cancellation/routing/internal-session, browser-computer-use
   config+requirements+schema, plugin cache hardening, Bedrock setup, realtime
   `existingCall` attach, unified-exec shell snapshots / PATH / network policy). Each is
   classified above; the parity ledger range is **not** re-targeted to `970b7f2ff4` until
   the full sync23 set is aligned to avoid a false "complete" claim.

## Verification

```powershell
go build ./...
go test ./tui/... ./turn ./appserver ./config ./exec ./execserver ./codemode -count=1
go test ./... -count=1
```

Batch A target verification: `go build ./...` clean and scoped package tests pass at the
new frozen target `970b7f2ff4`.

## Cycle summary (2026-08-22, Batch A delivered)

- `go build ./...` clean at the start of the cycle and after Batch A changes.
- Batch A delivered and verified:
  - `go build ./codemode ./tui/bottom_pane` clean; `go test ./codemode ./tui/bottom_pane -count=1` PASS.
  - Commits #39940 and #39864 implemented with Rust-parity comments.
- Parity ledger **not** re-targeted yet: sync23 is 2/52 aligned; the range stays at the
  sync22 target `bd19459358` until the remaining commits are completed and verified.
- Next actions: continue Batch B clusters (MCP executor discovery/bearer/OAuth, Guardian,
  browser-computer-use config+schema, plugin cache hardening, Bedrock, realtime attach,
  unified-exec shell/PATH/network) then re-freeze the parity snapshots at `970b7f2ff4`.
