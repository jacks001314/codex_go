# Codex Go update alignment plan - 2026-09-11 (sync31: 344-commit upstream range)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust previous HEAD (sync30 last applied): `574a36ff99` (#42883, "Add client-side exec-server RPC attempt metrics")
- Rust new HEAD (sync31): `02a8f038b8` (#44755, "Check folder consent before creating or resuming TUI tasks")
- `git fetch origin main` advanced `574a36ff99..02a8f038b8` (344 commits, 1828 files, +159733/-40972).
- After the first sync31 commits, upstream advanced one test-only commit:
  `02a8f038b8..624ccf7947` (#44814, "Test approved command execution with
  managed unified exec disabled"). The L0 manifest pinned the new
  `config_requirements_exec` v2 suite module and the refreshed
  `app-server/tests/suite/v2` fixture count / `mod.rs` hash.
- Upstream then advanced `624ccf7947..7a6f469dcf` (#44826, "Expose advertised
  MCP server capabilities in status responses"): the vendored precomputed
  exports were refreshed and the `McpServerStatus.serverCapabilities` wire
  field was added to Go (see the batch note below).
- Go baseline commit: `34c41ac` (sync30 landed).
- Certified baseline (`parity.json` / `candidateRustTo`): unchanged at `970b7f2ff4` (#40031, sync26).

Priority for sync31 follows the operator instruction: land non-TUI core
(schema / protocol / config / model / state / app-server runtime) first, and
schedule TUI ports last. Every batch below is `pending` until its runtime
behavior and verifier land; the plan is re-visited at the end of each pass.

## Scope exclusion (operator instruction)

Voice-related upstream work is owned by a separate in-flight process, so it is
out of scope for this plan: `voice-host` / native voice runtime packaging
(#43100, #43244, #43248, #43090, #43079, #43102, #43117, #43121, #43125,
#43126, #43144, #44714, #44062, #44080, #43983, #43983), realtime voice TUI
surfaces (#43581, #43651, #43656, #43676, #43683, #43690, #43695, #43698,
#43699, #44749, #44622, #44671, #43934), and voice audio utils. Do not port
these in this plan; the parallel process owns them.

## L0 static re-freeze - DONE (this pass)

The upstream range moves feature keys, model metadata, workspace members,
fixtures, TUI snapshots, and the vendored precomputed exports. The L0 static
layer was re-pinned against `02a8f038b8` and `go test ./parity` is green:

- `features/features.go`: renamed `guardian_thread_context` ->
  `guardianv2.thread_context` (Rust #43104, moved into the `guardianv2`
  configuration namespace) and added:
  `api_key_model_discovery` (UnderDevelopment, off, #44392),
  `codex_apps_mcp_2026_07_28` (UnderDevelopment, off, #44318),
  `reasoning_effort_override` (UnderDevelopment, off, #43795).
  `appserver/guardian_reviewer.go` and `config/config_test.go` updated to the
  renamed key.
- `model/catalog.go`: added `ModelInfo.SupportsExperimentalContext`
  (`supports_experimental_context`, serde default false, Rust #43147) with the
  raw parse path; `parity/rust_enum_surface_test.go` re-pinned (Rust 43 -> 44,
  Go 45 -> 46).
- `appserver/runtime_router.go`: `experimentalContextManagementEligible` now
  requires the starting model to advertise `supports_experimental_context`
  (Rust 6af345407d), so unsupported models stay ineligible.
- `parity/rust_snapshot_test.go`: workspace members re-generated from
  `codex-rs/Cargo.toml` (146 -> 147; added `ext/guardian-reviewer`,
  `user-verification`; removed `mcp-server`); critical-file SHA256 re-pinned for
  `Cargo.toml`, `exec/src/lib.rs`, `core/src/client.rs`,
  `app-server-protocol/src/protocol/common.rs`, `app-server/tests/suite/v2/mod.rs`,
  `core/tests/suite/mod.rs`.
- `parity/rust_inventory_test.go`: top-level directory inventory refreshed
  (removed `mcp-server`, added `user-verification`).
- `parity/rust_fixture_manifest_test.go`: fixture-root counts re-pinned
  (cli/tests 23 -> 26, app-server/tests/suite/v2 127 -> 142,
  tui/src/chatwidget/tests 59 -> 81, tui/src/chatwidget/tests/snapshots
  27 -> 44).
- `parity/rust_tui_snapshot_manifest_test.go`: TUI snapshot total 832 -> 983;
  per-dir counts refreshed and three new snapshot dirs registered
  (`bottom_pane/async_questions`, `chatwidget/realtime`,
  `chatwidget/realtime_tests`).
- `parity/rust_unified_exec_sandbox_manifest_test.go`: added
  `windows_elevated_setup_rejects_default_root_deny`.
- `appserver/schema/precomputed/*.zst`: re-vendored byte-identical to Rust
  (stable 148121 -> 150067, experimental 151703 -> 156139).
- `appserver/schema_test.go`: precomputed artifact SHA-256 pins re-frozen to the
  re-vendored exports (stable `1447217d...`, experimental `5f805159...`).
- `appserver/protocol.go` / `appserver/schema.go`: declared the new stable
  app-server surface so `ClientRequest.json` / `ClientRequest.ts` fixtures
  match - methods `thread/attachment/add|list|remove` with
  `ThreadAttachmentAdd|List|RemoveParams`/`...Response` types and the
  `thread/attachment/updated` `ThreadAttachmentUpdatedNotification`. (Runtime
  handlers for these methods are the next batch; the surface is declared so the
  fixture gates are honest.)
- `appserver/appserver_v2_suite_manifest_test.go`: added the 10 new Rust v2
  suite modules (`config_model_provider_requirements`,
  `connection_handling_stdio`, `daemon_update_recovery`, `mcp_protocol_default`,
  `memory_read`, `thread_attachments`, `thread_fork_multi_agent`,
  `thread_goal_empty_responses`, `user_verification`, `user_verification_mcp`).

Verification: `go build ./...` clean; `go test ./parity ./appserver -count=1`
green with `CODEX_RUST_ROOT` pointed at the sync31 checkout; full
`go test ./... -count=1` green.
Note: `TestRuntimeRouterThreadSettingsApprovalPolicySurvivesColdResume` is
flaky only under whole-suite load (passes in isolation and on re-run); it is
unrelated to this pass.

## sync31 non-TUI core batches (work queue)

### batch-aws-credential-export - DONE (this pass)
- `fe52d795c9` #44028 Add AWS credential export commands for Amazon Bedrock
  - **status: done (config + runtime)** - `provider.aws.credential_export`
    (`command`, `args`, `timeout_ms`, default 30000ms) is parsed, validated
    (non-empty command, absolute-or-bare executable, rejected together with
    `aws.profile`), and carried through the Bedrock override merge
    (`model_provider_info` message now lists `aws.credential_export`).
    `model/credential_export.go` implements the shared exporter: bounded 64 KiB
    stdout, timeout, flat credential-process and nested STS `Credentials`
    output, empty-key/RFC 3339/expired validation, one-hour default lifetime,
    five-minute refresh window, serialized command execution, in-memory cache,
    and a per-config registry shared across sessions. `auth.LoadAWSAuthContextWithProvider`
    injects the exporter as the SigV4 credential provider, `APIAuth` uses it
    when configured, and Bedrock recovery re-exports after `aws.auth_refresh`
    instead of reloading the SDK chain. Rust's `blake3`/`rand_regex`-based
    credential-process comparison tooling is Rust-internal and N/A.

### batch-mcp-enterprise-auth-44832 - DONE (this pass)
- `654b0a77d0` #44832 Add trusted enterprise MCP auth configuration
  - **status: done (config + provenance + runtime refusal)** - ported the
    enterprise MCP authorization contract:
    - `features`: added the disabled-by-default `use_xaa` flag
      (`features.UseXAAKey`).
    - `config/mcp_ema.go`: `MCPServerIdPOAuthConfig`,
      `MCPEnterpriseManagedAuthConfig`, `PluginMCPServerEMAAuthConfig`, and
      `ResolveMCPEnterpriseManagedAuth`. The resolver mirrors Rust
      `McpEnterpriseManagedAuthConfig::resolve`: it validates server
      declarations (`oauth.authorization_server_issuer` requires
      `auth = "ema_auth"`), enforces non-project registration ownership
      (projects cannot add, replace, downgrade, or re-enable an enterprise
      registration; registrations must be atomic in one non-project layer),
      selects one complete trusted IdP (managed MDM/System/EnterpriseManaged/
      legacy-managed layers win over user, project is never a source), and
      requires `use_xaa = true` to come from a non-project layer or a managed
      feature requirement.
    - `config`: recognized the `mcp_enterprise_managed_auth` top-level key,
      strict-validated its shape, allowed the `[mcp_servers.X.oauth]` table with
      the EMA issuer field, and wired the resolver into `ConfigService.Read`
      (both the CWD and non-CWD read paths) so invalid provenance fails at the
      config boundary.
    - `mcp`: `ServerConfig.OAuthAuthorizationServerIssuer` + parse of
      `oauth.authorization_server_issuer`, `ServerAuthEMAAuth`,
      `ValidateEMAAuthTransport` (host-owned streamable HTTP, no alternate
      credentials), `ValidateServerAuth` refusing EMA connections
      ("EMA MCP connections are not enabled in this version"), auth status
      reporting `unsupported`, and `OauthLogin` refusing EMA login.
    - `plugin`/`appserver`: plugin MCP declarations selecting `ema_auth` are
      dropped (`dropPluginEMAAuthServers`) and the runtime overlay builder also
      skips them; enterprise authentication remains host-configured.
    - `app`: CLI `mcp` config read/write round-trips `auth = "ema_auth"` and
      `oauth.authorization_server_issuer`, and `mcp login` refuses EMA with the
      account sign-in guidance from Rust `mcp_cmd.rs`.
    - Tests: IdP precedence/authority, incomplete registration, project
      downgrade rejection, XAA opt-in source, EMA transport refusal, auth
      status/login refusal, plugin self-declaration rejection, and config
      round-trip. Parity pins refreshed for the new Rust schema property,
      `ConfigToml` field, `core/tests/suite` fixture count, and
      `core/tests/suite/mod.rs` hash.
  - Verified: `go build ./...` clean; `go test ./config ./mcp ./features
    ./plugin ./app ./appserver ./parity -count=1` green.
  - Residual: the per-plugin `ema_auth` **policy application** (forcing
    `auth = ema_auth` from host `[plugins.<id>.mcp_servers.<s>.ema_auth]`) is
    accepted/validated but has no Go runtime consumer yet, because Go has no
    equivalent host plugin-MCP policy pipeline; EMA connections are unsupported
    in both implementations, so this has no observable runtime difference yet.

### batch-schema-v2-sync31 (app-server protocol surface, non-TUI)
- `ad931a45b2` #43265 experimental user verification API contracts,
  `b7ad941b1f` #43547 provider abstractions and RPC adapters,
  `ca6fb194b6` #43568 wire RPCs to the native provider,
  `82d4a98912` #43925 cancellation for native RPCs,
  `f326857cf4` #43524 workspace-scoped identity,
  `3cd6004dc4` #43289 capability-gated handling,
  `555b82afa9` #43352 opt-in transport,
  `d996b4f02a` #44359 OAuth failures in MCP status,
  `e7637306bc` #43624 macOS Secure Enclave provider,
  `d6489472f3` #43715 bundled TUI enablement (TUI half deferred).
  - **status: done (surface + runtime, provider-gated)** -
    `appserver/user_verification.go` defines the exact wire contract
    (`UserVerificationStatus|Enroll|Delete|Verify|CancelParams`/`...Response`,
    `UserVerificationProof`, the four closed error categories, and the typed
    `data` object). `appserver/user_verification_runtime.go` implements the five
    experimental RPCs: status reports `providerUnavailable`, enroll/delete/verify
    return the typed `unavailable` error (-32603), verify mirrors Rust's
    challenge/title/description validation (-32602 `invalidRequest`/`invalidParams`),
    and cancel acks as a no-op. The methods are registered as experimental so
    `experimentalApi` capability gating applies. Because Go has no Secure
    Enclave/biometric backend, this is exactly the behavior of a Rust build whose
    platform provider is unsupported (`platform_supported() == false`); a native
    Go provider (and the WebSocket/remote-control origin restriction, which the
    Go transport does not model) is the remaining platform gap.
- `3319d9b296` #44564 stored thread attachments app-server APIs,
  `130d6e4fba` #44330 paginated attachment listing,
  `589874be81` #43949 transactional attachment mutations,
  `9d88e9ae08` #43927 artifacts -> attachments rename.
  - **status: done (surface + runtime)** - `appserver/thread_attachments.go`
    defines the exact wire contract (`ThreadAttachment`,
    `ThreadAttachmentAdd|List|RemoveParams`/`...Response`,
    `ThreadAttachmentAddOutcome`, `ThreadAttachmentOperation`,
    `ThreadAttachmentUpdatedNotification`). `appserver/thread_attachments_runtime.go`
    implements add/list/remove on the shared `Router` (so both the app-server
    runtime and direct router users behave identically) with Rust's limits
    (payload <= 64 KiB, type/identity <= 256 bytes, <= 100 per thread, page
    limit default 25 clamped to [1,100], keyset cursor
    `threadId|createdAt|id`), idempotent adds, silent no-op removes,
    invalid-params (-32602) errors, and `thread/attachment/updated`
    notifications for real mutations. Attachments persist through
    `session.Metadata.Extra` (`thread_attachments`) instead of a dedicated
    SQLite table because the Rust migrations that add `thread_attachments` are
    newer than the certified `candidateRustTo` migration inventory pinned by
    `parity/state_migrations_test.go` - the same approach the port already uses
    for originator/daybreak. Tests cover the lifecycle, notification emission
    and suppression, pagination, validation, and the per-thread cap.
- `2cbbf0c9b5` #43827 memory dual writing and v2 readiness,
  `3f76e88a48` #43797 configurable memory versions,
  `553df1c691` #43813 dedicated memory v2 prompts,
  `e7f5de0a6a` #43808 v2 extraction chunking,
  `74d3a5bf10` #43800 summary-only extraction.
  - **status: partial** - `memory/status` is ported end-to-end: the
    experimental `memory/status` request returns `v2ConsolidatedThreads` and
    `v2Ready`, defaults the threshold to 20, rejects thresholds outside 1..=4096
    with invalid params, and reports ready only when the largest successful
    consolidation thread count meets the threshold and
    `<codex_home>/memories_v2/memory_summary.md` passes the shared
    `memories.IsValidV2Summary` check (first line `v1`, < 10,000 bytes, all four
    required headings). `memories` persists the largest consolidation count
    across pruning and clears it (and the v2 root) on memory reset; the
    `memories.dual_write` config flag is parsed. Tests cover summary validation,
    progress monotonicity/reset, and handler readiness/threshold/gating.
  - **remaining** - the v2 write pipeline itself (dual-write extraction/consolidation
    into isolated `memories_v2` storage, plus `memory/read`) is still pending;
    without it the status endpoint reports `v2ConsolidatedThreads = 0` and
    `v2Ready = false`, exactly as a Rust build that has not run v2 consolidation.
  - `3f76e88a48` #43797 configurable memory versions with isolated storage:
    **status: done (version selection)** - `memories.version` accepts `v1`
    (default) or `v2`; an unrecognized value fails config load instead of
    silently falling back. `config.MemoryVersion` carries the enum and root
    name, `memories.RootForVersion` routes to `memories` vs the isolated
    `memories_v2` sibling root, the memory-tool developer instructions read the
    selected version's `memory_summary.md`, the startup pipeline writes to the
    selected root, and shell memory-usage telemetry recognizes both roots.
    Memory reset already clears both roots. Tests cover version parsing/load
    validation, per-version summary selection, root mapping, and usage
    classification. Remaining: a separately-scoped v2 state/job store and the
    dual-write runtime (#43827's write half).
  - `553df1c691` #43813 dedicated memory v2 consolidation and read prompts:
    **status: partial (read template)** - v2 now uses Rust's dedicated
    `read_path_v2.md` read instructions, embedded byte-for-byte
    (`memories/templates/read_path_v2.md`, verified against the Rust template)
    with the selected version's `base_path` (`memories_v2`), while v1 keeps the
    original template. Same 2,500-token summary truncation as Rust. Remaining:
    Rust splits the rendered v2 instructions into <=8,900-byte fragments
    (`MemoryContextFragment::ReadInstructions`) whereas Go emits one
    developer-policy string; the v2 consolidation prompt
    (`consolidation_v2.md`, summary-only without `raw_memories.md`/`MEMORY.md`)
    and version-selected citation-usage recording are still pending.
- `6baa076eb6` #44571 MCP protocol mode per HTTP server.
  - **status: done** - `mcp.ServerConfig` gains an optional
    `ProtocolModeOverride`; `RuntimeConfig` gains the independent
    `HostOwnedAppsProtocolMode` resolved from `codex_apps_mcp_2026_07_28`;
    `NewMCPService` resolves the effective Streamable HTTP protocol mode as
    override > host-owned Apps mode > default, mirroring Rust's
    `McpConnectionSet` (#44571). Overrides survive overlay merge and
    registration cloning, and selecting a mode grants no Apps cache or
    environment authority. Tests cover the two independent-mode combinations
    and override precedence.
- `7a6f469dcf` #44826 Expose advertised MCP server capabilities in status
  responses
  - **status: done** - MCP clients capture the raw `capabilities` object
    advertised during initialization (stdio legacy `initialize` and modern
    `server/discover`, HTTP `initialize`/`server/discover`); the status type
    gains `serverCapabilities` (object or null) emitted in both `full` and
    `toolsAndAuthOnly` modes, including thread-scoped reads. Capabilities are
    read after the initialization attempt so they survive a tool-discovery
    failure, a fresh connection attempt clears them, and a failed
    initialization serializes as null. Capture uses a dedicated mutex so it
    cannot deadlock the client's handshake lock. Tests cover capability
    exposure through a failing `tools/list` in both modes and the null wire
    contract. Vendored precomputed exports + SHA pins refreshed.
- `9b033b4642` #44628 session analytics state in Responses turn metadata,
  `196964ef10` #44611 root turn attribution in turn-start events,
  `e004dc6a4b` #44659 turn triggers across delegated agent work,
  `c8a8295e79` #44656 turn metrics attributed to models,
  `0df6366a87` #44336 bounded tool-result metadata,
  `78932f4493` #43918 queued event count on `CodexThread`.
  - **status: pending** for `c8a8295e79`
    (`0df6366a87` #44336 bounded tool-result metadata: **status: done** -
    `model.ToolResultMetadata` is a host-only, size-bounded MCP `_meta` snapshot
    that serializes transparently or as the `omitted_due_to_size_limit` marker,
    never deserializes from untrusted input, and redacts its `String()`. It is
    attached to `ExecutedToolCall` (`SetToolResultMetadata`/
    `HasToolResultMetadata`) and shed before source evidence, calls, or
    completion markers when the request budget is exceeded. Source parse
    failures now serialize as the array entry `{type:"parse_failed",id:""}`.
    Raw metadata is filtered by resolved destination to HTTPS `api.openai.com`
    or allowed ChatGPT hosts for both HTTP and WebSocket requests, and the
    `turn` recorder gained `RecordToolResultMetadata` (MCP capture remains
    disabled). Tests cover bounds/redaction/serialization, the shed ordering,
    parse-failed shape, destination allow/deny, and direct/Code-Mode recording.)
  - `78932f4493` #43918 queued event count: **N/A** - it exposes
    `CodexThread::queued_event_count()` = `io.rx_event.len()` on the Rust tokio
    mpsc event receiver. Go's core session/turn/agent code has no per-thread
    event queue (the app server streams notifications directly; no
    `PendingEvents`/`eventQueue` exists), so there is nothing to expose.
  - `c8a8295e79` #44656 turn metrics by model: Go has a native `state.TaskMetrics`
    layer but no `codex.guardian_v2`/turn-token histogram with `token_type`
    tags, so this is portable-but-pending telemetry work rather than N/A.
- `e017e93ace`... (sync30 already landed earlier schema work); `2cfee7de25`
  #43039 `app/installed` live tool refresh, `32351a7b1a` #43031 MCP tool
  catalogs with their clients.
  - **status: pending**.

### batch-config-requirements-sync31
- `e53c444964` #44691 Warn about ignored configuration settings.
  - **status: done (config + requirements core)** - `config/ignored_settings.go`
    implements `IgnoredConfigWarning`: it collects unrecognized fields from the
    merged effective configuration (top-level keys plus unknown feature keys,
    including under `[profiles.<name>.features]`) and from the raw
    `requirements.toml` top-level keys, attributes each to its source layer,
    dedupes, and emits the bounded warning with the Rust migration hints for
    `network_proxy`, `allowed_permissions`, and `include_view_image_tool`
    (values are never included; labels are control-escaped and capped at 200
    bytes). `ConfigService.IgnoredSettingsWarning(cwd)` computes it for the
    startup layers (empty cwd) or the full CWD layer set, and the app server
    emits it at `initialize` and again for a thread's project layers at
    `thread/start`, suppressing repeats. Tests cover merged/layered collection,
    requirements, value redaction, the 3-entry bound and hints, and the
    initialize + dedup delivery.
  - Known gap: Go's manual config schema cannot enumerate unknown fields at
    every nesting depth the way Rust's serde_ignored does; nested coverage is
    limited to the schemas Go already validates (notably features and
    requirements). Requirements inspection covers the local `requirements.toml`;
    typed managed-requirement overrides lose unknown keys and are not inspected.
- `aff3e0db94` #44693 Preserve selected profile settings over managed new-thread
  defaults
  - **status: pending**.
- `1aaa453ce2` #44650 Enforce managed model provider selection and definitions
  - **status: done (local requirements path)** - `ConfigRequirements` gained
    `modelProvider`/`modelProviders` (parse, wire marshal, clone, empty check);
    `applyManagedModelProviderOverride` forces the required selection and
    replaces each required provider definition wholesale (including auth and
    headers) in the effective config; config write APIs reject managed
    `model_provider`/`model_providers` paths, including provider IDs containing
    dots. Tests cover override precedence, local-id replacement, requirement
    exposure, and write rejection. The cloud-bundle fragment-merge/path-
    resolution path is Go-specific and already resolves source-relative paths.
- `ed4ca07ba6` #44241 credential provider source remapping across config layers;
  `e8e7103cb9`... - **status: pending**.
- `5c94936b56` #44654 missing environment variable diagnostics in doctor;
  `3422443ec4` #44615 non-interactive dumb terminals as doctor warnings
  - **status: #44615 done** - `TERM=dumb` is a warning (no replacement
    suggestion) when neither stdin nor stdout is a terminal, stays a failure
    otherwise, and terminal issues are sorted by descending severity so
    failures surface first. Tests cover the four stream combinations and the
    retained non-interactive details.
  - **status: #44654 done** - `redactDoctorDetail` now preserves the env-var
    name and `is not set` diagnosis for `env var` / `bearer token env var` /
    `header env var` details before generic secret redaction, redacting
    malformed or credential-shaped names (sk-/ghp_/github_pat_/xoxp-/AWS keys)
    and identifiers, and stripping URL credentials. Tests mirror Rust's
    redaction cases plus the JSON detail map.
- `5b682c9875` #43948 Show configured app-server updater settings in doctor
  - **status: done** - `backgroundServerCheck` reads the daemon
    `settings.json` (bounded to 16 KiB) and reports `automatic updates:
    enabled|disabled (configured)` and `update interval: N minutes
    (configured)`; malformed settings (non-boolean flag, zero interval,
    oversized/unreadable file) report `configured updater settings: unreadable
    or invalid` instead of an effective value, and a missing file reports
    nothing. Tests cover valid, invalid, and missing settings.
- `1bfd383890` #44056 configurable credential providers in the network proxy,
  `5a9aec40a5` #44066 configured credential brokerage aliases,
  `f45115a137` #44068 broker destinations across environment filtering,
  `38cbebaf3f` #44072 providers across shell snapshots,
  `ec512d2347` #44040 credential handling in shell snapshots/replay,
  `a548463b78` #44038 copied credentials in broker and shell snapshots,
  `2e220af1f6` #43909 protect shell snapshots with brokerage,
  `634ebc1865` #44089 plaintext HTTP tunnel brokering,
  `56d3e8192f` #44077 credential-broker tunnel protocol detection,
  `9ba1d9eb5b` #44049 credential broker environment/registry helpers,
  `808b3411fd` #43954 protected shell snapshot cache.
  - **status: pending**.

### batch-model-runtime-sync31
- `9b033b4642` #44628 Expose session analytics state in Responses turn metadata
  - **status: done** - `codexapi.AnalyticsEnabledKey` (`analytics_enabled`) is
    reserved and filtered from client/product metadata;
    `turn.MetadataState` and `turn.ResponsesClientMetadataOptions` carry an
    optional analytics flag emitted only when set; the app-server resolves it
    from the selected session analytics client's `Enabled()` for turn,
    turn-recovery, and web-search requests (omitted when no session analytics
    context is initialized). Tests cover enabled/disabled/omitted states,
    reserved-key filtering, and `MetadataState` aliasing.
- `196964ef10` #44611 Preserve root turn attribution in turn-start events
  - **status: done** - `rollout.Recorder.AppendTurnStartedWithRoot` persists
    `root_turn_id` on the turn-start event (the inherited root turn ID when
    available, otherwise the turn's own ID), `AppendTurnStarted` keeps the
    legacy shape, and the replay builder restores attribution into
    `session.TurnSnapshot.RootTurnID`; older records default to none. The
    app-server turn-start, shell-command, and manual-compaction paths route
    through one `rootTurnIDForTurn` helper. Tests cover persistence, legacy
    compatibility, replay attribution, and the fallback rule.
- `60825b4988` #44646 Honor thread analytics opt-outs when using shared clients
  - **status: done** - a thread (or turn) whose configuration sets
    `analytics_enabled = false` suppresses its own analytics events without
    affecting sibling threads or re-enabling a host-disabled client. A single
    `threadAnalyticsDisabled` helper resolves the persisted thread config
    snapshot plus the active turn config, and every thread-scoped emitter
    (turn, turn-steer, thread-initialized, compaction, goal, hook-run, review/
    guardian, skill invocation, artifact operation, accepted-lines, command/
    file/mcp/dynamic/collab/web-search/image tool items) honors it. Turn
    metadata reports the effective per-thread analytics state. Tests cover the
    config rule, per-thread opt-out vs siblings, host-disabled suppression, and
    the metadata option.
- `713caa89f3` #44523 Bound app-server stdio shutdown and handle Unix SIGTERM
  - **status: done (Go equivalent)** - the standalone app-server now serves
    stdio with `ServeWithShutdown`: EOF or Unix SIGTERM cancels the connection
    so the existing cleanup path runs (notification/server-request sinks
    cleared, `router.Close()` terminates owned commands and runs session-end
    hooks), and a shared 45-second watchdog preserves the first EOF/SIGTERM
    deadline, exiting with status 1 if teardown stalls. The read loop was
    refactored to read stdin on a dedicated goroutine and select on
    cancellation so SIGTERM does not wait for a blocked pipe. Rust's dedicated
    Tokio-runtime concerns (uncancellable blocking stdin, watchdog independent
    of logging) map to Go's process-owned goroutine plus an `os.Exit` watchdog.
    Tests cover EOF arming, cancellation ending the loop, and the watchdog
    first-deadline behavior.
- `0735c51978` #44320 Block goals after three empty automatic continuation turns
  - **status: done** - automatically admitted goal continuations are marked
    (`markGoalContinuation`) and their finished turns are classified from the
    model result (`recordGoalResultItems`): user/reasoning/tool/other items
    mark activity, a non-commentary agent message with no text marks an empty
    final answer, and tool executions always mark activity. Three consecutive
    empty automatic continuations block the active goal with the same
    accounting/status/notification ordering as the execution-failure path; the
    streak resets on activity, non-automatic turns, aborts, goal create/update/
    clear, and goal changes. Tests cover the threshold, activity resets
    (final-answer text and tool success), non-automatic turns, and goal change.
- `cc737efd65` #43540 / `4f1a2bb5ff` #43545 preserve the multi-agent version
  across fork cutoffs
  - **status: already satisfied (Go architecture, audited)** - Go persists the
    resolved multi-agent version on the thread record (`Metadata.MultiAgentVersion`,
    written at thread start and agent spawn) instead of only in a `TurnContext`,
    and `session.forkRecordLocked` clones the source metadata before applying the
    turn cutoff. A trimmed fork therefore cannot lose the runtime version, and
    `runtimeMultiAgentVersionForTurn` prefers the persisted record value. Added
    `TestStoreForkPreservesMultiAgentVersionAcrossCutoffLikeRust` to lock the
    behavior.
- `818f1cca8c` #44586 Remove `repo_url` from skill invocation analytics events.
  - **status: done** - dropped `SkillInvocationEventParams.RepoURL` and its
    producer assignment; the repository URL is still folded into the derived
    skill ID (as in Rust), it is simply no longer serialized in the event
    payload. Tests updated.
- `f046cf35df` #43906 model catalog caches scoped to provider/auth identity,
  `f31bd3adff` #43897 persist provider and auth identity with caches
  - **status: done** - the disk `models_cache.json` stores an opaque
    `identity` (`model.ModelsCatalogIdentity`: provider id + account
    type/plan/email + credential fingerprint hash) and the manager rejects
    cache reuse when the identity is absent (legacy unscoped entries) or does
    not match the current provider/auth; TTL renewal only extends freshness
    when version, identity, and ETag all still match; an empty identity
    disables cached reuse. Both production managers (provider resolution and
    the app-server lazy manager) supply the identity. Tests cover the fresh
    hit, wrong version, identity mismatch, legacy unscoped entry, and an
    unscoped manager.
  - `94e4b3d0bd` #43895 preserve `__oailb` routing cookies: **N/A** -
    verified Go has no ChatGPT infrastructure/Cloudflare cookie store or
    `CookieJar` (`chatgptapi`, `codexapi`, `network`, `appserver`, `auth`,
    `model` contain no cookie handling), so there is no allowlist to extend.
  - `8e694e955a` #43604 exclude base instructions from bundled model catalog:
    **N/A (data-driven)** - Go does not bundle `models.json`; `model/catalog.go`
    reads the upstream `models-manager/models.json` at runtime, which the
    updated Rust checkout no longer populates with `base_instructions`.
  - `eb7bd64ef9` #44250 remove retired model entries preserving migration
    prompts: **N/A (data) + TUI-deferred** - the only non-test changes are
    `models-manager/models.json` (read directly by Go) and TUI
    `config_persistence.rs` / `startup_prompts.rs`.
  - `ea53c8d4f7` #44392 opt-in model discovery for OpenAI API keys:
    **status: done** - OpenAI API-key auth can now opt into authoritative model
    discovery. `RemoteModelsManager` records `supportsAPIKeyModels` /
    `apiKeyAuth` / `commandAuth`, exposes `SupportsAPIKeyDiscovery`, and gates
    both refresh requests and cached catalogs behind
    `SetAPIKeyModelDiscoveryEnabled`; a visible remote catalog becomes
    authoritative for API-key discovery just like a ChatGPT account catalog. An
    OpenAI provider with API-key auth and no explicit base URL routes the models
    request to `ChatGPTCodexBaseURL` while inference keeps its own base URL.
    `api_key_model_discovery` was added to the app-server experimental feature
    enablement list, and the app-server applies the policy at model-manager
    construction and after `experimental/feature/set`. `LazyModelsManager`
    forwards the policy (remembering it until the manager is built) and
    `ModelService` exposes the setter. Tests cover the disabled/enabled gate,
    authoritative catalog replacement, provider/auth prerequisites, the base-URL
    routing, and feature enablement.
  - `6eecd04fc1` #44249 normalize image detail for receiving model:
    **status: done** - request copies now normalize image details for the
    receiving model. Responses Lite still strips details, while other models
    downgrade `detail = "original"` to `"high"` when the model does not
    advertise `supports_image_detail_original` (covering user messages and
    function/custom tool outputs). Stored items are never mutated, so switching
    back to a supporting model retains `original`. Implemented in
    `model/responses_agent.go` (HTTP and WebSocket request builders) and
    `codexapi.ClientPrompt.FormattedInput`; tests cover the downgrade,
    supporting-model passthrough, non-original details, and stored-item
    immutability.
- `102fc57e4a` #44492 distinguish HTTP quota errors from rate limits
  - **status: done** - `responsesHTTPError` maps HTTP 429 bodies whose `type`
    is `insufficient_quota` or whose `code` is one of `insufficient_quota`,
    `credit_balance_exhausted`, `organization_spend_limit_exceeded`,
    `project_spend_limit_exceeded`, `organization_usage_limit_exceeded` to a
    `codexapi.ErrorQuotaExceeded` error (classified as
    `usageLimitExceeded`/`quota_exceeded`) instead of the generic rate-limit
    path; `rate_limit_exceeded`/`slow_down` keep returning `*ResponsesAPIError`.
    Tests cover every recognized quota body and the two rate-limit bodies.
- `5a65fd87d8` #43884 Close active network proxy connections on teardown
  - **status: done** - `ProxyServer` tracks every connection accepted by the
    HTTP and SOCKS5 listeners; teardown closes them before waiting so hijacked
    CONNECT tunnels and SOCKS5 connections no longer survive thread unload.
    Tracked wrappers preserve TCP half-close (`CloseWrite`/`CloseRead`) for the
    SOCKS5 relay, and opaque TCP relaying unwraps to the concrete connection
    while releasing its registration when the borrowed tunnel finishes (so
    tcpproxy's concrete-`*net.TCPConn` half-close detection keeps working).
    Tests verify an open CONNECT tunnel is closed (not timed out) after
    teardown.
- `e004dc6a4b` #44659 Preserve turn triggers across delegated agent work
  - **status: done** - the agent controller carries the initiating turn's
    trigger and sets `TurnTrigger` on both spawn and send-input child turns.
    This exposed an internal-request serialization gap: `turn/start` internal
    requests round-tripped through JSON, dropping internal-only params
    (`AdditionalInputItems`/`ParentTurnID`/`RootTurnID`/`Environments`/
    `TurnTrigger`). Internal requests now carry their params object via
    `Request.InternalParams`, so delegated turns keep parent/root attribution,
    the inherited trigger, environment selections, and (for V2) the
    inter-agent communication input. Client wire requests are unaffected
    (`InternalParams` is never decoded from JSON). `TurnTrigger` was also added
    to `TurnStartParams` wire marshaling to match Rust. Tests cover trigger/
    parent/root propagation and V2 communication-input delivery.

### batch-guardian-core-sync31
- `eca63f0803` #44536 + `4e6d5c0a96` #44544 reviewer settings/execution into
  `ext/guardian-reviewer`, `98a5cb46b1` #43570 thread manager ownership,
  `db0568dbbb` #43462 remove legacy approval paths, `d70044072c` #43490 shared
  reviewer helpers, `17e64839eb` #44166 aggregate budget enforcement,
  `d3ffbbed5a` #44164 cost/token telemetry, `fcd90d8f07` #44281 complete
  request budgets, `72348693ec` #44293 classifier input budget,
  `5b85aea979` #43442 consistent evidence / stale approvals,
  `e1eb98461c` #43432 extension decision API,
  `dee21ec1bc` #43002 tickets -> parent response IDs,
  `588b781ab4` #43005 failure reasons and connection timing metrics,
  `2bba3a29a0` #44181 histogram buckets.
  - **status: pending** - most Guardian context composition is Rust-crate
    restructuring; Go ports the budget/metrics/reviewer behavior that has a
    runtime effect.

### batch-not-applicable / rust-coupled (documented)
- `8c72f2ff56` #43961 curly apostrophes in protocol error messages: Go does not
  render Rust's usage-limit/high-demand display strings (the app server surfaces
  error codes), so there is nothing to change.
- `b1205c12d5` #43519 `recursion_limit = 256`: Rust compiler attribute, N/A.
- `b1205c12d5`, `dc55274818` #44694 / `1c40ffe427` #43144 / `a947db131b`
  #43114 etc.: Bazel / native voice runtime / Python SDK packaging /
  macOS-voice release plumbing, rust-coupled.
- `da20788df9`, `1a4096e273`, `8afccec87a`, `d45134c046`
  (#44084/#44086/#44032 Python SDK), `8283bc56b1` (#43281 npm staging):
  packaging/SDK tooling, not part of the Go runtime port.
- `4caa5d615d` #44658 / `e25bedc166` #44639 / `f11d0dd012` #44327 /
  `5e3f0ee94b` #43930 / `4fd2c460dd` #43903 (Windows sandbox service / MXC):
  native Windows service adapter and MXC native library are outside the Go port.

### batch-tui-sync31 (TUI LAST, per operator instruction)
- Realtime voice TUI (`b01c3986fd` #43581, `98c7c0415b` #43683,
  `4e93cf9b4e` #43656, `43645`...), async question components
  (`218e8df926` #42891, `07f18d5ff7` #42894, `c126b0d8ef` #42897,
  `6ae8dcf6e1` #42889), agents overview actions (`b348fc2667` #44433,
  `bf5ebd98c5` #44424, `3ef3cecd20` #44344), worktree browser
  (`b053ef9e5a` #43286, `f6976ab036` #43120, `d30f9cc72a` #43279),
  user-verification prompt component (`54e04f25db` #43702),
  folder trust/consent (`02a8f038b8` #44755, `84e7d4a1fe` #44746,
  `40b0409aa1` #44732), reduced motion (`78600239a1` #44666),
  `/copy` picker (`dd9512c000` #43846, `52e73e3a54` #43055),
  `e2a9ee05f4` #44354 footer hint wrapping, `9ec33e1926` #43921 streaming
  reasoning summaries, `333c41eef6` #43558 completion timestamps.
  - **status: pending (scheduled last)**.

## Remaining / not done this pass
- The batches above still marked `pending`:
  - `batch-schema-v2-sync31`: memory v2 + `memory/status`, MCP protocol mode
    per server, turn attribution/turn-trigger/tool-result metadata, live
    `app/installed` tool refresh.
  - `batch-config-requirements-sync31`: ignored-config warnings, managed
    provider/profile precedence, doctor env-var + updater-settings reporting,
    credential broker/proxy credential providers.
  - `batch-model-runtime-sync31`: model catalog caches scoped to provider/auth
    identity, base-instructions exclusion, retired model migration prompts,
    image-detail normalization, opt-in API-key model discovery.
  - `batch-guardian-core-sync31`: reviewer budgets/metrics/decision API.
  - `batch-tui-sync31`: all TUI ports (scheduled last).
- Residual detail for #44492: HTTP 429 quota bodies are now classified as
  quota errors, but the streaming retry loop still retries 429 before the body
  is read, so a quota 429 may consume its bounded retries before surfacing.
- L3 SDK differential (`sdktests`) remains gated on explicit model-cost
  authorization.
- L4 certification matrix (multi-platform build) not re-audited here.

## sync31 progress log (commits on main)

- `cf23744` L0 re-freeze to `02a8f038b8` + #44028 AWS credential export +
  thread-attachment stable surface.
- `79c2041` #44564 thread attachment add/list/remove runtime (+ tests).
- `973c018` #43265 experimental userVerification RPCs (+ tests).
- `6db4ede` #44628 analytics state in Responses turn metadata (+ tests).
- `ca9601c` #44492 HTTP quota errors distinguished from rate limits (+ tests).
- `fe4724b` #44615 non-interactive dumb terminals as doctor warnings (+ tests).
- `46594ac` upstream +1 test-only commit (#44814) v2 suite module.
- `f5c1d72` #44571 per-server MCP protocol mode for HTTP servers (+ tests).
- `4817c58` #44586 drop `repo_url` from skill invocation analytics (+ tests).
- `6117e1b` #44654 doctor missing env-var diagnostics (+ tests).
- `a19e733` #44650 managed model provider selection/definitions (+ tests).
- `671b81e` #43884 close active network proxy connections on teardown (+ tests).
- `5cb22d1` #44659 propagate turn triggers through delegated work (plus the
  internal-request params channel fix).
- `12857eb` #44611 root turn attribution on turn-start events (+ tests).
- `9408161` #43948 configured app-server updater settings in doctor (+ tests).
- `e5d12be` #44646 honor thread analytics opt-outs with shared clients (+
  tests).
- `dc41f84` #44523 bound app-server stdio shutdown and handle Unix SIGTERM
  (+ tests).
- `4950cbc` #44320 block goals after three empty automatic continuations
  (+ tests).
- `bfbbe91` #43540/#43545 audit + test for fork multi-agent version
  preservation.
- `a47bd50` #43897/#43906 scope model catalog caches to provider/auth identity
  (+ tests).
- `89e542d` #44826 expose advertised MCP server capabilities in status
  responses (+ tests; vendored exports/SHA pins refreshed for 7a6f469dcf).
- (next) #44832 trusted enterprise MCP auth configuration: `use_xaa` feature,
  `mcp_enterprise_managed_auth` + EMA server/plugin config surface, non-project
  provenance enforcement, runtime/login refusal, CLI round-trip (+ tests;
  parity schema/field/fixture/hash pins refreshed for 654b0a77d0).
- `73e8b98` #44832 trusted enterprise MCP auth configuration (committed and
  pushed).
- #44249 normalize image detail for the receiving model (batch commit): downgrade
  `original` -> `high` on request copies for models without
  `supports_image_detail_original`, keep Responses Lite stripping, preserve
  stored items (+ tests in `model` and `codexapi`).
- `ab5d94b` #44392 opt-in API-key model discovery: default-off
  `api_key_model_discovery` gate on requests + cached catalogs, authoritative
  API-key remote catalog, Codex-backend models routing, experimental feature
  enablement, app-server wiring (+ tests in `model` and `features`).
- (next) #44691 warn about ignored configuration settings: bounded warning for
  unrecognized config/requirements fields with source attribution, migration
  hints, value redaction, and initialize/thread-start delivery with dedup
  (+ tests in `config` and `appserver`).
- (next) #44336 bounded tool-result metadata on executed tool calls: host-only
  size-bounded MCP `_meta` snapshots with redaction, shed before sources/calls,
  `parse_failed` array shape, and per-destination filtering for HTTP/WebSocket
  (+ tests in `model` and `turn`).
- (next) #43827 memory/status contract: `v2ConsolidatedThreads`/`v2Ready` RPC
  with threshold validation, shared v2 summary validation, consolidation
  progress persistence + reset, and `memories.dual_write` config (+ tests in
  `memories`, `config`, `appserver`).
- (next) #43797 configurable memory versions: `memories.version` v1/v2 enum with
  load validation, `memories_v2` isolated root routing for instructions/pipeline,
  dual-root usage telemetry (+ tests in `config` and `memories`).
- (next) #43813 dedicated memory v2 read instructions: byte-pinned
  `read_path_v2.md` template routed by memory version (+ tests in `memories`).

Each commit was verified with `go build ./...` and the relevant package tests;
the relevant package suites (`parity`, `model`, `appserver`, `app`, `session`)
are green as of `a47bd50`. Voice packages are owned by a parallel process and
are excluded from this plan's verification.

## Upstream commit inventory (344 commits, oldest -> newest)

See `git -C D:\qax\reagent\dev\git\codex log --oneline 574a36ff99..02a8f038b8`.
