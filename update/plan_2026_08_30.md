# Codex Go update alignment plan - 2026-08-30 (sync29: 100-commit upstream range)

## Baseline

- Rust upstream checkout: `D:\qax\reagent\dev\git\codex`
- Rust previous frozen target (sync28, static freeze): `307ce6cda9` (#41023)
- Rust previous HEAD (sync28 last applied): `b592a0bfed` (#41072)
- Rust new HEAD (sync29): `63d213884d` (#41586) - "Add Vim search motions to the composer"
- Rust `git pull origin main` advanced `b592a0bfed..63d213884d` (100 upstream commits, 529 files changed, +28459/-5328).
- Go baseline commit: `cd19116` (sync28 landed, `docs: record L3 SDK blocker ...`)
- Go baseline state (pre-sync29): `go build ./...` clean; `go test ./parity -count=1` green at the previous freeze; `go test ./features -count=1` green.

The certified baseline (`parity.json`) remains `970b7f2ff4` (#40031, sync26 certification target). `candidateRustTo` in `parity/alignment_control_test.go` is intentionally left at the certified baseline and is **not** advanced by this static re-freeze; the L0 freeze target for sync29 is the active alignment target `63d213884d`.

## L0 static re-freeze - DONE

The upstream range heavily re-organizes Rust assets and adds many new config/schema/feature surfaces, so the L0 static layer (critical file hashes, workspace members, vendored schema blobs, fixture/TUI snapshot manifests, feature-key surface, config schema surface, ModelInfo wire surface, top-level directory inventory, unified-exec sandbox manifest, contract oracle paths) required a re-pin against `63d213884d`.

### What was refreshed

- `parity/rust_snapshot_test.go`
  - Workspace members: added `guardian-context` (new crate, 137 -> 138).
  - Critical file SHA256 re-pinned for `Cargo.toml`, `exec/src/lib.rs`, `core/src/client.rs`, `app-server-protocol/src/protocol/common.rs`, `core/tests/suite/mod.rs` (5 changed of 9).
- `appserver/schema/precomputed/app-server-exports-{stable,experimental}.json.zst`: re-vendored byte-identical to the regenerated Rust blobs (stable 143375 -> 143884, experimental 147333 -> 148284).
- `parity/rust_fixture_manifest_test.go`: `cli/tests` 20 -> 21, `core/tests/suite` 172 -> 177.
- `parity/rust_tui_snapshot_manifest_test.go`: total 718 -> 722, `tui/src/bottom_pane/snapshots` 200 -> 201 (vim_composer_search), `tui/src/chatwidget/snapshots` 240 -> 243 (model_picker x3).
- `features/features.go`: added 4 new Rust feature keys with matching stage/defaults:
  - `sleep_tool` (#41243): Stable, default-on.
  - `powershell_shell_version` (#41232): UnderDevelopment, default-off.
  - `local_thread_store_shared_compression` (#41357): UnderDevelopment, default-off.
  - `omit_app_server_notification_media` (#41416): UnderDevelopment, default-off.
- `model/catalog.go` `ModelInfo`: added `multi_agent_reasoning_effort *string` (`json:"multi_agent_reasoning_effort,omitempty"`), mirroring Rust `openai_models::ModelInfo` (#41206 Ultra reasoning fallback / multi-agent reasoning effort). Surface count 41 -> 42 (Rust), Go JSON count 43 -> 44.
- `parity/config_schema_surface_test.go`: config.schema.json top-level property count 95 -> 96; allowlisted the new `mcp_optional_startup_grace_ms` (#41199) as a Rust-only config key Go does not yet parse.
- `parity/rust_surface_drift_test.go`: ConfigToml top-level field count 98 -> 99 (`mcp_optional_startup_grace_ms`).
- `parity/rust_inventory_test.go`: `guardian-context` added to the Rust top-level directory inventory.
- `parity/rust_unified_exec_sandbox_manifest_test.go`: `core/tests/suite/unified_exec.rs` gained `managed_one_shot_command_is_terminated_when_the_turn_is_interrupted` and `managed_unified_exec_disable_runs_commands_without_retained_authority`.
- `parity/contracts/manifest.json`: `apply-patch-freeform-spec` rustPaths `core/src/tools/handlers/apply_patch.lark` -> `core/assets/tools/apply_patch.lark` (#41477 asset re-org).

### Verification (this pass)

- `go build ./...` clean.
- `go test ./parity -count=1` pass (full static + L0/L1 surface + differential gates).
- `go test ./features -count=1` pass.
- `go test ./model -count=1` pass (ModelInfo field addition).

## sync29 runtime batch roadmap (work queue)

Each batch is `status: pending` until its runtime behavior and verifier land. Batches are ordered by Go-side value/feasibility. Many upstream commits are Rust-internal or tied to subsystems the Go port does not implement; they are marked `rust-coupled` and inherited via the Rust components rather than ported.

### batch-config-fields-sync29
- `124e560b93` #41199 Make the optional MCP startup grace configurable
  - Go config does not yet parse `mcp_optional_startup_grace_ms`; allowlisted at L0. The per-server optional-startup grace window needs a config field plus MCP startup gate wiring.
  - **status: done** - `mcp_optional_startup_grace_ms` parsed (`Config.MCPOptionalStartupGrace`, absent=1s, explicit 0=disabled) and wired into the turn MCP catalog capture; `OptionalStartupGrace==0` now routes to the blocking startup wait. Removed from the L0 Rust-only allowlist (Go key count 101->102).
- `f6494dc8f5` #41210 Enable clock tools from model metadata
  - Convert current-time/clock tool enablement from feature-flag-only to model-metadata driven (mirrors #40942 which landed the persistent-reasoning default). **status: done** - `modelHasClock` detection + `enableCurrentTimeTool = currentTimeReminderEnabled || modelHasClock` in `decideClockToolEnablement`.
- `ec9620c231` #41243 Add configurable gating for the sleep tool
  - `sleep_tool` feature key added (Stable/default-on). Runtime gating (per-turn enable / disable) plus the sleep tool plan wiring is the remaining behavior. **status: done (gating)** - `config.SleepToolMode()` + always_on/model_driven gating in `decideClockToolEnablement`.

### batch-model-catalog-sync29
- `03861e69ef` #41457 Source proactive multi-agent instructions from the model catalog
  - **status: done (core)** - `MultiAgentModeMessages.Proactive` added; `multiAgentModeCatalogText` sources the catalog proactive message for proactive (Ultra) mode (nil → built-in, empty → suppress the `<multi_agent_mode>` message). Mid-turn model-switch refresh not yet replicated.
- `3c062df036` #41461 Source async user message descriptions from the model catalog
  - **status: done (core)** - `ModelMessages.Tools`/`ToolMessages`/`ToolMessage` added to the Go model catalog; `send_user_message_async` tool description overrides the built-in from the catalog's `tools.send_user_message_async.description` (nil falls back, explicit empty string preserved). Mid-turn model-change refresh not yet replicated.
- `b8c86376a2` #41570 Fix proactive multi-agent instruction grammar
  - **status: done** - `proactiveMultiAgentModeText` aligned to Rust ("If at any point...").
- `7f135e1314` #41206 Make Ultra reasoning fallback model-aware
  - `multi_agent_reasoning_effort` / `ModelMessages.tools.send_user_message_async` / proactive instruction fragments are model-owned. The catalog parsing of these fields (and the model-owned tool-message override) needs Go-side ModelMessages / model-catalog wiring.
  - **status: done (core)** - `responsesReasoningParam` now maps Ultra through the model-aware fallback (`reasoningEffortForRequest`): supported `multi_agent_reasoning_effort` override, else `max`, else highest supported non-`ultra`, else `medium`. Covered by `TestReasoningEffortForRequestModelAwareUltraFallback`. `gpt-test` fixture gained supported reasoning levels to keep `ultra → max`.
- `2c4a95736b` #41087 Expose response usage metadata in completion events
  - `response_usage` metadata surfaced in completion events (new `ResponseUsageMetadata` fields on the v2 schema). Needs Go event/response mapping.
  - **status: done** - `model.ResponseUsageMetadata` parsed from `response.usage_metadata.amount` (preserved as a string) and propagated to the `rawResponse/completed` notification's `usageMetadata`.

### batch-turn-session-sync29
- `4210c08def` #41562 Preserve turn lineage across goal continuations
- `f5636bb733` #41567 Restore thread cwd from owned settings snapshots
  - **status: done** - the thread-owned settings snapshot now persists the owner `thread_id` and `cwd`, and a cold resume restores the thread's own retained cwd from the latest owned snapshot while ignoring fork-copied snapshots owned by another thread (so a forked session does not inherit a foreign cwd). `rollout.LastPersistedOwnedThreadCWD` scans the rollout lines for the latest `thread_settings_applied` whose `thread_id` matches the resolved thread; legacy snapshots without an owner stay readable for approval policy but no longer override the startup cwd. `Router.handleThreadResume` resolution order is `params.CWD`, then owned cwd, then `record.Metadata.CWD`. `RuntimeRouter.persistThreadSettingsUpdate` captures the thread's current `settings.CWD` onto the snapshot. Covered by `TestLastPersistedOwnedThreadCWDLikeRust`.
- `0d22692962` #41429 Retain the last selected step context for each turn
- `f9cdc90c2c` #41424 Preserve context baselines across nested agent forks
- `0b45b171ca` #41464 Preserve permissions when updating session metadata
  - **status: no-op (Go divergent permission model)** - Rust's fix lives in `SessionConfiguration::apply` (legacy-sandbox-policy projection: it only recomputes a cwd-bound legacy sandbox policy when a cwd change may require rebinding project-root writes, and keeps client name/version updates from resolving filesystem paths or mutating the permission snapshot). The Go port does not have an equivalent monolithic session-settings `apply`: `ThreadExtraService.UpdateSettings` only stores per-thread settings (CWD/approval/model/service-tier/...) and `RuntimeRouter.persistThreadSettingsUpdate` only persists approval-policy/permission-profile plus the owned cwd snapshot (no sandbox-policy projection). So Go does not recompute a sandbox policy on a client-metadata update, and the Rust regression has no Go equivalent as-is.
- `8228e9b867` #41162 Resolve token budgets from each step's active model
  - **status: done (context-window guidance transition + ModelInfo::usable_context_window)** - `contextWindowGuidanceWorldStateInputItem` emits a one-time replacement notice (`This context-window guidance replaces all previously provided context-window guidance.`) when guidance changes and a removal notice (`The previously provided context-window guidance no longer applies.`) when guidance is disabled/blank, mirroring Rust's world-state section diff. Covered by `TestRuntimeRouterContextWindowGuidanceTransitionsLikeRust` (updated `TestRuntimeRouterContextWindowGuidanceWorldStateDiffs`). Added `model.ModelInfo.resolvedContextWindow`/`usableContextWindow`, mirroring Rust `ModelInfo::usable_context_window` so reserved-headroom capacity is distinguishable from the resolved context window and auto-compaction limit; covered by `TestModelInfoUsableContextWindowLikeRust`. The per-step token-budget resolution remains a distinct sub-feature to be ported separately.
- `4761851ff3` #41183 Account subagent token usage toward root goals
  - **status: done** - descendant/subagent token usage is recorded via `recordGoalTokenUsageWithDescendants` in the turn's `OnTokenUsage` callback and rolled into the root ancestor thread's goal through `recordDescendantGoalTokenUsage` (the `ParentThreadID` spawn lineage is walked to the root). Active and idle goal progress now include the descendant token delta, and the descendant accounting baseline resets when a goal is (re)anchored or replaced. Covered by `TestDescendantTokenUsageRollsIntoRootGoalLikeRust`, `TestDescendantTokenUsageBaselineResetsOnGoalChangeLikeRust`, and `TestDescendantTokenUsageRoutedToRootThreadGoalLikeRust`.
- `dc2ccc6843` #41308 Make subagents follow the root service tier
  - **status: done (appserver spawn slice)** - the app-server spawn path now follows the root thread's service tier instead of a per-spawn override: `runtimeAgentController.Spawn` no longer honors `args.ServiceTier`; instead it reads the root thread's `Metadata.ServiceTier` via the new `rootServiceTierForSpawn` helper and applies it to the subagent's `TurnStartParams.ServiceTier`/`ServiceTierSet`. `RuntimeRouter.appServiceTierForTurn` then drops an unsupported tier (and gates on fast_mode), mirroring Rust's silent-drop model-support check. `service_tier` was removed from the `spawn_agent` tool input schema in `agent/tools.go`. Covered by `TestRuntimeAgentControllerRootServiceTierForSpawnLikeRust`. Verified: `go build ./...`; plaintext `go test ./agent ./appserver ./parity ./features ./config ./turn ./exec ./tool -count=1` all green; `go vet`/`gofmt` clean.
  - **exec-side slice done (2026-08-30)** - the exec spawn path no longer honors a per-spawn `args.ServiceTier`: `resolveSpawnModelOverrides` ignores a provided `service_tier` (removed the validate/resolve block) and `startTask` no longer emits a `service_tier=` config override. Because `childReq` clones the root `s.base` request, a subagent now inherits the root thread's tier from the cloned base instead of a per-spawn override, matching Rust. Covered by the updated `TestExecAgentControllerValidatesV2SpawnModelOverrides` (per-spawn tier left unmodified, not validated/resolved). Exec + agent + appserver + parity + features + config + turn + tool tests green.
  - **per-turn propagation done (2026-08-30)** - Rust `Session::get_or_prepare_turn` overrides the resolved step-settings service tier for every subagent turn, not just the spawn turn. The Go app-server now does this in `appTurnConfig`: after `appServiceTierForTurn`, if the turn's thread is a subagent and its root thread has a configured service tier, `serviceTier` is re-resolved from the root tier via `model.ServiceTierForRequest` (preserving fast_mode gating and dropping an unsupported tier). New helper `RuntimeRouter.subagentRootServiceTier` walks the parent-thread chain to the root (returns "" for non-subagents or a root with no tier). Covered by `TestRuntimeRouterSubagentRootServiceTierLikeRust`. appserver + agent + exec + parity + features + config + turn + tool tests green.
  - **role-level removal done (2026-08-30)** - Rust #41308 removed role-level `service_tier` entirely (role.rs, multi_agents_common.rs). The Go port now does too: `service_tier` is removed from `SpawnAgentArgs` and `SpawnDefaults`; `RoleAwareToolController.SpawnAgent` no longer applies a `ServiceTier` spawn default and `applyRoleSpawnSettings` no longer maps a role `service_tier`; `RoleResolver.Apply` no longer applies a role `service_tier`; and `lockedSettingsNotes` no longer renders the role `service_tier` note in the spawn tool description. Tests updated (`TestRoleResolverApplyPreservesProviderUnlessSet`, `TestSpawnToolDescriptionFormatsRoles`, `TestRoleAwareToolControllerAppliesDefaultsAndLockedRoleSettings`, `TestExecAgentControllerValidatesV2SpawnModelOverrides`). agent + exec + appserver + parity + features + config + turn + tool tests green.
  - **remote-compaction done (2026-08-30)** - Rust `compact_remote_request`/`compact_remote_v2` route remote-compaction requests through `step_context.settings.service_tier` (the resolved root tier for subagents). The Go port now resolves the compaction service tier via `RuntimeRouter.remoteCompactServiceTierForRecord`: it uses the thread's `Metadata.ServiceTier` but takes the root thread's tier for a subagent (`subagentRootServiceTier`), then applies the model-support check (`model.ServiceTierForRequest`) so an unsupported tier is dropped. `agentCompactRunner` carries `serviceTier` and sets it on the compaction `AgentRequest`. Covered by `TestRuntimeRouterRemoteCompactServiceTierLikeRust` (child inherits root tier; unsupported tier dropped; root-with-tier applied).
  - **status: port complete** - #41308 is fully ported across appserver spawn, exec spawn, per-turn step-settings, role-level removal, and remote compaction. Remaining work for this batch is only the dynamic L1-L3 differential verifiers (record-replay/SDK), driven by the standard per-batch flow as in sync28.
- `5f49aba876` #41165 Require explicit requests for spawn model overrides
  - **status: done (no-op / already aligned)** - Rust only removes the `or there is a clear task-specific reason` clause from the long-form `spawn_agent_tool_description` in `multi_agents_spec.rs`. The Go port's `spawn_agent` description is the brief `"Spawns a sub-agent to work on a task."` (`agent/tools.go`), which contains neither the removed clause nor the model-override guidance; no Go text needs changing and no Go test pins the long description. No Go-side change required.
- `19321435b1` #41207 Propagate executor OS into turn environments
  - **status: done** - exec-server `EnvironmentInfo` and app-server `EnvironmentInfoResponse` carry `platformOs` (local exec servers report `runtime.GOOS`); the value is preserved on each selected `UnifiedExecEnvironment`. Covered by `TestEnvironmentInfoReportsLocalPlatformOSLikeRust`.
- `d4d2b293b4` #41204 Propagate executor home directories into sandbox contexts
  - **status: done** - executor-reported user home is carried in exec-server environment info, cached on the selected turn environment, and URI-normalized into the filesystem sandbox context (local + apply_patch/exec paths), with executors that do not report a home preserved as empty. Covered by `TestFileSystemSandboxContextPropagatesUserHomeDirLikeRust`.

### batch-mcp-plugin-sync29
- `5bf0ba3dd6` #41202 Let extensions process MCP tool results
- `f742dabc6f` #41421 Support per-tool MCP output limits
  - **status: done** - `mcp.ToolConfig` gained `OutputTokenLimit` (`output_token_limit`), parsed from the runtime config map; `RestrictOutputTokenLimit` applies the most-restrictive (smallest) limit while keeping the approval mode independent. `MCPService.ConfiguredToolOutputLimit` resolves the effective limit for a server/tool, and `ToolExecutor.Execute` truncates an over-budget MCP tool response before it reaches the model. Covered by `TestToolConfigOutputTokenLimitParseLikeRust`, `TestToolConfigRestrictOutputTokenLimitLikeRust`, and `TestTruncateMCPResponseToOutputTokenLimitLikeRust`.
- `92f887ec35` #41344 Use refreshed MCP tool caches during binding capture
- `7625343977` #41336 Preserve cached MCP tools during binding capture
- `d9511fb788` #41400 Refresh MCP HTTP helper headers after authorization failures
- `c4c51c56e4` #41208 Honor per-repository plugin configuration in catalog requests
- `8bcac28f93` #41375 Preload plugin recommendations during session startup
- `91c66024c7` #41231 Instrument the loaded plugin cache
- `bb998aeb8e` #41396 Refresh runtimes for remote plugin state changes
- `56239d7a61` #41193 Report affected capabilities from remote plugin syncs
- `aa89cf62bb` #41117 Freeze plugin roots in MCP tool attribution
  - **status: done (core)** - the host-discovered plugin root is retained on `ServerRegistration`/`ConfigOverlay` and carried through `PluginAttribution` when building trusted MCP attribution, so a later plugin-ID lookup cannot resolve a different root. Covered by `TestPluginAttributionFromRegistration` and `TestRuntimeConfigFreezesPluginHostRootLikeRust`.
- `e9a6d731b7` #41230 Apply app routing policy to unauthenticated plugin reads
  - **status: done** - `handlePluginRead` now applies the plugin app/MCP routing policy even without an auth mode: unauthenticated reads omit unavailable app declarations while retaining MCP server alternatives. Covered by the extended `TestAppMcpRoutingPolicy` empty-auth case.
- `2181224dad` #41456 Support app targets in executor plugin hooks

### batch-exec-sandbox-sync29
- `b836aecd4d` #41393 Preserve one-shot exec when unified exec is disabled
  - **status: done** - when resumable unified exec is disabled, `exec_command` stays available in a completion-only (one-shot) mode: `BuildToolRegistry` registers a one-shot `exec_command` executor (no PTY/session/write_stdin, `timeout_ms` argument), and a timed-out one-shot command reports exit code 124. Covered by `TestOneShotExecSpecRemovesSessionArgsLikeRust`, `TestOneShotExecReportsExit124OnTimeoutLikeRust`, and the updated `TestBuildToolRegistryHonorsToolDisableOptions`.
- `e4d0ba4e92` #41384 Support configurable timeouts for thread shell commands
  - **status: done** - `ShellCommandParams.TimeoutMs` (optional, non-negative; default one hour, zero = immediate timeout) is validated and propagated to `thread/shellCommand` execution via a context deadline. Covered by `TestShellCommandParamsTimeoutValidationLikeRust`.
- `34e74fda0e` #41209 Align deny-read matching with executor path semantics
- `426fa8cdab` #41227 Use compatible PowerShell for elevated Windows sandbox commands
  - **status: done** - a local elevated Windows sandbox PowerShell command that targets a Store (WindowsApps) shell is rewritten to the first compatible `pwsh.exe`/`powershell.exe` discovered on PATH or the standard fallback locations, and `-NoProfile` is ensured. New `tool/shell_sandbox.go` (`isInaccessibleWindowsAppsPowerShellPath`, `isElevatedSandboxCompatiblePowerShellPath`, `fallbackPowerShellShellForElevatedWindowsSandbox`, `preparePowerShellCommandForElevatedWindowsSandbox`) mirrors Rust `shell_detect.rs`/`runtimes/mod.rs`; `UnifiedExecManager.execWindowsSandbox` applies the rewrites. Remote/non-elevated/non-PowerShell commands are preserved. Covered by `TestElevatedSandboxFilterRejectsStoreAndScriptPowerShellPaths`, `TestPreparePowerShellCommandForElevatedWindowsSandbox`, and `TestFallbackPowerShellShellForElevatedWindowsSandboxNonStoreReturnsNil`. Verified: `go build ./...`; `go test ./tool ./appserver ./exec ./parity ./features ./config ./turn ./agent -count=1` green; `go vet`/`gofmt` clean.
- `5ed294d49d` #41368 Match Windows shell guidance to the executor platform
  - **status: done** - `exec_command` Windows safety guidance follows the selected environment's reported executor platform OS when known (single environment), and falls back to host-derived guidance for unknown/multi environments. Covered by `TestUnifiedExecSpecWindowsGuidanceFollowsExecutorPlatformLikeRust`.
- `39507eea53` #41354 Reject NUL bytes in reviewed terminal input
  - **status: done** - `writeStdinApprovalForTurn` rejects terminal input containing a NUL byte before any approval request or write, with `TestWriteStdinApprovalForTurnRejectsNULByteLikeRust`.
- `5eea8d0dd3` #41328 Review terminal input against retained permissions
  - **status: no-op (rust-coupled via Guardian review flow)** - Rust builds/formats a Guardian approval action (new `unified_exec/stdin_approval.rs`, `format_guardian_action_pretty`) and evaluates retained permissions against it. The Go `writeStdinApprovalForTurn` (`appserver/runtime_router.go`) is a simplified session-approval gate (feature flag + fresh session command/approval) with no Guardian action formatting and no retained-permission evaluation.
- `bce96bcb43` #41159 Reject oversized reviewed terminal input
  - **status: no-op (not directly mappable to Go's simplified gate)** - Rust rejects a reviewed write_stdin action whose formatted action + reason exceeds 8,000 bytes (or is truncated) before approval/write. The Go write_stdin path (`writeStdinApprovalForTurn`) does not format a reviewed action, so there is no equivalent `reviewed.text.len()` bound.
- `0ae94fdd49` #41436 Respond to terminal queries from TTY subprocesses
  - **status: done** - the unified-exec TTY read path wraps the subprocess reader with `terminalQueryReader`, which answers device-status, window-size, cursor-position, and DEC private-mode queries while passing every other escape sequence/output through. split-across-chunk handling included. Covered by `TestTerminalQueryResponderRepliesToBlockingQueriesLikeRust`, `TestTerminalQueryResponderHandlesSplitChunksLikeRust`, and `TestTerminalQueryReaderWritesResponsesToStdinLikeRust`.
- `c2abf869d5` #41432 Run executor hooks for interrupted turns
- `c6bf330b42` #41435 Allow bundled browser cleanup hooks on subagent stop

### batch-guardian-sync29
- `0918cd2c08` #41422 Add shared Guardian transcript collection
- `4878401e8f` #41392 Add shared Guardian context primitives
- `60fc699560` #41385 Give Guardian classifications distinct turn identities
- `f98649cde9` #41309 Honor required reviews when reusing Guardian scores
- `31d338a1ea` #41322 Isolate required-model Guardian approval coverage
- `2d929eb7c3` #41221 Honor turn token budgets in Guardian review rollover
- `bae69125df` #41215 Roll over Guardian context before follow-up reviews
- `4f2a1d8666` #41152 Fail closed on unbounded Guardian parent compactions
- `89650c66f2` #41094 Require synchronous review for sensitive MCP actions
- `e8b938b02e` #41146 Use typed outcomes for Guardian V2 classification
- `e9a446d79d` #41100 Add Guardian V2 decision metrics
- `453a9bcc69` #41158 Reduce Guardian V2's default tool-call lag
- `5ed334a29f` #41151 Extract Guardian action rendering into its own module
- `df9f537a6e` #41150 Remove locking from trusted skill collection
- `694edc23b2` #41118 Propagate trusted root skills to delegated workers
- `6c59264b14` #41108 Increase Guardian V2 async test timeouts
- `8935ff19db` #41191 Stabilize Guardian WebSocket tests
- `dd453867fa` #41226 Move Guardian review session tests to a separate file
  - Most changes live in Rust `ext/guardian-v2` / `core/src/guardian` (review_session, async_scorer, transcript collection). The Go port has a Guardian circuit-breaker / review surface but not the V2 async-scorer; these are `rust-coupled`. The Go-relevant subset (review rollover token budgets, action rendering, required-model isolation) is tracked as the batch's Go-side slice.

### batch-misc-sync29
- `63d213884d` #41586 Add Vim search motions to the composer
  - Go TUI has Vim motions for `gg`/`G` (sync28). Composer search motions (`/`,`?`,`n`,`N`) in Rust `bottom_pane/textarea/vim_search.rs` need a Go-side vim_search port.
- `a847c71a15` #41143 Limit inline diff previews in the TUI
  - **status: done** - `CreateDiffPreview` bounds the inline patch preview to 12 content rows across all files (with an omission notice), while the transcript/`RawLines` and full summary retain the complete diff. Covered by `TestCreateDiffPreviewLimitsRowsLikeRust`.
- `7d6f808b97` #41285 Drive keymap conflict checks from the action registry
- `4fea523466` #41218 Share linked tool mention parsing in the TUI
- `5fc7840cf6` #41467 Refresh the TUI model picker from the app server
- `c29bc99949` #41192 Preserve restored permission profiles in TUI sessions
- `868c9edb0d` #41349 Assign stable IDs to generated Responses input items
- `94311d4475` #41292 Forward history note images to the model
  - **status: done (core)** - Go history/notes tool output converts backend `images` into `input_image` function-call content items alongside encrypted/plaintext content, keeps image bytes out of the logged body, and rejects malformed attachments. Covered by `TestHistoryNotesToolOutputConvertsImagesToContentItemsLikeRust`, `TestHistoryNotesToolOutputPlaintextWithImagesLikeRust`, and `TestHistoryNotesToolOutputRejectsMalformedImagesLikeRust`.
- `6be2a6ca95` #41260 Let the history backend enforce tool output budgets
  - **status: done** - Go history/notes tool executors return the backend result unchanged (dropped the client-side token-limit re-truncation), with `TestHistoryNotesToolOutputPreservesUnboundedResultLikeRust`.
- `f1bb4c168d` #41235 Sanitize history notes backend errors
  - **status: done** - Go history-notes backend errors now return a consistent `Unable to perform operation:` message that omits underlying error details, with `TestHistoryNotesBackendSanitizesErrorsLikeRust`.
- `170da98842` #41413 Optimize history item lookups
- `2008d27e98` #41427 Filter media from function call output notifications
  - **status: done (covered by #41416)** - the generic media filter strips image/audio from `function_call_output` thread item output content while preserving text and encrypted content, verified by the `TestWithoutNotificationMediaOmitItemMediaLikeRust` function-call-output assertion.
- `03147407e3` #41416 Add app-server notification media filtering (`omit_app_server_notification_media`)
  - **status: done** - when `omit_app_server_notification_media` is enabled, `RuntimeRouter.notify` strips inline image/audio content from `item/started`, `item/completed`, and `rawResponseItem/completed` payloads while the model request keeps the media. Covered by `TestWithoutNotificationMediaOmitItemMediaLikeRust`.
- `f4f85add41` #41360 Measure Codex home usage at app-server startup
- `41d3dc56a0` #41239 Surface model provider authentication recovery progress
  - **status: schema surface only** - `modelProvider/authRecoveryStarted`/`authRecoveryCompleted` notification methods, `AuthRecoveryNotification`, and schema mappings added; runtime emission from the model client's auth-recovery path is still pending.
- `dc031d4bc7` #41232 Expose the PowerShell version in environment context (`powershell_shell_version`)
  - **status: done (core)** - when the `powershell_shell_version` feature is enabled for a single local PowerShell environment, `turnEnvironmentContextTextAt` renders `<shell_version>major.minor</shell_version>` using a bounded, cached PowerShell query. World-state diff ("unavailable" restatement) not replicated (Go renders the full context).
- `1cc81ca89a` #41357 Support compression for shared rollout lineages (`local_thread_store_shared_compression`)
- `8faf7252f0` #41364 Test resuming compressed shared rollouts
- `f6726e8986` #41223 Add recency sorting to `project/list`
  - **status: done (core)** - `project/list` now accepts `sortKey` (`position`/`recencyAt`) and `sortDirection`; `Project` responses include `recencyAt` (newest non-archived assigned thread, Unix seconds, null when none). The state layer computes recency via a nullable `MAX(recency_at_ms)` subquery, orders by position or recency (recency places empty projects last in either direction), and encodes/parses new `v1|key|direction|value|id` cursors while retaining the legacy ascending-position cursor format. `state.Project.RecencyAtMS` is nullable; `CreateProject`/`UpdateProject`/`GetProject` carry/compute recency. Covered by `TestProjectListRecencySortLikeRust` and updated cursor/lifecycle tests. Verified: `go build ./...`; `go test ./state ./appserver ./parity ./features ./config ./turn ./exec ./tool ./agent -count=1` green; `go vet`/`gofmt` clean.
  - **deferred** - the Rust `0052_projects_recency.sql` index on `threads(project_id, recency_at_ms DESC)` is a performance optimization; the subquery is correct without it. It is deferred because `TestStateMigrationsMatchRustTarget` pins the Go migration inventory byte-for-byte to the certified `candidateRustTo` (970b7f2ff4, sync26) baseline, which the plan intentionally does not advance. The recency sort works correctly without the index; the index can be added when the migration baseline is advanced.
  - **schema blobs** - verified 2026-08-30: the Rust `app-server-exports-stable.json.zst` at the sync29 freeze target `63d213884d` also does NOT contain `recencyAt`/`ProjectSortKey`/`sortKey` in the embedded TypeScript/JSON exports (the generated `Project.ts`/`ProjectSortKey.ts` files do, but the precomputed blob does not — an upstream Rust inconsistency). The Go precomputed blobs therefore match the Rust sync29 blob state and are **not** stale relative to it; no re-vendoring needed for parity with the sync29 target. The Go structs are ahead of the embedded blobs only in the sense that the Rust runtime structs are also ahead of the Rust blob.
- `e325e3acd9` #41219 Retry confirmed remote registration conflicts
  - **status: done** - remote exec-server registration retries only explicit `503 registration_conflict` responses with jittered exponential backoff during both initial registration and post-rejected-relay re-registration; all other failures fail fast. Covered by `TestRegistrationConflictRetryDelayBoundsLikeRust`, `TestRegisterRemoteEnvironmentWithRetry`, and `TestRegisterRemoteEnvironmentWithRetryDoesNotRetryAmbiguousFailure`.
- `a73bf25d17` #41313 Decouple HTTP retry backoff from overload integration testing
- `3ae4225b17` #41403 Restrict cloud task credentials to trusted origins
- `18b9e7fd9e` #41250 Include thread source in realtime connection metadata
  - **status: done** - `realtimeThreadHeaders` adds the saved `thread_source` to `x-codex-turn-metadata` as header-safe ASCII JSON, omitting absent, oversized (>256 bytes), or invalid values. Covered by `TestRealtimeThreadHeadersIncludeThreadSourceLikeRust` and `TestASCIICJSONHeaderStringEscapesNonASCIILikeRust`.
- `eec4a23cb1` #41447 Support `openai/elicitation` form requests
- `a5c581e247` #41448 Clarify question handling in Default collaboration mode
  - **status: done** - Default collaboration-mode instructions updated in `appserver` and `tui/chatwidget` to reserve `request_user_input` for optional quality-improving questions, continue on no answers, never use it for permission requests, and ask one concise plain-text question otherwise.
- `430d26b543` #41331 Classify clock tools as built-in control tools
  - **status: no-op (already aligned)** - Rust marks `clock.curr_time`/`clock.sleep` as `is_builtin_control_tool` so their calls emit control-tool analytics and are excluded from the dynamic-tool count. The Go port already excludes them from the dynamic-tool count (built-in clock tools are not marked `dynamicToolCall`, so `threadItemLooksLikeDynamic` returns false); there is no separate control-tool analytics category to wire, so no Go change is required for the dynamic-tool exclusion. Verified by `threadItemLooksLikeDynamic` checking only the `dynamicToolCall` marker.
- `7e41be641e` #41365 Restrict async user messages to questions
  - **status: done** - `send_user_message_async` built-in description and message-parameter guidance reserve the tool for questions, with `TestSendUserMessageAsyncHandlerRestrictsToQuestionsLikeRust`.
- `035295b46e` #41196 Improve sandboxing, MCP errors, and cached approvals
- `0182ff3480` #41195 Finalize model-specific tool plans in `ToolRouter`
- `e931d07b88` #41205 Track executor MCP discovery telemetry
- `48e22a5fa0` #41452 Report code mode host request durations
- `62b458c931` #41454 Block goals after repeated execution host failures
  - **status: done** - a thread's active goal is blocked after three consecutive turns of handler-executed code-mode `exec` failures without an intervening successful tool (per-thread `execFailureTurns` counter, reset on success/goal change/clear). Covered by `TestGoalBlockedAfterRepeatedExecHostFailuresLikeRust` (and the reset/replacement tests).

## Commit classification (b592a0bfed..63d213884d)

### Rust-coupled / no Go equivalent (inherited via Rust components)
- Guardian V2 async-scorer, transcript, context, review-session internals (`#41385 #41392 #41422 #41100 #41146 #41151 #41158 #41108 #41191 #41226`).
- `#41202` extension MCP tool-result processing, `#41456` executor plugin hook app targets, `#41435/#41432` executor cleanup hooks on subagent stop/interrupt, `#41219` remote registration conflict retry, `#41403` cloud-task credential origin restriction, `#41400` MCP HTTP header refresh, `#41396` remote plugin runtime refresh, `#41344/#41336` MCP tool cache binding capture, `#41117` plugin root freeze, `#41193/#41231` plugin cache instrumentation/affected-capability reporting.
- `#41477` asset re-org (Rust resource directory moves; Go reads lark grammar from the certified baseline, not the moved path), `#41476` rules_rs release-binary platform (Bazel), `#41449` Seatbelt policy rename (macOS), `#41226` test-file moves.

### No Go functional impact (Bazel / Rust-internal / test-only)
- `#41476` release-binaries rules_rs platform (Bazel).
- `#41194`, `#41189`, `#41226`, `#8935ff19db` (test-only reorganization/flakiness).
- `#a73bf25d17` decouple HTTP retry backoff from overload integration testing (test harness).

### Go-implementable runtime work (tracked in batches above)
- Config/model/turn/session/exec/mcp/plugin/tui/sandbox batches as grouped above. The most valuable and tractable first slices are `batch-config-fields-sync29`, `batch-model-catalog-sync29` (model-owned tool messages + multi-agent reasoning effort) and the `mcp_optional_startup_grace_ms` config field, since the L0 surface (feature keys, ModelInfo field, config count) is already re-frozen.

## Verification status
- L0 static layer re-frozen to `63d213884d`; `go build ./...`, `go test ./parity -count=1`, `go test ./features -count=1`, `go test ./model -count=1`, `go test ./config -count=1`, `go test ./appserver -count=1` all green.
- sync29 runtime slice landed (`batch-config-fields-sync29`): #41210 + #41243 clock/sleep gating via `decideClockToolEnablement` (+ `config.SleepToolMode`), with `TestDecideClockToolEnablementLikeRust` and `TestSleepToolModeConfig`.
- sync29 runtime slice landed (`batch-model-catalog-sync29`): #41461 async user-message description sourced from the model catalog (`ModelMessages.Tools` + `send_user_message_async` description override), with `TestModelMessagesToolMessagesRoundTrip`, `TestSendUserMessageAsyncHandlerDescriptionOverride`, and `TestBuildToolRegistrySourcesAsyncMessageDescriptionFromCatalog`.
- sync29 runtime slice landed (`batch-model-catalog-sync29`): #41206 model-aware Ultra reasoning fallback (`reasoningEffortForRequest`), with `TestReasoningEffortForRequestModelAwareUltraFallback`.
- sync29 runtime slice landed (`batch-model-catalog-sync29`): #41457 proactive multi-agent instructions from the catalog (`MultiAgentModeMessages.Proactive` + `multiAgentModeCatalogText`) and #41570 grammar fix, with `TestMultiAgentModeCatalogText` and `TestModelMessagesMultiAgentProactiveRoundTrip`.
- sync29 runtime slice landed (`batch-model-catalog-sync29`): #41087 response usage metadata in completion events (`model.ResponseUsageMetadata` + `rawResponse/completed` `usageMetadata`), with `TestUsageMetadataFromStreamEventData` and `TestRawResponseCompletedNotificationIncludesUsageMetadata`.
- sync29 runtime slice landed (`batch-config-fields-sync29`): #41199 configurable optional MCP startup grace (`mcp_optional_startup_grace_ms`), with `TestMCPOptionalStartupGrace` and `TestListStatusCheckedDisabledGraceWaitsForOptionalStartup`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41235 sanitized history-notes backend errors (`Unable to perform operation:` prefix, no underlying error details), with `TestHistoryNotesBackendSanitizesErrorsLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41365 async user messages restricted to questions (built-in description + message-parameter guidance), with `TestSendUserMessageAsyncHandlerRestrictsToQuestionsLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41232 PowerShell version in environment context (`powershell_shell_version` feature + bounded/cached version query), with `TestParsePowerShellVersionLikeRust` and `TestTurnEnvironmentContextTextAtRendersPowerShellVersion`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41448 clarified Default collaboration-mode question handling (`request_user_input` only for optional quality-improving questions; no permission requests; one plain-text question otherwise), in `appserver` + `tui/chatwidget`, with `TestCollaborationModeDefaultQuestionGuidanceLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41250 realtime connection metadata thread source (`x-codex-turn-metadata` ASCII JSON, 256-byte cap), with `TestRealtimeThreadHeadersIncludeThreadSourceLikeRust` and `TestASCIICJSONHeaderStringEscapesNonASCIILikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41260 history backend enforces tool output budgets (removed client-side re-truncation), with `TestHistoryNotesToolOutputPreservesUnboundedResultLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41292 forward history note images to the model (`input_image` content items; image bytes kept out of the logged body; malformed attachments rejected), with the three `TestHistoryNotesToolOutput*ImagesLikeRust` tests.
- sync29 runtime slice landed (`batch-exec-sandbox-sync29`): #41354 reject NUL bytes in reviewed terminal input (write_stdin approval), with `TestWriteStdinApprovalForTurnRejectsNULByteLikeRust`.
- sync29 runtime slice landed (`batch-turn-session-sync29`): #41207 propagate executor OS into turn environments (`platformOs` on exec-server metadata), with `TestEnvironmentInfoReportsLocalPlatformOSLikeRust`.
- sync29 runtime slice landed (`batch-exec-sandbox-sync29`): #41368 match Windows shell guidance to the executor platform (`exec_command` guidance follows `platformOs`, host-derived fallback), with `TestUnifiedExecSpecWindowsGuidanceFollowsExecutorPlatformLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41416 app-server notification media filtering (`omit_app_server_notification_media`), with `TestWithoutNotificationMediaOmitItemMediaLikeRust`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41427 filter media from function call output notifications (covered by the #41416 generic filter), with the extended `TestWithoutNotificationMediaOmitItemMediaLikeRust` function-call-output assertion.
- sync29 runtime slice landed (`batch-exec-sandbox-sync29`): #41436 respond to terminal queries from TTY subprocesses (`terminalQueryReader` on the unified-exec TTY read path), with the three `TestTerminalQuery*LikeRust` tests.
- sync29 runtime slice landed (`batch-exec-sandbox-sync29`): #41384 configurable thread shell-command timeouts (`ShellCommandParams.TimeoutMs`), with `TestShellCommandParamsTimeoutValidationLikeRust`.
- sync29 runtime slice landed (`batch-mcp-plugin-sync29`): #41117 freeze plugin roots in MCP tool attribution (`PluginAttribution.HostRoot` carries the host-discovered root), with `TestPluginAttributionFromRegistration` and `TestRuntimeConfigFreezesPluginHostRootLikeRust`.
- sync29 runtime slice landed (`batch-mcp-plugin-sync29`): #41230 apply app routing policy to unauthenticated plugin reads (`handlePluginRead` applies `ApplyAppMcpRoutingPolicy` with empty auth mode), with the extended `TestAppMcpRoutingPolicy`.
- sync29 runtime slice landed (`batch-misc-sync29`): #41143 limit inline diff previews in the TUI (12-row bound + omission notice), with `TestCreateDiffPreviewLimitsRowsLikeRust`.
- App-server schema/static fixes surfaced by the full `./appserver` run: precomputed export SHA hashes re-pinned to the sync29 blobs, `AuthRecoveryNotification` surface added (#41239), and the sync28 skill-catalog aliases wording reconciled in the image-generation test.
- Remaining: dynamic L1-L3 differential (record-replay, SDK) for the runtime batches above, driven as each batch lands, as in sync28.
- L3 SDK differential remains gated on explicit model-cost authorization (unchanged from sync28; did not re-audit this pass).

## Status summary
Static re-freeze landed for the 100-commit sync29 range. The runtime batch roadmap is the work queue; each batch still needs its own differential verifier before `parity.json` certification can advance. The next actionable Go-side slice (config/model-catalog) is the highest-value follow-up.

---

## sync29 post-landing addendum (2026-08-30)

- Upstream pull after the sync29 plan advanced 63d213884d..dde85b435b (one commit, #41613 "Move Vim history tests into the history search module"). It is a Rust test-only reorganization of the Vim composer history-navigation tests and requires **no Go-side change**; it also does not disturb the L0 static freeze (critical-file hashes, TUI snapshot manifest, workspace members all still match 63d213884d).
- Fixed a shared-fixture verifier regression introduced when the sync29 runtime slice #41448 (clarify Default collaboration-mode question handling) advanced Go's default collaboration-mode instructions to the new Rust text but left TestRustCollaborationModeTemplatesMatchGo reading the certified baseline 970b7f2ff4 (sync26) default template. Re-pinned the collaboration-mode-templates verifier to the sync29 L0 freeze target 63d213884d; plan.md, collaboration_mode_presets.rs, and config_types.rs are unchanged at that commit. go test ./parity -count=1 is green again.
---

## sync29 #41454 port (2026-08-30)

Ported #41454 "Block goals after repeated execution host failures" end-to-end:

- **Dispatcher**: added 	urn.ToolExecutionResult.HandlerExecuted, populated in executeToolInvocation from the pre-executor onStarted callback (Rust ToolCallOutcome mapping): a handler-executed error sets 	rue; a pre-tool-hook block or a completed success/non-success output leaves it alse. Nested code-mode tool completions set it from 
estedErr != nil.
- **Goal accounting**: stateGoalTurnSnapshot gained SuccessfulTool/FailedExecution; a per-thread execFailureTurns counter (goalExecutionFailureState) tracks the consecutive streak. ecordGoalToolOutcome (called from ccountGoalToolProgressForCompletion) marks the active goal's turn on a successful tool (reset) or a default-namespace exec handler-executed failure. inishStateThreadGoalTurn calls dvanceGoalExecutionFailure after charging the turn and blocks the goal (ThreadGoalBlocked) at a streak of 3; esetGoalExecutionFailure clears the per-thread streak when a goal is cleared/replaced.
- **Tests**: ppserver/goal_execution_failure_test.go (block after 3 failures, no block on success/blocked, no carry-over to a replacement goal) and 	urn/tool_dispatcher_test.go TestToolExecutionResultHandlerExecuted* (handler failure => true, blocked pre-hook / completed output => false).

Verified: go build ./..., go test ./turn ./appserver ./parity ./features ./model ./config -count=1 green; gofmt/go vet clean.
---

## sync29 #41204 port (2026-08-30)

Ported #41204 "Propagate executor home directories into sandbox contexts" end-to-end, following the landed #41207 (platformOs) pattern:

- **Protocol**: execserver.EnvironmentInfo and ppserver.EnvironmentInfoResponse gained UserHomeDir (userHomeDir,omitempty), populated from os.UserHomeDir() for local executors and from the remote exec-server response otherwise.
- **Turn environment**: 	ool.UnifiedExecEnvironment gained UserHomeDir; unifiedExecEnvironmentsForTurn carries the EnvironmentInfoResponse.UserHomeDir onto the selected environment.
- **Sandbox context**: execserver.FileSystemSandboxContext and 	ool.FileSystemSandboxContextOptions gained UserHomeDir; unifiedExecSandboxContext wires ShellRequest.UnifiedExecUserHomeDir (set from environment.UserHomeDir in the shell executor) into the context, URI-normalized like CWD/WorkspaceRoots. Executors that do not report a home preserve an empty field.

Tests: 	ool/unified_exec_test.go TestFileSystemSandboxContextPropagatesUserHomeDirLikeRust (home propagated as a path URI; absent home stays empty), execserver/server_test.go local environment home assertion, ppserver/environment_config_test.go local home report assertion.

Verified: go build ./..., go test ./execserver ./tool ./appserver ./parity -count=1 green; gofmt/go vet clean.
---

## sync29 #41219 port (2026-08-30)

Ported #41219 "Retry confirmed remote registration conflicts" into execserver/remote.go:

- Added egisterRemoteEnvironmentWithRetry, used for both the initial registration and the post-rejected-relay re-registration. It retries only an explicit 503 registration_conflict response (a emoteRegistryHTTPError with that status/code) using jittered exponential backoff, matching Rust; all other failures fail fast because a timed-out request may still have replaced a newer registration.
- Added egistrationConflictRetryDelay (Rust egistry_recovery_retry_delay): base 500ms doubling per attempt capped at 5s, plus a deterministic FNV-1a jitter of up to half the base so competing executors do not retry in lockstep. stableRegistryHash seeds the jitter from the retry key and attempt.

Tests: execserver/remote_retry_test.go — delay-bounds (mirrors the Rust case table), retry on explicit conflict succeeds after backoff, and an ambiguous failure does not retry.

Verified: go build ./..., go test ./execserver ./parity ./features ./model ./config -count=1 green; gofmt/go vet clean.
---

## sync29 #41183 port (2026-08-30)

Ported #41183 "Account subagent token usage toward root goals" onto the goal-accounting infrastructure from #41454:

- ecordDescendantGoalTokenUsage(threadID, usage) accumulates the descendant token delta per thread; descendantTokenDelta returns the pending delta since the last accounted baseline.
- ecordGoalTokenUsageWithDescendants (wired into the turn OnTokenUsage callback) records the turn's own usage and, when the thread is a spawned descendant, routes the usage to the root ancestor thread's goal via ootGoalThreadID (walks ParentThreadID).
- ccountStateThreadGoalProgress/ccountIdleGoalProgress include the descendant token delta in the token delta passed to AccountThreadGoalUsage, so subagent spend counts against the root goal budget.
- The descendant accounting baseline resets when a goal is anchored (markStateThreadGoalTurnActiveNow) or replaced (ensureStateThreadGoalTurnActive), so descendant usage does not carry to a replacement goal.

Tests: ppserver/goal_descendant_test.go (roll-up, baseline reset on goal change, root-thread routing). Verified go build ./..., go test ./appserver ./parity ./features ./model ./config -count=1 green; gofmt/go vet clean.
---

## sync29 #41393 port (2026-08-30)

Ported #41393 "Preserve one-shot exec when unified exec is disabled":

- Added ShellExecutorOptions.OneShot and ShellExecutor.oneShot. A one-shot exec_command runs to completion via the local shell runner (no PTY/session/write_stdin) and exposes a 	imeout_ms argument; its spec removes 	ty/yield_time_ms and uses the "cannot be resumed" description (Rust one_shot_exec_command_spec).
- A one-shot command terminated on timeout or turn cancellation reports exit code 124 (Rust exec_command_to_completion + 124).
- BuildToolRegistry now registers a one-shot exec_command when EnableUnifiedExec is false but legacy shell command is supported, instead of dropping it.

Tests: TestOneShotExecSpecRemovesSessionArgsLikeRust, TestOneShotExecReportsExit124OnTimeoutLikeRust, and TestBuildToolRegistryHonorsToolDisableOptions updated to expect a one-shot exec_command while unified exec is disabled. Verified go build ./..., go test ./tool ./turn ./appserver ./parity ./features ./model ./config -count=1 green; gofmt/go vet clean.
---

## sync29 #41421 port (2026-08-30)

Ported #41421 "Support per-tool MCP output limits":

- mcp.ToolConfig gained OutputTokenLimit (output_token_limit), parsed from the runtime config map (untimeConfigPositiveIntAny); RestrictOutputTokenLimit applies the most-restrictive (smallest) explicit budget without changing the approval mode (Rust McpServerToolConfig::restrict_output_token_limit).
- MCPService.ConfiguredToolOutputLimit(server, tool) resolves the effective per-tool limit from the resolved server config (Rust PreparedMcpCall::output_token_limit).
- ToolExecutor.Execute resolves the effective limit and truncates an over-budget MCP tool response text before it reaches the model, keeping non-text content items.

Tests: TestToolConfigOutputTokenLimitParseLikeRust, TestToolConfigRestrictOutputTokenLimitLikeRust, TestTruncateMCPResponseToOutputTokenLimitLikeRust. Verified go build ./..., go test ./mcp ./appserver ./exec ./turn ./parity ./features ./model ./config -count=1 green; gofmt/go vet clean. The config-schema surface test is unaffected because output_token_limit is a nested MCP tool-config property, not a top-level key.

---

## sync29 #41567 port (2026-08-30, continued)

Verified the in-flight #41567 "Restore thread cwd from owned settings snapshots"
port and landed it:

- Fixed a placement bug in `rollout/rollout_test.go` where
  `TestLastPersistedOwnedThreadCWDLikeRust` had been inserted inside the body of
  `TestRecorderCreateAppendLoad` (a Go syntax error). Extracted it into its own
  top-level test.
- `go build ./...` clean; `go test ./rollout ./appserver ./parity ./features ./model ./config -count=1` all green; `go vet ./rollout ./appserver` clean; `gofmt` clean.

## sync29 upstream advance to 28327355b8 (#41630, 2026-08-30)

`git pull origin main` advanced the Rust checkout `dde85b435b..28327355b8` (one
commit: `28327355b8` "Update tests for default-enabled update_plan (#41630)").
It is a Rust **test-only** change that:

- Covers the default, explicitly enabled, and explicitly disabled states of
  `tools.update_plan.enabled`.
- Verifies prompt tool lists remain consistent across requests.
- Explicitly enables `update_plan` in fixtures that depend on the tool.

It touches only `codex-rs/.../tests/*.rs` and `config_tests.rs`/`session/tests.rs`/
`spec_plan_tests.rs`; it adds no new test files and does not change
`core/tests/suite/mod.rs`, so the L0 static freeze target (`63d213884d`) is
**not** disturbed. The Go model already matches the default-enabled behavior:
`config.Config.UpdatePlanEnabled()` returns `true` when the key is absent, and
`TestToolEnablementUsesRustDefaultsAndNestedConfig` covers the
default/enabled/disabled states. **No Go-side change required** for #41630.
